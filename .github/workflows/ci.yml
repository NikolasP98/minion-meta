# .github/workflows/ci.yml
# Meta-repo CI — gates every PR against dev (the integration branch) and main.
# Runs lint-all, typecheck-all, build-all, test-all, changeset:status, the
# specs/ hardening gate's own fixture self-test (scripts/*.test.mjs), and the
# specs/ hardening gate itself (scripts/spec-index.mjs --check).
name: CI

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev, main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4.4.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm run build-all

      - name: Typecheck
        run: pnpm run typecheck-all

      - name: Lint
        run: pnpm run lint-all

      - name: Test
        run: pnpm run test-all

      - name: Spec index gate self-test
        run: pnpm run test:scripts

      - name: Spec index check
        run: node scripts/spec-index.mjs --check
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}

      - name: Changesets status (PR only)
        if: github.event_name == 'pull_request'
        run: pnpm run changeset:status
