name: Claude Code

# Only events whose workflow run is attached to the PR head branch are
# subscribed here. `issue_comment` and `issues` runs are pinned by GitHub to the
# default branch (`main`) and its HEAD SHA, so every monitor-intake bot comment
# filtered out by the `if:` below still left a `skipped` run on the deploy
# branch — 51 of the 53 check runs on 5ffdfec, and 98 of the newest 100 runs
# returned by `GET /actions/runs?branch=main`, burying the real CI/Release
# verdict deep enough for deploy-status probes to read the branch as not-green.
# The interactive path lost nothing: across 563 runs of this workflow not one
# ever executed (542 skipped, 21 unapproved `action_required`).
# See proposals/2026-08-28-base-deploy-status-skipped-run-noise.md.
on:
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # This is an optional setting that allows Claude to read CI results on PRs
          additional_permissions: |
            actions: read

          # Optional: Give a custom prompt to Claude. If this is not specified, Claude will perform the instructions specified in the comment that tagged it.
          # prompt: 'Update the pull request description to include a summary of changes.'

          # Optional: Add claude_args to customize behavior and configuration
          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://code.claude.com/docs/en/cli-reference for available options
          # claude_args: '--allowed-tools Bash(gh pr *)'

