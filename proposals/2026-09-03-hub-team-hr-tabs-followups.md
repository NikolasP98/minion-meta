# /team HR tabs (S2–S4) — deferred follow-ups

- **Status**: resolved (branch `feat/hr-followups`, hub commit `6876bcd0`, PR pending)
- **Created**: 2026-09-03
- **Repos**: minion_hub
- **Origin**: spec `2026-09-02-hub-team-hr-module-spec` slices S2–S4 (branch `feat/hr-module-s2`)

## Open ends

1. ✅ `6876bcd0` — `setWeeklyOff` (hr.service.ts) reconciles both ways: inserts the checked weekdays, deletes `weekly_off=true` rows in the range whose weekday is unchecked (`staleWeeklyOffDates`, unit-tested in `leave-rules.test.ts`); named holidays untouched. Browser-verified Sat+Sun → 104 rows, uncheck Sat → 52, clear → 0. ~~**Weekly-off chooser is add-only.** `materializeWeeklyOff` (hr.service.ts) only inserts rows; unchecking a weekday in `HolidaysTab.svelte` and applying does not delete the rows already materialised for it — the user must delete them one by one. Fix: make the POST `{weeklyOff, from, to}` reconcile (delete `weekly_off=true` rows whose weekday is no longer chosen inside the range). Site: `src/lib/components/team/HolidaysTab.svelte` `applyWeeklyOff` (`TODO(handoff)`).
2. ✅ `6876bcd0` — removal now rides on `scheduling:edit` via `PATCH /api/scheduling/hr/holidays/[id] {deleted:true}` (the old DELETE handler delegates to the same service call); `canDelete` gating dropped from `HolidaysTab`. Same pattern for rooms (`PATCH /api/scheduling/resources/[id] {deleted:true}`). ~~**Holiday deletion needs `scheduling:delete`.**~~ The central write hook maps DELETE under `/api/scheduling/*` to `scheduling:delete`; FACES' owner account only holds `scheduling:edit`, so the delete button is hidden for them (`canDelete` prop). Decide whether HR deletes should ride on `scheduling:edit` (hrms treats holiday-list edits as one permission) or whether the role matrix should grant `scheduling:delete` to owners.
3. ✅ `6876bcd0` — the tab is always listed; without `users.manage` it renders an `EmptyState` explaining the users-manage capability (en/es) instead of vanishing. Gate unchanged; the O3 `hr` module remains the right end state. ~~**Members & access tab hidden for the FACES owner.**~~ `/team` is now `permission:scheduling:view`; the Members tab (TeamTab + SharedAccountsPanel) stays behind `users.manage`, which the FACES owner account does not hold (it held no `/team` access at all before this change either). Spec O3 proposal (`hr` module with `hr:view/edit/approve`) is still the right end state.
4. ✅ `6876bcd0` — loader exposes `myEmployeeId` (`hr_employees.profile_id` = session profile); `TimeOffTab` hides Approve/Reject on the viewer's own pending request and shows the caption `team_err_self_approval`; the API 409 stays as fallback (both browser-verified with a temporary employee bound to the QA profile, then removed). ~~**Self-approval not browser-verified.**~~ The QA account is not an org *person* member, so it cannot be enrolled and the `self_approval` 409 path was only covered by the S1 unit test; the UI mapping was exercised through the `overlap` 409 instead (same code path, `hr-error.ts`).
5. ℹ️ No change (note only) — **Balances panel recomputes client-side** (`TimeOffTab.svelte` `balances`) instead of calling `getLeaveBalance`; it mirrors `leaveBalance()` but will drift if the rule changes. Replace with a loader-computed map when the leave ledger (spec §1.1 "next") lands.
6. ✅ `6876bcd0` — fifth `/team` tab "Rooms & equipment" (`?tab=resources`, `ResourcesTab.svelte`): list with kind badge + inactive badge, `+Add` dialog (name, kind room|equipment, color), active toggle, delete, and the shared `AvailabilityEditor` per resource. `/api/scheduling/resources` POST gained `kind`; both resource handlers dropped the pre-RBAC `requireAdmin` so the central hook (`scheduling:edit`) is the gate, as for `/api/scheduling/hr/*`. ~~**Rooms & equipment** (non-staff `sched_resources`) lost their UI~~ when `/scheduling/resources` became a redirect; spec §3 leaves them "in scheduling". They still work through the API and the booking engine; a small list needs a home (scheduling settings?).

## People view fold (2026-09-03, branch `feat/team-people-view`)

Roster · Availability · Members & access folded into one master-detail `PeopleView.svelte` (`?tab=people`, `?person=<employeeId|member:<profileId>>`; ≥1024px = grid column, below = `Sheet`). Open ends:

7. **Raw `fetch` in the extracted access/invite components.** `src/lib/components/users/MemberAccessControls.svelte` (member-role POST/DELETE) and `JoinLinkForm.svelte` (join-link POST) were moved verbatim out of `TeamTab.svelte`, keeping their bare `fetch` + hardcoded EN strings (`'Create a shareable join link'`, `'Generate link'`, `'Copy'`, toast copy). Migrate both to `jsonMutation` (+ add them to `mutation-integrity.test.ts`) and paraglide keys. `TODO(handoff)` at the top of each file.
8. **Access edits on `/team` invalidate `settings:team`, not `team:data`.** `MemberAccessControls` keeps TeamTab's `invalidate('settings:team')`; on `/team` the change is carried by `PeopleView`'s optimistic `roleOverrides` map until the next full load. Harmless today; if the loader ever derives something from member roles, thread an `invalidateKey` prop through.
9. **`UserEditor` still owns the platform-role `<Select>`.** It is a dirty-tracked form field, so it was not folded into `MemberAccessControls` (which shows the platform role read-only). Editing the platform role from `/team` needs the settings/team editor.
10. **SSR renders the mobile layout first.** `new MediaQuery('(min-width: 1024px)')` falls back to `false` on the server; desktop hydration swaps the `{#if desktop.current}` block (Svelte repairs if-block mismatches). Pass a viewport hint from the layout if the flash is noticeable.

## Calendar polish (2026-09-04, branch `feat/team-calendar-polish`)

`/team` gained a secondary side menu (`TeamNav` + `/team/+layout.svelte` SectionShell, `?tab=people|timeoff|resources|settings`), a roster **timeline** column (one shared day-header scroller, rows mirror its offset, ±45-day window that grows by 30 days per edge), a Notion-style Time off calendar on `@event-calendar/core` (month / week / agenda), holidays imported per country from Nager.Date with per-row enable / move-observed-date, weekly off as ONE recurring rule in `hr_settings`, and hrms Employee `department` + `employment_type`. Open ends:

11. **Timeline leave/holiday marks are current-year only.** `timeline.svelte.ts` fetches bookings for every extended range, but leave bars and holiday shading come from the page loader's year-bounded `requests`/`holidays`; scrolling into another year shows bookings without leave. Fetch `/api/scheduling/hr/leave-requests?from&to` and `/holidays?from&to` per extension the same way. `TODO(handoff)` in `Timeline.#extend`.
12. **Holiday import skips dates already held by a manual holiday** (`onConflictDoNothing` on the `(org, date)` unique index) and reports them only through the `imported`/`total` counts. Surface the skipped names in the notice, or merge (mark the manual row `source='country'`). `TODO(handoff)` in `importCountryHolidays`.
13. **Nager.Date is keyless and uncached.** Each import fetches live; a 5xx surfaces as `import_failed` (409). If the clinic imports often, cache the country-year JSON (KV or a table). No SLA — see the service comment.
14. **Leave types: add only.** `TeamSettingsView` lists and adds leave types; rename / deactivate / `max_days` edits need the settings UI (the API `POST` upserts by `code`, so an edit form is one modal away). `TODO(handoff)` in `TeamSettingsView.svelte`.
15. **Country list is a 10-entry curated constant** (`COUNTRIES` in `TeamSettingsView.svelte`). Nager's `AvailableCountries` endpoint can feed a full `Select` when a non-LatAm org appears.
16. **`hr_holidays.weekly_off` is now legacy** (always `false`; the rule lives in `hr_settings.value.weeklyOff`). Drop the column once nothing reads it (`pg-hr-schema.ts` comment).
17. **Dependency bumps:** `svelte` 5.56.4 → 5.57.0 (required by `@event-calendar/core@5.12.2`) and `svelte-check` 4.7.2 → 4.7.6 (5.57's new `SnippetReturn` typing broke the older checker with 32 "two different types with this name" errors in untouched files). Watch for any other package pinning `svelte <5.57`.
18. **Allocation removal rides on `scheduling:edit`** via `PATCH /api/scheduling/hr/leave-allocations/[id] {deleted:true}` (same pattern as holidays, item 2).
