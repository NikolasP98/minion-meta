# Minion 360 resumption review — 2026-09-11

The user asked Codex to resume after Claude continued the operation. This review reconciles the continuation ledger against current GitHub status, public npm metadata, preserved private candidates and staged worktrees. It is a status review, not a fresh production health certificate or a full test rerun.

## Current verified position

- The exact allowlist contains **74 plans**, all present, covering the existing49 requirements and12 phases. No phase or requirement is globally complete.
- Meta PRs374–378 are merged. Shared0.12.0 and shells-bridge0.2.0 are **not published**: public npm metadata currently lists latest0.11.0 and0.1.6 respectively and contains neither requested version. Release run34440681982 remains failed. Its log shows npm PUT E404 for both packages. The ledger attributes this to an invalid/expired token; these log lines establish publication failure, not the exact credential cause. Recover registry publish access without exposing credentials, then revalidate license and immutable artifact gates before retrying.
- Gateway PR277 is merged, and production deploy run34474818000 succeeded, including the production compatibility gate and Netcup deploy job. Claude's last host receipt records main0aafeec7, both services1/1 and healthy. Current host digest/health was not re-read in this status pass.
- Hub PR245 is merged; Claude recorded production Vercel readiness for1df0a921. Current authenticated dialog smoke is still absent; do not treat login200 as dialog acceptance.
- Gateway DEV includes merged PR278/279. Latest CI run34481018447 passes Linux and Windows jobs but fails macOS: three assertions in two files. Two failures are in `scan-paths.symlink.test.ts`; the third is `canvas-host/server.test.ts` expecting `openclawA2UI` from a `minionA2UI` fixture. The ledger's grouping of all three as symlink-path failures was inaccurate.
- Hub PR246 remains open: six checks pass and Vercel is pending. Its review contains unresolved telemetry-sanitization/capture and finance-parser limit findings. Do not substitute an owner-review override for resolving those findings.

## Private durable candidate identity

Root rehashed all six14-12 receiver files and all13 owned14-17 files against their saved verification/freeze receipts: all match. Inherited receiver/manager/endpoint/facade source in14-17 is identical to the accepted14-12 snapshot. Saved root test logs retain69/69 receiver and127/127 aggregate results; no tests were rerun now.14-17 still has five documented unowned type diagnostics, unexecuted startup ordering and an effective operator.read authorization intersection. Sender/receiver end-to-end acceptance remains open.

## Preserved staged delivery work

All14 staged worktrees exist, with expected branches and staged changes only:148 path occurrences, zero unstaged/untracked/conflicts. Historical1Password signing failures are recorded; current vault state was not tested. Current GitHub open-PR lists do not contain these prepared branch groups.

The bulk `~/.cache/claude-tmp/flush-staged.sh` must not be run unchanged. Its frozen SHA-256 is `6fea4570998bbe87d1acd1fb8d1d905b092b5e2a329d59817c5053629c3e202b`. Confirmed defects:

1. Push failure is hidden by a `tail` pipeline without `pipefail`; a failed PR lookup can be treated as no existing PR.
2. Fallback body paths contain branch slashes but their parent directory does not exist. Hub/Site dependency branches collide on the same fallback body filename. The explicitly named sender PR body is missing.
3. The Site target is wrong: script uses `NikolasP98/minion_site`; both actual remotes use `NikolasP98/minion-site`.
4. No staged-path/hash/branch or matching existing-PR identity check protects execution.
5. `hub-10-01` is entirely duplicated in `hub-10x`: all four staged Git blobs, including migration090100, are identical. Preserve both worktrees, select one delivery path.
6. `hub-10x` and `hub-12-02` have different staged `vitest.config.ts` edits. Reconcile dependency test discovery while preserving native-SQL and credential-loading-suite exclusions.

## PR246 source review

The preserved PR checkout `~/.cache/claude-tmp/hub-16-01-15-02` is clean at `99715f50109fe5785009d23544166e3d79bc0ce2`. Source inspection confirms four raw PostHog callers bypass the wrapper, an ignored oversized CSV header, blank rows counted before filtering, and an unobserved asynchronous `flush()` rejection. JWT/sk-shaped correlation headers are dropped by the final filter, so that specific review claim overstates the end-to-end leak; other arbitrary opaque credentials can still pass the heuristic.

Bounded next fixes: parser source plus existing test; observability context/source tests and PostHog boundary with mocked SDK capture/flush tests; four explicit producer adoptions for layout, server creation, marketplace installation and provisioning. Preserve useful allowlisted fields while closing raw-client access. These are defects within the candidate's claims, separate from deferred Sentry/gateway/agent producers and parser-version persistence. No fixes or new tests were performed during this review.

## Resumption order

1. Finish PR246 source triage and repair confirmed security/parser defects before merge consideration.
2. Freeze each staged manifest and repair delivery preparation; handle branches individually. Carry paired Hub security changes together; calendar timezone fix before mobile-calendar adoption.
3. Deliver the jobs chain once, preserving migration/preflight gates; reconcile the dependency test configuration explicitly.
4. Reconcile receiver/caller/sender evidence and close missing lifecycle/authority/end-to-end checks before activation. Registry publication is a dependency, not proof of complete agent execution.
5. Continue remaining portability, ACP, authenticated UI, observability, container/runtime, capacity/recovery and phase20 audit gates. No entire phase is inferred complete from merged slices.

## Evidence limits and preservation

No signing, push, merge, publish, credential rotation, production mutation or fixture tests were executed in this resumption review. Existing active checkouts remain dirty and behind their integration branches; do not copy a whole checkout over current upstream. Original Claude continuation entries are retained as historical evidence; this dated review supersedes their stale current-status statements.

Earlier automatic approval review rejected additional driver fault-injection work. That action remains excluded; no rerouting or repetition was attempted. The rejection's detailed rationale is not available in this handoff.
