# User-requested wrap-up and release

**Final status: Site production deployed and verified; remaining 360 implementation paused at the user’s request.**

The user requested finishing current work, committing progress and deploying, then pausing the remaining 360 operation. This release finishes the Site members UI. Hub, sender, finance and the other staged candidates remain separate from production adoption.

## Saved progress

Signed meta commits `3397b17c` and `67b95671` are pushed to `qc/360-checkpoint-20260911`. They preserve the 89-plan program, research, QC tools, exact Hub/Site UI packets, independently tested sender/finance source, portable Site CI evidence and the task completion inventory. Active unrelated source and staged worktrees remain preserved.

Sender 11-10 independently passes 102 tests. Finance 15-07 independently passes 102 unit/parser plus 41 PostgreSQL tests; its owned cluster was verified clean and stopped. These source candidates depend on staged foundations and were not deployed by this wrap-up.

Configured 1Password SSH signing succeeds with a private TMPDIR because the default /tmp quota is exhausted. Signing was not disabled. New Site production commit attribution uses the authenticated GitHub account NikolasP98. GitHub recognizes its author and committer; GitHub reports the SSH key as unknown, so this is a signed commit without a GitHub Verified badge.

## Bounded Site production candidate

[PR 31](https://github.com/NikolasP98/minion-site/pull/31) targets master from `feat/qc-ui-production-20260911`, commit `b3ad0102a2e9d153bd784f670a8ed0dc24444282`, based on master `6bcdd7ec772e167023d9ab4d8909de69d5f4f9a5`. The isolated checkout is `/home/nikolas/.cache/minion-qc/site-production-i01etdap/minion_site`. Its 46 reviewed paths contain 45 changed files. It preserves all 307 existing message values in each language, localized logout, production metadata, session-poll recovery and existing CI gates.

The development integration is separately saved in [PR 30](https://github.com/NikolasP98/minion-site/pull/30), commits `c2bb652b` and `ba0f11f4`. Its hosted build/browser run 34577937913 passed, but its preview was blocked because the factory commit author had no associated GitHub account. Production integration fixes the additional acknowledged-stream retirement and fresh-test bootstrap defects found during review. PR 30 must not be treated as byte-identical to this production candidate.

Existing [PR 29](https://github.com/NikolasP98/minion-site/pull/29) contains an unrelated 379-file development promotion with conflicts and open review findings. It was not merged. The active Site and Hub checkouts were not switched or overwritten.

The exact first-party shared archive `d01a528579e7b0877d05565b9d3ad2747ab45610d634948f51b6a25b4bf4f756` is accepted for this application delivery under its existing MIT metadata and verified 44-member source identity. This does not publish an npm package or close its shipped-license packaging follow-up. Published shared 0.11.0 lacks required callbacks; shared 0.12.0 remains unavailable.

## Qualification

Final production source passes 23 tooling controls, 87 ordinary tests, 23 real-service contract cases, and 81 browser cases (27 each in Chromium, Firefox and WebKit), with zero skips or retries. Full native Kit sync/Svelte check passes with zero errors or warnings. Formatting and both design gates pass. The data fixture refuses nonempty or linked output and installs its network guard before compiler imports.

Root independently rehashed all 46 frozen files, 17 logs/receipts and 146 built fixture artifacts. The source manifest is `ba4b86271b8cc0f729d5ee30ae5240c0dd0be10fbdf1b48538e1dbbf768eb3ec`; final local receipt is `0ac38015dc3eb0c14a228eb03fc8c48fd6731a57c92ad70bd5b35ca2530a00ff`; portable acceptance is `e3b08fa5388896a4ae7d2838aff5aa821d352bb60897dcff7f6d8f3b342319de`. Copies and hashes are in [site-production](checkpoint-2026-09-11/site-production/files.json).

Graph tests reproduce and repair zero-width ECharts initialization and ResizeObserver callback layout loops. Service tests reproduce and repair ACK followed by disconnect leaving a stale stream. Cold test execution reproduces and repairs missing generated message imports. Negative evidence remains preserved.

Hosted CI run `34579288615` passes both check-and-build and ui-fixtures. Root downloaded artifact `10191046833` and independently checked its 87 ordinary, 23 service and 81 browser results, zero skips/retries and matching source hashes. Independent final source review passes at `7dd52e53b4ad37f60bc26d68016b18594c289f21ad33da9768877bf40aeb831b`. Vercel preview `dpl_8KFqVGGWzL8AJKpg6et1nUJ1AXkc` is READY for exact b3ad0102. Production merge/deployment are still pending; append their exact receipts before the final handoff.

## Pause boundary

[Progress](PROGRESS-2026-09-11.md) measures completed planned task deliverables: 172/229 (75.1%), with 15 partial and 42 without complete receipts. It is not product readiness, effort completion or deployed coverage. No entire phase or any of the 51 requirements is closed. Security's 19/19 concerns bounded foundation tasks; deployment and live security acceptance remain open.

Authenticated production journeys, physical devices, Hub adoption, actual agent/receiver/sender integration, package publication, container qualification, load/recovery and full observability remain pending in their existing plans and proposals. No new implementation categories are opened during this wrap-up.

## Final review amendment

Commit `2359978b7a2399c85e7afe0858b1a514092a8b3c` fixes the automatic review findings within six existing paths. Qualification now enforces the fixed archive digest and all 44 installed members, including same-length corruption, missing/extra files and symlinks. Same-session old final events no longer reload history during a newer run, and new sends retire prior pending history. The two service regressions were reproduced before repair.

Amended qualification: 31 tooling controls, 87 ordinary and 25 service cases; full native Svelte check remains zero errors/warnings. Browser and hosted release evidence are recorded below when complete. Source freeze `61e6d6ff8f27b3ea7e421338d4fe0a1b7d1d7c915765f249df7a001ca466176a`; independent amendment review `a4fbcc9e6938ae6d1353b77176680c6b75ba1713d74d06a121b32cc4fadf7bcc`. Original receipts are retained as history; the final delivery includes this amendment.

## Production promotion

PR31 merged as `0ed4e1b16cef4ca9287314df5af58a8af17f537c`; its Git tree exactly equals tested head `2359978`. Hosted CI run `34580815730` passes both jobs. Final local results are 31 tooling, 87 ordinary, 25 gateway and 81 browser cases, zero skips/retries; full Svelte check is 0/0. Vercel production deployment `dpl_91CAPYnw4eFqjMp9cC3rtw8UWw3J` is READY for this exact merge and serves `www.minion-ai.org` and `minion-ai.org`.

Live browser smoke confirms the English desktop homepage renders at 1280px and anonymous members access redirects to the login page, which renders at 390px without horizontal overflow. Screenshots remain in the private release cache. This does not claim authenticated production acceptance. PR30 is closed as superseded; its branch is preserved. Remaining work is paused.
