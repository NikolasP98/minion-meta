# First implementation-wave 360 reassessment

**Current checkpoint (2026-09-09, resumed by Claude after Codex usage limit):** 73 admitted plans, 49 requirements, 12 phases; none globally complete. Private plugin component: 56 tests, full Hub check and native Chromium qualification pass, including both handshake orderings. Corpus foundation: 48 native PostgreSQL cases, 43 unit cases and combined Hub check pass. Required-durable canonical contract: 114 tests/types pass; the matching complete SDK archive is independently qualified; the 14-14 facade amendment (110 tests) and the 14-12 native durable receiver (69 native/loopback cases) pass root rerun in private candidates. 12-08 clean shared-package emission passes its 12-case matrix with a root-rerun byte-identical 44-member tarball; license text, immutable version and installed-consumer gates stay open. Active installed and deployed adoption remain open. Dated sections below preserve historical evidence; use this checkpoint and the exact-plan index for current status.

Date: 2026-09-09. This answers the original audit questions after the first source slices and independent review. It is an interim assessment, not the final phase20 or production certificate. The operation remains active.

| Question | Evidence-backed answer after this wave | Remaining action and owner |
|---|---|---|
| What improved? | Raw assistant SQL is disabled before DB access; personal actor selection requires current assignment/membership; flow reads are SQLite-enforced read-only and graph input uses bindings. Nonstream and streaming Drone boundaries were repaired. Native dialog consumers and tooltip formatting were repaired. | Root reconciles independent verification and exact candidate patches; release remains phase20. |
| What should be cut? | Unsafe arbitrary assistant SQL is intentionally unavailable. Regex-only SQL authorization is unsuitable. Proven dependency-removal candidates include DOMPurify stub types and unused d3 scale/shape declarations. Peer-only dependencies must not be removed by import search alone. | Phase12 performs separate peer-aware removal/build transactions; phase15 restores useful typed analytics. No library removal has occurred in this wave. |
| What should be kept or revamped? | Keep PostgreSQL, the append-only stock ledger, existing graph library, typed Drone host boundary, native Dialog and distinct editor workflows. Revamp Shells admission/correlation/durable outcomes, generic job lease ownership, server authority consistency and migration ownership. | Phases10/11/14/15; do not replace the platform wholesale. |
| Are docs outdated? | Yes: original audit found active storage/auth/package/branch claims that diverge from source. GSD itself had a preserved older phase15 and summary-count completion hazards. Exact-path dispatch and explicit phase-wide open status now protect this operation. | 18-01 corrected four active instruction documents and passes a five-test source inventory plus independent verification. 18-02 inventories678 current Markdown documents across two distinct local snapshots, with56 divergent same-ID bodies and0 index consistency issues;10 tooling tests plus independent probes pass. Semantic disposition and historical revision coverage remain open. |
| Is the UI fully mobile/desktop compatible? | No full qualification. The real secret/image dialog fixture passes native Chromium focus, Escape, saving, retry and390px image bounds. A queued-close race was reproduced and fixed in the shared foundation. Three actual builtin artifacts also pass native opaque-frame loading/render/error checks at390x844/1440x900 and real BFCache restoration. Earlier live Home/Calendar clipping remains separate work. | Phase13 Home/Calendar composition, seeded authenticated journeys, Firefox/WebKit, coarse pointer, reduced motion and nonvisual charts. Screenshots alone do not certify behavior. |
| Which libraries need updates or replacement? | Current-lock advisory and peer inventories identify framework, editor, XML and test-runner families. Deprecated Paraglide adapter, alpha table engine and hand-written ACP transport deserve bounded replacement studies/fixtures. Site postgres and Tiptap node-range remain required peers. | Phase12 patch by family; phase11 real ACP conformance; phase14 package/consumer interoperability. No blanket latest-version transaction. Registry versions/advisories must be rechecked at the actual patch. |
| Is AI governance stable, testable and replicable? | Drone now has stronger definition and cancellation semantics, live tests are explicitly gated, and196 unit tests pass after streaming repairs. This is not whole-agent-platform certification. Factory's live identity and weak effective flags are now observed, rather than inferred from source. | Streaming independent verification passed. Shells active admission now independently passes18 tests after a malformed JSON-null crash was repaired. A native SQLite run/outbox foundation independently passes21 contract cases and33 native cases on each available Node runtime; cancellation ordering and test-cache isolation findings were corrected. Receiver/sender wiring, minimum runtime, model/tool/schema/image evidence and one pinned real harness remain under phase11. Factory activation requires phase17 drills and live authority. |
| Do cancellation, retry and restart preserve execution truth? | Drone caller deadlines and the selected job lease/revision boundaries have independent evidence. Shared full-manifest receipts now pass 27 unit and 25 native tests; finance passes 47 unit/parser and 19 native tests; brain passes 43 unit and 30 native tests. Provider admission without a durable response remains indeterminate and cannot automatically repeat. | Corpus multi-head batching, worker claim fencing/receipts, durable Shells outcomes, operator recovery and released migration qualification remain open. A cancelled local wait does not undo an already sent remote effect. |
| Are parsing and data pipelines well implemented? | Brain duplicate-source and empty-result races were reproduced and repaired using complete source/provider manifests. Corpus processing batches across documents; per-document grouping would multiply requests. The vector worker has a separate in-memory result/retry path and different input normalization. | 10-10 source foundation is active with bounded logical payloads and SQL ownership review before native qualification. Preserve current Qdrant mode, qualify the complete worker migration chain, and separately fix worker receipts/claim generation. Phase 15 still owns pre-materialization bounds, repeated parsing and schema authority. |
| Does the DB enforce rules under concurrency? | Real SQLite negative tests and20 local PostgreSQL assignment cases strengthen selected boundaries. Local stock uniqueness/replay and draft locks are implemented; independent review found alternate UUID input drift, now corrected with66 independently passing tests. Live metadata still lacks the new constraint. PGlite does not establish separate-connection races; a disposable native PostgreSQL17.10 runtime now proves distinct connections, with22 native cases included in the independently passing44-case final batch. Backend termination also reproduced an actual postgres3.4.9 asynchronous driver crash, separately admitted as12-04. | Phase10 real PostgreSQL claim/takeover/cancel/stock races and additive migration preflight; phase15 schema authority. No production migration executed. |
| Is the SDK/HTTP/WS/ACP connection natural and safe? | Shared protocols and existing plugin/Workforce libraries should be reused. Local history exposes missing consumer error hooks, iframe identity/capability gaps and an unbounded response read even in the reference HTTP correction. Local, reference, installed, published and deployed bytes are separate identities. | Plugin bridge source independently passes 84 tests after the restored-lifetime request-ID repair. Workforce package passes 105 tests, and real Hub helper/route checks pass 39 candidate and 36 installed-baseline cases. Only the candidate provides cancellation and bounds. Authenticated reconnect foundation independently passes 65 tests; Private Hub58 and Site22 reconnect cases independently pass; both complete shared archives are qualified locally. The actual plugin component reproduces8 lifecycle failures with5 controls and is under repair. Installed adoption and real ACP remain open. |
| Are containers reproducible and the app scalable? | Exact Factory/gateway image identities and effective privileges/resources were inspected. Factory runs root with a writable Docker socket and no configured resource limits; a safety packet is prepared. No load envelope, restore success or supported tenant count has been demonstrated. | Phase17 supported disposable deployment/SBOM/restore/rollback; phase19 measured capacity, fairness and recovery targets. No production load or container mutation. |
| Are PostHog/Sentry sufficient? | PostHog aggregates exist but mixed-project totals are not production error rates. A fresh connector query returned zero valid uploaded PostHog symbol sets; its advertised SDK doctor endpoint was unavailable. Sentry source integration is conditional; present access cannot prove release association, uploaded source maps, event receipt or alerts. | Phase16 sanitized attribution and local fixtures; legitimate project/environment access for receipt and source-map proof. Do not call unverified telemetry healthy or absent. |
| What did this review newly catch? | Full-source manifest races, ambiguous historical receipts, two tests that eagerly load normal database credentials, per-document batching cost, incomplete Qdrant fixture DDL, stale worker claim exposure, unbounded SDK response allocation and misleading reaper RSS. | Each finding is linked to bounded follow-up scope and evidence. Preserve partial supersession requirements in older docs; do not infer health from a green typecheck, dependency bump, summary count or timeout log. |

## Evidence

- Original audit: `.lavish/minion-qc-2026-09-08/report.md` and specialist appendices.
- Full planning review: `GSD-FULL-PLAN-REVIEW.md`; exact admitted inventory: `plan-allowlist.json`.
- Independent initial security review: `.planning/phases/09-security-containment/09-SOURCE-VERIFICATION.md`.
- Drone: `.planning/phases/11-agent-lifecycle/11-01-VERIFICATION.md`, `11-06-SUMMARY.md`, and subsequent independent11-06 verification.
- UI fixture: `minion_hub/tests/fixtures/overlay-native/README.md`; screenshots/results under `.lavish/minion-360-implementation/overlay-evidence/`.
- Runtime: `.planning/research/360-infrastructure-verification.md`; UI/dependency/Sentry follow-up: `.planning/research/360-ui-dependency-verification.md`.

No phase or requirement is globally closed in this assessment. Missing release, browser, harness, policy or operational evidence remains visible in the corresponding plan; it is not waived by passing local tests.

## Dependency and failure-recovery checkpoint

The isolated12-01 candidate passes actual framework/editor/XML security regressions and Chromium fixtures; the unchanged dependency baseline reproduces three of four current Node fixture failures. Candidate default-heap compilation succeeds but Vercel packaging exhausts memory. The8GB diagnostic was interrupted at its bound while still tracing, without a second observed OOM. The matched unchanged-dependency build also exhausted the default heap during Vercel packaging. Both failures are established; an identical cause or absence of candidate regression is not established. Plan 12-05 completed bounded diagnostics; the translation module completed, while full graphs stopped before outside-root metadata probes. Admitted 12-06 uses the installed OS isolation tooling to investigate without suppressing assets. No manifest/lock upgrade is accepted yet.

The driver repair plan requires actual caller settlement, safe rollback and healthy reuse after a marked disposable backend is terminated. A null check that hides the crash while leaving promises pending must be rejected.

## Receiver and remaining-handler plan review

09-06 adds the gateway-side Shells receiver boundary: runtime frame validation, reusable-credential registration ordering, exact connection ownership and bridge-origin event allowlists. It now independently passes39 receiver/manager/store tests; sender11-02 did not satisfy this separate receiver boundary.

10-07 independently passed 24 unit and 14 native tests plus a full current-source Hub type check with zero errors/warnings. Finance adoption independently passes 47 unit/parser and 19 native tests; legacy jobs require explicit retry to establish a fresh revision. Failed-job/import-state convergence and a recovery UI remain open. Finance10-04 retains deterministic chunk progress with revision arbitration to prevent reset ABA; The actual PostgreSQL/pgvector fixture is now qualified. Brain10-05 exposed two additional duplicate-job races: a mutable module can admit extra embedding batches after another job publishes, and an empty URL response can compete with an admitted paid manifest. Shared whole-source manifest binding now passes independent foundation and brain checks; corpus 10-06 remains gated on batching and multi-head support. A single scheduling cursor cannot arbitrate semantic duplicate jobs. The exact admitted inventory contains 70 plans across 49 requirements; planning counts are scope, not completion.

10-08 independently passed 8 discovery and 22 native PostgreSQL tests. Default unit discovery now excludes SQL integration modules before import, and a separate exact-file lane requires an explicitly marked disposable database. Missing runtime identity and empty selections fail qualification. Future native fixtures require separate admission.

12-04 automatic safety review interrupted fault injection after four baseline failures were recorded. The isolated upstream guard candidate was never executed or selected. Its settlement/recovery behavior remains unknown; no rejected action was rerouted. Independent work continues on ordinary job correctness, vector qualification and build diagnostics.


## Current verification limits after the manifest and tracing follow-ups

The fresh Hub snapshot containing finance, brain and shared-manifest changes passes typecheck with zero errors and warnings; snapshot SHA-256 `ef6654794ba04713b540f04e835fa5149540e27e737345a91a97e70ea2f48e91`. Subsequent test-discovery configuration and diagnostic profiler changes have their own focused checks. Compilation is not a full application build or release certificate.

The default test lane now excludes two additional normal-environment credential-loading modules before import. Root and independent discovery checks each pass 8/8. Four useful offline business persistence cases remain preserved inside a quarantined mixed file until an explicit split; they are not counted as executed. Finance/brain native receipts use explicit disposable configs, and no new legacy SQL test is admitted automatically.

All four 12-06 contained traces reached their 60-second limits without dependency closure or OOM evidence. Their common final messages-file read motivates profiling but does not identify the cause. Reaper RSS cannot represent the Node worker; corrected auxiliary per-PID samples have limited windows. 12-07 passes 20 independent harness checks. Its one application profile and decoder completed, but the last profile receipt covers only 3.570 seconds of a 60.025-second run. Mostly initialization samples do not identify the packaging stall; no further experiment is admitted. The candidate dependency update remains unapplied, and complete Vercel packaging remains unresolved.

The actual worker migration replaces the Hub base trigger and enqueues text-only Qdrant chunks. A fixture using only Hub DDL is incomplete; that observation is not a deployed failure. Worker source review separately found non-durable generated vectors and same-content claim-owner gaps. Those findings need their own implementation boundary and qualification.

Agent restart preflight found no durable receiver acknowledgment/query, an in-memory run outcome path and a stop operation that does not confirm process exit. A native SQLite receipt foundation is being scoped with an explicit supported-runtime gate; this is not implemented durability. Node official v22 documentation identifies unflagged node:sqlite from22.13, while the current bridge declares>=22.0. Restore and remote delivery need separate consumer/receiver changes.


## Current follow-up reassessment

The admitted inventory now has64 exact-path plans. Shared14-08 independently passes65tests; full14-11 archive identity is qualified for unpublished local testing. Site14-10 independently passes22cases and complete candidate appcheck. Hub 14-09 independently passes 44 cases and its full app check after repairing delayed-update and pre-authentication event admission. Neither app has adopted the candidate package/source in its active checkout.

Artifact14-07 now reuses one generated bridge rather than three maintained protocol shells. Source tests106/fullHubcheck and actual3-peer native mobile/desktop/BFCache evidence pass. These results do not certify all mobile workflows, arbitrarygenerated/storedHTML, ArtifactHost capabilities or deployed behavior. Compiler helper provenance is verified from the actual registry archive and notices retained.

The agent journal foundation is locally persistent and testable, but its existence does not make current ACP timeouts or remote effects durable. Receiver planning found real authorization gaps: some service/shared-token callers have no validated tenant identity, and method-scope registration cannot be replaced by a handler-only patch. Canonical contract reuse, tenant authority, durable receiver receipts and sender startup/process ownership remain separate prerequisites.


## Latest integration reassessment

The exact inventory is70 plans/49 requirements/12 phases. These are scope counts.14-13 independently qualifies46 canonical bilateral-negotiation cases;14-11 independently qualifies the resulting125-file full archive including runtime/type exports.14-14 now owns a pure gateway facade and bounded input grammar in a private package-paired snapshot, without routing or durability-mode activation.

The paired actual plugin fixture exposes a first-ready bridge replacement and stale selected-host dispatch. Root reproduced all8 regression assertions, with5 controls passing, before admitting14-06 component repairs. Final paired Hub aggregate and native browser qualification remain pending. Agent cancellation is still cooperative; source/client retirement cannot retract an already dispatched effect.

ACP research verifies protocol-shape and permission/correlation mismatches against pinned official SDK evidence. No SDK migration or real harness run occurred. The standalone vector worker received only exact-site handoff comments, independently verified as comment-only; its claim-generation, migration activation, transport stop, receipt and Qdrant ordering changes remain separate gates.

Corpus work retains cross-document batching and limits selected logical payloads; synthetic memory measurements do not certify production capacity or SQL working memory. Actor GUCs are writable transaction context, so SQL ownership design is under review before the new migration is qualified. No production database, installed package or runtime was changed by these steps.


Latest continuation:11-08 environment injection independently passes10cases/types.14-14 facade/input independently passes91cases/types. Corpus initial28 native cases pass, including exact16MiB versus16MiB+1 selected-response proof; remaining native acceptance cases are active. Plugin49focusedcases pass after URL serialization and fixture typing corrections; typed pending classification is a separate two-file utility amendment before finalpairedaggregate/browserchecks. These source results do not close the broader70-plan operation.


Current checkpoint: 70 admitted plans across 12 phases and 49 requirements. The final private 14-06 component independently passes 56 mounted cases and the full paired Hub check with zero errors/warnings; 14-09 session and 14-15 compatibility are included in that aggregate check. Native browser qualification is being prepared. 11-08 injected environment parsing and 14-14 gateway input/facade are independently verified. Corpus 10-10 passed 38 native cases before an additional exact PostgreSQL descriptor-boundary correction; final expanded qualification is still active. No phase, requirement or release is closed.

## Second-wave reassessment — 2026-09-09 (resumed after the Codex usage limit)

Same original questions, answered as a delta over the first wave. Only slices with root-rerun evidence count; private candidates are not adopted, installed, published or deployed.

| Question | Evidence-backed answer after this wave | Remaining action and owner |
|---|---|---|
| Wave 2 · What improved? | The gateway now has a private native durable receiver (14-12): MSR1 three-table SQLite store, admission committed before one machine forward, terminal receipt committed before ACK, keyed replay/recovery, per-org and global retained limits, quiesce/drain; 69 native/loopback cases, strict types, lint and format pass root rerun. The 14-14 facade re-exports the adopted 14-16 durable contract from the fresh complete SDK archive (110 cases). The shared package builds a clean 44-member release archive with no tests, maps or local paths (12-08, byte-identical root rerun). | Caller routing/authorization child, sender 11-03 adoption of `shells.invoke_durable`, production store/lifecycle wiring and adoption of the four packaging files remain root/owner decisions. |
| Wave 2 · What should be cut? | Shipped test outputs and 62 unusable source maps in the SDK archive are cut by the candidate build; the emitting `prepublishOnly` is replaced by a noEmit typecheck. No library was removed this wave. | Phase 12 dependency-family removals still need peer-aware transactions; DOMPurify/d3 stub candidates unchanged. |
| Wave 2 · What should be kept or revamped? | Keep native SQLite as the receiver transaction engine (no bridge-journal import), keep all 09-06 registration/ordering controls (preserved and re-tested), keep the existing per-shell short-operation queue. Revamped: Shells outcome durability is now receiver-side and immutable rather than in-memory. | Sender/restore (11-03) and lifecycle/config children own the remaining revamp; no wholesale replacement. |
| Wave 2 · Are docs outdated? | Program ledgers (STATE, PROGRAM, plan index, full-plan review, proposals) were refreshed to 73 admitted plans with dated root verification; the shared package gains a README that states entry points, peer behavior and debugging limits truthfully. | 18-02 semantic disposition of 56 divergent same-ID documents still open; license text remains missing. |
| Wave 2 · Is the UI fully mobile/desktop compatible? | No change this wave; first-wave plugin/artifact/dialog evidence stands. | Phase 13 unchanged. |
| Wave 2 · Which libraries need updates or replacement? | The SDK packaging path no longer depends on `tsc` incremental state; build uses the installed local compiler only. Found and fixed: Node resolves symlinks in the ESM entry before `import.meta.url`, so link-safety checks must use `process.argv[1]`. | License/attribution, immutable version and installed-consumer qualification before any publish; phase 12 advisories unchanged. |
| Wave 2 · Is AI governance stable, testable and replicable? | Durable Shells outcomes now have a replicable private native test lane (loopback-only guard, one worker, private caches, zero denied network attempts). Authority is connection-bound (accepted v1 + registered org on the actual socket), never inferred from payload, heartbeat or timestamps. | Still not whole-platform certification: sender integration, JWT-derived org for callers and real ACP harness remain open. |
| Wave 2 · Do cancellation, retry and restart preserve execution truth? | Receiver-side: replay never resends, lost ACK or lost admission response recovers by exact scoped key, uncertain send grants no retry, close-before-commit denies, post-COMMIT ACK failure retains the receipt readable from a second handle, quiesce refuses new work and drains only entered local work. | Failed-COMMIT/process/power-loss recovery is untested by design; sender-side truth and operator recovery remain 11-03/phase 15. |
| Wave 2 · Are parsing and data pipelines well implemented? | No change this wave. | 10-10 caller/worker adoption and phase 15 unchanged. |
| Wave 2 · Does the DB enforce rules under concurrency? | New SQLite evidence: STRICT tables, composite/partial unique constraints, constraint rollback, second-handle visibility and a controlled BEGIN IMMEDIATE contention sequence with bounded busy refusal then convergence. Two sequential handles are not treated as concurrent-writer proof. | PostgreSQL side unchanged from wave 1; live metadata constraint still absent. |
| Wave 2 · Is the SDK/HTTP/WS/ACP connection natural and safe? | Gateway and SDK now share one canonical durable contract through a selective facade rather than copied definitions; the machine endpoint accepts `shells.commit_outcome` only from the registered, version-accepted, current socket. The release archive resolves all five entry points from the extracted tarball, and the Node entry fails natively without `ws` and constructs a client with it. | Hub/Site/Paperclip still consume the old installed 0.6.0; consumer qualification and a distinct release identity precede adoption. |
| Wave 2 · Are containers reproducible and the app scalable? | No change this wave. Reservation math shows `TOO_LARGE` is unreachable for valid v1 records, so capacity limits are logical counts, not measured throughput. | Phases 17/19 unchanged. |
| Wave 2 · Are PostHog/Sentry sufficient? | No change this wave. | Phase 16 unchanged. |
| Wave 2 · What did this review newly catch? | A provisional facade copy in the receiver snapshot differed from the frozen bytes (caught by the sync check before execution); a symlinked package ancestor bypassed the first build-script guard; pnpm's lifecycle runner spawns `ps` (allowlisted after strace review); a pre-existing bridge-ws test socket leak; the codex-prepared runner's env allowlist is required for pnpm to run at all. | Each item is recorded in 12-08/14-12 verification; none is closed by a green run. |
