# Phase09/10 acceptance inventory — read-only, 2026-09-12

Scope: original phase09 containment and phase10 durable jobs/stock plus the final-table gaps. Root explicitly keeps SEC06 typed analytics (phase15) and SEC09 provider logout (phase13) later. No implementation, DB connection, provider call or customer mutation was performed for this inventory.

## Revision and evidence boundary

Remote Hub master77445c01ec38657c767d6f011e77148be3cc4ccf and Gateway main7a1501e969014ec2fcfb69021bda843d0f90d23b remain the verified production sources. Gateway DEVb841c36750e4bf10dd3f81a4896b19c699fe3132 has identical production tree. Meta dev advanced9216d409 to6c2a5385f1f43483c18395e956dd6f2b94e3f160; comparison is rankings/index.json only, not planning changes.

Hub inspected private checkout: /home/nikolas/.cache/minion-qc/ui-next-jgR8fM/hub, HEAD32f7b2293700f803fd66307bf94960fac4874443, treea7434373eb515107876767035f3446d9c9af1298 = deployed774 tree. Gateway inspected private checkout: /home/nikolas/.cache/minion-qc/gateway-portability-qku87ie7/minion, source treea61dc1769f5737ed3c18ef196257d4a1d763c3c2 = deployed7a tree. Active checkouts differ and were not modified.

Historical progress remains19/19 phase09 tasks,23/23 phase10 tasks,50/54 phase14 tasks; these are scoped implementation credits, not whole-requirement closure.

## Acceptance checklist

| Requirement/gap | Existing evidence | Exact remaining acceptance and safe action | External gate |
|---|---|---|---|
| SEC01 raw SQL denial | Original09-01, actual fail-closed route/source and regression receipts | Re-run actual route tests on final integrated source: arbitrary SQL rejected before identity/DB access; stable nonretryable error and zero calls. Keep disabled branch through typed analytics work. | None for containment; SEC06 restoration is separate. |
| SEC02 persisted gateway principal | assistant-principal.ts84–217; unit and real-engine assistant-principal.pg.test.ts assignment/capability cases | Final integration tests: removed/ambiguous/wrong-gateway/wrong-org assignment; membership revoked between requests; browser delegated actor; unassigned brain denial. Real-service/native RLS can use synthetic identities. | Genuine Supabase login/JWT refresh/revocation requires identified nonproduction project and disposable users. |
| SEC03 safe server diagnostics | Adopted servers route repair and focused/fullCI tests | Final route errors for create/update: credentials, nested DB/request/provider exception text absent from response and captured logs; preserve allowed response behavior. | None for source/route contract. |
| SEC04/05 SQLite and bound input | Native gateway flow guard and parameter fixtures; full Linux/Mac runtime/Windows on exact release | Retain real-engine negative writes/CTEs/PRAGMA/ATTACH/file-management/function/VFS boundaries, validated binds and rejected legacy interpolation. Test exact release native runtime with owned tempDB; no backend kill. | None for owned fixture; deployed profile trust requires read-only identity audit. |
| SEC07 tenant and equivalent file paths | hooks tenant and assignment tests; new09-07/08 address attachment/linked-record authority | Full tenant/module/owner action matrix through actual routes/services, positive access and denies without existence leaks; all generic list/detail/raw/update/delete paths; mixed links; intent/finalize adoption; current membership and URL enrollment policy. Native RLS + deterministic lifecycle races required. | Real-provider browser session matrix remains explicit until safe test project/users identified. No customer file mutation. |
| SEC08 filesystem/process trust | Deployed missing-ancestor canonicalization + workdir normalization; 25focused path/caller cases | Exact-image SQLite profile inventory: initializer owns writable schema, consumer is readonly, approved VFS/temp settings and DB location; record trusted writers. Re-run alias/missing-child/dangling/permission and SQL-file cases in isolated temp dirs. | Check/use path race remains under trusted-process boundary; hostile same-UID writer/process isolation is a separately declared deployment decision, not certified by path tests. |
| JOB01 fenced owner and heartbeat | Actual bg-runtime fenced predicates; hosted native concurrency and effects receipts | Final handler matrix for groupchat, statement_ingest, brain_ingest, conversation/legacy corpus, business corpus: long pending provider, heartbeat failure/lost ownership, cancel and reclaim; stale domain/progress/terminal writes zero. Confirm tick imports every handler and deployed worker source. | Read-only migration and scheduler/old-worker identity audit below. |
| JOB02 canonical recovery | Received effects replay; admitted/no-result fails indeterminate; actual requests/manifests/pages | Prove graceful process restart, lease expiry/reclaim, response received before failed final commit, source revision supersession, and no automatic repeated paid call when admission lacks result. Whole-handler brain-ingest10-05 and groupchat are outside continuing five-file lane; admit precise safe fixture additions separately. Reconcile import status with durable failed job and explicit retry. | Explicit indeterminate recovery/retention policy; actual driver/backend-loss gate still prohibited. Synthetic provider is not paid-provider or remote exactly-once proof. |
| STK01 invoice uniqueness |901/902 migration fixture, active invoice expression index and native race cases | Read-only deployed index predicate/validity+collision preflight, and final actual service concurrent same-invoice/cross-org/cancelled history semantics. Keep no duplicate/reposted ledger. | Applying missing migration requires reviewed preflight/adoption packet; no automated history deletion. |
| STK02 draft/submit and booking recovery | Current invoice recovery native tests; booking transaction correction; new10-11 extraction | Caller-owned transaction rollback for stock/accrual helpers; later durable booking producer/worker/outbox idempotency and deletion coupling; crash after booking commit before effect; after draft before submit; submit before receipt; superseded booking; parallel duplicate; old owner. Verify terminal/retry status visible and deterministic. |10-11 helper extraction alone cannot close booking recovery. Later producer/worker/migration plan needed. |

## Existing native lane and gaps

.github/workflows/ci.yml:231 uses pinned pgvector0.8.6/PostgreSQL18 image, actual vector(1536), HNSW and serving-index trigger. scripts/qc/jobs-postgres-contract.ts:7 allowlists exactly job-stock-concurrency, job-effects, job-effect-pages, finance-statements.effect-ownership, brain-corpus.effect-ownership SQL fixtures. Final hosted148/148,0skip. The older vitest.disposable.config.ts two-file lane is distinct, not evidence that hosted coverage is absent. Missing/empty/skipped/report mismatches fail closed. Do not wildcard-discover SQL suites or invoke credential-loading legacy tests. Attachments/new booking SQL need exact root admission.

Original10-05 brain-ingest native pgvector qualification exists historically but is not in this continuing lane. Provider responses are synthetic. Actual Qdrant mode/worker activation is not certified by a preserved generation trigger. It need only be activated/qualified if that deployment mode is selected; do not make inactive optional mode a prerequisite to every stock requirement.

## Worker and migration evidence still required

src/routes/api/jobs/tick/+server.ts imports all handler services (groupchat, brains, both corpus services, finance). It requires CRON_SECRET. src/lib/automations/system-automations.ts:38 claims Netcup per-minute wiring based on2026-07-25 hand verification; vercel.json does not schedule jobs. Do not invoke tick as a read-only probe: it advances customer work. Read current scheduler metadata instead, redact command bodies/tokens, output only schedule/path/target hostname/enabled state; then use existing access/status logs without payloads for last successful invocation. Confirm no scheduler targets an old immutable Vercel deployment and no old worker deployment remains active. bg-runtime.ts:54 missing-handler TODO is stale; actual handlers already accept JobExecution.

Read-only catalog inspection must match migrations20260909090100 through90500: bg_jobs lease_generation/default/check, stock active-invoice unique index valid/ready and exact predicate, job_effects manifest/receipt constraints, pages/batches/units/functions/grants/RLS. Supabase migration ledger alone is insufficient: compare actual catalog. Preflight script supports explicit STOCK_PREFLIGHT_DB_URL and read-only transaction; do not print its optional entry/org identifiers for broad reports, retain aggregate counts only. No migration commands in qualification.

Job policy source pointers: job-effects.service.ts:750 indeterminate admission; brains.service.ts:1032 and1055 recovery/retention/driver/adoption gaps; finance-statements.service.ts:491 domain status can remain queued/parsing after durable failure; groupchat.service.ts:389/538 remote outcome and unreadable checkpoint recovery. These are genuine remaining policy/operational gates, not missing handler plumbing.

## Safe target and driver boundaries

Names-only inspection found generic SUPABASE_DB_URL/PUBLIC_SUPABASE_URL/service-role names but no dedicated nonproduction target. Process env has no admitted MINION_QC_DATABASE_URL. Subsequent root-authorized sanitized identity shows active .env.local public projectgxvsaskbohavnurfvshr.supabase.co, pooleraws-1-us-east-2.pooler.supabase.com:6543/databasepostgres. This does NOT establish nonproduction or authorize customer mutation. Root target review pending before DB connection.

Safe credential-free acceptance: marked owned PostgreSQL + real schemas/RLS/services, owned fake object store, local HTTP provider with request counters and intentional response loss/ignored abort, graceful owned worker restart and normal lease expiration. These do not qualify PostgreSQL backend loss. Existing12-04 backend termination/fault injection remains prohibited; no process kill, container restart or proxy sabotage as a workaround. Record the unavailable proof explicitly or obtain a reviewed alternative gate from root; never equate rollback/fixture exception with actual lost backend.

## SDK next wave after09/10 disposition

Exactly four historical partial tasks remain:14-02Task1 consumer matrix,14-02Task2 immutable installed adoption,14-03Task2 CRM Site archive+browser boundary,14-17Task3 actual Gateway startup/owned shutdown. Plan order:

1. Freeze current integrated shared protocol/package inputs and verify12-03/12-08 installed provenance.14-08 +14-13 ->14-11 immutable emission;14-13 ->14-16 caller contract;14-13/14-11 ->14-14 facades. Historical completed snapshot credits do not prove consumer installation.
2. Complete14-17Task3 on real Gateway entrypoint with owned temporary SQLite/store: store-before-manager, required-store failure tears relay down/fails startup, disabled mode neverloads SQLite, quiesce then close, late pending settlement touches no closedstore. Dependencies14-12/14-14/09-06. Run local authenticated synthetic caller/shell sessions for same/foreign org, explicit requiredmode and no legacyfallback. Production configuration enablement remains separate.
3. Complete14-02Task1 through actual Hub, Site, registered Paperclip adapter, plugin/Workforce matrix: token rotation, reconnect/challenge, old/new/unsupported peers, malformed/duplicate events, cancellation and safe response-loss behavior. Repair empty Paperclip test discovery; zero tests cannot pass. Record perconsumer exact source+archive+lock.
4. Complete14-02Task2 one manifest writer perrepo; only selected immutable archives; actual bundle/declaration identity and live consumer behavior, not package.json alone. Reuse14-18 Site fixture proof where exact installed bytes match. Add precise adoptionchildren for failed/outdated consumers.
5. In parallel after provenance freeze,14-03Task2 adopt fixed CRM archive through actual Site leadroute; synthetic duplicatelead/DNI/redaction database tests, direct/transitive browserimport denial and actual browserbundle symbol scan. Current expected-failure archive check is not closure.

No SDK implementation started. Authenticated production dispatch, paid providers, registry release and enablement are not implied by this acceptance inventory.

## Independent plan review

09-07/09-08: scoped source, native RLS, alternative routes and lock order are appropriate. Treat as inseparable deployment packet because09-07 durable classification depends on09-08 table although implementation waves are sequential. Require preflight classification ambiguity to fail safely; all supported record deletion dispositions must exist before all-record claims. Deletion retry liveness needs an explicitly adopted caller/schedule or documented manual recovery; disabled cron alone cannot establish automatic recovery. Source-only09-08 completion can remain distinct from operational acceptance.

10-11: sound helper-only preparation; no scope claim that extraction fixes producer/worker durability. Must require exact historical nativeDDL for fin_products/naming_series_counters (requested read-only catalog), preserve naming/stock audit atomicity and zero nested organization transactions. Subsequent producer/worker plan and tests remain required.
