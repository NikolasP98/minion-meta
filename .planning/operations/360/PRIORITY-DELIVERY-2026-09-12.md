# Priority delivery and independent review — 2026-09-12

Planning scope: the 89-plan task denominator describes the [archived local orchestration snapshot](https://github.com/NikolasP98/minion-meta/tree/b9d0ab4a95cc76d5f64a48b33947247db59649c2/.planning/phases), with its [state and qualifications](https://github.com/NikolasP98/minion-meta/blob/b9d0ab4a95cc76d5f64a48b33947247db59649c2/.planning/STATE.md). The archive is evidence, not adoption of that historical GSD tree or runtime source into `dev`. This documentation delivery preserves the current `dev` planning baseline.

**193/229 original scoped tasks are complete (84.3%); 29 remain partial and seven lack complete receipts.** This measures planned deliverables, including audits and preparation, not production readiness. No whole requirement is independently closed (0/51). Hub jobs/stock and UI repairs are deployed; reviewed Gateway and Factory source has the release limits below. There is no whole-platform security or production certification.

## Findings and corrections

The independent takeover review found that some completion claims exceeded their evidence. It identified a finance parser/service version mismatch, non-atomic booking edits, client-visible persistence errors, cross-shell outcome authorization, a dropped legacy invocation key, and a native corpus fixture that substituted text for vectors and omitted the serving trigger. Corrections now have regression evidence at their actual boundaries.

Review of the new work also caught reduced-motion code that froze pixel spawn/despawn cleanup and a Gateway path guard that accepted missing descendants below escaping symlinks. Both were corrected. The ACP SDK candidate remains outside production because caller sessions cannot safely be assumed to be independent one-shot requests.

## Delivery and release evidence

| Slice | Reviewed source and adoption | Qualification |
|---|---|---|
| Hub jobs/stock and API repairs | PR269: reviewed `a8efe289b4d1377edcf3b58ab8ae2d6f22888b52`; deployed `f97efb2d43742772d5bc44b93260920bb20bb0a4` | 148 native cases across five exact suites, zero skips; both CRM native lanes and ordinary/build gates passed. |
| Hub UI/accessibility | PR270: reviewed `32f7b2293700f803fd66307bf94960fac4874443`; deployed `77445c01ec38657c767d6f011e77148be3cc4ccf`, matching reviewed tree | 3,800 ordinary passes/25 existing skips; 148 native passes/zero skips; full build and design/token checks; public mobile/desktop login smoke. |
| Bridge Node support floor | Meta PR400: merged dev `6103e982508ec3cdc18488eb4f855474ef52c907` | 216 actual Node 22.13.0 cases; emitted journal on 22.13.0 and 22.23.1; unavailable-builtin rejection on 20.18.0. |
| Official ACP SDK candidate | Meta PR401: merged dev `48e67faff5aa9f320f371f985dd599efdf82bde0` | 157/157 package cases, including 22 SDK stdio cases; SDK 1.4.0/Zod 4.3.6 are exact dev dependencies. Normal typecheck and archive exclusion passed. No runtime replacement or publication. |
| Gateway — Linux Docker release pending | PR292 merged DEV `2a2fdace858536114783d6d7dc4a3fbb76801898`; PR294 reviewed `27f7cf3469d03e8ffa819641e29cb5aa1e239439`, merged normally to current DEV `dd9dde24a601501a3d3eb63bd7d0b291ec9e764d`. PR293 production promotion awaits the release owner's final receipt. | 99 authorization/manager/RPC cases and 40 focused portability/security cases passed. DEV CI `34716632510`: Linux and macOS TypeScript each passed 13,031 cases/nine existing skips. Swift formatting failed; native build/tests were not reached. Applicable full Windows checks and final image/live identity must be recorded before scoped release closure. Docker release run: `34716632624`. |
| Factory — stopped before rollout | PR187 governance source merged dev `905b4b10a2553f56625331eefcade4794589a188`; PR188 dependency-selection repair merged current dev `467906db88f51476e2ca5fb561c2c4a79459e268`. Main/live/marker remain `02900306a1fcc7b182bae726a24260d08467f81e`. | 184 hosted boundary cases, zero skips. Trusted promoter `34715953488` passed candidate tests, then the initial authenticated broker turn failed with HTTP 502 `UPSTREAM_FAILURE`; one bounded exact-workflow retry failed identically before rollout. |

Hub deployment `6413695862` / Vercel `J1pG3JEVZVs31aEHNw3QmzEju2dc` serves exact `77445c01`. Public sign-in rendered at 1440×1000 and 390×844 without horizontal overflow; this does not qualify authenticated workflows. Hosted ordinary CI retains its existing retry setting; focused independent checks ran without retries. The owner-authorized Hub review exception allowed exact-head admin squash after required tests passed because GitHub required an external review. No test gate was waived.

Gateway's PR Windows gate was curated smoke: 255 passes/five skips, despite its broader label. It is not full Windows qualification. The current macOS job failed SwiftFormat 0.63.0 on 116/208 untouched Swift files after TypeScript passed. No verified formatter pin or recent successful native baseline was found; no downgrade, rule disabling or broad formatting was applied. Root explicitly scoped Linux Docker delivery after applicable runtime/security/protocol/build/full Windows checks, using normal merge without a gate bypass. Overall cross-platform CI remains red. The [native Swift follow-up](../../../proposals/2026-09-12-gateway-native-swift-qualification.md) has an unadopted comment-only handoff, `1ed362a32db68aa1e58f36e8b9121b45108c380b`. Gateway PR294 carries an SSH signature; GitHub reports `unknown_key`, not verified signing.

Factory's broker maps several SDK/turn failures to the same 502. Probe cleanup removed the underlying artifacts; retained output cannot distinguish provider availability, authorization/quota, routing/model or SDK/network failure. This is not evidence that OpenRouter returned 502. No additional paid probe, provider switch, activation flag or release override was attempted. Original workers completed before any rollout: discovery passed; rank and reconcile errored while live identity remained unchanged. No deployment interruption is inferred. Factory remains on its trusted promoter route.

## Implemented scope

**Jobs and data:** finance checkpoints bind parser version 3; incompatible historical cursors return 409. Booking PATCH validates and transacts coupled changes, including rollback on invalid invoice/event type or audit failure. Server APIs return generic errors. Native stock fixtures include `archived_at`. Corpus qualification uses actual `vector(1536)`, HNSW indexes and the hash-pinned serving trigger, with handler ownership, replay, deletion and RLS cases. CI rejects missing, skipped or failed selected suites. Backend-loss fault injection remains excluded.

**UI:** keyboard agent selection and task entry call existing handlers and the native shared Dialog. Reduced motion preserves pixel lifecycle cleanup. Classic/Habbo Pixi pulse, reaction and hover effects honor initial/live preferences and release listeners; Rapier physics continues. Evidence covers 18 component/lifecycle browser cases, nine native WebGL/physics cases across Chromium/Firefox/WebKit, 11 native unit cases, 30 critical fixture cases and 13 Chromium navigation cases. These qualify components and engines, not the complete authenticated Workshop.

**Agents and security:** Gateway outcome admission binds shell identity to the authenticated connection; keyed legacy requests cannot silently lose retry semantics. Nearest-existing-ancestor path resolution rejects escaping, dangling and cyclic symlinks and error paths. The normalized workdir is both checked and executed, with unavailable-cwd fallback preserved. The 40-case repair packet also covers packaged JS/MJS MIME and real filesystem-watch behavior. This establishes normalized snapshot containment, not atomic protection against symlink swaps. Factory fixtures exercise actual MCP→HTTP→runner→SQLite admission, denial, owned-child restart/replay and revoked capability. Official ACP examples qualify SDK reuse outside production; current session behavior remains unchanged.

## Remaining implementation and decisions

| Area | Required next work and boundary |
|---|---|
| Jobs/stock recovery | Booking commits still precede best-effort stock effects. Implement atomic durable intent admission and reconciliation; native fixture success does not establish every producer/provider, old-worker drain, migration adoption or crash/restore behavior. See the [recovery proposal](../../../proposals/2026-09-12-hub-booking-stock-postcommit-recovery.md) and [executable design](../../phases/10-durable-jobs-stock/10-BOOKING-STOCK-RECOVERY-DESIGN.md). |
| UI parity/accessibility | [Remaining consumer plans](../../phases/13-ui-qualification/13-REMAINING-CONSUMER-PLANS.md) cover relationship/element/camera keyboard tasks and Reliability filter/legend/Sankey alternatives. Full Workshop composition, authenticated Supabase/ERP/logout/revocation, native assistive technology and real devices remain open. A non-production identity target was requested; none was invented. |
| Governance and ACP | Qualify budget exhaustion, mutable tool/schema/model/image identity, expiry and external executor effects. Implement persistent caller→provider session mapping, load/restart and late-update fencing before ACP adoption. Credential-free examples do not qualify the selected real harness/provider. Published shared/bridge packages and workstation-image adoption remain separate from newest sender source. |
| Unresolved security findings | Attachment read/download/link/unlink need linked-record/module/owner authorization. Link admission can race the sweeper; booking deletion can leave references. Keep the sweeper cron disabled. Atomic filesystem containment is also open; see [review follow-ups](../../../proposals/2026-09-12-360-continuation-review-followups.md). |
| Release and operations | Complete the scoped Gateway receipt above; classify Factory's broker failure with allowlisted diagnostics and preserved, redacted artifacts before another release attempt. Native Swift qualification remains separate. |
| Remaining 360 coverage | Dependency advisory/consumer adoption; configured Sentry/PostHog and actual alert delivery; full container readiness/shutdown; load/SLO and cross-store restore/erasure; semantic proposal cleanup and exact-artifact integration remain open. |

The stock design maps every producer to one domain intent table, existing job/revision ownership, transaction-taking stock helpers, booking-specific database uniqueness, operator status/retry and native restart/concurrency/RLS tests. Three inventory policies remain undecided: preserve or replace unsubmitted drafts; resolve defaults at admission or execution; use operator or automatic retry for business blocks. Questions were sent; no answer has been assumed. The design does not add completion credit.

## Progress and evidence method

Progress rose from 190/229 (83.0%) to **193/229 (84.3%)** through three exact deliverables: `10-06 Task 3` native corpus qualification, `11-07 Task 3` bridge engine-floor qualification, and `13-04 Task 2` canvas audit with precise follow-up plans. The UI audit credit does not mean the remaining controls are implemented. Independent review recomputed 89 plans/229 tasks and checked the three credits against their receipts and canonical scopes. Newly discovered findings remain outside the historical denominator.

See the [category table](PROGRESS-2026-09-12.md), [full task map](PROGRESS-2026-09-12.json) and [receipt manifest](priority-delivery-2026-09-12/manifest.json). Historical source-only receipts retain their limits; later release receipts establish adoption only for their exact slices. Active dirty checkouts were preserved and release work used private clones. Tests changed no production data, tenant permissions, customer accounts or runtime activation flags.
