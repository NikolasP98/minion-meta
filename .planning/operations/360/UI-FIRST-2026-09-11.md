# UI-first implementation checkpoint — September 11

The user prioritizes mobile/desktop UI parity, followed by the existing 360 program. Current scope is **89 admitted plans, 51 requirements and 12 phases**. No entire phase or requirement is complete. Plan structure, source behavior, installed artifacts and deployment are separate checks.

## Current UI candidates

The combined Hub candidate is `/home/nikolas/.cache/minion-qc/ui-integration-u8r6xtfh/hub`; its parent holds exact source manifests and logs. The Site candidate is `/tmp/minion-13-08-dn7zbntf/minion_site`. Both are ordinary private snapshots. Existing working trees and staged changes are preserved. This continuation has not committed, pushed, merged or deployed changes.

| Slice | Current evidence | Remaining boundary |
|---|---|---|
| Home/calls 13-05 | 39 coarse-input cases and 3 separate fine-desktop cases; scoped checks pass | Real microphone/provider behavior |
| Calendar timezone 13-13 | 27 native cases independently repeated in New York; agent also tested UTC/Kathmandu. Root browser verifies 08:00 placement in Lima/New York/Kathmandu | Fold-spanning rendering, overlap query, other calendar consumers |
| Charts 13-04 | 23 mounted/native-engine cases; 18 browser cases and 3 corrected wide-table cases pass | Unsupported shapes, consumer keyboard actions, custom effects/Pixi |
| Navigation 13-07 | 39 browser cases; 4 root-repeated policy cases; screenshots reviewed | Authenticated whole-app routing |
| Critical journeys 13-06 | Root combined candidate passes all 30 mandatory cases in five browser/input projects | Real authentication, providers and deployed connectivity |
| Calendar touch 13-10 | 42 main cases, 6 affected cases after final tab fix, and 3 root-added capability-absence cases pass | Real mobile hardware; counts are separate runs |
| Site Graph/Files 13-08 | Final combined native suite passes 56 cases; 13-12 repeats browser coverage with actual fonts | Authenticated production connectivity |
| Site logout 13-09 | 13 endpoint and 8 AppBar cases within the final 56-case suite; actual-font browser checks pass | Durable provider revocation |
| Site chat/tabs 13-11 | 36 actual-font cases pass across Chromium, Firefox and WebKit; full Site check has zero errors/warnings | Hosted CI, physical IME and assistive technology |
| Site fonts 13-12 | 21 browser cases pass with seven actual font faces; archive, emitted bytes and licenses verified | Full production bundle and deployed resource behavior |

**The full combined Hub Svelte check passes with zero errors and zero warnings after navigation and final Calendar toolbar integration.** The mandatory root journey report is `evidence/critical-integrated-v2/critical-results.json`; artifact manifest SHA256 is `76d94b8544ffcb1d7e1959b357f03c54428c56e8ed978ab257df5e7ed3cdb267`.

Root critical/chart runs use Chromium 149. Home/call/navigation/toolbar lanes use Chromium 151.0.7922.34 from the recorded executable. Firefox 151 and Linux WebKit 26.5 complete the engine coverage. WebKit uses privately extracted libraries. These fixtures prove component behavior under synthetic state; they do not certify native iOS hardware or application authorization.

## Site tooling and evidence correction

Native Vitest 2 exposed an incompatible nested Vite 5 while the existing Svelte plugin requires Vite 6. The private candidate now uses Vitest 3.2.6 while retaining existing Vite 6 and Svelte identities. The formatter now uses its declared native Svelte plugin. Focused formatting passes; the global check reports 289 unchanged baseline style warnings. No mass formatting was performed.

The previous standalone Site fixture omitted the Google-hosted product fonts. Its behavior results remain useful, but its geometry is fallback-font evidence. Plan 13-12 uses two exact maintained Fontsource packages, native CSS imports and upstream licenses. Actual face registration and nonempty font-load results are required before new geometry claims.

## Next implementation

The independently reviewed server telemetry repair 16-04 passes a root replay of all 50 tests. Four producers now use the sanitized wrapper; telemetry errors preserve business outcomes. Parser correction 15-06 passes all 60 independently repeated native cases. Full Site sync, type checking and all 56 native cases pass. Plan 14-18 passes 22 service cases, 56 ordinary Site cases, 18 real loopback HTTP/WebSocket browser cases and full native Site sync/check. Root independently repeated the 22 service cases and rehashed all ten owned source files, 44 installed SDK members and 48 emitted artifact files. Plan 13-14 now implements the portable native CI command for the combined 75-case browser matrix. The current UI selection has an independently checked unapplied exact-file review packet; no active checkout is overwritten.

The remaining sequence is security and data boundaries, durable agent/SDK integration, dependency publication/provenance, migrations and processing, container deployment/recovery, documentation and the complete cross-project re-audit. Exact merged/unpublished/host-evidence limits are in `RESUME-2026-09-11.md`. No runtime or release gate is silently treated as complete.

## Live evidence and infrastructure

Read-only PostHog checks are recorded in `POSTHOG-READONLY-2026-09-11.md`. The production-host-filtered seven-day report has 38 occurrences across 9 error groups. Device totals are 201 desktop and 30 mobile pageviews; they are not unique users or a deduplicated customer count. No analytics settings, captures or issues were changed. Sentry tools were unavailable.

The Docker socket is mode 0660 root:docker; this process lacks the Docker group. `sudo -n docker version` reports that a password is required. No permissions, groups, daemon or containers were changed. This is an access gate, not a broken daemon. The full disposable authenticated stack remains unqualified; independent local implementation continues.
