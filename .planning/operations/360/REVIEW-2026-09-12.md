# Claude continuation review — 2026-09-12

Planning scope: the 89-plan task denominator describes the [archived local orchestration snapshot](https://github.com/NikolasP98/minion-meta/tree/b9d0ab4a95cc76d5f64a48b33947247db59649c2/.planning/phases), with its [state and qualifications](https://github.com/NikolasP98/minion-meta/blob/b9d0ab4a95cc76d5f64a48b33947247db59649c2/.planning/STATE.md). The archive is evidence, not adoption of that historical GSD tree or runtime source into `dev`. This documentation delivery preserves the current `dev` planning baseline.

Claude made substantial delivery progress, but the current release does not pass a production-readiness review. The executor receipts generally distinguish partial work correctly; the delivery ledger's claim that the entire Hub program is complete is too broad. Source adoption omitted verified repairs, current CI is red, and several authorization/data-integrity boundaries remain defective.

Review baseline: meta `38699809` through `8a217d7d`. Runtime source reviewed: Hub `25e2bb758678d7618b23b4f20b0f00ecc57a51cc`, gateway `064b4d34444274275be61e8e707982b772ca3543`. Active dirty checkouts were preserved. This was a review, not a new implementation or deployment.

## Verified delivery

| Surface | Result | Limit |
|---|---|---|
| Hub | Vercel `dpl_GPuq6t2tE6tq55ANmgk28kg3oXgs` READY, production alias `hub.minion-ai.org`, exact `25e2bb75`; includes 360 and ERP PRs through #268 | Deployment success does not mean tests passed or authenticated workflows were accepted |
| Gateway | Main `064b4d34`; deployment run34649530514 succeeded | Current host convergence was not independently re-probed; durable mode remains off by default |
| npm | shared0.12.0 and shells-bridge0.2.0 published; actual tarballs inspected | These are the older package generation: newest sender/envelope repairs remain on meta dev or private candidates |
| Site | Remote master remains `0ed4e1b` | Previous deployment receipt retained; no new authenticated acceptance claimed |
| ERP | Attachment APIs/components, booking editing/linkage/tags, stock/CRM changes and fixtures are shipped | Separate user initiative; excluded from the original 229-task denominator. B2 CORS, sweeper scheduling and authenticated upload/calendar acceptance remain open |

Hub CI run [34682740127](https://github.com/NikolasP98/minion_hub/actions/runs/34682740127): **15 failed, 3,735 passed, 25 skipped**. Build/typecheck and the two CRM PostgreSQL jobs pass. The failed files are `stock.sourced-issue.test.ts` and `api/servers/server.test.ts`. Stock failures involve the fixture missing `archived_at`; this is not evidence that production lacks the column. Server tests include an outdated telemetry expectation and a real raw-error-response failure.

PR268's head test job failed at **08:12:49 UTC**, before merge at **08:13:57 UTC** on September12. Therefore a blanket statement that every merged PR was CI-green is incorrect. This finding concerns release qualification, not an inference about the owner's merge authorization.

## Findings

Line references below are to the exact reviewed commits, not the stale active working trees.

| ID / priority | Source | Trigger, impact and required correction |
|---|---|---|
| R1 High | Gateway `src/shells/manager.ts:658–682`; `src/shells/run-store.ts:369–384` | Receiver checks the authenticated socket's registered shell, but storage selects the outcome's shell ID. A same-org shell with another shell's admission tuple can finalize its run. Reproduced using actual manager + native SQLite: registered shell-a, payload shell-c, accepted receipt shell-c, row committed. Bind outcome shell ID to authenticated shell before persistence and add the same-org adversarial regression. No live production exploit was attempted. |
| R2 High | Hub `src/routes/api/attachments/+server.ts:15–26`, `[fileId]/+server.ts:13–18`, `_guard.ts:14–18`; `rbac.service.ts:1079–1081` | Attachment reads lack linked-record view/owner authorization; writes check module edit but not object existence/owner scope. Within-org users knowing IDs can cross record/module permission boundaries. Enforce target-record authority on list, download, link and unlink; test restricted-role access. Tenant isolation remains present; cross-tenant disclosure was not shown. |
| R3 High | Hub `finance-statements.service.ts:59,178–189,281`; `finance-statement-parser.ts:35` | Parser exports v3 while the service stores/checks v1. Historical v1 cursors can resume under v3 semantics. Adopt the already verified 15-07 binding and its historical-resume regression against the actual integrated jobs foundation. Private candidate completion is not deployment completion. |
| R4 High | Hub `attachments.service.ts:371–399` | Sweeper selects unlinked files, then deletes storage/row without coordinating with later link creation. A link created during that interval can lose its document and leave a dangling reference. Coordinate link admission and deletion through a durable state/lock; add an interleaving test before enabling the currently unscheduled cron. Source-proven race; not exercised against customer data. |
| R5 High | Hub `src/routes/api/servers/+server.ts:59–63`; `server.test.ts:81–85` | Persistence failure returns raw `Error.message`. Hosted CI reproduces a synthetic bound credential appearing in the response instead of the generic error. Restore response sanitization and align the telemetry assertion without weakening the privacy test. This proves a vulnerable error path, not an observed real credential disclosure. |
| R6 Medium | Hub booking PATCH `src/routes/api/scheduling/bookings/[id]/+server.ts:95–127`; `scheduling-bookings.service.ts:684–711` | Reschedule/status/resource writes can commit before later invoice/event-type validation fails. Request returns400 after changing the booking. Validate the complete request first and transact coupled writes/effect admission; test mixed valid/invalid patches. |
| R7 Medium | Gateway `src/gateway/server-methods/shells.ts:377–384`; manager `:383` | Legacy wire handler drops invocationKey before the manager's new rejection. A keyed legacy call can be repeated on retry. Preserve the key for explicit rejection or route through supported durable admission; add a wire-level regression. Existing gap left unclosed by this train. |
| R8 Medium | Hub `vitest.config.ts:20–27`; `.github/workflows/ci.yml` | Generic tests exclude native SQL integration files. Hosted PostgreSQL lanes cover selected CRM tests, not new jobs/stock/finance ownership invariants. Add a disposable marked database lane before claiming continuous coverage. Prior private PostgreSQL evidence remains useful. |

Additional lifecycle gap: booking hard delete still assumes attachment links do not exist (`scheduling-bookings.service.ts:863–865`) and leaves links behind. Address it with R4's reference/deletion lifecycle.

## Quality assessment

**Standards:** partial. Good practices include source hashes, isolated fixtures, refusal of unsupported Windows durable storage, scoped migrations and explicit partial executor summaries. Release qualification falls short because failed tests were merged, key SQL suites are absent from hosted CI, and current planning state was stale.

**Spec:** partial. UI and jobs foundations advanced, raw assistant SQL is disabled before database access, and tenant resolution no longer fabricates organization authority. However parser-version adoption, shell identity binding, attachment record authorization and transactional booking behavior prevent full sign-off. Published bridge0.2.0 lacks durable advertisement/invoke/commit and ACP initialization; shared0.12.0 lacks the newer envelope/parser contract. Package publication alone did not close agent/SDK integration.

106 focused built-in tests were independently rerun and passed. 114/117 inspected claimed source-file hashes matched. Three12-01 snapshot files moved after their receipt; the original after-file remains available, but those hashes do not certify current snapshot bytes. 12-01 SUMMARY also has invalid YAML frontmatter (`base` contains an unquoted colon). No broad test suite with production environment or customer-data probe was run.

## Reassessed progress

[Task-level progress](PROGRESS-2026-09-12.md): **190/229 complete (83.0%)**, previously172/229 (75.1%);32partial;7without complete implementation receipts. Same89plans and51requirements. No whole requirement is independently closed.

These are equal-weight planned deliverables, including private code, research, preparation and diagnostic tasks. They are not effort, security certification, deployed functionality or a weighted product-readiness score. Container25% and capacity50% credit preparation/diagnostics only: no actual container acceptance, application load/SLO certification or real cross-store restore is established. Existing private security credits do not certify the defective deployed boundaries above.

Required next sequence: address R1/R2/R5 authorization/privacy and R3 parser adoption; repair attachment/booking lifetime transactions; restore the full green CI gate and native SQL coverage; then perform authenticated cross-module acceptance. Keep sweeper scheduling pending until its deletion race is fixed. Latest sender/envelope/ACP adoption and real container/load/restore acceptance remain separate work.

Current evidence: [delivery and CI](review-2026-09-12/delivery-evidence.json), [remote status](review-2026-09-12/remote-status.json), [gateway reproduction](review-2026-09-12/gateway-review.json), [source receipt checks](review-2026-09-12/progress-hash-review.json). Full diagnostic logs remain under `/home/nikolas/.cache/minion-qc/review-20260912/`.
