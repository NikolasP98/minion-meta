---
phase: 10-durable-jobs-stock
plan: "03"
type: execute
wave: 2
depends_on: ["10-01","10-02"]
files_modified:
  - "minion_hub/src/server/services/bg-runtime.ts"
  - "minion_hub/src/server/services/groupchat.service.ts"
  - "minion_hub/src/server/services/groupchat.effect-ownership.test.ts"
  - "minion_hub/scripts/qc/disposable-postgres.ts"
  - "minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts"
autonomous: true
requirements: ["JOB-02","JOB-01","STK-01","STK-02"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "No next effect starts after cancellation or lease loss; response-loss replay cannot duplicate the persisted group-chat message."
    - "All race/failure tests actually execute on identified disposable PostgreSQL; tenant isolation and one canonical terminal/effect outcome hold."
  artifacts:
    - path: "minion_hub/src/server/services/bg-runtime.ts"
      provides: "Carry stable effect identity through group-chat job steps"
    - path: "minion_hub/scripts/qc/disposable-postgres.ts"
      provides: "Run real concurrency and crash boundaries in an isolated database"
  key_links:
    - from: "minion_hub/src/server/services/bg-runtime.ts"
      to: "minion_hub/src/server/services/bg-runtime.test.ts"
      via: "Read-first contract and behavior verification for carry stable effect identity through group-chat job steps"
    - from: "minion_hub/scripts/qc/disposable-postgres.ts"
      to: "minion_hub/src/server/services/crm-funnel.concurrent.integration.test.ts"
      via: "Read-first contract and behavior verification for run real concurrency and crash boundaries in an isolated database"
---

<objective>
Qualify handler effect ownership with disposable PostgreSQL.
Purpose: JOB-02, JOB-01, STK-01, STK-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/10-durable-jobs-stock/10-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/src/server/services/bg-runtime.test.ts
@minion_hub/src/server/services/groupchat.service.ts
@minion_hub/src/server/services/crm-funnel.concurrent.integration.test.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export type AdvanceResult = { done: boolean; cursor?: unknown; error?: string };
export type JobHandler = { type: string; advance: (job: BgJob) => Promise<AdvanceResult> };
// Existing public functions: enqueueJob, cancelJobsByRef, advanceJob, runTick.
// createIssueFromInvoice(ctx, input) currently owns invoice-derived stock issue creation.
```
</interfaces>

<admission_amendment date="2026-09-09">
Root approved a JobExecution envelope with stable run/logical-turn effect identity,
AbortSignal and short ownership transactions. Recheck the tenant, generation,
running status and live lease after acquiring the job row lock. Groupchat stores
an admitted/committed checkpoint in the existing server-owned run.settings
namespace and job.cursor; preserve other settings, bind configuration and actual
model payload, and use the stable key as the message primary key. Never hold a
transaction across the provider call. An admitted result without a commit is
indeterminate and cannot automatically call the provider again.

Also approved within groupchat.service.ts: when the handler observes a cancelled
run before cancelJobsByRef completes, revoke its owned job atomically instead of
finishing it done. A late cancellation must not replace a completed run terminal.
Keep existing public route authorization and no additional route/schema ownership.
The actual public cancel route cancels jobs before setting run status. Commit
run/job completion together so cancellation cannot split those terminal records.
Root also approved original-owner indeterminate/conflict finalization: atomically
fail the current job and its run, but fail only a different duplicate job without
inferring the original owner's failure. Owner job/generation are provenance,
never part of stable effect identity. No cross-owner job lock is acquired after
the current job/run locks.
Independent review also admitted a narrow terminal-finalization repair: tag
failure of the known completion transaction, then fail run/job together only
when a fresh ownership transaction verifies our committed checkpoint. Explicit
retry may finish that committed effect without repeating the model call. Unknown
or unreadable ownership and unavailable storage remain reconciliation gaps;
do not add a general exception abstraction or infer domain authority.

Root owns the disposable-postgres.ts helper, the app_ledger fixture role and the
marked/unmarked fixture databases. The test owns only its random marked schema,
tables and connections. Startup search_path must survive reconnect; validate
schema names before client allocation. The approved native server is loopback
port 55439, PostgreSQL 17.10, database minion_qc_jobs_stock, explicit opt-in only.

Root approved isolating intentional backend loss in a child test process. Require
the exact expected child exit/error and independently inspect rollback/retry from
the surviving parent. A known driver crash remains a runtime/dependency defect,
even when stock recovery passes; no global exception suppression is permitted.
Other registered handler domain effects require root-owned child plans before
JOB-02 closes. No dependency edits are admitted to this slice.
</admission_amendment>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Carry stable effect identity through group-chat job steps</name>
  <files>minion_hub/src/server/services/bg-runtime.ts, minion_hub/src/server/services/groupchat.service.ts, minion_hub/src/server/services/groupchat.effect-ownership.test.ts</files>
  <read_first>minion_hub/src/server/services/bg-runtime.test.ts, minion_hub/src/server/services/groupchat.service.ts</read_first>
  <behavior>No next effect starts after cancellation or lease loss; response-loss replay cannot duplicate the persisted group-chat message. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Extend JobHandler.advance with an execution envelope containing job identity, lease generation, abort signal, and stable logical step/effect key. Effect identity must not change when ownership generation changes. Wire groupchat's message persistence/admission against this key and current ownership; distinguish indeterminate remote outcome from retryable pre-effect failure. Discover every registerJobHandler caller and prove compatibility; if another handler needs a source change, add an exact bounded follow-up plan before claiming JOB-02 complete.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/groupchat.effect-ownership.test.ts src/server/services/bg-runtime.test.ts</automated></verify>
  <done>No next effect starts after cancellation or lease loss; response-loss replay cannot duplicate the persisted group-chat message.</done>
</task>

<task type="auto">
  <name>Task 2: Run real concurrency and crash boundaries in an isolated database</name>
  <files>minion_hub/scripts/qc/disposable-postgres.ts, minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts</files>
  <read_first>minion_hub/src/server/services/crm-funnel.concurrent.integration.test.ts</read_first>
  <behavior>All race/failure tests actually execute on identified disposable PostgreSQL; tenant isolation and one canonical terminal/effect outcome hold. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Create a harness that requires an explicitly disposable database URL, validates loopback or test-container identity, rejects known production hosts and never reads normal app .env. Seed two synthetic tenants and parallel clients. Test lease A/B takeover, late A finish, cancel vs progress, two invoice creates, crash after issue commit and retry after ledger commit. Record schema/source hash and cleanup only objects it created. CI command must fail on skipped database cases rather than produce a green no-op.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/job-stock-concurrency.sql.integration.test.ts</automated></verify>
  <done>All race/failure tests actually execute on identified disposable PostgreSQL; tenant isolation and one canonical terminal/effect outcome hold.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Job claimant / database / external effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-10-03-01 | S/E | Stale lease or cross-tenant owner writes | mitigate | Generation predicates, tenant identity, actual concurrency tests and deterministic effect keys. |
| T-10-03-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/10-durable-jobs-stock/10-03-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>
