---
phase: 10-durable-jobs-stock
plan: "08"
type: execute
wave: 3
depends_on: ["10-03"]
status: admitted
autonomous: true
requirements: [JOB-02, AGT-02]
files_modified:
  - minion_hub/vitest.config.ts
  - minion_hub/vitest.disposable.config.ts
  - minion_hub/scripts/qc/test-lanes.test.ts
  - minion_hub/scripts/qc/README.md
  - minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts
  - minion_hub/src/server/services/brain-business-persistence.service.test.ts
  - minion_hub/src/server/services/crm-funnel.concurrent.integration.test.ts
must_haves:
  truths:
    - Default unit discovery excludes SQL integration files even when normal application credentials exist.
    - Disposable qualification discovers only explicitly reviewed marked-runtime tests and fails absent opt-in or identity instead of silently skipping.
    - Existing unit aliases/setup and dependency security fixture discovery remain intact.
  artifacts:
    - path: minion_hub/vitest.disposable.config.ts
      provides: Exact disposable test allowlist with explicit environment gate
    - path: minion_hub/scripts/qc/test-lanes.test.ts
      provides: Actual Vitest discovery regression without importing live SQL tests
---
<objective>Separate offline unit discovery from native database qualification. Current include src/**/*.test.ts also discovers four legacy SQL tests that load normal application environment and the newly mandatory disposable fixture.</objective>
<execution_context>@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@minion_hub/CLAUDE.md
@minion_hub/vitest.config.ts
@minion_hub/scripts/qc/disposable-postgres.ts
@.planning/phases/10-durable-jobs-stock/10-03-VERIFICATION.md</context>
<boundaries>Exact five files only after independent admission. Preserve the dependency owner's tests/dependencies include and existing aliases/setup. No application environment reads, live test execution, new dependency, package/lock edits, production action or automatic commit. Root owns the helpers and config during this slice. Legacy SQL files remain preserved but excluded from the ordinary unit lane; they are not automatically admitted to the disposable lane.</boundaries>
<tasks>
<task type="auto">
<name>Task 1: Prove discovery boundaries and add a dedicated disposable configuration</name>
<files>minion_hub/vitest.config.ts, minion_hub/vitest.disposable.config.ts, minion_hub/scripts/qc/test-lanes.test.ts</files>
<read_first>minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts, minion_hub/src/server/services/brain-business-corpus.sql.integration.test.ts</read_first>
<action>Use installed Vitest list/files-only discovery or its public configuration API to prove default collection excludes every *.sql.integration.test.ts without executing those test modules. Add that exclusion while preserving Vitest default excludes. Add a separate config reusing actual unit plugin/setup/aliases with the sole initial exact allowlist entry src/server/services/job-stock-concurrency.sql.integration.test.ts; do not include future missing files as proof of coverage. Validate explicit MINION_QC_DISPOSABLE and database URL at qualification startup using the shared helper's validation contract, and let each fixture verify the server marker before creating schema. Missing opt-in/URL, nonexistent selected tests, or a filtered empty suite must fail qualification. Test exclusion with synthetic normal-credential environment, valid explicit disposable config, missing/invalid opt-in and legacy file selection. Do not run a live SQL module just to test rejection. Keep dependencies security unit fixtures discoverable; the12-04 native driver fixture is named tests/dependencies/postgres-close-recovery.sql.integration.test.ts. Adding it to the disposable allowlist requires explicit root admission after its source is ready; it is not in the initial allowlist.</action>
<verify><automated>node node_modules/vitest/vitest.mjs run scripts/qc/test-lanes.test.ts</automated></verify>
<done>Actual default/explicit discovery boundaries pass without importing legacy live SQL files or connecting to an application database.</done>
</task>
<task type="auto">
<name>Task 2: Qualify and document the native lane</name>
<files>minion_hub/scripts/qc/README.md, minion_hub/vitest.disposable.config.ts, minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts</files>
<read_first>.planning/research/360-native-postgres-fixture.md, minion_hub/scripts/qc/disposable-postgres.ts</read_first>
<action>Add only the dedicated config argv to the existing job-stock crash child; preserve its logic/assertions and explicit child environment. Document exact unit and disposable commands, approved runtime identity, test allowlist amendment procedure, and the difference between not collecting native tests in unit runs and passing native release qualification. Run the current native job-stock fixture through the explicit config with the marked runtime and rerun small unit discovery/regression cases. Root must admit every later native fixture to this config at ownership freeze; neither wildcard future discovery nor skipped tests establish coverage. Legacy live SQL checks remain separately gated with normal environment forbidden in this operation.</action>
<verify><automated>MINION_QC_DISPOSABLE=1 MINION_QC_DATABASE_URL=postgres://minion_qc@127.0.0.1:55439/minion_qc_jobs_stock node node_modules/vitest/vitest.mjs run --config vitest.disposable.config.ts</automated></verify>
<done>The explicit native fixture actually passes and the default lane cannot discover these credential-loading SQL fixtures.</done>
</task>
</tasks>
<verification>Independent review of actual discovery results, unchanged aliases and source hashes. Do not claim this audits every possible network effect in all unit modules; this exact SQL discovery seam is the bounded contract. Separate normal-env legacy tests remain outside native acceptance.</verification>
<output>Write10-08-SUMMARY and independent10-08-VERIFICATION. Root owns allowlist and proposal; execution follows the admitted exact ownership.</output>

## Reviewed foundation-fixture admission

Root and the independent dependency lane admitted the 10-07 fixture after its source/isolation review and 24-unit/14-native pass. This continuation changes only the already-owned dedicated config, discovery test and QC README: add the exact job-effects SQL fixture beside job-stock, assert the sorted two-file discovery list, and name both fixtures. Preserve every negative selection/identity check, one worker, default exclusions and no-empty-pass policy. Driver and later adoption fixtures remain excluded until separately reviewed. The README describes random isolated schemas inside a marked database; it does not claim that every schema has its own COMMENT marker.

## Root-admitted legacy business fixture quarantine

Preflight found brain-business-persistence.service.test.ts resolves SUPABASE_DB_URL with loadEnv at module load and conditionally runs live EXPLAINs despite its ordinary unit filename. The SQL-suffix exclusion does not cover it. Under this narrow amendment root owns vitest.config.ts, scripts/qc/test-lanes.test.ts and scripts/qc/README.md for exact-file exclusion before import and a files-only discovery regression. Add only a TODO comment immediately before the legacy test's environment resolution; no test-body changes or execution of that module are admitted. Preserve its four offline persistence cases for a subsequent exact split/marked-native migration; they are quarantined, not passed or deleted. Do not add it to the disposable allowlist or rely on absent credentials. Native qualified files and aliases stay unchanged. No full-suite run until remaining application-environment tests are inventoried.

## Root-admitted concurrent CRM fixture quarantine

The bounded remaining-test search found crm-funnel.concurrent.integration.test.ts also calls loadEnv at module load and selects an existing organization before synthetic insert/update work. It is not covered by the SQL-suffix pattern. Extend the same exact-file default exclusion/discovery regression and QC README to this path; add a comment-only TODO at its environment-resolution site. No test body, schema, CI or disposable allowlist change; never execute this module against normal application configuration. Preserve its concurrency cases for a separately owned reproducible full-schema fixture. Other matches in pg-pool/pg-client unit tests are explicit mocked environments; the search does not by itself certify all transitive network behavior.
