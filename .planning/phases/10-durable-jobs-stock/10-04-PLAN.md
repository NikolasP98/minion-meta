---
phase: 10-durable-jobs-stock
plan: "04"
type: execute
status: admitted_execution_gated
execution_ready: false
wave: 4
depends_on: ["10-03","10-07"]
files_modified:
  - "minion_hub/src/server/services/finance-statements.service.ts"
  - "minion_hub/src/server/services/finance-statements.service.test.ts"
  - "minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts"
  - "minion_hub/src/routes/api/finances/statement-imports/[id]/retry/+server.ts"
autonomous: true
requirements: ["JOB-01", "JOB-02"]
closure_policy: "Independent admission and actual behavior evidence required; no global requirement closure from this plan."
must_haves:
  truths:
    - "Only the current job owner and current import request revision can persist a chunk, counters, failure state or progress."
    - "Undo/retry invalidates old work even when nextChunk returns to zero; duplicate and restarted jobs cannot resurrect an undone import or double-count rows."
    - "File reads occur outside owned database transactions and cancellation stops subsequent domain writes; RLS remains enforced."
  artifacts:
    - path: "minion_hub/src/server/services/finance-statements.service.ts"
      provides: "Revision-bound deterministic import chunks and serialized reset/enqueue"
    - path: "minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts"
      provides: "Native-engine chunk/undo/retry/tenant concurrency evidence"
  key_links:
    - from: "minion_hub/src/server/services/finance-statements.service.ts"
      to: "minion_hub/src/server/services/job-effects.service.ts"
      via: "Shared entity-head revision and owned tenant transaction; no paid-result receipt"
    - from: "minion_hub/src/server/services/finance-statements.service.ts"
      to: "minion_hub/src/server/db/pg-finance-schema.ts"
      via: "Existing nextChunk and import/source-row uniqueness stay the deterministic progress contract"
    - from: "minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts"
      to: "minion_hub/src/server/services/finance-statements.service.ts"
      via: "Real runtime and service with controlled file fetch on two native connections"
---

<objective>
Fence statement import chunks and explicit undo/retry requests while preserving financial parsing and accounting policy.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Use the installed runtime above; the catalog's ~/.Codex runtime path is absent.
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-durable-jobs-stock/10-REMAINING-HANDLER-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-03-PLAN.md
@.planning/phases/10-durable-jobs-stock/10-03-SUMMARY.md
@minion_hub/src/server/services/bg-runtime.ts
@minion_hub/src/server/db/with-org-core.ts
@minion_hub/scripts/qc/disposable-postgres.ts
</context>

<boundaries>
This plan is independently admitted; execution requires the stated prerequisites and exact ownership. Root owns canonical planning, admission, proposals, fixture server and release decisions. Before source edits, require admitted exact PLAN hash, dependency summaries and exclusive file ownership. Preserve all dirty/concurrent work; no commit, stash, branch/worktree operation, package/lock change, production migration, paid call or deployment. New implementation seams require exact amendments before edits.
The phase goal covers jobs AND stock; these children address only the stated JOB-01/JOB-02 gaps. They cannot close stock, the full phase, deployed behavior or every historical proposal.
Use the marked, explicitly opted-in native PostgreSQL helper. Never load application .env or use SUPABASE_DB_URL for fixtures. Preserve server/database/user/marker checks, random validated schema ownership, independent connections and cleanup. Missing fixture/extension, skipped cases or a watchdog expiry are pending evidence, never green. Backend-kill qualification depends on completed 12-04; ordinary two-connection tests may run earlier. No fault injection outside the recorded child backend.
Keep provider work outside database transactions. A committed admission with no durable response is indeterminate; do not automatically replay a paid call after lost response, cancellation or takeover. Cancellation fences subsequent admission and database effects; it cannot undo a remote request already dispatched.
Any unfinished source behavior requires an exact TODO(handoff) and proposal text sent to root for the ledger. Remove existing registration TODOs only when the entire corresponding handler path is qualified. Source fixes and tests do not establish release readiness.
</boundaries>

<prerequisite>
@.planning/phases/10-durable-jobs-stock/10-07-PLAN.md
@.planning/phases/10-durable-jobs-stock/10-07-SUMMARY.md
The implemented and independently verified shared foundation must freeze first. Its new schema must be installed in the disposable target. Consume the admitted interfaces; any required change to that foundation needs a separate exact ownership amendment.
</prerequisite>

<source_evidence>
persistImportChunk at finance-statements.service.ts:199 fetches/parses inside withOrgCore, inserts rows and conditionally updates nextChunk without checking a returned CAS row. undoImport:370 accepts queued work, deletes transactions and resets nextChunk; retryImport:337 resets status and enqueues separately. finStatementImports has no request epoch or updatedAt. Status/cursor alone cannot distinguish old work from a new reset-to-zero request. The root accepted shared entity-head revisions for this ABA boundary; no new finance column or paid response ledger is required.
</source_evidence>

<tasks>
<task type="auto">
  <name>Task 1: Bind import/reset/enqueue to one durable request revision</name>
  <files>minion_hub/src/server/services/finance-statements.service.ts, minion_hub/src/server/services/finance-statements.service.test.ts</files>
  <read_first>minion_hub/src/server/db/pg-finance-schema.ts, minion_hub/supabase/migrations/20260722234500_fin_statement_imports.sql, minion_hub/src/server/services/job-effects.service.ts</read_first>
  <behavior>A first chunk paused before persistence loses authority after undo/retry; old jobs cannot attach to the new revision even with identical nextChunk/content.</behavior>
  <action>Write reset/queued-first-chunk and duplicate-job regressions. Wire JobExecution through the registered handler and deterministic chunk helper. Use the shared head revision in initial import enqueue, explicit retry, undo and existing content-dedup return paths where jobs can be created. Keep content SHA/parser version as immutable input identity and nextChunk as progress. Advance revision, reset domain state and enqueue the new revision-bound job in one transaction; undo invalidates the head with its deletions and counters. Preserve existing authorized context and established rejection behavior for actively parsing imports. Do not call cancelJobsByRef as a substitute for atomic revision fencing or acquire old job rows after taking a head lock. Missing/legacy cursor revision requires bounded foundation adoption, not latest-head rebinding.
Read/hash/parse the file outside a database transaction, using the execution signal for fetch and checking it before parsing/commit. Recheck current owner, head revision, import state, content identity/parser version and expected cursor in a short owned RLS transaction. Insert accepted rows, counters, nextChunk and durable job progress together. Lock/recheck the domain row or reject a failed CAS and roll back the entire chunk; never commit inserted rows with an unverified cursor update. Gate parse-failure persistence too. Treat stale ownership as revocation, not parse failure.
Do not change CHUNK_SIZE, parseStatementCsv, money/currency handling, confidence, rejection definitions, source-row uniqueness or content dedup policy. Blob-upload orphan cleanup and format support remain outside this slice; preserve any known source TODO and send root precise proposal text rather than claiming those problems fixed.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/finance-statements.service.test.ts</automated></verify>
  <done>All chunk/failure/reset paths use the current owned revision without changing parser/accounting output.</done>
</task>
<task type="auto">
  <name>Task 2: Prove import effect and reset races on PostgreSQL</name>
  <files>minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts</files>
  <read_first>minion_hub/src/server/services/job-effects.sql.integration.test.ts, minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts</read_first>
  <behavior>Concurrent chunks preserve exact inserted/rejected counts; undo/retry cannot resurrect stale work; wrong-tenant input is denied by RLS.</behavior>
  <action>Use actual import/transaction schema constraints and shared receipt-head DDL in marked fixtures with synthetic CSV and controlled fetch. Execute two jobs for one import, one owner takeover, cancel during fetch, failed parse after revocation, rollback between insert and cursor update, commit followed by lost handler response/restart, and queued first-chunk versus undo then retry. Assert domain rows, counters, status, head revision and job cursor independently. Include partial duplicate rows and malformed CSV expected by existing parser tests. Confirm no database transaction stays open while fetch is blocked, no old job admits work for a later revision, no cross-org read/write and no role leak. Test native transaction rollback before driver repair; actual backend termination waits for 12-04 and must settle normally.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/finance-statements.effect-ownership.sql.integration.test.ts</automated></verify>
  <done>Every named race runs on the identified engine and has one consistent domain/progress outcome; no skips or swallowed driver faults.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Required control |
|---|---|
| Job owner and domain state | Current tenant/lease plus shared request revision, short transactions and one lock order. |
| Tenant-scoped SQL and privileged job bookkeeping | Existing-transaction RLS scope with verified restoration; no broad bg_jobs grant. |
| Local admission and remote provider outcome | Pinned request and durable response receipt; ambiguous outcome never permits automatic paid replay. |
| Fixture and application infrastructure | Marker/identity checks and synthetic data; no normal environment fallback or production fault injection. |

## STRIDE register

| Threat | Category | Disposition |
|---|---|---|
| Forged tenant, receipt key or revision | S/E | Derive authority from current execution and authorized context; constrained identities and FORCE RLS. |
| Stale worker overwrites newer result/progress | T | Recheck locked request head and domain/source identity; commit effects with progress. |
| Secret/source payload retained in receipt or evidence | I | Store approved hashes/vector results and bounded metadata; exclude credentials/raw source payloads. |
| Duplicate/ambiguous provider retry or blocked lock | R/D | Durable admission/result states, bounded waits, explicit attempt count and no locks during external calls. |

These controls are acceptance requirements, not claims that the draft implements them.
</threat_model>

<verification>
Use red-green behavior tests and actual native-engine concurrency with synthetic tenants. Fake only the remote provider/file response at its network boundary; execute the real handler, receipt service, runtime and database paths. Observe actual outbound calls, transaction visibility and final rows; implementation-shaped mocks alone do not prove fencing.
Run the named focused tests with the installed Node/Vitest runner and Hub's Bun check/build commands in the root-provided environment-free validation copy. Do not run the existing brain-business-corpus.sql.integration.test.ts against application environment: it loads normal credentials and is outside these fixtures. Root coordinates full isolated Hub regression checks after file ownership freezes. Required check/build errors remain unresolved until repaired through admitted scope.
Validate schema/DDL agreement where applicable, git diff --check, source/artifact hashes and exact dependency/runtime identities. Independently review Standards (scope, RLS, isolation, compatibility) and Spec (all stated outcomes and failure races).
</verification>

<output>
Create .planning/phases/10-durable-jobs-stock/10-04-SUMMARY.md with admitted plan hash, source before/after hashes, named behavioral outcomes, exact commands/runtime, remaining gates and TODO/proposal text for root. Freeze files for independent verification. Do not update global planning or commit.
</output>

## Root-admitted legacy recovery boundary

Unversioned imports fail closed before file retrieval or domain writes; a nonzero cursor does not establish which request admitted an old job. The existing retry endpoint is the explicit recovery operation. Extend retryImport to queued/parsing/failed/undone states: a new revision supersedes old work while retaining persisted rows, counters and nextChunk. Done imports remain no-op. Re-read status under head/domain locks; a stale pre-read must not reset a concurrently completed import. Concurrent explicit retries serialize and only the latest revision may advance. Parsing undo remains HTTP 409.

The only additional owned file is src/routes/api/finances/statement-imports/[id]/retry/+server.ts, for its outdated API comment only; authentication, module and personal-org checks remain unchanged. Task 1 includes that comment correction and tests for legacy queued/parsing recovery, preserved progress, completed no-op/race and superseded retries. No new UI recovery control is claimed. Shared foundation/config ownership stays with root; any required interface change needs a separate amendment.
