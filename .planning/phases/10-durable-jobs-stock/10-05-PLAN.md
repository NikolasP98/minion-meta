---
phase: 10-durable-jobs-stock
plan: "05"
type: execute
status: admitted_execution_gated
execution_ready: false
wave: 4
depends_on: ["10-03","10-07"]
files_modified:
  - "minion_hub/src/server/services/brains.service.ts"
  - "minion_hub/src/server/services/brains.service.test.ts"
  - "minion_hub/src/server/services/brains.effect-ownership.sql.integration.test.ts"
autonomous: true
requirements: ["JOB-01", "JOB-02"]
closure_policy: "Independent admission and actual behavior evidence required; no global requirement closure from this plan."
must_haves:
  truths:
    - "Duplicate/taken-over brain jobs cannot repeat an indeterminate embedding call or publish chunks for a superseded/deleted document request."
    - "Received batches can resume without another paid attempt; document chunks/status and effect completion commit under current ownership and RLS."
    - "Cancellation reaches URL loading and embeddings while existing access checks, SSRF protection, chunking and vector semantics remain intact."
  artifacts:
    - path: "minion_hub/src/server/services/brains.service.ts"
      provides: "Revision-aware document lifecycle and receipt-backed ingestion"
    - path: "minion_hub/src/server/services/brains.effect-ownership.sql.integration.test.ts"
      provides: "Native brain ingestion/reset/provider failure qualification"
  key_links:
    - from: "minion_hub/src/server/services/brains.service.ts"
      to: "minion_hub/src/server/services/job-effects.service.ts"
      via: "Durable head/revision and per-batch admitted/received/committed receipts"
    - from: "minion_hub/src/server/services/brains.service.ts"
      to: "minion_hub/src/server/services/embeddings.ts"
      via: "Pinned normalized request plus caller signal and explicit single-attempt job policy"
    - from: "minion_hub/src/server/services/brains.effect-ownership.sql.integration.test.ts"
      to: "minion_hub/src/server/services/brains.service.ts"
      via: "Actual runtime, RLS document/chunk schema and fake outbound provider"
---

<objective>
Make brain_ingest use durable request revisions, bounded embedding receipts and atomic tenant-scoped document publication.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Use the installed runtime above; the catalog's ~/.Codex runtime path is absent.
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-durable-jobs-stock/10-REMAINING-HANDLER-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-03-PLAN.md
@.planning/phases/10-durable-jobs-stock/10-03-SUMMARY.md
@minion_hub/src/server/services/bg-runtime.ts
@minion_hub/src/server/db/with-org-core.ts
@minion_hub/scripts/qc/disposable-postgres.ts
</context>

<boundaries>
This plan is independently admitted; execution requires the stated prerequisites and exact ownership. Root owns canonical planning, admission, proposals, fixture server and release decisions. Before source edits, require admitted exact PLAN hash, dependency summaries and exclusive file ownership. Preserve all dirty/concurrent work; no commit, stash, branch/worktree operation, package/lock change, production migration, paid call or deployment. New implementation seams require exact amendments before edits.
The phase goal covers jobs AND stock; these children address only the stated JOB-01/JOB-02 gaps. They cannot close stock, the full phase, deployed behavior or every historical proposal.
Use the marked, explicitly opted-in native PostgreSQL helper. Never load application .env or use SUPABASE_DB_URL for fixtures. Preserve server/database/user/marker checks, random validated schema ownership, independent connections and cleanup. Missing fixture/extension, skipped cases or a watchdog expiry are pending evidence, never green. Backend-kill qualification depends on completed 12-04; ordinary two-connection tests may run earlier. No fault injection outside the recorded child backend.
Keep provider work outside database transactions. A committed admission with no durable response is indeterminate; do not automatically replay a paid call after lost response, cancellation or takeover. Cancellation fences subsequent admission and database effects; it cannot undo a remote request already dispatched.
Any unfinished source behavior requires an exact TODO(handoff) and proposal text sent to root for the ledger. Remove existing registration TODOs only when the entire corresponding handler path is qualified. Source fixes and tests do not establish release readiness.
</boundaries>

<prerequisite>
@.planning/phases/10-durable-jobs-stock/10-07-PLAN.md
@.planning/phases/10-durable-jobs-stock/10-07-SUMMARY.md
The implemented and independently verified shared foundation must freeze first. Its new schema must be installed in the disposable target. Consume the admitted interfaces; any required change to that foundation needs a separate exact ownership amendment.
</prerequisite>

<source_evidence>
advanceBrainIngest at brains.service.ts:751 updates ingesting, loads/chunks/embeds, replaces chunks and catches errors into failed across separate unowned transactions. embedInBatches:544 does not receive cancellation. addDocument:292 and reingestDocument:381 enqueue separately from domain mutation; removeDocument:350 deletes without invalidating queued work. brain_documents has no private receipt/settings column; receipt data belongs in 10-07's shared table.
</source_evidence>

<tasks>
<task type="auto">
  <name>Task 1: Bind explicit document requests and every ingestion effect</name>
  <files>minion_hub/src/server/services/brains.service.ts, minion_hub/src/server/services/brains.service.test.ts</files>
  <read_first>minion_hub/src/server/services/job-effects.service.ts, minion_hub/src/server/services/embeddings.ts, minion_hub/src/server/db/pg-schema/brains.ts</read_first>
  <behavior>Reingest/remove during blocked fetch or embedding invalidates old work; a received batch is reused after restart; an admission without a durable response never calls the provider again automatically.</behavior>
  <action>Write document-reset/duplicate/response-loss regressions first. Create the shared head request revision with addDocument/reingestDocument and enqueue atomically; removeDocument invalidates it atomically with existing deletion. Preserve requireAccess, principal, actor and audit behavior. Old jobs must not silently adopt a newly requested revision; use the explicit legacy-adoption contract for preexisting queued work.
Carry JobExecution through loading, batch preparation, admission, receipt persistence, final publication and failure handling. Keep the existing document chunking algorithm and 64-input batches; each batch has a stable logical unit and pinned source/chunking/provider descriptor. Persist received vectors per batch under ownership so multi-batch restart does not repeat completed requests. Do not replace published chunks until the full current document revision has valid results; chunk replacement, ready status, receipt commit and progress are one owned RLS transaction. Recheck document existence/source identity/current revision. Receipt result vectors are tenant-protected data and bounded by existing batch/dimension rules.
Compose the job signal with the existing URL timeout and retain manual redirect/SSRF validation at every hop. No locks remain held while loading or embedding. Pre-effect deterministic errors may use existing failed status under current ownership; ownership loss, stale revision and indeterminate remote outcomes must remain distinct and must not overwrite the newer request's status. No provider retry or new UI/operator workflow is inferred for indeterminate work; preserve its durable state and return a stable diagnosable failure for later reviewed recovery.
Keep query/search embedding callers compatible; this plan adopts only brain_ingest, not all independent semantic-search activity.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/brains.service.test.ts src/server/services/embeddings.test.ts</automated></verify>
  <done>Document creation/reset/removal and all job write/provider boundaries are tied to the admitted request revision.</done>
</task>
<task type="auto">
  <name>Task 2: Exercise native ingestion ownership and real vector persistence</name>
  <files>minion_hub/src/server/services/brains.effect-ownership.sql.integration.test.ts</files>
  <read_first>minion_hub/src/server/services/job-effects.sql.integration.test.ts, minion_hub/supabase/migrations/20260702120000_brains.sql</read_first>
  <behavior>Real concurrent database operations prove atomic vector/chunk publication, no stale replacement, isolated tenants and no duplicate outbound attempts.</behavior>
  <action>Require marked PostgreSQL with the actual pgvector extension and production-equivalent document/chunk constraints/RLS; root owns provisioning. A text stand-in for vector storage or an in-memory-only fixture does not qualify this task. Run synthetic note and controlled URL cases with fake valid 1536-dimension provider vectors. Cover duplicate jobs, multiple batches, received-batch restart, response loss before receipt, cancel while provider ignores abort, takeover, reingest/delete races, failed final transaction, and source/config mismatch. Assert outbound request bodies/counts, head/effect rows, old published chunks until atomic commit, final job progress and cross-org denial. Retain existing source-type/chunking/access tests. Native backend-loss acceptance waits for repaired driver12-04; absent pgvector keeps this task pending rather than silently skipping it.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/brains.effect-ownership.sql.integration.test.ts</automated></verify>
  <done>Actual vector/database and caller-settlement evidence meets every named case, with provider activity entirely synthetic.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Required control |
|---|---|
| Job owner and domain state | Current tenant/lease plus shared request revision, short transactions and one lock order. |
| Tenant-scoped SQL and privileged job bookkeeping | Existing-transaction RLS scope with verified restoration; no broad bg_jobs grant. |
| Local admission and remote provider outcome | Pinned request and durable response receipt; ambiguous outcome never permits automatic paid replay. |
| Fixture and application infrastructure | Marker/identity checks and synthetic data; no normal environment fallback or production fault injection. |

## STRIDE register

| Threat | Category | Disposition |
|---|---|---|
| Forged tenant, receipt key or revision | S/E | Derive authority from current execution and authorized context; constrained identities and FORCE RLS. |
| Stale worker overwrites newer result/progress | T | Recheck locked request head and domain/source identity; commit effects with progress. |
| Secret/source payload retained in receipt or evidence | I | Store approved hashes/vector results and bounded metadata; exclude credentials/raw source payloads. |
| Duplicate/ambiguous provider retry or blocked lock | R/D | Durable admission/result states, bounded waits, explicit attempt count and no locks during external calls. |

These controls are acceptance requirements, not claims that the draft implements them.
</threat_model>

<verification>
Use red-green behavior tests and actual native-engine concurrency with synthetic tenants. Fake only the remote provider/file response at its network boundary; execute the real handler, receipt service, runtime and database paths. Observe actual outbound calls, transaction visibility and final rows; implementation-shaped mocks alone do not prove fencing.
Run the named focused tests with the installed Node/Vitest runner and Hub's Bun check/build commands in the root-provided environment-free validation copy. Do not run the existing brain-business-corpus.sql.integration.test.ts against application environment: it loads normal credentials and is outside these fixtures. Root coordinates full isolated Hub regression checks after file ownership freezes. Required check/build errors remain unresolved until repaired through admitted scope.
Validate schema/DDL agreement where applicable, git diff --check, source/artifact hashes and exact dependency/runtime identities. Independently review Standards (scope, RLS, isolation, compatibility) and Spec (all stated outcomes and failure races).
</verification>

<output>
Create .planning/phases/10-durable-jobs-stock/10-05-SUMMARY.md with admitted plan hash, source before/after hashes, named behavioral outcomes, exact commands/runtime, remaining gates and TODO/proposal text for root. Freeze files for independent verification. Do not update global planning or commit.
</output>

## Root-admitted legacy recovery clarification

All unversioned brain_ingest jobs are rejected before source loading/provider calls. A pending document has no immutable historical admission identity, so pending-only legacy adoption is not proof. Existing explicit reingestDocument and its authorized route already accept the needed recovery states; the adopted atomic request/domain/enqueue path creates a fresh revision. Add native legacy rejection and explicit reingest recovery evidence. No new route or shared-foundation seam is admitted. The qualified vector runtime is recorded in .planning/research/360-pgvector-fixture.md; actual namespace/type/operator qualification remains mandatory.
