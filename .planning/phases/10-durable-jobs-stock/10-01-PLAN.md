---
phase: 10-durable-jobs-stock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "minion_hub/src/server/db/pg-schema/bg-jobs.ts"
  - "minion_hub/supabase/migrations/20260909090100_bg_job_lease_generation.sql"
  - "minion_hub/src/server/services/bg-runtime.test.ts"
  - "minion_hub/src/server/services/bg-runtime.ts"
autonomous: true
requirements: ["JOB-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Old owner writes affect zero rows after takeover; migration preserves existing records and rejects invalid generations."
    - "A call longer than the lease remains owned while healthy; abandoned owner can be reclaimed; no interval survives completion/cancel/error."
  artifacts:
    - path: "minion_hub/src/server/db/pg-schema/bg-jobs.ts"
      provides: "Define generation-aware lease storage and denial fixtures"
    - path: "minion_hub/src/server/services/bg-runtime.ts"
      provides: "Wire fenced updates and heartbeat cleanup"
  key_links:
    - from: "minion_hub/src/server/db/pg-schema/bg-jobs.ts"
      to: "minion_hub/src/server/services/bg-runtime.ts"
      via: "Read-first contract and behavior verification for define generation-aware lease storage and denial fixtures"
    - from: "minion_hub/src/server/services/bg-runtime.ts"
      to: "minion_hub/src/server/services/finance-statements.service.ts"
      via: "Read-first contract and behavior verification for wire fenced updates and heartbeat cleanup"
---

<objective>
Fence background leases and heartbeat admitted work.
Purpose: JOB-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/10-durable-jobs-stock/10-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/src/server/services/bg-runtime.ts
@minion_hub/src/server/db/pg-schema/bg-jobs.ts
@minion_hub/src/server/services/finance-statements.service.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export type AdvanceResult = { done: boolean; cursor?: unknown; error?: string };
export type JobHandler = { type: string; advance: (job: BgJob) => Promise<AdvanceResult> };
// Existing public functions: enqueueJob, cancelJobsByRef, advanceJob, runTick.
// createIssueFromInvoice(ctx, input) currently owns invoice-derived stock issue creation.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Define generation-aware lease storage and denial fixtures</name>
  <files>minion_hub/src/server/db/pg-schema/bg-jobs.ts, minion_hub/supabase/migrations/20260909090100_bg_job_lease_generation.sql, minion_hub/src/server/services/bg-runtime.test.ts</files>
  <read_first>minion_hub/src/server/services/bg-runtime.ts, minion_hub/src/server/db/pg-schema/bg-jobs.ts</read_first>
  <behavior>Old owner writes affect zero rows after takeover; migration preserves existing records and rejects invalid generations. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Add a nonnegative lease generation/token and atomic claim increment; preserve existing jobs through an additive migration. Write fake-clock and real-Postgres fixtures for expired lease takeover, queued claim race and cancellation before any owner update. All heartbeat/progress/finalization predicates must include id, running status and the generation returned by claim. Existing callback advance(job) must remain compatible until plan10-03 wires the handler envelope; do not call production migration commands.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/bg-runtime.test.ts</automated></verify>
  <done>Old owner writes affect zero rows after takeover; migration preserves existing records and rejects invalid generations.</done>
</task>

<task type="auto">
  <name>Task 2: Wire fenced updates and heartbeat cleanup</name>
  <files>minion_hub/src/server/services/bg-runtime.ts, minion_hub/src/server/services/bg-runtime.test.ts</files>
  <read_first>minion_hub/src/server/services/finance-statements.service.ts</read_first>
  <behavior>A call longer than the lease remains owned while healthy; abandoned owner can be reclaimed; no interval survives completion/cancel/error. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Return a lease handle from claim, thread it through progress/finish, and heartbeat while advance is pending. Stop the heartbeat in finally, stop further admission when lease is lost, and distinguish handler deadline from proof of effect termination. Inject time/storage seams only where fixtures require them. On cancellation revoke the generation and preserve cancelled terminal state against late success or failure. No process-global tenant fallback.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/bg-runtime.test.ts</automated></verify>
  <done>A call longer than the lease remains owned while healthy; abandoned owner can be reclaimed; no interval survives completion/cancel/error.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Job claimant / database / external effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-10-01-01 | S/E | Stale lease or cross-tenant owner writes | mitigate | Generation predicates, tenant identity, actual concurrency tests and deterministic effect keys. |
| T-10-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/10-durable-jobs-stock/10-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

