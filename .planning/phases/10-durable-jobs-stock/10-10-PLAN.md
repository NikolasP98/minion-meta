---
phase: 10-durable-jobs-stock
plan: "10"
type: execute
status: source_implementation_admitted
execution_ready: true
wave: 6
depends_on: ["10-07", "10-09"]
files_modified:
  - "minion_hub/src/server/db/pg-schema/job-effects.ts"
  - "minion_hub/src/server/db/pg-schema/job-effect-pages.ts"
  - "minion_hub/src/server/services/job-effects.service.ts"
  - "minion_hub/src/server/services/job-effect-pages.service.ts"
  - "minion_hub/src/server/services/job-effect-pages.service.test.ts"
  - "minion_hub/src/server/services/job-effect-pages.sql.integration.test.ts"
  - "minion_hub/supabase/migrations/20260909090500_job_effect_page_batches.sql"
autonomous: false
requirements: ["JOB-01", "JOB-02"]
closure_policy: "Foundation qualification only; corpus adoption, worker governance, deployment and whole-phase requirements remain open."
must_haves:
  truths:
    - "Overlapping pages reuse canonical semantic units while missing units retain cross-document64-input batching."
    - "Any admitted ambiguous overlap prevents all new paid work for that page attempt."
    - "Unsent orphan reservations recover through nonblocking owner prelocks and immutable abandonment/repacking; admitted replay never renews dispatch permission."
    - "Only the current dispatch lease owner retains the complete immutable response; partial member supersession never confers publication authority."
    - "Each page publishes its complete current source set and canonical progress atomically, independently of another page consuming a shared transport response."
    - "Full source/provider/payload manifests, ordered locks and tenant constraints prevent incompatible reuse or cross-tenant publication."
    - "Full canonical/source/prepared/foreign-closure limits and bounded historical projection reject oversize work without partial reservation/publication; existing single-head APIs remain compatible."
  artifacts:
    - path: "minion_hub/src/server/db/pg-schema/job-effect-pages.ts"
      provides: "Constrained page, transport receipt and semantic unit declarations"
    - path: "minion_hub/supabase/migrations/20260909090500_job_effect_page_batches.sql"
      provides: "Additive tenant RLS, reference keys, immutable transitions and complete batch membership validation"
    - path: "minion_hub/src/server/services/job-effect-pages.service.ts"
      provides: "Owned page binding, atomic missing-work admission, historical response retention and page publication"
    - path: "minion_hub/src/server/services/job-effect-pages.sql.integration.test.ts"
      provides: "Actual PostgreSQL overlap, owner, supersession, publication and RLS races"
  key_links:
    - from: "minion_hub/src/server/services/job-effect-pages.service.ts"
      to: "minion_hub/src/server/services/job-effects.service.ts"
      via: "Additive owned-scope transaction and canonical existing head identity"
    - from: "minion_hub/src/server/services/job-effect-pages.service.ts"
      to: "minion_hub/src/server/services/embeddings.ts"
      via: "Validate and dispatch the same private prepared request exactly once per admitted batch"
    - from: "minion_hub/src/server/services/job-effect-pages.service.ts"
      to: "minion_hub/src/server/db/pg-schema/job-effect-pages.ts"
      via: "Separate page consumption from immutable transport response and canonical unit mapping"
    - from: "minion_hub/src/server/services/job-effect-pages.sql.integration.test.ts"
      to: "minion_hub/supabase/migrations/20260909090500_job_effect_page_batches.sql"
      via: "Actual authored903/904-to905 upgrade, constraints and tenant role qualification"
---

<objective>
Provide the shared effect foundation needed for corpus jobs to retain existing cross-document batching and page atomicity while safely reusing vectors across overlapping pages. Qualify ownership and semantic authority separately from immutable provider-response retention.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-durable-jobs-stock/10-10-API-SCHEMA-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-10-PLAN-REVIEW.md
@.planning/phases/10-durable-jobs-stock/10-10-CAPACITY-RECEIPT.md
@.planning/DECISIONS.md
@.planning/phases/10-durable-jobs-stock/10-REMAINING-HANDLER-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-07-VERIFICATION.md
@.planning/phases/10-durable-jobs-stock/10-09-PLAN.md
@.planning/phases/10-durable-jobs-stock/10-06-PLAN.md
@minion_hub/src/server/services/bg-runtime.ts
@minion_hub/src/server/services/embeddings.ts
@minion_hub/src/server/db/with-org-core.ts
@minion_hub/scripts/qc/disposable-postgres.ts
</context>

<admission_and_ownership>
Root-reviewed source candidate. D360-13 now selects numerical input/closure limits, nonblocking foreign-owner prelocks and immutable unsent abandonment/repacking, alongside historical complete response retention by the current dispatch owner. The54-scenario capacity receipt exists. This draft adds a selected16 MiB cumulative projected vector/metadata limit and records the unused905 filename reservation; no SQL file exists. Root independently accepts the final API/schema packet with the16 MiB result bound and distinct owner_missing recovery-required outcome. Source and authored additive SQL creation are admitted within the exact seven files; native SQL execution remains gated on fixture/config review. No production DDL or corpus/worker adoption is admitted.

After admission, source ownership is exactly the seven files listed above plus10-10-SUMMARY.md. Existing job-effects.ts schema changes are additive reference-key changes only. Existing job-effects.service.ts changes are the new owned-scope helper, its bounded nonblocking bookkeeping-role owner prelocks/typed busy error, and head-ID helper only. Preserve existing single-head signatures and behavior. No changes to embeddings, runtime, RLS helper, corpus services, barrel, config, package/lock, older migrations or existing fixture migration lists. If feasibility requires another seam, report its exact file and await a reviewed scope amendment.

Root owns global planning, proposals, test-lane/config admission, disposable servers and release decisions. The draft author owns only this PLAN and its API/schema packet. Preserve concurrent dirty work. No commit/branch/stash/worktree operations, production SQL, live providers, Qdrant calls, backend termination or fault-injection workaround. Capacity work uses only root-approved temporary artifacts and bounded synthetic data. Resource windows must be coordinated.
</admission_and_ownership>

<design_contract>
The accompanying API/schema packet is normative for review. It specifies three related tables in the existing shared foundation, complete source manifests, canonical unit identity independent of job/page, exact transport identity, per-page publication state, ownership-scoped helpers and the proposed public API. No independent store, scheduler or provider abstraction is added.

Preserve64-input cross-document batching and cap this new API at four active calls; existing corpus concurrency configuration is unchanged. Inspect the entire required set and bounded foreign closure before any new reservation/admission. With own-job ownership already locked, prelock foreign owner rows nonblockingly under bookkeeping role before any head/domain lock. Locked/live owners yield busy; a missing tenant-scoped owner yields owner_missing/recovery_required without reclaim or endless retry; a frontier change rolls back and yields canonical progress. Never acquire a new foreign lock after heads. Generation/status/lease invalidation is proved under those prelocks, with no broad app_ledger job grants.

Resume an exact fully revalidated reserved batch or atomically tombstone it abandoned_unsent, clear all old placements, and repack only current required units across documents. Preserve immutable batch membership snapshots; admitted/received mappings never move. Reserve all missing batches atomically, then commit each reserved→admitted conditional transition immediately before its privately prepared attempt. Only the successful conditional transition plus outer commit yields a one-use in-memory dispatch permit. Admitted replay, zero-row CAS or failed commit grants none. Interrupted reservations are not remote uncertainty; admitted-without-receipt remains indeterminate and is never retransmitted automatically.

Persist a returned complete transport response only under its current original dispatch job/generation and immutable descriptor/membership. Semantic member supersession does not block that historical retention. It does block publication and new dispatch for the superseded member. Response retention does not mark all batch members published. Page publication asserts every current source/head/manifest and atomically writes domain data, applicable deletions, member consumption, page state and canonical job progress. Same-page published replay skips its callback; another page's consumption does not.

Bind zero-paid-work disabled, empty and qdrant modes through the same current source/page authority. Qdrant generation is pinned in the manifest when applicable, but actual Qdrant RPC/triggers and consumer adoption remain10-06. Unchanged chunk/vector omission requires host source validity; status ready alone is insufficient. Existing source loading and pagination policy remain unchanged.

Selected limits:64 current heads;256 full canonical/required units;2,097,152 full-source UTF-16 code units and6 MiB UTF-8;256 KiB canonical descriptor;3 MiB prepared JSON per batch and12 MiB per page;foreign closure1,024 units/heads,256 batches/owners and1 MiB serialized metadata. Count unchanged/disabled content before additional copies. Reject oversize closure completely. Project only at most256 selected1,536-dimensional vectors, with selected16 MiB cumulative selected vector/metadata UTF-8 serialization: SQL size proof before payload return, then application byte validation before parsing/additional copies/publication. This does not bound upstream source loading, provider res.json(), SQL internal working memory or total process memory; those phase15 limits remain open.
</design_contract>

<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Independently admit the selected capacity, recovery and projection contract</name>
  <files>.planning/phases/10-durable-jobs-stock/10-10-PLAN.md, .planning/phases/10-durable-jobs-stock/10-10-API-SCHEMA-DECISION.md</files>
  <action>Read D360-13, the capacity receipt and R1–R5 follow-up review. The synthetic experiment and root's input/closure/recovery choices are complete; do not rerun capacity merely to restate them. Independently review the amended seven-file contract, exact abandonment/placement SQL guards, bookkeeping-role prelocks, typed canonical busy yield and selected16 MiB projected-result limit. Confirm905 is still absent before later creation and record reservation only until admission. Admit both final document hashes before source/DDL execution. This is a root architecture/admission decision, not an invented user permission request.</action>
  <verify>Capacity evidence and root's explicit final admission exist; execution_ready is true only for the exact root-admitted source scope; native execution remains separately gated.</verify>
  <done>Exact limits, reservation recovery, migration reservation, API contract and ownership are independently admitted.</done>
</task>
<task type="auto">
  <name>Task 2: Implement constrained page and transport identities with owned transaction wiring</name>
  <files>minion_hub/src/server/db/pg-schema/job-effects.ts, minion_hub/src/server/db/pg-schema/job-effect-pages.ts, minion_hub/supabase/migrations/20260909090500_job_effect_page_batches.sql, minion_hub/src/server/services/job-effects.service.ts, minion_hub/src/server/services/job-effect-pages.service.ts, minion_hub/src/server/services/job-effect-pages.service.test.ts, minion_hub/src/server/services/job-effect-pages.sql.integration.test.ts</files>
  <read_first>minion_hub/src/server/services/job-effects.service.ts, minion_hub/src/server/db/with-org-core.ts, minion_hub/supabase/migrations/20260909090300_job_effect_receipts.sql, minion_hub/supabase/migrations/20260909090400_job_request_manifest.sql, minion_hub/src/server/services/job-effects.sql.integration.test.ts</read_first>
  <behavior>Current owned jobs bind complete source/page manifests under ordered locks; canonical semantic mappings remain unique across pages and tenant boundaries; progress saves after scoped-role restoration.</behavior>
  <action>Write red unit and actual-engine cases first. Add only the admitted additive reference key, new typed tables and authored905 migration. Upgrade representative903/904 heads/receipts and prove they remain byte/semantically unchanged. Enforce immutable page descriptors, complete batch membership snapshots, reserved→admitted→received or reserved→abandoned_unsent transitions, head-only tenant foreign keys and direct-SQL placement rules. Deferred checks require dense exact active mappings and zero active tombstone mappings. Reject altered tombstones, incomplete companion release, admitted/received movement, result mutation and incompatible partial catalog state.

Implement the additive helper using existing fenced ownership: bounded foreign-owner NOWAIT prelocks before role switch/heads, exact generation invalidation and typed rollback on busy. Preserve scoped role/GUC restoration; save canonical cursor after restoration without a single-head cursor binding. No new bg_jobs authority for app_ledger. Build page load/bind/current-page DB scope and canonical completion/incomplete busy results. Existing page rebind cannot reset captured revisions from stale data. Empty/disabled/qdrant descriptors use the same full authority and capacity contract. Test malformed/duplicate identities, full manifest and null-history conflict, mode/provider/generation mismatch, all-unchanged canonical count, UTF-16/UTF-8 boundaries including non-ASCII and surrogate cases, escaped request bytes, exact/limit+1 descriptors/closures, and typed capacity rejection with zero writes/admissions. Limit checks precede further serialization/copies.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/job-effect-pages.service.test.ts src/server/services/job-effects.service.test.ts --maxWorkers=1 --minWorkers=1</automated></verify>
  <done>Schema/API invariants and actual ownership/RLS restoration pass, with existing single-head behavior preserved.</done>
</task>
<task type="auto">
  <name>Task 3: Qualify overlap admission, historical response retention and atomic page consumption</name>
  <files>minion_hub/src/server/services/job-effect-pages.service.ts, minion_hub/src/server/services/job-effect-pages.service.test.ts, minion_hub/src/server/services/job-effect-pages.sql.integration.test.ts</files>
  <read_first>minion_hub/src/server/services/embeddings.ts, minion_hub/src/server/services/embeddings.test.ts, minion_hub/src/server/services/brain-corpus.service.ts, minion_hub/src/server/services/brain-business-corpus.service.ts</read_first>
  <behavior>Fresh pages retain ceil(missingUnits/64) batching; received overlap is reused; ambiguous overlap starts no new paid work; stale ownership cannot retain results; partial supersession retains evidence without granting publication; each consuming page remains atomic.</behavior>
  <action>Implement the packet's complete admission/dispatch/retention/publication sequence using existing preparation/execution and single-attempt caller-signal behavior. Observe actual synthetic outbound request bodies/counts rather than mocking the high-level embed API. Validate and retain identical privately prepared requests against complete source/provider/payload manifests, including provider changes before/after preparation. No SQL locks across fetch; at most four active attempts and64 inputs per request. Pre-abort prevents admission; cancellation stops later dispatch while already-running promises remain observed. Commit success and a one-row reserved CAS are required for each private dispatch permit; same-owner admitted replay, zero-row CAS, failed commit and stale owner must produce zero replayed calls.

Use actual independent PostgreSQL connections to test pages[A,B] and[B,C] with different ordering/partitions; receivedB reuse; ambiguousB plus missingC rejection before all new insertion/calls; simultaneous missingB uniqueness; and reversed input ordering without lock-order deadlock. Resolve transport[A,B], advanceB while response is pending, then prove the current original owner retains both vectors, a currentA-only page can consumeA, the staleB page cannot publish, and no automaticB retransmission occurs. Repeat with original owner losing its lease generation: no response persists. Validate response descriptor/vector cardinality mismatch and immutable replay.

Prove same-page publication replay differs from another page consumingB. A superseded/invalid source or domain callback failure rolls back all page/domain/consumption/progress changes while received evidence survives. Add failure-after-provider-before-receipt, commit-to-call ambiguity, cancellation, completion while loading, disabled/empty/qdrant conflicts and legacy alias identity cases. Never classify an unsent reservation as a remote effect.

Exercise reciprocal foreign owners, locked versus missing owner, heartbeat/claim/finish/generation races, frontier change before head locking, exact reserved transfer, and whole-batch abandonment when[B] is unavailable/superseded in reserved[A,B] while a new page needs[A,C]. Assert full companion release, immutable tombstone, only current[A,C] cross-document repacking, no automaticB dispatch and rollback restoring every placement. A stale dispatcher paused before CAS loses permission; one already admitted prevents reclamation. Busy returns canonical incomplete progress without terminal job failure or a remote-indeterminate label.

Test complete closure bounds (including non-requested companions and distinct heads/batches/owners), with exact and limit+1 rejection before changes. For received overlaps, select at most256 required indices instead of whole batches. Direct native SQL must prove cumulative projected vector+metadata text over16 MiB returns scalar rejection only, and exact-boundary allowed rows remain within the gate. Application UTF-8 checks precede parse/aggregate/publication copies; cover finite numeric representations, malformed vectors and metadata expansion. No publication/cursor changes occur on a projection cap failure, and historical complete responses remain intact. Preserve existing receipt rows and unrelated cursor fields. Record every remaining recovery/read/retention limitation through TODO and root proposal text.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/job-effect-pages.service.test.ts src/server/services/job-effects.service.test.ts src/server/services/embeddings.test.ts --maxWorkers=1 --minWorkers=1</automated></verify>
  <done>Native overlap/supersession/ownership/publication cases and observed outbound-attempt tests pass; the frozen API supports later10-06 adoption without changing its source in this child.</done>
</task>
</tasks>

<verification>
Root must review the exact new native fixture before granting a temporary single-file config or canonical disposable-lane admission. Use MINION_QC_DISPOSABLE=1, the shared URL/marker validator, root-approved loopback PostgreSQL, random isolated schema and distinct asserted pg_backend_pid values. No normal env loading, SUPABASE_DB_URL fallback, live provider or fake skip. Foundation vectors are receipt JSON validated against the existing1536-dimensional contract; this does not certify pgvector/Qdrant consumers. Apply actual authored903→904→905 plus necessary base job/domain schema; do not invent substitute receipt tables or mock the scoped transaction.

Meaningful native checks include restricted-role writes/denials, cross-org keys, no broad bg_jobs authority, role/GUC restoration before job progress/finalization, original-error preservation on SQL failure, simultaneous unit admission, nonblocking reciprocal owner prelocks and ordered full-closure heads. Direct SQL must exercise immutable tombstones, incomplete/dense membership, forbidden admitted placement movement, result mutation and both stages of the projected byte gate. Tests distinguish response retention after semantic supersession from stale dispatch ownership and prove page/domain/cursor rollback. Complete canonical/unchanged/disabled, Unicode UTF-8 and escaped prepared JSON limits are mandatory; cardinality alone is insufficient.

Run bounded new unit/native tests, existing foundation/embedding unit regressions and root-coordinated existing foundation native compatibility. Root then performs a current-source environment-free Hub typecheck and applicable release gates after the candidate freezes. Do not run competing full builds or weaken existing test discovery. Record before/after hashes, exact commands, versions, migration hash, test mapping/counts and actual capacity evidence in SUMMARY. An independent reviewer verifies source wiring and runtime results; an executor's own review is not independent.

Standards review covers strict types, API scope, RLS/DDL/provenance, no forged prepared requests, no source secrets in receipts and preserved legacy behavior. Spec review covers every truth above and actual synthetic cross-document packing. The future10-06 consumer remains an explicit unimplemented key link outside this slice; do not mark the entire phase or JOB-01/JOB-02 complete.
</verification>

<threat_model>
| Failure | Control |
|---|---|
| Another page repacks an already reserved or admitted unit | Canonical semantic identity, ordered head locks, all-page preflight and atomic batch reservation |
| Partial supersession discards useful transport evidence or permits stale publication | Current dispatch owner can retain complete immutable response; publication independently checks all current semantic references |
| Lease takeover stores an old response | Original dispatch job/generation fence at response persistence |
| One page's consumption suppresses another page's domain callback | Separate page state from transport received state and per-unit consumption |
| Provider or source changes between preparation and dispatch | Full source manifest and exact private prepared descriptor/payload binding |
| Tenant/reference manipulation | FORCE RLS, composite tenant/head-kind/batch foreign keys and runtime identity checks |
| Oversized materialized page exhausts resources | Admitted explicit prepared-page ceilings before admission; earlier source loading remains a separately recorded gap |
| Remote outcome is uncertain after interruption | Separate reserved/admitted states; safe reserved recovery after owner invalidation; no automatic admitted resend or exactly-once claim |
</threat_model>

<open_items_and_non_goals>
Capacity, cross-job reservation recovery and admission unresolved mean no implementation yet. Once implemented, add exact-site TODO(handoff) comments plus proposal text to root for retained unknown admitted outcomes, any remaining orphan reservation recovery limitation, receipt retention/GC, source loading bounds, and unadopted corpus/worker paths. Do not add source TODOs during this planning-only task. Root owns canonical ledger writes.

No corpus source edits, job-type migration, page-size change, per-document provider multiplication, parser/accounting/UOM changes, active Qdrant generation cutover, worker claim rewrite, dependency update, driver fault qualification, production DDL or deployment. Worker governance is a candidate separate child described in the packet, not an executable extra plan. Existing non-job corpus consumers remain outside the foundation's ownership claim.
</open_items_and_non_goals>

<output>
After admitted execution, create10-10-SUMMARY.md and submit the frozen seven-file candidate for independent verification. This drafting task produces only10-10-PLAN.md and10-10-API-SCHEMA-DECISION.md.
</output>


## Root exact source admission

Root completed independent contract/capacity/lock-order review after the author follow-up. Source ownership is the seven frontmatter files, plus this phase's10-10-SUMMARY.md for execution evidence. Begin with actual focused red unit/service and authored native fixture cases. Use existing private preparation/embedding execution and real ownership/RLS helpers; no high-level fake paid-effect algorithm. Before any test command root selects the private credential-free snapshot/config and verifies no mutable cache link or ordinary app-env bootstrap. Before native execution root reviews the exact fixture and approves its marked loopback database lane. This permits code and an additive905 file, not running it against an arbitrary database.

Source/provider reads before these new bounds remain phase15, and corpus/worker adoption remains outside this child. The missing-owner limitation requires an exact-site TODO and the paired root proposal; do not relabel it remote uncertainty or claim arbitrary orphan recovery. No OS/backend/driver fault injection is admitted.


### Root private unit-lane selection

Use /tmp/minion-10-10-backend-3ilrc5vv/minion_hub for unit execution, with qc-run-units.mjs/qc-unit.config.mjs and copied native generated tsconfig provenance. Root copied2305 source/migration/QC files and linked954 installed dependencies individually; mutable caches are private. The seven owned implementation files may be synchronized from the executor's frozen active candidate into this separate backend snapshot before each explicit test run; do not alter other copied source or frontend snapshots. Record hashes each time. Run only env -i PATH=/usr/bin:/bin HOME=<private base> LANG=C.UTF-8 node qc-run-units.mjs. The actual cache path is asserted before tests, and setup counts/denies every native socket connection attempt.

This admits the three listed focused unit files once the new file exists. No native SQL lane or config is yet admitted. If a required import/environment seam is missing, report it for a narrow snapshot/config correction, not an app-env load or a default test run. Actual provider fetch is synthetic and its attempted calls remain counted in the owned fixtures. No unrestricted network, driver fault injection or extra suite.


Root API clarification: jobEffectPageAdvanceResult always returns done:false with recorded per-page completion/current cursor; only the later corpus consumer may declare whole-job completion. Busy retains canonical progress. This clarifies the existing page helper without new source ownership or a changed runtime contract.


Root admits a focused diagnostic native TypeScript check in the same separate backend snapshot after each owned-file sync: env -i PATH=/usr/bin:/bin HOME=/tmp/minion-10-10-backend-3ilrc5vv LANG=C.UTF-8 node node_modules/typescript/bin/tsc -p qc-backend-types.json --noEmit. Root config includes owned source/test entries plus native generated declarations copied from the verified private Hub; all transitive diagnostics remain visible. This is not the final full Hub Svelte check and cannot omit newly failing transitive errors. No source extractions, replacement compiler declarations or active cache writes. Record errors before changing any out-of-scope file.


## Root actor-context amendment

The existing server-side SQL boundary is trusted: organization GUC plus SET LOCAL ROLE prevents accidental unscoped access, not arbitrary malicious SQL from the privileged login. The new actor GUCs are likewise writable labels, never authentication credentials. Within that boundary, admit transaction-local app.job_effect_job_id, app.job_effect_lease_generation and a bounded app.job_effect_prelocked_owners manifest around the domain callback in withOwnedJobScope. Preserve/restore previous values, including nested calls; original SQL errors remain primary and the outer transaction must roll back. No new bg_jobs grants to app_ledger.

The manifest contains only exact validated owner refs actually locked by the helper, at most256 input refs, deterministically ordered, and at most128KiB serialized UTF-8. Reject capacity before installing context or writing domain state; build bounded known-data records and account escaped JSON bytes before aggregate construction. This is an additional defensive context limit, not a production throughput target. Current actor is already locked separately.

The905 actor guard is immediate, not deferred: deferred triggers cannot depend on restored actor GUCs. It validates exact tenant/live actor job/generation and appropriate OLD/NEW ownership, then requires every foreign owner being rechecked to be in the declared prelocked manifest before any foreign-row lock. Use bounded parsing, fixed search_path and fully qualified references, no PUBLIC direct execution authority, and NOWAIT owner rechecks. The real helper's lock order remains current job, bounded foreign jobs, then heads/domain. An arbitrary SQL caller can forge GUC/manifest values; strict proof of preheld row locks and actor authentication against such a caller is explicitly outside this trusted-service contract. Do not claim those properties from a copied job/generation. Native direct-SQL tests still must prove stale/foreign/absent-context denial, immutable transitions and exact tenant isolation under the defined role boundary.

Independent review is recorded in10-10-ACTOR-REVIEW.md. This amendment remains within the owned helper/new905 migration and related owned fixtures; it introduces no general security-definer API, broad grants or extra file authority. Native execution remains separately gated.


## Root initial native diagnostic admission

Independent pre-native reviewfb888c3db92354af5e82ad8fef2d8d3f58ae805ac14de78b68ef2f5e4ec9f9ae confirms corrected setup-owner rollback/schema-drop/finally-close cleanup and no additional initial-run safety blocker. Initial fixture4b05481f4224614a13fb399956dec8a76827eaf4f2617cfd034be42b1cb74251 and905bad75afb3ed7c7d413d1bd82c64c4c09d40d48a751d9ba74085b264c8999e7ef are reviewed; broader acceptance remains pending.

Admit exact single-file qc-run-native.mjs/qc-native.config.mjs in /tmp/minion-10-10-backend-3ilrc5vv/minion_hub. Root's qc-native-network.mjs denies every native socket except literal127.0.0.1:55439 and denies unmocked fetch, recording forbidden attempts even if a caller catches them. Cache qc-native-cache is private and checked before collection. Exact command: env -i PATH=/usr/bin:/bin HOME=/tmp/minion-10-10-backend-3ilrc5vv LANG=C.UTF-8 MINION_QC_DISPOSABLE=1 MINION_QC_DATABASE_URL=postgres://minion_qc@127.0.0.1:55439/minion_qc_jobs_stock node qc-run-native.mjs. Validator must verify database/user/marker/literal-loopback identity before writes; no app environment fallback.

Use only the existing marked disposable PG17.10 database and one randomly generated qc_job_stock_<32hex> schema per run, actual901/903/904/905 with documented public-prefix isolation transform, existing app_ledger role, distinct backend assertions and bounded cleanup. No provider, external HTTP, process/backend termination, socket fault injection, driver candidate or production SQL. The diagnostic may expose ordinary SQL/constraint failures and be corrected within seven-file ownership, retaining all errors. New native cases for existing acceptance criteria stay within this reviewed lane; new resources or fault classes require root review. Record actual restricted-role attributes, function ownership and ACLs before claiming enforcement. Root owns independent final qualification.


Root descriptor representation clarification: the256KiB persisted descriptor limit uses actual PostgreSQL jsonb::text bytes, including its structural spaces. Keep the compact-JS preflight for early allocation rejection, then obtain scalar octet_length of the bounded descriptor cast tojsonb before page INSERT and emit typed capacity on excess. If current descriptor construction requires head/binding writes inside the still-owned transaction, rejection must roll back all of them; claim zero committed effects, not zero attempted SQL statements. Exact262144/262145-byte native fixtures must show priorheads, page andcursor unchanged on rejection. Reuse database serialization; no hand-written equivalent serializer, raised constraint or partially committed repair.


Root native diagnostic runner refinement: qc-run-native.mjs now accepts one optional bounded alphanumeric/space/underscore/hyphen test-name substring argument for focused corrections in the same exact native file. It rejects extra/invalid arguments and fails if no test actually passes. Existing cache/network/marker/schema guards remain. Filtered runs must record skipped versus executed counts and cannot qualify the full39-case-or-later suite; root repeats the complete final fixture after source freeze. This avoids repeated unchanged high-cardinality closure fixtures while resolving a specific descriptor boundary.
