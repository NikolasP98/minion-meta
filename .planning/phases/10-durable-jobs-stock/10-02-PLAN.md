---
phase: 10-durable-jobs-stock
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "minion_hub/src/server/db/pg-schema/stock.ts"
  - "minion_hub/scripts/stock-invoice-dedupe-preflight.ts"
  - "minion_hub/supabase/migrations/20260909090200_stock_invoice_issue_identity.sql"
  - "minion_hub/src/server/services/stock.sourced-issue.test.ts"
  - "minion_hub/src/server/services/stock.service.test.ts"
  - "minion_hub/src/server/services/stock.service.ts"
autonomous: true
requirements: ["STK-01","STK-02"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Clean fixture accepts migration; colliding fixture fails safely; cancelled history remains visible and allowed replacement semantics are explicit."
    - "Concurrent same-invoice calls yield one submitted issue; crash/retry preserves the same entry; ledger history stays unchanged except the single valid effect."
  artifacts:
    - path: "minion_hub/scripts/stock-invoice-dedupe-preflight.ts"
      provides: "Preflight and constrain invoice effect identity"
    - path: "minion_hub/src/server/services/stock.service.ts"
      provides: "Make create and submit retries converge"
  key_links:
    - from: "minion_hub/src/server/db/pg-schema/stock.ts"
      to: "minion_hub/supabase/migrations/20260909090200_stock_invoice_issue_identity.sql"
      via: "Identical active invoice expression index and predicate; independent admission addendum 2026-09-09"
    - from: "minion_hub/scripts/stock-invoice-dedupe-preflight.ts"
      to: "minion_hub/src/server/db/pg-schema/stock.ts"
      via: "Read-first contract and behavior verification for preflight and constrain invoice effect identity"
    - from: "minion_hub/src/server/services/stock.service.ts"
      to: "minion_hub/src/server/services/stock.logic.ts"
      via: "Read-first contract and behavior verification for make create and submit retries converge"
---

<objective>
Serialize invoice stock issue and recover interrupted submission.
Purpose: STK-01, STK-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/10-durable-jobs-stock/10-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/src/server/db/pg-schema/stock.ts
@minion_hub/src/server/services/stock.service.ts
@minion_hub/src/server/services/stock.logic.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export type AdvanceResult = { done: boolean; cursor?: unknown; error?: string };
export type JobHandler = { type: string; advance: (job: BgJob) => Promise<AdvanceResult> };
// Existing public functions: enqueueJob, cancelJobsByRef, advanceJob, runTick.
// createIssueFromInvoice(ctx, input) currently owns invoice-derived stock issue creation.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Preflight and constrain invoice effect identity</name>
  <files>minion_hub/src/server/db/pg-schema/stock.ts, minion_hub/scripts/stock-invoice-dedupe-preflight.ts, minion_hub/supabase/migrations/20260909090200_stock_invoice_issue_identity.sql, minion_hub/src/server/services/stock.sourced-issue.test.ts</files>
  <read_first>minion_hub/src/server/db/pg-schema/stock.ts, minion_hub/src/server/services/stock.service.ts</read_first>
  <behavior>Clean fixture accepts migration; colliding fixture fails safely; cancelled history remains visible and allowed replacement semantics are explicit. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Define the invoice issue identity as tenant + invoice source + active issue, matching current cancelled/draft/submitted semantics. Add the matching expression uniqueIndex to the Drizzle stock.ts schema, with exactly the migration predicate; preserve cross-org reuse, excluded rows without invoiceId and cancelled-history replacement. Add dry-run preflight reporting counts and identifiers without customer fields, and an additive partial unique constraint/index only after proving the predicate against synthetic duplicates. Existing collision makes migration stop with a remediation report; never delete or repost history. Obtain single schema-writer ownership from root before allocating timestamp or journal changes.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/stock.sourced-issue.test.ts</automated></verify>
  <done>Clean fixture accepts migration; colliding fixture fails safely; cancelled history remains visible and allowed replacement semantics are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Make create and submit retries converge</name>
  <files>minion_hub/src/server/services/stock.service.ts, minion_hub/src/server/services/stock.sourced-issue.test.ts, minion_hub/src/server/services/stock.service.test.ts</files>
  <read_first>minion_hub/src/server/services/stock.logic.ts</read_first>
  <behavior>Concurrent same-invoice calls yield one submitted issue; crash/retry preserves the same entry; ledger history stays unchanged except the single valid effect. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Serialize createIssueFromInvoice on the actual invoice row or transaction advisory lock; use database uniqueness as the final race boundary. A retry finding its matching draft resumes submission, a matching submitted issue returns the established result, and incompatible payload identity rejects. Ensure post-commit response loss and failure between creation/submission cannot create another ledger effect. Preserve insufficient-stock handling, invoice evidence vs physical consumption distinction, UOM rules and append-only ledger. Independent admission addendum 2026-09-09: add FOR UPDATE to the tenant-scoped draft reads in updateEntry/deleteEntry so submission cannot race a stale draft guard; retain missing-row and non-draft behavior. Verify denied edit/delete after committed submission, unchanged ledger history and emitted locking SQL; keep actual multi-connection contention qualification in 10-03. Independent UUID amendment: carry locked invoice.id into submission; normalize canonical grouped item/warehouse UUID spelling before conversion and reject alternate spellings before any draft. Only update the three invoice fixture IDs in stock.service.test.ts to canonical UUIDs; actual-engine regressions belong to stock.sourced-issue.test.ts. No live backfill.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/stock.sourced-issue.test.ts src/server/services/stock.service.test.ts</automated></verify>
  <done>Concurrent same-invoice calls yield one submitted issue; crash/retry preserves the same entry; ledger history stays unchanged except the single valid effect. Draft edit/delete guards lock their tenant-scoped entry read; committed submitted entries reject both operations without changing history. Multi-connection contention remains a 10-03 gate.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Job claimant / database / external effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-10-02-01 | S/E | Stale lease or cross-tenant owner writes | mitigate | Generation predicates, tenant identity, actual concurrency tests and deterministic effect keys. |
| T-10-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/10-durable-jobs-stock/10-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

