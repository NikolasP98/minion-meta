---
phase: 07-ws-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/ws-duplication-audit.md
autonomous: true
requirements:
  - WS-01
must_haves:
  truths:
    - "A single file at specs/ws-duplication-audit.md inventories every WS/gateway client implementation across minion_hub, minion_site, and paperclip-minion."
    - "The audit names every duplicate class, frame-type definition, uuid/session-key helper, and import site that must change in later plans."
    - "The audit states the target consolidation shape (@minion-stack/shared + @minion-stack/shared/node) so 07-02 has a concrete scaffold."
  artifacts:
    - path: "specs/ws-duplication-audit.md"
      provides: "WS-01 audit deliverable: per-consumer WS client inventory + consolidation plan"
      min_lines: 120
      contains: "minion_hub"
  key_links:
    - from: "specs/ws-duplication-audit.md"
      to: "packages/shared/src/gateway/"
      via: "documents existing Phase 4 primitives as the reuse baseline"
      pattern: "packages/shared/src/gateway"
    - from: "specs/ws-duplication-audit.md"
      to: "minion_hub/src/lib/services/gateway.svelte.ts"
      via: "names hub's gateway service as the largest duplicate (920 LOC)"
      pattern: "gateway.svelte.ts"
    - from: "specs/ws-duplication-audit.md"
      to: "paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts"
      via: "names paperclip's Node GatewayWsClient class as the Node-runtime duplicate"
      pattern: "gateway-client.ts"
---

<objective>
Produce `specs/ws-duplication-audit.md` — the WS-01 deliverable that inventories every WebSocket / gateway client implementation across `minion_hub`, `minion_site`, and `paperclip-minion/packages/adapters/openclaw-gateway`, and prescribes the exact consolidation shape downstream plans will execute.

Purpose: Close WS-01 with a concrete, grep-sourced document that later plans (07-02 publish, 07-03 migrate, 07-04 verify) reference as the single source of truth. The audit must name exact file paths, LOC counts, import sites, and the target API surface (`GatewayClient`, `createNodeGatewayClient`, `PROTOCOL_VERSION`) so 07-02 can be executed without further exploration.

Output: A single markdown file at `specs/ws-duplication-audit.md` (meta-repo root), committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ws-consolidation/07-RESEARCH.md

<interfaces>
<!-- Key primitives already in @minion-stack/shared (Phase 4). The audit must reference these as the reuse baseline. -->

From packages/shared/src/gateway/ (already exists, 0.2.0 local):
```
types.ts      — RequestFrame, ResponseFrame, EventFrame, GatewayFrame, Agent, Session, ChatEvent
protocol.ts   — sendRequest, handleResponseFrame, flushPending, PendingRequest
connection.ts — createConnectionState, connect, disconnect
```

From packages/shared/src/utils/ (already exists):
```
uuid.ts        — uuid()
session-key.ts — parseAgentSessionKey()
text.ts        — extractText, cleanText, parseGatewayMetadata (simpler base; hub has a divergent superset)
```

Consumers today:
- minion_hub: NO dep on @minion-stack/shared; carries verbatim copies of types.ts, uuid.ts, session-key.ts; divergent text.ts; 920-LOC gateway.svelte.ts with inline WebSocket lifecycle
- minion_site: dep on @minion-stack/shared@^0.1.0; consumes protocol helpers; 373-LOC member-gateway.svelte.ts with its own WebSocket lifecycle
- paperclip-minion: NO dep on @minion-stack/shared; 356-LOC GatewayWsClient class in openclaw-gateway adapter using `ws` npm package
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-sourced duplication inventory</name>
  <files>specs/ws-duplication-audit.md</files>
  <read_first>
    - .planning/phases/07-ws-consolidation/07-RESEARCH.md (sections: Summary, Current structure, Runtime State Inventory, Validation Architecture)
    - packages/shared/src/gateway/types.ts
    - packages/shared/src/gateway/protocol.ts
    - packages/shared/src/gateway/connection.ts
    - minion_hub/src/lib/services/gateway.svelte.ts
    - minion_hub/src/lib/types/gateway.ts
    - minion_hub/src/lib/utils/uuid.ts
    - minion_hub/src/lib/utils/session-key.ts
    - minion_hub/src/lib/utils/text.ts
    - minion_site/src/lib/services/member-gateway.svelte.ts
    - paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts
    - paperclip-minion/packages/adapters/openclaw-gateway/src/server/execute.ts
    - paperclip-minion/packages/adapters/openclaw-gateway/src/server/hire-approved.ts
  </read_first>
  <action>
    Create `specs/ws-duplication-audit.md` with the following sections (use exact headings):

    1. `# WS Client Duplication Audit` — front-matter paragraph: date 2026-04-21, phase 07, status "pre-migration inventory".
    2. `## Summary` — one-paragraph statement of the problem: three runtime-distinct implementations (hub browser, site browser, paperclip Node) with overlapping lifecycle, frame types, and helper utils. Phase 4 already consolidated the protocol layer (sendRequest, handleResponseFrame, frame types) into `@minion-stack/shared`; Phase 7 consolidates the outer WebSocket lifecycle + deletes duplicates in hub and paperclip.
    3. `## Baseline: @minion-stack/shared (Phase 4)` — table with three columns (File, Exports, Size in LOC). Run `wc -l` on packages/shared/src/gateway/{types,protocol,connection}.ts and packages/shared/src/utils/{uuid,session-key,text}.ts and record the actual line counts. List every exported symbol.
    4. `## Consumer 1: minion_hub (browser, bun SvelteKit)` — enumerate:
       - `src/lib/services/gateway.svelte.ts` — LOC via `wc -l`, summary of responsibilities (WebSocket lifecycle, reconnect backoff 800ms→15s, challenge handshake, device-identity fetch, agent/session event routing, Yjs binary channel, toast integration)
       - `src/lib/types/gateway.ts` — mark as DUPLICATE of packages/shared/src/gateway/types.ts; run `diff minion_hub/src/lib/types/gateway.ts packages/shared/src/gateway/types.ts` and paste a line-count diff summary
       - `src/lib/utils/uuid.ts` — mark DUPLICATE; confirm byte-for-byte match with packages/shared/src/utils/uuid.ts via `diff`
       - `src/lib/utils/session-key.ts` — mark DUPLICATE; confirm via `diff`
       - `src/lib/utils/text.ts` — mark DIVERGENT SUPERSET; list the extra content block types it handles (tool_use, tool_result, image) vs shared's simpler version; **disposition: KEEP hub's copy local per phase decision**
       - Import-site inventory: run `grep -rn "\$lib/types/gateway\|\$lib/utils/\(uuid\|session-key\|text\)" minion_hub/src --include='*.ts' --include='*.svelte'` and record every file path. Estimate count.
    5. `## Consumer 2: minion_site (browser, bun SvelteKit)` — status: partially migrated in Phase 4:
       - `src/lib/services/member-gateway.svelte.ts` — LOC, summary. Already imports `sendRequest`, `handleResponseFrame`, `ConnectionState` from `@minion-stack/shared`. Still rolls its own WebSocket lifecycle — to be replaced by shared `GatewayClient` in 07-03.
       - Import-site inventory: `grep -rn "@minion-stack/shared" minion_site/src --include='*.ts' --include='*.svelte'`.
       - Note: site has `@minion-stack/shared@^0.1.0` in package.json — will bump to ^0.3.0 in 07-03.
    6. `## Consumer 3: paperclip-minion (Node, pnpm workspace)` — adapter `@paperclipai/adapter-openclaw-gateway`:
       - `packages/adapters/openclaw-gateway/src/server/gateway-client.ts` — LOC, summary: exports `class GatewayWsClient` (356 LOC), `PROTOCOL_VERSION = 3`, plus a zoo of helpers (`asRecord`, `nonEmpty`, `withTimeout`, `headerMapGetIgnoreCase`, `headerMapHasIgnoreCase`, `toAuthorizationHeaderValue`, `resolveAuthToken`, `toStringRecord`, `isResponseFrame`, `isEventFrame`).
       - Disposition table for each export: GatewayWsClient → re-export from shared/node, PROTOCOL_VERSION → re-export from shared, frame-type guards (isResponseFrame, isEventFrame) → use shared types + narrow, header/auth helpers (asRecord..toStringRecord) → STAY LOCAL (paperclip-specific auth logic).
       - Import-site inventory: `grep -rn "./gateway-client" paperclip-minion/packages/adapters/openclaw-gateway/src --include='*.ts'`. List `execute.ts` + `hire-approved.ts` as the two call sites.
       - Note: `paperclip-minion/server/src/realtime/live-events-ws.ts` uses `WebSocketServer` (server-side, inbound); explicitly mark OUT OF SCOPE.
    7. `## Target state: @minion-stack/shared@0.3.0 API surface` — specify the new exports Phase 7 will add:
       - `packages/shared/src/gateway/client.ts` — `class GatewayClient` with constructor options `{ url, WebSocketImpl?, wsConstructorArgs?, onChallenge, onEvent?, onOpen?, onClose?, onReconnectScheduled?, autoReconnect?, connectTimeoutMs?, requestTimeoutMs? }` and methods `connect(): Promise<unknown>`, `request<T>(method, params?, opts?): Promise<T>`, `close(code?, reason?): void`
       - `packages/shared/src/gateway/constants.ts` OR inline in client.ts — `export const PROTOCOL_VERSION = 3`
       - `packages/shared/src/node/index.ts` — `export function createNodeGatewayClient(opts): GatewayClient` using `ws` npm package; re-exports GatewayClient + all types + utils
       - `packages/shared/package.json` exports-map addition: `"./node": { "types": "./dist/node/index.d.ts", "import": "./dist/node/index.js" }` and `peerDependencies: { "ws": "^8.0.0" }` with `peerDependenciesMeta: { "ws": { "optional": true } }`.
    8. `## Decisions locked in this phase` — bullet list (copy from planning_context locked decisions):
       - D-01 Package target: `@minion-stack/shared` (not `@minion/shared` — roadmap scope note is stale)
       - D-02 Publish order: 0.2.0 first (close Phase 4 gap), then 0.3.0 for the new client
       - D-03 Yjs binary frames STAY hub-local (not moved to shared)
       - D-04 `onLog` callback STAYS in paperclip adapter (not moved to shared)
       - D-05 Hub's `utils/text.ts` KEPT local (superset; not deleted)
       - D-06 Site verification via manual smoke runbook (no vitest install this phase)
       - D-07 `minion/` gateway server OUT OF SCOPE
       - D-08 Scope correction: ROADMAP says `@minion/shared` but canonical is `@minion-stack/shared`
    9. `## Requirement trace` — table mapping WS-01, WS-02, WS-03, WS-04, WS-05 each to a plan (07-01 audit → WS-01; 07-02 publish → WS-02; 07-03 migrate → WS-03+WS-04; 07-04 verify → WS-05).
    10. `## Open assumptions to verify during execution` — list A1, A2, A3, A7 from 07-RESEARCH.md Assumptions Log so downstream executors know what to validate.

    Every file path MUST be absolute from meta-repo root (e.g. `minion_hub/src/lib/services/gateway.svelte.ts`). Every LOC count MUST come from an actual `wc -l` run recorded at the top of the section ("measured 2026-04-21").

    Do NOT create stub code yet. This plan is pure documentation.
  </action>
  <verify>
    <automated>test -f specs/ws-duplication-audit.md && wc -l specs/ws-duplication-audit.md | awk '{ exit ($1 >= 120) ? 0 : 1 }' && grep -q "minion_hub/src/lib/services/gateway.svelte.ts" specs/ws-duplication-audit.md && grep -q "paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts" specs/ws-duplication-audit.md && grep -q "minion_site/src/lib/services/member-gateway.svelte.ts" specs/ws-duplication-audit.md && grep -q "createNodeGatewayClient" specs/ws-duplication-audit.md && grep -q "PROTOCOL_VERSION" specs/ws-duplication-audit.md && grep -q "WS-01" specs/ws-duplication-audit.md && grep -q "WS-02" specs/ws-duplication-audit.md && grep -q "WS-03" specs/ws-duplication-audit.md && grep -q "WS-04" specs/ws-duplication-audit.md && grep -q "WS-05" specs/ws-duplication-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - File `specs/ws-duplication-audit.md` exists and is ≥120 lines
    - Names every consumer file path (hub gateway.svelte.ts, site member-gateway.svelte.ts, paperclip gateway-client.ts)
    - Names target API (`GatewayClient`, `createNodeGatewayClient`, `PROTOCOL_VERSION`)
    - All 5 requirement IDs (WS-01..WS-05) referenced in the requirement-trace table
    - 8 locked decisions documented
  </acceptance_criteria>
  <done>
    Audit document committed to meta-repo root; 07-02 can read this file and know exactly what API surface to scaffold and what to delete in hub + paperclip.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces documentation only. No trust boundaries change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | specs/ws-duplication-audit.md | accept | Audit documents file paths and LOC counts; contains no secrets. Ensure no tokens accidentally pasted from source files (grep the final audit for `TOKEN\|SECRET\|KEY=` before commit). |
</threat_model>

<verification>
- `wc -l specs/ws-duplication-audit.md` ≥ 120
- `grep -c "^##" specs/ws-duplication-audit.md` ≥ 8 (at least 8 top-level sections)
- Every consumer-file path present
- No leaked secrets: `! grep -E "(TOKEN|SECRET|API_KEY)=[A-Za-z0-9]" specs/ws-duplication-audit.md`
</verification>

<success_criteria>
WS-01 is closed: a grep-sourced, decision-bearing audit document exists at `specs/ws-duplication-audit.md` and is referenced by all downstream Phase 7 plans as the single source of truth for what to migrate and what to leave alone.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ws-consolidation/07-01-SUMMARY.md` noting:
- Audit file path + final line count
- Any deviations from the RESEARCH.md-predicted state (e.g., if hub turns out to already have a @minion-stack/shared dep, or if a duplicate surfaces in an unexpected file)
- Measured LOC counts for each duplicate (so 07-03 can verify deletions closed the gap)
</output>
