---
phase: 07-ws-consolidation
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - .planning/phases/07-ws-consolidation/07-VERIFICATION.md
  - specs/ws-duplication-audit.md
autonomous: false
requirements:
  - WS-04
  - WS-05
must_haves:
  truths:
    - "Grep across hub, site, and paperclip returns zero duplicate WebSocket-client class definitions or duplicate frame-type interface declarations outside packages/shared."
    - "Paperclip E2E smoke (scripts/smoke/openclaw-gateway-e2e.sh) completes a full agent -> gateway -> response round-trip using the consolidated client."
    - "Hub and site each connect to the live staging gateway, complete the challenge handshake, and round-trip at least one chat message via the consolidated client (human-verified)."
    - "A phase VERIFICATION.md captures evidence (command outputs, timestamps, PR merge states) for WS-04 and WS-05."
  artifacts:
    - path: ".planning/phases/07-ws-consolidation/07-VERIFICATION.md"
      provides: "Evidence ledger for WS-04 (paperclip adapter consumes shared) + WS-05 (zero duplicates across platform)"
      min_lines: 80
      contains: "WS-05"
    - path: "specs/ws-duplication-audit.md"
      provides: "Updated with post-migration grep results and a 'Post-consolidation status: CLOSED' section"
      contains: "Post-consolidation"
  key_links:
    - from: ".planning/phases/07-ws-consolidation/07-VERIFICATION.md"
      to: "grep outputs across minion_hub/, minion_site/, paperclip-minion/"
      via: "pasted command + output blocks"
      pattern: "grep"
    - from: ".planning/phases/07-ws-consolidation/07-VERIFICATION.md"
      to: "scripts/smoke/openclaw-gateway-e2e.sh"
      via: "E2E run log excerpt"
      pattern: "openclaw-gateway-e2e"
---

<objective>
Close Phase 7 by proving — with grep and with an end-to-end smoke — that exactly one WS client implementation exists across the platform (WS-05), that paperclip's adapter is on the shared client (WS-04), and writing 07-VERIFICATION.md as the phase's evidence ledger.

Purpose: WS-04 and WS-05 are the phase's gate. Without a grep-verified zero-duplicates result and an E2E round-trip, the phase cannot be marked complete.

Output:
1. Task 1 (auto): Zero-duplicate grep sweep across all three consumers. Update specs/ws-duplication-audit.md with a "Post-consolidation status" section. Any non-zero grep match is a blocker.
2. Task 2 (checkpoint:human-verify): E2E smoke across all three consumers (paperclip automated smoke + manual hub + site verification against live gateway).
3. Task 3 (auto): Write 07-VERIFICATION.md ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ws-consolidation/07-RESEARCH.md
@specs/ws-duplication-audit.md
@.planning/phases/07-ws-consolidation/07-01-SUMMARY.md
@.planning/phases/07-ws-consolidation/07-02-SUMMARY.md
@.planning/phases/07-ws-consolidation/07-03-SUMMARY.md
@.planning/phases/07-ws-consolidation/SITE-SMOKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zero-duplicate grep sweep (WS-05)</name>
  <files>
    specs/ws-duplication-audit.md
  </files>
  <read_first>
    - specs/ws-duplication-audit.md (baseline inventory from 07-01)
    - .planning/phases/07-ws-consolidation/07-03-SUMMARY.md (list of files deleted in 07-03)
  </read_first>
  <action>
    Step A — Run the canonical zero-duplicate grep sweeps from meta-repo root. Record each command and its output verbatim (even when empty).

    Sweep 1: Duplicate frame-type interface declarations.
    `grep -rn "interface RequestFrame\\|interface ResponseFrame\\|interface EventFrame\\|type GatewayFrame" minion_hub/src minion_site/src paperclip-minion/packages/adapters --include="*.ts" --include="*.svelte" | grep -v "node_modules" | grep -v "dist/"`
    Expected result: zero matches. The only legitimate definitions live in packages/shared/src/gateway/types.ts.

    Sweep 2: Duplicate WebSocket client class declarations.
    `grep -rn "class GatewayWsClient\\|class GatewayClient" minion_hub/src minion_site/src paperclip-minion/packages --include="*.ts" | grep -v "node_modules" | grep -v "dist/" | grep -v "packages/shared"`
    Expected result: zero matches.

    Sweep 3: `new WebSocket(` outside shared (gateway-client contexts).
    `grep -rn "new WebSocket(" minion_hub/src minion_site/src paperclip-minion/packages/adapters --include="*.ts" --include="*.svelte" | grep -v "node_modules" | grep -v "dist/"`
    Expected result: zero matches in gateway-client files. Matches in live-events-ws.ts (server-side, OUT OF SCOPE per A6) are acceptable — they are inbound WebSocket servers, not client connections to the gateway. If any matches appear, classify each: either in-scope (blocker — file should have migrated) or out-of-scope (document as exception).

    Sweep 4: Duplicate uuid()/parseAgentSessionKey() implementations.
    `grep -rn "function uuid\\|export const uuid\\|export function parseAgentSessionKey" minion_hub/src minion_site/src paperclip-minion/packages --include="*.ts" | grep -v "node_modules" | grep -v "dist/" | grep -v "packages/shared"`
    Expected result: zero matches. Only @minion-stack/shared defines these.

    Sweep 5: Old import paths in hub (should be zero after 07-03).
    `grep -rn "\\\$lib/types/gateway\\|\\\$lib/utils/uuid\\|\\\$lib/utils/session-key" minion_hub/src --include="*.ts" --include="*.svelte"`
    Expected result: zero matches.

    Step B — If any sweep returns a non-zero result, stop execution and record the file paths as blockers. The blocker must be fixed (back in 07-03 scope) before this task completes.

    Step C — Update specs/ws-duplication-audit.md by appending a new section:
    ```
    ## Post-consolidation status (2026-04-XX, after Phase 7)

    **WS-05 verified: zero duplicates.**

    | Sweep | Command | Matches | Result |
    |-------|---------|---------|--------|
    | Frame-type interfaces | grep ... | 0 | CLEAR |
    | WS client classes | grep ... | 0 | CLEAR |
    | new WebSocket() in adapter | grep ... | <N> | <in-scope 0 / out-of-scope N> |
    | uuid()/parseAgentSessionKey impls | grep ... | 0 | CLEAR |
    | Old hub imports | grep ... | 0 | CLEAR |

    **Deletions:**
    - minion_hub/src/lib/types/gateway.ts (was <LOC>)
    - minion_hub/src/lib/utils/uuid.ts (was <LOC>)
    - minion_hub/src/lib/utils/session-key.ts (was <LOC>)
    - paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts (was 356 LOC; now <N>-line re-export shim)

    **Preserved (by design):**
    - minion_hub/src/lib/utils/text.ts (hub superset — tool_use/tool_result/image content blocks) [D-05]
    - paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-helpers.ts (auth helpers moved here from gateway-client.ts)
    - paperclip-minion/packages/adapters/openclaw-gateway/src/shared/device-auth.ts (Ed25519 signing stays local)
    ```
    Pull actual LOC counts from 07-01-SUMMARY and 07-03-SUMMARY.
  </action>
  <verify>
    <automated>test "$(grep -rn 'interface RequestFrame\|interface ResponseFrame\|interface EventFrame\|type GatewayFrame' minion_hub/src minion_site/src paperclip-minion/packages/adapters --include='*.ts' --include='*.svelte' 2>/dev/null | grep -v 'node_modules' | grep -v 'dist/' | wc -l)" = "0" && test "$(grep -rn 'class GatewayWsClient\|class GatewayClient' minion_hub/src minion_site/src paperclip-minion/packages --include='*.ts' 2>/dev/null | grep -v 'node_modules' | grep -v 'dist/' | grep -v 'packages/shared' | wc -l)" = "0" && test "$(grep -rn '\$lib/types/gateway\|\$lib/utils/uuid\|\$lib/utils/session-key' minion_hub/src --include='*.ts' --include='*.svelte' 2>/dev/null | wc -l)" = "0" && grep -q "Post-consolidation status" specs/ws-duplication-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - Sweep 1 (frame interfaces): 0 matches outside packages/shared
    - Sweep 2 (client classes): 0 matches outside packages/shared
    - Sweep 3 (new WebSocket): 0 matches in adapter contexts (live-events-ws.ts exceptions documented)
    - Sweep 4 (duplicate utils): 0 matches outside packages/shared
    - Sweep 5 (old hub imports): 0 matches
    - specs/ws-duplication-audit.md contains a "Post-consolidation status" section with the sweep table
  </acceptance_criteria>
  <done>
    WS-05 grep evidence captured. specs/ws-duplication-audit.md updated. Any blockers resolved before proceeding to Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E smoke across all three consumers (WS-04 + WS-05 end-to-end)</name>
  <what-built>
    Phase 7 consolidated the WS gateway client into @minion-stack/shared@0.3.0. Hub, site, and paperclip all consume it now. The final gate is proving a live end-to-end session works using the consolidated client — any latent bug in the shared GatewayClient would surface here.

    Three smokes:
    A) Paperclip E2E (automated): `pnpm smoke:openclaw-gateway` in paperclip-minion. Existing script at scripts/smoke/openclaw-gateway-e2e.sh spins up Docker Paperclip + local gateway + runs a canned agent.
    B) Hub manual smoke against staging gateway.
    C) Site manual smoke against staging gateway per SITE-SMOKE.md runbook.
  </what-built>
  <how-to-verify>
    **A) Paperclip automated E2E:**
    1. Ensure three consumer PRs are merged OR apply them as a combined local checkout:
       - minion_hub dev branch includes 07-03 Task 1 commit
       - minion_site master branch includes 07-03 Task 2 commit
       - paperclip-minion minion-integration branch includes 07-03 Task 3 commit
       (If any PR is unmerged, check out the feat/ws-shared-client branch locally in that subproject to run E2E.)
    2. `cd paperclip-minion && pnpm smoke:openclaw-gateway`
    3. Expected: exit 0. Last lines of output should show "agent completed" + a response frame id.
    4. Record the last ~20 lines of output for the VERIFICATION ledger.

    **B) Hub manual smoke against staging gateway:**
    1. `cd minion_hub && OPENCLAW_GATEWAY_URL=<staging-url> OPENCLAW_GATEWAY_TOKEN=<token-from-Infisical> bun run dev` (or deploy the feat/ws-shared-client branch to staging Vercel).
    2. Log into the hub dashboard.
    3. Open browser devtools -> Network -> WS. Observe:
       - Connection opens (status 101).
       - Server sends `connect.challenge` event (nonce visible in payload).
       - Client sends `connect` request with minProtocol: 3, maxProtocol: 3, client: 'ui', and device-identity signature.
       - Server returns helloOk.
    4. Navigate to an agent session. Send a chat message. Verify response frame with a `chat.*` event.
    5. Checklist: [ ] connects  [ ] challenge succeeds  [ ] chat round-trip succeeds  [ ] reconnect after network flap succeeds.

    **C) Site manual smoke (per SITE-SMOKE.md):**
    Follow the 6-step runbook at .planning/phases/07-ws-consolidation/SITE-SMOKE.md.
    Checklist: [ ] connects  [ ] challenge succeeds  [ ] chat round-trip  [ ] reconnect succeeds.

    **Report format (paste into resume signal):**
    ```
    Paperclip E2E: <PASS|FAIL> — <last-line-of-output>
    Hub smoke: <PASS|FAIL> — <notes>
    Site smoke: <PASS|FAIL> — <notes>
    ```
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three smokes are PASS. If any fails, describe the failure (error text, WS frame sequence, browser console log) — the planner can route to a gap-closure plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 07-VERIFICATION.md phase ledger</name>
  <files>
    .planning/phases/07-ws-consolidation/07-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/07-ws-consolidation/07-01-SUMMARY.md
    - .planning/phases/07-ws-consolidation/07-02-SUMMARY.md
    - .planning/phases/07-ws-consolidation/07-03-SUMMARY.md
    - specs/ws-duplication-audit.md (now including Post-consolidation status)
  </read_first>
  <action>
    Create .planning/phases/07-ws-consolidation/07-VERIFICATION.md modeled on the Phase 4 and Phase 6 VERIFICATION.md files (already in .planning/phases/04-fold-minion-shared/ and 06-auth-extraction/). Required sections:

    1. Front-matter: phase 07-ws-consolidation, completion date, author.
    2. `## Requirements status` — table with WS-01..WS-05, each with status (Complete / N/A) and evidence link (plan number + SUMMARY path or external PR URL).
    3. `## Published versions` — confirm @minion-stack/shared@0.2.0 and @minion-stack/shared@0.3.0 both on npm (from 07-02-SUMMARY). Record `npm view @minion-stack/shared versions` output.
    4. `## Consumer PRs` — table with three rows (hub, site, paperclip) showing PR URL, target branch, merge state, CI status.
    5. `## Zero-duplicate grep evidence` — paste the 5 grep sweeps from Task 1 verbatim with their results.
    6. `## E2E smoke evidence` — paste/summarize the Task 2 checkpoint report (paperclip E2E output tail, hub manual notes, site manual notes).
    7. `## Deletions summary` — files removed (hub types/gateway.ts, utils/uuid.ts, utils/session-key.ts; paperclip gateway-client.ts 356 LOC -> shim).
    8. `## Decisions realized` — how each D-01..D-08 locked decision shows up in the shipped code (one line per decision). D-01: shared scope @minion-stack not @minion. D-02: 0.2.0 then 0.3.0 published. D-03: Yjs binary stays hub-local. D-04: onLog stays in paperclip via withLogging decorator. D-05: hub utils/text.ts preserved. D-06: SITE-SMOKE.md created (no vitest added). D-07: minion/ gateway server untouched. D-08: ROADMAP "@minion/shared" note corrected to @minion-stack/shared.
    9. `## Deferred / follow-ups` — any tech debt discovered (e.g., if the `(client as any).ws` binary-channel workaround shipped, file a Phase 8 issue).
    10. `## Phase 7 closed` — one-line summary: "WS consolidation complete. Single WS client implementation at @minion-stack/shared@0.3.0 consumed by hub, site, and paperclip. Zero duplicates confirmed via grep + E2E."

    Use the same markdown conventions as prior VERIFICATION.md files. Target length 80-200 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-ws-consolidation/07-VERIFICATION.md && wc -l .planning/phases/07-ws-consolidation/07-VERIFICATION.md | awk '{ exit ($1 >= 80) ? 0 : 1 }' && grep -q "WS-01" .planning/phases/07-ws-consolidation/07-VERIFICATION.md && grep -q "WS-05" .planning/phases/07-ws-consolidation/07-VERIFICATION.md && grep -q "0.3.0" .planning/phases/07-ws-consolidation/07-VERIFICATION.md && grep -q "Zero-duplicate" .planning/phases/07-ws-consolidation/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/07-ws-consolidation/07-VERIFICATION.md exists, ≥80 lines
    - All five WS-01..WS-05 referenced with status
    - @minion-stack/shared@0.3.0 version recorded
    - Zero-duplicate grep evidence section present with pasted command + output
    - E2E smoke evidence section present
    - All 8 decisions (D-01..D-08) tracked to realized behavior
  </acceptance_criteria>
  <done>
    Phase 7 closed with a complete evidence ledger. Roadmap + REQUIREMENTS.md can be marked Complete by orchestrator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging/prod gateway -> consumer apps during smoke | E2E uses real tokens; smoke run could expose credentials if logged carelessly. |
| VERIFICATION.md -> meta-repo git history | Evidence file committed publicly — must not contain secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information disclosure | Token or nonce leaked in VERIFICATION.md | mitigate | When pasting browser devtools WS frame samples, redact `auth.token`, `device.signature`, and any nonce longer than 8 characters (truncate to `nonce: "abc12..."`). Explicit instruction in Task 3 action. Also run `grep -E "TOKEN|SECRET|KEY=|nonce.*[A-Za-z0-9]{20}" .planning/phases/07-ws-consolidation/07-VERIFICATION.md` before committing. |
| T-07-14 | Denial of Service | Paperclip E2E smoke hammers production gateway | mitigate | Script uses the dedicated E2E canned agent; rate-limited by the smoke script itself. Run once per verification, not in loop. |
| T-07-15 | Tampering | Unmerged PR state during E2E means the smoke tests stale code | mitigate | Task 2 checkpoint instructions require either merged PRs or local feat/ws-shared-client checkout in all three consumers. Recorded in VERIFICATION ledger ("Consumer PRs" table shows merge state at verification time). |
</threat_model>

<verification>
- Task 1: All 5 grep sweeps return 0 matches (or documented exceptions for live-events-ws.ts server-side code).
- Task 2: All 3 smokes (paperclip auto + hub manual + site manual) report PASS.
- Task 3: VERIFICATION.md exists with all required sections and passes the "no leaked secrets" grep.
</verification>

<success_criteria>
Phase 7 complete. WS-01..WS-05 all marked Complete in REQUIREMENTS.md. A single WS client implementation lives at @minion-stack/shared@0.3.0; grep across the three consumers confirms zero duplicates; E2E smoke proves the consolidated client works in production-shape scenarios.
</success_criteria>

<output>
After completion, create .planning/phases/07-ws-consolidation/07-04-SUMMARY.md noting:
- Grep sweep results (match counts per sweep)
- E2E outcomes (three PASS/FAIL lines from Task 2)
- VERIFICATION.md final line count
- Any deferred items flagged for Phase 8 (e.g., binary-channel API upstream issue, bundler-edge cases)
- STATE.md update hint: mark Phase 7 complete, advance to Phase 8
</output>
