---
phase: 18-docs-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/qc/repo-truth.mjs"
  - "scripts/qc/repo-truth.test.mjs"
  - ".planning/phases/18-docs-governance/18-REPO-TRUTH.json"
  - "AGENTS.md"
  - "minion_hub/CLAUDE.md"
  - "minion_site/CLAUDE.md"
  - "README.md"
autonomous: true
requirements: ["DOC-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Manifest exposes stale auth/storage/table-count/branch claims and names authoritative source for each replacement."
    - "Active instructions match supported source/runtime boundaries and executable commands; no unsupported live-success claims."
  artifacts:
    - path: "scripts/qc/repo-truth.mjs"
      provides: "Generate repository and command truth manifest"
    - path: "AGENTS.md"
      provides: "Update only active instruction claims and safe commands"
  key_links:
    - from: "scripts/qc/repo-truth.mjs"
      to: "AGENTS.md"
      via: "Read-first contract and behavior verification for generate repository and command truth manifest"
    - from: "AGENTS.md"
      to: ".planning/phases/18-docs-governance/18-REPO-TRUTH.json"
      via: "Read-first contract and behavior verification for update only active instruction claims and safe commands"
---

<objective>
Correct active instructions against current source.
Purpose: DOC-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/18-docs-governance/18-CONTEXT.md
@.agents/skills/minion-technical-writing/SKILL.md
@AGENTS.md
@minion_hub/CLAUDE.md
@minion_site/CLAUDE.md
@packages/db/package.json
@.planning/phases/18-docs-governance/18-REPO-TRUTH.json

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Generate repository and command truth manifest</name>
  <files>scripts/qc/repo-truth.mjs, scripts/qc/repo-truth.test.mjs, .planning/phases/18-docs-governance/18-REPO-TRUTH.json</files>
  <read_first>AGENTS.md, minion_hub/CLAUDE.md, minion_site/CLAUDE.md, packages/db/package.json</read_first>
  <behavior>Manifest exposes stale auth/storage/table-count/branch claims and names authoritative source for each replacement. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Read actual repo boundaries/remotes/branches, package scripts, DB clients/auth entrypoints and current deployment receipts. Generate timestamped source identity and confidence per field, avoiding secret env values. Distinguish local dirty branch from supported integration/release branches and declared package schema from physical catalog. Validate safe commands by --help or source rather than running db:push/seed.</action>
  <verify><automated>node --test scripts/qc/repo-truth.test.mjs</automated></verify>
  <done>Manifest exposes stale auth/storage/table-count/branch claims and names authoritative source for each replacement.</done>
</task>

<task type="auto">
  <name>Task 2: Update only active instruction claims and safe commands</name>
  <files>AGENTS.md, minion_hub/CLAUDE.md, minion_site/CLAUDE.md, README.md</files>
  <read_first>.planning/phases/18-docs-governance/18-REPO-TRUTH.json</read_first>
  <behavior>Active instructions match supported source/runtime boundaries and executable commands; no unsupported live-success claims. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Replace outdated Turso/BetterAuth and subproject count/path claims only where current truth verifies them; preserve actual surviving legacy paths as qualified. Remove unsafe first-tenant fallback advice from active usage docs and reflect fail-closed authorization. Keep historical milestone documents historical. Do not rewrite unrelated conventions or imply deployed patch from local source. Read Minion technical-writing skill before edits and verify links/script names.</action>
  <verify><automated>node scripts/qc/repo-truth.mjs --check-docs &amp;&amp; git diff --check</automated></verify>
  <done>Active instructions match supported source/runtime boundaries and executable commands; no unsupported live-success claims.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Historical instructions / current executable authority | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-18-01-01 | S/T | Stale docs authorize unsafe or retired behavior | mitigate | Source-backed mapping, status checks and no historical approval inheritance. |
| T-18-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/18-docs-governance/18-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

