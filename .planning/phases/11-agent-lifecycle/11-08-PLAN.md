---
phase: 11-agent-lifecycle
plan: "08"
type: execute
wave: 1
depends_on: ["11-04"]
files_modified:
  - packages/shells-bridge/src/config.ts
  - packages/shells-bridge/src/config.test.ts
autonomous: true
requirements: [AGT-05]
readiness: "Root owns two-file environment-source correction; isolated focused tests only. ACP replacement and command parsing are excluded."
closure_policy: "Configuration injection qualification only; no ACP/harness/image or full AGT-05 closure."
must_haves:
  truths:
    - "An explicit environment object supplies every configuration field without ambient fallback."
    - "Default invocation still reads the current process environment."
    - "Missing injected required fields fail despite conflicting ambient values."
  artifacts:
    - path: packages/shells-bridge/src/config.test.ts
      provides: "Real config-loader injection and ambient-default regression cases"
  key_links:
    - from: packages/shells-bridge/src/config.ts
      to: packages/shells-bridge/src/config.test.ts
      via: "Actual loadConfig and existing required/integer parsing helpers"
---
<objective>
Fix inconsistent environment injection in the bridge configuration loader. Preserve its existing value parsing and exported API so later isolated ACP fixtures cannot silently consult ambient credentials/settings.
</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md
@packages/shells-bridge/src/config.ts
</context>
<boundaries>
Root owns only the two listed active source/test files and this child evidence. No lock/dependency, command grammar, numeric parsing/range, protocol, permission, journal, lifecycle, Docker or systemd behavior change. Preserve shared WIP; no branch/worktree/stash/commit/release. Config was clean before this work. Preserve before-image. Tests use synthetic environment values in a fresh credential-free private package snapshot, installed tools read-only, private cache asserted before collection, exact config.test selection and one worker. No network, process spawn, DB or application bootstrap. Normal test script/broad package suite is excluded. Test environment mutations must be restored.
</boundaries>
<tasks>
<task type="auto">
  <name>Qualify and correct the actual injected environment boundary</name>
  <files>packages/shells-bridge/src/config.ts, packages/shells-bridge/src/config.test.ts</files>
  <action>Freeze before-image and native runner identity. Add meaningful red cases: explicit complete env wins over conflicting ambient values; missing injected required field fails even if ambient is populated; optional integers and defaults come from supplied env; default no-argument load uses ambient; source object remains unchanged and synthetic mutations restore. Pass env explicitly to existing private required/optionalInt helpers, retaining parsing/error/default behavior and public signature. Correct misleading injection comment. Add exact-site TODO for separate quoted-command parsing and image environment evidence, paired in the root proposal. Do not infer a safe shell grammar or fix arbitrary integer parsing in this scope.</action>
  <verify>Native isolated Vitest config.test red/green, focused strict native TypeScript check, scoped lint/diff, and independent source-to-snapshot identity review.</verify>
  <done>Injected config consistently uses its argument, default ambient behavior remains, and the actual regressions pass. Other ACP/image/command guarantees remain open.</done>
</task>
</tasks>
<verification>
Root source inspection plus explicit actual-module negative tests. A successful loader test is not protocol conformance or real harness readiness. Source and private runtime bytes must match; no production or package adoption is implied.
</verification>
<output>
11-08-SUMMARY.md records source hashes, real red/green cases, commands and remaining exact-site/proposal handoffs.
</output>
