---
phase: 11-agent-lifecycle
plan: "04"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - ".planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md"
  - "packages/shells-bridge/test/fixtures/acp-transcript.json"
  - "packages/shells-bridge/src/acp-conformance.test.ts"
  - "packages/shells-bridge/src/acp-client.ts"
  - "packages/shells-bridge/package.json"
  - "pnpm-lock.yaml"
autonomous: true
requirements: ["AGT-05"]
readiness: "Task 1 research document only is admitted; transcript/test source and Task 2 need exact root review. ACP research does not depend on sender integration or restore."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Protocol fixtures distinguish requests from responses; research names one executable pinned harness and every unresolved real-run prerequisite."
    - "Real pinned harness completes initialization/session/permission/prompt/cancel contract; synthetic regression suite passes; exact versions and command outputs recorded."
  artifacts:
    - path: ".planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md"
      provides: "Build official-protocol transcript and harness admission evidence"
    - path: "packages/shells-bridge/src/acp-client.ts"
      provides: "Implement the chosen adapter and execute real conformance gate"
  key_links:
    - from: ".planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md"
      to: "packages/shells-bridge/src/acp-client.ts"
      via: "Read-first contract and behavior verification for build official-protocol transcript and harness admission evidence"
    - from: "packages/shells-bridge/src/acp-client.ts"
      to: ".planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md"
      via: "Read-first contract and behavior verification for implement the chosen adapter and execute real conformance gate"
---

<root_research_admission>
2026-09-09: Admit only read-only source and primary-document research, recorded in 11-ACP-RESEARCH.md. No harness/model execution, package installation, lock or source/test changes are admitted yet. Identify the process generation, timeout, cancellation acknowledgment, exit and startup semantics needed by the later sender. Separate a synthetic protocol contract from real paid-harness qualification. Removing the old 11-03 dependency breaks the planning cycle; it does not waive integration acceptance.
</root_research_admission>

<objective>
Establish pinned ACP conformance before adapter replacement.
Purpose: AGT-05 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/11-agent-lifecycle/11-CONTEXT.md
@AGENTS.md
@packages/shells-bridge/src/acp-client.ts
@packages/shells-bridge/docker/Dockerfile.codex
@.planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
// AcpClient owns spawned harness, pending JSON-RPC calls and notification events.
// Bridge.start(): void; Bridge.shutdown(): Promise<void>.
// Existing maps: activeRuns(sessionId -> ActiveRun), acpSessionToRun(sessionId -> runId).
// Drone exports defineDrone/runDrone/runDroneStream and defineStreamingDrone/runStreamingSchemaDrone.
// Existing streaming-schema result: {type: "partial"|"done"|"error", ...}.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Build official-protocol transcript and harness admission evidence</name>
  <files>.planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md, packages/shells-bridge/test/fixtures/acp-transcript.json, packages/shells-bridge/src/acp-conformance.test.ts</files>
  <read_first>packages/shells-bridge/src/acp-client.ts, packages/shells-bridge/docker/Dockerfile.codex</read_first>
  <behavior>Protocol fixtures distinguish requests from responses; research names one executable pinned harness and every unresolved real-run prerequisite. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Verify official ACP TypeScript SDK documentation and installed/available harness versions through Context7 or primary docs. Record exact sdk/harness version, install provenance, stdio lifecycle and permission contract. Build synthetic server requests with string/numeric IDs, initialize capabilities, session/new, prompt content arrays, permission allow/deny, cancellation, malformed messages and exit. Select one installed credential-free real harness path when available; otherwise emit a concrete required harness/auth/budget gate before actual model execution. A mocked transcript does not satisfy real harness acceptance.</action>
  <verify><automated>pnpm --filter @minion-stack/shells-bridge exec vitest run src/acp-conformance.test.ts</automated></verify>
  <done>Protocol fixtures distinguish requests from responses; research names one executable pinned harness and every unresolved real-run prerequisite.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the chosen adapter and execute real conformance gate</name>
  <files>packages/shells-bridge/src/acp-client.ts, packages/shells-bridge/src/acp-conformance.test.ts, packages/shells-bridge/package.json, pnpm-lock.yaml</files>
  <read_first>.planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md</read_first>
  <behavior>Real pinned harness completes initialization/session/permission/prompt/cancel contract; synthetic regression suite passes; exact versions and command outputs recorded. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Proceed only after research resolves library/harness compatibility and root grants exclusive root lock ownership. Reuse official SDK where it fulfills the lifecycle; implement initialize, negotiated capabilities, created session identity, permission requests and cancellation before declaring ready. Run transcript suite and pinned real harness smoke with recorded explicit live-test authority if it requires a paid provider. Missing gate leaves AGT-05 pending; never substitute a fake harness result or auto-enable live tests.</action>
  <verify><automated>pnpm --filter @minion-stack/shells-bridge test &amp;&amp; pnpm --filter @minion-stack/shells-bridge typecheck</automated></verify>
  <done>Real pinned harness completes initialization/session/permission/prompt/cancel contract; synthetic regression suite passes; exact versions and command outputs recorded.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Caller definition / bridge / harness / effect host | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-11-04-01 | S/T/E | Mutable authority or lost lifecycle state | mitigate | Snapshot metadata, denied admission, verified cancellation and durable receipts. |
| T-11-04-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/11-agent-lifecycle/11-04-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

