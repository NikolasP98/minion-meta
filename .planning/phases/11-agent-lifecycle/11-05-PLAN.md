---
phase: 11-agent-lifecycle
plan: "05"
type: execute
wave: 4
depends_on: ["11-01","11-04","11-06"]
files_modified:
  - "minion_factory/runner/src/governance-replay.test.ts"
  - "minion_factory/runner/test/fixtures/governance-replay.json"
  - ".planning/phases/11-agent-lifecycle/11-GOVERNANCE-GAPS.md"
  - ".planning/phases/11-agent-lifecycle/11-VERIFICATION.md"
autonomous: true
requirements: ["AGT-06"]
readiness: "Evaluation executes locally; any newly evidenced source mutation requires generated bounded gap plan."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Fixtures fail when a prohibited effect is admitted or manifest identity is missing; repeated seeded runs give identical effect verdicts."
    - "Effect evidence and every source gap disposition are recorded; no failed or untested boundary is marked complete."
  artifacts:
    - path: "minion_factory/runner/src/governance-replay.test.ts"
      provides: "Define deterministic admission/effect governance fixtures"
    - path: ".planning/phases/11-agent-lifecycle/11-GOVERNANCE-GAPS.md"
      provides: "Close proven source gaps with explicit follow-up admission"
  key_links:
    - from: "minion_factory/runner/src/governance-replay.test.ts"
      to: "minion_factory/runner/src/containment-effects.ts"
      via: "Read-first contract and behavior verification for define deterministic admission/effect governance fixtures"
    - from: ".planning/phases/11-agent-lifecycle/11-GOVERNANCE-GAPS.md"
      to: "minion_factory/runner/src/governance-replay.test.ts"
      via: "Read-first contract and behavior verification for close proven source gaps with explicit follow-up admission"
---

<objective>
Verify governance through effects and immutable manifests.
Purpose: AGT-06 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/11-agent-lifecycle/11-CONTEXT.md
@AGENTS.md
@minion_factory/runner/src/containment-effects.ts
@minion_factory/runner/src/budget-reservations.ts
@drone/src/define.ts
@paperclip-minion/packages/adapters/minion-drone/src/server/runtime.test.ts
@minion_factory/runner/src/governance-replay.test.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
// AcpClient owns spawned harness, pending JSON-RPC calls and notification events.
// Bridge.start(): void; Bridge.shutdown(): Promise<void>.
// Existing maps: activeRuns(sessionId -> ActiveRun), acpSessionToRun(sessionId -> runId).
// Drone exports defineDrone/runDrone/runDroneStream and defineStreamingDrone/runStreamingSchemaDrone.
// Existing streaming-schema result: {type: "partial"|"done"|"error", ...}.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Define deterministic admission/effect governance fixtures</name>
  <files>minion_factory/runner/src/governance-replay.test.ts, minion_factory/runner/test/fixtures/governance-replay.json</files>
  <read_first>minion_factory/runner/src/containment-effects.ts, minion_factory/runner/src/budget-reservations.ts, drone/src/define.ts, paperclip-minion/packages/adapters/minion-drone/src/server/runtime.test.ts</read_first>
  <behavior>Fixtures fail when a prohibited effect is admitted or manifest identity is missing; repeated seeded runs give identical effect verdicts. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Build a fixed no-provider fixture corpus covering spoofed actor, denied tool, budget exhaustion, mutable input metadata, changed tool/schema/model/image identity, expired approval, retry after commit and cancellation. Assert actual attempted/committed effects and denial receipts, not generated text keywords. Manifests include source/hash versions and policy identity without prompt bodies/secrets. Record known differences among Factory, Drone and Paperclip rather than forcing a universal runtime.</action>
  <verify><automated>cd minion_factory/runner &amp;&amp; node --import tsx --test src/governance-replay.test.ts</automated></verify>
  <done>Fixtures fail when a prohibited effect is admitted or manifest identity is missing; repeated seeded runs give identical effect verdicts.</done>
</task>

<task type="auto">
  <name>Task 2: Close proven source gaps with explicit follow-up admission</name>
  <files>.planning/phases/11-agent-lifecycle/11-GOVERNANCE-GAPS.md, .planning/phases/11-agent-lifecycle/11-VERIFICATION.md</files>
  <read_first>minion_factory/runner/src/governance-replay.test.ts</read_first>
  <behavior>Effect evidence and every source gap disposition are recorded; no failed or untested boundary is marked complete. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Run the corpus against exact candidates. For each failing runtime seam write the exact source function, expected effect, smallest owned patch/test plan and dependency before allowing implementation; no broad governance refactor inside this evaluation slice. Phase completion requires all mandatory fixes executed and independently verified. Preserve phase17 external containment activation gate even when local effects pass.</action>
  <verify><automated>node /home/nikolas/.claude/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/11-agent-lifecycle/11-05-PLAN.md</automated></verify>
  <done>Effect evidence and every source gap disposition are recorded; no failed or untested boundary is marked complete.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Caller definition / bridge / harness / effect host | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-11-05-01 | S/T/E | Mutable authority or lost lifecycle state | mitigate | Snapshot metadata, denied admission, verified cancellation and durable receipts. |
| T-11-05-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/11-agent-lifecycle/11-05-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

