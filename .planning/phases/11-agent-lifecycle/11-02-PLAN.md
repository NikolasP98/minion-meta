---
phase: 11-agent-lifecycle
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "packages/shells-bridge/src/bridge.test.ts"
  - "packages/shells-bridge/package.json"
  - "packages/shells-bridge/src/bridge.ts"
autonomous: true
requirements: ["AGT-03"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Tests reproduce map overwrite before repair; default test command fails when suite discovery is empty."
    - "No invocation overwrites another owner; stale completion cannot delete a current run; callers receive stable busy/duplicate outcomes."
  artifacts:
    - path: "packages/shells-bridge/src/bridge.test.ts"
      provides: "Define observable busy/retry and run ownership behavior"
    - path: "packages/shells-bridge/src/bridge.ts"
      provides: "Admit and release runs with identity-checked state"
  key_links:
    - from: "packages/shells-bridge/src/bridge.test.ts"
      to: "packages/shells-bridge/src/bridge.ts"
      via: "Read-first contract and behavior verification for define observable busy/retry and run ownership behavior"
    - from: "packages/shells-bridge/src/bridge.ts"
      to: "packages/shells-bridge/src/config.ts"
      via: "Read-first contract and behavior verification for admit and release runs with identity-checked state"
---

<objective>
Enforce Shells session admission and request correlation.
Purpose: AGT-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/11-agent-lifecycle/11-CONTEXT.md
@AGENTS.md
@packages/shells-bridge/src/bridge.ts
@packages/shared/src/gateway/shells.ts
@packages/shells-bridge/src/config.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
// AcpClient owns spawned harness, pending JSON-RPC calls and notification events.
// Bridge.start(): void; Bridge.shutdown(): Promise<void>.
// Existing maps: activeRuns(sessionId -> ActiveRun), acpSessionToRun(sessionId -> runId).
// Drone exports defineDrone/runDrone/runDroneStream and defineStreamingDrone/runStreamingSchemaDrone.
// Existing streaming-schema result: {type: "partial"|"done"|"error", ...}.
```
</interfaces>

</context>

<boundaries>
Root-authorized review amendment: reject malformed JSON and non-object, null, or array frames before property access in `onFrame`. Prove that malformed input causes no dispatch and a subsequent valid request still obeys admission. Full protocol schema, payload size and authentication validation remain in 14-01; this guard does not certify those broader boundaries.
Active-request duplicate replay is scoped to the original WebSocket connection. Completed-request and restart-safe deduplication belong to 11-03; this slice must not add an unbounded completed-request cache. ACP notifications currently contain session identity only; late-effect reconciliation before session reuse belongs to 11-03/04. Keep both source/proposal handoffs explicit and do not claim durable idempotency from active replay.
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Define observable busy/retry and run ownership behavior</name>
  <files>packages/shells-bridge/src/bridge.test.ts, packages/shells-bridge/package.json</files>
  <read_first>packages/shells-bridge/src/bridge.ts, packages/shared/src/gateway/shells.ts</read_first>
  <behavior>Tests reproduce map overwrite before repair; default test command fails when suite discovery is empty. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Introduce controlled fake ACP and WS transports without model calls. Capture concurrent same-session and separate-session invocation, duplicate request identity, old completion racing a newer request, and unknown cancellation. Remove passWithNoTests only after the suite is discovered. Preserve advertised maxConcurrentRuns=1 unless the actual contract specifies a wider instance limit; don't silently increase concurrency.</action>
  <verify><automated>pnpm --filter @minion-stack/shells-bridge test</automated></verify>
  <done>Tests reproduce map overwrite before repair; default test command fails when suite discovery is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Admit and release runs with identity-checked state</name>
  <files>packages/shells-bridge/src/bridge.ts, packages/shells-bridge/src/bridge.test.ts</files>
  <read_first>packages/shells-bridge/src/config.ts</read_first>
  <behavior>No invocation overwrites another owner; stale completion cannot delete a current run; callers receive stable busy/duplicate outcomes. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Reject or deterministically acknowledge duplicate invokes before mutating session maps. Reserve admission atomically, release only the same run identity in finally, and correlate every notification to the originating ACP session/run pair. Preserve existing RPC response shapes unless a new error is added compatibly to shared contracts in phase14. Test instance and session concurrency explicitly, including close/exit during admission.</action>
  <verify><automated>pnpm --filter @minion-stack/shells-bridge test &amp;&amp; pnpm --filter @minion-stack/shells-bridge typecheck</automated></verify>
  <done>No invocation overwrites another owner; stale completion cannot delete a current run; callers receive stable busy/duplicate outcomes.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Caller definition / bridge / harness / effect host | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-11-02-01 | S/T/E | Mutable authority or lost lifecycle state | mitigate | Snapshot metadata, denied admission, verified cancellation and durable receipts. |
| T-11-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/11-agent-lifecycle/11-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>
