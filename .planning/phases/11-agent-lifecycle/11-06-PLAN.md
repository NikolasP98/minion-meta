---
phase: 11-agent-lifecycle
plan: "06"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - "drone/src/async-boundary.ts"
  - "drone/src/streaming-schema.ts"
  - "drone/src/streaming-schema.test.ts"
  - "drone/src/stream.ts"
  - "drone/src/stream.test.ts"
  - "drone/README.md"
  - "drone/src/types.ts"
autonomous: true
requirements: ["AGT-01","AGT-02"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Caller mutation cannot change admitted schema/model; every uncooperative async seam returns bounded TIMEOUT/ABORTED with no late success or new effects."
    - "runDroneStream and runStreamingSchemaDrone match nonstream deadline guarantees; test suite has no provider calls or leaked timers/listeners."
  artifacts:
    - path: "drone/src/streaming-schema.ts"
      provides: "Snapshot streaming schema definitions and bound schema-stream waits"
    - path: "drone/src/stream.ts"
      provides: "Close regular stream cancellation and listener cleanup gaps"
  key_links:
    - from: "drone/src/stream.ts"
      to: "drone/src/async-boundary.ts"
      via: "Shared abort-aware wait and cancellable iterator lifecycle"
    - from: "drone/src/streaming-schema.ts"
      to: "drone/src/async-boundary.ts"
      via: "Shared abort-aware wait and cancellable iterator lifecycle"
    - from: "drone/src/streaming-schema.ts"
      to: "drone/src/define.ts"
      via: "Read-first contract and behavior verification for snapshot streaming schema definitions and bound schema-stream waits"
    - from: "drone/src/stream.ts"
      to: "drone/src/run.ts"
      via: "Read-first contract and behavior verification for close regular stream cancellation and listener cleanup gaps"
---

<objective>
Apply definition and deadline guarantees to both streaming APIs.
Purpose: AGT-01, AGT-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/11-agent-lifecycle/11-CONTEXT.md
@AGENTS.md
@drone/src/define.ts
@drone/src/run.ts
@drone/src/types.ts
@drone/vitest.config.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
// AcpClient owns spawned harness, pending JSON-RPC calls and notification events.
// Bridge.start(): void; Bridge.shutdown(): Promise<void>.
// Existing maps: activeRuns(sessionId -> ActiveRun), acpSessionToRun(sessionId -> runId).
// Drone exports defineDrone/runDrone/runDroneStream and defineStreamingDrone/runStreamingSchemaDrone.
// Existing streaming-schema result: {type: "partial"|"done"|"error", ...}.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Snapshot streaming schema definitions and bound schema-stream waits</name>
  <files>drone/src/async-boundary.ts, drone/src/streaming-schema.ts, drone/src/streaming-schema.test.ts</files>
  <read_first>drone/src/define.ts, drone/src/run.ts, drone/src/types.ts</read_first>
  <behavior>Caller mutation cannot change admitted schema/model; every uncooperative async seam returns bounded TIMEOUT/ABORTED with no late success or new effects. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Reuse the admitted metadata snapshot semantics of11-01 for defineStreamingDrone model/fallbacks/output schema while preserving callback identity and caller mutability. Add pre-abort, never-resolving credential, idle iterator.next(), never-resolving getFinalResponse(), late completion/rejection and external abort fixtures. All waits must honor one cleanup-safe deadline/abort contract; do not wait indefinitely for iterator.return cleanup. Emit exactly one terminal event and never claim a returned timeout killed a remote effect.</action>
  <verify><automated>cd drone &amp;&amp; node node_modules/vitest/vitest.mjs run src/streaming-schema.test.ts</automated></verify>
  <done>Caller mutation cannot change admitted schema/model; every uncooperative async seam returns bounded TIMEOUT/ABORTED with no late success or new effects.</done>
</task>

<task type="auto">
  <name>Task 2: Close regular stream cancellation and listener cleanup gaps</name>
  <files>drone/src/async-boundary.ts, drone/src/stream.ts, drone/src/stream.test.ts, drone/README.md, drone/src/types.ts</files>
  <read_first>drone/src/run.ts, drone/vitest.config.ts</read_first>
  <behavior>runDroneStream and runStreamingSchemaDrone match nonstream deadline guarantees; test suite has no provider calls or leaked timers/listeners. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Replace leaking manual anySignal listener lifecycle with the verified cleanup-safe approach, covering resolveApiKey, model/stream iterator and tool invocation awaits. Check abort before admission and between tools, absorb late rejected promises and ensure consumer early-return cleanup does not hang. Align README/types across all three exported execution APIs, with explicit cooperative host effect-termination boundary. Use only fake hosts/iterators; live tests remain opt-in and no model calls are authorized.</action>
  <verify><automated>cd drone &amp;&amp; node node_modules/vitest/vitest.mjs run src/stream.test.ts src/streaming-schema.test.ts src/run.test.ts &amp;&amp; node node_modules/typescript/bin/tsc --noEmit</automated></verify>
  <done>runDroneStream and runStreamingSchemaDrone match nonstream deadline guarantees; test suite has no provider calls or leaked timers/listeners.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Caller definition / bridge / harness / effect host | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-11-06-01 | S/T/E | Mutable authority or lost lifecycle state | mitigate | Snapshot metadata, denied admission, verified cancellation and durable receipts. |
| T-11-06-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/11-agent-lifecycle/11-06-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>
