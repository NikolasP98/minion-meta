---
phase: 11-agent-lifecycle
plan: "07"
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - "packages/shared/src/gateway/shells.ts"
  - "packages/shared/src/gateway/shells-outcome.test.ts"
  - "packages/shells-bridge/src/run-journal.ts"
  - "packages/shells-bridge/src/run-journal.test.ts"
  - "packages/shells-bridge/package.json"
autonomous: true
requirements: ["AGT-04"]
readiness: "DRAFT: requires root independent review, exact-file admission and runtime/artifact availability gates."
closure_policy: "Foundation only. No sender wiring, authenticated receiver, restore, remote effect, image or AGT-04 completion claim."
must_haves:
  truths:
    - "Additive durable-run and outcome-receipt contracts distinguish execution certainty, cancellation acknowledgment and delivery acknowledgment without changing legacy final semantics."
    - "The real native SQLite fixture persists immutable run/outcome identities across reopen, denies conflicting replay and rolls back failed transitions without exposing a committed receipt."
    - "A Node22.13.0 no-flag support floor is explicit and qualified separately from current-local, emitted-package and pinned-image behavior."
  artifacts:
    - path: "packages/shared/src/gateway/shells.ts"
      provides: "Additive versioned Shells invocation, observation and receipt contracts"
    - path: "packages/shells-bridge/src/run-journal.ts"
      provides: "Unwired native SQLite admission/outcome/outbox foundation"
    - path: "packages/shells-bridge/src/run-journal.test.ts"
      provides: "Actual temporary SQLite and minimum-runtime/package fixtures"
  key_links:
    - from: "packages/shells-bridge/src/run-journal.ts"
      to: "packages/shared/src/gateway/shells.ts"
      via: "Typed versioned identities and receipt validation; canonical exports remain browser-safe"
    - from: "packages/shells-bridge/src/run-journal.test.ts"
      to: "packages/shells-bridge/package.json"
      via: "Native builtin availability and generated package behavior match admitted engine floor"
---

<objective>
Build the first durable Shells foundation: an additive protocol contract and a small SQLite-backed local run/outbox adapter, with actual native SQL fixtures. Reuse SQLite transactions rather than a custom journal file format. Do not connect it to Bridge, the gateway or a harness yet. The subsequent receiver child14-12 consumes this independently verified contract; 11-03 sender integration follows receiver acceptance.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
The installed GSD runtime is ~/.claude/get-shit-done; do not create the stale ~/.Codex path. Root owns review/admission/global state; this plan does not authorize commits.
</execution_context>

<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/11-agent-lifecycle/11-DURABLE-OUTCOME-DECISION.md
@.planning/phases/11-agent-lifecycle/11-02-VERIFICATION.md
@.planning/phases/09-security-containment/09-06-VERIFICATION.md
@.planning/phases/11-agent-lifecycle/11-03-PLAN.md
@.planning/phases/11-agent-lifecycle/11-04-PLAN.md
@packages/shared/src/gateway/shells.ts
@packages/shared/src/gateway/index.ts
@packages/shells-bridge/src/bridge.ts
@packages/shells-bridge/package.json
@packages/workstation-image/manifest/versions.json

Before-images: sender bridge70f7ba8c08d16db505ecc74d9afa2bc25955db6b81bca0bfbc6ea5318c14fe3c; canonical shells40cad5e97be00713f590092aabfc46c46e522dee747b20e890fd749983c872b1. Recheck all owned file hashes/diffs at admission. Root must resolve any competing shared-package or bridge-package owner first.

Primary runtime evidence supplied by root's exclusive browser review: https://nodejs.org/docs/latest-v22.x/api/sqlite.html (page title Node.jsv22.23.2), History: added22.5.0, no longer requires --experimental-sqlite from22.13.0, remains experimental. Bridge currently declares >=22.0.0; workstation source pins22.23.1. Installed bridge @types/node22.19.17 documents allowExtension since22.13 and constructor timeout since22.16. Do not use a later API under the proposed floor.
</context>

<boundaries>
Exact five source/test/manifest files above plus11-07-SUMMARY.md only. No edits to Bridge, ACP, backup, config, entrypoint, gateway shim/receiver/registry, root lock, image or another package. No schema migration of gateway JsonShellsStore, message ledger, Pi transcripts or customer data. Preserve all shared WIP. No branches/worktrees/stash/commit/publish/deploy. No provider/model/browser/live network tests or production database access.

Use node:sqlite, not an undeclared transitive import. No new package dependency or lock mutation. better-sqlite3 exists in the gateway, not the bridge; choosing it would require a separate dependency/native-binding plan. Missing builtin is an explicit unsupported-runtime failure, never memory/JSON fallback. The proposed package engine change to >=22.13.0 requires root admission of this plan and qualification below; do not silently add Node flags.

Native fixtures use only test-created temporary directories and synthetic data, clean after final handle closure. No external paths from process.env. Tests may exercise transaction failure, malformed fixture databases and ordinary two-connection contention; no OS/backend/process fault injection, remote database, permission escalation or concurrent heavy work. Any additional process-interruption durability experiment needs a later explicit plan.

No numeric retention/RPO policy is inferred. Constructor limits for this foundation are explicit test-supplied options; production configuration/admission is later ownership. No automatic deletion of unresolved/unacknowledged state. No full package suite that imports unrelated live fixtures.
</boundaries>

<tasks>
<task type="auto">
  <name>Task1: Freeze additive run, outcome and receipt contracts</name>
  <files>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/shells-outcome.test.ts</files>
  <read_first>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/index.ts, packages/shared/src/index.ts, minion/src/shells/shared-types.ts, minion/src/shells/bridge-ws.ts, 11-DURABLE-OUTCOME-DECISION.md</read_first>
  <action>
Add a durable-outcome version1 capability, optional caller invocation key, and an explicit internal gateway-to-bridge admitted-run envelope. Keep the original caller ShellsInvokeParams compatible: server-issued run identity belongs in the separate internal envelope, never in caller-controlled authority fields. Bind shellId, runId, sessionId, invocation identity and an opaque validated input digest. The later receiver owns authenticating/scoping caller keys and defining semantic input normalization; this foundation must not pretend the digest alone grants authority or performs end-user deduplication.

Define immutable observed-terminal records with stable event identity, outcome digest and fields from the bounded existing final vocabulary. Cancellation-request/acknowledgment and unresolved execution are separate observations, not additions that old ShellFinalPayload silently interprets as terminal. Keep existing final/error/aborted event behavior unchanged. Define commit_outcome response and get_outcome query/result types; no method becomes live merely because its name is exported.

Define pure browser-safe runtime guards/normalization for these exact new records in the existing canonical module. Validate finite safe timestamps/durations, explicit version/state, bounded identifiers, digest syntax, required identities and allowed terminal fields. Reject null/arrays/unknown authority fields rather than assertions over unknown. Fix the terminal digest projection order and compare normalized fields; do not introduce a general-purpose canonical JSON framework or hash arbitrary unknown input. Keep legacy capability absence explicit and supported. Use explicit guard limits supplied by caller/test where production limits remain undecided; tests must deny missing/invalid limits rather than create unbounded behavior.

Test valid records, exact normalization/idempotence, unsupported version, malformed identity/state, cancellation acknowledgment pretending to be terminal, oversized input and hostile extra fields. Verify original invoke/final/register shapes remain valid. Canonical root and gateway barrels already export shells.ts; no barrel edit or Node builtin import is needed. Gateway shim parity and installed artifacts remain14-12/package gates, not hidden edits here.
  </action>
  <verify><automated>pnpm --filter @minion-stack/shared exec vitest run src/gateway/shells-outcome.test.ts --maxWorkers=1 --minWorkers=1</automated></verify>
  <done>The exact version1 contract and meaningful negative fixtures pass without a gateway receiver or browser-only package importing Node APIs.</done>
</task>

<task type="auto">
  <name>Task2: Implement the unwired SQLite run/outbox adapter and actual SQL fixtures</name>
  <files>packages/shells-bridge/src/run-journal.ts, packages/shells-bridge/src/run-journal.test.ts</files>
  <read_first>Task1 source/fixture results, packages/shells-bridge/src/bridge.ts, minion/src/memory/sqlite.ts, minion/src/memory/sqlite-pragmas.ts, minion/src/infra/message-ledger.ts, minion/src/shells/store.ts</read_first>
  <action>
Use DatabaseSync via the Node builtin, private parameterized statements and explicit transactions. This is a domain adapter, not a new WAL/recovery engine. Export the smallest operations required for the later bridge: open/close; admit or read exact invocation; record uncertainty/cancel observation; commit immutable terminal plus pending delivery; list pending; validate and mark exact receipt acknowledgment; inspect the recorded run. It must remain unused by Bridge until receiver14-12 is independently accepted.

Create a dedicated database with application_id0x4D534A31 (MSJ1), user_version1 and exact expected tables/indexes for metadata, runs and outcome deliveries. Bind one shell identity at creation. Persist the input digest and necessary invocation metadata, not prompts, raw ACP frames, secrets or model output. A partial uniqueness constraint enforces one nonterminal/unresolved admission slot for this single-concurrency bridge. A duplicate admitted identity with identical bound metadata returns its original run; changed identity/digest conflicts. Completion atomically makes the terminal immutable, creates the stable outbox item and releases the slot. Unresolved or acknowledged-cancel observations do not release it.

Select WAL/FULL explicitly and verify effective PRAGMAs. Use only APIs present at22.13.0; use SQL busy_timeout rather than the later constructor option. Inspect a nonempty existing database read-only before any journal-mode/header/schema mutation: foreign application_id, unsupported version, mismatched shell, corrupt schema or unknown tables must fail without relabeling it. Revalidate identity within the write transaction before mutation to prevent a stale preflight authorizing another database state. Empty new path initialization is transactional. Existing journal files do not exist in current production source: do not invent imports from old maps/JSON/transcripts. No automatic destructive reset/migration and no caller-provided migration SQL.

Each terminal identity maps to one normalized outcome. Identical commit returns the stored row; changed content under the same event/run fails without partial mutation. ACK must match event/run/shell/digest and cannot acknowledge another pending record. The adapter validates identity only; it cannot prove a remote peer authenticated or committed anything. Leave an exact TODO at its ACK API pointing to the root QC proposal and14-12/11-03 integration gate. Keep acknowledged tombstones and all unresolved/unacked rows. Require explicit maxRuns and maxOutcomeBytes options, validate both, and count tombstones toward maxRuns. Reserve the one bounded outcome obligation as part of admission; do not apply new-admission capacity rejection to a terminal for an already admitted run. Reject an oversized terminal before any mutation and retain its unresolved obligation. This is a logical size/count bound, not a filesystem quota or proof that disk exhaustion cannot lose an observation; production outage handling remains sender integration ownership.

Run native SQLite tests using real temporary files, not mocked SQL. Reopen and verify persistence; test exact duplicate and conflicting admission/terminal/ACK; terminal monotonicity; cancel/timeout uncertainty; rollback on a constraint failure after an earlier write within the same transaction; two independent connections contending for the same admission; foreign/application_id/schema/identity refusal with before/after file identity evidence; capacity and fixed outcome-size boundaries; close and post-close operations. Verify no successful receipt is exposed before COMMIT and a failed commit cannot be read as committed from another handle. Catching errors and updating an in-memory Map is not evidence.

A missing new module is structural red evidence only. Record meaningful native behavior failures observed during implementation and their correction; do not manufacture historical passing claims. Tests that cannot exercise a failure deterministically must leave that exact claim pending rather than weaken assertions.
  </action>
  <verify><automated>pnpm --filter @minion-stack/shells-bridge exec vitest run src/run-journal.test.ts --maxWorkers=1 --minWorkers=1</automated></verify>
  <done>Native temporary-database fixtures prove the local adapter invariants across transactions/reopen. No receiver-authentication, process/power-loss, actual model or deployed durability claim is made.</done>
</task>

<task type="auto">
  <name>Task3: Qualify the engine floor and exact emitted package without changing installed peers</name>
  <files>packages/shells-bridge/package.json, packages/shells-bridge/src/run-journal.test.ts</files>
  <read_first>packages/shells-bridge/package.json, packages/shells-bridge/tsconfig.json, packages/shared/package.json, packages/workstation-image/manifest/versions.json, Task1/2 hashes/results</read_first>
  <action>
After root admits the support-floor decision, change only engines.node from >=22.0.0 to >=22.13.0. Preserve every dependency, script, version and unrelated manifest edit. No lock regeneration. Record the compatibility narrowing and the fact that missing-builtin distributions still fail explicitly.

Use an isolated credential-free copy of the two exact candidate packages for declaration/build qualification; exclude .env*, .git, node_modules, dist, build, data and nested worktrees from the copied source. Reuse installed tooling/runtime dependencies through explicit links, but link the copied bridge's @minion-stack/shared to the copied shared package so it actually consumes the freshly emitted contract. Do not replace source-checkout dist, installed shared peers or user package caches. Build shared then bridge into the isolated copy, using installed tsc and the owning package scripts. The bridge's type-only/new runtime imports must resolve through the emitted shared package; a source alias alone is insufficient. Preserve source, emitted declaration/JS and archive-content identities.

Run focused contract/native fixtures and a small raw-Node import/open/admit/commit/reopen/ACK/close fixture against the emitted package under an exact verified Node22.13.0 binary without NODE_OPTIONS or --experimental-sqlite. Qualify the same fixture under the pinned image's22.23.1 runtime once that executable is available; local22.23.2 results remain separately labeled. Use environment allowlisting and fixture-only temp paths. Test unavailable/unsupported builtin behavior without adding a fallback or launching a production process. Runtime fixture code remains in the owned test file and may generate disposable helper text; no permanent runner/config file outside the allowlist.

No runtime download/install or image build is authorized implicitly. If22.13.0 or the exact image runtime is absent, report the missing artifact/hash/provenance gate to root, continue current-runtime source/native tests, and leave minimum/artifact acceptance pending. A separately admitted runtime-fetch/CI matrix can supply it. Do not mark the support-floor truth verified from official docs or current Node alone. No package publication, image digest repin or real installed-peer mutation.
  </action>
  <verify><automated>pnpm --filter @minion-stack/shared typecheck</automated></verify>
  <done>Current-source checks and isolated emitted-package evidence are recorded independently from minimum/pinned-runtime results. All claimed runtime lanes have actual no-flag output and exact identities; missing lanes block that claim and later adoption.</done>
</task>
</tasks>

<subsequent_boundary>
Root must create and admit receiver child14-12 after this contract freezes.14-12 depends on11-07 scoped contract/native evidence plus verified09-06 receiver authority, not completion of all phase11/14. It owns gateway shim/runtime guards, a dedicated receipt store and production injection, authenticated commit ACK, tenant-authorized outcome query and logical invocation forwarding. It must not reuse the bridge adapter's single-active-slot assumption as a gateway-wide concurrency limit. Exact next seams are listed in11-DURABLE-OUTCOME-DECISION.md; no gateway file is owned here.

11-03 sender integration must then consume the verified receiver and move config/startup/shutdown/session uncertainty wiring under its amended allowlist. The local lifecycle foundation and restore/digest work stay separate. Root owns removal of overlapping run-journal files from11-03 execution ownership and adjustment of11-04 dependency sequencing. Package release/declaration/archive/image adoption remains separate from source completion.
</subsequent_boundary>

<verification>
Focused tests use installed Vitest2 with both --maxWorkers=1 and --minWorkers=1. Run shared typecheck, and bridge typecheck against the isolated correctly emitted shared artifact; source-checkout stale dist cannot stand in for that. Run scoped oxlint and git diff --check. Record exact effective commands/cwd, environment allowlist, source/artifact/runtime hashes, real passed/failed/skipped counts and partial gates. No broad suite/full Hub check or image build. Independent review must inspect native transactions and negative cases, not only mocks or SQL text. Report standards and scoped-spec outcomes separately.
</verification>

<success_criteria>
Task1 contract and Task2 actual SQL invariants are independently verified at frozen hashes. Task3 cannot be called complete until its required minimum/pinned-runtime evidence exists; useful source/native progress may be accepted with packaging blocked. This foundation leaves the production bridge and receiver behavior unchanged. It does not fulfill durable delivery, cancellation, restore, peer adoption or AGT-04. No invisible unowned source gap: exact TODO(handoff) sites and root-maintained proposal entries must cover the intentionally unwired adapter and its unproven remote authority.
</success_criteria>

<output>
Create11-07-SUMMARY.md with exact five-file scope, source/artifact identities, native fixture results and all runtime/adoption gates. Send root TODO/proposal wording; do not edit global STATE/ROADMAP/REQUIREMENTS or other plans. Root obtains independent review before any14-12 or11-03 integration dispatch.
</output>


## Root review amendments before admission

The author's follow-up review identified six gaps. Root accepts these corrections as normative; this plan remains unadmitted until independent review.

1. This adapter proves multi-connection transaction correctness. One nonterminal slot constrains admissions, not the number of writable handles/processes. Lifetime single-owner service enforcement and process generations belong to11-03. Two-connection fixtures are intentional.
2. Require an existing private trusted parent and reject linked parent/database/WAL/SHM paths at inspection. State the trusted-directory/process assumption explicitly. Database-state revalidation inside transactions does not prevent arbitrary same-UID pathname/inode replacement between native path opens. Do not claim race-free filesystem authority from lstat. Refuse nonempty foreign stores without relabeling; preserve their bytes. Never bootstrap this store from arbitrary caller or environment paths.
3. Persist policyVersion, maxRuns and maxOutcomeBytes in metadata and the reserved outcome bound on admission. Reopen with changed policy rejects. Exact duplicate admission, terminal persistence and ACK still work at full capacity; a new distinct run fails atomically. No pruning/tombstone or policy migration is admitted.
4. The candidate terminal digest is lowercase hexadecimal SHA-256 over UTF-8 JSON.stringify(['minion.shells.outcome',1,shellId,runId,sessionId,invocationId,inputDigest,eventId,state,durationMs,stopReason??null,errorMessage??null]). Strictly validate identities, states and bounded string fields first; duration is a nonnegative safe integer, reject malformed Unicode and do not normalize Unicode. Measure limits over encoded UTF-8 bytes. Exclude receiptId, receiver committedAt and digest itself; reject unknown outcome fields/raw result/usage. Browser-safe shared code owns pure normalization/canonical text, Node owners compute/recompute the hash. Input digest remains opaque until receiver semantic admission is specified. Independent review must check compatibility with actual final vocabulary before freezing this schema.
5. WAL/FULL and graceful reopen tests establish actual settings and transaction behavior, not power/process-loss or RPO guarantees. A failed statement followed by rollback is distinct from failed COMMIT. Name actual evidence precisely; do not inject a driver fault to manufacture a commit failure. If no admitted deterministic fixture exercises a claim, leave it unverified explicitly.
6. Construct the isolated shared/bridge artifact resolution before any bridge native test importing the new shared runtime contract. Existing installed shared dist cannot stand in for it. The emitted run-journal module is private (package exports only '.'); qualify its actual emitted file path without inventing a public export. Root owns later11-03/11-04 dependency/ownership amendments and14-12's multi-shell receiver contract.

Available pinned source-image runtime: `/home/nikolas/.local/share/mise/installs/node/22.23.1/bin/node`, SHA `93956de2e59480474a7b46571da1651180b1a050cdf32641ebec4ce6e478e068`, version output v22.23.1 under empty environment. This does not qualify a container image. Node22.13.0 executable was not found in bounded known locations; minimum-runtime verification remains pending. No fetch/install was admitted.


## Root foundation execution admission

Independent review76ff86fdc13177bdf73712149683137139c119daac457b1c2773ed3cbcb37f82 is accepted with both amendments: verify the complete expected sqlite_schema object/constraint definitions, reject triggers/views/extra indexes and altered constraints with actual refusal fixtures; use a fixed finite per-run representation for cancellation/uncertainty (bounded reason fields and latest observation, no unbounded append history). Retain immutable terminal semantics and unacknowledged deliveries. Unknown objects must never run before foreign-schema rejection.

Root admits Tasks1/2 and current/pinned-local-runtime portions ofTask3 under the exact five source/test/manifest files plusSUMMARY. Whole-shared input freeze from14-11 is released for this new shells.ts ownership only; its accepted immutable archive remains the earlier snapshot and must not be relabeled as containing this contract. Do not edit shared client/dist or any active installed packages. Preserve the existing dirty bridge package manifest except the specified engine field. Reserve private temporary compilation/fixture directories and record actual paths and all artifacts before execution. No new dependency, external runtime fetch, server wiring or protocol rollout. Missing Node22.13.0 remains an explicit minimum-runtime gate; use available22.23.1 and22.23.2 only within the admitted native fixtures. No driver/process fault injection. Meaningful native statement/constraint and ordinary transaction contention fixtures are authorized.

11-03/11-04 source integration is unadmitted while this foundation runs. Root will remove overlapping journal ownership and correct dependency edges before their later dispatch; the current plan does not create a receiver or invoke ACP.


## Root observation-order correction

Author and independent reviewer reproduced by source inspection that same-millisecond cancel_requested then cancel_acknowledged is rejected as CONFLICT. A timestamp is not an observation identity. Admit a narrow journal/native-test correction: identical observations are idempotent; cancellation acknowledgment dominates requested for this run, including same timestamp, and a later repeated request must not erase an observed acknowledgment. An ACK with an earlier timestamp still records the acknowledged fact; timestamp ordering cannot deny that state transition. Repeated ACK metadata and unresolved observations use the bounded latest record with same-time serialized replacement, retaining older-record suppression where it does not downgrade the acknowledged fact. Terminal outcomes remain immutable; cancellation acknowledgment never releases the execution slot or claims termination. Add actual native same-tick request→ACK, repeated request afterACK, earlier-timeACK and unchanged terminal/reservation regressions. No new observation log/schema/API/sender wiring is admitted.

Private .vite cache symlink correction and prior active-cache results.json write exception must remain in SUMMARY/VERIFICATION. Reuse the reviewer's corrected native runner/private directories for final qualification; never erase or overwrite the historical failed isolation receipt.
