---
phase: 11-agent-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - drone/src/define.ts
  - drone/src/define.test.ts
  - drone/src/run.ts
  - drone/src/run.test.ts
  - drone/src/types.ts
  - drone/vitest.config.ts
  - drone/vitest.live.config.ts
  - drone/README.md
autonomous: true
requirements: [AGT-01, AGT-02]
must_haves:
  truths:
    - Caller mutation cannot replace an admitted model, tool callback or schema field.
    - Aborted or timed-out runs stop awaiting uncooperative async hosts and admit no new tools.
    - Default test selection excludes paid live tests independently of credentials.
  artifacts:
    - path: drone/src/define.ts
      provides: Definition snapshot and frozen metadata
    - path: drone/src/run.test.ts
      provides: Async host deadline and no-new-effect fixtures
    - path: drone/vitest.config.ts
      provides: Explicit live-test exclusion
  key_links:
    - from: drone/src/run.ts
      to: drone/src/types.ts
      via: Documented cooperative cancellation and bounded caller wait
---

<objective>
Make the existing Drone primitive reproducible at definition admission and bounded at asynchronous host seams without adding a framework or claiming process isolation.
</objective>
<context>
@.planning/operations/360/PROGRAM.md
@.planning/phases/11-agent-lifecycle/11-CONTEXT.md
@.lavish/minion-qc-2026-09-08/agents-infra.md
@drone/src/define.ts
@drone/src/run.ts
</context>
<tasks>
<task type="auto">
  <name>Snapshot definition metadata without freezing caller objects</name>
  <files>drone/src/define.ts, drone/src/define.test.ts</files>
  <read_first>drone/src/types.ts, installed TypeBox clone implementation</read_first>
  <action>Add mutation-after-definition fixtures for model/fallbacks, tool callback/parameter schema, skills and output schema. Reuse installed TypeBox cloning for schema symbols/functions; snapshot definition records and freeze cloned metadata. Preserve callback identity and document that mutable callback closure state remains the host's responsibility. Do not mutate caller objects or introduce JSON serialization that drops TypeBox symbols.</action>
  <verify>cd drone &amp;&amp; node node_modules/vitest/vitest.mjs run src/define.test.ts</verify>
  <done>Independent caller mutations cannot alter admitted values; TypeBox validation still recognizes cloned schemas; original callback executes.</done>
</task>
<task type="auto">
  <name>Bound async waits and prevent new admission after abort</name>
  <files>drone/src/run.ts, drone/src/run.test.ts, drone/src/types.ts</files>
  <read_first>drone/src/run.ts, drone/src/run.test.ts</read_first>
  <action>Use one cleanup-safe abort-aware await helper at credential resolution, model resolution/completion and tool invocation. Map deadline/external abort consistently, absorb late promise rejection, do not start the next tool after abort. Keep real side-effect termination explicitly delegated to cooperative host or process isolation; a returned TIMEOUT is not a kill receipt.</action>
  <verify>cd drone &amp;&amp; node node_modules/vitest/vitest.mjs run src/run.test.ts</verify>
  <done>Never-resolving host fixture returns TIMEOUT; externally aborted run returns ABORTED; late resolution cannot invoke further tools; existing success/fallback/schema fixtures pass.</done>
</task>
<task type="auto">
  <name>Separate default unit discovery from explicit live evaluation</name>
  <files>drone/vitest.config.ts, drone/vitest.live.config.ts, drone/README.md</files>
  <read_first>drone/package.json, drone/src/streaming-schema.live.test.ts</read_first>
  <action>Extend Vitest default excludes with **/*.live.test.ts. Require DRONE_LIVE_TESTS=1 in the live config even when provider credentials exist, and document the exact invocation. Validate default discovery with a dummy key, never a real provider call.</action>
  <verify>cd drone &amp;&amp; node node_modules/vitest/vitest.mjs list --filesOnly; node node_modules/vitest/vitest.mjs run; node node_modules/typescript/bin/tsc --noEmit</verify>
  <done>Default discovery includes no live tests; explicit live config rejects absent opt-in; full unit suite passes without provider calls; any existing typecheck failures are evidenced and separately tracked.</done>
</task>
</tasks>
<verification>
Record red-green outputs, whole Drone unit result and typecheck identity. Independently review standards/spec before closing AGT requirements. Preserve pre-existing Drone WIP. No release or paid test.
</verification>
