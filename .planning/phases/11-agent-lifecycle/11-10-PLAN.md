---
phase: 11-agent-lifecycle
plan: "10"
type: execute
wave: 6
depends_on: ["11-03", "11-07"]
files_modified:
  - "packages/shells-bridge/src/bridge.ts"
  - "packages/shells-bridge/src/bridge.test.ts"
  - "packages/shells-bridge/src/run-journal.ts"
  - "packages/shells-bridge/src/run-journal.test.ts"
autonomous: true
requirements: ["AGT-04", "AGT-05", "JOB-02"]
readiness: "Source review confirmed unresolved restart replay and uncertain-terminal gaps in the preserved sender; exact native regression repair precedes ACP SDK replacement"
must_haves:
  truths:
    - "Only a newly committed admission grants this caller one dispatch; identical replay never re-prompts, including after restart."
    - "Transport failure, timeout and invalid terminal payload remain unresolved observations."
    - "Failed journal outcome persistence cannot release the local execution owner."
  artifacts:
    - path: "packages/shells-bridge/src/run-journal.ts"
      provides: "Atomic fresh-versus-replay admission result without breaking existing admit callers"
    - path: "packages/shells-bridge/src/bridge.test.ts"
      provides: "Native journal plus synthetic transport regressions for restart and uncertain outcomes"
  key_links:
    - from: "packages/shells-bridge/src/bridge.ts"
      to: "packages/shells-bridge/src/run-journal.ts"
      via: "Fresh committed admission dispatch authority and durable terminal persistence"
---
<objective>Prevent duplicate prompt execution and false terminal outcomes in the preserved durable sender before replacing its ACP protocol implementation.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/11-agent-lifecycle/11-03-PLAN.md
@.planning/phases/11-agent-lifecycle/11-03-SUMMARY.md
@.planning/phases/11-agent-lifecycle/11-07-VERIFICATION.md
@.planning/phases/11-agent-lifecycle/11-ACP-RESEARCH.md
@.planning/DECISIONS.md
@proposals/2026-09-08-platform-qc-remediation.md
The preserved sender is /home/nikolas/.cache/claude-tmp/meta-11-03. Exact verified SDK schema artifacts for stop-reason evidence are /home/nikolas/.cache/minion-qc/acp-artifact-review; these are evidence, not new product dependencies.
</context>
<boundaries>
Use a fresh ordinary private sender snapshot, preserving exact staged source and current dependencies from meta-11-03. Own exactly four listed source files. Private test config/guards/receipts are separate setup artifacts. No shared package, lock, protocol method, receiver, ACP client, config, restore, image, systemd, process supervision or active source/index edits. No branch/worktree/commit/push/deploy, paid provider or installed user harness startup. Preserve current journal schema/disk contract and old admit() return compatibility. Do not infer actual ACP conformance from a synthetic adapter or replay test. Separate official SDK adoption and caller integration remain required.

D360-19: a fresh committed journal insertion grants exactly one dispatch opportunity to that successful caller; returning an existing identical record grants none. A crash after insertion but before prompt is unresolved, not permission to retry. Do not use a read-before-admit check or process-local set as durable authority. Unknown prompt outcome stays unresolved and retains admission occupancy. A terminal outcome releases local ownership only after successful durable commit; delivery failure then remains retryable without re-execution.

Tests use bounded private SQLite files, native Node and synthetic WebSocket/ACP seams. No driver fault injection or external process termination. Deterministic journal-method failure is a local application seam, not a database-driver fault experiment. Root reviews exact source/runtime setup before independent acceptance. Keep legitimate same-process duplicate responses, pending terminal delivery and cancellation observations unchanged.
</boundaries>
<tasks>
<task type="auto">
<name>Reproduce missing sender invariants</name>
<files>packages/shells-bridge/src/bridge.test.ts, packages/shells-bridge/src/run-journal.test.ts</files>
<action>Pin all original sender source/runtime inputs. Add meaningful regressions: reopen a native journal with an unresolved admission and a fresh Bridge with empty activeRuns, deliver the same request and assert no prompt; simulate committed admission before dispatch and refuse automatic recovery; two journal handles contend on an identical insertion with only one fresh winner. Preserve existing terminal-before-restart replay controls. Cover arbitrary prompt rejection, timeout-like rejection, undefined/null/primitive/unknown stopReason and valid stable terminal responses. Replace the old test that incorrectly expects every rejection to become terminal error. Inject a deterministic journal commit method failure and assert local run/session ownership remains; no native driver faults. Retain existing valid delivery/retry and same-process duplicate tests.</action>
<verify><automated>New tests fail against unchanged candidate for the identified behavior, while existing positive controls remain meaningful.</automated></verify>
<done>The source findings have bounded executed regressions rather than only static claims.</done>
</task>
<task type="auto">
<name>Bind dispatch and terminal release to durable facts</name>
<files>packages/shells-bridge/src/run-journal.ts, packages/shells-bridge/src/bridge.ts</files>
<action>Add an atomic fresh-versus-replay API using the existing transaction implementation and preserve admit()'s JournalRun contract. Return dispatch authority only after successful insertion commit; conflict/busy/capacity validation remains. The sender returns the existing admission response for replay without creating active ownership or invoking a prompt. Current dispatch owns its original run/session generation. Rejections and unsupported result shapes record an unresolved observation without commitOutcome or releaseRun. Validate only the exact verified ACP stable stop reasons end_turn, max_tokens, max_turn_requests, refusal, cancelled; cancelled maps to aborted and the others to final with the reason preserved. Final means a protocol turn ended, not guaranteed task success. Do not accept an arbitrary nonempty string as proof. Release local ownership after successful journal commit only; delivery/receipt failure preserves the stored outcome for replay. If uncertainty persistence itself fails, retain the execution owner and propagate/report a fixed safe failure without fabricating a terminal. Preserve existing method names and canonical outcome shape.</action>
<verify><automated>All sender/journal native regressions execute with zero skips; existing admission/schema/delivery/cancellation tests pass.</automated></verify>
<done>One dispatch authority and durable terminal evidence govern execution across process restart and failure.</done>
</task>
<task type="auto">
<name>Freeze and retain ACP/operations boundaries</name>
<files>packages/shells-bridge/src/bridge.ts, packages/shells-bridge/src/bridge.test.ts, packages/shells-bridge/src/run-journal.ts, packages/shells-bridge/src/run-journal.test.ts</files>
<action>Run exact native tests, scoped types/format and original source/dependency/index preservation checks. Record executed red/green outcomes, native SQLite identity and exact four-file freeze. Update stale comments only within owned sites; retain paired TODOs for official ACP initialization/session/prompt/cancel mapping, actual lifecycle/termination and unresolved reconciliation. No automatic restart replay or invented completion is a recovery mechanism. Root independently inspects and repeats relevant native cases before source integration.</action>
<verify><automated>Root-reviewable four-file source and runtime receipt, no unresolved unpaired implementation ends or release claims.</automated></verify>
<done>Sender correction is locally qualified while full ACP, process lifecycle, historical recovery and deployment remain explicit.</done>
</task>
</tasks>
<threat_model>
| Boundary | Failure | Required mitigation |
|---|---|---|
| Durable record into process-local execution | Restart repeats an uncertain external effect | Atomic fresh committed insertion grants dispatch; replay grants none |
| Transport observation into durable terminal | Lost response masquerades as stopped/completed work | Known protocol terminal validation; otherwise unresolved occupancy |
| Journal persistence into local release | Failed commit frees owner and permits another execution | Release only after successful durable outcome write; native regression |
| Synthetic tests into production authority | Fake ACP masks actual protocol/process defects | Separate official SDK and real lifecycle gates with exact source TODOs |
</threat_model>
<verification>Native journal behavior and synthetic transport are separate evidence from real agent/provider effects. No global requirement closes from this four-file repair.</verification>
<output>Root writes 11-10-SUMMARY and independent acceptance; preserve original 11-03 historical receipts.</output>
