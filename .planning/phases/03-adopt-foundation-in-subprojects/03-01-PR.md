# 03-01 PR Record

- **PR:** https://github.com/NikolasP98/minion-ai/pull/77
- **Branch:** `feat/adopt-minion-stack` → `NikolasP98/minion-ai@DEV`
- **Opened:** 2026-04-20
- **CI runs:**
  - https://github.com/NikolasP98/minion-ai/actions/runs/24693394745
  - https://github.com/NikolasP98/minion-ai/actions/runs/24693410364
- **Commits on branch:**
  - `38f1230a7` feat(03-01): install @minion-stack/* devDeps + extend tsconfig from shared node variant
  - `d7771bd1b` feat(03-01): extend oxlint from shared preset + ship .env.defaults + refresh .env.example header

## Local verification (pre-push)

| Step | Command | Result | Notes |
|------|---------|--------|-------|
| Install | `pnpm add -D -w @minion-stack/tsconfig@^0.1.0 @minion-stack/lint-config@^0.1.0` | Pass | Both resolved from npm at 0.1.0 |
| Typecheck (tsgo) | `pnpm tsgo` | 27 pre-existing errors | Baseline confirmed unchanged via `git stash` bisection |
| Lint | `pnpm lint` | 68 pre-existing errors (134→68) | Adoption reduced errors |
| Plugin SDK dts | `pnpm build:plugin-sdk:dts` | 6 pre-existing errors | Same as DEV |
| Format check | `pnpm format:check` | Fails on pre-existing `.agents/skills/` YAML | Not adoption-caused |

## Baseline evidence

`git stash`-based A/B comparison performed during Task 1:
- Pre-adoption `tsgo`: 27 errors
- Post-adoption `tsgo` (with transitional override): 27 errors
- Post-adoption `tsgo` (without transitional override): 1643 errors

All pre-existing failures match memory `project_minion_ai_ci_patterns.md` — systemic CI failures on DEV independent of this change.

## CI gates

- `ci.yml` (oxlint + oxfmt + tsgo + tests) — running
- `install-smoke.yml` — running
- `secrets` scan — running
- `docs-scope`, `changed-scope`, `label`, `label-issues` — running

## Deviations documented

- Transitional `tsconfig.json` overrides: `noUncheckedIndexedAccess: false` + `noImplicitOverride: false`
- Transitional `.oxlintrc.json` overrides: 12 preset rules set to `off`
- Full rationale and follow-up plan: `.planning/phases/03-adopt-foundation-in-subprojects/03-01-ISSUES.md`
- Deferred follow-up logged in `.planning/phases/03-adopt-foundation-in-subprojects/deferred-items.md`

## Do NOT merge

Per D-24: user merges after CI passes. Note: pre-existing DEV failures may still show red; compare against DEV baseline before merging.
