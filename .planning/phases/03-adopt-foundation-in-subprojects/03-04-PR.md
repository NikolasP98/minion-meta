# 03-04 PR Record

- **PR:** https://github.com/NikolasP98/paperclip/pull/1
- **Branch:** `feat/adopt-minion-stack` → `NikolasP98/paperclip@minion-integration`
- **Opened:** 2026-04-20T22:42:33Z
- **Commits on branch:**
  - `89635bb6` feat(03-04): install @minion-stack/* at workspace root + extend tsconfig.base.json from shared node variant
  - `f11bf290` feat(03-04): wire Prettier + expand .env.example to full Infisical coverage + ship .env.defaults

## Local verification (pre-push)

| Step | Command | Result | Notes |
|------|---------|--------|-------|
| Install | `pnpm add -D -w @minion-stack/tsconfig@^0.1.0 @minion-stack/lint-config@^0.1.0 prettier@^3` | Pass | All three resolved from public npm |
| Typecheck | `pnpm -r typecheck` | 0 errors across 21 packages | With transitional overrides layered |
| Tests | `pnpm test:run` | 1187/1187 passing | One unhandled postgres cleanup error (race, pre-existing, out-of-scope) |
| Build | `pnpm build` | All 21 packages build clean | `ui` emits large-chunk warnings (pre-existing) |
| Prettier smoke | `pnpm exec prettier --check package.json` | Pass | Via local `prettier.config.cjs` shim |
| Doctor | `node packages/cli/dist/index.js doctor` (meta-repo) | 114 vars resolved, no drift flags specific to adoption | Same `symlink-ext (drift)` misclassification as 03-01 (pnpm content-addressable store) |

## Baseline evidence

`git stash`-based comparison of `pnpm -r typecheck`:
- Adopted without transitional override: 428 errors across 13 packages
- Adopted with transitional override: 0 errors
- Shared base's `noUncheckedIndexedAccess: true` + `noImplicitOverride: true` are the cause; per-package fixing deferred to Phase 8

`pnpm test:run` was run both before and after the branch's commits — same 1187 tests pass; unhandled postgres error is a race in cleanup of `heartbeat-comment-wake-batching.test.ts`, pre-existing.

## Dockerfile deps stage note

Adding `@types/node` to `packages/shared/package.json` did NOT break the "Validate Dockerfile deps stage" CI job — the Dockerfile already `COPY packages/shared/package.json packages/shared/` in its deps stage.

## CI gates

**Critical finding:** All paperclip workflows (`pr.yml`, `docker.yml`, `release.yml`, `refresh-lockfile.yml`) trigger only on `master` pushes/PRs. Our PR targets `minion-integration`, so **no CI runs automatically on this PR**.

Recommended before merge:
- Run `pnpm install --frozen-lockfile && pnpm -r typecheck && pnpm test:run && pnpm build` locally once more against HEAD of `minion-integration` rebased
- OR retarget the PR to `master` after user approves scope (would need lockfile handled via `chore/refresh-lockfile` branch first)

## Scope notes

- `tsconfig.json` (root solution-style) UNCHANGED — preserves references to 14 packages
- `server/tsconfig.json` UNCHANGED — inherits `@minion-stack/tsconfig/node.json` transitively via `../tsconfig.base.json`
- `ui/tsconfig.json` UNCHANGED — deferred to Phase 8 (React variant of shared tsconfig needed)
- Full linter deferred to Phase 8 (paperclip has no linter today)
- `pnpm-lock.yaml` NOT committed (policy compliance)

## Deviations documented

1. **Transitional `tsconfig.base.json` overrides** — `noUncheckedIndexedAccess: false` + `noImplicitOverride: false` (same Phase 8 follow-up bucket as 03-01)
2. **`@paperclipai/shared` missing `@types/node`** — added as devDep; surfaced by node.json variant's `types: ["node"]` declaration
3. **`prettier.config.cjs` shim** — `@minion-stack/lint-config@0.1.0` ships a broken Prettier entry (CJS in ESM package); shim inlines values

Full rationale: `.planning/phases/03-adopt-foundation-in-subprojects/03-04-ISSUES.md`

## Fallback path (if minion-integration → master cutover needed)

1. Rebase `feat/adopt-minion-stack` onto `master` locally
2. Resolve conflicts (unlikely — changes are config-only)
3. Push `chore/refresh-lockfile` branch with only the regenerated lockfile, merge first
4. Update this PR's base from `minion-integration` → `master`
5. pr.yml pipeline will then run

## Do NOT merge

Per D-24, user merges after review. Note: since pr.yml doesn't run on this base, user should verify by pulling the branch locally and running `pnpm install && pnpm -r typecheck && pnpm test:run && pnpm build`.
