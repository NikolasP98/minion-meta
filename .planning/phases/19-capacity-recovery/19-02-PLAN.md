---
phase: 19-capacity-recovery
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - "minion_factory/runner/src/queue.capacity.test.ts"
  - "minion_hub/src/server/db/pg-client.capacity.test.ts"
  - ".planning/phases/19-capacity-recovery/19-BOTTLENECKS.md"
  - ".planning/phases/19-capacity-recovery/19-CAPACITY-REPAIR-PLANS.md"
  - ".planning/phases/19-capacity-recovery/19-AFTER.md"
autonomous: true
requirements: ["CAP-02"]
readiness: "Measure before mutation; source repair plans are generated from actual limiting seams, not guessed capacity."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Tests reproduce measured growth/starvation and preserve required scheduling semantics."
    - "Every measured bottleneck has bounded repair plan and matching regression; after evidence must exist before closure."
  artifacts:
    - path: "minion_factory/runner/src/queue.capacity.test.ts"
      provides: "Isolate actual boundedness bottlenecks"
    - path: ".planning/phases/19-capacity-recovery/19-CAPACITY-REPAIR-PLANS.md"
      provides: "Apply only measured scoped bounds then repeat workload"
  key_links:
    - from: "minion_factory/runner/src/queue.capacity.test.ts"
      to: "minion_factory/runner/src/queue.ts"
      via: "Read-first contract and behavior verification for isolate actual boundedness bottlenecks"
    - from: ".planning/phases/19-capacity-recovery/19-CAPACITY-REPAIR-PLANS.md"
      to: ".planning/phases/19-capacity-recovery/19-BOTTLENECKS.md"
      via: "Read-first contract and behavior verification for apply only measured scoped bounds then repeat workload"
---

<objective>
Repair measured queue and pool bounds with fairness proof.
Purpose: CAP-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/19-capacity-recovery/19-CONTEXT.md
@AGENTS.md
@minion_factory/runner/src/queue.ts
@minion_factory/runner/src/db.ts
@minion_hub/src/server/db/pg-client.ts
@minion_hub/src/server/services/bg-runtime.ts
@.planning/phases/19-capacity-recovery/19-BOTTLENECKS.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Isolate actual boundedness bottlenecks</name>
  <files>minion_factory/runner/src/queue.capacity.test.ts, minion_hub/src/server/db/pg-client.capacity.test.ts, .planning/phases/19-capacity-recovery/19-BOTTLENECKS.md</files>
  <read_first>minion_factory/runner/src/queue.ts, minion_factory/runner/src/db.ts, minion_hub/src/server/db/pg-client.ts, minion_hub/src/server/services/bg-runtime.ts</read_first>
  <behavior>Tests reproduce measured growth/starvation and preserve required scheduling semantics. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Use baseline to choose exact expensive query/admission/pool paths, including Factory all-history materialization. Build synthetic old-run histories, one noisy tenant, slow downstream and retry storm tests. Assert fairness/order semantics from current policy, bounded rows/memory/connection waits and rejection behavior; never change business priorities to make tests pass. If baseline doesn't show the suspected bottleneck, retain evidence and choose measured seam.</action>
  <verify><automated>(cd minion_factory/runner &amp;&amp; node --import tsx --test src/queue.capacity.test.ts) &amp;&amp; (cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/db/pg-client.capacity.test.ts)</automated></verify>
  <done>Tests reproduce measured growth/starvation and preserve required scheduling semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Apply only measured scoped bounds then repeat workload</name>
  <files>.planning/phases/19-capacity-recovery/19-CAPACITY-REPAIR-PLANS.md, .planning/phases/19-capacity-recovery/19-AFTER.md</files>
  <read_first>.planning/phases/19-capacity-recovery/19-BOTTLENECKS.md</read_first>
  <behavior>Every measured bottleneck has bounded repair plan and matching regression; after evidence must exist before closure. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Generate2-3task child plans per chosen source seam with exact SQL/index/admission/pool files, measured before/after target and rollback. Typical all-history query fix must query relevant lineage rows without breaking dependency ordering; pool caps need timeout/admission contracts, not arbitrary larger max. Execute only after ownership approval and rerun identical workload. CAP-02 remains open until concrete fixes and repeat fairness proof pass.</action>
  <verify><automated>node /home/nikolas/.claude/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/19-capacity-recovery/19-02-PLAN.md</automated></verify>
  <done>Every measured bottleneck has bounded repair plan and matching regression; after evidence must exist before closure.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Tenant workload / shared resources / recovery stores | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-19-02-01 | D/I | Noisy tenant exhausts resources or restore resurrects erased copies | mitigate | Bounded synthetic workload, measured fairness and decided cross-store recovery policy. |
| T-19-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/19-capacity-recovery/19-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>
