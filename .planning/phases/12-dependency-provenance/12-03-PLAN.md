---
phase: 12-dependency-provenance
plan: "03"
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - "scripts/qc/package-provenance.mjs"
  - "scripts/qc/package-provenance.test.mjs"
  - "packages/shared/package.json"
  - "packages/crm-sdk/package.json"
  - "packages/design-tokens/package.json"
  - "scripts/qc/package-consumer-matrix.mjs"
  - "scripts/qc/package-consumer-matrix.test.mjs"
  - ".planning/phases/12-dependency-provenance/12-PACKAGE-MATRIX.md"
autonomous: true
requirements: ["DEP-03"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Same version/different digest rejected, token contract hashes verified, shipped archive license text identified for every admitted package."
    - "Clean consumers resolve declared exports/peers from the same archive identity; unsupported combinations explicitly blocked."
  artifacts:
    - path: "scripts/qc/package-provenance.mjs"
      provides: "Build archive manifest and immutable identity checks"
    - path: "scripts/qc/package-consumer-matrix.mjs"
      provides: "Exercise clean consumer installation from exact archives"
  key_links:
    - from: "scripts/qc/package-provenance.mjs"
      to: "packages/shared/src/index.ts"
      via: "Read-first contract and behavior verification for build archive manifest and immutable identity checks"
    - from: "scripts/qc/package-consumer-matrix.mjs"
      to: "minion_hub/package.json"
      via: "Read-first contract and behavior verification for exercise clean consumer installation from exact archives"
---

<objective>
Prove internal archive identity and shipped licenses.
Purpose: DEP-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-CONTEXT.md
@AGENTS.md
@packages/shared/src/index.ts
@packages/design-tokens/contract.json
@packages/crm-sdk/src/index.ts
@minion_hub/package.json
@minion_site/package.json
@paperclip-minion/package.json

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Build archive manifest and immutable identity checks</name>
  <files>scripts/qc/package-provenance.mjs, scripts/qc/package-provenance.test.mjs, packages/shared/package.json, packages/crm-sdk/package.json, packages/design-tokens/package.json</files>
  <read_first>packages/shared/src/index.ts, packages/design-tokens/contract.json, packages/crm-sdk/src/index.ts</read_first>
  <behavior>Same version/different digest rejected, token contract hashes verified, shipped archive license text identified for every admitted package. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Pack each owned package into temporary artifacts using its native tooling. Record name/version/source revision/full SHA256/archive file list and SPDX/license-file presence. Verify license ownership before including source LICENSE/NOTICE files; a metadata MIT label alone does not prove archive content. Fail repeated version with differing bytes; propose distinct versions/changesets without publishing. Remaining packages get exact gap tasks if their archive fails, not blanket metadata edits.</action>
  <verify><automated>node --test scripts/qc/package-provenance.test.mjs</automated></verify>
  <done>Same version/different digest rejected, token contract hashes verified, shipped archive license text identified for every admitted package.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise clean consumer installation from exact archives</name>
  <files>scripts/qc/package-consumer-matrix.mjs, scripts/qc/package-consumer-matrix.test.mjs, .planning/phases/12-dependency-provenance/12-PACKAGE-MATRIX.md</files>
  <read_first>minion_hub/package.json, minion_site/package.json, paperclip-minion/package.json</read_first>
  <behavior>Clean consumers resolve declared exports/peers from the same archive identity; unsupported combinations explicitly blocked. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Use disposable copies with no credentials to install the exact packed archives in Hub/Site/Paperclip and compile their real imports. Record consumer lock hash and installed byte digest, especially design tokens previously sharing0.1.0 with different bytes. Do not rewrite active consumer locks in this verification task or npm publish. Matrix failure creates an exact adoption task under phase14 before release.</action>
  <verify><automated>node --test scripts/qc/package-consumer-matrix.test.mjs &amp;&amp; node scripts/qc/package-consumer-matrix.mjs --fixture-only</automated></verify>
  <done>Clean consumers resolve declared exports/peers from the same archive identity; unsupported combinations explicitly blocked.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Registry/archive / build / installed consumer | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-12-03-01 | T/E | Substituted or vulnerable dependency | mitigate | Exact digests, lock coherence, peer checks, safe compatibility fixtures. |
| T-12-03-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/12-dependency-provenance/12-03-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

