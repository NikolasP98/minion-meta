---
phase: 12-dependency-provenance
plan: "06"
type: execute
wave: 1
depends_on: ["12-05"]
status: admitted_execution_gated
autonomous: true
requirements: [DEP-01, DEP-02]
files_modified:
  - minion_hub/scripts/qc/trace-build-contained.mjs
  - minion_hub/scripts/qc/trace-build-contained-worker.mjs
  - minion_hub/scripts/qc/trace-build-contained.test.mjs
  - .planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESULTS.md
must_haves:
  truths:
    - Every application trace is preceded by a passing isolation proof in the same namespace and launch.
    - nft performs ordinary absent-path probes against the isolated filesystem, never the host tree or a suppressing hook.
    - Artifact/runtime identity, complete emitted closure, resource outcome and remaining uncertainty are recorded without implying a deployment pass.
  artifacts:
    - path: minion_hub/scripts/qc/trace-build-contained.mjs
      provides: Fail-closed explicit-manifest namespace launcher with exact runtime mounts and lifecycle controls
    - path: minion_hub/scripts/qc/trace-build-contained-worker.mjs
      provides: Same-launch isolation checks and observed installed nft tracing inside the isolated root
    - path: minion_hub/scripts/qc/trace-build-contained.test.mjs
      provides: Actual namespace filesystem/network/immutability/cleanup qualification and actual-nft asset fixtures
    - path: .planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESULTS.md
      provides: Exact executed identities, isolation proof and bounded full-graph findings
  key_links:
    - from: minion_hub/scripts/qc/trace-build-contained.mjs
      to: minion_hub/scripts/qc/trace-build-contained-worker.mjs
      via: Hash-pinned read-only worker in mandatory private namespaces, before loading the artifact's installed nft
    - from: minion_hub/scripts/qc/trace-build-contained-worker.mjs
      to: minion_hub/node_modules/@vercel/nft/out/node-file-trace.js
      via: Exact selected artifact's installed implementation with filesystem-root semantics and no asset suppression
    - from: .planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESULTS.md
      to: .planning/phases/12-dependency-provenance/12-ADVISORIES.md
      via: Inform the unresolved packaging gate without applying the frozen dependency candidate
---

<objective>Obtain faithful bounded build-graph evidence inside a proven isolated filesystem, so ordinary absent-path probes do not consult host metadata and the unresolved packaging OOM can be investigated without suppressing dependencies.</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Use the installed GSD structure. No automatic commit, dependency install or worktree action is authorized.
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/phases/12-dependency-provenance/12-05-PLAN-REVIEW.md
@.planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md
@.planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESEARCH.md
@.planning/phases/12-dependency-provenance/12-ADVISORIES.md
@minion_hub/scripts/qc/trace-build-graph.mjs
@minion_hub/scripts/qc/trace-build-graph.test.mjs
</context>

## Admission and ownership

Root independently admitted this PLAN after reviewing 12-05 source/results and passing its eight harness tests. It is included in the exact execution allowlist. Tasks 1 and 2 may execute under their four-file ownership; Task 3 requires separate root acceptance of actual isolation/asset evidence and an explicit heavy-resource window. The research document records successful lightweight namespace capability probes only. No application artifact has been mounted or traced by this proposal.

Only the four listed implementation/result files may change after admission, plus the normal 12-06 summary output. Existing 12-05 harness/results, product source, build configuration, CI, package/lockfiles, node_modules, generated production artifacts and locale behavior remain outside this scope. The root owns global planning, proposals, independent reviews, resource windows and dependency application. Preserve all shared WIP.

No sudo, setuid helper addition, sysctl change, system package or application dependency install, Docker socket escalation, network-enabled sandbox, paid API call, production connection, build, deployment or source repair. A denied namespace/control must end qualification; there is no weaker fallback. Do not bind the host root or broad system/home/checkout directories. Do not make host-tree metadata exceptions or return fabricated absence from tracing hooks.

<tasks>
<task type="auto">
<name>Task 1: Implement the exact-root launcher and same-launch isolation gate</name>
<files>minion_hub/scripts/qc/trace-build-contained.mjs, minion_hub/scripts/qc/trace-build-contained-worker.mjs, minion_hub/scripts/qc/trace-build-contained.test.mjs</files>
<read_first>minion_hub/scripts/qc/trace-build-graph.mjs, .planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESEARCH.md, installed bwrap --help</read_first>
<action>Implement a Node-native launcher with an explicit versioned manifest and marker for an owned canonical temporary artifact and separate empty owned output directory. Reuse safe validation concepts from 12-05 without modifying that harness or weakening its host-tree guard. Reject active checkout roots, marker/hash mismatch, path traversal, unexpected private environment files at any depth, escaped symlinks and sockets/FIFOs/devices before launch. Create and hash a complete regular-file/symlink inventory of the immutable source artifact, covering installed nft and dependencies, while excluding only the separately identified diagnostic output. Record package/lock/entry and worker hashes explicitly; reject changes at a post-execution inventory comparison. Never run an executable supplied by the artifact during runtime discovery.

Resolve only the explicitly selected trusted installed Node executable and its ELF interpreter/shared-library closure using read-only installed tooling. Record hashes and canonical/logical path mappings for each file. Missing or unexpected runtime entries fail rather than mounting their parent directory. Pin the Bubblewrap executable identity and supported mandatory options. Assemble arguments directly without a shell and allow no arbitrary extra bind/network/command arguments in the manifest.

Create an empty root using Bubblewrap with mandatory private user, PID, network, IPC and UTS namespaces, --disable-userns, --assert-userns-disabled, --cap-drop ALL, --new-session, --die-with-parent and --clearenv. Bind only the artifact read-only at its original absolute temporary path, individual runtime files read-only, the diagnostic worker/manifest read-only, private proc/dev support, and a single empty task output directory read-write. Expose no user home, host temp tree, database/browser/D-Bus sockets or host proc. Retain filesystem-root trace semantics. Close inherited descriptors except explicit standard output/error and required verified control descriptors. No credential-bearing environment; permit only known synthetic locale/PWD values if required. Prefer making the constructed root read-only after mounts, with the sole output mount writable; record any required ephemeral runtime paths explicitly.

Before requiring nft or reading an application entry, the same worker and namespace must verify the expected mount inventory, runtime hashes, private PID/network namespace identities, zero effective capabilities and no-new-privileges. It must prove a synthetic sentinel outside the bound tree is absent, the application artifact is readable but not writable, the host cwd/home and known outside URL/setup probes are absent, and a parent-owned loopback listener is unreachable from the child. Do not attempt external network access. Listener and sentinel are generated only in the owned temporary fixture and contain no secrets. The parent also verifies the namespace/control status; an environment flag or self-reported success alone cannot enable tracing. Any failed proof exits before an nft/entry-loaded marker.

Parent-enforce a short probe timeout, capped stdout/stderr, bounded Node heap and process-group cleanup with SIGTERM then SIGKILL. Bubblewrap's PID namespace/init and die-with-parent must terminate descendants; verify all owned listeners, descriptors, timers and processes close on success, failure, signal and timeout. Use a bounded output/event byte ceiling so the writable evidence mount cannot grow indefinitely. Isolation/probe mode must be available without an application trace.</action>
<verify><automated>cd minion_hub &amp;&amp; node --test scripts/qc/trace-build-contained.test.mjs</automated></verify>
<done>Actual namespace fixtures demonstrate host-path and parent-loopback isolation, read-only artifact enforcement and descendant cleanup. Missing runtime/namespace/required option fails rather than skips. Parent independently admits the isolation proof before any application artifact is traced.</done>
</task>

<task type="auto">
<name>Task 2: Qualify actual nft behavior without suppressing paths</name>
<files>minion_hub/scripts/qc/trace-build-contained-worker.mjs, minion_hub/scripts/qc/trace-build-contained.test.mjs</files>
<read_first>selected artifact's installed @vercel/nft out/node-file-trace.js and out/fs.js, minion_hub/scripts/qc/trace-build-graph.test.mjs</read_first>
<action>After the same-launch isolation gate, resolve the exact installed nft from the marked artifact. Observe it using its unchanged cached IO/limiter and documented resolver hooks. Trace with filesystem-root base and processCwd, default conditions and analysis, unlimited analysis depth, and initial IO concurrency 1024. All ordinary read/stat/readlink/glob operations execute inside the namespace. Do not use ignore callbacks, synthetic ENOENT, package patches, disabled analysis, lowered depth, excluded assets or altered source. Record import/read/glob categories where supported, emitted files and reasons, warning counts, file hashes/bytes and logical cached-read counts; do not claim physical IO counts. Distinguish artifact, runtime-support and kernel-support paths; unexpected emitted support assets make deployment interpretation incomplete rather than silently filtering them.

Use the actual existing nft package in small copied fixtures to prove an ordinary import and runtime asset are included; dynamic asset matches and reasons survive; absolute URL suffix literals receive ordinary absence inside the namespace; a deliberate absolute asset included in the isolated fixture is retained; and a missing required relative dependency produces explicit incomplete/error evidence. The absolute fixture must live in the synthetic artifact layout, not require a generic arbitrary host mount. Assert no handler execution, no network, and no entry-loaded marker after a failed isolation gate. All selected inputs remain hash-identical after the run. End-to-end negative cases must include stale runtime hash, escaped symlink, unexpected binding, missing mandatory namespace capability and a child/descendant that ignores SIGTERM.

Record harness/runtime/manifest identities in every process result, including preflight rejection and timeout where available. A warning, missing required asset, outside policy failure, output budget, timeout, signal or OOM is explicit incomplete evidence. Missing runtime must fail the test command; do not skip qualification.</action>
<verify><automated>cd minion_hub &amp;&amp; node --test scripts/qc/trace-build-contained.test.mjs</automated></verify>
<done>The final harness passes both actual isolation and actual-nft asset-preservation tests with complete identities. No path suppression or product code change is used to obtain a passing fixture.</done>
</task>

<task type="auto">
<name>Task 3: Execute bounded contained traces and report the remaining decision</name>
<files>.planning/phases/12-dependency-provenance/12-BUILD-CONTAINMENT-RESULTS.md</files>
<read_first>.planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md, .planning/phases/12-dependency-provenance/12-ADVISORIES.md</read_first>
<action>Do not start this task until root independently reviews the isolation/asset evidence and explicitly grants a heavy-resource window. Supply the frozen original-lock and candidate artifact inventories and exact runtime/worker identities. Run sequentially with a 60-second wall deadline and 2048 MiB Node heap ceiling per trace: candidate messages module, candidate generated manifest, candidate full function entry, and baseline full function entry. Each run repeats the same-launch isolation proof; immutable input identities are checked before and after. No full application build is authorized. Stop further traces if isolation or process cleanup fails. Timeout/OOM on a trace can be recorded while moving to the next admitted comparison, but no higher resource ceiling or IO-concurrency variation is authorized here.

Preserve sanitized local evidence and durable hashes, parent/child timing and peak RSS/CPU with measurement limits, exact final file/reason lists when available, warnings, byte/read counts and exit/budget outcomes. Record ordinary absent results for the two paths that blocked 12-05. A complete isolated trace is evidence only for the enumerated artifact/runtime root, not a Vercel deployment qualification; check every expected relative/absolute runtime asset against the deployment contract and flag unresolved external setup-script ownership. Compare with the previous bounded results without treating instrumentation changes as application improvements. If neither trace discriminates the OOM cause, state the remaining uncertainty and the next exact diagnostic comparison; do not select a product repair by intuition.

Write results with the frozen 12-01 candidate still pending and all relevant requirements open. Add TODO(handoff) at the launcher result/decision boundary and send root the unresolved cause/asset/completeness details for the matching QC proposal. Root owns any further diagnostic or repair PLAN, packaging acceptance and EN/ES routing gates. Release the resource window only after owned children/listeners/descriptors have all closed.</action>
<verify><automated>cd minion_hub &amp;&amp; node scripts/qc/trace-build-contained.mjs --manifest "$MINION_CONTAINED_TRACE_MANIFEST"</automated></verify>
<done>Exact contained-run identities and outcomes are recorded; a reproduced cause is distinguished from timeout/incomplete evidence. No dependency application, build repair, deployment or requirement closure is implied.</done>
</task>
</tasks>

<verification>Independent review must inspect launcher arguments and exposed files, same-launch isolation before nft loading, network/descriptor/process cleanup, actual asset preservation and evidence identity. Synthetic isolation is necessary but not sufficient for a real trace: repeat the gate in each launch. A complete trace or lower memory result alone cannot waive complete Vercel packaging and deployment closure, EN/ES direct URLs/redirects, locale isolation, SSR/hydration, localized links and static assets.</verification>

<output>Write 12-06-SUMMARY after admitted execution. Root owns independent 12-06 verification, global admission/allowlist/counts/proposals, and any next repair decision. Keep DEP-01/DEP-02 and the 12-01 candidate build gate open.</output>


## Root-admitted public vendor default classification amendment

The fixed public Bottleneck file is classified by exact content equality with immutable source commit 8a8bde9cde151c278397703c48f2bba4ded2bae7. The classification appendix records the evidence and the unverified registry-tarball boundary. Under the existing launcher/test ownership, permit only artifact-relative `node_modules/bottleneck/.env`, a regular 37-byte file with SHA-256 `9a71de52ef575866ca37dce0fe306c2571ed84f780ae2e8a263a6135fc42ac74`, accompanied by the regular package.json SHA-256 `9e73efe094fc7802ce2288f1e342e9f28c2fe75f017002a96a35a478eb1df4d9`. Do not allow an escaped or symlinked environment file or package identity. Retain the file and hash it in the ordinary complete inventory and read-only artifact mount.

This allowance does not match arbitrary `.env` files, other dependency locations, changed contents or package metadata. Add exact-positive, altered-content, alternate-path and mismatched-package tests; the existing private-environment rejection tests remain mandatory. Do not print file values. No nft hook/asset filter, source/dependency change, new mount or higher resource limit is admitted. Root independently checks this delta before the four existing Task 3 traces resume.
