---
phase: 12-dependency-provenance
plan: "05"
type: execute
wave: 1
depends_on: []
status: admitted
autonomous: true
requirements: [DEP-01, DEP-02]
files_modified:
  - minion_hub/scripts/qc/trace-build-graph.mjs
  - minion_hub/scripts/qc/trace-build-graph.test.mjs
  - .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESEARCH.md
  - .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md
must_haves:
  truths:
    - Diagnostic input identity and resource limits are enforced before tracing any real build artifact.
    - Actual single-module and full-graph evidence distinguishes a reproduced culprit from an untested hypothesis or timeout.
    - No product build repair, exclusion, dependency application or requirement closure is implied by diagnostic completion.
  artifacts:
    - path: minion_hub/scripts/qc/trace-build-graph.mjs
      provides: Credential-free isolated trace runner with input identity and process limits
    - path: minion_hub/scripts/qc/trace-build-graph.test.mjs
      provides: Negative isolation, identity and timeout/cleanup fixtures
    - path: .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md
      provides: Exact trace evidence and a bounded repair decision or explicit unresolved cause
  key_links:
    - from: minion_hub/scripts/qc/trace-build-graph.mjs
      to: minion_hub/node_modules/@vercel/nft/out/node-file-trace.js
      via: Resolve the exact installed dependency in a marked disposable copy; preserve production source
    - from: .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md
      to: .planning/phases/12-dependency-provenance/12-02-PLAN.md
      via: Route proven translation-generation causes to the independently scoped supported migration
---

<objective>Identify why Hub Vercel packaging exhausts memory before changing build configuration or admitting the frozen 12-01 dependency candidate.</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
The installed GSD workflow is under ~/.claude; the skill catalog's ~/.Codex path is stale. No installation or automatic commit is authorized.
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-01-SUMMARY.md
@.planning/phases/12-dependency-provenance/12-ADVISORIES.md
@.planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESEARCH.md
@.planning/phases/12-dependency-provenance/12-02-PLAN.md
@minion_hub/svelte.config.js
@minion_hub/vite.config.ts
</context>

## Admission and boundaries

Root must independently admit this exact diagnostic scope and grant file/resource ownership. It can run while 12-01 remains pending because it consumes frozen failure artifacts; it does not depend on falsely marking 12-01 complete. Root owns global planning, proposal updates and the manifest/lock transaction. No other heavy build, typecheck or browser validation runs concurrently with the trace experiments.

Only the four listed files may change. No edits to package.json, lockfiles, node_modules, generated production modules, CI, Svelte/Vite config, locale hooks, routes or application code. No dependencies, application environment reads, production connections, deployment, worktree/branch/stash/commit action. Read only the marked disposable artifact tree. Any outside-tree access attempt is evidence to report and must stop that trace before reading content; containment stops are not successful complete traces.

Use primary upstream findings in the research record as hypotheses. Prefer supported upstream settings/releases after a culprit is reproduced. Do not maintain an in-house trace bypass. Any required repair needs an exact child PLAN and fresh independent review before source/config changes. If it relies on 12-02 while 12-01 is still pending, root must explicitly resolve that dependency/ownership ordering rather than skipping a prerequisite.

<tasks>
<task type="auto">
<name>Task 1: Build and verify the bounded trace harness</name>
<files>minion_hub/scripts/qc/trace-build-graph.mjs, minion_hub/scripts/qc/trace-build-graph.test.mjs</files>
<read_first>minion_hub/svelte.config.js, minion_hub/node_modules/@vercel/nft/out/node-file-trace.js, minion_hub/node_modules/@vercel/nft/out/analyze.js, .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESEARCH.md</read_first>
<action>Create a Node-native diagnostic harness using the existing installed nft API through a selected disposable root. Require an explicit JSON input manifest and marker, canonical root beneath the OS temporary directory, exact package/lock/entry SHA-256 values and an output location in that marked tree. Reject active checkout roots, absent/stale markers, hash mismatch, missing generated entries, nonempty private environment bindings, path traversal and symlink escape before starting a child. Resolve dependencies only from that copy, with no install fallback. Launch each trace in its own process group with an allowlisted environment and parent-enforced wall/heap budgets. Initial real traces use at most 60 seconds and 2 GiB Node heap each, sequentially; a larger budget needs a recorded root resource decision. Record stdout/stderr locally, sanitized relative-path categories, file/byte counts, reasons and resource/termination metadata; never log source content or private paths. Hook reads/resolution for observation, validate the nft glob/ignore boundary before glob expansion, and refuse outside-root content access; preflight all artifact symlinks. A containment guard must mark the run blocked if it suppresses any path, even when nft subsequently returns a file list; that list is never a complete trace. Record blocked access separately from success. Diagnostic files may be written only under the selected marked temporary output tree. Add synthetic fixtures using the actual nft library: an ordinary imported asset is retained, a dynamic asset's reason is recorded, an absent entry/hash mismatch/outside symlink is rejected, and an intentionally stuck child is killed with no child or timer left alive. Missing runtime must fail qualification rather than skip.</action>
<verify><automated>cd minion_hub &amp;&amp; node --test scripts/qc/trace-build-graph.test.mjs</automated></verify>
<done>Positive dependency/asset preservation and negative isolation/identity/cleanup fixtures pass against installed nft; the harness cannot mistake a timeout or containment stop for a successful trace.</done>
</task>

<task type="auto">
<name>Task 2: Run discriminating traces and select the next exact scope</name>
<files>minion_hub/scripts/qc/trace-build-graph.mjs, .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESEARCH.md, .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESULTS.md</files>
<read_first>.planning/phases/12-dependency-provenance/12-01-SUMMARY.md, .planning/phases/12-dependency-provenance/12-BUILD-TRACE-RESEARCH.md, .planning/phases/12-dependency-provenance/12-02-PLAN.md</read_first>
<action>After root releases the heavy-work window, produce marked trace manifests for the already compiled original-lock and candidate copies, preserving their source/lock/entry identities. Run sequentially: messages.js plus its runtime dependency (60-second/1-GiB diagnostic budget); generated manifest (120-second/2-GiB); complete Vercel function entry (180-second/4-GiB). These are investigative limits, not product SLOs. Retain normal analysis/conditions and the adapter's base/processCwd semantics unless a labeled containment stop prevents the experiment. Record the exact first unsupported access and stop rather than widening filesystem scope. If single-module evidence is cheap but graph evidence grows, permit one additional 180-second/4-GiB comparison with lower IO concurrency only, recording unchanged trace options and any closure differences. Do not disable glob analysis, lower depth or ignore a dependency to claim success. Compare actual module counts, bytes, reasons, peak RSS, CPU/time and exit states; identify a reproduced cause only when the experiment discriminates it. Refresh official adapter/nft/Paraglide issue/release evidence and document how it relates to the observed installed source. Results must recommend a bounded supported repair or state exactly what remains unknown. Root then creates/reviews the repair scope, including deployment completeness and EN/ES routing gates; this task performs no repair and cannot close DEP-01/DEP-02.</action>
<verify><automated>cd minion_hub &amp;&amp; node scripts/qc/trace-build-graph.mjs --manifest "$MINION_TRACE_MANIFEST"</automated></verify>
<done>Results preserve exact identities and resource outcomes, distinguish reproduced cause from uncertainty, and define the next repair or diagnostic scope with no weakened packaging gate.</done>
</task>
</tasks>

<verification>Independent review checks the manifest guard, process cleanup and actual trace evidence. A completed partial/single-module trace does not prove the full runtime closure. Future repair acceptance requires complete Vercel output plus dependency/assets closure and EN/ES direct routes, redirects, request locale isolation, SSR/hydration and localized links. No module omission, adapter switch, increased heap or route split alone can satisfy that gate.</verification>

<output>Write 12-05-SUMMARY only after admitted diagnostics execute; root owns independent 12-05-VERIFICATION, global proposal/allowlist/status and any repair PLAN. Keep an in-code TODO(handoff) at the diagnostic decision boundary plus the root-owned QC proposal for an unresolved cause or repair. Diagnostic completion leaves 12-01 candidate admission and platform requirements pending.</output>
