---
phase: 12-dependency-provenance
plan: "08"
type: execute
wave: 1
depends_on: []
status: setup_admitted
autonomous: true
requirements: [DEP-03]
files_modified:
  - packages/shared/package.json
  - packages/shared/tsconfig.build.json
  - packages/shared/scripts/build.mjs
  - packages/shared/README.md
readiness: "Root independently reviewed the four-file plan and admitted private snapshot/fixture preparation. Root reviews exact setup before private implementation and native artifact execution; active source adoption remains separate."
closure_policy: "This child can qualify shared packaging behavior only. License provenance, immutable release version, installed consumers and DEP-03 remain separate open gates."
must_haves:
  truths:
    - The shared release archive contains current production JavaScript, public declarations and README, without tests, maps, stale output or local source paths.
    - All five public entry points and optional ws peer semantics remain unchanged, and all ten current source tests remain in the existing noEmit typecheck.
    - The build refuses linked output paths or ancestors before cleanup, and compiler failure prevents a successful pack or reuse of stale output.
    - Runtime and declaration consumers use one fully enumerated archive identity; the Node entry passes with the actual optional peer and fails predictably without it.
    - Missing applicable license text remains an explicit failed release gate; unpublished local candidate evidence is not installed or published acceptance.
  artifacts:
    - path: packages/shared/tsconfig.build.json
      provides: Production-only emission with declarations, no maps and no incremental reuse
    - path: packages/shared/scripts/build.mjs
      provides: Fixed package-local output validation, clean emission and compiler exit propagation
    - path: packages/shared/package.json
      provides: Native build/pack lifecycle routing without changing public package identity or exports
    - path: packages/shared/README.md
      provides: Actual entry-point, peer, development and debugging contracts with release limitations
  key_links:
    - from: packages/shared/package.json
      to: packages/shared/scripts/build.mjs
      via: build and prepack invoke the same bounded builder; prepublishOnly cannot re-emit tests
    - from: packages/shared/scripts/build.mjs
      to: packages/shared/tsconfig.build.json
      via: Installed local TypeScript invoked with the exact production build configuration
    - from: packages/shared/tsconfig.build.json
      to: packages/shared/tsconfig.json
      via: Extends the unchanged development configuration and overrides emission-only settings
    - from: packages/shared/package.json
      to: packages/shared/README.md
      via: Native files allowlist and complete packed-member verification
---

<objective>
Separate shared-package release emission from test typechecking, remove unusable source maps and local-path leakage, and prove the resulting archive against actual runtime and declaration consumers. Keep the source/packaging repair bounded to four files. Do not close the unresolved license or immutable-release gates.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
The installed GSD runtime is under ~/.claude/get-shit-done; the catalog's ~/.Codex/get-shit-done path is absent. Root owns admission and independent review. Private setup preparation is admitted; source mutation and native build/pack await exact setup review. No install or publication is authorized.
</execution_context>

<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.agents/skills/minion-engineering/references/testing-and-review.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-CONTEXT.md
@.planning/phases/12-dependency-provenance/12-03-PLAN.md
@.planning/phases/14-sdk-transport/14-11-PLAN.md
@.planning/phases/14-sdk-transport/14-11-SUMMARY.md
@packages/shared/package.json
@packages/shared/tsconfig.json
@packages/tsconfig/library.json
@packages/tsconfig/base.json
@packages/shared/vitest.config.ts
@packages/shared/src/index.ts
@packages/shared/src/gateway/index.ts
@packages/shared/src/utils/index.ts
@packages/shared/src/node/index.ts
@packages/shared/src/brain-vector/index.ts
</context>

## Verified starting evidence

The actual phase directory is `12-dependency-provenance`; no existing `12-08-PLAN.md` was present when this draft was created. The requested `12-dependencies-build` directory is not the registered phase directory.

The inspected unpublished archive is `/tmp/minion-14-11-1ss7iase/archives/minion-stack-shared-0.9.0.tgz`, SHA-256 `f6a3c237c0264a2a8ef75906991ae8fa84504bf637d3b6655d07058582b603fa`. Its 125 ordinary members comprise:

| Non-overlapping category | Members | Unpacked bytes |
|---|---:|---:|
| Production JavaScript | 21 | 60,749 |
| Production declarations | 21 | 52,217 |
| Production JavaScript/declaration maps | 42 | 88,699 |
| Test-derived JavaScript/declarations/maps | 40 | 209,302 |
| Packed package.json | 1 | 1,505 |

There are 62 maps in total, including 20 test maps. None has embedded `sourcesContent`. No README, LICENSE, NOTICE or source file is shipped. For example, `package/dist/gateway/cache-events.js.map` and its declaration map reference `../../../../../../../home/nikolas/Documents/CODE/MINION/packages/shared/src/gateway/cache-events.ts`. These paths expose the original build location and do not resolve to shipped source. Do not follow those paths during archive inspection.

`packages/shared/package.json:47-56` includes `dist` and an absent README, uses `tsc` for build and prepublishOnly, and uses `tsc --noEmit` for typecheck. `tsconfig.json:7` includes all source files, including ten `*.test.ts` files. Inherited `packages/tsconfig/library.json` enables declarations, declaration maps, JavaScript maps and composite output. Excluding tests without cleaning would leave previous emitted tests/maps behind.

These counts identify the inspected snapshot, not a permanent source inventory. Reconcile any newly frozen input against this baseline before implementation; never discard concurrent additions to force the old count.

## Selected map and license policy

Omit both map classes in this bounded repair. Preserve JavaScript and declarations. Hub/Site consume browser entry points and the Paperclip Node adapter consumes the Node wrapper; the inspected Hub/Site Vite configuration and adapter source do not establish a qualified source-map upload or debugger contract. Existing maps contain neither shipped nor embedded source. README must state that this archive supports declaration navigation and compiled-JavaScript debugging, without claiming TypeScript-source stepping.

Future source debugging would require matching production sources plus portable maps, or embedded JavaScript sources and separately qualified declaration-source resolution. It would need actual consumer verification. Do not alter inherited shared compiler settings or add source shipping to this child.

The package declares `license: MIT`; no applicable license text was found at root or in this package. Tracked skill THIRD_PARTY_NOTICES files do not establish this package's attribution. Preserve metadata without treating it as verified legal provenance. LICENSE/NOTICE creation, attribution selection and license-field changes are outside these four files' authorized behavior. Add a precise README `TODO(handoff)` pointing to the root-owned packaging/license proposal selected at dispatch. Root must record the same unresolved item in that proposal. A successful local artifact remains unreleasable under DEP-03 until applicable text and attribution are independently established and shipped through separately admitted scope.

<boundaries>
- Own exactly the four source/document files listed in frontmatter after admission. Preserve all source modules, the original tsconfig, inherited configs, tests, dependency manifests outside this package, locks, version WIP, all five exports, main/types, optional ws range and metadata. Do not add LICENSE or NOTICE.
- No branch, stash, worktree, commit, publish, registry fetch, dependency installation, active consumer adoption, application startup, database, browser, provider or external network activity. Root retains whole-source/14-11/facade coordination and global planning/proposal ownership.
- This child repairs a finding exposed by 12-03/14-11; it does not require completion of unrelated Hub dependency-family changes. Empty depends_on does not release the shared package's ownership freeze or the ROADMAP integration gates.
- Before edits, root selects a complete current source/config/compiler snapshot and grants exclusive four-file ownership. Record before-images/hashes, current branch and existing dirty changes. Root must reconcile any source changes since the inspected archive, including facade work, before emission.
- Execute artifacts only in a new private directory created with prefix `/tmp/minion-12-08-`, ordinary copied source/config/metadata files and explicit private HOME/cache/output locations. Do not reuse or overwrite any previous 14-11 archive, active dist, active tsbuildinfo, installed consumer tree or cache.
- Use the already installed native pnpm/Node/TypeScript and actual ws peer, recording resolved paths, versions and hashes. Avoid bootstrap wrappers, npx/bunx or installs. Any metadata-only workspace link must point within the same private snapshot and be enumerated; compiler/tool resolution is distinct from output ownership. Reject cache symlinks back to active trees.
- Root reviews the exact private runner, native lifecycle argv and environment before execution. Baseline packing may disable hooks only when recording that limitation; candidate acceptance must exercise the new actual prepack/build path. No unrelated lifecycle scripts are admitted.
- Run one bounded process at a time, coordinate build/check resources with root, preserve failures and enforce the selected runner's timeout/output limits. Use an environment allowlist with no application env loading, credentials or inherited Node preload options. Record zero external-network attempts with the existing qualified observation mechanism.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Capture the failure and specify the private artifact contract</name>
  <files>packages/shared/package.json, packages/shared/tsconfig.build.json, packages/shared/scripts/build.mjs, packages/shared/README.md</files>
  <read_first>packages/shared/package.json, packages/shared/tsconfig.json, packages/tsconfig/library.json, packages/tsconfig/base.json, packages/shared/vitest.config.ts, .planning/phases/14-sdk-transport/14-11-SUMMARY.md</read_first>
  <behavior>The unchanged package fails release-content checks for tests/maps/README while public closures and the ten-test typecheck baseline are recorded independently. No test can pass by resolving active source aliases or an ancestor's installed peer.</behavior>
  <action>
Before product edits, prepare a root-reviewed private Node assertion runner and fixtures inside the fresh temporary directory. These are evidence artifacts, not additional repository source files. Preserve their exact content and hashes in the receipt so independent review can rerun them. Use actual native compiler and archive tooling; do not implement a fake emitter or hand-edit the archive manifest to manufacture a pass.

Record the existing archive's complete ordinary-member hashes and the failed tests/maps/README predicates. Freeze the current source inventory and native compiler/config closure separately. Enumerate TypeScript's actual noEmit inputs and prove inclusion of all ten current test paths. An intentional type error added only to a private test copy must make the original noEmit command fail, then pass when restored; no canonical test edit is permitted.

Prepare externally asserted cases for stale output, symlink safety, compilation failure, pack lifecycle, runtime/type export closure, ws presence/absence and copied-source path portability. Keep external sentinel files outside the fixture package and hash them before/after refusal cases. Do not broaden the source list if the existing native toolchain cannot be reused safely; report the exact prerequisite.
  </action>
  <verify><automated>Root-reviewed private Node runner: baseline member predicates fail for the expected reasons; actual noEmit input enumeration includes ten test paths; private test-only type error fails.</automated></verify>
  <done>The baseline failure, preserved behavior, tool identity and reproducible negative fixture definitions exist before source changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement production-only clean emission and truthful package documentation</name>
  <files>packages/shared/package.json, packages/shared/tsconfig.build.json, packages/shared/scripts/build.mjs, packages/shared/README.md</files>
  <read_first>packages/shared/tsconfig.json, packages/tsconfig/library.json, packages/shared/src/node/index.ts</read_first>
  <behavior>Only production code/declarations enter clean output. Tests remain checked through the original configuration. Unsafe cleanup targets and compiler failure terminate with a nonzero exit before an archive can be accepted.</behavior>
  <action>
Add tsconfig.build.json extending ./tsconfig.json. Override sourceMap and declarationMap to false, composite and incremental to false, and noEmitOnError to true; retain declaration emission and existing strict/module/target behavior. Keep rootDir/outDir package-local and exclude current test naming (*.test.ts), plus spec/test fixture directories only where supported by the frozen inventory. Do not exclude production modules by a broad substring filter. TypeScript excludes alone do not prevent an imported test from entering the graph: the artifact assertion must reject any such emitted file rather than silently accepting it.

Add scripts/build.mjs using Node built-ins and the actual installed local TypeScript CLI. Derive a fixed package root from the script; accept no caller-selected cleanup/output path. Validate lexical invocation/module location, expected package identity and every existing ancestor of the fixed package-local dist path with lstat before cleanup, including the package root. Reject symlink/junction output or ancestor paths, unexpected file types and any lexical/realpath disagreement; do not erase through a link and then report success. Recheck immediately before mutation. A missing dist is allowed only under validated ordinary ancestors. Deleting a nested stale symlink must never follow or modify its target. State the trusted, exclusive local-workspace assumption: these checks are not race-proof containment against a malicious concurrent filesystem writer.

Remove only the validated dist tree, recreate it, and invoke the installed compiler with this exact configuration and package cwd. Resolve and record the real compiler instead of searching for a downloadable executable. Propagate spawn errors, nonzero exit and termination without reporting a successful build. Disable incremental reuse so cleanup cannot be followed by a skipped emission based on old tsbuildinfo. A failed compile must leave no previous valid output; noEmitOnError prevents new partial compiler output for ordinary diagnostics. Successful completion must have the declared public runtime/type entry files.

Route build to `node scripts/build.mjs` and prepack to the same build through native pnpm. Replace the old emitting prepublishOnly with the existing noEmit typecheck command through pnpm so it cannot restore test/map outputs. Verify actual pnpm lifecycle ordering rather than assume it; stop on either compiler/typecheck failure. Preserve test, typecheck and lint scripts, version, exports, peers and all unrelated package fields. Preserve native packed-manifest normalization as separately recorded behavior, not source edits.

Create README covering all five entry points (`.`, `./gateway`, `./utils`, `./node`, `./brain-vector`), existing ESM/declaration behavior, browser-safe root versus Node ws wrapper, optional-peer responsibility, build versus test/typecheck commands, omitted-map debugging limits, and the unresolved license/release TODO. Do not claim deployment, authentication correctness or optional Node compatibility until the fixture proves the relevant behavior.
  </action>
  <verify><automated>In the private snapshot: native pnpm run build; unchanged pnpm run typecheck; private cleanup/compiler-failure assertions; scoped formatting/lint for the four owned files.</automated></verify>
  <done>Production output is clean, all ten tests remain typechecked, unsafe paths refuse without sentinel changes, and compiler failure cannot produce a successful artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify complete archive identities and actual consumer entry points</name>
  <files>packages/shared/package.json, packages/shared/tsconfig.build.json, packages/shared/scripts/build.mjs, packages/shared/README.md</files>
  <read_first>packages/shared/package.json, packages/shared/src/index.ts, packages/shared/src/gateway/index.ts, packages/shared/src/utils/index.ts, packages/shared/src/node/index.ts, packages/shared/src/brain-vector/index.ts</read_first>
  <behavior>Native packing independently removes contamination, every public runtime/declaration closure resolves from that archive, and optional-peer controls use the actual installed ws without a live socket. License/release failure remains visible after all technical checks pass.</behavior>
  <action>
Run the complete matrix below against the copied current sources and candidate four files. Build from ordinary snapshot source paths, never active-source symlinks or temporary outDir overrides applied to the active project. Pack into the private artifact directory with the pinned native pnpm and the actual candidate prepack enabled. Preserve native stdout/stderr/argv/exit and packed manifest separately from source metadata. Verify its workspace dependency normalization explicitly; reject unexplained changes.

Enumerate every archive member with type, path, size and SHA-256. Reject duplicates, absolute/traversal names and linked/nonordinary members before extraction. Derive expected production files from the independently frozen source/export inventory, not from whatever dist happens to contain. Preserve all five exports exactly and walk all reachable relative runtime/declaration edges. Assert zero emitted tests/spec fixtures, .map files, sourceMappingURL references, tsbuildinfo, source copies or local workspace/home/temp path leaks. Assert README's exact bytes are present. At the inspected 21-production-module baseline this means 42 emitted files plus package.json and README (44 ordinary files); any different source inventory requires a reconciled expected count, not an automatic failure or silent exclusion.

Resolve all runtime entries from the extracted archive in fresh credential-free consumer roots, with no source aliases or fallback to active package copies. Browser-compatible entries must import without ws. For ./node, make one consumer's complete resolution ancestry demonstrably ws-free and assert the native missing-peer error; give another consumer only the recorded actual ws and its necessary metadata/type dependencies, then import and exercise createNodeGatewayClient without connect/server creation. Use declaration fixtures for all five public entries with the actual compiler and Node peer types as required. Hash every resolved package file and peer path. No installed consumer package/lock mutation or network package installation is allowed.

Repeat builds/packs in two separately named private snapshot roots from identical inputs. Compare extracted path/type/content manifests and production runtime/declaration digests; record both tarball digests. Require stable member content without local path leakage. Do not infer byte-identical tarballs merely from equivalent member content; retain any container-metadata differences explicitly.

Run the existing package's focused/full unit suite as appropriate to the frozen source in the same private lane with private caches, plus unchanged noEmit typechecking. Separate new packaging evidence from previously accepted client behavior. Freeze the four file hashes and preserve active source/dist/cache before/after manifests. Obtain independent Standards and Spec review before root accepts this child. Summarize missing license text as a failed release gate even when packaging behavior passes.
  </action>
  <verify><automated>Root-reviewed private runner executes the matrix, native pnpm pack and all five runtime/declaration probes; outputs complete member hashes, positive/negative peer results, noEmit/unit results and preservation manifests.</automated></verify>
  <done>One complete archive identity passes technical packaging and entry-point checks, repeat-build content is compared, and independent review retains explicit license/version/installed-consumer release gaps.</done>
</task>
</tasks>

## Mandatory private acceptance matrix

| Case | Required observation |
|---|---|
| Existing archive | Correct baseline failure for tests/maps/missing README; license failure recorded separately |
| Old dist contamination | Add a removed production output, test output, map and stale build marker in the private copy; subsequent direct build and native pack contain none |
| Missing dist with stale tsbuildinfo | Full production output is emitted; old incremental state cannot skip it |
| Linked dist | Refuse before cleanup/compiler; link and external target sentinel remain unchanged |
| Linked package/output ancestor | Refuse before cleanup/compiler, including invocation through a linked package ancestor; preserve external sentinel |
| Nested stale link | Cleanup never follows its target; external sentinel remains unchanged |
| Non-directory dist / malformed package identity | Refuse safely, without deleting the unexpected target |
| Production syntax/type error | Actual compiler exits nonzero; native pack exits nonzero and creates no new accepted archive, even after a previously good build |
| Compiler launch failure | Builder exits nonzero, with no false success or accepted archive; use a private missing-tool fixture, not process fault injection |
| Test-only type error | Original noEmit typecheck fails while the production build remains independent; restoration passes and all ten paths remain included |
| Public graph preservation | All five export keys and runtime/type entry values unchanged; transitive closures resolve from the same extracted archive |
| Optional ws absent/present | Root/browser entries work absent ws; Node entry fails natively absent ws and works with actual pinned ws, without opening a socket |
| Repeated copied-source build | Member content stable across two private roots; no maps/sourceMappingURL/local-path leakage; tarball identity reported separately |
| Missing license | Still reported as unresolved and blocking release; no invented attribution or legal file |

<threat_model>
| Boundary | Threat | Required control |
|---|---|---|
| Output cleanup / local filesystem | Linked output deletes unrelated files | Fixed package-local path, ancestor/link checks, unchanged external sentinels; exclusive trusted workspace assumption |
| Failed compiler / stale dist / archive | Old or partial files appear current | Clean production-only nonincremental emission, noEmitOnError, propagated failure, native pack failure regression |
| Source paths / consumers | Local paths leak; broken maps imply nonexistent source support | Omit both map classes, inspect every member and generated reference, document debugging limits |
| Archive / runtime/types / optional peer | Aliases or ancestor dependencies manufacture success | Complete member identity and closed resolution graph; actual positive and truly absent peer controls |
| Metadata / license / publication | MIT string or local version mistaken for accepted release | Explicit unresolved provenance and immutable-version gates; no license invention or publication |
</threat_model>

<verification>
Use the MINION engineering two-axis review: Standards checks ownership, environment, cleanup, type safety, tooling and disclosure; Spec checks all mandatory matrix observations and preserved public contracts. No broad application builds or network tests. Root must approve an exact private runner before artifact execution, and a reviewer must rerun it against frozen inputs. GSD structural validity alone is not implementation admission.

Record complete commands, tool/runtime/config hashes, before/after files, source and packed manifests, archive/member identities, test results and limitations. Do not require edits to existing provenance tooling, CI, inherited configs, source tests or consumer locks to call this child complete; any necessary additional source seam requires a separately reviewed amendment. Private regression fixtures must be preserved and handed to the 12-03 evidence owner; canonical CI integration remains outside this four-file scope and must not be claimed.
</verification>

<success_criteria>
All technical must-haves have actual native artifact and independent-review evidence at the selected frozen candidate. License provenance remains explicitly unresolved and DEP-03/phase 12 stay open. Version reuse across differing candidate bytes remains acceptable only as unpublished local evidence; root must select a distinct immutable release identity and qualify each installed consumer before release. No package is published or deployed by this child.
</success_criteria>

<output>
After implementation, root owns assembly/admission of 12-08-SUMMARY.md and the matching open-item proposal. Executor returns the exact four source hashes, private fixture/runner location and hash, full artifact/member manifests, commands/results, Standards/Spec findings and unresolved license/version/consumer/debugging limits. Independent reviewer owns a separate verification report when dispatched. Do not update global ROADMAP/STATE/REQUIREMENTS, allowlists or unrelated proposals from this task.
</output>
