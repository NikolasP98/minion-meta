---
status: admitted
reviewer: root
reviewed: 2026-09-09
plan_sha256: 6cdc42511bb3a7c23e82581e2c61f74b6fa314ee78fcd462aeb19b80a04f276e
requirements_completed: []
---

# Independent build diagnostic admission

The exact four-file diagnostic scope is admitted. It consumes frozen failure artifacts and does not require pretending12-01 is complete. Original and candidate default builds both fail packaging; the actual cause remains unproven. The plan distinguishes single-module, manifest and full-entry traces and preserves complete-deployment acceptance for the later repair.

The manifest and filesystem checks must reject stale/missing identity, active checkout roots, private environment bindings and symlink escape before reading content. A suppressed path is a blocked trace, never a complete dependency result. Actual nft fixtures must prove ordinary and dynamic asset inclusion. Record which artifacts and module entrypoints were used; do not infer filesystem-wide scanning from the adapter's base alone.

Root adds initial resource bounds of60 seconds and2GiB Node heap per sequential real trace. Timeout/OOM is diagnostic evidence with that bound, not proof no supported budget can finish. Process cleanup must complete before another heavy task starts. No node_modules patch, source/locale change, route splitting, heap policy change or dependency candidate application is admitted. A proposed repair requires its own reviewed scope and EN/ES plus deployment-closure tests.

Standards and required behavior are adequately separated. The diagnostic harness adds temporary observability only; it must not become an alternative production tracer or permanent bypass. Root retains manifest/lock and CI ownership.
