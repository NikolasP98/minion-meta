---
phase: 12-dependency-provenance
plan: "04"
type: execute
wave: 2
depends_on: ["12-01", "10-03"]
status: admitted_execution_gated
autonomous: true
requirements: [DEP-01, JOB-02, CAP-02]
files_modified:
  - minion_hub/package.json
  - minion_hub/bun.lock
  - minion_hub/patches/postgres@3.4.9.patch
  - minion_hub/tests/dependencies/postgres-close-recovery.sql.integration.test.ts
  - minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts
  - .planning/phases/12-dependency-provenance/12-POSTGRES-DECISION.md
must_haves:
  truths:
    - Backend termination and rollback settle callers without an uncaught nextWrite exception, leaked wait or unsafe replay.
    - The selected driver artifact has reproducible patch/archive identity and preserves actual stock/job transaction outcomes.
    - A crash-avoidance guard is not accepted if it merely turns failure into an indefinitely pending query.
  artifacts:
    - path: minion_hub/tests/dependencies/postgres-close-recovery.sql.integration.test.ts
      provides: Isolated actual PostgreSQL process/connection-close regression
    - path: .planning/phases/12-dependency-provenance/12-POSTGRES-DECISION.md
      provides: Primary upstream evidence, exact artifact identity, removal trigger and residual limits
  key_links:
    - from: minion_hub/tests/dependencies/postgres-close-recovery.sql.integration.test.ts
      to: minion_hub/scripts/qc/disposable-postgres.ts
      via: Explicit disposable identity and marker, no normal app environment
---

<objective>Resolve the postgres3.4.9 asynchronous null-socket crash discovered by10-03 without silently maintaining a driver fork or accepting hung queries.</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-01-SUMMARY.md
@.planning/phases/10-durable-jobs-stock/10-03-SUMMARY.md
@.planning/research/360-native-postgres-fixture.md
@minion_hub/scripts/qc/disposable-postgres.ts
</context>

## Admission and ownership

Independent review and exact manifest/lock/test ownership are mandatory before execution.12-01 and10-03 must freeze first. Preserve dirty shared WIP and their checks. No production backend termination, app.env reads, branch/worktree/stash/commit/deploy or global exception suppression. No broad driver substitution is authorized by this patch plan.

Primary evidence: https://github.com/porsager/postgres/issues/1066 and https://github.com/porsager/postgres/pull/1168 . Registry latest was3.4.9 on2026-09-09; refresh before selection. A proposed upstream patch is evidence to test, not a reviewed release. Do not install a floating Git branch.

<tasks>
<task type="auto">
<name>Task 1: Reproduce and select the bounded repair</name>
<files>minion_hub/tests/dependencies/postgres-close-recovery.sql.integration.test.ts, .planning/phases/12-dependency-provenance/12-POSTGRES-DECISION.md</files>
<read_first>minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts, minion_hub/node_modules/postgres/src/connection.js, .planning/research/360-native-postgres-fixture.md</read_first>
<action>Extract a minimal child-process reproduction against explicit marked disposable PostgreSQL, including backend close during a transaction and attempted rollback/pool reuse. Record baseline child exit/stack, exact driver/Node/PG versions and source hash. Require bounded query rejection/settlement, subsequent healthy connection reuse and closed handles/timers. Refresh upstream release status and inspect exact proposed changes. Prefer an official fixed compatible release when actual fixtures pass; otherwise evaluate a tracked Bun patch in an isolated copy, recording upstream commit, patch SHA, rationale and removal trigger. If the guard leaves queries pending or changes transaction ownership, reject it and produce an exact separately reviewed driver-adapter plan rather than broadening this slice.</action>
<verify><automated>Explicit disposable runtime: node node_modules/vitest/vitest.mjs run tests/dependencies/postgres-close-recovery.sql.integration.test.ts</automated></verify>
<done>Red evidence exists and the exact candidate proves handled failure, bounded settlement and healthy recovery without process-global exception suppression.</done>
</task>
<task type="auto">
<name>Task 2: Apply reproducibly and rerun domain recovery</name>
<files>minion_hub/package.json, minion_hub/bun.lock, minion_hub/patches/postgres@3.4.9.patch, minion_hub/src/server/services/job-stock-concurrency.sql.integration.test.ts, .planning/phases/12-dependency-provenance/12-POSTGRES-DECISION.md</files>
<read_first>.planning/phases/12-dependency-provenance/12-POSTGRES-DECISION.md, minion_hub/package.json, minion_hub/bun.lock</read_first>
<action>For the admitted candidate only, preserve unrelated lock records and existing internal archive integrity. Use Bun's tracked patch mechanism, first in a disposable env-free copy; never leave a silent node_modules edit. If an official release supersedes the patch, amend exact files/version before applying. Change the10-03 driver-defect expectation to require a handled rejection/no uncaught crash while retaining the committed-draft/rollback/retry assertions; do not delete the crash scenario. Run frozen clean install, bounded driver fixture, full targeted stock/job native recovery and typecheck/build. Inspect the packaged artifact to prove the patch is present. Record patch removal when an upstream release passes the same suite. Independent verification and release identity remain required.</action>
<verify><automated>Explicit disposable runtime: node node_modules/vitest/vitest.mjs run tests/dependencies/postgres-close-recovery.sql.integration.test.ts src/server/services/job-stock-concurrency.sql.integration.test.ts; then bun run check and bun run build</automated></verify>
<done>Clean installed/package artifacts reproduce the fix; domain recovery remains correct; residual connection issues and release limits are explicit.</done>
</task>
</tasks>

<verification>Use real separate PostgreSQL connections and isolated child processes for termination. Missing runtime or skipped cases are failures of qualification. Do not count19assertions with an unhandled exception as a passing suite. Keep source TODO plus proposal for unresolved driver/runtime defects. No production fault injection or data changes.</verification>

<output>Write12-04-SUMMARY with exact source/archive/patch hashes and commands. Independent reviewer writes12-04-VERIFICATION. Root owns allowlist, status and proposals. Admission grants no execution before the ownership gates or requirement closure.</output>

Test-lane amendment: the native driver fixture uses the SQL integration naming contract. After10-08, root must explicitly admit it into vitest.disposable.config.ts before running it through that lane; it must never be discovered as an ordinary dependency unit test. This names the existing planned fixture without adding a source file or relaxing its marker checks.
