---
phase: 12-dependency-provenance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "minion_hub/vitest.config.ts"
  - "minion_hub/tests/dependencies/security-compatibility.test.ts"
  - ".planning/phases/12-dependency-provenance/12-ADVISORIES.md"
  - "minion_hub/package.json"
  - "minion_hub/bun.lock"
autonomous: true
requirements: ["DEP-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Platform inventory identifies every supported dependency surface, exact affected versions/paths and minimum safe families; unknown surfaces remain explicit."
    - "Reachable affected paths patched with passing compatibility/build evidence; residual advisory and candidate hashes recorded."
  artifacts:
    - path: "minion_hub/tests/dependencies/security-compatibility.test.ts"
      provides: "Reconcile current lock and create compatibility fixtures"
    - path: "minion_hub/package.json"
      provides: "Update compatible security families and rerun the product checks"
  key_links:
    - from: "minion_hub/tests/dependencies/security-compatibility.test.ts"
      to: ".planning/research/360-ui-dependency-verification.md"
      via: "Read-first contract and behavior verification for reconcile current lock and create compatibility fixtures"
    - from: "minion_hub/package.json"
      to: ".planning/phases/12-dependency-provenance/12-ADVISORIES.md"
      via: "Read-first contract and behavior verification for update compatible security families and rerun the product checks"
---

<objective>
Inventory platform dependency exposure and patch the bounded Hub runtime families.
Purpose: DEP-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-CONTEXT.md
@AGENTS.md
@.planning/research/360-ui-dependency-verification.md
@minion_hub/package.json
@minion_hub/bun.lock
@.planning/phases/12-dependency-provenance/12-ADVISORIES.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile current lock and create compatibility fixtures</name>
  <files>minion_hub/vitest.config.ts, minion_hub/tests/dependencies/security-compatibility.test.ts, .planning/phases/12-dependency-provenance/12-ADVISORIES.md</files>
  <read_first>.planning/research/360-ui-dependency-verification.md, minion_hub/package.json, minion_hub/bun.lock</read_first>
  <behavior>Platform inventory identifies every supported dependency surface, exact affected versions/paths and minimum safe families; unknown surfaces remain explicit. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Inventory every supported repository and container runtime dependency surface: meta packages, gateway/extensions, Hub, Site, Paperclip, Pixel Agents, Drone, Shells, LangGraph and Factory, plus packaged/plugin runtimes where executable dependencies exist. Record exact candidate/lock/image identity, current primary advisory evidence, importer/runtime reachability and reviewed/not-reviewed status per surface. The source mutation in this plan remains limited to Hub. Record local/remote candidate and lock hashes, current advisories and actual importer chains. Independently admitted config amendment: add only tests/dependencies/**/*.test.ts to the existing Vitest include array so this durable fixture actually runs. Create hostile Accept-header, editor paste/Markdown roundtrip, DOM sanitization and XML signature fixtures using existing runtime libraries. Classify runtime/build/test reachability; never equate package count with exploit count. Obtain manifest ownership and select a coherent lock baseline preserving staged WIP before installing.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run tests/dependencies/security-compatibility.test.ts</automated></verify>
  <done>Platform inventory identifies every supported dependency surface, exact affected versions/paths and minimum safe families; unknown surfaces remain explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Update compatible security families and rerun the product checks</name>
  <files>minion_hub/package.json, minion_hub/bun.lock, minion_hub/tests/dependencies/security-compatibility.test.ts</files>
  <read_first>.planning/phases/12-dependency-provenance/12-ADVISORIES.md</read_first>
  <behavior>Reachable affected paths patched with passing compatibility/build evidence; residual advisory and candidate hashes recorded. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Update SvelteKit, all active Tiptap packages together, xmldom through compatible parent/resolution and Vitest patch to verified unaffected releases; update DOMPurify where advisory exposure requires. Use Bun in this repo and preserve lock coherence. Do not perform major-version update-all. Run frozen clean install in an isolated disposable copy without private .env, component/browser/XML fixtures, build/check and updated audit. Obtain exclusive browser-harness ownership and use a unique BU_NAME for the mandatory MINION_DEPENDENCY_BROWSER=1 verification gate; default unit execution skips native browser ownership and cannot close that gate. Each remaining advisory gets a reachability/disposition entry; failures stop family admission. Create an exact bounded child PLAN per affected non-Hub family, with exclusive manifest/image ownership, native package manager, compatibility fixtures and candidate identity. Execute and independently verify those plans before DEP-01 closure. Hub-only scan/patch success cannot qualify the platform; unknown or unreviewed supported surfaces keep DEP-01 pending.</action>
  <verify><automated>cd minion_hub &amp;&amp; MINION_DEPENDENCY_BROWSER=1 node node_modules/vitest/vitest.mjs run tests/dependencies/security-compatibility.test.ts &amp;&amp; bun run check &amp;&amp; bun run build</automated></verify>
  <done>Reachable affected paths patched with passing compatibility/build evidence; residual advisory and candidate hashes recorded.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Registry/archive / build / installed consumer | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-12-01-01 | T/E | Substituted or vulnerable dependency | mitigate | Exact digests, lock coherence, peer checks, safe compatibility fixtures. |
| T-12-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/12-dependency-provenance/12-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

