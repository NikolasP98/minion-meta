# Partial CPU profiling admission

Root reviewed the three-task plan against the four contained timeouts and actual installed nft analysis boundaries. GSD structure passes with no errors/warnings. Admitted PLAN SHA-256: `efa76681274dca44a86593f5bc1596b5690498bd7cb039299518418b72de0ccf`.

Tasks1/2 may change only the existing diagnostic launcher, worker and test files plus results/summary. Fixed V8 statistical profiling streams capped raw bytes separately from nonce/proof control. Timeout retention must be demonstrated with a synchronous synthetic function and useful actual built-in decoding before any application profile. Decode exposes one exact pinned bounded regular data file after the same isolation proof; it adds no host tree, executable, native symbol tools, network or arbitrary profiler flags. Existing ordinary trace and asset behavior must remain identical. Unknown symbols and partial records are limits, not inferred call stacks.

Task3 is separately gated on root independent review and a resource window. Its maximum scope is one messages-only60-second/2048MiB profile and one equally bounded useful decode. No higher budget, actual build, source repair, dependency update, baseline/whole-entry rerun or new general profiler is admitted. Failure of the selected profiling mechanism returns for review without a weaker fallback. RSS attribution remains unresolved; this is CPU-only evidence.

Synthetic timing clarified after observed startup-only failures:5seconds total within existing1–5s envelope, unchanged realbounds. Amended PLAN SHA-256 `5e3402b065c3e0374ae0979031a35b3dd0102f060b7cdd43ce0f271cbee67a3b`.
