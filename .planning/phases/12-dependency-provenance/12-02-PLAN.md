---
phase: 12-dependency-provenance
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - "minion_hub/package.json"
  - "minion_hub/bun.lock"
  - "minion_site/package.json"
  - "minion_site/bun.lock"
  - "minion_hub/vite.config.ts"
  - "minion_hub/tests/dependencies/locale-parity.test.ts"
autonomous: true
requirements: ["DEP-02"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Every removed dependency has graph proof and clean consumer build; all required peers remain present."
    - "Deprecated adapter removed and both locale builds/SSR fixtures pass without changed message identities."
  artifacts:
    - path: "minion_hub/package.json"
      provides: "Run peer-aware removal trials in Hub and Site"
    - path: "minion_hub/vite.config.ts"
      provides: "Migrate deprecated Paraglide adapter with locale parity"
  key_links:
    - from: "minion_hub/package.json"
      to: ".planning/research/360-ui-dependency-verification.md"
      via: "Read-first contract and behavior verification for run peer-aware removal trials in hub and site"
    - from: "minion_hub/vite.config.ts"
      to: "minion_hub/project.inlang/settings.json"
      via: "Read-first contract and behavior verification for migrate deprecated paraglide adapter with locale parity"
---

<objective>
Remove proven dead declarations and replace deprecated locale adapter.
Purpose: DEP-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/12-dependency-provenance/12-CONTEXT.md
@AGENTS.md
@.planning/research/360-ui-dependency-verification.md
@packages/crm-sdk/package.json
@minion_hub/project.inlang/settings.json
@minion_hub/src/hooks.server.ts

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Run peer-aware removal trials in Hub and Site</name>
  <files>minion_hub/package.json, minion_hub/bun.lock, minion_site/package.json, minion_site/bun.lock</files>
  <read_first>.planning/research/360-ui-dependency-verification.md, packages/crm-sdk/package.json</read_first>
  <behavior>Every removed dependency has graph proof and clean consumer build; all required peers remain present. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Confirm import/config/peer edges for d3-scale, d3-shape, SQL CodeMirror mode and @types/dompurify; remove only proven dead declarations in one owned lock transaction. Site argon2/shaders need equivalent clean-build proof. Keep Site postgres and Tiptap node-range peers, d3-cloud and actually used Workshop features. Optional telemetry package with no measured use is a decision record, not automatic product removal. Run each repo's frozen clean build/check in disposable copies.</action>
  <verify><automated>(cd minion_hub &amp;&amp; bun run check &amp;&amp; bun run build) &amp;&amp; (cd minion_site &amp;&amp; bun run check &amp;&amp; bun run build)</automated></verify>
  <done>Every removed dependency has graph proof and clean consumer build; all required peers remain present.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate deprecated Paraglide adapter with locale parity</name>
  <files>minion_hub/vite.config.ts, minion_hub/package.json, minion_hub/bun.lock, minion_hub/tests/dependencies/locale-parity.test.ts</files>
  <read_first>minion_hub/project.inlang/settings.json, minion_hub/src/hooks.server.ts</read_first>
  <behavior>Deprecated adapter removed and both locale builds/SSR fixtures pass without changed message identities. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Verify current official Paraglide plugin API before selecting direct paraglide-js integration. Preserve EN/ES message identifiers, generated output location, SSR routing and request locale semantics; no translation rewrite. Add roundtrip fixture for both locales and parallel SSR requests preventing locale bleed. If hooks need changing, acquire ownership and split the exact integration task before editing. A table-library major migration is separate evidence, not bundled here.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run tests/dependencies/locale-parity.test.ts &amp;&amp; bun run check &amp;&amp; bun run build</automated></verify>
  <done>Deprecated adapter removed and both locale builds/SSR fixtures pass without changed message identities.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Registry/archive / build / installed consumer | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-12-02-01 | T/E | Substituted or vulnerable dependency | mitigate | Exact digests, lock coherence, peer checks, safe compatibility fixtures. |
| T-12-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/12-dependency-provenance/12-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>
