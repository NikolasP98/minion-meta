---
phase: 20-integration-reaudit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/qc/release-evidence.mjs"
  - "scripts/qc/release-evidence.test.mjs"
  - ".planning/phases/20-integration-reaudit/20-CANDIDATES.md"
  - "scripts/qc/integration-matrix.mjs"
  - "scripts/qc/integration-matrix.test.mjs"
  - ".planning/phases/20-integration-reaudit/20-INTEGRATION-VERIFICATION.md"
autonomous: true
requirements: ["QC-01"]
readiness: "Manifest validation executable early; release qualification requires all mandatory source/runtime/policy gates."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Manifest fails missing requirement proof, mismatched identity, unresolved mandatory gate or unrelated patch content."
    - "Every required integration test actually passes on candidate identity; unrun/skipped tests cannot produce complete status."
  artifacts:
    - path: "scripts/qc/release-evidence.mjs"
      provides: "Build per-repo release patch and evidence manifest"
    - path: "scripts/qc/integration-matrix.mjs"
      provides: "Run independent integration checks against candidate bytes"
  key_links:
    - from: "scripts/qc/release-evidence.mjs"
      to: ".planning/REQUIREMENTS.md"
      via: "Read-first contract and behavior verification for build per-repo release patch and evidence manifest"
    - from: "scripts/qc/integration-matrix.mjs"
      to: ".planning/phases/14-sdk-transport/14-COMPATIBILITY.md"
      via: "Read-first contract and behavior verification for run independent integration checks against candidate bytes"
---

<objective>
Verify full requirement coverage on exact candidates.
Purpose: QC-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/20-integration-reaudit/20-CONTEXT.md
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/operations/360/baseline.json
@.planning/operations/360/GSD-REMAINING-PLAN-INDEX.md
@.planning/phases/14-sdk-transport/14-COMPATIBILITY.md
@.planning/phases/20-integration-reaudit/20-CANDIDATES.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Cross-phase admission: All phases09-19 required behavior evidence and gap-plan closure.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Build per-repo release patch and evidence manifest</name>
  <files>scripts/qc/release-evidence.mjs, scripts/qc/release-evidence.test.mjs, .planning/phases/20-integration-reaudit/20-CANDIDATES.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/operations/360/baseline.json, .planning/operations/360/GSD-REMAINING-PLAN-INDEX.md</read_first>
  <behavior>Manifest fails missing requirement proof, mismatched identity, unresolved mandatory gate or unrelated patch content. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Enumerate the current 51 requirements from the canonical requirements file and all exact allowlisted child plans; never rely on a stale hardcoded requirement count. Record per repo base/source/patch/lock/archive/image identity, scoped file ownership and independent behavior evidence. Exclude unrelated WIP without stash/reset/branch/worktree changes. Generate reviewable patch packets from owned edits and preserve before-images. A completed plan summary cannot close unrelated requirements in its phase; discovery-only and missing runtime gates remain pending.</action>
  <verify><automated>node --test scripts/qc/release-evidence.test.mjs</automated></verify>
  <done>Manifest fails missing requirement proof, mismatched identity, unresolved mandatory gate or unrelated patch content.</done>
</task>

<task type="auto">
  <name>Task 2: Run independent integration checks against candidate bytes</name>
  <files>scripts/qc/integration-matrix.mjs, scripts/qc/integration-matrix.test.mjs, .planning/phases/20-integration-reaudit/20-INTEGRATION-VERIFICATION.md</files>
  <read_first>.planning/phases/14-sdk-transport/14-COMPATIBILITY.md, .planning/phases/20-integration-reaudit/20-CANDIDATES.md</read_first>
  <behavior>Every required integration test actually passes on candidate identity; unrun/skipped tests cannot produce complete status. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Use disposable candidate copies and native package managers to run required build/type/lint/tests, security negative fixtures, DB race tests, browser journeys, package/transport compatibility, and qualified runtime drill evidence. Distinguish Standards review from Spec/requirement review. Validate GSD plans and evidence-link coverage, not only document schema. Any regression gets exact failing case and bounded repair plan before release admission. No production calls.</action>
  <verify><automated>node --test scripts/qc/integration-matrix.test.mjs &amp;&amp; node scripts/qc/integration-matrix.mjs --validate-manifest .planning/phases/20-integration-reaudit/20-CANDIDATES.md</automated></verify>
  <done>Every required integration test actually passes on candidate identity; unrun/skipped tests cannot produce complete status.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Candidate artifact / trusted release / actual runtime | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-20-01-01 | T/E | Unrelated WIP or unverified candidate reaches production | mitigate | Scoped patch identity, independent checks and exact authorized deployment proof. |
| T-20-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/20-integration-reaudit/20-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>


## September 11 bounded early admission

Task 1's local manifest tooling may execute before global release prerequisites close, as the readiness field already permits. Root grants its three files only after reviewing the executor's proposed contract. It must generate reviewable evidence/patch packets and reject drift, unrelated scope and missing proof. A local tooling test pass does not grant candidate or release qualification. Task 2 and all runtime/release decisions remain gated. No manifest may provide arbitrary shell commands for automatic execution.

Root admitted exactly the three Task 1 files on September 11 after reviewing the contract: authoritative exact allowlist; canonical requirements and PLAN hashes; separate SHA-pinned ownership/gate policy; caller-supplied roots; before/after source and distinct artifact identities; bounded regular-file reads without symlinks; exclusive unapplied patch packet output; invalid/incomplete/evidence-qualified verdicts with explicit scope and releaseAuthorized:false. No manifest-supplied shell execution. Native tests must reject path, scope, identity and missing-proof defects, and cover text/binary additions/deletions and paths with spaces. All other files remain root-owned.
