---
phase: 20-integration-reaudit
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - ".planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md"
  - "scripts/qc/release-authority.test.mjs"
  - "scripts/qc/release-smoke.mjs"
  - "scripts/qc/release-smoke.test.mjs"
  - ".planning/phases/20-integration-reaudit/20-DEPLOYMENT-VERIFICATION.md"
autonomous: true
requirements: ["QC-02"]
readiness: "Packet/local verifier ready; all publish/migration/restart/promotion actions require concrete existing live authority."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Packet contains immutable targets, passed checks, bounded blast radius and explicit authorized/pending actions."
    - "Authorized runtime matches exact candidate with actual smoke receipts; absence of release authority is pending rather than auto-waived."
  artifacts:
    - path: ".planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md"
      provides: "Produce concrete per-environment release and rollback packet"
    - path: "scripts/qc/release-smoke.mjs"
      provides: "Execute only authorized operations and verify deployment identity"
  key_links:
    - from: ".planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md"
      to: ".planning/phases/20-integration-reaudit/20-CANDIDATES.md"
      via: "Read-first contract and behavior verification for produce concrete per-environment release and rollback packet"
    - from: "scripts/qc/release-smoke.mjs"
      to: ".planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md"
      via: "Read-first contract and behavior verification for execute only authorized operations and verify deployment identity"
---

<objective>
Prepare authorized release and exact-identity smoke.
Purpose: QC-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/20-integration-reaudit/20-CONTEXT.md
@AGENTS.md
@.planning/phases/20-integration-reaudit/20-CANDIDATES.md
@minion_factory/scripts/promotion/deploy-exact.sh
@.planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Produce concrete per-environment release and rollback packet</name>
  <files>.planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md, scripts/qc/release-authority.test.mjs</files>
  <read_first>.planning/phases/20-integration-reaudit/20-CANDIDATES.md, minion_factory/scripts/promotion/deploy-exact.sh</read_first>
  <behavior>Packet contains immutable targets, passed checks, bounded blast radius and explicit authorized/pending actions. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Name exact repo commits, package/image digests, environments, affected services/tenants, DB additive migrations and preflight collision results, expected disruption, checks, rollback and live authority required. Respect existing trusted release ownership; do not create blanket merge/publish permission. Include Factory interruption risk and containment activation separately; no approved source change implies authorization for either. Prepare everything reviewable before asking for the missing live-action permission.</action>
  <verify><automated>node --test scripts/qc/release-authority.test.mjs</automated></verify>
  <done>Packet contains immutable targets, passed checks, bounded blast radius and explicit authorized/pending actions.</done>
</task>

<task type="auto">
  <name>Task 2: Execute only authorized operations and verify deployment identity</name>
  <files>scripts/qc/release-smoke.mjs, scripts/qc/release-smoke.test.mjs, .planning/phases/20-integration-reaudit/20-DEPLOYMENT-VERIFICATION.md</files>
  <read_first>.planning/phases/20-integration-reaudit/20-RELEASE-PACKET.md</read_first>
  <behavior>Authorized runtime matches exact candidate with actual smoke receipts; absence of release authority is pending rather than auto-waived. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Validate recorded user authorization immediately before each publish/migration/restart/promotion action; if absent leave ready packet and continue independent work. Use native trusted workflow for exact candidate, then read-only verify deployed source/image/archive, health, signed actor denial, safe UI journey and telemetry attribution. No destructive smoke against production. Failed identity or health triggers only the authorized rollback path and keeps QC-02 open; no alias-only deployment proof.</action>
  <verify><automated>node --test scripts/qc/release-smoke.test.mjs &amp;&amp; node scripts/qc/release-smoke.mjs --validate-only</automated></verify>
  <done>Authorized runtime matches exact candidate with actual smoke receipts; absence of release authority is pending rather than auto-waived.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Candidate artifact / trusted release / actual runtime | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-20-02-01 | T/E | Unrelated WIP or unverified candidate reaches production | mitigate | Scoped patch identity, independent checks and exact authorized deployment proof. |
| T-20-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/20-integration-reaudit/20-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

