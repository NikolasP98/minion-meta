---
phase: 17-container-runtime
plan: "04"
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - "scripts/qc/image-provenance.mjs"
  - "scripts/qc/image-provenance.test.mjs"
  - ".planning/phases/17-container-runtime/17-IMAGE-PROVENANCE.md"
  - "scripts/qc/fixture-restore.mjs"
  - "scripts/qc/fixture-restore.test.mjs"
  - ".planning/phases/17-container-runtime/17-RESTORE-RESULTS.md"
autonomous: true
requirements: ["OPS-04"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Manifest identifies all build inputs and unexplained drift; absence of transitive lock cannot be hidden by a top-level tarball digest."
    - "Synthetic restores/rollback actually execute; checksums/effects reconciled; observed results separated from undecided recovery policy."
  artifacts:
    - path: "scripts/qc/image-provenance.mjs"
      provides: "Record dependency and image build identity"
    - path: "scripts/qc/fixture-restore.mjs"
      provides: "Restore synthetic durable state and rehearse rollback"
  key_links:
    - from: "scripts/qc/image-provenance.mjs"
      to: "minion_factory/runner/Dockerfile"
      via: "Read-first contract and behavior verification for record dependency and image build identity"
    - from: "scripts/qc/fixture-restore.mjs"
      to: "minion_factory/scripts/promotion/rollback-previous.sh"
      via: "Read-first contract and behavior verification for restore synthetic durable state and rehearse rollback"
---

<objective>
Prove rebuild provenance fixture restore and rollback.
Purpose: OPS-04 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/17-container-runtime/17-CONTEXT.md
@minion_factory/README.md
@minion_factory/runner/Dockerfile
@minion_factory/agent/Dockerfile
@packages/shells-bridge/docker/Dockerfile.base
@minion_factory/scripts/promotion/rollback-previous.sh
@minion_factory/scripts/promotion/previous-image-state.mjs
@packages/shells-bridge/src/backup.ts

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Record dependency and image build identity</name>
  <files>scripts/qc/image-provenance.mjs, scripts/qc/image-provenance.test.mjs, .planning/phases/17-container-runtime/17-IMAGE-PROVENANCE.md</files>
  <read_first>minion_factory/runner/Dockerfile, minion_factory/agent/Dockerfile, packages/shells-bridge/docker/Dockerfile.base</read_first>
  <behavior>Manifest identifies all build inputs and unexplained drift; absence of transitive lock cannot be hidden by a top-level tarball digest. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Inventory base image digests, installer hashes, global npm transitive lock coverage, runtime package files and licenses. Generate SBOM using an installed verified tool or prepare exact pinned tool requirement; compare two clean disposable builds with declared timestamps/metadata handling and distinguish reproducible content from immutable deployment digest. Unpinned installers/transitives get exact Dockerfile/lock child plans before rebuild certification. Do not rebuild/push production images.</action>
  <verify><automated>node --test scripts/qc/image-provenance.test.mjs</automated></verify>
  <done>Manifest identifies all build inputs and unexplained drift; absence of transitive lock cannot be hidden by a top-level tarball digest.</done>
</task>

<task type="auto">
  <name>Task 2: Restore synthetic durable state and rehearse rollback</name>
  <files>scripts/qc/fixture-restore.mjs, scripts/qc/fixture-restore.test.mjs, .planning/phases/17-container-runtime/17-RESTORE-RESULTS.md</files>
  <read_first>minion_factory/scripts/promotion/rollback-previous.sh, minion_factory/scripts/promotion/previous-image-state.mjs, packages/shells-bridge/src/backup.ts</read_first>
  <behavior>Synthetic restores/rollback actually execute; checksums/effects reconciled; observed results separated from undecided recovery policy. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Create synthetic job/effect/attachment state in disposable volumes, snapshot/hash, restore into a separate project and verify identity, pending effects, checksums and startup compatibility. Rehearse exact-image previous/candidate rollback without touching live promotion state. Record observed recovery duration/data loss; do not fabricate target RPO/RTO. Require policy decisions in19 before claiming recovery meets service obligations.</action>
  <verify><automated>node --test scripts/qc/fixture-restore.test.mjs &amp;&amp; node scripts/qc/fixture-restore.mjs --disposable-only</automated></verify>
  <done>Synthetic restores/rollback actually execute; checksums/effects reconciled; observed results separated from undecided recovery policy.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Host / Docker socket / agent / remote effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-17-04-01 | E/T | Host escape, wrong image or uncontrolled operational mutation | mitigate | Exact identity gates, disposable resources, external drill proof and explicit live-action authority. |
| T-17-04-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/17-container-runtime/17-04-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

