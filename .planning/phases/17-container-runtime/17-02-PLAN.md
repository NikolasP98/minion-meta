---
phase: 17-container-runtime
plan: "02"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - "ops/compose.qc.yml"
  - "ops/caddy/Caddyfile.qc"
  - "scripts/qc/compose-contract.test.mjs"
  - "scripts/qc/compose-smoke.mjs"
  - "scripts/qc/compose-smoke.test.mjs"
  - ".planning/phases/17-container-runtime/17-COMPOSE-RESULTS.md"
autonomous: true
requirements: ["OPS-02"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Profile resolves without secrets/missing contexts and documents actual included/external services and readiness."
    - "Supported profile passes actual boot/ready/shutdown flows; missing images/services fail explicitly, not mocked health acceptance."
  artifacts:
    - path: "ops/compose.qc.yml"
      provides: "Resolve and constrain the supported service topology"
    - path: "scripts/qc/compose-smoke.mjs"
      provides: "Execute readiness failure and graceful shutdown fixtures"
  key_links:
    - from: "ops/compose.qc.yml"
      to: "ops/compose.yml"
      via: "Read-first contract and behavior verification for resolve and constrain the supported service topology"
    - from: "scripts/qc/compose-smoke.mjs"
      to: "ops/compose.qc.yml"
      via: "Read-first contract and behavior verification for execute readiness failure and graceful shutdown fixtures"
---

<objective>
Qualify one supported disposable deployment profile.
Purpose: OPS-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/17-container-runtime/17-CONTEXT.md
@minion_factory/README.md
@ops/compose.yml
@ops/caddy/Caddyfile
@minion/Dockerfile
@minion_factory/docker-compose.yml
@ops/compose.qc.yml

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Resolve and constrain the supported service topology</name>
  <files>ops/compose.qc.yml, ops/caddy/Caddyfile.qc, scripts/qc/compose-contract.test.mjs</files>
  <read_first>ops/compose.yml, ops/caddy/Caddyfile, minion/Dockerfile, minion_factory/docker-compose.yml</read_first>
  <behavior>Profile resolves without secrets/missing contexts and documents actual included/external services and readiness. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Build a disposable loopback profile using existing supported gateway image/port and actual dependencies with no production volumes or credentials. Do not reference nonexistent Hub Dockerfile or old Turso/BetterAuth defaults. If Hub remains Vercel-hosted, explicitly represent its external authenticated boundary and use a test server/client in local profile; don't silently claim local Hub startup. Name unique project/volumes and verify localhost ports, least-required mounts, dependency health/readiness and shutdown contract.</action>
  <verify><automated>node --test scripts/qc/compose-contract.test.mjs &amp;&amp; docker compose -f ops/compose.qc.yml config --quiet</automated></verify>
  <done>Profile resolves without secrets/missing contexts and documents actual included/external services and readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Execute readiness failure and graceful shutdown fixtures</name>
  <files>scripts/qc/compose-smoke.mjs, scripts/qc/compose-smoke.test.mjs, .planning/phases/17-container-runtime/17-COMPOSE-RESULTS.md</files>
  <read_first>ops/compose.qc.yml</read_first>
  <behavior>Supported profile passes actual boot/ready/shutdown flows; missing images/services fail explicitly, not mocked health acceptance. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Start only the uniquely named disposable project using exact images. Exercise authenticated connection, dependency unavailable at boot, readiness recovery and SIGTERM with in-flight synthetic request. Capture image IDs/ports/health/exit and verify no unintended host socket/volume access. Tear down only project-owned ephemeral resources after result capture. Resource defaults need measured justification; retain explicit unqualified limits until19 results.</action>
  <verify><automated>node --test scripts/qc/compose-smoke.test.mjs &amp;&amp; node scripts/qc/compose-smoke.mjs --profile ops/compose.qc.yml --disposable-only</automated></verify>
  <done>Supported profile passes actual boot/ready/shutdown flows; missing images/services fail explicitly, not mocked health acceptance.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Host / Docker socket / agent / remote effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-17-02-01 | E/T | Host escape, wrong image or uncontrolled operational mutation | mitigate | Exact identity gates, disposable resources, external drill proof and explicit live-action authority. |
| T-17-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/17-container-runtime/17-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

