---
phase: 17-container-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "scripts/qc/runtime-posture.mjs"
  - "scripts/qc/runtime-posture.test.mjs"
  - ".planning/phases/17-container-runtime/17-RUNTIME-IDENTITY.md"
  - ".planning/phases/17-container-runtime/17-OPERATOR-GATE.md"
  - "scripts/qc/operator-gate-contract.test.mjs"
autonomous: true
requirements: ["OPS-01"]
readiness: "Read-only ready; operational gate packet does not itself authorize runner recreation."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Collector rejects identity drift, excludes secrets and distinguishes actual process uid from Config.User."
    - "Packet fixes target image/flags and preserves queued work; known race and needed maintenance authority explicit."
  artifacts:
    - path: "scripts/qc/runtime-posture.mjs"
      provides: "Automate allowlisted runtime identity collection"
    - path: ".planning/phases/17-container-runtime/17-OPERATOR-GATE.md"
      provides: "Refresh the exact-image pause/recreation packet"
  key_links:
    - from: "scripts/qc/runtime-posture.mjs"
      to: ".planning/research/360-infrastructure-verification.md"
      via: "Read-first contract and behavior verification for automate allowlisted runtime identity collection"
    - from: ".planning/phases/17-container-runtime/17-OPERATOR-GATE.md"
      to: ".planning/research/360-infrastructure-verification.md"
      via: "Read-first contract and behavior verification for refresh the exact-image pause/recreation packet"
---

<objective>
Record exact runtime posture and prepare bounded operational gate.
Purpose: OPS-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/17-container-runtime/17-CONTEXT.md
@minion_factory/README.md
@.planning/research/360-infrastructure-verification.md
@minion_factory/scripts/promotion/host-preflight.sh
@minion_factory/docker-compose.yml

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Automate allowlisted runtime identity collection</name>
  <files>scripts/qc/runtime-posture.mjs, scripts/qc/runtime-posture.test.mjs, .planning/phases/17-container-runtime/17-RUNTIME-IDENTITY.md</files>
  <read_first>.planning/research/360-infrastructure-verification.md, minion_factory/scripts/promotion/host-preflight.sh, minion_factory/docker-compose.yml</read_first>
  <behavior>Collector rejects identity drift, excludes secrets and distinguishes actual process uid from Config.User. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Create a read-only collector for actual image ID/revision, runningSha, configured flags, process uid, mounts and resource/security limits. Use approved SSH identity/host from verified evidence, not stale Tailscale status. Parse docker inspect privately and emit allowlisted fields only; never full env or customer logs. Compare Factory health/host/receipt identity plus gateway/vector images. A mismatch blocks change admission and does not trigger repair automatically.</action>
  <verify><automated>node --test scripts/qc/runtime-posture.test.mjs</automated></verify>
  <done>Collector rejects identity drift, excludes secrets and distinguishes actual process uid from Config.User.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the exact-image pause/recreation packet</name>
  <files>.planning/phases/17-container-runtime/17-OPERATOR-GATE.md, scripts/qc/operator-gate-contract.test.mjs</files>
  <read_first>.planning/research/360-infrastructure-verification.md</read_first>
  <behavior>Packet fixes target image/flags and preserves queued work; known race and needed maintenance authority explicit. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Prepare current immutable runner image, promotion flock, live worker/sweep preflight, selected flags and rollback/postconditions. Preserve distinction AUTOPROMOTE spec admission vs deployment promotion. Existing runtime has no in-process drain barrier; packet must explicitly expose maintenance interruption/race and request that authority only when ready. Do not execute env mutation/recreation here or enable containment. No empty queue snapshot is represented as a drain proof.</action>
  <verify><automated>node --test scripts/qc/operator-gate-contract.test.mjs</automated></verify>
  <done>Packet fixes target image/flags and preserves queued work; known race and needed maintenance authority explicit.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Host / Docker socket / agent / remote effect | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-17-01-01 | E/T | Host escape, wrong image or uncontrolled operational mutation | mitigate | Exact identity gates, disposable resources, external drill proof and explicit live-action authority. |
| T-17-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/17-container-runtime/17-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

