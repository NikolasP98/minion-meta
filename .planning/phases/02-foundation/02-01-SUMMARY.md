---
plan: 02-01
phase: 02-foundation
status: complete
requirements: [FOUND-02, FOUND-08, FOUND-09]
completed_at: 2026-04-19
---

# Plan 02-01 Summary: Meta-repo scaffold

## Objective
Establish the meta-repo skeleton so subsequent plans can publish packages into `packages/*` and the `minion` CLI can read `minion.json`.

## What was built

**Branch:** default renamed `master` → `main` (no remote yet — push is Plan 02-02).

**Files created / rewritten (14 total):**

- `.gitignore` — rewritten for meta-repo model: 7 subprojects + 2 defensive worktree entries + VAULT symlinks + root one-off files all ignored; legacy "intentionally tracked" comment removed
- `package.json` — `@minion/root` private workspace, `packageManager: pnpm@10.15.0`, Node >=22, `workspaces: ["packages/*"]`, `@changesets/cli@^2.27.0` devDep
- `pnpm-workspace.yaml` — `packages/*` glob
- `.env.defaults` — root-level non-secret defaults (MINION_META_REPO_ROOT, MINION_DEFAULT_INFISICAL_DOMAIN, MINION_DEFAULT_INFISICAL_CORE_PROJECT)
- `.env.example` — required secret var names (INFISICAL_UNIVERSAL_AUTH_CLIENT_ID/SECRET)
- `minion.json` — subproject registry for all 6 IDs (minion, hub, site, paperclip, pixel-agents, plugins) with exact branches + remotes + commands + Infisical project names from CONTEXT D3
- `packages/cli/minion.schema.json` — draft-07 JSON Schema validating the registry shape
- `packages/cli/package.json` — `@minion/cli` skeleton (private:true, bin:minion → dist/index.js)
- `packages/env/package.json` — `@minion/env` skeleton
- `packages/tsconfig/package.json` — `@minion/tsconfig` skeleton with `exports` map for 4 variants
- `packages/lint-config/package.json` — `@minion/lint-config` skeleton with `exports` map for 3 configs
- `.changeset/README.md` — from `changeset init`
- `.changeset/config.json` — overridden per D5: `access: public`, `baseBranch: main`, `updateInternalDependencies: patch`, no `ignore`
- `pnpm-lock.yaml` — generated by `pnpm install`

All 4 skeletons are `private: true` to block accidental npm publish before Plans 02-03/04/05/06 fill in implementation.

## Commit

`506df4dc` — `feat(02-01): meta-repo scaffold — workspace, registry, changesets` (14 files, 1131 insertions)

## Verification (automated checks all passed)

- [x] `git branch --show-current` returns `main`
- [x] `.gitignore` ignores all 7 subprojects + `paperclip-pi-fallback/` + `paperclip-meta-repair/` defensive entries
- [x] `.gitignore` no longer contains "intentionally tracked" legacy comment
- [x] `package.json` declares `@minion/root` with packageManager + workspace glob
- [x] `pnpm install` succeeded; `node_modules/@changesets/cli` present
- [x] `pnpm exec changeset status` CLI callable (recognises AI/ as Changesets root)
- [x] `.changeset/config.json` has `baseBranch: main`, `access: public`
- [x] `minion.json` has 6 subproject IDs with correct branch/remote per `reference_github_repo_names.md`
- [x] All 4 `packages/<name>/package.json` skeletons exist with correct `@minion/<name>` names
- [x] `packages/cli/minion.schema.json` is a valid JSON Schema (has `definitions.subproject`)
- [x] `git ls-files` returns ZERO entries under `minion/`, `minion_hub/`, `minion_site/`, `paperclip-minion/`, `pixel-agents/`, `minion_plugins/`, `minion-shared/`, `omnisearch/` — subprojects correctly ignored

## Ready for next wave

**Plan 02-02 (Wave 2)** can now proceed with:
- npm `@minion` org creation (human-action)
- `gh repo create NikolasP98/minion-meta` (human-action)
- Initial push of all 14 meta-repo commits (the 12 pre-existing + the scaffold commit + 02-01-SUMMARY commit)

No meta-repo file conflicts expected in Wave 2 — 02-02 only touches external state (npm + GH) and adds initial remote config.

## Self-Check
PASSED — all acceptance criteria met, scaffold committed on `main`, subproject isolation verified.
