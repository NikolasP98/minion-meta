---
phase: 04-fold-minion-shared
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - .gitignore
autonomous: true
requirements:
  - SHARE-01
  - SHARE-05

must_haves:
  truths:
    - "`minion-shared/` line is removed from `.gitignore`"
    - "`minion-shared/` directory is deleted from the filesystem"
    - "Meta-repo git history is clean — no stray tracked files from minion-shared/"
    - "VERIFICATION.md documents SHARE-01 and SHARE-05 as N/A with rationale"
  artifacts:
    - path: ".gitignore"
      provides: "updated ignore file without minion-shared/ entry"
    - path: ".planning/phases/04-fold-minion-shared/04-VERIFICATION.md"
      provides: "phase verification document noting N/A items"
  key_links:
    - from: ".gitignore"
      to: "filesystem"
      via: "minion-shared/ no longer gitignored (directory deleted)"
      pattern: "^(?!.*minion-shared)"
---

<objective>
Remove `minion-shared/` from `.gitignore` and delete the directory, completing the migration. Document SHARE-01 and SHARE-05 as N/A in the phase VERIFICATION.md (no git history to subtree-add, no GitHub repo to archive).

Note on SHARE-01: `git subtree add` is explicitly N/A. `minion-shared/` has no separate git history — it is a plain directory gitignored from the meta-repo. There is no git history to import.

Note on SHARE-05: Archiving the old GitHub repo is explicitly N/A. `minion-shared` does not have a separate GitHub repository; it was only published to npm. No archive action is possible or needed.

Purpose: Leaves the meta-repo in a clean state — no dead directory, no misleading gitignore entry.
Output: `minion-shared/` is gone from disk. `.gitignore` no longer references it. VERIFICATION.md written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fold-minion-shared/04-CONTEXT.md
@.planning/phases/04-fold-minion-shared/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove minion-shared/ from .gitignore and delete the directory</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore — read full contents to find exact minion-shared/ line
    - .planning/phases/04-fold-minion-shared/04-03-SUMMARY.md — confirm PR is open (migration complete before deletion)
  </read_first>
  <action>
    PREREQUISITE CHECK: Confirm the minion_site migration PR is open before deleting anything.
    Run: `gh pr list --repo NikolasP98/minion-site --state open | grep minion-stack-shared`
    If no PR found, STOP and flag this to the user — do not delete minion-shared/ until migration is confirmed.

    If PR is confirmed open:

    1. Read `.gitignore` in full.

    2. Remove the line `minion-shared/` from `.gitignore`. The line appears in the subproject gitignore section. Remove exactly that line; preserve all other content.

    3. Write the updated `.gitignore` back.

    4. Verify the removal:
       `grep "minion-shared" /home/nikolas/Documents/CODE/AI/.gitignore`
       Expected: no output.

    5. Delete the `minion-shared/` directory from the filesystem:
       `rm -rf /home/nikolas/Documents/CODE/AI/minion-shared/`

    6. Verify the directory is gone:
       `ls /home/nikolas/Documents/CODE/AI/minion-shared/ 2>&1`
       Expected: `ls: cannot access '...minion-shared/': No such file or directory`

    7. Run `git status` from the meta-repo root to confirm no unexpected changes were tracked:
       `cd /home/nikolas/Documents/CODE/AI && git status`
       The only change should be `.gitignore` (minion-shared/ was gitignored, so deleting it adds no untracked/deleted entries to git — it was never tracked).

    8. Stage and commit the gitignore change:
       ```bash
       cd /home/nikolas/Documents/CODE/AI
       git add .gitignore
       git commit -m "chore(phase-04): remove minion-shared/ from gitignore + delete directory

       minion-shared/ source has been migrated to packages/shared/ as @minion-stack/shared.
       The deprecation shim (minion-shared@0.2.0) is published on npm.
       minion_site PR is open to migrate the sole consumer.

       SHARE-01: N/A (no git history existed — plain gitignored directory)
       SHARE-05: N/A (no GitHub repo existed — npm-only package)"
       ```
  </action>
  <verify>
    <automated>
      ! grep "minion-shared" /home/nikolas/Documents/CODE/AI/.gitignore && ! ls /home/nikolas/Documents/CODE/AI/minion-shared/ 2>/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "minion-shared" .gitignore` returns no output (grep-verifiable)
    - `ls minion-shared/` returns "No such file or directory" (filesystem check)
    - `git status` in meta-repo root shows only `.gitignore` was changed (no unexpected deletions)
    - `git log --oneline -1` shows the chore commit message containing "minion-shared"
  </acceptance_criteria>
  <done>minion-shared/ removed from gitignore and deleted. Meta-repo git state is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write phase VERIFICATION.md</name>
  <files>.planning/phases/04-fold-minion-shared/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-fold-minion-shared/04-01-SUMMARY.md
    - .planning/phases/04-fold-minion-shared/04-02-SUMMARY.md
    - .planning/phases/04-fold-minion-shared/04-03-SUMMARY.md
  </read_first>
  <action>
    Create `.planning/phases/04-fold-minion-shared/04-VERIFICATION.md` documenting the phase outcome and N/A items.

    The document must cover:

    1. **SHARE-01 status**: N/A — `git subtree add` was not applicable. `minion-shared/` had no separate git history (it was a plain directory gitignored from the meta-repo, never a git submodule or standalone git repo). Source migration was done via directory copy, preserving all content. Rationale: preserving git history only applies when a subdirectory has its own git repo; this did not.

    2. **SHARE-02 status**: Complete — `@minion-stack/shared@0.1.0` published to npm. Include the `npm view @minion-stack/shared version` output from 04-02-SUMMARY.md.

    3. **SHARE-03 status**: Complete — `minion-shared@0.2.0` published as deprecation shim. `npm deprecate` executed. Include confirmation from 04-02-SUMMARY.md.

    4. **SHARE-04 status**: Complete (PR open) — `minion_site` PR opened migrating 3 import files from `'minion-shared'` to `'@minion-stack/shared'`. Confirmed `minion_hub` and `paperclip-minion` are non-consumers (zero import of minion-shared). Include PR URL from 04-03-SUMMARY.md.

    5. **SHARE-05 status**: N/A — `minion-shared` had no separate GitHub repository. It was published to npm from the `minion-shared/` directory but was never a standalone GitHub repo with its own remote. No archive action is possible. Rationale: archiving only applies to GitHub repos; this package had none.

    6. **Phase success criteria check** (from ROADMAP.md):
       - SC1: `packages/shared` contains full minion-shared source (git subtree N/A — direct copy used)
       - SC2: `@minion-stack/shared` published and importable ✓
       - SC3: Old `minion-shared` deprecated with notice ✓
       - SC4: minion_site migrated (PR open); minion_hub + paperclip confirmed non-consumers ✓
       - SC5: Old minion-shared GitHub repo archived — N/A (no such repo)

    7. **Deferred items** (if any): None.

    Format the file as clean Markdown with dated verification timestamp.
  </action>
  <verify>
    <automated>
      ls /home/nikolas/Documents/CODE/AI/.planning/phases/04-fold-minion-shared/04-VERIFICATION.md && grep "SHARE-01" /home/nikolas/Documents/CODE/AI/.planning/phases/04-fold-minion-shared/04-VERIFICATION.md && grep "N/A" /home/nikolas/Documents/CODE/AI/.planning/phases/04-fold-minion-shared/04-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `04-VERIFICATION.md` exists under the phase directory
    - File contains "SHARE-01" with N/A rationale (grep-verifiable)
    - File contains "SHARE-05" with N/A rationale (grep-verifiable)
    - File contains "SHARE-02" and "SHARE-03" marked Complete (grep-verifiable)
    - File contains "SHARE-04" marked Complete with PR URL
    - File documents all 5 ROADMAP phase success criteria with pass/N/A status
  </acceptance_criteria>
  <done>VERIFICATION.md written and accurately reflects phase 4 completion state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem deletion | rm -rf is irreversible; mitigated by prerequisite PR check |
| .gitignore mutation | Removing gitignore entry for a deleted directory — no security implication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | rm -rf minion-shared/ | mitigate | Prerequisite: confirm minion_site PR is open before delete. Source is preserved in packages/shared/ and on npm. The directory was gitignored and never tracked by meta-repo git, so no history is lost. |
| T-04-12 | Denial of Service | Premature deletion | mitigate | Task 1 explicitly checks for open PR via gh cli before proceeding. If PR not found, task stops and flags to user. |
</threat_model>

<verification>
After both tasks complete:
1. `grep "minion-shared" .gitignore` → no output
2. `ls minion-shared/` → "No such file or directory"
3. `git log --oneline -1` → shows chore commit
4. `ls .planning/phases/04-fold-minion-shared/04-VERIFICATION.md` → file exists
5. `grep -c "N/A" .planning/phases/04-fold-minion-shared/04-VERIFICATION.md` → at least 2 (SHARE-01 and SHARE-05)
6. `npm view @minion-stack/shared version` → 0.1.0 (npm-side check, no local dependency)
</verification>

<success_criteria>
- `minion-shared/` is deleted from disk
- `.gitignore` no longer references `minion-shared/`
- Meta-repo git state is clean (only `.gitignore` modified)
- `04-VERIFICATION.md` documents all 5 SHARE-xx requirements with accurate status
- SHARE-01 and SHARE-05 documented as N/A with full rationale
</success_criteria>

<output>
After completion, create `.planning/phases/04-fold-minion-shared/04-04-SUMMARY.md` with:
- Confirmation that minion-shared/ was deleted
- git status output showing clean state
- Link to 04-VERIFICATION.md
- Phase 4 overall completion status

This is the final plan in Phase 4. After this SUMMARY is written, run `/gsd-transition` to mark Phase 4 complete and advance STATE.md to Phase 5.
</output>
