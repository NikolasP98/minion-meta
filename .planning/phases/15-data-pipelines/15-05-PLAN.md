---
phase: 15-data-pipelines
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - minion/services/brain-vector/src/worker.ts
  - minion/services/brain-vector/src/outbox.ts
  - minion/services/brain-vector/src/types.ts
  - minion/services/brain-vector/src/database-migration.ts
  - minion/services/brain-vector/sql/002_brain_vector_claim_fencing.sql
  - minion/services/brain-vector/test/worker.test.ts
  - minion/services/brain-vector/test/outbox.test.ts
  - minion/services/brain-vector/test/claim-fencing.postgres.fixture.ts
autonomous: false
requirements: [DATA-02, JOB-02]
readiness: "Task 1 exact-site comments only admitted. Behavioral contract, eight-file implementation and native execution remain draft-gated."
closure_policy: "Claim bookkeeping and cooperative dispatch stop do not close remote-effect, reconcile, migration deployment or SDK requirements."
must_haves:
  truths:
    - "A reclaimed claim of the same desired revision cannot be renewed, ACKed, retried or dead-lettered by its former claimant."
    - "One heartbeat operation covers all unsettled claimed rows; abort or ownership loss prevents new external dispatch."
    - "Forward002 and exact v2 runner verification preserve applied001; legacy mutation cannot bypass the new worker-role contract."
  artifacts:
    - path: minion/services/brain-vector/sql/002_brain_vector_claim_fencing.sql
      provides: "Forward claim-generation, v2 RPC and migration-receipt contract"
    - path: minion/services/brain-vector/test/claim-fencing.postgres.fixture.ts
      provides: "Explicit opt-in actual PostgreSQL ownership and migration verification"
  key_links:
    - from: minion/services/brain-vector/src/outbox.ts
      to: minion/services/brain-vector/sql/002_brain_vector_claim_fencing.sql
      via: "Required complete claim tuple on every v2 mutation; no legacy fallback"
    - from: minion/services/brain-vector/src/worker.ts
      to: minion/services/brain-vector/src/outbox.ts
      via: "Observed nonoverlapping renewal and guarded settlement of actual claims"
    - from: minion/services/brain-vector/src/database-migration.ts
      to: minion/services/brain-vector/sql/002_brain_vector_claim_fencing.sql
      via: "Explicit v2 opt-in, serialized preflight, immutable hash receipt and exact ACL/catalog validation"
---

<objective>
Prepare and qualify a bounded canonical vector-worker claim-fencing candidate. Separate source revision from transient claim ownership, prevent stale database settlement, and stop new work after process cancellation or lease loss. Preserve the existing serving/search/reconcile APIs and make unresolved remote effects explicit. This plan does not activate the candidate or replace the Hub corpus pipeline.
</objective>

<context>
@AGENTS.md
@minion/AGENTS.md
@minion/.dmux-hooks/CLAUDE.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/15-data-pipelines/15-VECTOR-WORKER-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-10-API-SCHEMA-DECISION.md
@.planning/phases/10-durable-jobs-stock/10-REMAINING-HANDLER-DECISION.md
@minion/services/brain-vector/src/embedding-client.ts
@minion/services/brain-vector/src/qdrant.ts
@minion/services/brain-vector/src/cli.ts
@minion/services/brain-vector/sql/001_brain_vector.sql
@minion/services/brain-vector/test/migration.postgres.test.ts
@minion/services/brain-vector/README.md
</context>

<boundaries>
Root admits Task 1 comments only below; no behavioral implementation is admitted. Current gateway branch is fix/ci-cost-remaining-gaps; inspect, do not switch. Preserve shared WIP. No automatic commits, branch/worktree/stash operations, installs, provider/network calls, live database mutation, applied001 edits or broad tests. Root owns global planning, proposals, execution admission and resources. The eight listed implementation files plus a later15-05-SUMMARY are the maximum proposed executor scope. Additional source/config callers require a separate amendment/child.

No new scheduler, queue library or database client. Reuse existing PostgreSQL RPCs/transactions and AbortSignal/timers facilities. Current postgres3.4.9 is evidence, not an upgrade request. No Node engine change. No Hub service imports into the standalone worker, no normalization/model/batch-size changes, no retry-budget changes. Preserve per-org/generation partitioning and payload/search protocol.

The source-only Task1 gate may be admitted before actual SQL fixture execution. Task2 requires root's exact claim/RPC/migration contract selection. Task3 cannot run until root has admitted a marked disposable PostgreSQL target with required extensions, namespace/database/role cleanup authority, environment allowlist and private runner configuration. Existing migration.postgres.test.ts starts Docker host networking: do not run it, the package default test script or the per-org E2E. Missing fixture infrastructure is an unmet gate, not permission to load application environment or substitute mocks for concurrency.

Root must separately admit migration/CLI/artifact adoption and remote publication qualification before activation. Old/new workers are not rolling-compatible. No connection/backend/process fault injection is part of this plan.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze exact contracts and record worker handoffs</name>
  <files>minion/services/brain-vector/src/worker.ts, minion/services/brain-vector/src/outbox.ts, minion/services/brain-vector/src/database-migration.ts</files>
  <action>Recheck frozen source identities in the decision packet, actual branch and local ownership. Preserve before-images. Add only exact-site TODO(handoff) comments first: in-memory embedding materialization; retry after uncertain Qdrant/ACK outcome; unclaimed reconcile materialization/write; run signal boundary; outbox desired-revision versus claim-owner seam; migration execution before version/ledger verification. Point each to proposals/2026-09-08-platform-qc-remediation.md and send paired concrete proposal wording to root. Do not annotate applied001 or claim a comment repairs behavior.

Return for root selection of the required v2 tuple, global noncycling claim sequence, deterministic heartbeat batch contract, lease clock, legacy RPC revocation and explicit versioned migration runner described in the decision packet. Record the hardcoded001 CLI and existing unsafe default native fixture as separate adoption/test-lane gates. Confirm no15-05 migration filename collision or concurrent writer. No runtime/test/SQL call is needed for this preparation task.</action>
  <verify><automated>Read-only diff and SHA-256 comparison of the three owned files; verify only the admitted handoff comments changed.</automated></verify>
  <done>Exact open ends have code-site and root-proposal counterparts; root has a concrete contract to admit before behavioral source work.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Root admission of contract and native verification boundary</name>
  <files>.planning/phases/15-data-pipelines/15-05-PLAN.md, .planning/phases/15-data-pipelines/15-VECTOR-WORKER-DECISION.md</files>
  <action>Root independently reviews Task1 and the decision packet. Admit the precise version2 RPC signatures and complete catalog/ACL postconditions, migration hash receipt and legacy replay refusal, plus the eight-file candidate. Native execution remains separately gated until an explicit credential-free disposable fixture runner/target is recorded. No paid-effect or Qdrant ordering success is implied.</action>
  <verify>Root records the exact selected contract, file scope and separate native target boundary before dependent execution.</verify>
  <done>A concrete root admission is recorded; absent admission keeps behavioral and native tasks pending.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the bounded claim and cooperative-stop candidate</name>
  <files>minion/services/brain-vector/src/worker.ts, minion/services/brain-vector/src/outbox.ts, minion/services/brain-vector/src/types.ts, minion/services/brain-vector/src/database-migration.ts, minion/services/brain-vector/sql/002_brain_vector_claim_fencing.sql, minion/services/brain-vector/test/worker.test.ts, minion/services/brain-vector/test/outbox.test.ts</files>
  <behavior>Write focused red cases for missing/malformed owner fields, stale ACK/retry/dead-letter, claim takeover at unchanged desired revision, ownership loss during materialization, abort during claim/remote work, later-group suppression, heartbeat failure/overlap and duplicate retry after ACK failure. Preserve current worker/reconcile parser and routing regressions.</behavior>
  <action>Add required claimGeneration bigint, leaseOwner and database expiry to ClaimedVectorJob; exclude new claim-only fields from ReconcileChunk's Omit. Parse exact database integer strings without Number precision loss and reject absent/malformed ownership. Use explicit v2 routines only in the production outbox store. Preserve public ACK/retry/dead-letter return semantics but include the complete organization/chunk/collection/revision/owner/claim tuple. Add bounded heartbeat renewal to the existing store interface.

Author002 as a forward transaction after actual Hub/001 prerequisites. Use a global noncycling bigint sequence for every successful claim so ACK deletion/reinsertion cannot recreate an old tuple. Preserve desired revisions and source-update enqueue semantics. Every settlement/renewal requires exact tuple, running state and live lease checked after row locking. Renewal must not resurrect an expired lease. Validate bounded heartbeat descriptors and lock deterministically. Revoke legacy unfenced mutator execution from the worker role and other exposed application roles; preserve current other RPC grants, worker login topology, no direct table privileges and no PUBLIC execution. Do not change legacy001, Hub migrations or unrelated source schemas.

Extend the existing migration runner with explicit version selection: legacy unmigrated mode remains distinguishable; v2 is opt-in with exact002 path/hash, preflight and final contract. Refuse001 before executing it when v2 catalog/ledger exists. Serialize migration attempts;002's version/hash receipt must commit with its DDL, with no fabricated001 applied hash. Matching complete repeat is a no-op; mismatch/partial catalog fails before mutation. Do not strip transaction SQL, invent a generic migration framework, or imply CLI migration has been updated. The unchanged CLI caller is an explicit activation blocker handled in another child.

Propagate run's signal into processBatch scheduling. Maintain one nonoverlapping heartbeat operation over all unsettled claimed rows while awaited work runs, including later organization groups. Observe and clear timers/promises in finally; do not overlap renewed and removed claims. On ownership failure or signal abort, stop new provider/Qdrant/reconcile dispatch, observe an already-started call, and prevent stale settlement/retry. On abort, keep renewal only while the observed in-flight call settles, then release remaining claims by expiry; no invented cancellation success. Check ownership before the next external boundary, acknowledging that this is not remote CAS. A failed ACK must not retry previously finalized peers or enter duplicate nested retry loops. Existing transport timeout behavior stays in place; full caller-signal propagation is a separate six-file child.

Preserve materialization normalization, batching, retry caps and reconcile-once behavior. Narrow resolved TODOs; retain receipt/Qdrant/reconcile/transport limitations at exact sites. Send any unexpected required caller/test boundary to root rather than casting around a broken contract.</action>
  <verify><automated>From minion/services/brain-vector with only PATH/HOME/LANG, use the installed runner: node node_modules/vitest/vitest.mjs run --config vitest.config.ts test/outbox.test.ts test/worker.test.ts --maxWorkers=1; then node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit. Resolve actual local binaries first; do not install or broaden the suite if absent.</automated></verify>
  <done>Focused real worker/store-boundary tests pass with meaningful negatives; typecheck is clean; source/migration candidate is frozen. Mock SQL cannot satisfy the next task or establish lease concurrency.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify exact forward migration and native ownership</name>
  <files>minion/services/brain-vector/test/claim-fencing.postgres.fixture.ts</files>
  <action>Create a specifically selected .fixture.ts that is omitted from the existing default test glob. Root provides a private no-env Vitest config selecting exactly this file and an explicitly marked disposable PostgreSQL target; do not alter normal config or start Docker. Assert target identity/isolation before any write; reject missing marker, inherited/unapproved URL, wrong database and unbounded cleanup. Use root-authorized actual Hub outbox/generation migrations plus unchanged001 before002; a synthetic baseHubContract alone cannot qualify compatibility. If actual prerequisite DDL needs additional fixture/role setup beyond the admitted target, return that exact gap before mutation.

Use distinct live backend PIDs to qualify concurrent claims and stale settlement. Claim A, expire its lease through test-owned SQL, reclaim B at unchanged desired revision, then verify A heartbeat/ACK/retry/dead-letter all return false and B's row is unchanged. Verify current B actions, expiry-without-takeover denial, different org/collection/source-revision/worker token denial, renewal before expiry and no revival after expiry. Test ACK then source enqueue/reinsert with the same desired revision cannot reuse a past claim generation. Verify one of simultaneous claimers wins, heartbeat ordering, duplicates/malformed input and exact per-claim outcomes.

Exercise actual runner v1 prerequisite→v2 upgrade, repeated matching002 no-op, conflicting hash/partial catalog refusal before changes, old001 refusal afterv2, failed migration rollback and version-specific restricted role/ACL sets. Assert actual worker role cannot call legacy mutation functions or access tables/ledger/sequence directly. No backend termination or network/process fault injection. Prove cleanup of only owned fixture rows/schema/database/roles according to root's selected target contract. If a separate database is required because public RPC names are hardcoded, do not improvise per-schema substitutions.

Keep logs/source/catalog hashes separate from SQL unit expectations. Freeze source and SUMMARY for independent root review. Do not execute another full build or expand native tests without a concrete unresolved concern.</action>
  <verify><automated>Root-selected explicit private Vitest configuration, exact claim-fencing.postgres.fixture.ts, one worker, allowlisted environment and marked disposable PostgreSQL only; exact command/target identity must be recorded before execution.</automated></verify>
  <done>Independent actual-engine evidence establishes claim fencing and forward compatibility within the selected disposable catalog. CLI/container adoption, paid receipts, transport abort and Qdrant ordering remain open release gates.</done>
</task>
</tasks>

<verification>
Root performs separate Standards and Spec reviews. Required identities include unchanged001, proposed002, exact runner/source, actual emitted entry when later built, and native catalog; a source hash is not deployment evidence. Preserve all previous findings. Do not close JOB-02 from timers/mocks or DATA-02 from authored SQL. This plan is not admitted until root fills the exact contract and fixture prerequisites above.
</verification>

<output>
15-05-SUMMARY.md will distinguish comment preparation, source candidate, native evidence and deployment blockers. Root owns the paired proposal and subsequent bounded transport-stop, paid-receipt, Qdrant/reconcile and CLI/artifact children described in15-VECTOR-WORKER-DECISION.md. Draft IDs for those children are intentionally unassigned until collision/dependency review.
</output>


## Root Task 1 annotation admission

Root reviewed the three-file comment-only preparation boundary. Add exact-site TODO(handoff) comments in worker.ts, outbox.ts and database-migration.ts as Task1 specifies, after confirming local instructions/current hashes. Do not change executable tokens, SQL, APIs, tests, imports, fixtures or applied001. Root pairs the concrete findings in the canonical proposal. A token-equivalence or narrowly inspected diff is sufficient verification; no test or server run is needed for these annotations. Source behavior and the remaining seven/eight-file candidate are not accepted by this admission. Root must independently review the migration/ownership contract before any behavioral work.
