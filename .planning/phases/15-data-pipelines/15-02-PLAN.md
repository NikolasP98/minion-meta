---
phase: 15-data-pipelines
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "minion_hub/src/server/services/finance-statement-parser.ts"
  - "minion_hub/src/server/services/finance-statement-parser.test.ts"
  - "minion_hub/src/server/services/finance-statements.service.ts"
  - "minion_hub/src/server/services/finance-statements.service.test.ts"
autonomous: true
requirements: ["DATA-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Malformed/ambiguous input yields traceable rejection; valid records preserve financial meaning and deterministic source identity."
    - "Interrupted replay neither loses nor duplicates records; source version change is explicit; work/memory bounds hold."
  artifacts:
    - path: "minion_hub/src/server/services/finance-statement-parser.ts"
      provides: "Qualify parser boundary with malformed and oversized records"
    - path: "minion_hub/src/server/services/finance-statements.service.ts"
      provides: "Bind ingestion retry identity and cursor to the source version"
  key_links:
    - from: "minion_hub/src/server/services/finance-statement-parser.ts"
      to: "minion_hub/src/server/services/finance-statements.service.ts"
      via: "Read-first contract and behavior verification for qualify parser boundary with malformed and oversized records"
    - from: "minion_hub/src/server/services/finance-statements.service.ts"
      to: "minion_hub/src/server/services/bg-runtime.ts"
      via: "Read-first contract and behavior verification for bind ingestion retry identity and cursor to the source version"
---

<objective>
Preserve finance parser provenance and bounded ingestion.
Purpose: DATA-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/15-data-pipelines/15-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/src/server/services/finance-statements.service.ts
@minion_hub/src/server/services/bg-runtime.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export interface StatementEntryRejected { sourceRow: number; ok: false; reason: string; raw: Record<string,string> }
// StatementEntryOk preserves sourceRow, raw, warnings and signedAmount as a fixed(2) string.
// StatementParseResult has entries, rows, rejected and headerFields.
// Brain job registration keys: brain_corpus_conversations and legacy brain_corpus_whatsapp.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Qualify parser boundary with malformed and oversized records</name>
  <files>minion_hub/src/server/services/finance-statement-parser.ts, minion_hub/src/server/services/finance-statement-parser.test.ts</files>
  <read_first>minion_hub/src/server/services/finance-statements.service.ts</read_first>
  <behavior>Malformed/ambiguous input yields traceable rejection; valid records preserve financial meaning and deterministic source identity. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Exercise parseStatementCsv/Date/Amount using deterministic synthetic UTF-8, quoted multiline fields, decimal/date ambiguity, duplicates, oversized records and invalid encodings. Preserve explicit rejected variants and original record identity; don't convert ambiguous values to fabricated defaults. Add bounded input/row work where absent, using documented enforced limits and actionable rejection reasons. Keep financial sign/currency semantics and source raw value provenance.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/finance-statement-parser.test.ts</automated></verify>
  <done>Malformed/ambiguous input yields traceable rejection; valid records preserve financial meaning and deterministic source identity.</done>
</task>

<task type="auto">
  <name>Task 2: Bind ingestion retry identity and cursor to the source version</name>
  <files>minion_hub/src/server/services/finance-statements.service.ts, minion_hub/src/server/services/finance-statements.service.test.ts</files>
  <read_first>minion_hub/src/server/services/bg-runtime.ts</read_first>
  <behavior>Interrupted replay neither loses nor duplicates records; source version change is explicit; work/memory bounds hold. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Trace parse result through actual import persistence and cursor ownership. Bind retry identity to source hash/row identity and persist progress only after durable outcomes; input change invalidates stale cursor rather than skipping records. Use phase10 owner/effect envelope where this service is a consumer. Inject interruption before/after persistence and replay duplicates, preserving reject reasons. No live finance import or historical correction.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/server/services/finance-statements.service.test.ts</automated></verify>
  <done>Interrupted replay neither loses nor duplicates records; source version change is explicit; work/memory bounds hold.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Input document / tenant query / retained stores | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-15-02-01 | T/I/E | Ambiguous parsing or unauthorized data/copy access | mitigate | Source identity, closed datasets, independent field policy and explicit deletion authority. |
| T-15-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/15-data-pipelines/15-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

