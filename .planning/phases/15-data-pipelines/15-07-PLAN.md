---
phase: 15-data-pipelines
plan: "07"
type: execute
wave: 5
depends_on: ["15-06", "10-04", "10-07"]
files_modified:
  - "minion_hub/src/server/services/finance-statements.service.ts"
  - "minion_hub/src/server/services/finance-statements.service.test.ts"
  - "minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts"
  - "minion_hub/src/server/services/finance-statement-parser.ts"
  - "minion_hub/src/server/services/finance-statement-parser.test.ts"
autonomous: true
requirements: ["DATA-01", "JOB-01", "JOB-02"]
readiness: "Coherent 10x base and two parser overlays identified; private native DB execution requires exact fixture/config admission"
must_haves:
  truths:
    - "New imports record the actual exported parser version, while stale resumable versions cannot mutate imports or accounting progress."
    - "Raw source-byte integrity and normalized parser provenance are validated independently."
    - "Historical imports remain attributable and unsafe header failures have fixed content-free diagnostics."
  artifacts:
    - path: "minion_hub/src/server/services/finance-statements.service.ts"
      provides: "Version/provenance binding within the existing durable ownership fence"
    - path: "minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts"
      provides: "Native transaction and resumable cursor regressions"
  key_links:
    - from: "minion_hub/src/server/services/finance-statements.service.ts"
      to: "minion_hub/src/server/services/finance-statement-parser.ts"
      via: "Exported version and decoded normalized input provenance"
---
<objective>Integrate the qualified parser v3 with the preserved durable finance job implementation without rewriting historical imports.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@minion_hub/CLAUDE.md
@.planning/phases/15-data-pipelines/15-06-SUMMARY.md
@.planning/phases/10-durable-jobs-stock/10-04-SUMMARY.md
@.planning/phases/10-durable-jobs-stock/10-07-SUMMARY.md
@proposals/2026-09-10-hub-finance-parser-version-binding.md
Read-only decision: /home/nikolas/.cache/minion-qc/finance-binding-review/REPORT.md, SHA256 d638e093e819f424524802034ed58634e6dcb76f49d8efd1c7ff1b416718cc52.
</context>
<boundaries>
Use a fresh credential-free ordinary source copy of the preserved hub-10x tree at immutable base 1df0a9216ad3f7f85d989eb59cfccb779d9f7285 with its exact 39 staged delivery overlays. Preserve all staged/index/source bytes in original trees. Overlay only the two qualified parser files from hub-16-01-15-02; never replace the durable service/helpers/config with its unfenced versions. Product ownership is exactly five paths. Private snapshot/manifest, unit/native configs and network guard are setup artifacts outside repositories. No package/lock, runtime/helper/migration/schema, UI/route, telemetry or global-config changes. No branch/worktree/commit, production DB, customer import, provider request, fault injection, backend termination or historical backfill.

Root selects D360-20: old-version queued/parsing work and explicit retry fail closed with a fixed parser-version conflict. Preserve historical version, cursor, counters and transactions. Ordinary retry must not silently upgrade or claim recovery. Exact current historical done/failed results may return stored outcomes without re-parsing under v3. A new historical version-replacement/recovery workflow requires its own data contract; it is not implemented by a zero-cursor shortcut. Maintain monetary sign/currency/dedupe/sourceRow semantics.

Unit/native suites use explicit private configuration, no application .env/default config, one worker and bounded resources. Native execution requires root review of a newly verified marked literal-loopback fixture, all exact migration/config inputs and unconditional cleanup. Current canonical disposable config excludes finance; private acceptance is not canonical CI discovery. Root owns global plans/proposals and any later test-lane amendment.
</boundaries>
<tasks>
<task type="auto">
<name>Assemble coherent source and reproduce binding defects</name>
<files>minion_hub/src/server/services/finance-statement-parser.ts, minion_hub/src/server/services/finance-statement-parser.test.ts, minion_hub/src/server/services/finance-statements.service.test.ts, minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts</files>
<action>Pin the full source/config baseline and 39 staged overlays. Copy parser SHA09e9799c5f7a16f0989df10034416d12d3017098f2b532a53ea8cdcb5c4b92f5 and its unchanged 60-case test SHA81380d728a0f58f3ff9596b40d39906d4c86af0099466bc18ad81361ad96b6d4. Preserve current durable helpers. Add focused regressions for new version storage/request identity, stale versions at zero/committed cursor, explicit retry, historical terminal/status handling, byte-versus-normalized hashes and safe header failures. Fix the owned native fixture cleanup with unconditional finally/rollback/close before any actual DB run; retain original errors. Present exact fixture/config/setup identities for root native admission.</action>
<verify><automated>Source/hash preservation and meaningful failing service regressions; reviewed native cleanup/config prerequisite.</automated></verify>
<done>Failures expose the actual integration contract on a coherent durable candidate.</done>
</task>
<task type="auto">
<name>Bind parser provenance inside durable ownership</name>
<files>minion_hub/src/server/services/finance-statements.service.ts, minion_hub/src/server/services/finance-statement-parser.ts, minion_hub/src/server/services/finance-statements.service.test.ts</files>
<action>Replace the private version constant with the exported parser version for new rows and request identity. Reject stale resumable work before file lookup and mutation; repeat locked version/source/revision checks before success and failure writes. Reject stale retry before enqueue and inside the locked callback. Historical done/failed results keep stored counts/version and avoid new-parser rejection recomputation. Always validate retrieved bytes against content_sha256; independently validate parse provenance against the exact decoded normalized text and actual version. Parser-provenance and stale request-identity conflicts stay outside parse-failure persistence. Preserve the existing owned failure-status path for corrupt stored bytes: those bytes never reach parsing or accounting-row writes, while a fenced failed-import status may be recorded. Map real header/limit error reasons to fixed content-free codes/messages, never arbitrary thrown text. Preserve same-version retry, transactional chunk/resume fencing and all existing domain behavior. Narrow only the parser integration TODO, retaining historical recovery and durable provenance limitations in source and root proposal.</action>
<verify><automated>Existing parser60 and service cases plus focused negative/regression cases; no skipped tests or changed financial policy.</automated></verify>
<done>Current imports and retries are correctly bound without modifying historical source identity or bookkeeping.</done>
</task>
<task type="auto">
<name>Qualify native effect boundaries and freeze</name>
<files>minion_hub/src/server/services/finance-statements.effect-ownership.sql.integration.test.ts, minion_hub/src/server/services/finance-statements.service.test.ts, minion_hub/src/server/services/finance-statements.service.ts, minion_hub/src/server/services/finance-statement-parser.ts, minion_hub/src/server/services/finance-statement-parser.test.ts</files>
<action>After fixture admission, execute the real existing runtime/effect/org-scope code and authored migrations in the marked private DB lane. Preserve takeover/cancel, ABA, CAS rollback, 501-row chunk resume, lost-handler-response, done race and RLS assertions. Prove stale version/header outcomes cannot write a replacement head, and retry admission races roll back. Distinguish import/domain no-effects assertions from legitimate runtime claim/failure bookkeeping. Verify raw CRLF/BOM bytes and parser normalization independently. Run scoped native types/format, source/helper/config preservation and exact five-file freeze. No copied PR246 Vitest configuration or global lane expansion. Root independently reviews/repeats before integration acceptance.</action>
<verify><automated>All admitted native DB cases pass, no skips/leaked connections/schemas, exact source/dependency/command receipts and source preservation.</automated></verify>
<done>Bounded service integration is locally qualified; native lane wiring, historical recovery and actual delivery remain explicit.</done>
</task>
</tasks>
<threat_model>
| Boundary | Threat | Required mitigation |
|---|---|---|
| Stored source to parser | Byte corruption or false provenance | Independent raw-byte and normalized decoded-text digests with version check before writes |
| Old job/retry to current import head | Stale cursor, duplicate accounting or historical relabel | Existing job/head/domain lock ordering and revision/effect fences plus locked parser-version rejection |
| Parser errors to persisted diagnostics | Statement/header disclosure | Fixed allowlisted error codes/reasons and secret-marker regressions |
| Test fixture to local database | Unintended schema or leaked handles | Root-reviewed marked loopback lane, exact migrations, unconditional cleanup and no production environment |
</threat_model>
<verification>New native evidence must match the five-file candidate. Preserve every old scoped receipt as historical; no source qualification implies live migration, canonical CI discovery or successful historical recovery.</verification>
<output>Root records 15-07-SUMMARY and independent evidence, with remaining recovery/provenance/lane gates paired in the existing proposal.</output>
