---
phase: 06-auth-extraction
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - .planning/phases/06-auth-extraction/06-05-PROD-DEPLOY-LOG.md
  - .planning/phases/06-auth-extraction/06-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - AUTH-04

must_haves:
  truths:
    - "Plan 06-04 returned a `GO` or `GO-WITH-CONFIG-CHANGE` decision before this plan starts"
    - "If `GO-WITH-CONFIG-CHANGE`: the remediation (env var or factory patch + republish + consumer bump) is applied and verified BEFORE the production merge step"
    - "Hub's `dev` branch is merged to `master` and deployed to Vercel production"
    - "Site's `master` branch is confirmed live on Vercel production with the 06-03 consumer changes"
    - "Both production deploys complete within a 15-minute window to minimize version-drift window"
    - "Post-deploy smoke: at least one real sign-in and one sign-up on production hub return HTTP 200 with session cookies"
    - "Existing user sessions are not force-invalidated (BETTER_AUTH_SECRET not rotated during cutover)"
    - "30-minute observation window shows no spike in auth-failure events in Vercel logs or bug reporter"
    - "`06-VERIFICATION.md` maps AUTH-01..04 to evidence artifacts"
    - "ROADMAP.md Phase 6 checkbox flipped to `[x]`; REQUIREMENTS.md AUTH-01..04 status flipped to Complete"
  artifacts:
    - path: ".planning/phases/06-auth-extraction/06-05-PROD-DEPLOY-LOG.md"
      provides: "Timestamped log of production deploys: hub SHA, site SHA, smoke outputs, observation window summary, any rollback actions"
      contains: "production"
    - path: ".planning/phases/06-auth-extraction/06-VERIFICATION.md"
      provides: "Phase closure document mapping AUTH-01..04 to evidence; ready for /gsd-verify-work"
      contains: "AUTH-01"
  key_links:
    - from: "hub production"
      to: "site production"
      via: "shared Infisical `minion-core` BETTER_AUTH_SECRET + shared Turso production DB"
      pattern: "BETTER_AUTH_SECRET"
    - from: "06-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "each AUTH-0N cited with evidence artifact"
      pattern: "AUTH-0"
---

<objective>
Coordinate the production cutover of hub + site onto `@minion-stack/auth` and close Phase 6 with a written VERIFICATION. No new code authored here — deploy coordination, production smoke testing, and phase-closure documentation only.

Purpose: Satisfy AUTH-04 production half and close Phase 6 per ROADMAP success criterion 5 ("Coordinated production deploy of hub + site passes smoke tests with no forced logouts").

Output: Production deploy log, phase VERIFICATION.md, ROADMAP + REQUIREMENTS + STATE updated to reflect completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-auth-extraction/06-CONTEXT.md
@.planning/phases/06-auth-extraction/06-RESEARCH.md
@.planning/phases/06-auth-extraction/06-01-SUMMARY.md
@.planning/phases/06-auth-extraction/06-02-SUMMARY.md
@.planning/phases/06-auth-extraction/06-03-SUMMARY.md
@.planning/phases/06-auth-extraction/06-04-SUMMARY.md
@.planning/phases/06-auth-extraction/06-04-STAGING-RESULTS.md

@.planning/phases/04-fold-minion-shared/04-VERIFICATION.md
@.planning/phases/05-db-extraction/05-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm go/no-go from Plan 06-04 staging results</name>
  <decision>Does staging evidence justify a production cutover right now?</decision>
  <context>
    Plan 06-04 recorded one of three outcomes in `06-04-STAGING-RESULTS.md`.

    - `GO`: proceed directly to Task 2.
    - `GO-WITH-CONFIG-CHANGE`: production needs something staging did not exercise — typically a `crossSubDomainCookies` factory param + Vercel env var addition, OR a factory 0.1.1 patch. Before Task 2:
      - If env-var-only: make the change in both Vercel project dashboards (hub + site production), trigger redeploy, re-run the cross-subdomain part of Plan 06-04 Task 2 Step 7 against the new staging deploy.
      - If factory patch required: create a new changeset for `packages/auth` (bump to 0.1.1), publish, bump hub + site `@minion-stack/auth` to `^0.1.1`, re-PR, re-merge to staging branches, repeat relevant Plan 06-04 Task 2 steps. Record the patch commit SHA here before proceeding.
    - `NO-GO`: halt. Plan 06-05 cannot proceed. Confirm the blocker is recorded in STATE.md and escalate to the user for replanning.
  </context>
  <options>
    <option id="proceed-go">
      <name>Proceed (staging is GO)</name>
      <pros>Production cutover can happen within 15 min</pros>
      <cons>None — staging already proved continuity</cons>
    </option>
    <option id="proceed-after-config">
      <name>Apply config change, then proceed (staging is GO-WITH-CONFIG-CHANGE)</name>
      <pros>Fixes the identified production-specific issue before live users encounter it</pros>
      <cons>Adds another iteration; factory patch requires a republish cycle</cons>
    </option>
    <option id="halt-no-go">
      <name>Halt (staging is NO-GO)</name>
      <pros>Prevents a production outage</pros>
      <cons>Phase stays open; requires replanning work to unblock</cons>
    </option>
  </options>
  <acceptance_criteria>
    - User has replied with one of: `proceed-go`, `proceed-after-config: <what was changed + commit SHA>`, or `halt-no-go: <reason>`
    - If `proceed-after-config`: remediation commit SHA is provided and staging re-verification is confirmed passed
  </acceptance_criteria>
  <resume-signal>Reply `proceed-go`, `proceed-after-config: <what changed + commit SHA>`, or `halt-no-go: <reason>`. If the second, attach the remediation commit SHA and confirm staging re-verification passed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute coordinated production cutover</name>
  <what-built>Both integration branches carry the @minion-stack/auth consumer changes: hub's `dev` branch (from the 06-02 PR merge) and site's `master` (from the 06-03 PR merge — site deploys from master, so it is already staged). Production deploy for hub is triggered by merging `dev` into `master` on minion_hub.</what-built>
  <how-to-verify>
    **Pre-deploy checklist** — confirm every item before cutting:
    - [ ] `npm view @minion-stack/auth version` → the intended production version (0.1.0 or 0.1.1 if Task 1 required a patch)
    - [ ] Infisical: `BETTER_AUTH_SECRET` in `minion-core` (prod env) is the same value hub + site have been using. Do NOT rotate during cutover — rotation forces all existing sessions invalid.
    - [ ] Infisical: `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` in `minion-core` (prod env) are set; Google Cloud Console has both hub prod URL and site prod URL in the OAuth redirect allowlist.
    - [ ] Vercel prod project for hub: `@minion-stack/auth` will auto-install from `minion_hub/package.json`; no manual npm install step needed.
    - [ ] Vercel prod project for site: same — auto-install from `minion_site/package.json`.

    **Hub production deploy:**
    1. On `NikolasP98/minion_hub`: open a PR merging `dev → master`. Title: `release: auth extraction (Phase 6)`. Wait for CI green, then merge.
    2. Vercel auto-deploys `master`. Wait for deployment to complete (Vercel dashboard or `gh api repos/NikolasP98/minion_hub/deployments --jq '.[0].sha'`).
    3. Confirm hub prod URL responds: `curl -s -o /dev/null -w '%{http_code}\n' https://<hub-prod>/api/auth/session` → 200 or 401.

    **Site production confirm:**
    Site's `master` branch was already updated when Plan 06-03's PR merged. Confirm the currently-live Vercel deploy matches the 06-03 commit:
    ```bash
    gh api repos/NikolasP98/minion-site/deployments --jq '.[0].sha'
    ```
    Compare to the 06-03 merge commit SHA. If not current, trigger a redeploy via Vercel dashboard.

    **Post-deploy smoke tests** (run within 5 min of both deploys confirmed live):
    ```bash
    HUB=https://<hub-prod>
    SITE=https://<site-prod>

    # New-user sanity: fresh sign-up works on hub
    EMAIL="phase6-prod-$(date +%s)@test.minion.example"
    curl -i -c prod-cookies.txt -X POST "$HUB/api/auth/sign-up/email" \
      -H 'content-type: application/json' \
      -d "{\"email\":\"$EMAIL\",\"password\":\"ProdPhase6!\",\"name\":\"Prod Smoke\"}"
    # Expect: HTTP 200, set-cookie

    # JWT audience check: factory normalization is live in production
    curl -s -b prod-cookies.txt "$HUB/api/auth/token" | jq -r '.token' \
      | cut -d. -f2 | base64 -d 2>/dev/null | jq '{aud, iss}'
    # Expect: aud="openclaw-gateway"

    # Clean up test user afterward:
    # Run against hub's DB: DELETE FROM user WHERE email LIKE 'phase6-prod-%'
    # (use bun run db:studio or a direct SQL session against Turso)
    ```

    **30-minute observation window:**
    - Vercel function logs for hub + site — watch for `env.BETTER_AUTH_SECRET is undefined`, `Cannot find module '@minion-stack/auth'`, or `drizzleAdapter` errors.
    - PostHog `session_started` event count — compare the 30-min post-deploy window against the same-weekday prior-week window. A drop >20% is a red flag (possible forced-logout wave).
    - Hub bug reporter — watch for incoming auth-related tickets.

    **Rollback procedure (if smoke test fails):**
    - Hub: revert the `dev → master` merge via `gh pr revert <pr-number>` or Vercel "promote previous deployment".
    - Site: revert the 06-03 merge commit on minion-site master and trigger Vercel redeploy.
    - `@minion-stack/auth@0.1.0` stays on npm — never unpublish a released version.
    - Record rollback in `06-05-PROD-DEPLOY-LOG.md` + STATE.md and halt phase for replanning.
  </how-to-verify>
  <acceptance_criteria>
    - Hub production Vercel deploy SHA matches the `dev → master` merge commit
    - Site production Vercel deploy SHA matches the 06-03 merge commit (or 06-03 + 0.1.1 patch if GO-WITH-CONFIG-CHANGE)
    - New sign-up smoke test returns HTTP 200 + set-cookie on production hub
    - JWT audience claim is `openclaw-gateway` on production hub
    - 30-minute observation window shows no increase in auth-failure events
  </acceptance_criteria>
  <resume-signal>Reply `prod-cutover-complete` with hub + site prod deploy SHAs + smoke-test outputs (tokens redacted to first 8 chars), OR `prod-cutover-rolled-back: <reason>` if a smoke test failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write VERIFICATION.md and close Phase 6 in planning docs</name>
  <files>
    .planning/phases/06-auth-extraction/06-05-PROD-DEPLOY-LOG.md,
    .planning/phases/06-auth-extraction/06-VERIFICATION.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (current state — find Phase 6 section to update)
    - .planning/REQUIREMENTS.md (current state — find AUTH-01..04 traceability rows)
    - .planning/STATE.md (current state — update progress counters)
    - .planning/phases/04-fold-minion-shared/04-VERIFICATION.md (template reference)
    - .planning/phases/05-db-extraction/05-VERIFICATION.md (template reference)
    - All 06-0N-SUMMARY.md files (evidence pointers for VERIFICATION.md)
    - Smoke-test outputs provided by user in Task 2 reply
  </read_first>
  <action>
    1. Create `.planning/phases/06-auth-extraction/06-05-PROD-DEPLOY-LOG.md`:
       ```markdown
       # Phase 6 — Production Deploy Log

       **Date:** <ISO timestamp>
       **Plan:** 06-05

       ## Pre-deploy Checklist
       - [ ] @minion-stack/auth version confirmed: <version>
       - [ ] BETTER_AUTH_SECRET parity confirmed (NOT rotated): <how verified>
       - [ ] Google OAuth redirect URIs confirmed for hub + site prod URLs
       - [ ] Vercel auto-install confirmed for hub + site

       ## Hub Deploy
       - PR: dev → master on NikolasP98/minion_hub
       - Merge SHA: <sha>
       - Vercel deploy start: <timestamp>
       - Vercel deploy complete: <timestamp>
       - Smoke: /api/auth/session → <http_code>

       ## Site Deploy Confirmation
       - 06-03 merge SHA confirmed live: <sha>
       - Vercel deploy confirmed: <timestamp>
       - Smoke: /api/auth/session → <http_code>

       ## Post-Deploy Smoke Tests
       - Sign-up smoke: HTTP <code> + set-cookie <yes/no>
       - JWT audience: <aud value>
       - Rollback executed: no

       ## 30-Minute Observation Window
       - Vercel log errors: <none / list>
       - PostHog session_started delta vs prior week: <delta%>
       - Bug reporter incidents: <none / list>

       ## Outcome
       Production cutover: SUCCESS / ROLLED BACK
       ```

    2. Create `.planning/phases/06-auth-extraction/06-VERIFICATION.md`:
       ```markdown
       # Phase 6 — Auth Extraction — VERIFICATION

       **Closed:** <date>
       **Outcome:** Complete

       ## Requirements

       | ID | Status | Evidence |
       |----|--------|----------|
       | AUTH-01 | Complete | packages/auth/src/factory.ts (`createAuth` export + 12 tests); 06-01-SUMMARY.md |
       | AUTH-02 | Complete | `npm view @minion-stack/auth version` → <version>; 06-01-SUMMARY.md (publish commit SHA) |
       | AUTH-03 | Complete | minion_hub + minion-site both import createAuth from @minion-stack/auth; PRs in 06-02-SUMMARY.md + 06-03-SUMMARY.md |
       | AUTH-04 | Complete | 06-04-STAGING-RESULTS.md (staging continuity GO) + 06-05-PROD-DEPLOY-LOG.md (prod cutover SUCCESS) |

       ## Success Criteria (from ROADMAP Phase 6)

       1. packages/auth exports createAuth() — YES (factory.ts, AUTH-01)
       2. @minion-stack/auth published — YES (npm view, AUTH-02)
       3. Both hub and site call createAuth() with identical secret/provider config — YES (grep confirms both consumers, AUTH-03)
       4. Shared-session continuity works staging — YES (06-04-STAGING-RESULTS.md, AUTH-04 staging half)
       5. Coordinated prod deploy, no forced logouts — YES (06-05-PROD-DEPLOY-LOG.md, 30-min observation window, AUTH-04 prod half)

       ## Deviations from Plan

       <list any, or "None">

       ## Open Follow-ups

       - Better Auth upgrade (1.4.19 → 1.6.x) — deferred to its own phase per RESEARCH State of the Art
       - Hub's gateway-jwt.service.ts custom JWT signer stays in hub per RESEARCH Assumption A6
       - Desktop-mode session persistence stays in hub per Assumption A5
       - crossSubDomainCookies config: <if GO-WITH-CONFIG-CHANGE, record what was changed; if GO, record "N/A — not required for current deployment topology">
       ```

    3. Update `.planning/ROADMAP.md`:
       - Flip Phase 6 entry from `[ ]` to `[x]` with completion date: `[x] **Phase 6: Auth Extraction** — ... (completed <date>)`
       - Flip all five plan checkboxes under Phase 6 to `[x]`
       - Update the Progress table row for Phase 6: `5/5 | Complete | <date>`

    4. Update `.planning/REQUIREMENTS.md` traceability table:
       - AUTH-01: `Pending` → `Complete`
       - AUTH-02: `Pending` → `Complete`
       - AUTH-03: `Pending` → `Complete`
       - AUTH-04: `Pending` → `Complete`
       - Also flip the `[ ]` checkboxes in the Auth Extraction section to `[x]` for AUTH-01..04

    5. Update `.planning/STATE.md`:
       - Increment `completed_phases` count (5 → 6)
       - Increment `completed_plans` count (+5 for plans 06-01 through 06-05)
       - Update `stopped_at` to `Completed 06-05-PLAN.md`
       - Update `last_activity` to today's date
       - Add a decision entry: `[Phase 06]: Phase 6 complete — createAuth() factory live at @minion-stack/auth@<version>; hub + site migrated; session continuity verified staging + prod`

    6. Commit everything:
       ```
       docs(phase-06): complete — auth extraction live, AUTH-01..04 satisfied

       - 06-VERIFICATION.md: AUTH-01..04 all Complete with evidence pointers
       - 06-05-PROD-DEPLOY-LOG.md: production deploy log (SHAs, smoke tests, 30-min observation)
       - ROADMAP.md: Phase 6 marked complete; 5/5 plans done
       - REQUIREMENTS.md: AUTH-01..04 traceability flipped to Complete
       - STATE.md: phase closure + progress bumped

       Requirements: AUTH-04 (production half)
       ```
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/06-auth-extraction/06-VERIFICATION.md`
    - `grep -q "AUTH-01" .planning/phases/06-auth-extraction/06-VERIFICATION.md`
    - `grep -q "AUTH-04" .planning/phases/06-auth-extraction/06-VERIFICATION.md`
    - `grep -q "\[x\].*Phase 6" .planning/ROADMAP.md` — Phase 6 checkbox flipped
    - `grep -qE "AUTH-01.*Complete" .planning/REQUIREMENTS.md`
    - `grep -qE "AUTH-04.*Complete" .planning/REQUIREMENTS.md`
    - `grep -q "Completed 06-05-PLAN.md" .planning/STATE.md`
    - `git log -1 --pretty=%B | grep -q "AUTH-04"`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-auth-extraction/06-VERIFICATION.md && grep -q "AUTH-01" .planning/phases/06-auth-extraction/06-VERIFICATION.md && grep -q "AUTH-04" .planning/phases/06-auth-extraction/06-VERIFICATION.md && grep -qE "\[x\].*Phase 6" .planning/ROADMAP.md && grep -q "Completed 06-05-PLAN.md" .planning/STATE.md</automated>
  </verify>
  <done>06-VERIFICATION.md committed; ROADMAP Phase 6 marked complete; REQUIREMENTS AUTH-01..04 flipped to Complete; STATE.md updated. Phase 6 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production Vercel env to production Infisical | BETTER_AUTH_SECRET rotation during cutover would force-logout all users — explicitly blocked by pre-deploy checklist |
| existing user sessions to new deploy | Session cookies minted pre-cutover must still validate post-cutover — guaranteed by same secret + same DB (no rotation, no migration) |
| OAuth redirects to Google | Google Cloud Console OAuth redirect URIs must include both hub and site prod URLs before cutover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Denial of Service (forced global logout) | BETTER_AUTH_SECRET rotation | mitigate | Task 2 pre-checklist item explicitly forbids rotation during cutover; 30-min observation window catches any inadvertent session drop |
| T-06-21 | Information Disclosure (prod tokens in deploy log) | 06-05-PROD-DEPLOY-LOG.md | mitigate | Task 3 redacts cookies + JWTs to first 8 chars; never commits full tokens |
| T-06-22 | Tampering (deploy SHA mismatch between hub and site) | coordinated cutover window | mitigate | Task 2 requires both deploys complete inside 15 min; pre-deploy checklist confirms target SHAs |
| T-06-23 | Spoofing (malicious package at install time) | @minion-stack/auth supply chain | accept | npm package-lock.json integrity hashes catch tampering; 2FA on @minion-stack publish account prevents unauthorized republish |
| T-06-24 | Elevation of Privilege (cookie bleed in production) | crossSubDomainCookies.domain at prod | mitigate | Task 1 ensures any GO-WITH-CONFIG-CHANGE remediation is applied before production cutover; domain set to narrowest shared root |
</threat_model>

<verification>
- `06-05-PROD-DEPLOY-LOG.md` exists with deploy SHAs + smoke-test evidence + 30-min observation summary
- `06-VERIFICATION.md` exists with all 4 AUTH-0N requirements mapped to evidence
- ROADMAP.md Phase 6 shows `[x]` with all 5 plan checkboxes `[x]`
- REQUIREMENTS.md AUTH-01..04 traceability rows show `Complete`
- STATE.md `stopped_at: Completed 06-05-PLAN.md`, `completed_phases` incremented
- Commit ties all phase-closure artifacts together
- Production hub + site both running `createAuth()` from `@minion-stack/auth` with no user-visible auth regressions
</verification>

<success_criteria>
- Hub + site production run `createAuth()` from `@minion-stack/auth` (AUTH-03 live in production)
- No users forced to re-login during cutover window (30-min observation metric passes, AUTH-04)
- Fresh sign-ups and existing-user session resolution both work in production
- All four Phase 6 requirements (AUTH-01..04) have evidence pointers in `06-VERIFICATION.md`
- ROADMAP Phase 6 marked complete; REQUIREMENTS updated; STATE.md reflects closure
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-extraction/06-05-SUMMARY.md` documenting:
- Production deploy SHAs + timestamps
- Smoke test outcomes (redacted)
- Observation-window metrics (PostHog delta, any incidents)
- Final phase status
- Any follow-ups added to STATE.md or v2 requirements (Better Auth upgrade, crossSubDomainCookies long-term config, etc.)
</output>
