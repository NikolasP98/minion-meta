# Remaining UI plans after native motion packet

Root-adopted bounded follow-up scopes,2026-09-12. Baseline: Hub packet bf8bbf532abbdc3e3d6edf3a227b667cd4d7206e, integrated by PR270 at77445c01ec38657c767d6f011e77148be3cc4ccf. These plans are not completed implementation. Root must assign exclusive file ownership and serialize locale/shared component edits before execution. They close parts of13-04Task2; authenticated13-03 requires its selected non-production identity/database separately.

## Plan UI-K1 — Workshop keyboard relationship, element and camera tasks

Owner files: `src/lib/components/workshop/WorkshopCanvas.svelte`; `_workshop-canvas/WorkshopAgentControls.svelte`; new `_workshop-canvas/WorkshopElementControls.svelte`; existing relationship prompt component found at its import; a shared action helper only if needed to invoke exactly the same pointer/keyboard handler; fixture/spec files under `tests/fixtures/workshop-accessibility/` and `tests/e2e/ui-audit/workshop-accessibility.spec.ts`. New copy only in `messages/en.json` and `messages/es.json`, serialized with root. No server routes, physics engine, scheduler, finance/calendar, renderer replacement, package or lock changes.

1. Inventory actual pointer actions at WorkshopCanvas `handleElementContextAction` (~1460), `handleRelationshipCreate` (~1535), pointer drag/drop and camera handlers. Record view-mode differences (pixel tile positions versus world coordinates) and existing undo/autosave semantics before edits. Existing agent select/task controls and action-bar behavior/delete/chat handlers are retained, not duplicated.
2. Add native keyboard-operable source/target selectors and label entry to invoke the existing relationship-create path. Reject missing/deleted/self endpoints before applying effect. Expose existing relationship removal action with the same undo/physics/rope/autosave effects; if no single handler exists, extract only that action and reuse it from both input paths.
3. Add element list with explicit open/remove/move actions. Refactor `handleElementContextAction` to accept explicit instance identity (currently it requires a pointer-created `elementContextMenu`). Movement uses existing world/tile coordinate conversion, validation, renderer/physics update and save checkpoint. Open uses existing overlay and focus restoration; removal uses same state/renderer/physics cleanup.
4. Expose camera pan/reset/zoom via labelled keyboard buttons using the same clamp/transform logic as pointer/wheel; keep key handling scoped to the canvas toolbar so text inputs retain their keys. No global shortcut overhaul. Distinguish operations that only exist in a particular view mode rather than offering a broken equivalent.
5. Compile actual Workshop composition in an owned loopback fixture with deterministic agent/element/relationship state and explicit synthetic transport/save boundary. Browser-harness owns one private browser; native three-engine tests at390/1280 exercise each task with keyboard and pointer, compare exact resulting state/undo history/effect call count, test disconnected restrictions, missing endpoints, focus/Escape, and both supported coordinate modes. No production save or live harness call.

Acceptance: Every exposed pointer task has a documented actual keyboard path or an explicit unresolved row. Correct same entity IDs/coordinates, one action per activation, no forbidden effect, current-state undo/autosave, no page errors, correct focus. Existing pixel lifecycle/Pixi-motion tests and lint:design/lint:tokens pass. A real authenticated Workshop/save acceptance remains a separate provider gate. Write exact source TODO and canonical proposal for any unqualified action; do not mark all13-04 complete from a toolbar alone.

## Plan UI-C1 — Reliability chart consumer task alternatives

Owner files: `src/lib/components/charts/Chart.svelte` and its existing accessibility tests only when an actual consumer demonstrates missing functionality; `src/lib/components/reliability/AgentLlmAnalytics.svelte`; `src/lib/components/reliability/ActivityLogTable.svelte`; `src/routes/(app)/reliability/+page.svelte`; a new small reliability/chart fixture and e2e spec. Existing shared design tokens and native controls only. No gateway RPC/protocol, telemetry ingestion, SQL, application auth or other route rewrites. Locale copy as a root-serialized separate change.

1. Freeze fixture data and map actual event effects: reliability `handleTopEventClick`, `handleSeverityClick`, `handleSankeyClick`, `handleTimelineClick`; AgentLlmAnalytics `onBarClick(model|channel|provider|source|agent)`; ActivityLogTable `onTimelineClick`. Record how each chart changes the filter, selected event or detail panel. Validate actual legend behavior and current empty/error states.
2. Reuse Chart's existing line/bar semantic table and callbacks for eligible Cartesian data. Test actual consumer handlers, not just Chart emitting a callback. For Sankey, supply an explicit labelled node/link list/table with source, target and value and equivalent select/filter action; do not coerce its graph into the generic Cartesian table. Dataset/encode, tuples and mixed axes stay explicit unsupported inputs until a real selected consumer needs a specific adapter.
3. Expose keyboard legend toggles wired to the same native ECharts action and consumer filter state where applicable. Keep table/controls visible or discoverable without visual-canvas interaction. Preserve meaningful units/category labels and show no misleading rows for invalid/absent data.
4. Build actual selected consumers against deterministic transport/data. Three-engine browser tests at390/1280 select every supported dimension and Sankey node/link, toggle legend, inspect the changed real filter/detail state and compare pointer results. Include empty data, duplicate labels with distinct identities, repeated keyboard activation, long translated labels, collapsed/hidden legend, and initial/live reduced motion. Assert native ECharts option/model state and semantic state rather than CSS animation counts.

Acceptance: concrete drilldown/filter tasks work from keyboard and pointer with identical identity/effect semantics; correct summaries/labels/units; no false generic alternative for unsupported series; no errors/overflow; native Chart and design/token tests pass. Native assistive-technology and actual authenticated Reliability/provider data remain separate qualification. Unused/unexercised charts are listed explicitly; no claim of platform-wide chart certification.
