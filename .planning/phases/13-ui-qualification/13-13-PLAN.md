---
phase: 13-ui-qualification
plan: "13"
type: execute
wave: 5
depends_on: ["13-02"]
files_modified:
  - "minion_hub/src/lib/components/scheduling/calendar/SchedulingCalendar.svelte"
  - "minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.test.ts"
  - "minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.ts"
  - "minion_hub/src/lib/components/scheduling/calendar/types.ts"
  - "minion_hub/src/server/scheduling/load-calendar-events.ts"
  - "minion_hub/src/server/scheduling/tz.test.ts"
  - "minion_hub/src/server/scheduling/tz.ts"
autonomous: true
requirements: ["UI-03", "UI-04"]
readiness: "Reconcile an inherited, already repaired and locally reviewed Calendar candidate into exact GSD delivery ownership"
must_haves:
  truths:
    - "Calendar preserves original instants while presenting appointment wall time correctly across viewer timezone and DST boundaries."
    - "The seven inherited files have explicit delivery ownership, native regression evidence and remaining rendering/query gates."
  artifacts:
    - path: "minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.ts"
      provides: "UTC-neutral calendar axis and lossless original-instant projection"
    - path: "minion_hub/src/server/scheduling/tz.test.ts"
      provides: "Native timezone regression evidence"
  key_links:
    - from: "minion_hub/src/lib/components/scheduling/calendar/SchedulingCalendar.svelte"
      to: "minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.ts"
      via: "Viewer wall-time projection and original instant callback conversion"
---

<objective>Reconcile and independently qualify the inherited Calendar timezone repair as an exact seven-file delivery slice.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/13-ui-qualification/13-02-PLAN.md
@.planning/phases/13-ui-qualification/13-10-SUMMARY.md
@proposals/2026-09-10-hub-calendar-utc-offset-dropped.md
@minion_hub/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
</context>
<boundaries>The user-authorized inherited Calendar repair was already implemented and locally checked before this exact GSD packet existed. This plan does not backdate planning or claim new implementation. Root's new release ownership check found that these seven files lacked an exact PLAN mapping. Root now admits them for verified delivery. Preserve source in /home/nikolas/.cache/claude-tmp/hub-cal-tz and the combined ordinary UI snapshot; base is fetched Hub commit 1df0a9216ad3f7f85d989eb59cfccb779d9f7285. No active old-branch overwrite, branch/index/worktree operation, schema/business/provider mutation or deployment. Any new behavioral repair needs a reviewed amendment. Native tests use synthetic data, private caches, bounded workers and denied network.</boundaries>
<tasks>
<task type="auto">
<name>Reconcile inherited source and behavior</name>
<files>minion_hub/src/lib/components/scheduling/calendar/SchedulingCalendar.svelte, minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.test.ts, minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.ts, minion_hub/src/lib/components/scheduling/calendar/types.ts, minion_hub/src/server/scheduling/load-calendar-events.ts, minion_hub/src/server/scheduling/tz.test.ts, minion_hub/src/server/scheduling/tz.ts</files>
<action>Verify the seven current hashes against calendar-manifest.json and preserved before-images. Read the actual installed Event Calendar 5.12.2 conversion, which uses today's timezone offset. Confirm the repaired UTC-neutral axis, viewer wall-time projection and original timestamp callbacks preserve milliseconds and repeated-hour identity. Retain earlier red cases as historical executed evidence.</action>
<verify><automated>Exact source/runtime manifest comparison and independent source review; no remapping based only on filenames.</automated></verify>
<done>Inherited source and its prior authorization/evidence are explicit without pretending this plan preceded the implementation.</done>
</task>
<task type="auto">
<name>Qualify the combined native and browser behavior</name>
<files>minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.test.ts, minion_hub/src/server/scheduling/tz.test.ts</files>
<action>Run the existing 27 native cases on the combined source in a recorded timezone. Retain prior UTC/New York/Kathmandu evidence and actual browser placement at 08:00 in Lima/New York/Kathmandu. Confirm latest mobile toolbar and mandatory component journeys still include this exact source and that the combined full Svelte check is green. Separate test setup failures from product failures.</action>
<verify><automated>All native cases execute without skips; exact browser artifact/source and full-check receipts remain linked.</automated></verify>
<done>The selected delivery source has current native evidence and the existing actual-component/browser check identity.</done>
</task>
<task type="auto">
<name>Record bounded delivery and remaining gates</name>
<files>minion_hub/src/lib/components/scheduling/calendar/SchedulingCalendar.svelte, minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.test.ts, minion_hub/src/lib/components/scheduling/calendar/calendar.svelte.ts, minion_hub/src/lib/components/scheduling/calendar/types.ts, minion_hub/src/server/scheduling/load-calendar-events.ts, minion_hub/src/server/scheduling/tz.test.ts, minion_hub/src/server/scheduling/tz.ts</files>
<action>Include only the seven owned files in the reviewed UI packet. Preserve the source TODO and matching proposal for fold-spanning event rendering, overlap queries, initial day/timezone selection and other calendar consumers. None is silently closed by ordinary DST tests. Keep physical-device, actual authorization and deployment separate.</action>
<verify><automated>Root release tooling accepts exact plan ownership and rejects source drift/unrelated files; paired handoff remains present.</automated></verify>
<done>Reviewable inherited repair is mapped to GSD without overstating broader Calendar or release acceptance.</done>
</task>
</tasks>
<verification>Source ownership reconciliation is not a new behavior fix. Actual test/source identities and open boundaries govern acceptance.</verification>
<success_criteria>The seven inherited files are reviewable, natively qualified and correctly scoped for the UI packet.</success_criteria>
<output>Root records 13-13-SUMMARY.md and current packet evidence.</output>
