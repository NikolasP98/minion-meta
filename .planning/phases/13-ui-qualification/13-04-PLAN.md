---
phase: 13-ui-qualification
plan: "04"
type: execute
wave: 4
depends_on: ["13-03","13-01"]
files_modified:
  - "minion_hub/src/lib/components/charts/Chart.svelte"
  - "minion_hub/src/lib/components/charts/chart-accessibility.test.ts"
  - "minion_hub/src/lib/components/crm/CrmSentimentTrend.svelte"
  - "minion_hub/tests/e2e/ui-audit/canvas-accessibility.spec.ts"
  - ".planning/phases/13-ui-qualification/13-CANVAS-COVERAGE.md"
  - "minion_hub/messages/en.json"
  - "minion_hub/messages/es.json"
  - "minion_hub/tests/fixtures/chart-accessibility/Fixture.svelte"
  - "minion_hub/tests/fixtures/chart-accessibility/build.mjs"
  - "minion_hub/tests/fixtures/chart-accessibility/fixture.css"
  - "minion_hub/tests/fixtures/chart-accessibility/main.js"
  - "minion_hub/tests/fixtures/chart-accessibility/messages.js"
autonomous: true
requirements: ["UI-06","UI-03"]
readiness: "Shared chart slice executable; whole-canvas closure requires evidence-driven consumer gap plans."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Reduced-motion disables real chart transitions; actual chart values are accessible without canvas; existing callbacks still work."
    - "Coverage lists real engine checks and task equivalents; no missing consumer is hidden by passing shared component tests."
  artifacts:
    - path: "minion_hub/src/lib/components/charts/Chart.svelte"
      provides: "Add reusable chart motion and accessible data contract"
    - path: "minion_hub/tests/e2e/ui-audit/canvas-accessibility.spec.ts"
      provides: "Audit canvas motion and issue exact consumer completion plans"
  key_links:
    - from: "minion_hub/src/lib/components/charts/Chart.svelte"
      to: "minion_hub/src/lib/components/charts/EChartsSparkline.svelte"
      via: "Read-first contract and behavior verification for add reusable chart motion and accessible data contract"
    - from: "minion_hub/tests/e2e/ui-audit/canvas-accessibility.spec.ts"
      to: "minion_hub/src/lib/workshop/pixel/game-loop.ts"
      via: "Read-first contract and behavior verification for audit canvas motion and issue exact consumer completion plans"
---

<objective>
Qualify reduced motion and nonvisual chart/canvas access.
Purpose: UI-06, UI-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/13-ui-qualification/13-CONTEXT.md
@.claude/skills/ui-design-governance/SKILL.md
@minion_hub/src/lib/components/charts/EChartsSparkline.svelte
@minion_hub/src/lib/components/charts/Chart.svelte
@minion_hub/src/lib/workshop/pixel/game-loop.ts
@minion_hub/src/lib/workshop/habbo-renderer.ts
@minion_hub/tests/e2e/ui-audit/certification.ts

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Add reusable chart motion and accessible data contract</name>
  <files>minion_hub/src/lib/components/charts/Chart.svelte, minion_hub/src/lib/components/charts/chart-accessibility.test.ts, minion_hub/src/lib/components/crm/CrmSentimentTrend.svelte</files>
  <read_first>minion_hub/src/lib/components/charts/EChartsSparkline.svelte, minion_hub/src/lib/components/charts/Chart.svelte</read_first>
  <behavior>Reduced-motion disables real chart transitions; actual chart values are accessible without canvas; existing callbacks still work. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Add a typed text/table alternative contract populated from actual series values, and honor prefers-reduced-motion initially and when it changes. Do not mutate caller-owned ECharts options. Preserve legend/item callbacks and semantic tokens; add meaningful names and keyboard access for actions. Wire the sentiment consumer completely as the first consumer and inventory all Chart callers; required consumers without alternatives remain explicit child plans and block UI-06.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/components/charts/chart-accessibility.test.ts &amp;&amp; bun run lint:design &amp;&amp; bun run lint:tokens</automated></verify>
  <done>Reduced-motion disables real chart transitions; actual chart values are accessible without canvas; existing callbacks still work.</done>
</task>

<task type="auto">
  <name>Task 2: Audit canvas motion and issue exact consumer completion plans</name>
  <files>minion_hub/tests/e2e/ui-audit/canvas-accessibility.spec.ts, .planning/phases/13-ui-qualification/13-CANVAS-COVERAGE.md</files>
  <read_first>minion_hub/src/lib/workshop/pixel/game-loop.ts, minion_hub/src/lib/workshop/habbo-renderer.ts, minion_hub/tests/e2e/ui-audit/certification.ts</read_first>
  <behavior>Coverage lists real engine checks and task equivalents; no missing consumer is hidden by passing shared component tests. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Exercise Workshop/Pixi/physics and chart consumers with reduced-motion and keyboard fixtures. Observe actual animation engine state and task completion, not document.getAnimations alone. Inventory meaningful nonvisual operations, equivalent list/controls and missing alternatives by exact component. Generate bounded follow-up plans for failed consumers before declaring UI-06 complete. Human assistive-technology evidence is requested only after runnable automated fixtures and specific verification steps exist.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/@playwright/test/cli.js test tests/e2e/ui-audit/canvas-accessibility.spec.ts</automated></verify>
  <done>Coverage lists real engine checks and task equivalents; no missing consumer is hidden by passing shared component tests.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Untrusted data / rendered UI / user action | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-13-04-01 | I/D | Unsafe HTML or inaccessible essential action | mitigate | Escaped text, native focus, motion alternatives and actual headless journey tests. |
| T-13-04-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/13-ui-qualification/13-04-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>


<scope_amendment date="2026-09-11">Root admits the preserved Claude fixture/source files named in frontmatter for current qualification.13-05 now owns subsequent CallControls/Home/mobile-helper repairs;13-04 owns current Chart and mounted-test corrections. Existing staged blobs remain preserved; their existence alone does not establish acceptance.</scope_amendment>

<scope_amendment date="2026-09-12">User prioritized UI/accessibility after jobs/stock. Root admits bounded pixel-office initial/live reduced-motion handling, existing-handler keyboard task controls and TaskPromptDialog native focus/Escape semantics, with credential-free real-browser fixtures. Next bounded engine slice owns src/lib/workshop/motion-preference.ts, agent-sprite.ts, habbo-renderer.ts and habbo-element-sprite.ts animation guards/reset/cleanup, plus real Pixi/Rapier fixture and focused tests. Simulation, physics scheduling and camera behavior remain read-only; tests must prove actual physics continues. Relationship/element editing and real authenticated acceptance remain open until separately evidenced. Missing navigation/critical fixture entrypoints may be restored portably without overwriting ERP behavior.</scope_amendment>
