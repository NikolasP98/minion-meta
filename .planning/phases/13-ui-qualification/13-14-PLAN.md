---
phase: 13-ui-qualification
plan: "14"
type: execute
wave: 7
depends_on: ["13-08", "13-09", "13-11", "13-12", "14-18"]
files_modified:
  - "minion_site/scripts/qc/compile-ui-messages.mjs"
  - "minion_site/scripts/qc/compile-ui-messages.test.mjs"
  - "minion_site/scripts/qc/ui-fixtures.mjs"
  - "minion_site/scripts/qc/ui-fixtures.test.mjs"
  - "minion_site/playwright.ui-fixtures.config.ts"
  - "minion_site/tests/e2e/member-data-parity.spec.ts"
  - "minion_site/.github/workflows/ci.yml"
  - "minion_site/tests/fixtures/member-data-parity/README.md"
autonomous: true
requirements: ["UI-05", "UI-07", "SDK-01", "SDK-02"]
readiness: "Source implementation may prepare now; final candidate copy and execution require the frozen qualified 14-18 service/archive transaction"
must_haves:
  truths:
    - "One native local entrypoint executes the complete credential-free Site UI and transport matrix without accepting skipped or selected subsets."
    - "CI invokes the same portable entrypoint and preserves all existing gates."
    - "Generated translations, actual fonts, complete SDK and current source identity remain reproducible."
  artifacts:
    - path: "minion_site/scripts/qc/ui-fixtures.mjs"
      provides: "Fixed native test and fixture orchestration with complete result verification"
    - path: "minion_site/.github/workflows/ci.yml"
      provides: "Isolated additive UI fixture job"
  key_links:
    - from: "minion_site/.github/workflows/ci.yml"
      to: "minion_site/scripts/qc/ui-fixtures.mjs"
      via: "Same native local acceptance entrypoint"
---

<objective>Make the qualified Site UI and real local gateway connection tests routine and portable in CI.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/14-sdk-transport/14-18-PLAN.md
@.planning/phases/13-ui-qualification/13-11-SUMMARY.md
@.planning/phases/13-ui-qualification/13-12-SUMMARY.md
@minion_site/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
Read-only decision: /home/nikolas/.cache/minion-qc/ui-ci-review/REPORT.md, SHA256 534278963dac1b5400cc306065558be323191eb160bd341c1f45af98f1763980.
</context>
<boundaries>
Own exactly eight paths in a new ordinary private complete Site candidate derived from frozen 14-18. Preserve all earlier candidates and active checkout/index. No dependency/version/lock changes, product component/service changes, production Paraglide migration, branch/worktree/commit/push/deploy, provider/auth/DB mutation or host package install. Reuse installed native Paraglide, Vitest and Playwright; do not build a generic test framework, arbitrary-command executor or replacement reporter when native behavior suffices. Any new source seam needs root-reviewed exact amendment. Root owns global plans/proposals.

Do not skip the current 289 global format warnings or weaken the existing CI job. Add only a separate ui-fixtures job and extend its existing CI event branches to include dev alongside main/master, reflecting the documented development branch; do not change deploy/release triggers or branch protection. Use a stock Ubuntu24.04 job, read-only contents, checkout persist-credentials:false and recorded exact Node/Bun versions. Preparation may use native frozen Bun and installed Playwright browser/system-package setup on the disposable CI worker. Local execution never installs OS packages. Runtime is credential-free: private HOME/cache/TMPDIR, exact allowlisted environment, no .env loading, no production URLs, and existing owned loopback servers only. Serial builds, one browser worker, retries zero, explicit deadlines and fresh nonshared artifacts. Resource limits are test execution budgets, not product SLOs. Keep source/runtime/archive identity distinct.
</boundaries>
<tasks>
<task type="auto">
<name>Provide portable native translation generation</name>
<files>minion_site/scripts/qc/compile-ui-messages.mjs, minion_site/scripts/qc/compile-ui-messages.test.mjs</files>
<action>Adapt the already qualified offline generator to repository-relative native dependency resolution. Resolve the existing Paraglide message-format plugin through its owning package, validate EN/ES source paths and confined output, opt out of telemetry and reject network access before compiler import. Generate actual Paraglide modules, never handwritten translations. Test real EN/ES roundtrip and unsafe/missing/output-escape controls. Do not claim CDN lint-plugin execution or alter production settings.</action>
<verify><automated>Native Node tests using installed actual Paraglide and its declared plugin, including a fresh real compilation.</automated></verify>
<done>Fresh candidates generate real translations without laptop-specific paths or CDN access.</done>
</task>
<task type="auto">
<name>Run the complete native and browser matrix</name>
<files>minion_site/scripts/qc/ui-fixtures.mjs, minion_site/scripts/qc/ui-fixtures.test.mjs, minion_site/playwright.ui-fixtures.config.ts, minion_site/tests/e2e/member-data-parity.spec.ts</files>
<action>Use fixed native CLI arguments and existing builders/specs in dependency order. The eighth-file amendment adds uniform per-case runtime, page-error and blocked-network attachments to the existing member-data spec; preserve every behavior/font assertion and enforce these observations in all seven cases. This closes a proven missing evidence seam, not permission to rewrite its tests. Run the 56 ordinary Vitest cases and separate 22 service cases, retaining actual Svelte client rune compilation proof under hidden paths. Build data/chat/gateway fixtures once each. Require all three engines and the exact approved titles: 7 data, 12 chat and 6 gateway per engine (75 browser cases). Capture native JSON reports and artifacts; validate exact identities, no skips/retries/flaky/subsets, required runtime/font/network evidence and immutable artifact hashes. Reject baseline environment switches, selectors, missing setup and ambient secret canaries. Prefer native reporter and exit semantics; any small additional verifier must be tested against a real native failed and passed reporter fixture. Use private cache paths before runner import, finite waits and owned-server cleanup. Never launch an application DB/auth server.</action>
<verify><automated>Native adversarial runner tests, all 78 native app/service cases and all 75 mandatory browser cases with actual fonts and local HTTP/WebSocket.</automated></verify>
<done>One portable command fails incomplete evidence and executes every mandatory selected behavior on the frozen candidate.</done>
</task>
<task type="auto">
<name>Wire additive CI and document the command</name>
<files>minion_site/.github/workflows/ci.yml, minion_site/tests/fixtures/member-data-parity/README.md</files>
<action>Add the separate pinned native-tool UI job with bounded preparation/execution and artifacts. Call the identical local entrypoint. Preserve original check/format/build steps and existing permissions. Document synthetic UI and transport boundaries, real-font/source receipts, fixed test matrix, optional diagnostic versus acceptance distinction, format debt and stock Ubuntu qualification still pending until actually run. Keep an exact TODO(handoff) for that external CI acceptance gate and pair it in the root platform proposal.</action>
<verify><automated>Native YAML/workflow structure checks and source diff review; no inferred hosted CI success.</automated></verify>
<done>CI source is concrete, uses the tested entrypoint and retains all existing gates.</done>
</task>
<task type="auto">
<name>Freeze source and independently review portable acceptance</name>
<files>minion_site/scripts/qc/compile-ui-messages.mjs, minion_site/scripts/qc/compile-ui-messages.test.mjs, minion_site/scripts/qc/ui-fixtures.mjs, minion_site/scripts/qc/ui-fixtures.test.mjs, minion_site/playwright.ui-fixtures.config.ts, minion_site/.github/workflows/ci.yml, minion_site/tests/fixtures/member-data-parity/README.md, minion_site/tests/e2e/member-data-parity.spec.ts</files>
<action>Record source before/after, exact dependency identities, native result counts and command resources. Verify unchanged UI/service/archive/lock files and no active worktree/index changes. Root independently reviews the complete local execution and negative controls. Source qualification is separate from actual hosted CI, full production build, authenticated UI and global requirement closure.</action>
<verify><automated>Exact eight-file freeze and preservation receipts; scoped native format/design checks appropriate to changed files.</automated></verify>
<done>Portable implementation is reviewable with all remaining gates paired and no release claim.</done>
</task>
</tasks>
<threat_model>
| Boundary | Failure | Required mitigation |
|---|---|---|
| Checkout and CI credentials into fixture process | Secret or real provider use | Empty child environment, no .env loading, read-only checkout without persisted credentials, network controls and canary regressions |
| File output and dependency resolution | Traversal, linked-store overwrite or unrelated package mutation | Confined fresh private paths, exact native package resolution and unchanged lock/archive/source checks |
| Partial reports into acceptance | Missing engines, baseline mode, skipped/retried cases or substituted artifacts appear green | Fixed exact matrix, native reporter exit checks, required attachments and source/font/artifact identity verification |
| Synthetic local pass into deployment claim | Tests conceal missing real authorization or hosted CI failure | Separate local source, stock Ubuntu runtime and authenticated/deployed gates with paired TODO/proposal |
</threat_model>
<verification>All required tools, native behavior and engines must execute; absence is pending evidence rather than skip/pass. No whole-phase or deployment claim follows from local fixtures.</verification>
<output>Root writes 13-14-SUMMARY.md and updates the canonical inventory after independent review.</output>
