---
phase: 13-ui-qualification
plan: "09"
type: execute
wave: 4
depends_on: ["13-08"]
files_modified:
  - "minion_site/src/lib/components/members/AppBar.svelte"
  - "minion_site/src/lib/components/members/AppBar.test.ts"
  - "minion_site/src/routes/auth/logout/+server.ts"
  - "minion_site/src/routes/auth/logout/logout.test.ts"
  - "minion_site/messages/en.json"
  - "minion_site/messages/es.json"
autonomous: true
requirements: ["UI-03", "UI-07", "SEC-09"]
readiness: "Admitted exact local source/fixture scope; independent acceptance required"
must_haves:
  truths:
    - "Route logout through POST /auth/logout using the server-selected provider and the current request session. Supabase uses local scope; Better Auth receives the original request headers and returns its complete cookie-bearing Response. Enforce same-origin requests. AppBar prevents duplicate attempts, redirects only on reported success and offers a localized accessible failure/retry state. Verify provider routing, cookie propagation, origin rejection and failure behavior without real sessions."
  artifacts:
    - path: "minion_site/src/lib/components/members/AppBar.svelte"
      provides: "Site provider-aware logout and visible failure recovery"
  key_links:
    - from: "minion_site/src/routes/auth/logout/logout.test.ts"
      to: "minion_site/src/lib/components/members/AppBar.svelte"
      via: "Actual native component behavior under synthetic state"
---

<objective>Site provider-aware logout and visible failure recovery. Route logout through POST /auth/logout using the server-selected provider and the current request session. Supabase uses local scope; Better Auth receives the original request headers and returns its complete cookie-bearing Response. Enforce same-origin requests. AppBar prevents duplicate attempts, redirects only on reported success and offers a localized accessible failure/retry state. Verify provider routing, cookie propagation, origin rejection and failure behavior without real sessions.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/DECISIONS.md
@.planning/phases/13-ui-qualification/UI-SPEC.md
@.planning/operations/360/PROGRAM.md
@.claude/skills/ui-design-governance/SKILL.md
@minion_site/CLAUDE.md
</context>
<boundaries>
D360-17 prioritizes UI parity; these defects were found during its source re-audit. Preserve existing staged/working bytes with exact before-images. No branch, worktree, stash, auto-commit, release, provider, real file/graph request, live logout or authentication mutation. Use the existing Hub candidate or an ordinary private Site snapshot. No .env loading or shared cache writes; native tooling uses private cache/output and explicit installed dependency identities. Serialize expensive compilation, run one browser worker and restrict fixture traffic to an owned literal-loopback server. Root owns integration and acceptance; each executor owns exactly its six listed files. New source seams require a reviewed bounded amendment. Passing synthetic component tests cannot certify real authorization or the whole application.
</boundaries>
<tasks>
<task type="auto">
<name>Task 1: Preserve and reproduce the actual defects</name>
<files>minion_site/src/lib/components/members/AppBar.svelte, minion_site/src/lib/components/members/AppBar.test.ts, minion_site/src/routes/auth/logout/+server.ts, minion_site/src/routes/auth/logout/logout.test.ts, minion_site/messages/en.json, minion_site/messages/es.json</files>
<action>Read current source, actual consumers and installed primitives. Preserve before-images and candidate identity. Build meaningful failing cases for the reported behavior; distinguish source inference from native observation.</action>
<verify><automated>Run focused failing native tests or headless real-component cases; record failures alongside passing controls and installed/source hashes.</automated></verify>
<done>Exact file ownership, source identity and observable failure are documented without touching shared WIP.</done>
</task>
<task type="auto">
<name>Task 2: Repair and qualify the owned behavior</name>
<files>minion_site/src/lib/components/members/AppBar.svelte, minion_site/src/lib/components/members/AppBar.test.ts, minion_site/src/routes/auth/logout/+server.ts, minion_site/src/routes/auth/logout/logout.test.ts, minion_site/messages/en.json, minion_site/messages/es.json</files>
<action>Route logout through POST /auth/logout using the server-selected provider and the current request session. Supabase uses local scope; Better Auth receives the original request headers and returns its complete cookie-bearing Response. Enforce same-origin requests. AppBar prevents duplicate attempts, redirects only on reported success and offers a localized accessible failure/retry state. Verify provider routing, cookie propagation, origin rejection and failure behavior without real sessions. Use native lifecycle/focus semantics, explicit cleanup and semantic tokens. Exercise deferred failures and out-of-order responses where applicable. Avoid success-shaped mocks that bypass the behavior under test.</action>
<verify><automated>Run meaningful native component tests, actual headless browser cases at mobile/desktop widths, native Svelte checks and required design/token gates. Preserve per-case browser identity and zero-skip results.</automated></verify>
<done>Each reported defect has a passing behavioral regression; source-only and authenticated gaps remain explicit.</done>
</task>
<task type="auto">
<name>Task 3: Freeze for root integration and re-audit</name>
<files>minion_site/src/lib/components/members/AppBar.svelte, minion_site/src/lib/components/members/AppBar.test.ts, minion_site/src/routes/auth/logout/+server.ts, minion_site/src/routes/auth/logout/logout.test.ts, minion_site/messages/en.json, minion_site/messages/es.json</files>
<action>Freeze exact hashes, commands, before/after results, screenshots and unresolved limitations. Root independently reviews and reruns the relevant combined candidate. Pair every open source defect with TODO(handoff) and a proposal; do not claim full UI parity from a fixture.</action>
<verify><automated>Compare exact integration hashes and independently rerun targeted acceptance plus appropriate combined checks.</automated></verify>
<done>Reviewable evidence and precise remaining gates exist; root explicitly records acceptance status.</done>
</task>
</tasks>
<verification>Authentication, real gateway authority, real hardware and unaffected routes require their own evidence. Native browser behavior must match the behavior asserted.</verification>
<success_criteria>All owned behavior passes independent review and relevant tests with no lost staged work.</success_criteria>
<output>Root records13-09-SUMMARY.md and verification receipt after review.</output>

<authentication_boundary>Do not represent a redirect or cleared browser cookies as proof of durable server revocation. Installed Better Auth catches storage deletion errors internally; record that source-backed limitation and its separate real-session failure gate. No home-grown auth/session replacement is admitted here. Both provider identities must be qualified with disposable sessions before authenticated closure.</authentication_boundary>

<shared_fixture_amendment date="2026-09-11">13-08's executor exclusively owns the existing Site member-data-parity fixture/build/spec and adds actual AppBar browser coverage for13-09 using held/failing/successful synthetic POST /auth/logout and a fixture-only /login response. Test390/1280px, touch geometry, duplicate prevention, visible failure/retry and success-only navigation.13-09's executor remains sole writer of its six listed source/translation files; this amendment grants no concurrent fixture writes or real logout.</shared_fixture_amendment>
