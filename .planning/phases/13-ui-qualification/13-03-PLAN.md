---
phase: 13-ui-qualification
plan: "03"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - "minion_hub/tests/e2e/ui-audit/fixtures.ts"
  - "minion_hub/tests/e2e/ui-audit/personas.ts"
  - "minion_hub/tests/e2e/ui-audit/authenticated-critical-journeys.spec.ts"
  - "minion_hub/playwright.config.ts"
  - "minion_hub/.github/workflows/ci.yml"
  - "minion_hub/tests/e2e/ui-audit/README.md"
  - "minion_hub/tests/e2e/ui-audit/run-artifact.ts"
autonomous: true
requirements: ["UI-05","UI-01","UI-03"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Native dialog and critical route behavior is proven across declared browsers/input modes with reproducible seeded identity."
    - "Required CI includes actually executable critical matrix with no silent skips; artifact includes identity and per-case verdict."
  artifacts:
    - path: "minion_hub/tests/e2e/ui-audit/fixtures.ts"
      provides: "Make authenticated journey fixtures deterministic"
    - path: "minion_hub/.github/workflows/ci.yml"
      provides: "Wire mandatory CI evidence without opt-in bypass"
  key_links:
    - from: "minion_hub/tests/e2e/ui-audit/fixtures.ts"
      to: "minion_hub/tests/e2e/ui-audit/route-audit.spec.ts"
      via: "Read-first contract and behavior verification for make authenticated journey fixtures deterministic"
    - from: "minion_hub/.github/workflows/ci.yml"
      to: "minion_hub/tests/e2e/ui-audit/capture-readiness.ts"
      via: "Read-first contract and behavior verification for wire mandatory ci evidence without opt-in bypass"
---

<objective>
Install seeded cross-browser journey release gates.
Purpose: UI-05, UI-01, UI-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/13-ui-qualification/13-CONTEXT.md
@.claude/skills/ui-design-governance/SKILL.md
@minion_hub/tests/e2e/ui-audit/route-audit.spec.ts
@minion_hub/tests/e2e/ui-audit/certification.ts
@minion_hub/tests/e2e/ui-audit/capture-readiness.ts

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Make authenticated journey fixtures deterministic</name>
  <files>minion_hub/tests/e2e/ui-audit/fixtures.ts, minion_hub/tests/e2e/ui-audit/personas.ts, minion_hub/tests/e2e/ui-audit/authenticated-critical-journeys.spec.ts, minion_hub/playwright.config.ts</files>
  <read_first>minion_hub/tests/e2e/ui-audit/route-audit.spec.ts, minion_hub/tests/e2e/ui-audit/certification.ts</read_first>
  <behavior>Native dialog and critical route behavior is proven across declared browsers/input modes with reproducible seeded identity. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Extend the passing fixtures.ts/personas.ts created and qualified by 13-02; do not overwrite its Home/Calendar baseline or move initial fixture creation into this later plan. Seed additional test-only users/roles/data through that disposable backend or test transport; never add a production auth bypass. Cover login/session, Home send controls, Calendar staff navigation, secret save failure/retry/blocked dismissal, image dialog Escape/Tab/return focus, and representative CRM path in Chromium/Firefox/WebKit with keyboard/coarse pointer. Browser interaction must use browser-harness-approved sessions; native Playwright release suite uses headless config. Assertions must fail when cases are skipped.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/@playwright/test/cli.js test tests/e2e/ui-audit/authenticated-critical-journeys.spec.ts</automated></verify>
  <done>Native dialog and critical route behavior is proven across declared browsers/input modes with reproducible seeded identity.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mandatory CI evidence without opt-in bypass</name>
  <files>minion_hub/.github/workflows/ci.yml, minion_hub/tests/e2e/ui-audit/README.md, minion_hub/tests/e2e/ui-audit/run-artifact.ts</files>
  <read_first>minion_hub/tests/e2e/ui-audit/capture-readiness.ts</read_first>
  <behavior>Required CI includes actually executable critical matrix with no silent skips; artifact includes identity and per-case verdict. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Add required critical-journey jobs using installed browser revisions and private-free fixtures; preserve existing checks. Emit screenshots, trace failures and exact source/browser/viewport metadata. Missing fixture/browser is a failure or explicit blocked job, not E2E_UI_AUDIT optional green. Keep exhaustive audit suite separate if expensive but critical cases mandatory. Do not generate real production sessions or upload sensitive traces.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/@playwright/test/cli.js test tests/e2e/ui-audit/authenticated-critical-journeys.spec.ts --list &amp;&amp; bun run lint:design &amp;&amp; bun run lint:tokens</automated></verify>
  <done>Required CI includes actually executable critical matrix with no silent skips; artifact includes identity and per-case verdict.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Untrusted data / rendered UI / user action | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-13-03-01 | I/D | Unsafe HTML or inaccessible essential action | mitigate | Escaped text, native focus, motion alternatives and actual headless journey tests. |
| T-13-03-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/13-ui-qualification/13-03-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>


<scope_amendment date="2026-09-11">13-06 owns the separate component critical-journeys.spec.ts and playwright.critical.config.ts. This plan retains authenticated-critical-journeys.spec.ts, real seeded identity, application routes and mandatory CI integration. Synthetic transport must never satisfy its authentication requirement.</scope_amendment>
