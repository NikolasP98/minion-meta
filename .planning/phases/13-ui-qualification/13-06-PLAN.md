---
phase: 13-ui-qualification
plan: "06"
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - "minion_hub/playwright.critical.config.ts"
  - "minion_hub/tests/e2e/ui-audit/critical-journeys.spec.ts"
  - "minion_hub/tests/e2e/ui-audit/critical-artifact.ts"
  - "minion_hub/tests/fixtures/critical-journeys/build.mjs"
  - "minion_hub/tests/fixtures/critical-journeys/serve.mjs"
  - "minion_hub/tests/fixtures/critical-journeys/gateway-transport.ts"
  - "minion_hub/tests/fixtures/critical-journeys/HomeFixture.svelte"
  - "minion_hub/tests/fixtures/critical-journeys/README.md"
autonomous: true
requirements: ["UI-01", "UI-03", "UI-05"]
readiness: "Admitted bounded local implementation; independent integration acceptance pending"
must_haves:
  truths:
    - "Exercise actual Home, Calendar and overlay components at mobile/tablet/desktop widths. Drive real gateway-client connection and recovery transitions through a synthetic transport rather than forcing connected state. Enumerate required Chromium/Firefox/WebKit cases and reject missing bundles, engines or skipped cases. Full authenticated application, Supabase, CRM, app navigation and production parity remain13-03; synthetic data cannot close those gates."
  artifacts:
    - path: "minion_hub/playwright.critical.config.ts"
      provides: "Mandatory critical component journeys across browser engines"
  key_links:
    - from: "minion_hub/tests/fixtures/critical-journeys/README.md"
      to: "minion_hub/playwright.critical.config.ts"
      via: "Reproducible actual-component qualification"
---

<objective>Mandatory critical component journeys across browser engines. Exercise actual Home, Calendar and overlay components at mobile/tablet/desktop widths. Drive real gateway-client connection and recovery transitions through a synthetic transport rather than forcing connected state. Enumerate required Chromium/Firefox/WebKit cases and reject missing bundles, engines or skipped cases. Full authenticated application, Supabase, CRM, app navigation and production parity remain13-03; synthetic data cannot close those gates.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/DECISIONS.md
@.planning/operations/360/RESUME-2026-09-11.md
@.planning/phases/13-ui-qualification/13-02-PLAN.md
@.planning/phases/13-ui-qualification/UI-SPEC.md
@.claude/skills/ui-design-governance/SKILL.md
@minion_hub/CLAUDE.md
</context>
<boundaries>
D360-17 prioritizes UI parity. Root owns integration and acceptance; executor owns exactly the nine files of13-05 or eight files of13-06. Preserve existing staged bytes and record source/dependency identities. No branch, worktree, stash, automatic commit, deployment or shared dependency mutation. Use the existing candidate or an ordinary private snapshot. No secret environment loading, app autostart, real authentication, microphone, providers or production data. Build with envDir:false/configFile:false and private cache/output. Browser traffic is restricted to an owned literal-loopback fixture origin; missing browser engines are failures, not skips. Serialize expensive compilation and use one browser worker. Follow the browser-harness and UI design governance skills. Any uncovered source seam needs a concrete child scope and paired TODO/proposal. These fixtures qualify only the components and transitions they actually execute.
</boundaries>
<tasks>
<task type="auto">
<name>Task 1: Preserve and characterize the baseline</name>
<files>minion_hub/playwright.critical.config.ts, minion_hub/tests/e2e/ui-audit/critical-journeys.spec.ts, minion_hub/tests/e2e/ui-audit/critical-artifact.ts, minion_hub/tests/fixtures/critical-journeys/build.mjs, minion_hub/tests/fixtures/critical-journeys/serve.mjs, minion_hub/tests/fixtures/critical-journeys/gateway-transport.ts, minion_hub/tests/fixtures/critical-journeys/HomeFixture.svelte, minion_hub/tests/fixtures/critical-journeys/README.md</files>
<action>Read owned source, instructions and installed dependencies. Preserve staged/working before-images. Record private source manifest and meaningful baseline failures before product repairs.</action>
<verify><automated>Inspect scoped git diff and saved manifest; run isolated baseline cases with explicit expected case counts.</automated></verify>
<done>Baseline is reproducible, ownership is exact, and no shared index or environment was altered.</done>
</task>
<task type="auto">
<name>Task 2: Implement and exercise the bounded behavior</name>
<files>minion_hub/playwright.critical.config.ts, minion_hub/tests/e2e/ui-audit/critical-journeys.spec.ts, minion_hub/tests/e2e/ui-audit/critical-artifact.ts, minion_hub/tests/fixtures/critical-journeys/build.mjs, minion_hub/tests/fixtures/critical-journeys/serve.mjs, minion_hub/tests/fixtures/critical-journeys/gateway-transport.ts, minion_hub/tests/fixtures/critical-journeys/HomeFixture.svelte, minion_hub/tests/fixtures/critical-journeys/README.md</files>
<action>Exercise actual Home, Calendar and overlay components at mobile/tablet/desktop widths. Drive real gateway-client connection and recovery transitions through a synthetic transport rather than forcing connected state. Enumerate required Chromium/Firefox/WebKit cases and reject missing bundles, engines or skipped cases. Full authenticated application, Supabase, CRM, app navigation and production parity remain13-03; synthetic data cannot close those gates. Use actual component semantics and callbacks. Reject unexpected network or microphone attempts. Test compact widths360/390, tablet768 and desktop1440, keyboard focus and reduced motion where applicable. Preserve dark/light semantic tokens.</action>
<verify><automated>Run the exact isolated native browser suite with one worker, inspect failures and zero-skip count; run native Svelte checks and design/token gates for product changes.</automated></verify>
<done>The stated behavior passes with captured engine, viewport, source identity and artifact evidence; missing execution remains explicit.</done>
</task>
<task type="auto">
<name>Task 3: Freeze for independent integration review</name>
<files>minion_hub/playwright.critical.config.ts, minion_hub/tests/e2e/ui-audit/critical-journeys.spec.ts, minion_hub/tests/e2e/ui-audit/critical-artifact.ts, minion_hub/tests/fixtures/critical-journeys/build.mjs, minion_hub/tests/fixtures/critical-journeys/serve.mjs, minion_hub/tests/fixtures/critical-journeys/gateway-transport.ts, minion_hub/tests/fixtures/critical-journeys/HomeFixture.svelte, minion_hub/tests/fixtures/critical-journeys/README.md</files>
<action>Record final source hashes, exact commands, observed results, browser dependencies and remaining gaps. Root independently reviews changed source and rechecks the combined UI candidate before acceptance. Pair every remaining implementation limitation with its exact source TODO and proposal.</action>
<verify><automated>Compare final hashes to the integrated candidate and run the combined relevant checks without skipped required cases.</automated></verify>
<done>Reviewable candidate and honest partial/complete boundaries exist; full13-03 and UI requirements remain open until their own evidence is complete.</done>
</task>
</tasks>
<verification>Missing credentials, browser dependencies, actual stack or production evidence must remain explicit. Passing a component fixture does not certify the authenticated application.</verification>
<success_criteria>All owned behavior independently verified, staged work preserved and integration gaps documented.</success_criteria>
<output>Root records13-06-SUMMARY.md and corresponding verification receipt after independent review.</output>
