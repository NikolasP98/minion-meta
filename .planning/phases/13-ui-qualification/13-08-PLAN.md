---
phase: 13-ui-qualification
plan: "08"
type: execute
wave: 3
depends_on: ["13-05"]
files_modified:
  - "minion_site/src/lib/components/members/GraphTab.svelte"
  - "minion_site/src/lib/components/members/GraphTab.test.ts"
  - "minion_site/src/lib/components/members/FilesTab.svelte"
  - "minion_site/src/lib/components/members/FilesTab.test.ts"
  - "minion_site/tests/fixtures/member-data-parity/Fixture.svelte"
  - "minion_site/tests/fixtures/member-data-parity/build.mjs"
  - "minion_site/tests/fixtures/member-data-parity/README.md"
  - "minion_site/tests/e2e/member-data-parity.spec.ts"
  - "minion_site/package.json"
  - "minion_site/bun.lock"
  - "minion_site/vitest.config.ts"
  - "minion_site/vitest.workspace.ts"
  - "minion_site/prettier.config.cjs"
autonomous: true
requirements: ["UI-03", "UI-06", "UI-07"]
readiness: "Admitted exact local source/fixture scope; independent acceptance required"
must_haves:
  truths:
    - "Keep a stable Graph render container, bound automatic loading to agent/connection identity, make retry explicit, reject stale results and dispose late imports/observers. Treat graph labels as untrusted display data and prevent HTML tooltip injection. Provide named node/edge text and reduced-motion behavior using the existing chart engine. Fence file-content publication against selection, agent and lifetime changes so stale responses cannot appear under a different filename."
  artifacts:
    - path: "minion_site/src/lib/components/members/GraphTab.svelte"
      provides: "Site member graph and file-viewer asynchronous correctness"
  key_links:
    - from: "minion_site/tests/e2e/member-data-parity.spec.ts"
      to: "minion_site/src/lib/components/members/GraphTab.svelte"
      via: "Actual native component behavior under synthetic state"
---

<objective>Site member graph and file-viewer asynchronous correctness. Keep a stable Graph render container, bound automatic loading to agent/connection identity, make retry explicit, reject stale results and dispose late imports/observers. Treat graph labels as untrusted display data and prevent HTML tooltip injection. Provide named node/edge text and reduced-motion behavior using the existing chart engine. Fence file-content publication against selection, agent and lifetime changes so stale responses cannot appear under a different filename.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/DECISIONS.md
@.planning/phases/13-ui-qualification/UI-SPEC.md
@.planning/operations/360/PROGRAM.md
@.claude/skills/ui-design-governance/SKILL.md
@minion_site/CLAUDE.md
</context>
<boundaries>
D360-17 prioritizes UI parity; these defects were found during its source re-audit. Preserve existing staged/working bytes with exact before-images. No branch, worktree, stash, auto-commit, release, provider, real file/graph request, live logout or authentication mutation. Use the existing Hub candidate or an ordinary private Site snapshot. No .env loading or shared cache writes; native tooling uses private cache/output and explicit installed dependency identities. Serialize expensive compilation, run one browser worker and restrict fixture traffic to an owned literal-loopback server. Root owns integration and acceptance; each executor owns exactly its thirteen listed files. New source seams require a reviewed bounded amendment. Passing synthetic component tests cannot certify real authorization or the whole application.
</boundaries>
<tasks>
<task type="auto">
<name>Task 1: Preserve and reproduce the actual defects</name>
<files>minion_site/src/lib/components/members/GraphTab.svelte, minion_site/src/lib/components/members/GraphTab.test.ts, minion_site/src/lib/components/members/FilesTab.svelte, minion_site/src/lib/components/members/FilesTab.test.ts, minion_site/tests/fixtures/member-data-parity/Fixture.svelte, minion_site/tests/fixtures/member-data-parity/build.mjs, minion_site/tests/fixtures/member-data-parity/README.md, minion_site/tests/e2e/member-data-parity.spec.ts</files>
<action>Read current source, actual consumers and installed primitives. Preserve before-images and candidate identity. Build meaningful failing cases for the reported behavior; distinguish source inference from native observation.</action>
<verify><automated>Run focused failing native tests or headless real-component cases; record failures alongside passing controls and installed/source hashes.</automated></verify>
<done>Exact file ownership, source identity and observable failure are documented without touching shared WIP.</done>
</task>
<task type="auto">
<name>Task 2: Repair and qualify the owned behavior</name>
<files>minion_site/src/lib/components/members/GraphTab.svelte, minion_site/src/lib/components/members/GraphTab.test.ts, minion_site/src/lib/components/members/FilesTab.svelte, minion_site/src/lib/components/members/FilesTab.test.ts, minion_site/tests/fixtures/member-data-parity/Fixture.svelte, minion_site/tests/fixtures/member-data-parity/build.mjs, minion_site/tests/fixtures/member-data-parity/README.md, minion_site/tests/e2e/member-data-parity.spec.ts</files>
<action>Keep a stable Graph render container, bound automatic loading to agent/connection identity, make retry explicit, reject stale results and dispose late imports/observers. Treat graph labels as untrusted display data and prevent HTML tooltip injection. Provide named node/edge text and reduced-motion behavior using the existing chart engine. Fence file-content publication against selection, agent and lifetime changes so stale responses cannot appear under a different filename. Use native lifecycle/focus semantics, explicit cleanup and semantic tokens. Exercise deferred failures and out-of-order responses where applicable. Avoid success-shaped mocks that bypass the behavior under test.</action>
<verify><automated>Run meaningful native component tests, actual headless browser cases at mobile/desktop widths, native Svelte checks and required design/token gates. Preserve per-case browser identity and zero-skip results.</automated></verify>
<done>Each reported defect has a passing behavioral regression; source-only and authenticated gaps remain explicit.</done>
</task>
<task type="auto">
<name>Task 3: Freeze for root integration and re-audit</name>
<files>minion_site/src/lib/components/members/GraphTab.svelte, minion_site/src/lib/components/members/GraphTab.test.ts, minion_site/src/lib/components/members/FilesTab.svelte, minion_site/src/lib/components/members/FilesTab.test.ts, minion_site/tests/fixtures/member-data-parity/Fixture.svelte, minion_site/tests/fixtures/member-data-parity/build.mjs, minion_site/tests/fixtures/member-data-parity/README.md, minion_site/tests/e2e/member-data-parity.spec.ts</files>
<action>Freeze exact hashes, commands, before/after results, screenshots and unresolved limitations. Root independently reviews and reruns the relevant combined candidate. Pair every open source defect with TODO(handoff) and a proposal; do not claim full UI parity from a fixture.</action>
<verify><automated>Compare exact integration hashes and independently rerun targeted acceptance plus appropriate combined checks.</automated></verify>
<done>Reviewable evidence and precise remaining gates exist; root explicitly records acceptance status.</done>
</task>
</tasks>
<verification>Authentication, real gateway authority, real hardware and unaffected routes require their own evidence. Native browser behavior must match the behavior asserted.</verification>
<success_criteria>All owned behavior passes independent review and relevant tests with no lost staged work.</success_criteria>
<output>Root records13-08-SUMMARY.md and verification receipt after review.</output>

<scope_amendment date="2026-09-11">Add only dev dependency happy-dom20.11.6 plus its Bun lock resolution in the private Site snapshot so native mounted tests are reproducible after a normal install. No unrelated dependency upgrades or active checkout install. Private borrowed tooling remains baseline evidence only.</scope_amendment>

<scope_amendment date="2026-09-11">Native Vitest2 config/workspace isolates the new mounted cases from existing Node tests. A dev-only ws8.20.0 constraint preserves the prior shared optional-peer resolution while HappyDOM uses its nested dependency. Normal and production-only resolver proof is required before accepting lock changes.</scope_amendment>

<shared_fixture_amendment date="2026-09-11">13-08's executor exclusively owns the existing Site member-data-parity fixture/build/spec and adds actual AppBar browser coverage for13-09 using held/failing/successful synthetic POST /auth/logout and a fixture-only /login response. Test390/1280px, touch geometry, duplicate prevention, visible failure/retry and success-only navigation.13-09's executor remains sole writer of its six listed source/translation files; this amendment grants no concurrent fixture writes or real logout.</shared_fixture_amendment>

<dependency_alignment date="2026-09-11">Admit exact development Vitest3.2.6, @playwright/test1.61.1, HappyDOM20.11.6 and ws8.20.0 preservation pin in the owned package/lock. Actual isolated Vitest lock experiment changed twelve test records, added six and removed duplicate Vite5/esbuild tooling; existing Vite6.4.2, Svelte5.55.9/plugin5.1.1 and non-test records remain unchanged. This aligns native plugin types without an unchecked adapter. Re-run all old/new native tests and scoped config types; preserve production-resolution evidence because Better Auth's existing optional Vitest peer may materialize test dependencies even in a production install. Do not claim production omission or bundle inclusion without separate evidence.</dependency_alignment>

<wire_contract_amendment date="2026-09-11">Gateway memory.snapshot returns listAllRelationships rows with fromId/toId/relType (typed-schema), while the old Site expected source/target/relation. Admit an exact Graph display adapter for canonical fields plus legacy UI-shaped responses; reject conflicting mixed shapes and preserve unused payload metadata without mutation. Native tests and browser fixtures must use canonical relationship fields and verify both rendered/nonvisual relationships. Keep omitted/empty legacy node-label fallback to id; canonical label is non-null string. No gateway mutation or generic parser package. Prior legacy-shaped fixture passes do not establish canonical wire parity.</wire_contract_amendment>

<formatter_amendment date="2026-09-11">After the preserved twelve-file freeze,13-08 executor may write only package.json, bun.lock and new prettier.config.cjs to declare development prettier-plugin-svelte4.1.1 and extend the installed shared lint preset with its native parser. Verify peers/resolver identity and native parser discovery; format-check the frozen owned UI files only, no repository-wide rewrite. Preserve the earlier receipt and append a tooling supplement. Workspace/messages have transferred to13-11 and must not be edited by this executor. Existing global formatting debt is distinct from parser availability.</formatter_amendment>
