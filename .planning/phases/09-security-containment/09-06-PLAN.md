---
phase: 09-security-containment
plan: "06"
type: execute
wave: 3
depends_on: ["11-02"]
status: admitted
autonomous: true
requirements: [SEC-02, SDK-01, AGT-03]
files_modified:
  - minion/src/shells/bridge-ws.ts
  - minion/src/shells/bridge-ws.test.ts
  - minion/src/shells/manager.ts
  - minion/src/shells/manager.test.ts
must_haves:
  truths:
    - Malformed bridge frames cannot escape as unhandled promise rejections or invoke manager authority.
    - Only the exact current registered bridge connection can settle its pending RPC or mutate its shell connection state.
    - Replacement connections survive stale close, event, response and registration callbacks.
  artifacts:
    - path: minion/src/shells/bridge-ws.test.ts
      provides: Real loopback WebSocket frame and registration-boundary regression tests
    - path: minion/src/shells/manager.test.ts
      provides: Exact pending-owner and replacement-connection tests
  key_links:
    - from: minion/src/shells/bridge-ws.ts
      to: minion/src/shells/manager.ts
      via: Registered shell and actual socket identity, never request ID alone
---

<objective>Constrain the gateway-side Shells receiver before adding durable outcome delivery. This is the receiver counterpart of11-02, not its sender implementation or a durable acknowledgment protocol.</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@AGENTS.md
@minion/AGENTS.md
@minion/.dmux-hooks/CLAUDE.md
@.planning/operations/360/PROGRAM.md
@.planning/phases/11-agent-lifecycle/11-02-VERIFICATION.md
@minion/src/shells/bridge-ws.ts
@minion/src/shells/manager.ts
@minion/src/shells/types.ts
</context>

## Evidence and boundary

Root source inspection found JSON.parse(null) followed by frame.type in an async function dispatched with void and no rejection handler. Responses pass only a global pending request ID to the manager; the ID is a random UUID, so this is missing authority binding, not proof of a practical guessed-ID attack. Close handlers detach by shell ID without the originating socket. Event relay is transient and has no durable acknowledgment;11-03 and a separately reviewed phase14 receiver plan own that protocol.

Read actual current branch/SHA and preserve unrelated WIP. Root grants only the four files above after independent admission. No provider calls, production sockets, token export, protocol version change, persistence migration, source alignment, commit or deployment. Reuse native ws and existing fake provider/store fixtures. Do not change shared package schemas for this defensive receiver slice. New required source seams need an explicit amendment.

<tasks>
<task type="auto">
<name>Task 1: Prove and contain malformed-frame and registration races</name>
<files>minion/src/shells/bridge-ws.ts, minion/src/shells/bridge-ws.test.ts</files>
<read_first>minion/src/shells/manager.ts, minion/src/shells/manager.test.ts</read_first>
<action>Add actual loopback WebSocket tests for null, arrays, scalars, malformed req/res/event fields and unsupported frame types before valid registration. Validate runtime frame shape before accessing manager methods; use safe bounded error handling without raw credentials/errors. Prevent simultaneous registration requests on one socket from admitting multiple shell identities; reject registration completing after the originating socket closes. Catch asynchronous dispatch failures and keep unrelated connections usable. Test registration failure recovery and prevent overlapping credential validation from admitting multiple identities on one socket. consumeDeviceToken validates a reusable persisted machine credential; preserve valid reconnects and correct the stale single-use comment in this owned file. Do not serialize all post-registration messages behind long forwarded RPCs.</action>
<verify><automated>pnpm exec vitest run src/shells/bridge-ws.test.ts</automated></verify>
<done>Malformed and concurrent-registration cases fail closed with no unhandled rejection; valid unrelated bridge traffic survives.</done>
</task>
<task type="auto">
<name>Task 2: Bind pending outcomes and lifecycle changes to their connection owner</name>
<files>minion/src/shells/manager.ts, minion/src/shells/manager.test.ts, minion/src/shells/bridge-ws.ts, minion/src/shells/bridge-ws.test.ts</files>
<read_first>minion/src/shells/types.ts, minion/src/shells/store.ts</read_first>
<action>Capture the actual bridge socket/connection and shell ID on forwarded requests. Require exact current identity before settling, heartbeating, fatal reporting, relaying events or detaching. A wrong registered shell supplied a known pending ID in a synthetic fixture must not settle it. Replace/disconnect rejects and clears only pending work belonging to that old connection, preserving timers and newer owners. Allow only bridge-origin shell.delta, shell.final and shell.backup_done event names and validate their required payload fields; reject gateway-owned online/archived/error/quota event spoofing. Ensure late async event lookup or registration cannot emit or attach after replacement/close. Define current-owner linearization for each persistent mutation, not only a check after an awaited write. Serialize per-shell registration/fatal/status mutations or provide equivalent ordering so an older deferred store update cannot overwrite a newer connection. Use deferred credential/get/update store boundaries in tests. Admission may precede socket closure, but closure must fence subsequent callbacks; distinguish that accepted transition from claiming an in-flight storage write was undone. Preserve reusable credentials and legitimate reconnects. Test stale close, stale result, overlapping registration/replacement and ordinary invoke/cancel/backup behavior. Internal administrative dropBridge remains explicit owner-independent action; do not weaken legitimate shutdown/archive paths. Durable outcomes remain11-03/phase14 work and must retain TODO plus proposal.</action>
<verify><automated>pnpm exec vitest run src/shells/bridge-ws.test.ts src/shells/manager.test.ts src/shells/store.test.ts; pnpm exec tsgo; scoped oxlint and oxfmt checks</automated></verify>
<done>Each pending RPC and lifecycle effect has an exact authenticated connection owner; stale/mismatched sockets cannot alter replacement state.</done>
</task>
</tasks>

<threat_model>Untrusted pre-auth JSON crosses parser authority; registered remote shells cross tenant/connection ownership; reconnect callbacks cross generations. Validate frame shape and bind server-held socket identity. UUID unpredictability is not authority proof. No production exploit or claimed durable delivery.</threat_model>
<verification>Red-green loopback and actual manager cases, no process-global rejection suppression, bounded test cleanup, independent source/behavior review and exact file hashes. Preserve prior11-02 sender tests. Root adds exact allowlist entry only after plan review. Source fixes do not establish deployed receiver or published package adoption.</verification>
<output>Write09-06-SUMMARY with exact source/commands and evidence limits. Independent reviewer writes09-06-VERIFICATION. Root owns proposal, allowlist and canonical status; execution requires exclusive ownership and the recorded verification gates.</output>
