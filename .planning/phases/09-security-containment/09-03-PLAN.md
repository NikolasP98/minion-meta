---
phase: 09-security-containment
plan: 03
type: execute
wave: 2
depends_on: [09-01]
autonomous: true
requirements: [SEC-07, SEC-02, SEC-03]
status: awaiting_admission
findings: [SV-01, SV-03]
files_modified:
  - minion_hub/src/hooks.server.ts
  - minion_hub/src/hooks.tenant.test.ts
  - minion_hub/src/server/auth/tenant-ctx.ts
  - minion_hub/src/server/auth/tenant-ctx.test.ts
  - minion_hub/src/routes/api/servers/[id]/+server.ts
  - minion_hub/src/routes/api/servers/[id]/server.test.ts
  - minion_hub/src/routes/onboarding/+page.server.ts
  - minion_hub/src/routes/onboarding/page.server.test.ts
must_haves:
  truths:
    - A verified browser user without a resolved membership tenant cannot create, update or delete a server in any globally selected organization.
    - The tenant helper never queries organizations or synthesizes authority from orgId alone; it returns only the resolved tenant context or null.
    - Explicit valid tenant contexts for authorized members and admins remain unchanged.
    - Public and separately authenticated API paths remain reachable without receiving fabricated tenant authority.
    - No-org users retain the join request and invite flow; onboarding redirects them to join before provisioning or linking a gateway.
    - URL updates pass the same existing URL policy as creation before persistence; token-only updates retain their existing behavior.
  artifacts:
    - path: minion_hub/src/hooks.tenant.test.ts
      provides: Actual hook-to-POST regression from verified-user/no-tenant identity output with persistence negative control
    - path: minion_hub/src/server/auth/tenant-ctx.test.ts
      provides: Direct helper denial and explicit-context positive controls without global organization lookup
    - path: minion_hub/src/routes/api/servers/[id]/server.test.ts
      provides: Actual PUT policy parity, authorization ordering and safe diagnostics
    - path: minion_hub/src/routes/onboarding/page.server.test.ts
      provides: No-org join redirect before agent provisioning plus valid-tenant onboarding control
  key_links:
    - from: minion_hub/src/hooks.server.ts
      to: minion_hub/src/routes/api/servers/+server.ts
      via: Unresolved tenant stays absent through the actual handler chain
    - from: minion_hub/src/routes/api/servers/[id]/+server.ts
      to: minion_hub/src/server/services/ssrf-guard.ts
      via: Existing assertSafeUrl runs before updateServer whenever a URL is supplied
    - from: minion_hub/src/routes/onboarding/+page.server.ts
      to: minion_hub/src/server/auth/tenant-ctx.ts
      via: Null context redirects to the existing join enrollment route
---

<objective>
Close verifier findings SV-01 and SV-03: remove global-organization tenant inference and make server URL update policy match creation. Preserve explicit identity-derived tenant authority, the enrollment route and existing private-network operator opt-ins. This is an extension of the Phase 09 security goal; SEC-02/03 links are related coverage, not a claim that their original wording fully described these newly found gaps. Parent owns any requirement additions.
</objective>

<execution_context>
@.agents/skills/minion-engineering/SKILL.md
@/home/nikolas/.claude/get-shit-done/templates/phase-prompt.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-security-containment/09-SOURCE-VERIFICATION.md
@.planning/phases/09-security-containment/09-01-SUMMARY.md
@minion_hub/CLAUDE.md
@minion_hub/AGENTS.md
@minion_hub/src/server/auth/resolve-identity.ts
@minion_hub/src/routes/join/+page.server.ts
@minion_hub/src/routes/api/join-requests/+server.ts

Author reviewed the actual identity output, finishApp, tenant helper, server mutations, URL guard, onboarding, join actions and app-layout no-membership redirect. No source edits before reviewer admission and parent exclusive ownership grant. Work on the current dirty checkout; preserve existing hooks instrumentation and all unrelated edits. No branch/stash/worktree/commit, dependencies, provider calls, production requests, deployment or production data mutation. Root owns proposals, requirement/state/index files and whole-project typecheck.
</context>

<tasks>
<task type="auto">
  <name>SV-01: Remove inferred tenant authority at the hook and helper boundaries</name>
  <files>minion_hub/src/hooks.server.ts, minion_hub/src/hooks.tenant.test.ts, minion_hub/src/server/auth/tenant-ctx.ts, minion_hub/src/server/auth/tenant-ctx.test.ts</files>
  <read_first>minion_hub/src/server/auth/resolve-identity.ts, minion_hub/src/hooks.server.ts, minion_hub/src/server/auth/tenant-ctx.ts, minion_hub/src/routes/api/servers/+server.ts, minion_hub/src/server/auth/authorize.ts, .planning/phases/09-security-containment/09-SOURCE-VERIFICATION.md</read_first>
  <action>Write a permanent fixture around the exported actual hook sequence and actual server POST with actual requireAuth and tenant helper. Inject the identity-provider boundary with a verified user and no tenant; stub persistence and external services only. Confirm the old chain selects a synthetic unrelated global org and persists before fixing. Remove organization reads and DB construction from finishApp and getTenantCtx. The hook's existing explicitly handler-authenticated/public exceptions may dispatch without a tenant; they must never receive an invented context. Retain existing internal/cron/password-auth handler authentication routing. Keep unresolved API requests outside those exceptions denied (401 anonymous, 403 authenticated without tenant). Preserve centralized write capability checks for existing resolved-tenant requests. getTenantCtx returns only locals.tenantCtx or null and attributes usage only when resolved; getOrCreateTenantCtx retains its 403 denial. An orgId field or platform-admin role alone does not construct a tenant. Do not alter resolveIdentity provider verification, membership selection, AUTH_DISABLED behavior or token caching. Test no-tenant member/admin POST denial before persistence, direct helper null/403/no lookup, explicit authorized context unchanged, anonymous server denial, locale canonicalization, and the existing join/public/internal/cron dispatch contracts without tenant synthesis. The actual hook/POST test must not mock getTenantCtx or requireAuth.</action>
  <verify>From minion_hub: bun run vitest run src/hooks.tenant.test.ts src/server/auth/tenant-ctx.test.ts src/server/auth/resolve-identity.test.ts src/routes/api/servers/server.test.ts</verify>
  <done>Observed red failures become green; no first-global organization query remains in these two boundaries, and no-tenant mutations cannot reach persistence. Real identity membership behavior remains covered by its nearby suite.</done>
</task>

<task type="auto">
  <name>Preserve enrollment and valid onboarding without borrowed organization authority</name>
  <files>minion_hub/src/routes/onboarding/+page.server.ts, minion_hub/src/routes/onboarding/page.server.test.ts</files>
  <read_first>minion_hub/src/routes/onboarding/+page.server.ts, minion_hub/src/routes/join/+page.server.ts, minion_hub/src/routes/api/join-requests/+server.ts, minion_hub/src/routes/(app)/+layout.server.ts</read_first>
  <action>Change the authenticated no-tenant onboarding redirect from login to the existing /join enrollment route, matching app-layout behavior. Verify the actual load redirects before personal-agent provisioning, gateway linking, host loading or identity reads. Retain valid-context onboarding and active-agent redirect positives. In hook fixtures, preserve GET/POST /join and no-tenant POST /api/join-requests reachability, without populating tenantCtx. Join's existing application target selection creates only a pending request; this plan does not change enrollment targeting or grant membership, and invite membership remains under existing consumeLink checks. Do not edit Svelte markup or join service policy.</action>
  <verify>From minion_hub: bun run vitest run src/routes/onboarding/page.server.test.ts src/server/services/join/requests.service.test.ts src/server/services/join/links.service.test.ts src/server/services/join/membership.test.ts src/hooks.tenant.test.ts</verify>
  <done>No-org onboarding follows enrollment without provisioning under another org; valid onboarding and existing join/invite service authorization remain green.</done>
</task>

<task type="auto">
  <name>SV-03: Apply creation URL policy to server updates</name>
  <files>minion_hub/src/routes/api/servers/[id]/+server.ts, minion_hub/src/routes/api/servers/[id]/server.test.ts</files>
  <read_first>minion_hub/src/routes/api/servers/+server.ts, minion_hub/src/routes/api/servers/[id]/+server.ts, minion_hub/src/server/services/ssrf-guard.ts, minion_hub/src/server/services/server.service.ts</read_first>
  <action>Add failing actual PUT tests using the real assertSafeUrl guard with DNS boundary stubs where required. Authenticate and authorize the existing server link before parsing/validating the update; absent tenant must fail through the real helper before persistence. Whenever body.url is supplied, validate with assertSafeUrl(body.url, 'server URL'); omitted URL retains token/name-only update behavior. Return the same fixed 422 message as POST for SsrfBlockedError and preserve fixed credential-safe 500 diagnostics. Cover blocked local/private URLs, invalid/supplied-empty URL, valid public ws/wss/http/https URL, explicitly opted-in private hostname or Tailscale policy, token-only update, supplied unchanged valid URL, denied server link and missing tenant. Do not modify the shared guard's policy, DNS behavior or operator allowlists. This establishes persistence-policy parity; do not claim a confirmed outgoing SSRF exploit.</action>
  <verify>From minion_hub: bun run vitest run 'src/routes/api/servers/[id]/server.test.ts' src/routes/api/servers/server.test.ts src/server/services/ssrf-guard.test.ts src/server/services/server.service.test.ts</verify>
  <done>Denied URLs never reach updateServer; safe supplied URLs and existing authorized token-only updates pass. Missing tenant/link prevents mutation, and no request or exception credential values reach diagnostics.</done>
</task>

<task type="auto">
  <name>Record scoped verification and independent handoff</name>
  <files>.planning/phases/09-security-containment/09-03-SUMMARY.md</files>
  <read_first>.planning/phases/09-security-containment/09-03-PLAN.md, .planning/phases/09-security-containment/09-SOURCE-VERIFICATION.md</read_first>
  <action>Run the admitted focused and nearby suites, scoped formatting and diff checks. Give root exact new file paths for the standard typecheck it owns. Record red/green outcomes, fixture boundaries, valid onboarding and operator URL controls, source identity, consumer classification and limitations. Separate standards and spec review. Send any unresolved issue to root for proposal/requirement registration and add exact-site TODO(handoff) only when a source gap remains. Do not mark root requirements or phase state complete.</action>
  <verify>Native GSD verify plan-structure plus focused commands above and root-owned final check evidence.</verify>
  <done>Reviewer can trace the no-tenant input through the actual hook/handler and see bounded URL policy parity, with no production or rollout certification implied.</done>
</task>
</tasks>

<verification>
Required red-green regression crosses actual hook and actual mutation handler, preserving the real authorization/helper boundary. Dependency stubs must not pre-decide tenant denial. Test direct helper access as well as the hook to prevent alternate consumers reintroducing inference. Relevant no-tenant exception paths dispatch with no tenant and retain their own authentication. Existing no-org enrollment remains usable; resolved tenant member/admin paths remain explicit. URL guard tests use real policy with stubbed DNS, no outgoing sockets. Whole-project typecheck is not duplicated while root owns it.
</verification>

<success_criteria>
SV-01 global tenant inference is removed from both shared boundaries and server writes with unresolved tenant fail closed. Valid onboarding and authorized member/admin mutations remain available. SV-03 create/update URL policy is consistent before persistence, with fixed diagnostics and preserved operator opt-ins. Parent and independent reviewer admit before source work and verify afterward.
</success_criteria>

<output>
.planning/phases/09-security-containment/09-03-SUMMARY.md
</output>
