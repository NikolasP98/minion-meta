---
phase: 09-security-containment
plan: "08"
type: execute
wave: 2
gap_closure: true
status: admitted
autonomous: true
depends_on: ["09-07"]
requirements: [SEC-07]
files_modified:
  - minion_hub/src/lib/attachments/upload.ts
  - minion_hub/src/lib/attachments/upload.test.ts
  - minion_hub/src/server/db/pg-attachments-schema.ts
  - minion_hub/supabase/migrations/20260913020000_attachment_file_state.sql
  - minion_hub/src/server/services/attachment-lifecycle.ts
  - minion_hub/src/server/services/attachment-lifecycle.test.ts
  - minion_hub/src/server/services/attachments.lifecycle.sql.integration.test.ts
  - minion_hub/src/server/services/attachments.service.ts
  - minion_hub/src/server/services/attachments.service.test.ts
  - minion_hub/src/server/services/attachment-access.ts
  - minion_hub/src/server/services/file.service.ts
  - minion_hub/src/routes/api/attachments/sweep/tick/+server.ts
  - minion_hub/src/routes/api/attachments/authority.test.ts
  - minion_hub/src/routes/api/files/authority.test.ts
  - minion_hub/src/server/test-utils/attachment-postgres-fixture.ts
  - minion_hub/vitest.attachments-postgres.config.ts
  - minion_hub/src/server/test-utils/fixtures/attachment-catalog.json
  - minion_hub/src/server/test-utils/fixtures/attachment-auth-catalog.json
must_haves:
  truths:
    - File linking cannot race past record deletion or a committed file deletion claim.
    - External deletion failure remains retryable without returning the file to active state.
    - Booking deletion cleans attachment references inside its owning transaction.
---

<objective>Attachment deletion and link-race lifecycle</objective>

<execution_context>@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md; @minion_hub/CLAUDE.md; @.planning/operations/360/GAP-CLOSURE-2026-09-12.md; @proposals/2026-09-12-360-continuation-review-followups.md</context>

Owner: review_hub_takeover in isolated feature clone from actual Hub master77445c01. Root owns planning/admission/release; stock owner alone edits scheduling-bookings.service.ts and integrates the transaction-taking booking cleanup helper. Root approves necessary exact file amendments before scope expands. No active dirty checkout edits.
<tasks>
<task type="auto"><name>Task 1: Persist managed identity and deletion claims</name><action>Add one Hub-owned attachment_file_state table with immutable tenant/file/key identity and active/deleting lifecycle, claim metadata and actual RLS. Reserve migration20260913020000 distinct from stock10000. Safely backfill existing managed identities from authoritative refs/legacy attachment keys/category with duplicate/tenant preflight; do not delete customer records. Claim deletion and remove approved links in one SQL transaction before idempotent external object deletion. Never roll back to active after object deletion; retain failed/interrupted claims for bounded retry/reclaim, tied to the same immutable object. Describe consistency and error mapping without raw provider credentials. Prepare migration preflight and recovery/roll-forward contract for root review before production.</action><verify><automated>Native migration/tenant/claim rollback and delete failure/retry tests</automated></verify><done>Managed identity survives last unlink; interrupted deletion can finish safely without orphaned indefinite claims or object resurrection.</done></task>
<task type="auto"><name>Task 2: Serialize link admission and record cleanup</name><action>Use one documented order: actual record rows in stable type/id order, sorted file rows, then lifecycle/link mutations. Recheck link sets under lock and boundedly retry stale snapshots. No file-to-booking lock inversion. A new link must serialize with record deletion and refuse deleting files. Supply a transaction-taking booking cleanup helper to stock owner; it is called while the booking row is held before deletion. Add static allowlisted record-delete triggers for the eight supported CRM/scheduling/product/stock/invoice/POS target tables and CRM soft-delete transition in this same migration. The record row is already held; lock affected files in sorted order, persist managed identity, then remove only OLD tenant/record links. Keep explicit booking helper idempotent with its trigger. Pin function search_path, declare/review invoker versus definer privilege and test real RLS/ACL plus maintenance behavior. Never perform external I/O in triggers or use caller-controlled dynamic SQL. Native cases cover every supported type and CRM soft delete, not only booking. Preflight trigger lock impact and roll-forward/recovery before application. Sweeper first resumes deletion claims and never physically deletes newly linked or live referenced objects. Keep cron disabled until complete integration/release acceptance.</action><verify><automated>Real PostgreSQL deterministic link-versus-sweep, link-versus-record-delete, concurrent claim and cross-tenant cases</automated></verify><done>Observed committed DB state proves race outcomes; booking caller adoption and every supported record-delete disposition are explicit.</done></task>
</tasks>
<threat_model>Concurrent linkers/deleters, stale sweep snapshots, failure between SQL and object storage, missing records and stale retries. A database transaction cannot undo external blob deletion; durable claims and immutable object identity govern recovery.</threat_model>
<verification>Actual isolated PostgreSQL transactions/barriers/RLS and an owned fake object-store boundary with deterministic failure; no customer bucket deletion as a test. Required service/type/CI checks, independent review, migration preflight and exact runtime adoption precede closure. Scheduling activation is separate from proving implementation.</verification>
<output>09-08-SUMMARY.md and independent09-08-VERIFICATION.md. Exact source/fixture/test/release evidence; paired sourceTODO plus proposal for any remaining gap. Historical task denominator remains unchanged.</output>
