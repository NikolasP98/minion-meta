---
phase: 09-security-containment
plan: "09"
type: execute
wave: 2
gap_closure: true
status: admitted
autonomous: true
depends_on: ["09-07", "09-08"]
requirements: [SEC-01, SEC-02, SEC-03]
files_modified:
  - minion_hub/src/routes/api/servers/[id]/+server.ts
  - minion_hub/src/routes/api/servers/[id]/server.test.ts
  - minion_hub/src/server/auth/assistant-principal.sql.integration.test.ts
  - minion_hub/src/server/test-utils/fixtures/assistant-principal-catalog.json
  - minion_hub/src/server/test-utils/attachment-postgres-fixture.ts
  - minion_hub/src/server/test-utils/captured-postgres-catalog.ts
  - minion_hub/scripts/qc/principal-postgres-contract.ts
  - minion_hub/scripts/qc/principal-postgres-contract.test.ts
  - minion_hub/vitest.principal-postgres.config.ts
  - minion_hub/.github/workflows/ci.yml
must_haves:
  truths:
    - Every server ownership lookup failure omits raw exceptions and request identifiers from diagnostics.
    - Persisted membership and assignment revocation affect the next principal resolution.
    - Native schema evidence is distinguished from actual provider authentication.
---

<objective>Close residual server diagnostic leaks and qualify principal revocation on PostgreSQL</objective>
<execution_context>@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md; @minion_hub/CLAUDE.md; @.planning/operations/360/gap-closure/ACCEPTANCE.md</context>

Owner: review_hub_takeover. Preserve existing source authorization; no credentials, customer accounts or production SQL mutations. Root owns final release integration.

<tasks>
<task type="auto"><name>Task 1: Repair all three server ownership diagnostic catch paths</name><action>Reproduce credentials and request-ID leakage in the real PUT/DELETE handlers for each ownership lookup failure. Replace raw exception/request logging with stable sanitized stage codes. Preserve503 behavior and no unauthorized mutation. Repeat existing SEC01 fail-closed SQL route and principal/security regressions on the integrated candidate.</action><verify><automated>Six red-then-green real-handler sentinel cases plus existing focused regressions</automated></verify><done>All three diagnostic paths are covered and sensitive nested values are absent from response and captured logs.</done></task>
<task type="auto"><name>Task 2: Qualify principal revocation with actual native schema</name><action>Capture only the configured gateway/personal_agents/member_roles/permission_rules catalog and bounded FK/policy dependencies. Reuse the captured PostgreSQL fixture builder, preserving table definitions, defaults, constraints and appropriate captured roles. Execute real resolveAssistantPrincipal and installed Supabase query builder through an explicit local SELECT transport against owned PostgreSQL. A second connection commits membership removal, assignment removal and role override revocation; the next request must deny the corresponding authority. Add the exact suite to a strict native report guard. Distinguish application-principal/native-schema proof from actual PostgREST, JWT refresh, provider sessions or service-role RLS enforcement.</action><verify><automated>Strict native suite with zero skips; focused source tests; coherent required CI</automated></verify><done>Committed revocation is observed by real principal logic and remaining provider gates are explicitly recorded.</done></task>
</tasks>
<verification>No real-provider authentication claim from a local transport fixture. Record any new catalog function before executing it in the owned schema. Existing raw SQL containment remains closed without enabling typed analytics.</verification>
<output>09-09-SUMMARY.md and independent verification with exact source, schema and test receipts.</output>

Permission cache boundary: exercise the actual authorized mutation and invalidateRbac path before asserting immediate permission-revocation visibility. Direct external SQL changes obey the declared cache TTL and do not establish mutation invalidation. Membership and assignment next-request cases remain required.
