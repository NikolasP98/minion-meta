---
phase: 14-sdk-transport
plan: "15"
type: execute
wave: 2
depends_on: ["14-04"]
files_modified:
  - minion_hub/src/lib/plugins/compat.ts
  - minion_hub/src/lib/plugins/compat.test.ts
autonomous: true
requirements: [SDK-01, SDK-02]
readiness: "Private two-file typed compatibility classification admitted; component consumption remains14-06 ownership."
closure_policy: "Utility classification alone does not establish component, manifest, browser or deployment behavior."
must_haves:
  truths:
    - "Missing constrained facts remain denied and are classified pending."
    - "Known incompatible facts or malformed declarations take precedence over pending facts."
    - "Known empty methods differ from absent advertisement; unconstrained legacy remains compatible."
  artifacts:
    - path: minion_hub/src/lib/plugins/compat.ts
      provides: "Typed pending/incompatible discrimination inside the existing grammar"
  key_links:
    - from: minion_hub/src/lib/plugins/compat.ts
      to: minion_hub/src/lib/plugins/PluginIframe.svelte
      via: "14-06 consumes structured verdict without parsing prose or duplicating grammar"
---
<objective>
Distinguish pending gateway facts from known incompatibility while preserving fail-closed plugin mounting. Reuse the existing compatibility grammar rather than duplicating its version/parser logic in the component.
</objective>
<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
@.planning/phases/14-sdk-transport/14-06-PLAN.md
@.planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md
</context>
<boundaries>
Only compat.ts/compat.test.ts inside /tmp/minion-14-consumers-61p26r_z/minion_hub. Active Hub, component, other bridge files, source packages, locks and runtime remain unchanged. Root coordinates sibling component ownership. No full app suite, application environment, network, browser or library installation. Existing CalVer grammar remains authoritative; no new parser/dependency or broadened compatibility acceptance. Use root-owned exact single-file native runner/config/private cache. Preserve original before-images and complete existing test coverage. No branch/worktree/stash/commit.
</boundaries>
<tasks>
<task type="auto">
  <name>Classify denied compatibility results inside the existing utility</name>
  <files>minion_hub/src/lib/plugins/compat.ts, minion_hub/src/lib/plugins/compat.test.ts</files>
  <action>Add meaningful red cases before source. GatewayCapabilities.methods may be undefined to represent absent advertisement; [] is a known empty set. Null/undefined version is unknown, malformed supplied version is incompatible. Add mandatory kind:'pending'|'incompatible' on the false CompatVerdict branch; keep {ok:true} unchanged. Existing human reasons remain explanatory only. Missing facts matter only when a valid declared constraint needs them. Empty requiredRpc remains unconstrained. Invalid declarations, malformed supplied facts and known version/method/bridge mismatch outrank pending constraints when combined. Reuse existing version and bridge grammar; no copied parsing rules in component or tests. Explicit malformed method advertisements must deny rather than throw. No authorization may follow from kind; both false branches prohibit mounting/dispatch.

Test absent versus empty methods, unknown versus invalid version, invalid declaration precedence, mixed pending/known failures, unsupported bridge precedence, valid constraints and unconstrained legacy. Freeze exact source and native red/green/types for root.14-06 separately adopts this typed result and must test waiting/incompatible rendered states without changing utility logic. Update existing utility handoff comment to remain accurate for private versus active adoption.</action>
  <verify>Exact native compat.test.ts suite and focused strict types under root's private lane, scoped format/lint, independent source review; root later runs paired component/full Hub checks.</verify>
  <done>Structured classification is proven without changing deny semantics; dependent component tests and release gates remain explicit.</done>
</task>
</tasks>
<verification>
Root reviews typed output and precedence independently. A classification change cannot advertise gateway capabilities or certify a plugin manifest. Active installed behavior remains unchanged until coordinated adoption.
</verification>
<output>
14-15-SUMMARY.md records exact candidate identities and independent evidence; root owns global status and proposal integration.
</output>
