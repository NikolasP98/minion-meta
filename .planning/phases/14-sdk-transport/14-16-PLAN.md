---
phase: 14-sdk-transport
plan: "16"
type: execute
wave: 2
depends_on: ["14-13"]
files_modified:
  - "packages/shared/src/gateway/shells.ts"
  - "packages/shared/src/gateway/shells-outcome.test.ts"
autonomous: false
requirements: ["SDK-01", "AGT-04"]
readiness: "Two-file private implementation admitted after independent plan PASS and root exact runner/config/network-guard review; active adoption/emission excluded."
closure_policy: "Canonical source qualification does not enable durability, prove receiver readiness, or adopt an installed package."
must_haves:
  truths:
    - "A required durable invocation has a distinct method and an explicit strictly validated admission response."
    - "Fixed input and outcome policies use separate text limits, with nested runtime immutability."
    - "Existing legacy invocation, registration, outcome normalization and bilateral capability semantics remain compatible."
  artifacts:
    - path: "packages/shared/src/gateway/shells.ts"
      provides: "Additive method, required-admission response normalizer and immutable named v1 policy objects"
    - path: "packages/shared/src/gateway/shells-outcome.test.ts"
      provides: "Actual canonical response, profile, byte-boundary and compatibility fixtures"
  key_links:
    - from: "packages/shared/src/gateway/shells.ts"
      to: "minion/src/shells/invoke-contract.ts"
      via: "Later full emitted package and selective facade adoption; the input policy structurally matches the qualified parser policy"
---

<objective>
Define the smallest canonical contract needed for a client to require durable text invocation without an old gateway silently treating it as legacy execution. Publish explicit immutable input, outcome and reservation limits. Keep receiver storage, server configuration, caller dispatch and sender integration in their separately reviewed children.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/DECISIONS.md
@.planning/phases/14-sdk-transport/14-13-VERIFICATION.md
@.planning/phases/14-sdk-transport/14-14-VERIFICATION.md
@.planning/phases/14-sdk-transport/14-12-PLAN.md
@.planning/phases/11-agent-lifecycle/11-07-VERIFICATION.md
@packages/shared/src/gateway/shells.ts
@packages/shared/src/gateway/shells-outcome.test.ts
</context>

<boundaries>
Own only the two listed canonical files, initially in a fresh private copied-source candidate. Root owns admission, independent review, active adoption and global records. Do not edit the input parser, existing validation behavior, routes, configuration, manager, stores, bridge, barrels, package metadata, locks, dependency targets or active dist. Do not run database, browser, provider, socket, broad package or application suites. No network, installation, build/emission, package packing, branch/worktree changes, commits or publication. Preserve all concurrent WIP and earlier private snapshots. A fifth production surface is not implicitly admitted because a test exposes it.

The test/setup proposal below belongs to this plan. Its temporary files are runner configuration and receipts, not extra production ownership. No setup or source test has been executed for this draft.
</boundaries>

<selected_contract>

## Additive public API

Add `SHELLS_METHODS.invokeDurable = 'shells.invoke_durable'`; preserve every existing method value. An old gateway must reject this unknown method. A caller requiring durability must never retry through `shells.invoke`.

Add `ShellsInvokeDurableParams` as the existing invoke fields with `input` narrowed to the existing `{ kind: 'text'; text: string }` member. Reuse the existing type members; do not add a second parser, organization payload, admission payload, input version or durability flag. Keep `ShellsInvokeParams` and `ShellsInvokeResponse` unchanged, including legacy multimodal typing.

Add `ShellsInvokeDurableResponse` with exactly:

```ts
{ version: 1; durability: 'required'; runId: string; startedAt: number }
```

Add `normalizeShellsInvokeDurableResponse(value: unknown): ShellsInvokeDurableResponse`. It uses the fixed v1 outcome limits and existing private object/string/integer/version/bounded validation helpers. Do not accept a caller-supplied policy or infer a version. Require own enumerable data fields; reject unknown fields, symbols, accessors, arrays and unsupported prototypes using the canonical object boundary. Missing fields and explicit `undefined` are invalid. Accept only literal numeric `1` and literal `'required'`. `runId` follows existing canonical identifier semantics: nonempty, well-formed UTF-16, at most 256 UTF-8 bytes, preserving bytes. `startedAt` follows existing safe nonnegative integer semantics, including normalization of negative zero to zero. Return a fresh canonical object in fixed field order and bound its complete serialization. Do not claim arbitrary Proxy objects are a security sandbox.

The response means the gateway durably admitted the run. It does not prove ACP execution, completion, sender delivery, tenant authority or store readiness. Exact keyed recovery must later return the original `runId`/`startedAt`. Keep the existing register response and `negotiateShellDurableOutcomeVersion` behavior unchanged: `undefined` remains legacy absence; explicit malformed advertisement remains invalid even when its peer is absent. This child does not advertise a ready receiver.

## Fixed immutable profile

Use direct `Object.freeze` calls and readonly literal inference for the finite known objects. No generic deep-freeze utility, configurable policy framework, new dependency or runtime negotiation mechanism.

Export these exact named objects:

```ts
SHELL_DURABLE_V1_INPUT_POLICY = {
  contract: { identifierBytes: 256, textBytes: 65536, recordBytes: 524288 },
  organizationBytes: 256,
  invocationKeyBytes: 256,
  envelopeBytes: 524288,
}
SHELL_DURABLE_V1_OUTCOME_LIMITS = {
  identifierBytes: 256,
  textBytes: 4096,
  recordBytes: 65536,
}
SHELL_DURABLE_V1_PROFILE = {
  version: 1,
  input: SHELL_DURABLE_V1_INPUT_POLICY,
  outcome: SHELL_DURABLE_V1_OUTCOME_LIMITS,
  maxOutcomeBytes: 57344,
  maxReceiptBytes: 8192,
  maxCombinedOutcomeBytes: 65536,
  maxAdmissionMetadataBytes: 16384,
  reservedRunBytes: 81920,
}
```

Freeze the nested input `contract`, its enclosing policy, outcome limits and enclosing profile. Preserve shared reference identity for `profile.input` and `profile.outcome`. The qualified 14-14 parser consumes `contract.textBytes`; assigning the outcome policy to the input contract would incorrectly reduce supported input from 64 KiB to 4 KiB. A future facade consumer can pass the input object structurally without importing gateway types into shared.

The 80 KiB reservation consists of 64 KiB for the entire outcome/receipt aggregate, including its wrapper, plus at most 16 KiB for complete admission metadata. Nested 56/8 KiB limits alone are insufficient: the aggregate must also fit 64 KiB. Constants do not enforce storage reservations; receiver implementation must enforce these obligations transactionally. The metadata ceiling requires an actual receiver serialization fixture later. No raw prompt storage, physical disk quota, WAL/index allowance or measured production capacity is implied.

Global/per-organization retained-run defaults of 4096/1024 belong to later server configuration and receiver policy. Do not place deployment retention counts in this wire profile. Do not change existing caller-supplied canonical normalizers to force the new profile globally; the explicit durable path adopts these constants later.
</selected_contract>

<tasks>
<task type="auto">
  <name>Task 1: Freeze the private setup and demonstrate contract failures</name>
  <files>packages/shared/src/gateway/shells-outcome.test.ts</files>
  <action>After root admits the exact setup, prepare a fresh private copy and preserve complete baseline inputs before changes. Add tests against actual canonical exports. Keep all existing 46 cases and classify missing-export/setup failures separately from semantic assertions. Record baseline behavior and the new red cases before implementing source. A fixture that only reproduces the intended implementation is insufficient; retain adversarial validation and independently specified byte-boundary cases.</action>
  <verify>Run only the existing native Vitest lane selecting src/gateway/shells-outcome.test.ts with the corrected createVitest runner and actual pre-start private-cache assertion. Keep the first failure log intact.</verify>
  <done>Root can identify the unchanged baseline, exact test additions, private runner configuration and concrete reasons the new contract fails.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the additive contract and prove its boundaries</name>
  <files>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/shells-outcome.test.ts</files>
  <action>Implement only the selected API/profile. Reuse canonical helpers rather than duplicating validation. Add exact-site TODO(handoff) comments for future required caller selection/profile adoption and receiver admission/response readiness, pointing to proposals/2026-09-08-platform-qc-remediation.md and the selected root decision. Root owns the matching proposal update.

Required tests:

1. Exact new method string, unchanged legacy method, typed text-only durable params, unchanged legacy text/multimodal invoke and legacy register response. Include compile-time negative expectations for multimodal durable input and mutation of every readonly profile layer; do not execute those type-only mutation cases.
2. Actual response normalization accepts the exact shape, null-prototype own data, valid Unicode and safe timestamp boundaries; returns a fresh canonical copy. Reject absent/undefined/null/string/boolean/boxed/unsupported/fractional/nonfinite version values; reject non-required durability; reject missing/undefined fields, unknown fields, symbols, hidden properties, array/custom prototype, invalid identifiers, malformed Unicode, oversize UTF-8 and invalid timestamps. Keep bounded canonical error messages independent of malicious field contents.
3. Accessor and coercion canaries do not execute. Inherited required fields cannot become accepted response data. Any temporary Object.prototype canary uses try/finally restoration; avoid broad lint suppression. Validate actual plain-data boundary rather than inventing a recursive hostile-object engine.
4. Every named object and nested input contract is runtime-frozen. Reflect.set/Reflect.defineProperty return failure and bytes remain unchanged; assert shared nested reference identity and exact values. Do not mutate an exported object as a test setup dependency.
5. Feed the actual canonical outcome, receipt and combined-response normalizers maximum escaped fields: five 256-byte outcome identifiers; four 256-byte receipt identifiers; both 4096-byte outcome text fields; maximum safe integer timestamps/duration; longest terminal state; fixed valid digests. Use U+0000 to maximize JSON escaping per UTF-8 byte. Assert complete serialized outcome 57161 B, receipt 6323 B, combined response 63540 B and wrapper 56 B. Assert each nested cap, aggregate cap and logical reservation sum separately. Exact byte limits accept; one-byte-smaller limits reject. Non-ASCII and unpaired-surrogate cases prevent character-count substitutions.
6. Prove 64 KiB input and 4 KiB outcome policies are distinct and structurally valid through existing helpers. Do not claim this executes the gateway input parser; its actual profile consumption belongs to facade/consumer adoption. Preserve existing parameterized-limit behavior and all bilateral missing/malformed cases.

The capacity fixture derives from /tmp/minion-durable-capacity-m027f5ne/probe.mjs, SHA 2d3567f6d819346c6c41a4497e0ccfee5e6e652849bbd2622232fa3e1bfd41a9. Its emitted-package result SHA is 1ff3dfdeebd504e83a055dcd0180ef4dd1ae8cfc1ca2078bba9c67aba0f29636; root independently reproduced byte-identical output. Re-express assertions against this source candidate's real functions, never copy the normalizers. This is payload feasibility, not outer arbitrary RPC-id sizing or production storage measurement.</action>
  <verify>Focused native suite passes with retained red evidence. Run strict package no-emit types and the installed package's scoped oxlint for the two owned files only. Record any tooling/setup failure independently.</verify>
  <done>The required response and immutable profile are testable canonical source, with unchanged legacy contracts and no runtime activation.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze source for independent review and later package qualification</name>
  <files>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/shells-outcome.test.ts</files>
  <action>Freeze the two candidate files and exact source/test/config/tool manifests. Verify all unowned source inputs, linked dependency targets and active source/artifact/cache identities against before-images. Confirm the existing browser-safe barrels export the new symbols without editing barrels and that no Node builtin/import/dependency was added to the canonical module. Separate source typing from emitted/public declaration evidence: do not generate declarations here. Root reviews/adopts exact source bytes and owns SUMMARY, VERIFICATION, proposal and inventory updates. A later 14-11 full-package emission and package consumer parity must include this new contract.</action>
  <verify>Provide exact native commands, focused counts, error behavior, before/after digests and diff checks. Root independently repeats focused suite/types after freeze.</verify>
  <done>Root has a bounded two-file candidate and reproducible private evidence; installed packages and durable runtime remain explicitly unmodified.</done>
</task>
</tasks>

<private_setup_proposal>

## Fresh isolated execution proposal

After root review, create exactly one directory with native mkdtemp prefix `/tmp/minion-14-16-`, record its full resulting path as `TASK_ROOT`, and use `TASK_ROOT/shared` as the candidate. This is a proposed path allocation, not a preexisting/admitted directory.

Copy the complete current `packages/shared/src` tree, `package.json` and `tsconfig.json` from the canonical workspace; record membership and SHA-256 for every input. Do not copy .env files, active dist, caches, logs or tsbuildinfo. Preserve original copies under `TASK_ROOT/before` and plain receipts under `TASK_ROOT/checks`. The older 14-13 snapshot.json records pre-14-13 source hashes, so it is not the current source authority.

Create six individual dependency links matching the installed 14-13 recipe: `@minion-stack/tsconfig`, `@types/ws`, `oxlint`, `typescript`, `vitest`, `ws`. Verify their current real targets and freeze required compiler/config/tool dependencies and binaries. Never link the whole node_modules directory or reuse its mutable cache entries. Create real private directories `node_modules/.vite`, `node_modules/.vite-temp` and `private-cache`; verify containment and absence of cache symlinks before and after tests.

Copy the corrected existing runner `/tmp/minion-14-13-cuet0lis/shared/run-contract.mjs` (SHA aa95a3ade8c018ad98954a7929a074dbf529addfd708eb0422778a079eb0e4e3) and `vitest.config.ts` (SHA bcbdb343bcb087ce338418ecfc0d6297a23505a7369dacf095040fd20b67ae4a) as setup templates. The runner uses createVitest, reads `ctx.server.config.cacheDir` before `ctx.start`, and requires the actual path to equal this new root's `private-cache`. It closes the context in finally. Do not use the earlier invalid post-run `ctx.vite` accessor or launch a server. Keep one selected file, one worker, environment loading disabled and both optimizers disabled.

Propose one explicit setup addition: `network-guard.ts` in the private candidate, referenced through native `test.setupFiles`. Guard outbound net.Socket.connect, node:http/https request/get, fetch, WebSocket and datagram creation with fail-fast counters; refuse server listening. Assert no attempted use after each test, with all guards installed before importing source. This guards the native Node test lane; do not claim it proves all conceivable OS egress. Setup must not alias canonical modules, replace validators or emulate protocol behavior. Root reviews the exact runner/config/guard bytes before execution.

Run commands from `TASK_ROOT/shared`, substituting the recorded absolute path for TASK_ROOT:

```sh
env -i PATH=/usr/bin:/bin HOME="$TASK_ROOT" LANG=C.UTF-8 /usr/bin/node run-contract.mjs
env -i PATH=/usr/bin:/bin HOME="$TASK_ROOT" LANG=C.UTF-8 /usr/bin/node node_modules/typescript/bin/tsc --noEmit --incremental false --composite false
env -i PATH=/usr/bin:/bin HOME="$TASK_ROOT" LANG=C.UTF-8 /usr/bin/node node_modules/oxlint/bin/oxlint src/gateway/shells.ts src/gateway/shells-outcome.test.ts
```

The composite/incremental overrides keep typechecking from creating a cache outside the explicit private setup; they do not emit runtime/declarations. Verify the existing oxlint entrypoint/native binary and exact command before use; do not install a companion or invent a different lint policy if local setup differs. Redirect ordinary logs only into TASK_ROOT/checks. Existing source style should remain scoped; no whole-file mechanical format churn. Capture active cache bytes/mtimes before and after and distinguish an earlier receipt from a fresh assertion.

Current canonical source identities at draft time: shells.ts e480c90660f7c5936a32be9c933c857223077a893d3d66d0ad4bcc086ebaa8f7; shells-outcome.test.ts 3e8360cfa43e1b11fd12e8efb5f19176e26528b4afb1a1a6cd36c2d813280b49. Current root branch is feat/curated-engineering-skills (read-only observation). Recheck all identities at admission and report drift rather than replacing unrelated changes.
</private_setup_proposal>

<verification>
Source acceptance requires meaningful red/green canonical fixtures, strict types, scoped lint, immutable limits, complete escaped aggregate proof, zero guarded network attempts and unchanged unowned/active artifacts. No minimum-runtime, installed-consumer, physical capacity, receiver persistence or delivery claim follows. Existing parameterized validators remain reusable; callers deliberately select the new profile through a later admitted consumer change.
</verification>

<output>
Root receives the exact candidate/source and setup hashes for independent review. Runtime/public declaration qualification belongs to a subsequent complete 14-11 artifact. Gateway facade, receiver14-12, caller/config/lifecycle and sender11-03 remain separately admitted, acyclic work; this child neither executes nor closes those gates.
</output>


## Root implementation admission

Independent reviewer jobs_fencing_execute passed draft588a653594f2fb1810bea03a4a8f2ccc71911eacdf14f66ab2f4fd6ee7acfd84. Root inspected native runneraa95a3ade8c018ad98954a7929a074dbf529addfd708eb0422778a079eb0e4e3, configafba5a204ca0d4a7c4f8da02d69b0d3cdcd04392b6774bfc903dc10ee87502e8 and guard07cf249d0caa3105b040b077102baa0cf7ea3c21c2e23443974362840d4dc53d. Admit Tasks1–3 only in `/tmp/minion-14-16-2imdqkr6/shared`. Preserve31source/2metadata inputs, six dependency links and private caches; source ownership remains exactly two canonical files. Existing46 cases must execute alongside additions, with zero skips, collected test count and no unhandled errors. Root owns active adoption, complete package emission, global updates and independent verification.
