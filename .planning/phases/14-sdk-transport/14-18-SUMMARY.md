---
phase: 14-sdk-transport
plan: "18"
requirements: ["SDK-01", "SDK-02", "UI-03", "UI-07"]
requirements-completed: []
status: locally-qualified
---

# Real Site page and SDK session integration

The ten-file candidate is frozen at `/home/nikolas/.cache/minion-qc/14-18-y658krut/minion_site`. Its freeze manifest SHA256 is `f9f5ccfe6c1e9fb5e929453ac592ae3c39fb5ea3625d4737fded4c29f76cb70c`. Root independently compared every source file and frozen copy, all 44 installed SDK members against the complete archive, all 48 emitted artifact files, and repeated all 22 service tests without skips. Root receipt: `../checks/root-identity-review.json`.

The native Bun manifest/lock transaction selects complete archive C, SHA256 `d01a528579e7b0877d05565b9d3ad2747ab45610d634948f51b6a25b4bf4f756`. Its metadata is actually version 0.9.0; this private digest-qualified file dependency is not registry 0.12.0. All 27,738 unrelated dependency files remain unchanged, with root ws8.20.0 and nested HappyDOM ws8.21.3 preserved. The repaired service remains byte-identical to qualified 14-10, SHA256 `fb7a2a48c0ad68870b50e25db961f9932c73f0bbc0878212cd53015dbe54b823`. No new product state or service mock was introduced.

The browser fixture compiles the actual members page, state, service, installed client, native components and actual Barlow fonts. An owned loopback HTTP endpoint returns synthetic signing responses; native browser WebSocket talks to an owned ws server. Open alone cannot enable chat. Challenge, signing and correlated hello establish the session through the real client. Tests cover current streaming, reversed history responses, reconnect, no send replay, safe failures and mobile/desktop manual tabs. All 18 cases pass across Chromium, Firefox and WebKit, zero skips. This is a synthetic server contract, not real gateway authentication or cryptographic authorization.

The separate 22-case service suite retains retired-callback timing and a real rune subscriber sequence `[false,true,false,true,false]`. The 56 ordinary Site tests also pass. Full native Bun sync and Svelte check report zero errors/warnings; both installed build guards record zero network attempts. Scoped format/design checks pass. A native one-case browser subset returns failure despite that case passing; altered artifact bytes reject before server listen, and six reporter controls reject incomplete/duplicate/skipped evidence. The final browser spec differs from its passing run only in recorded Prettier whitespace; the portable 13-14 run will execute the final version again.

Three setup failures are retained separately from product results. Initial namespace-only Bun installation attempted host resolver/address-selection sockets; stronger bwrap isolation then passed without those calls. Temporary-directory EDQUOT interrupted an unexecuted new builder; the complete candidate was copied to private home-disk storage, preserving original evidence. Relocation under `.cache` exposed Vitest3's wildcard transform matching: it selected server compilation and the real subscriber failed. The exact absolute fixture path plus a fail-fast post-transform client proof fixes configuration without altering product state. Root's 22-case replay confirms the client transform and subscriber behavior.

Original UI and service snapshots, active repositories, package indexes and prior receipts remain preserved. Applicable SDK license/attribution, distinct immutable publication, hosted CI, real authentication/provider revocation and deployment remain open. Plan 13-14 now combines this candidate with portable CI automation; source and workflow qualification must remain distinct from an actual hosted run.
