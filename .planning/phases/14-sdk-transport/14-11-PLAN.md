---
phase: 14-sdk-transport
plan: "11"
type: execute
wave: 2
depends_on: ["14-08", "14-13"]
files_modified:
  - ".planning/phases/14-sdk-transport/14-PACKAGE-CANDIDATE-DECISION.md"
  - ".planning/phases/14-sdk-transport/14-11-SUMMARY.md"
autonomous: false
requirements: ["SDK-01", "SDK-02", "DEP-03"]
readiness: "Earlier reconnect and durable artifacts independently qualified. Fresh14-16 complete input/script freeze reviewed; native emission and qualification admitted for1ss7iase only."
closure_policy: "Unpublished candidate qualification cannot establish registry, active-installed or production identity."
must_haves:
  truths:
    - "Consumer runtime and types originate from the same independently verified candidate artifact."
    - "Active shared dist, manifests, locks and every active node_modules tree remain unchanged."
    - "The complete exported runtime/declaration dependency closure is identified, not just client.ts or index.js."
    - "A same-version different-byte candidate is explicitly unpublished and rejected as an immutable production identity."
  artifacts:
    - path: ".planning/phases/14-sdk-transport/14-11-SUMMARY.md"
      provides: "Exact temporary artifact locations, source/tool/archive/extracted digests, checks and non-adoption limits"
  key_links:
    - from: ".planning/phases/14-sdk-transport/14-11-SUMMARY.md"
      to: ".planning/phases/14-sdk-transport/14-09-PLAN.md"
      via: "Root-owned isolated Hub snapshot consumes exact extracted artifact runtime and types"
    - from: ".planning/phases/14-sdk-transport/14-11-SUMMARY.md"
      to: ".planning/phases/14-sdk-transport/14-10-PLAN.md"
      via: "Root-owned isolated Site snapshot consumes the identical extracted candidate"
---

<objective>
Produce one locally emitted and packed @minion-stack/shared candidate for the admitted authenticated-session client. Give root a complete runtime/declaration identity for isolated Hub/Site tests and typechecks without replacing active packages. Prepare the exact remaining immutable-adoption gate; do not execute it here.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/12-dependency-provenance/12-03-PLAN.md
@.planning/phases/14-sdk-transport/14-02-PLAN.md
@.planning/phases/14-sdk-transport/14-08-SUMMARY.md
@.planning/phases/14-sdk-transport/14-09-PLAN.md
@.planning/phases/14-sdk-transport/14-10-PLAN.md
@.planning/phases/14-sdk-transport/14-PACKAGE-CANDIDATE-DECISION.md
@packages/shared/package.json
@packages/shared/tsconfig.json
@packages/tsconfig/library.json
@packages/tsconfig/base.json
</context>

<boundaries>
The executor owns only the two receipt files above and a newly created private temporary directory `/tmp/minion-14-11-XXXXXXXX` (record actual mkdtemp result). All candidate, command, verification and archive files live there. No persistent QC framework, source, manifest, version, changeset, lock, config, active dist, tsbuildinfo or node_modules edit. No installs, fetches, registry lookup/publication, deployment, application/browser/server start, real socket/HTTP/provider/telemetry or credentials. Do not read application .env or user package-manager auth configuration into logs. Use allowlisted PATH/HOME/LANG and a temporary HOME with no npmrc; preserve enough explicit installed-tool paths to avoid bootstrapping Corepack or downloading runners.

Consumer snapshots and their dependency replacement are root-owned and require their own admission; this executor does not create or mutate them. No symlink may point a writable candidate dependency location at active node_modules. Root serializes source freezes and expensive checks. Recheck source and compiler inputs before and after emission; concurrent change means discard qualification and request a fresh frozen selection, never overwrite others. Existing shared source is broader than 14-08; capture all inputs without claiming all package functionality has been behaviorally qualified.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze emission inputs and preserve active artifacts</name>
  <files>.planning/phases/14-sdk-transport/14-PACKAGE-CANDIDATE-DECISION.md</files>
  <action>Require independent acceptance of 14-08's exact source/tests and root selection of the isolated-output path in the packet. Create one private mkdtemp directory with before/, build/, workspace/packages/shared/, workspace/packages/tsconfig/, archives/, extracted/, checks/, logs/ and a local tool-home/. Copy active shared dist, package manifest and any existing shared tsbuildinfo into before/ without removing or modifying originals; absence is a recorded baseline. Copy source inputs into before/source for review. Store sorted relative path/size/SHA-256 manifests plus the digest of each manifest. Inventory every shared src file, including tests, package/tsconfig files, the resolved @minion-stack/tsconfig package/configs, compiler runtime package files used, root lock/workspace/package identities and actual Node/pnpm/TypeScript versions/resolution. Do not infer installed compiler version from the declared range: currently local TypeScript resolves to 5.9.3 under node_modules/.bun despite native invocation through pnpm.

Use root's selected whole-input digest as the execution freeze, with client source c0c492d4e03f0a26ae9afd5b9993bd66c82451f7289670cfed411344ac677666 as a necessary but insufficient pin. Copy unchanged shared and tsconfig manifests into the temporary staging workspace. Copy existing package README/LICENSE/NOTICE only if present and ownership is established; record absence. Add only a temporary minimal private workspace root manifest and pnpm-workspace.yaml listing those two package directories, so pnpm can resolve workspace:* when packing. No dependencies are installed or linked there. Record the temporary scaffolding as packaging context, not a product manifest change. Freeze the packed manifest's expected changes: native pnpm workspace devDependency rewrite only; every unexpected script/dependency/export/file-policy change rejects the candidate. Current manifest has no prepare/prepack/postpack script; reject drift that introduces one until separately reviewed. Do not assume prepublishOnly is an emitted-artifact guarantee.</action>
  <verify><automated>Read-only hashes and manifest comparison; retain the exact shell/Node/Python commands and outputs under the selected temporary directory. No tool run with zero inspected files counts.</automated></verify>
  <done>Root can review exact whole-source and old-artifact manifests, temporary locations/tool identities, and known license/README gaps before admitting emission.</done>
</task>

<task type="auto">
  <name>Task 2: Emit and pack one isolated candidate using installed native tools</name>
  <files>.planning/phases/14-sdk-transport/14-11-SUMMARY.md</files>
  <action>After root releases Task 1's freeze, invoke the existing package build from the meta root using installed pnpm with only output/cache paths overridden:

`pnpm --filter @minion-stack/shared run build --outDir "$candidate_dir/workspace/packages/shared/dist" --tsBuildInfoFile "$candidate_dir/build/shared.tsbuildinfo" --noEmitOnError true`

Here candidate_dir is the exact mkdtemp path, not a fixed shared directory; record expanded argv. The temporary output begins empty. Do not use build-all or active `pnpm build` without the overrides. Keep declarations, declaration maps and JS maps as declared; list all emitted files, including currently included *.test outputs. Never filter them silently to manufacture a release-ready archive. Capture compiler exit and verify active dist/cache/input manifests unchanged. A failed compiler run rejects every partial output.

From the staged workspace/packages/shared directory run installed `pnpm pack --pack-destination "$candidate_dir/archives" --json`, with synthetic HOME/environment and no inherited npmrc, scripts or credential configuration. Installed pnpm 10.15.0 help confirms these pack flags; actual pack behavior and workspace rewrite are execution checks, not pre-certified facts. There must be no build/install lifecycle invocation or network request; unexpected hook or dependency-resolution requirement is a stop gate. Read the tarball's actual member list and packed package.json without executing content. Record archive SHA-256, byte size, packed manifest digest, full member list and per-file SHA-256. Reject absolute/traversal paths, links, duplicate normalized members, unexpected files or missing targets before extracting into extracted/package. Extraction must create only ordinary files/directories contained within extracted/. No external link or active source alias may satisfy a missing archive member.

For each of ., ./gateway, ./utils, ./node and ./brain-vector, verify every exported import/types target exists inside the archive. Trace and hash all transitively referenced emitted JS and declarations; classify relative imports, Node builtins and external peers separately. Record declarations' type-only external imports too. Confirm the declaration and runtime contain the optional authenticated callback/generation contract through the public root, gateway and Node option surfaces, not via a private source import. Full files and closure manifests identify the candidate even where only client behavior is tested. Map-only references outside the archive are recorded as source-map provenance/packaging debt, never followed into unrelated directories.

Run read-only import smoke probes from the extracted artifact for browser-safe exports with no browser socket instantiated and no ws peer available: those exports must resolve without loading ws or Node-only modules. Separately qualify ./node only in root's later peer-equipped isolated consumer, preserving required runtime ws despite its optional package metadata. Current ws source range is ^8.0.0; record installed ws and @types/ws manifests and actual resolution for that later fixture. No opportunistic peer installation here.

Inspect actual archive license text and ownership. Current metadata says MIT but source package/root LICENSE and declared README are absent; absent shipped text means release/provenance check FAILED. Root may explicitly allow an unpublished local candidate for these tests while keeping DEP-03/release blocked. Do not create legal text or relabel that result PASS. Retain native pack's workspace:* transformation and exact differences for root review. No same-version candidate is accepted as production identity.</action>
  <verify><automated>Build and pack commands above; source/active-artifact before-after digest equality; extracted archive export/runtime/declaration closure checks; offline import probes. Save command argv, runner versions, exit statuses and logs.</automated></verify>
  <done>Root receives one immutable-by-digest extracted local candidate with exact declarations/runtime, independent active-baseline receipt and explicit license/peer/release failures. A missing export/declaration rejects even test handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Verify candidate handoff and bound later installed adoption</name>
  <files>.planning/phases/14-sdk-transport/14-11-SUMMARY.md, .planning/phases/14-sdk-transport/14-PACKAGE-CANDIDATE-DECISION.md</files>
  <action>Prove the receipt rejects a changed member or declaration, missing transitive export file, wrong archive digest and a second byte identity offered under the same production name/version. These checks operate on separate temporary copies; preserve the selected archive. Do not invent a broad package registry verifier; reuse the exact native/hash/archive checks from Task 2 and retain their failing results.

Root then chooses the exact archive digest and explicitly amends 14-09/10 to permit candidate execution in root-owned isolated snapshots. Each snapshot must materialize the entire extracted package as a private dependency copy at node_modules/@minion-stack/shared (or an equivalently reviewed snapshot-only package resolution), with no active dependency tree mutation and no client-only source alias. Capture Node/Vite runtime resolution and TypeScript trace/declaration resolution from the snapshot. Both must land on that same copy; compare full file manifests to the archive. Preserve the old active installed digest as a separate observation. Do not map runtime to candidate while allowing types to resolve to old0.9.0, or map only the client while other root exports leak from installed/source paths.

Actual consumer execution remains owned by 14-09/10/root: their real service contract suites and full isolated Svelte checks must run against the selected extracted package, with their existing synthetic transport/network isolation. Their summaries record snapshot source/lock/compiler/config/artifact identities and exact command outcomes. Root separately decides whether/how snapshot service fixes return to active source; current installed declarations do not acquire the new callback just because isolated tests pass. This plan's completion can unblock those isolated checks but cannot claim their results in advance.

Finish with a concrete later immutable-adoption gate: root assigns one writer per actual shared package version/changeset and Hub/Site/Paperclip manifest/lock transaction; chooses distinct never-reused release version, resolves license/README/shipping scope and peer failures, reruns pack identity/provenance and exact consumer tests, and obtains registry/deploy authority separately. Do not name an unverified registry version as available. Pin archive content digest for the local candidate, with manifest0.9.0 labeled unpublished-candidate only. A production inventory presented with two different byte identities for the same name/version must reject it. Paperclip's older installed shared and ws/Node surface remain an explicit remaining consumer gate.

Workforce-client and plugin-ui-bridge are independent package candidates with their own source, manifest, transitive runtime/declaration exports, archive/license/peer/version and each installed consumer receipt. The prior plugin dist/index.js digest and Workforce client.ts digest are not tarball identity and cannot be reused as this shared candidate. Their adoption stays separately admitted. Record Standards/Spec review and all unmet gates; root owns global/proposal/consumer snapshot changes.</action>
  <verify><automated>Exact temporary tamper/missing-member/type mismatch/version-conflict checks reject; local source, dist, manifests, locks and active node_modules remain unchanged. Root validates consumer receipts independently under 14-09/10.</automated></verify>
  <done>Candidate packet names one archive/extracted digest, complete runtime/type closure and actual limitations. Root can admit isolated consumer work without claiming published/installed adoption. Release and other-package gates remain open.</done>
</task>

<task type="auto">
  <name>Task 4: Prepare the next full candidate with the verified durable contract</name>
  <files>.planning/phases/14-sdk-transport/14-PACKAGE-CANDIDATE-DECISION.md, .planning/phases/14-sdk-transport/14-11-SUMMARY.md</files>
  <action>Create a new private /tmp/minion-14-11-XXXXXXXX directory; preserve the original reconnect archive and all its checks unchanged. Repeat the complete Task 1 input, compiler, tool and active-artifact inventory for the current shared source after independently verified11-07 and14-13. The expected canonical input changes from the old candidate are shells.ts and the new shells-outcome.test.ts; compare full membership/content rather than assume these are the only changes. Record every changed external compiler/config/root metadata input. Shared14-08 client source remains unchanged. Root must compare and accept the new complete freeze before emission.

After separate root release, use the already qualified native pnpm/TypeScript path and temporary output overrides from Tasks2/3. Preserve native packed manifest normalization (workspace tsconfig version and removal of prepublishOnly), ordinary same-private-workspace metadata link, no active dependency/cache mutations, no network or lifecycle scripts. Repeat the actual archive member/runtime/declaration closure and all negative checks. Add public package entry probes for durable normalizers and bilateral negotiation, valid legacy/v1/invalid combinations, and matching public declarations without source aliases. A Node-only ws optional-peer failure still cannot certify a Node consumer. Shipping tests/maps and missing license/README remain explicit release gates. This is a new unpublished0.9.0 content identity, never an overwrite of the previous candidate or an immutable production release.

Root independently verifies the artifact and decides which isolated gateway parity/input child consumes it. Do not change the already qualified Hub/Site snapshots or their package bytes; their receipts remain tied to the older reconnect archive. No source, package adoption, publish, install or production change.</action>
  <verify><automated>Full selected input and active-artifact manifests, native emission/pack exit codes, actual member/declaration/runtime checks and separate archive digest.</automated></verify>
  <done>A new exact full shared artifact includes the durable foundation and bilateral contract, with prior consumer evidence and active installed bytes preserved.</done>
</task>

</tasks>

<verification>
No broad test/build fanout. Native build must exit successfully and current shared input bytes remain frozen. Runtime smoke does not replace the 14-08 actual client behavior suite or 14-09/10 real consumer suites. Any source or installed dependency/config drift invalidates inherited results. Source maps, shipped tests, missing README/license and optional ws are recorded separately from reachable browser runtime closure. A tarball SHA records container bytes; extracted member digests record content identity. Root independent review must check both. Do not claim byte-reproducible tarballs from one pack run.
</verification>

<output>
14-11-SUMMARY.md with exact selected plan/input hashes, absolute temporary paths, preservation manifest, compiler/native pack argv and identities, tarball/extracted closure digests, negative checks, license/peer/export findings, and root-owned consumer/adoption gates. This draft authorizes no build, pack, installation or source change.
</output>

## Root candidate preparation admission

Task 1 is admitted on the independently verified 14-08 source. Use the exact mkdtemp template and boundaries above. Root explicitly permits unpublished local test use while missing shipped license/README and same-version production identity remain failed release gates. No legal text is invented and no production identity is accepted. Tasks 2/3 await the actual whole-input receipt and root emission release; 14-09/10 snapshot source implementation remains separate.

## Root emission release

Task 1 whole-input manifest checks/whole-input.json SHA-256 f214dc035ae5007f3643da19aef7e63552b80e1eac1a919cdb2ceb6d495dd0ca at /tmp/minion-14-11-84XpBnCA is selected. Root verified its five nested manifest hashes. Tasks 2/3 are now admitted within that exact temporary directory. Use the pinned direct cached pnpm10.15.0 executable to avoid the PATH wrapper's tool bootstrap; no network/install or active output write. Rehash actual inputs and preserved active artifacts immediately before and after emission/packing; any drift invalidates handoff. All earlier release/license/production-identity gates remain unchanged.

## Root staged pack metadata-link amendment

The first isolated emission succeeded with active inputs/output preserved. Native packing failed with ERR_PNPM_CANNOT_RESOLVE_WORKSPACE_PROTOCOL because the staged tsconfig dependency was not installed; no archive was created. Preserve that failure and the initial staging manifest.

Root admits exactly one temporary resolution link: /tmp/minion-14-11-84XpBnCA/workspace/packages/shared/node_modules/@minion-stack/tsconfig points to /tmp/minion-14-11-84XpBnCA/workspace/packages/tsconfig. Verify the target is the previously staged ordinary directory with unchanged0.1.0 package metadata and resolves inside this private workspace. This is a metadata-only native pack prerequisite, not dependency installation or a link to active node_modules. Record link path/target and a separate amended staging manifest; do not relabel the original input receipt. Repeat the identical pinned native pack command once, then continue the admitted archive checks on success. Any new dependency, script or external-resolution requirement remains a fresh gate. No source/active manifest/lock/version or installed-tree change is authorized.

## Root native packed-manifest normalization acceptance

Root inspected tarball minion-stack-shared-0.9.0.tgz (72,021 bytes), SHA-256 b896e1df2d7da596f6f6dd0f3d2a26f225ffbe11587d95fae1deb233818595e9. Its only native manifest changes are workspace:* tsconfig devDependency to0.1.0 and removal of scripts.prepublishOnly. Accept both exact native normalizations for this unpublished artifact; retain source and packed manifests separately. No repack, source metadata edit or extra normalization is admitted. Continue extraction/closure/negative checks against these existing archive bytes.


## Root Task 4 input-freeze admission

Only preparation/inventory is admitted until root selects the new full-input digest. Canonical14-13 independent verification SHA0721aa8d3b742fbca6dcbf815490f00b2637dff054434621ec1e50a5a926cc2a passes46 tests and strict types. Reuse the earlier pinned native pnpm executable; reject drift instead of fetching. Exclude mutable test caches from dependency links. This amendment's dependency on14-13 applies to the new durable candidate; the historical reconnect candidate was qualified before that child and remains unchanged.


### Root Task 4 emission release

Root independently rehashed all12 bound manifests and their current/copy file records at /tmp/minion-14-11-FOyKEiLN, selected whole-input SHAebc9e72980bde83459e87cf59ae69416020183f7a24a9b4c360f7a585777afd1, and accepts only the expected shells source change/new contract test. Release one native emission/pack and complete closure/runtime/declaration checks under Task4. The only temporary metadata link may target the staged tsconfig within this same private workspace, as previously qualified. Native packing may rewrite workspace tsconfig version and omit prepublishOnly exactly as before. Reject all other normalization, script, tool or input drift. Preserve active artifacts, earlier archive and all qualified consumer snapshots; no source/install/publication authority.


## Root required-contract artifact continuation

14-16 source independently passes114cases/types and its exact two files are adopted into canonical source. Root reviewed fresh `/tmp/minion-14-11-1ss7iase/checks/whole-input.json`, SHAcac2fbbf0e71154818dd99ddec9bbbb1554f17c7d1e719e52303b1759917f42e, plus exact recipe diffs. Only shells.ts and shells-outcome.test.ts differ from the previously independently qualified complete durable artifact; compiler/config/tool/source membership and all prior consumer artifacts are preserved. Runtime and readonly public declaration probes add the required admission/profile contract without replacing existing qualification.

Admit the same bounded native emission/pack/export-closure/runtime/types/negative validation sequence in the fresh directory. The execution script may update its single expected PLAN digest to this amendment, with before/after script receipt; no command/environment/source change follows. Preserve pinned native pnpm/TypeScript, explicit private output/cache/HOME, strace network/exec evidence and no-install/no-publish policy. Root independently qualifies emitted/archive/declaration/source identities before any consumer adoption. The same0.9.0 version with new bytes is unpublished local evidence, never an immutable production identity. Existing packaging license/README/test-map and optional-ws-peer limits remain visible.
