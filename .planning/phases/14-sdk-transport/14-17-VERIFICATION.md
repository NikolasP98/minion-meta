# Caller routing, configuration and lifecycle verification (14-17)

Scope: 13-file private candidate at `~/.cache/claude-tmp/minion-14-17/minion`, copied from the qualified 14-12 receiver candidate (receiver and facade bytes must stay identical: run-store.ts `fe5181fb…`, manager.ts `5543ffdc…`, bridge-ws.ts `e7f30b92…`, shared-types.ts `114feca0…` — root rehash confirms).

Root independently repeated the frozen commands on 2026-09-10 (receipts `checks/root/`): `node qc-run-caller.mjs` exit 0 — 127/127 (receiver files still 69/69; shells dispatcher 27, server-methods 11, server-close 8, config regressions 8, methods-list 4), collected set equals selected, loopback guard 0 denied, `qc-db/` empty; `node qc-types-gate.mjs qc-types.json` exit 0 — 0 diagnostics on owned/receiver files, 5 structural diagnostics in unowned files reproduced by the untouched baseline project (pre-existing). Executor receipts for oxlint 0/0, oxfmt clean and `generate-capabilities.ts --check` (161 methods, drift proven) reviewed.

Verified truths: organization only from connect-time `client.orgId`; a caller-supplied `params.orgId` is refused by the strict canonical parser (`INVALID_SHELL_OUTCOME`) — stronger than the plan's "ignored"; unscoped admin refused before the manager; invoke admin-only; required mode refuses legacy `shells.invoke`; store options resolved by a pure `resolveShellsDurabilityStoreOptions` (defaults 4096/1024, absolute canonical path, disabled by absence); required-mode init failure rethrown instead of swallowed; teardown bound to the close handler; no positive durable-ready claim added.

Seams recorded, not expanded: (1) `server.impl.ts` is not importable in the credential-free snapshot (extensions/ absent), so gateway startup ordering is not executed in-process — root gate at adoption; (2) whole-graph `tsc` cannot exit 0 in the snapshot (two read-only extension symlinks were needed to import the real dispatcher). Open behaviour: effective `shells.get_outcome` authority is the intersection of the D360-15 ladder and the pre-existing shells-wide `requireOperator`; an `operator.read`-only session is still refused by the older gate (not changed here; caller-scope decision for root).

Not claimed: sender 11-03 end-to-end, production enablement, installed package (shared 0.12.0 not yet published), image runtime. SDK-01, SDK-02 and AGT-04 stay open. Nothing adopted.
