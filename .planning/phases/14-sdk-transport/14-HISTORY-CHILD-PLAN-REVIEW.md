# SDK history-derived child admission

Root reviewed both three-task plans and the bounded local-history/source evidence. They recover real interoperability requirements without reviving obsolete deployment instructions or duplicating existing reference fixes. Both plans are admitted to the exact inventory, with only Task1 discovery/packet ownership initially active. Source tasks require the named compatibility, HTTP policy, reference patch and candidate resolution receipts.

14-04 reuses the existing plugin bridge. It distinguishes origin/source checks, shape/correlation/lifecycle, version/capability policy and RPC authorization. Fake windows cannot certify browser/deployed compatibility. Unknown capability behavior and supported legacy variants require explicit matrix decisions before source changes; gateway loader/UI/package changes remain outside this child.

14-05 selects the reference typed-error fix before bounding JSON transport. Bytes must be bounded during stream consumption, with caller cancellation and a finite total deadline. Error-field truncation is not a download cap. Raw/proxy/streaming paths remain separate ownership; a failing real consumer cannot be mocked away. No blanket cap on legitimate streams, automatic retry, raw-body logging or version bump is admitted.

Root retains49 requirements and explicitly expands SDK-01/02 transport/consumer wording to include plugin postMessage and Workforce JSON boundaries. This recovers omitted requirement details under existing interoperability goals, not a claim of implementation. Three-consumer/three-hook adoption and exact reference-source selection remain14-01/02 gates.

| PLAN | Admitted SHA-256 |
|---|---|
| 14-04 | `d19863e129360b30df551e191db88a82b3c1dd9605dedd5e6d5cc1023609746f` |
| 14-05 | `29d0e7642297df6728136f5282edf8826b0bc741d65638fb39489562c52c9062` |


## 14-04 source selection

Root accepted the Task 1 packet and appended the explicit ordinary/opaque origin+source, legacy v1, strict major grammar, constrained-unknown deny and lifecycle policies to 14-04. Existing source ownership is unchanged. Component capability gating, handwritten artifact peers, generator, manifest projection, token/RPC policy, timeouts and browser/release adoption remain separate children. Before-images include installed entries before any local package build. Tasks 2/3 are admitted; root serializes the final Hub typecheck and independently verifies the frozen source. 14-05 remains Task 1 only until its separate HTTP policy selection.


## 14-06 and 14-07 evidence admission

Root reviewed the minimum boundaries and admits Task 1 only in each child. 14-06 must prove actual hello/client association before changing the component; gateway URL equality is insufficient. 14-07 must define the immutable bridge region separately from render customization before changing raw peers/prompt. Stored artifacts, arbitrary generated-output enforcement and gateway manifest projection remain separate. Neither source nor browser tasks are authorized by plan counting.
