---
phase: 14-sdk-transport
plan: "18"
type: execute
wave: 6
depends_on: ["14-10", "12-08", "13-11", "13-12"]
files_modified:
  - "minion_site/src/lib/services/member-gateway.svelte.ts"
  - "minion_site/src/lib/services/member-gateway.contract.fixture.ts"
  - "minion_site/vitest.gateway-contract.config.ts"
  - "minion_site/tests/fixtures/member-gateway-parity/Fixture.svelte"
  - "minion_site/tests/fixtures/member-gateway-parity/build.mjs"
  - "minion_site/tests/fixtures/member-gateway-parity/README.md"
  - "minion_site/tests/e2e/member-gateway-parity.spec.ts"
  - "minion_site/package.json"
  - "minion_site/bun.lock"
  - "minion_site/deps/minion-stack-shared-0.9.0-qc-d01a528579e7.tgz"
autonomous: true
requirements: ["SDK-01", "SDK-02", "UI-03", "UI-07"]
readiness: "Root full Site UI check and native suite pass; complete matching archive and service identities independently inspected"
must_haves:
  truths:
    - "The actual Site page, service and complete shared client agree on authenticated-session lifecycle."
    - "Reconnect and retired operations preserve current UI state without replaying effects."
    - "The private manifest and lock select the exact complete archive; source, publication and deployment remain distinct."
  artifacts:
    - path: "minion_site/tests/e2e/member-gateway-parity.spec.ts"
      provides: "Actual page and service qualification over synthetic local transport"
    - path: "minion_site/deps/minion-stack-shared-0.9.0-qc-d01a528579e7.tgz"
      provides: "Immutable complete private SDK input"
  key_links:
    - from: "minion_site/src/lib/services/member-gateway.svelte.ts"
      to: "minion_site/tests/e2e/member-gateway-parity.spec.ts"
      via: "Real authentication callback and current-session UI transitions"
---

<objective>Combine the final Site UI with its repaired gateway service and an exact complete SDK archive, then qualify the real connection lifecycle locally.</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/14-sdk-transport/14-COMPATIBILITY.md
@.planning/phases/14-sdk-transport/14-10-SUMMARY.md
@.planning/phases/12-dependency-provenance/12-08-VERIFICATION.md
@.planning/phases/13-ui-qualification/13-11-SUMMARY.md
@.planning/phases/13-ui-qualification/13-12-SUMMARY.md
@minion_site/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
</context>
<boundaries>
Create a fresh ordinary private snapshot from /tmp/minion-13-08-dn7zbntf/minion_site after verifying its final source selection. Preserve that snapshot, the original 14-10 snapshot and all active repositories. Executor owns exactly ten listed files in the new snapshot; root owns global plans/proposals. No branch/index/worktree/commit/publish/deploy, real authentication/provider/business mutation, .env loading or registry requests. Setup may use existing native Bun cache and the already verified archive; no new dependency version selection. Use private caches and complete private dependencies. Never overwrite a linked store. Browser/build traffic is denied except owned literal-loopback static/HTTP/WebSocket endpoints and explicit synthetic signing replies. One browser worker; serialize heavy compilation, bounded 2 GiB compiler and 1 GiB native worker where feasible. Report actual limits and failures; do not silently replace required native behavior with mocks.

Archive C SHA256 is d01a528579e7b0877d05565b9d3ad2747ab45610d634948f51b6a25b4bf4f756; source path is /tmp/minion-12-08-fa2dci45/cases/pack-root-a/minion-stack-shared-0.9.0.tgz. It has 44 ordinary files and actual metadata version 0.9.0. It is not the published or newly built 0.12.0 release. License/provenance and publication gates remain open; this is private compatibility work.
</boundaries>
<tasks>
<task type="auto">
<name>Preserve and install the exact complete transaction</name>
<files>minion_site/package.json, minion_site/bun.lock, minion_site/deps/minion-stack-shared-0.9.0-qc-d01a528579e7.tgz</files>
<action>Record the complete selected UI source and dependency baseline. Copy the exact archive into the admitted digest-named deps path and use a file dependency in the new private manifest. Use native Bun offline lock/install with lifecycle scripts disabled. Preserve all unrelated resolutions and font/test dependencies; inspect any peer change before accepting it. Rehash every installed SDK member against the complete archive and verify all entry/declaration paths. Keep original missing-onAuthenticated installation as an untouched negative control.</action>
<verify><automated>Archive/member, native offline lock/install, peer and resolution identity checks; no mixed runtime/declarations or source aliases.</automated></verify>
<done>The private app reproducibly selects the complete reviewed archive without changing active packages.</done>
</task>
<task type="auto">
<name>Qualify the frozen service under the current native runner</name>
<files>minion_site/src/lib/services/member-gateway.svelte.ts, minion_site/src/lib/services/member-gateway.contract.fixture.ts, minion_site/vitest.gateway-contract.config.ts</files>
<action>Copy the exact frozen 14-10 service and preserve all 22 cases. Migrate only archive-specific identity assertions and native Vitest 3 configuration. Retain actual Svelte rune compilation, same-runtime dynamic imports and reactive subscriber sequence [false,true,false,true,false]. No fabricated state store or cast hiding missing SDK exports. Record any setup failure separately from behavior. Keep this dedicated fixture out of the ordinary suite unless explicitly selected by the native config.</action>
<verify><automated>All preserved native contract cases execute without skips against archive C; strict declaration check and discovery/actual compiler-path proof.</automated></verify>
<done>Real service lifecycle and declaration compatibility pass on the selected installation.</done>
</task>
<task type="auto">
<name>Exercise the actual page over local HTTP and WebSocket transport</name>
<files>minion_site/tests/fixtures/member-gateway-parity/Fixture.svelte, minion_site/tests/fixtures/member-gateway-parity/build.mjs, minion_site/tests/fixtures/member-gateway-parity/README.md, minion_site/tests/e2e/member-gateway-parity.spec.ts</files>
<action>Compile the actual members page, final UI, real service, member state and installed client. Only page identity data and the remote server/signing endpoint are synthetic. Prefer actual browser WebSocket to an owned native ws loopback server and actual signing HTTP responses. Open alone must not enable chat; challenge/correlated hello through the real client must do so. Exercise current streaming, disconnect/same-client reconnect, delayed old results, no stale UI mutation/effect replay, safe errors and manual tabs at mobile/desktop. Keep deterministic native stale-callback tests when transport semantics cannot produce a particular browser ordering; never simulate that ordering by setting connected directly. Require all actual Barlow faces, license and immutable asset/module hashes before geometry. Mandatory engine/case coverage must reject skips/subsets; all owned servers/clients close after completion.</action>
<verify><automated>Zero-skip Chromium/Firefox/WebKit matrix; actual transport request/event counts and UI assertions; no unauthorized outbound traffic, stale state or effect replay.</automated></verify>
<done>Real UI/service/client interaction is evidenced separately from authorization and production.</done>
</task>
<task type="auto">
<name>Review the combined application and freeze</name>
<files>minion_site/src/lib/services/member-gateway.svelte.ts, minion_site/src/lib/services/member-gateway.contract.fixture.ts, minion_site/vitest.gateway-contract.config.ts, minion_site/tests/fixtures/member-gateway-parity/Fixture.svelte, minion_site/tests/fixtures/member-gateway-parity/build.mjs, minion_site/tests/fixtures/member-gateway-parity/README.md, minion_site/tests/e2e/member-gateway-parity.spec.ts, minion_site/package.json, minion_site/bun.lock, minion_site/deps/minion-stack-shared-0.9.0-qc-d01a528579e7.tgz</files>
<action>Repeat the prior UI/native suite and full native Site sync/check with explicit synthetic public environment values. Preserve exact source, lock, archive, runtime and command identities. Document every remaining integration end with source TODO and matching root proposal. Root independently reviews and reproduces relevant checks before any active adoption or release packet.</action>
<verify><automated>Previous 56 native cases plus dedicated service cases, affected browser matrices, full Site check, format/design/token and source-preservation checks.</automated></verify>
<done>A coherent reviewable private candidate exists; SDK, auth, license, publication and deployment requirements remain accurately scoped.</done>
</task>
</tasks>
<verification>Passing local synthetic server behavior is not gateway authorization, physical-device behavior, immutable registry publication or real deployment. No failed or skipped case counts as complete.</verification>
<success_criteria>The selected complete SDK and service preserve all qualified UI behavior and native session semantics on one candidate.</success_criteria>
<output>Root records 14-18-SUMMARY.md and independent acceptance.</output>
