---
phase: 14-sdk-transport
plan: "12"
status: root_policy_selected_draft_execution_not_admitted
implementation_admitted: false
requirements: [SDK-01, SDK-02, AGT-04]
---

# Durable outcome receiver decision packet

Root D360-15 selects the policy below for a six-file14-12 receiver/store/manager child. Prerequisites are now sequenced separately: canonical14-13, a new complete private emitted shared package, then a four-file gateway parity/input child. Caller authorization/routing and production lifecycle remain separate later children. The complete receiver cannot fit one eight-file plan without hiding required package, method-scope or shutdown work. Only this packet and14-12-PLAN.md are authored now; every proposed source transaction below remains unadmitted.

Read root instructions, gateway `AGENTS.md` and `.dmux-hooks/CLAUDE.md`; live gateway branch is `fix/ci-cost-remaining-gaps`. No branch mutation. Read11-07 final contract/journal, its independent verification,09-06 receiver verification and11-DURABLE-OUTCOME-DECISION. The final11-07 journal is an unwired bridge-local foundation, not the gateway receipt backend.

## What the current source actually proves

| Authority or integration seam | Current evidence | Decision consequence |
| --- | --- | --- |
| Caller organization | `minion/src/gateway/auth/auth-jwt.ts:89–111,132–135` validates required claims after jose signature/issuer/audience verification; `server/ws-jwt-auth.ts:66–89` forwards orgId; `server/ws-connection/message-handler.ts:835–839,999–1008` assigns it to the connection. | Consume the authenticated connection's orgId. No caller params.orgId, token contents, shell naming convention or session ID establishes membership. No live issuer was queried in this inspection. |
| Service accounts | `message-handler.ts:789–795` matches a configured credential but sets orgId to literal `service`; `service-account-auth.ts:12–17` has no tenant-org field. | Such a connection has authority only in the actual `service` namespace under existing semantics. Do not treat it as every customer organization. A configurable service-account tenant model is a separate auth change. |
| Shared operator credential | `server/ws-jwt-auth.ts:115–145` grants backward-compatible admin scopes with orgId undefined. | It remains forbidden for tenant receipt query/admission. Admin scope does not create tenant membership. |
| Shell ownership | `shells/types.ts:21–25` stores orgId; `shells/store.ts:50–59` preserves it during update. `manager.ts:707–720` compares a supplied orgId but permits omission. | New durable manager/store APIs must require nonempty authoritative orgId, not inherit optional-org bypass. Bind immutable run ownership from the owned stored shell at admission. |
| Registered bridge | `manager.ts:471–549` validates reusable device-token hash against a stored shell, checks harness and open socket, and installs the actual BridgeConn. `isCurrentBridge` at line660 compares actual ws identity and OPEN. | Commit authorization needs the registered shell AND actual current connection before/after every awaited lookup. connId strings or Date.now are not substitutes. Credential authentication grants shell-machine authority, not caller/user identity. |
| Run ownership | `manager.ts:315–324` forwards caller input directly; `shells/types.ts:86` heartbeat run IDs are observations; `store.ts:8–29` stores shells/backups/tokens only. Sender `bridge.ts:320–330` generates a run ID in memory. | There is currently no durable admitted run linking tenant/shell/session/input. Refuse terminal commit unless a dedicated receiver admission row already owns that exact tuple. Never adopt a bridge-supplied unknown run or infer ownership from its prefix. |
| Input grammar | Canonical `ShellsInvokeParams` allows text or open-ended multimodal parts. Gateway handler `server-methods/shells.ts:302–330` merely checks an object then casts; sender forwards it to ACP. | There is no justified semantic canonicalization for arbitrary multimodal values. Recommend durable v1 text only initially, with exact text preservation and bounded validation. Legacy multimodal stays on its existing explicitly nondurable path; durable multimodal fails before admission/forwarding. |
| Effective role policy | `server-core/server-methods.ts:96–182,238–302` has no Shells read/write entries and defaults unknown methods to admin. Local `server-methods/shells.ts:27,81–115` checks literal `operator`, but `users/types.ts:38–54` grants operator.read/write/admin. | Handler-only query work would remain effectively admin-only. Add new query classification and explicit local checks in a separate child. Preserve current effective invoke admin requirement pending root selection; do not silently widen every legacy Shells method. |
| Installed protocol | Gateway `package.json:170` declares shared^0.6.0; lock and installed manifest resolve0.6.0. `shells/shared-types.ts:1–9` still says remove after0.7.0. | Import real canonical emitted runtime/declarations through the public shared export. Replace the shim's duplicated exports with selective canonical re-exports after parity proof; keep the gateway-only MAX_TIMER_DELAY_MS local. No copied validators. |
| Registry and receipt durability | `server.impl.ts:595–605` injects JsonShellsStore; no run store. JsonShellsStore mutates inherited memory before serialized persistence; the11 decision already records this limitation. | Use a dedicated transactional receiver database, never the registry's JSON mutation/persistence queue or the unrelated message ledger. Existing registry persistence is not newly certified by receipt durability. |
| Startup/shutdown | `server.impl.ts:574–612` opens only registry/manager; config `types.gateway.ts:319–342` and `zod-schema.ts:532–546` have no durable store policy. `server-core/server-close.ts:23–47` has no Shells receiver drain/close hook. | Production enablement requires a separate strict config/path/lifecycle child. No new database can be silently opened from caller input, environment fallbacks or current-working-directory defaults. |

This is organization-level authority. No inspected Shells source supplies per-user session ACLs, an assigned-agent-to-shell relationship, or tenant authority from a heartbeat. Do not claim those exist. D360-15 selects organization-scoped query of admitted runs with operator.read/write/admin; no per-user ownership is inferred.

## Root-selected policy and remaining gates

| Decision | Selected policy or explicit unresolved gate | Alternative requiring another explicit scope |
| --- | --- | --- |
| Shared reuse | Consume frozen canonical normalizers and types from a full emitted shared package. Selective shim re-exports, preserving local timer constant; no runtime validator copies. | Keeping0.6.0 and hand-copying new guards is rejected. |
| Bilateral capability | Canonical14-13 adds optional `durableOutcomeVersion?: 1` to registration response and a shared bilateral helper. Receiver returns1 only when its actual store/policy is ready and the current registered peer advertises1. Recheck current/open connection after awaited attach/lookup and before registration acceptance/response. Absence remains legacy; every explicit malformed/unsupported value rejects, even opposite absence. | Do not infer receiver acceptance from peer advertisement alone or claim capability from an exported method name. |
| Initial input semantics | Exact `{kind:'text',text}` with required bounded, well-formed Unicode text; preserve whitespace and Unicode. Digest SHA-256 of UTF-8 `JSON.stringify(['minion.shells.input',1,'text',text])`. Session is separately immutable in admission; wake routing is not prompt equivalence. | Multimodal needs an enumerated part schema, ordering/content rules and golden vectors; never hash arbitrary unknown JSON. |
| Invocation identity | Optional bounded caller key scoped by authenticated org+shell. Same key requires same session and input digest. No key means a newly generated invocation/run identity for each accepted request. Native randomUUID issues internal IDs; startedAt is persisted once. | User-specific dedup requires a separately selected principal field. Transport request IDs and timestamps never become invocation keys. |
| Durable retry | Validate org+shell+key/session/input first and recover an exact existing admission BEFORE requiring a presently online/capable bridge or checking new-admission capacity. Return its original identity/status without forwarding, including lost initial response followed by disconnect. New admissions require current shell owner/current authenticated bridge/bilateral v1/readiness and are committed before one initial forward. Uncertain delivery never invents success or repeats effects. | Retrying a forward must wait for11-03's durable sender dedup/reconciliation contract; when admitted it must reuse the original identity and validated input. |
| Query scope | New get_outcome requires authenticated connection plus operator.read/write/admin and nonempty orgId. Query by org+shell+run, with the admission row's immutable owner authoritative. Foreign and missing rows produce the same not_found shape. | A user/session ACL or stricter org-admin-only query is a product/auth decision, not evidence from current source. |
| Invoke scope | Preserve the current effective admin-only gate for the existing invoke method, plus mandatory orgId. Explicit durable selection cannot silently downgrade to legacy. | Widening invoke to operator.write should be decided/tested separately against assigned-agent and organization policy. |
| Deleted shell history | Retain admission/receipt owner; same owning org may query or exactly recover a keyed historical admission after registry deletion. A new registry record cannot rewrite historical owner. New admissions/commits require the selected current stored-shell/bridge authority; historical lookup never resends. | If root chooses deny-after-delete instead, encode that policy in query tests. Never orphan/delete committed receipts implicitly. |
| Store | Dedicated gateway SQLite schema with immutable org/shell/run/invocation/input ownership and receipts. Multiple shells and organizations can have unresolved admissions concurrently; no copied bridge-global one-open-run index. | Per-shell concurrency comes from separately selected admission policy/capabilities, not a hidden global store constraint. |
| Capacity | UNRESOLVED numeric admission gate: root must select authoritative org/key/identifier/text/outcome/receipt/record and combined-query byte limits, plus global/per-org retained-run bounds. Persist policy/version, count tombstones, reserve a feasible receipt/query obligation at admission and preserve exact replay at capacity. No automatic pruning or fabricated defaults. | Production numeric choices, retention or physical disk quotas need root decision and operational evidence. Ensure the combined query envelope fits its response limit, not merely each nested record. |
| Receipt | Validate canonical outcome, recompute digest, require exact admitted tuple, commit terminal+stable receipt atomically, then ACK only on same current socket. Duplicate exact commit returns the original receiptId/committedAt. | Wrong owner/run/event/content/digest is a conflict; no receipt rewrite. Broadcast is optional notification after commit and cannot erase a receipt. |

A lost ACK after COMMIT preserves the receipt. A reconnecting authenticated bridge can resend the same outcome and receive the same receipt; a stale socket must not receive ACK after replacement. A close before SQL admission prevents mutation; a close after a committed transaction suppresses delivery, not storage. Native synchronous transaction work should have an explicit linearization point immediately after final authority/current-connection checks, with no awaited gap before BEGIN/COMMIT.

## Exact split and ownership proposals

14-13 is now the named canonical child; its execution admission remains root-owned. All other child proposals below remain unadmitted. Root assigns missing identifiers and serializes shared file owners. Existing paths were inspected; proposed new files are marked new.

| Slice | Exact source/test paths | Scope |
| --- | --- | --- |
| **14-13 — canonical negotiation (2)** | `packages/shared/src/gateway/shells.ts`; `packages/shared/src/gateway/shells-outcome.test.ts` | Additive response version and browser-safe bilateral decision only; canonical source/test freeze before any new artifact. No gateway shim/input, SQL or installation. |
| Private artifact preparation — root-owned, no consumer source ownership | Exact new temporary package/build/archive/extraction and gateway snapshot paths must be separately frozen by root | Complete shared package includes11-07 and14-13 with matching runtime/declarations, full export/transitive/source/tool digests and shipped license/README/peer checks. Prior14-11 bytes do not qualify. This is not a manifest/lock adoption or registry release. |
| Gateway parity/input child (4; identifier unassigned) | `minion/src/shells/shared-types.ts`; **new** `minion/src/shells/shared-types.test.ts`; **new** `minion/src/shells/invoke-contract.ts`; **new** `minion/src/shells/invoke-contract.test.ts` | AFTER the complete candidate exists, qualify selective canonical re-exports and text envelope/key/session/input-digest policy against actual public emitted imports. Preserve MAX_TIMER_DELAY_MS and existing consumers. No copied validator or source alias. |
| Immutable package adoption — later separate transaction (2) | `minion/package.json`; `minion/pnpm-lock.yaml` | Exact immutable version/archive and lock update, separately admitted after local candidate qualification. No same-version/different-byte production identity and no implicit active node_modules/install/publish authority. |
| **14-12 — receiver/storage manager foundation (6)** | **new** `minion/src/shells/run-store.ts`; **new** `minion/src/shells/run-store.test.ts`; `minion/src/shells/manager.ts`; `minion/src/shells/manager.test.ts`; `minion/src/shells/bridge-ws.ts`; `minion/src/shells/bridge-ws.test.ts` | Injected dedicated multi-shell store; real registered/current-socket commit; manager durable admission/query API; finite readiness/shutdown state. Synthetic actual manager/WS/native-store tests. No caller route publication or production database opening. |
| C — caller routing and exact method authorization (6) | `minion/src/gateway/server-methods/shells.ts`; `minion/src/gateway/server-methods/shells.test.ts`; `minion/src/gateway/server-core/server-methods.ts`; `minion/src/gateway/server-core/server-methods.test.ts`; `minion/src/gateway/server-core/server-methods-list.ts`; **new** `minion/src/gateway/server-core/server-methods-list.test.ts` | Wire validated invoke key/input and get_outcome to the actual manager; require identity and selected scopes through real dispatcher, not handler-only mocks. Advertise get_outcome only for supported receiver state; commit_outcome stays on machine endpoint. Preserve other Shells authorization. |
| L — strict production config, opening and close (6) | `minion/src/config/types.gateway.ts`; `minion/src/config/zod-schema.ts`; `minion/src/config/config.schema-regressions.test.ts`; `minion/src/gateway/server.impl.ts`; `minion/src/gateway/server-core/server-close.ts`; `minion/src/gateway/server-core/server-close.test.ts` | Explicit enabled/path/policy fields; open dedicated store before readiness; refuse invalid/unavailable storage without memory fallback; quiesce new work, drain admitted receiver operations, close bridge endpoint and database in owned shutdown. No provider or live server acceptance in this slice. |

14-12 must expose bounded receiver quiesce/drain hooks while it owns manager.ts; L wires them rather than silently expanding ownership. Recheck stopping/current authority when queued short work actually begins and immediately before SQL. Revoke readiness at quiesce; drain already admitted short operations, never the long remote forward response wait. A held registry lookup, queued write and concurrent quiesce must have a tested linearization order. Stopping-read policy remains an explicit root selection before source; recommended minimum refuses newly entering historical reads/keyed recovery once quiesce begins and drains existing reads. It never retransmits. If actual source demonstrates another unavoidable seam, root splits/amends before implementation instead of adding a ninth file. No catch-all new service framework is proposed.

Dependency order: verified11-07/09-06 → canonical14-13 source freeze → NEW complete private emitted shared package → separate gateway parity/input qualification →14-12 → C → local authenticated receiver acceptance →11-03 sender integration. Immutable production package/lock adoption stays separate from private artifact preparation. L can follow C independently of11-03's controlled fixtures, but production enablement requires L plus exact runtime/package/image acceptance. Receiver work never depends on11-03 or all phase14 completing.11-04 ACP/lifecycle and restore remain their own dependencies. Root must amend11-03's previous overlapping journal ownership; this packet does not change global plans.

## Artifact and runtime gates

Gateway currently declares Node>=22.12.0, below the selected native no-flag SQLite22.13.0 feature floor. Do not silently change the whole gateway engine declaration in14-12. D360-15 keeps the existing engine declaration and selects lazy durable-feature refusal. Importing manager/run-store in disabled legacy mode must not call the SQLite loader. Explicit durable opening loads the supported builtin lazily and refuses with a bounded feature-unavailable error if unavailable, with no fallback store or experimental flag. Controlled loader-unavailable fixtures prove that selected import/open behavior only, not an actual unsupported-runtime distribution. Exact22.13.0 and an unavailable-builtin distribution remain unexecuted;22.23.1 is a local executable, not an image receipt.

11-07 final emitted shared `shells.js` SHA `3f5c5a7861c5d3298eaf9166acf7830c04082ca8597621b55d499a815b2122db` and declarations `82140419eee5f8e6fc3626ee07e3be32dcc0eccde258f773ec04895efa49d81a` prove that frozen contract, before14-13's response/helper addition.11-07's manifest after observations is `b13a0214ec78b212de6df3b1151b3b9e2d4538c66df0e9063440276bdb2cb8ca`. It is not a published archive. The earlier14-11 archive does not contain11-07 and cannot stand in for it.

Root must select full source/tool/export/transitive digests, candidate emission/archive manifest and matching gateway declaration/runtime resolution before tests import new helpers. Preserve old installed0.6.0 as an explicit missing-contract baseline. Verify existing optional ws peer and every shim consumer against the chosen emitted package, then immutable package version/archive/lock adoption separately. No same-version/different-byte production identity. License/README, shipped test/source-map contents and minimum/image gates from12-03/14-02 remain open; a local source alias is not installed adoption.

Temporary fixture setup must use root-created credential-free snapshots, installed tools read-only and real private cache directories. Never link `.vite`, `.vite-temp` or other mutable caches back into active node_modules. Inspect actual cache resolution before tests; the11-07 exception is the concrete precedent. No fetch/install/build/server/browser/production action is authorized by this packet.

## Evidence identities and handoff

| Input | SHA-256 |
| --- | --- |
| Final11-07 independent verification | `7753207c14807fee5611409c4f01b090c107dbc6941f45b71f143e07927e3099` |
| Canonical shared shells source | `bc1c4ea5cb4c63bec21c1f1c13058e0e474e0485a1a3331139a25965ed601f80` |
| Gateway shim | `9e0652b8b398f13f16eede58e3e3e4a2b088099308b7d713e2671481e197002c` |
| Gateway manager | `a8db03c3683c13a4ef2794b7f3e41186384ec98ad13ce9e6e9304546f426cbe2` |
| Gateway bridge WS | `8a0b6fbd2026e7d30a2fa61e21684eaad185be9cc09b6880b20665bce5fde451` |
| Caller handlers | `8ab33a7e37c3b45aee1cbda097525905ede475d3c11ae836777922ed38d14b84` |
| Generic method authorization | `a6076749b1e9a67aaad692374953555d5911f65c6dbe719f6ae07edf9d40ee9b` |
| Connection identity producer | `a6561548678d252a02a32e764d418206f2ab86451cdfc20d981c581c268ba671` |
| JWT helper | `f2b574a7706ba54a44ac540f7ae8824eb682034380eb673f3a1dfa3029446a13` |
| Gateway package manifest | `8f421635bf5cece798bce75a0771b4d384cc5dc6e62a48ed1b6fcaa734509607` |
| Gateway lock | `f635c2aa60509f3f0bd4e05bd2621391d2f684cec1e37cc8c9defd82b6b6c1e1` |
| Installed shared0.6.0 manifest | `9cd1966ad7c9340508e37586aeffaf0d13e341f2076ef9ba50a90710f7d28543` |

D360-15 now selects first durable text grammar, role/organization policy, historical ownership, keyed recovery ordering and lazy runtime refusal. Source remains unadmitted. Required remaining gates are numeric capacity/byte values and reservation feasibility,14-13/new complete artifact/gateway parity-input receipts, selected stopping-read policy and exact safe execution setup. A root architecture selection is not package qualification, source admission or production activation. Existing source TODOs already identify receiver/sender/registry/authority gaps. Root owns paired proposal/global updates; later admitted source must retain exact-site TODOs for still-unwired boundaries. Read-only inspection plus two draft documents only; no runtime tests or package/database mutation occurred in this planning task.


## Independent review acceptance incorporated

R1 is reflected in admission ordering: historical exact org+shell+key lookup and session/input equality precede live-bridge/new-capacity checks. Test lost first response plus bridge loss, original runId/startedAt with zero forwards, changed session/input conflict, foreign isolation, full-capacity replay and retained history after deletion. New admission still needs actual current owner/bridge/readiness. No-key requests remain distinct new identities.

R2 is reflected in the engine policy: no SQLite access during disabled imports, lazy explicit durable opening, fixed unavailable-feature refusal without fallback. Gateway `src/memory/sqlite.ts` is the native loading seam; do not copy the bridge journal's module-level native invocation into an optionally imported manager/store module. Exact minimum/unsupported runtimes remain unexecuted gates.

R3 removes the mixed canonical/gateway prerequisite:14-13 source first, new complete artifact next, then four gateway parity/input files. The latter cannot be qualified against old installed0.6.0 or a source alias. Root owns their exact package/temporary-input receipt and later immutable adoption.

Registration acceptance must check actual current manager connection and open socket AFTER awaited attach/registry operations, before advertising accepted v1. An old socket left open by a failed close is a required negative case. Commit authorization is rechecked after awaited ownership lookup and immediately before synchronous native SQL; replacement after COMMIT suppresses delivery but preserves the stored receipt.

Physical storage checks retain trusted-private-parent and same-UID assumptions. Require complete schema/object/index/constraint identity, persisted-policy equality, regular owned single-link main/sidecars, effective WAL/FULL/foreign-key settings and no mutation on altered-policy/foreign-store refusal. Revalidate database policy/schema inside transactions. This does not establish protection against pathname/inode substitution by a same-UID actor between opens. Test second-handle committed visibility and a named controlled contention/lock sequence; merely opening two handles proves no race. Native statement rollback does not prove failed COMMIT, process loss, power loss or RPO.

Capacity remains an unresolved numeric gate, including authoritative org/key lengths and the combined canonical outcome+receipt query envelope. Valid nested objects can together exceed normalizeShellsGetOutcomeResponse recordBytes. Test maximum allowed fields and envelope feasibility, full global/per-org limits, unchanged reservation after oversized terminal refusal, exact duplicate/terminal replay and denied distinct admission. Logical bounds are not disk quotas or production sizing.

Canonical errorMessage is bounded sender-supplied public-safe text. Reject raw error objects, unknown usage/output/payload fields and malformed data. Neither the schema nor the digest proves accepted strings are redacted; do not promise secret-free arbitrary text or silently rewrite it after digest verification. A stricter vocabulary/redaction contract needs separate explicit canonical policy.

## Revision history

Preserved original reviewed identities:14-12 PLAN `c60925f7ae45c934939e7f89340f246f1f32dd9c4cdf63e421eb363a80e89025`; this packet `abc224208c002d6fa22b1f3ee31d42c4a30245fa250c23d1864ecc4ac7730e85`. Independent14-12 review `7d1e002da2be4b3683c44b24b20614ded1fd1c157e5ca48290ace6c835e00e42` required R1–R3 and the acceptance details now incorporated. This revision applies root D360-15 as planning only; no source/runtime/package action occurred. Root retains global/proposal ownership and must review exact revised bytes before source admission.
