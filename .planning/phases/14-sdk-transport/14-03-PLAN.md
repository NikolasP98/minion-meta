---
phase: 14-sdk-transport
plan: "03"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - "packages/crm-sdk/package.json"
  - "packages/crm-sdk/src/browser-denied.ts"
  - "packages/crm-sdk/src/server-boundary.test.ts"
  - "minion_site/src/routes/api/leads/leads-sdk.contract.test.ts"
  - ".planning/phases/14-sdk-transport/14-CRM-ADOPTION.md"
autonomous: true
requirements: ["SDK-03"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Browser bundling cannot include privileged client or credentials; existing server API imports keep type/runtime compatibility."
    - "Site route exercises fixed archive; public bundle excludes SDK secrets/code; idempotency fixtures pass."
  artifacts:
    - path: "packages/crm-sdk/package.json"
      provides: "Prevent privileged SDK exports from entering browser bundles"
    - path: "minion_site/src/routes/api/leads/leads-sdk.contract.test.ts"
      provides: "Prove lead-route adoption from immutable archive"
  key_links:
    - from: "packages/crm-sdk/package.json"
      to: "packages/crm-sdk/src/index.ts"
      via: "Read-first contract and behavior verification for prevent privileged sdk exports from entering browser bundles"
    - from: "minion_site/src/routes/api/leads/leads-sdk.contract.test.ts"
      to: "minion_site/src/routes/api/leads/+server.ts"
      via: "Read-first contract and behavior verification for prove lead-route adoption from immutable archive"
---

<objective>
Enforce server-only CRM SDK consumption.
Purpose: SDK-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/14-sdk-transport/14-CONTEXT.md
@AGENTS.md
@packages/crm-sdk/src/index.ts
@minion_site/src/routes/api/leads/+server.ts
@packages/crm-sdk/package.json

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export const PROTOCOL_VERSION = 3;
// GatewayClient is consumed by Hub gateway.svelte.ts, Site member-gateway.svelte.ts,
// and the actual Paperclip gateway adapter verified by its registration.
// sendRequest(ws, pending, method, params?, timeoutMs?, parentTraceparent?): Promise<unknown>
// CRM SDK root exports createCrmClient, lookupDni and typed contracts; postgres is a required peer.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Prevent privileged SDK exports from entering browser bundles</name>
  <files>packages/crm-sdk/package.json, packages/crm-sdk/src/browser-denied.ts, packages/crm-sdk/src/server-boundary.test.ts</files>
  <read_first>packages/crm-sdk/src/index.ts, minion_site/src/routes/api/leads/+server.ts</read_first>
  <behavior>Browser bundling cannot include privileged client or credentials; existing server API imports keep type/runtime compatibility. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Add a browser-denied export condition or equivalent verified package boundary while preserving Node server exports and postgres peer. Test direct and transitive browser import failure and server import success. Do not rely on documentation or process.env checks as the only boundary. Verify actual upstream fixed CRM idempotency/DNI source before adoption; avoid replaying historical fixes already upstream.</action>
  <verify><automated>pnpm --filter @minion-stack/crm-sdk test &amp;&amp; pnpm --filter @minion-stack/crm-sdk build</automated></verify>
  <done>Browser bundling cannot include privileged client or credentials; existing server API imports keep type/runtime compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Prove lead-route adoption from immutable archive</name>
  <files>minion_site/src/routes/api/leads/leads-sdk.contract.test.ts, .planning/phases/14-sdk-transport/14-CRM-ADOPTION.md</files>
  <read_first>minion_site/src/routes/api/leads/+server.ts, packages/crm-sdk/package.json</read_first>
  <behavior>Site route exercises fixed archive; public bundle excludes SDK secrets/code; idempotency fixtures pass. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Run synthetic duplicate lead/DNI/error/redaction fixtures against the packed fixed SDK through the actual Site route, using isolated database/fake identity provider and no real PII. Record installed archive digest and consumer lock. Check Site browser output for privileged SDK symbols. If adoption requires lock edits, serialize with12-02 and add exact package change under root ownership before marking SDK-03 fulfilled.</action>
  <verify><automated>cd minion_site &amp;&amp; node node_modules/vitest/vitest.mjs run src/routes/api/leads/leads-sdk.contract.test.ts &amp;&amp; bun run build</automated></verify>
  <done>Site route exercises fixed archive; public bundle excludes SDK secrets/code; idempotency fixtures pass.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| HTTP/WS/ACP input / authenticated server identity | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-14-03-01 | S/E | Caller identity or malformed wire data gains authority | mitigate | Versioned runtime validation, server-bound identity, denial and compatibility fixtures. |
| T-14-03-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/14-sdk-transport/14-03-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

