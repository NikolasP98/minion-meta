---
phase: 14-sdk-transport
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "packages/shared/src/gateway/envelope-contract.ts"
  - "packages/shared/src/gateway/envelope-contract.test.ts"
  - ".planning/phases/14-sdk-transport/14-COMPATIBILITY.md"
  - "packages/shared/src/gateway/client.ts"
  - "packages/shared/src/gateway/protocol.ts"
  - "packages/shared/src/gateway/client.test.ts"
  - "packages/shared/src/gateway/index.ts"
autonomous: true
requirements: ["SDK-01"]
readiness: "Local schema/client work executable after phase11/12 contracts; receiver gaps must have exact child plans before acceptance."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Malformed/oversized frames, forged identity and unsupported versions have deterministic rejection; documented supported combinations reflect actual consumers."
    - "Shared client tests cover pending-call cleanup, handshake/version errors, malformed frames and idempotent retry classification."
  artifacts:
    - path: "packages/shared/src/gateway/envelope-contract.ts"
      provides: "Freeze supported wire contracts and compatibility cases"
    - path: "packages/shared/src/gateway/client.ts"
      provides: "Validate shared client against protocol fixtures"
  key_links:
    - from: "packages/shared/src/gateway/envelope-contract.ts"
      to: "packages/shared/src/gateway/types.ts"
      via: "Read-first contract and behavior verification for freeze supported wire contracts and compatibility cases"
    - from: "packages/shared/src/gateway/client.ts"
      to: "packages/shared/src/gateway/connection.ts"
      via: "Read-first contract and behavior verification for validate shared client against protocol fixtures"
---

<objective>
Specify and validate transport authority contracts.
Purpose: SDK-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/14-sdk-transport/14-CONTEXT.md
@AGENTS.md
@packages/shared/src/gateway/types.ts
@packages/shared/src/gateway/client.ts
@packages/shared/src/gateway/version.ts
@packages/shells-bridge/src/acp-client.ts
@packages/shared/src/gateway/connection.ts
@packages/shared/src/gateway/envelope-contract.ts

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export const PROTOCOL_VERSION = 3;
// GatewayClient is consumed by Hub gateway.svelte.ts, Site member-gateway.svelte.ts,
// and the actual Paperclip gateway adapter verified by its registration.
// sendRequest(ws, pending, method, params?, timeoutMs?, parentTraceparent?): Promise<unknown>
// CRM SDK root exports createCrmClient, lookupDni and typed contracts; postgres is a required peer.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Cross-phase admission: 11-agent-lifecycle verified outcomes; 12-03 package provenance.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze supported wire contracts and compatibility cases</name>
  <files>packages/shared/src/gateway/envelope-contract.ts, packages/shared/src/gateway/envelope-contract.test.ts, .planning/phases/14-sdk-transport/14-COMPATIBILITY.md</files>
  <read_first>packages/shared/src/gateway/types.ts, packages/shared/src/gateway/client.ts, packages/shared/src/gateway/version.ts, packages/shells-bridge/src/acp-client.ts</read_first>
  <behavior>Malformed/oversized frames, forged identity and unsupported versions have deterministic rejection; documented supported combinations reflect actual consumers. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Extract actual protocol3 req/res/event, HTTP gateway identity and ACP negotiated capability contracts. Define runtime input validation, stable error codes, traceparent validation, actor/tenant provenance and optional request/effect identifiers; distinguish server-established identity from caller assertions. Preserve known supported old clients through additive negotiation and explicit unsupported-version errors. Inventory exact gateway receiver and Shells acknowledgment gaps from11-03; list before any sender-only wire change.</action>
  <verify><automated>pnpm --filter @minion-stack/shared exec vitest run src/gateway/envelope-contract.test.ts</automated></verify>
  <done>Malformed/oversized frames, forged identity and unsupported versions have deterministic rejection; documented supported combinations reflect actual consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Validate shared client against protocol fixtures</name>
  <files>packages/shared/src/gateway/client.ts, packages/shared/src/gateway/protocol.ts, packages/shared/src/gateway/client.test.ts, packages/shared/src/gateway/index.ts</files>
  <read_first>packages/shared/src/gateway/connection.ts, packages/shared/src/gateway/envelope-contract.ts</read_first>
  <behavior>Shared client tests cover pending-call cleanup, handshake/version errors, malformed frames and idempotent retry classification. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Wire validators at inbound/outbound shared client boundaries and export only required contracts. Reject malformed frames before pending-map access, flush pending calls on disconnect, validate trace context, and never replay non-idempotent calls automatically. Preserve binary Yjs transport and client lifecycle semantics. Any new receiver protocol requires an exact gateway extension plan generated from14-COMPATIBILITY before phase closure.</action>
  <verify><automated>pnpm --filter @minion-stack/shared test &amp;&amp; pnpm --filter @minion-stack/shared typecheck</automated></verify>
  <done>Shared client tests cover pending-call cleanup, handshake/version errors, malformed frames and idempotent retry classification.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| HTTP/WS/ACP input / authenticated server identity | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-14-01-01 | S/E | Caller identity or malformed wire data gains authority | mitigate | Versioned runtime validation, server-bound identity, denial and compatibility fixtures. |
| T-14-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/14-sdk-transport/14-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

