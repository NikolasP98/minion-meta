# Gateway facade and invocation input verification

Scope: private four-file14-14 candidate only. Root independently repeated91 tests (73 input/18 facade), exit0,618ms; strict native TypeScript check exited0. No reported skipped cases or unhandled errors. Root reviewed source and the inherited-field correction; accepted required fields must be own descriptors and known objects are copied to null-prototype records before parsing. Explicit undefined optional fields reject; actual omission retains defaults. Error text remains the fixed canonical validation error.

The gateway facade preserves all existing named exports and local timer while re-exporting actual canonical runtime objects. The pure input function validates exact text/session/key syntax with caller-supplied finite byte policies, preserves accepted bytes, and produces a fixed native SHA-256 projection. It reads no environment, store, clock, provider or current connection. Organization syntax is not authentication. Library reuse is actual installed Zod4.4.3 plus the full emitted shared package.

Root independently rehashed all125 package files, all four source files,4501 unchanged original inputs and101 read-only dependency links. Active gateway paths remain unchanged/absent. Private runtime and TypeScript declarations point to the same complete archive61ccc90584addf49dd8509cb71a1103ba5fa6dd632a46936d72f1716b794fef0. Agent type-aware lint0/0 and format receipts were reviewed; root repeated tests/types rather than claiming independent rerun of every tool.

Evidence: /tmp/minion-14-14-qwkly7fi/minion/qc-checks/root-verification.json, freeze.json, public-resolution.json, root-contract.log and root-types.log. Exact source identities:

- `src/shells/shared-types.ts`: `0cb893eb5c19d0124acaa4395f2fe1f99080ea603d57c8357f307a8402d6e742`
- `src/shells/shared-types.test.ts`: `89c4b1d47907c21b72f0e4ed170a44104825c9874640883e5de659f2dc70be55`
- `src/shells/invoke-contract.ts`: `b69a8256f935390181f270da26dc633163c54be0d780d21dbf374fd674d2cda1`
- `src/shells/invoke-contract.test.ts`: `47e26e9677037b61b83582376643e07f01fc97a51c2df5159dfe326b39bd3096`

Standards review: bounded domain grammar, strict types, existing libraries, no duplicate canonical validators or new dependencies. Spec review: exact legacy facade compatibility, text/key semantics, inherited/accessor refusal and immutable package provenance pass this pure prerequisite.

No route, receiver, authenticated organization resolution, durable-mode selector, production numeric capacity, installed package or release was implemented. Scope TODOs and canonical proposal retain these gates. A malicious JavaScript Proxy is outside this parsed-data boundary. Full gateway aggregate consumers and actual durable routing remain required before adoption. SDK-01/02 and AGT-04 remain open.

## Profile amendment verification (2026-09-09)

Scope: two-file facade amendment against the fresh 14-11 archive `f6a3c237…` in /tmp/minion-14-14-profile-lz9tu2fp/minion. Root independently repeated the frozen commands: tests exit 0, 110/110 (37 facade/profile + 73 unchanged parser, 0 skipped, 0 unhandled); strict `tsc -p qc-types.json --noEmit` exit 0 with empty output. Agent receipts for type-aware lint (0/0) and format were reviewed, not rerun. Runtime and declarations resolve to the same private complete package (3 trace matches). 4,503 unowned inputs, 101 dependency links and the original candidate are unchanged; the network guard recorded no connection attempt.

Amended identities:

- `src/shells/shared-types.ts`: `114feca04867ca80958183a10ddce35928154f34c36fceee521d30bccc5e4b28`
- `src/shells/shared-types.test.ts`: `3978f4a937602c12fd3df21617be7248250cf754450ed89b3f3d64e8f5ba571b`
- `src/shells/invoke-contract.ts` / `.test.ts`: unchanged (`b69a8256…` / `47e26e96…`)

Evidence: checks/freeze.json (rootIndependentReview: accepted), checks/root-profile-verification.json, root-profile-tests.log, root-profile-types.log. The amended pair was synced into the 14-12 receiver snapshot (/tmp/minion-14-12-receiver-foox9ehr/checks/facade-sync.json). This qualifies the facade for receiver import only; SDK-01/02 and AGT-04 remain open.
