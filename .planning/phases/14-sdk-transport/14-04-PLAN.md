---
phase: 14-sdk-transport
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md"
  - "minion/packages/plugin-ui-bridge/src/index.ts"
  - "minion/packages/plugin-ui-bridge/src/index.test.ts"
  - "minion/packages/plugin-ui-bridge/vitest.contract.config.ts"
  - "minion_hub/src/lib/plugins/bridge-protocol.ts"
  - "minion_hub/src/lib/plugins/bridge-protocol.contract.test.ts"
  - "minion_hub/src/lib/plugins/bridge-host.test.ts"
  - "minion_hub/src/lib/plugins/compat.ts"
  - "minion_hub/src/lib/plugins/compat.test.ts"
autonomous: true
requirements: ["SDK-01", "SDK-02"]
readiness: "ADMITTED_TASKS2_3: root matrix and exact source/test scope selected; browser, component and installed rollout remain gated."
closure_policy: "Local class fixtures do not certify browser, installed plugin or deployed gateway combinations. Missing consumers require exact child plans."
must_haves:
  truths:
    - "The existing plugin postMessage implementation is inventoried before repair; June historical missing-feature claims are not implementation authority."
    - "Supported host/plugin combinations reject malformed or wrongly sourced messages before privileged RPC forwarding."
    - "Legacy compatibility choices are explicit; exact package and consumer bytes are distinguished from source and deployment."
  artifacts:
    - path: ".planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md"
      provides: "Selected source/installed-byte identities, supported legacy decisions and cross-consumer evidence"
    - path: "minion_hub/src/lib/plugins/bridge-protocol.contract.test.ts"
      provides: "Real host/plugin class interoperability fixtures rather than component-existence tests"
  key_links:
    - from: "minion_hub/src/lib/plugins/bridge-protocol.ts"
      to: "minion/packages/plugin-ui-bridge/src/index.ts"
      via: "Shared protocol types plus both actual peer implementations exercised in a paired-window fixture"
    - from: "minion_hub/src/lib/plugins/compat.ts"
      to: ".planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md"
      via: "Approved legacy/version/capability cases"
---

<objective>
Qualify and repair the existing plugin iframe postMessage boundary. Recover the compatibility requirements of the superseded June distribution proposal without restarting its packaging or blue-green deployment program.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion/AGENTS.md
@minion/.dmux-hooks/CLAUDE.md
@minion_hub/CLAUDE.md
@.planning/operations/360/PROGRAM.md
@.planning/DECISIONS.md
@.planning/research/360-sdk-container-history.md
@specs/2026-06-15-plugin-distribution-cicd-design.md
@minion/packages/plugin-ui-bridge/package.json
@minion/packages/plugin-ui-bridge/src/index.ts
@minion_hub/src/lib/plugins/bridge-protocol.ts
@minion_hub/src/lib/plugins/bridge-host.ts
@minion_hub/src/lib/plugins/compat.ts
@minion_hub/src/lib/plugins/PluginIframe.svelte
</context>

<boundaries>
Root admits Task1 discovery and its owned packet only. Tasks2–3 require a reviewed selection receipt and exclusive source ownership before edits. This is an internal orchestration gate; routine choices already authorized by the user do not require repeated permission requests.

Observed branches at drafting: gateway `fix/ci-cost-remaining-gaps`, Hub `feat/level-2026-07-30`; re-read branch/SHA/diff. Do not change branches/worktrees/stash, commit, publish, fetch dependencies, edit node_modules or overwrite other WIP. No live gateway, customer data, external HTTP/WS, registry calls or paid agents. Fixtures use in-process windows, structured cloning and synthetic RPC functions; no application server or network listener is needed.

Read-only candidate inspection is allowed outside files_modified. It must identify actual plugin consumers and gateway manifest projection. Only listed source/test files may be edited. No markup/style/token changes, plugin package/version/lock changes, gateway loader/RPC policy changes or CDN migration. If a failure needs those seams, root admits a separate exact child plan; do not hide it by weakening the fixture. For later UI/browser work read ui-design-governance and browser-harness and obtain exclusive browser ownership. Fake windows are not browser evidence.

The shared gateway WS client is a different protocol surface, owned by 14-01/02. Its existing reference patches must be selected there before a mixed transport release; this plan does not rewrite them. Root is the sole writer of global plans, proposal/index/status and decisions.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze current plugin consumers and supported compatibility choices</name>
  <files>.planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md</files>
  <read_first>minion/packages/plugin-ui-bridge/src/index.ts, minion/packages/plugin-ui-bridge/src/index.test.ts, minion_hub/src/lib/plugins/bridge-protocol.ts, minion_hub/src/lib/plugins/compat.ts, minion_hub/src/lib/plugins/PluginIframe.svelte, minion/src/plugins/manifest.ts, minion/src/gateway/server-methods/plugins.ts</read_first>
  <action>Record before hashes, installed package resolution/digest and all actual plugin bridge imports using local source/locks only. Record the source package name/version without claiming npm publication. Compare relevant reference patches before considering new code. Trace host:hello/plugin:ready, ready-before-hello buffering, source/origin checks, sandboxed opaque-origin mode, RPC forwarding, method capabilities, timeout/disposal and asset origin vs gateway identity. Inventory every actual plugin consumer; reuse existing paths and tests. Freeze normal/sandboxed/legacy version-omitted/current/unsupported-version/unknown-capability/missing-requiredRpc matrix rows. Require root selection of unknown-capability and version-range behavior before edits: source currently assumes omitted version 1 and permits unknown facts, which is not a decision to fail open forever. Root records one exact source patch set and test configuration; any unresolved consequential product compatibility choice remains a named gate. No implementation in this task.</action>
  <verify><automated>git diff --check -- .planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md</automated></verify>
  <done>Root reviews the recorded identities, support matrix, exact ownership and decision receipt. Every discovered consumer is named, with source versus installed versus deployed evidence separated.</done>
</task>

<task type="auto">
  <name>Task 2: Repair peer validation and prove both real bridge implementations</name>
  <files>minion/packages/plugin-ui-bridge/src/index.ts, minion/packages/plugin-ui-bridge/src/index.test.ts, minion/packages/plugin-ui-bridge/vitest.contract.config.ts, minion_hub/src/lib/plugins/bridge-protocol.ts, minion_hub/src/lib/plugins/bridge-protocol.contract.test.ts, minion_hub/src/lib/plugins/bridge-host.test.ts</files>
  <behavior>Unrelated same-origin windows and malformed messages cannot reach privileged forwarding. Supported peers retain ready/hello ordering, request correlation, save replies and disposal behavior.</behavior>
  <action>After Task 1 selection, write failing cases at actual package PluginBridge/HostBridge and Hub HostBridge seams. Validate message-specific fields before callbacks/pending maps. Test null/array/unknown-type messages, malformed request IDs/methods/status/version/resize values, wrong origin, correct origin with wrong source, opaque-origin wrong source, duplicate responses, late responses after disposal and asynchronous callback failure. Use the known target window and the selected origin policy together; do not accept wildcard authority just because the same parent exists. Preserve supported sandbox semantics and legacy omitted-version behavior from the reviewed matrix. Add a minimal package-local Vitest config selecting only src/index.test.ts because gateway root test projects may not discover package tests. Pair the actual peer classes with a queued structured-clone window fixture; do not mock the protocol under test. Existing tests may gain realistic MessageEvent.source values while retaining their prior behavioral assertions. Any required new frame/export is reviewed before both peers change, never sender-only.</action>
  <verify><automated>(cd minion/packages/plugin-ui-bridge &amp;&amp; pnpm exec vitest run --config vitest.contract.config.ts src/index.test.ts &amp;&amp; pnpm run typecheck) &amp;&amp; (cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/plugins/bridge-protocol.contract.test.ts src/lib/plugins/bridge-host.test.ts)</automated></verify>
  <done>Selected actual peer combinations pass and each denial fixture reaches zero privileged RPC calls. Tests prove callback/pending cleanup; no skipped or empty test run counts as pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify compatibility policy and record consumer release gates</name>
  <files>minion_hub/src/lib/plugins/compat.ts, minion_hub/src/lib/plugins/compat.test.ts, .planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md</files>
  <behavior>The reviewed version/capability matrix has deterministic accepted and rejected results; no current-source success is reported as installed or deployed adoption.</behavior>
  <action>Apply only Task 1's selected compatibility decisions. Cover current and legacy omitted versions, malformed ranges, incompatible majors, absent gateway version/method advertisement and missing required RPCs. Preserve selected old-client support explicitly. Re-run the paired peer fixtures against current source and locally available installed artifact identities. For each plugin consumer, record exact import/build bytes and whether its real mounting seam was exercised. Missing publication/install/browser/loader/receiver evidence remains pending with an exact proposed child plan; no broad dependency or UI edits here. Record raw error/payload logging and RPC authorization gaps separately from shape validation. Report Standards and Spec reviews independently.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/plugins/compat.test.ts src/lib/plugins/bridge-protocol.contract.test.ts src/lib/plugins/bridge-host.test.ts &amp;&amp; bun run check) &amp;&amp; (cd minion/packages/plugin-ui-bridge &amp;&amp; pnpm run build)</automated></verify>
  <done>Compatibility results identify each supported source/installed combination and its exact limits. Required follow-up plans and source TODO(handoff)/proposal entries are recorded before handoff; SDK-01/02 remain pending while any mandatory consumer gate is open.</done>
</task>
</tasks>

<threat_model>
Untrusted iframe messages cross into the host's privileged RPC connection. Check origin and source identity, message shape, correlation and lifecycle before forwarding. Manifest compatibility is not authorization. Synthetic fixtures must contain no gateway tokens or customer data. Outbound effect retry/replay remains excluded.
</threat_model>

<verification>
Use installed package managers/runners only. Capture meaningful red/green behavior and exact source hashes. No browser claim from FakeWindow, component-existence tests or TypeScript success. Full Hub check is serialized by root with other expensive work; unresolved baseline failures retain their real attribution. Independent reviewer verifies the policy receipt, source scope, tests and remaining runtime gates.
</verification>

<success_criteria>
All admitted tasks have evidence and no failed consumer is omitted. A successful source slice is not complete platform compatibility. Deployment, publication, browser qualification and any generated child plans remain required separately.
</success_criteria>

<output>
After actual execution create 14-04-SUMMARY.md with selected plan hash, before/after source hashes, commands, matrix results and remaining gates. Root owns admission, global statuses and proposal integration; the executor does not commit or mark SDK requirements complete.
</output>


## Root source admission after Task 1

Root reviewed packet SHA-256 `ab9ba7c2730d4aaf3cf569e916e84b39fc31f59904bf4c3dba45ad28832b501b` and admits Tasks 2/3 within the existing file list. Selected policy:

- Ordinary ingress requires the exact expected origin and known source window. An unconstrained `*` origin is rejected; no new permissive opt-in. Explicit opaque sandbox mode requires expected source and origin `null` on host ingress and retains the required wildcard target for outbound delivery.
- Omitted protocol version means legacy v1 only. Numeric 1 is supported; null, strings, nonfinite numbers and unsupported majors are rejected. Preserve both handshake orderings and buffered latest hello/theme/locale. Privileged RPC requires supported handshake.
- Manifest bridge major is a positive integer string, with no ranges. Preserve documented numeric CalVer comparison only after grammar validation. Constrained requiredRpc/minGatewayVersion with unknown facts denies at the utility boundary. Unconstrained legacy plugins retain compatibility. Component mounting, manifest normalization and gateway projection require separate child admission before end-to-end enforcement can be claimed.
- Validate each message's actual fields before callbacks/forwarding. Preserve correlation and explicit request IDs; reject new work after disposal, remove pending state on synchronous send/clone failure, and suppress asynchronous replies after host disposal. No universal RPC timeout is introduced because current generation/edit operations deliberately permit 180 seconds.
- Preserve baseline installed entry bytes before a package build changes the local workspace-linked dist. Run installed-baseline and candidate-source combinations explicitly and report expected incompatibilities without weakening source validation. Do not mutate Hub node_modules, package versions or locks. A package-local build is a candidate artifact, not registry or deployed adoption.

The existing nine-file ownership is unchanged. Source TODOs in owned files must name component/capability, raw artifact peer/generator, timeout, token/RPC policy and browser/release gates when relevant. Root owns the matching proposal. Full Hub check is serialized by root after source freeze; do not overlap it with the admitted profiling window. No browser or live plugin execution is admitted here.

<request_identity_amendment>
Root admission 2026-09-09: accept 14-ARTIFACT-REUSE-AMENDMENT.md SHA-256 daa02905ed86fcf05348410e4468f233651682ef9e93c450bca1a7cfbc8de2da, request-identity portion only. Reopen exactly package src/index.ts, src/index.test.ts and Hub bridge-protocol.contract.test.ts. All other previously qualified source files stay frozen. Matrix and SUMMARY may append actual receipts. No version/lock/installed change.

Select a private lazy namespace of 16 native crypto.getRandomValues bytes and nonwrapping instance sequence. Generate with correct receiver before pending insertion or post; reject entropy absence/failure with fixed local error and no native cause, no weak fallback, no public injection option. This is collision resistance, not authorization. Preserve old handshake/source/origin/disposal semantics and all previous cases. Recheck disposed state after native generation if reentrant test behavior reveals it; no call may post after disposal.

Before implementation demonstrate the actual same-WindowProxy two-instance late-forwarding response failure described in the packet, freezing clock but using distinct test native entropy. Actual host and PluginBridge are required; do not reimplement correlation. Cover no entropy, native throw, lazy one-time allocation and nonwrapping sequence. Then repeat package tests/typecheck/build and the existing Hub peer/compat/mount suite. Existing installed baseline remains unchanged and explicitly unqualified. Preserve old emitted candidate bytes/manifest before build and record the new dist identity for root selection. This admits only existing local build commands, not package installation. Root independently reviews and schedules any aggregate check. 14-07 generated adapter and browser work remain separately gated.
</request_identity_amendment>
