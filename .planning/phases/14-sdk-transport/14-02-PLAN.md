---
phase: 14-sdk-transport
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - "minion_hub/src/lib/services/gateway.contract.fixture.ts"
  - "minion_site/src/lib/services/member-gateway.contract.fixture.ts"
  - "paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.contract.test.ts"
  - ".planning/phases/14-sdk-transport/14-ADOPTION-PACKET.md"
  - "scripts/qc/consumer-installed-identity.mjs"
  - "scripts/qc/consumer-installed-identity.test.mjs"
autonomous: true
requirements: ["SDK-02","SDK-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Actual consumer seams complete the declared matrix; credential rotation and reconnect do not leak prior actor/tenant or duplicate effects."
    - "Every consumer is either passing on exact installed bytes or blocked by a named executable adoption plan; package declaration alone never counts."
  artifacts:
    - path: "minion_hub/src/lib/services/gateway.contract.fixture.ts"
      provides: "Exercise actual consumers against old/new fake servers"
    - path: ".planning/phases/14-sdk-transport/14-ADOPTION-PACKET.md"
      provides: "Adopt exact archives through independently owned consumer transactions"
  key_links:
    - from: "minion_hub/src/lib/services/gateway.contract.fixture.ts"
      to: "minion_hub/src/lib/services/gateway.svelte.ts"
      via: "Read-first contract and behavior verification for exercise actual consumers against old/new fake servers"
    - from: ".planning/phases/14-sdk-transport/14-ADOPTION-PACKET.md"
      to: "scripts/qc/package-consumer-matrix.mjs"
      via: "Read-first contract and behavior verification for adopt exact archives through independently owned consumer transactions"
---

<objective>
Prove Hub Site and Paperclip consumer interoperability.
Purpose: SDK-02, SDK-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/14-sdk-transport/14-CONTEXT.md
@AGENTS.md
@minion_hub/src/lib/services/gateway.svelte.ts
@minion_site/src/lib/services/member-gateway.svelte.ts
@paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts
@scripts/qc/package-consumer-matrix.mjs

<interfaces>
Verified current seams (re-read exact candidate before mutation):
```typescript
export const PROTOCOL_VERSION = 3;
// GatewayClient is consumed by Hub gateway.svelte.ts, Site member-gateway.svelte.ts,
// and the actual Paperclip gateway adapter verified by its registration.
// sendRequest(ws, pending, method, params?, timeoutMs?, parentTraceparent?): Promise<unknown>
// CRM SDK root exports createCrmClient, lookupDni and typed contracts; postgres is a required peer.
```
</interfaces>

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Exercise actual consumers against old/new fake servers</name>
  <files>minion_hub/src/lib/services/gateway.contract.fixture.ts, minion_site/src/lib/services/member-gateway.contract.fixture.ts, paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.contract.test.ts</files>
  <read_first>minion_hub/src/lib/services/gateway.svelte.ts, minion_site/src/lib/services/member-gateway.svelte.ts, paperclip-minion/packages/adapters/openclaw-gateway/src/server/gateway-client.ts</read_first>
  <behavior>Actual consumer seams complete the declared matrix; credential rotation and reconnect do not leak prior actor/tenant or duplicate effects. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Build no-provider servers for every supported matrix entry from14-COMPATIBILITY. Test token rotation, reconnect, challenge, unknown/malformed events, duplicate responses, cancellation and response-loss retry. Verify installed shared package digest from12-03 per consumer. Do not treat Paperclip's currently named openclaw-gateway path as an assumed minion_gateway package; identify actual adapter registration. Expected unsupported combinations fail clearly without destructive retry.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/gateway.contract.fixture.ts) &amp;&amp; (cd minion_site &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/member-gateway.contract.fixture.ts) &amp;&amp; (cd paperclip-minion &amp;&amp; pnpm exec vitest run packages/adapters/openclaw-gateway/src/server/gateway-client.contract.test.ts)</automated></verify>
  <done>Actual consumer seams complete the declared matrix; credential rotation and reconnect do not leak prior actor/tenant or duplicate effects.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt exact archives through independently owned consumer transactions</name>
  <files>.planning/phases/14-sdk-transport/14-ADOPTION-PACKET.md, scripts/qc/consumer-installed-identity.mjs, scripts/qc/consumer-installed-identity.test.mjs</files>
  <read_first>scripts/qc/package-consumer-matrix.mjs</read_first>
  <behavior>Every consumer is either passing on exact installed bytes or blocked by a named executable adoption plan; package declaration alone never counts. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Create per-repo install/lock/source patch packets for only failed or outdated consumers, each with exact archive digest, peer requirements and executable checks. Root grants one manifest writer before changing active locks; add bounded child PLANs naming those files rather than editing all repos here. Check actual bundled bytes and browser/server export boundaries. No registry publication or live deployment. SDK-02 stays pending until each supported consumer has installed-identity and behavior evidence.</action>
  <verify><automated>node --test scripts/qc/consumer-installed-identity.test.mjs</automated></verify>
  <done>Every consumer is either passing on exact installed bytes or blocked by a named executable adoption plan; package declaration alone never counts.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| HTTP/WS/ACP input / authenticated server identity | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-14-02-01 | S/E | Caller identity or malformed wire data gains authority | mitigate | Versioned runtime validation, server-bound identity, denial and compatibility fixtures. |
| T-14-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/14-sdk-transport/14-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

## Child fixture ownership

14-09 and 14-10 exclusively own the Hub/Site contract.fixture.ts paths and dedicated configs during reconnect implementation. 14-02 extends their frozen fixtures only after ownership release. Their suffix deliberately excludes them from ordinary test discovery; the exact dedicated config must be passed. No child candidate alias result establishes installed package acceptance.
