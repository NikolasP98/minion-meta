---
phase: 14-sdk-transport
plan: "10"
type: execute
wave: 2
depends_on: ["14-08"]
files_modified:
  - "minion_site/src/lib/services/member-gateway.svelte.ts"
  - "minion_site/src/lib/services/member-gateway.contract.fixture.ts"
  - "minion_site/vitest.gateway-contract.config.ts"
  - ".planning/phases/14-sdk-transport/14-10-SUMMARY.md"
autonomous: false
requirements: ["SDK-01", "SDK-02"]
readiness: "DRAFT: requires verified 14-08, reviewed fixture/reporting selection (review R1–R5 accepted) and exact declaration/archive/installed adoption gate."
closure_policy: "Site session publication does not qualify every gateway protocol, browser or deployed consumer."
must_haves:
  truths:
    - "Site restores connected state and current-session initialization after internal reconnect without replacing the GatewayClient object."
    - "Old-client events, close, errors and asynchronous hydration cannot mutate a replacement member session."
    - "Only one polling owner remains, and no effectful request is replayed automatically."
  artifacts:
    - path: "minion_site/src/lib/services/member-gateway.svelte.ts"
      provides: "Current-client authenticated notification adoption and asynchronous session fences"
    - path: "minion_site/src/lib/services/member-gateway.contract.fixture.ts"
      provides: "Actual Svelte service plus candidate shared-client contract fixtures"
  key_links:
    - from: "minion_site/src/lib/services/member-gateway.svelte.ts"
      to: "packages/shared/src/gateway/client.ts"
      via: "Selected onAuthenticated callback on exact qualified consumer package"
    - from: "minion_site/src/lib/services/member-gateway.contract.fixture.ts"
      to: "minion_site/src/lib/state/member.svelte.ts"
      via: "Read actual reactive member state after reconnect/disconnect/replacement"
---

<objective>
Restore Site member connection state after internal reconnect and prevent obsolete client/hydration callbacks from changing the current member session. Keep this separate from Hub cutover and plugin work.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_site/CLAUDE.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/14-sdk-transport/14-RECONNECT-SESSION-DECISION.md
@.planning/phases/14-sdk-transport/14-08-SUMMARY.md
@.planning/phases/14-sdk-transport/14-02-PLAN.md
@minion_site/src/lib/services/member-gateway.svelte.ts
@minion_site/src/lib/state/member.svelte.ts
@minion_site/vitest.config.ts
@minion_site/package.json
</context>

<boundaries>
Only the three listed service/test/config files plus summary may change after independent root admission. No member state module, components/markup/style, auth/device API, shared client, Hub/Paperclip, package manifest/lock, node_modules or installed deps edits. Preserve existing role/scopes/signing and chat selection semantics. No branch/worktree/stash/commit, browser, application server, real fetch/signing, WS network, provider, telemetry or customer data. Tests use installed runners and synthetic injected transport; no new dependency is assumed.

Current installed shared0.9.0 lacks the callback contract. Product source changes wait for root's separately owned exact emitted declaration/archive/installation receipt, or an independently amended isolated candidate workflow. Test-only aliasing cannot establish installed adoption or excuse an incompatible production typecheck. Root owns global/proposal updates and all package transactions.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Admit the actual Site service fixture and reporting policy</name>
  <files>minion_site/src/lib/services/member-gateway.contract.fixture.ts, minion_site/vitest.gateway-contract.config.ts, .planning/phases/14-sdk-transport/14-10-SUMMARY.md</files>
  <action>Read verified14-08 and current Site source; draft baseline service SHA223a2ee825f8a99e62757945299fbd62ac30cee0f73bd1ed27da6566c0c74dc1, installed shared0.9.0 client SHA082e7c38fa050ceffbcb6e500b8ed2123a1856526c9cbdbad292d165280227bc. Recheck drift and installed declaration/entry identity. Confirm existing Svelte/Vitest compiler dependencies; do not install ad hoc if absent.

Create an isolated Svelte-aware config selecting only member-gateway.contract.fixture.ts. Import actual service/member reactive state and actual candidate GatewayClient with injected socket behavior. Mock navigator/device signing HTTP and side-effectful surroundings in the owned test; do not replace connection, state or initialization algorithms with a model. Test-only source alias must identify the selected digest and remain separate from the later exact installed-entry run. Assert no real network/provider operation.

Record root's independent choice for all three inherited error hooks: established sanitized reporting sink or explicit default acceptance per hook. Preserve the amended three-hook adoption requirement and keep reporters from driving reconnect. Get root fixture/policy/package-prerequisite admission, then demonstrate missing reconnect publication and stale-client behavior against unchanged service source.</action>
  <verify><automated>(cd minion_site &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/member-gateway.contract.fixture.ts --maxWorkers=1)</automated></verify>
  <done>Meaningful red actual-service cases, isolated fixture and three-hook policy are reviewed; exact declaration/archive/install prerequisite is recorded before source work.</done>
</task>
<task type="auto">
  <name>Task 2: Publish current member sessions and fence stale callbacks</name>
  <files>minion_site/src/lib/services/member-gateway.svelte.ts, minion_site/src/lib/services/member-gateway.contract.fixture.ts</files>
  <action>Capture each newly constructed client explicitly; compare callbacks to the active module client. Adopt onAuthenticated to mark connected and initialize after every successful active generation. Remove duplicate initial-promise success publication while keeping guarded error handling. Store accepted production generation with the client in a local session record; no Date.now or fixture-only epoch. Invalidate on current close/disconnect/replacement and reject duplicate success for the same tuple.

Fence event, close and connect-error callbacks from obsolete clients. Capture the accepted client/generation for async agents/sessions/history initialization and polling; check it before changing member state or starting later requests. Bind requests to that captured client instead of resolving a successor through mutable module state. Preserve assigned-agent/chat-session selection, selected history rendering, role/scopes/device signing and explicit no-replay behavior. Apply root's three-hook reporting policy without modifying auth or reconnect decisions.

Keep exactly one polling timer, stop it on explicit disconnect, and prevent late prior-session completion from restarting polling or overwriting the new session. Preserve public exports and avoid a new general state/event framework. No changes to member state shape or UI are required; if evidence establishes an unavoidable extra seam, request exact amendment before edits.</action>
  <verify><automated>(cd minion_site &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/member-gateway.contract.fixture.ts --maxWorkers=1)</automated></verify>
  <done>Same-object reconnect restores usable state; stale callbacks and async hydration cannot mutate or revive a successor/disconnected session.</done>
</task>
<task type="auto">
  <name>Task 3: Qualify exact consumer resolution and freeze</name>
  <files>minion_site/src/lib/services/member-gateway.contract.fixture.ts, .planning/phases/14-sdk-transport/14-10-SUMMARY.md</files>
  <action>Require initial connect, automatic reconnect with fresh hello, zero connected publication before authenticated callback, duplicate callback idempotence, failed reconnect remaining disconnected, explicit disconnect during challenge, late old success/event/close/error after replacement, old agents/sessions/history response after new session, old promise finally not clearing current loading state, and one polling timer across reconnect/disconnect. Assert no duplicate chat/effect request and no real network. Tests must read actual reactive state and use the real client's production generation.

Run the fixture first against explicitly identified candidate source, then the separately installed exact candidate entry and declarations. Preserve the old installed baseline result as a separate expected missing-contract observation, never a repaired claim. Once installation is consistent and root releases resources, run Site check. Record separate Standards/Spec review, source/reference/archive/installed digests, three-hook reporting selection and remaining browser/gateway authority gaps. Do not modify unrelated existing tests/config or infer a live browser pass.</action>
  <verify><automated>(cd minion_site &amp;&amp; bun run check)</automated></verify>
  <done>Focused source and exact installed consumer evidence plus Site typecheck pass; independent root review preserves broader SDK/browser/release gates.</done>
</task>
</tasks>

<verification>
Allowlisted PATH/HOME/LANG, installed package manager/runners, synthetic env and no network fixtures. Retain logs and exact resolution for source versus installed runs. Existing Site default Vitest config is server-oriented; do not claim it compiles the actual Svelte service without the admitted isolated config. Root schedules expensive checks and owns proposed source TODO/proposal gaps. Scoped diff check covers three files and summary. SDK-01/02 remain open while mandatory cross-consumer, authority, archive/release and runtime gates remain.
</verification>

<output>
14-10-SUMMARY.md with selected plan hash, baseline/reference/source/installed identities, true red/green results, reporting policy, Site check and independent-review limits. This draft is not source execution authority.
</output>

## Root consumer review selection

Root accepts 14-CONSUMER-PLAN-REVIEW.md SHA-256 baa6ea65d3247b0f25986f7cb9fbe0df9c1dec4da9ad3ea5eef1d248092446a2. Its R1–R5 requirements are normative additions to the corresponding tasks. This plan remains a draft until the exact package and temporary candidate path admission is complete.

Select .contract.fixture.ts with an explicit include in the dedicated config; ordinary test discovery must omit it, dedicated discovery must find exactly it with passWithNoTests:false. Do not widen normal config ownership. Use maxWorkers:1 and minWorkers:1 together with envDir:false. Root will amend the exact temporary runtime/declaration snapshot paths after 14-11 candidate artifact review; no source sketch or ambient cast can substitute.

Select all three reporting hooks: event failures use console.error with only the fixed app-prefixed message from the review; reconnect/socket failures use console.warn with only their fixed respective messages. Ignore obsolete-client diagnostics; reporters never retry, change connection state or send raw errors/frames/event names/URLs. Test secret canaries and throwing sinks. Existing unrelated raw log paths remain an explicit follow-up, not a claim of platform-wide redaction.

Fence sendChatMsg fulfillment/rejection by captured client/session and operation/run identity. A stale rejection must not initiate history RPC against the successor or clear its sending/loading state; preserve original idempotency and optimistic messages, and never invent send success. On invalidation settle only flags/timers owned by the retired operation so retained objects are not stranded. Clear and fence activity._workingTimer; an old timeout cannot clear replacement working state. Test replacement and same-object reconnect, stale send outcomes, same-session competing history finally, and cleanup without replay.


## Root isolated consumer admission

The independently verified 14-11 archive `b896e1df2d7da596f6f6dd0f3d2a26f225ffbe11587d95fae1deb233818595e9` is selected. Exact snapshot: `/tmp/minion-14-consumers-61p26r_z/minion_site`. Its private node_modules/@minion-stack/shared contains all121 extracted files, runtime and declarations together. Other dependencies are read-only links enumerated in dependency-links.json; before-manifest.json identifies copied current source. Do not mutate linked dependencies, active checkout, package caches or installed peers. Force optimizer/cache output inside the private snapshot. No source alias for shared, no mixed old declarations, and no silent nested-resolution substitution: record the actual resolved shared entry and types.

Task1 is admitted in this snapshot only, owning src/lib/services/member-gateway.contract.fixture.ts and vitest.gateway-contract.config.ts plus private check logs and the canonical SUMMARY. Read all app instructions from the active checkout. Run the unchanged actual service with the emitted candidate, synthetic environment and transport to establish genuine failing acceptance assertions. The candidate runtime deliberately includes the new callback while unchanged service still omits adoption; the active old installed package remains a preserved separate control. Inspect installed runner version: Vitest2 accepts minWorkers/maxWorkers, Vitest4 must use maxWorkers only. Override envDir:false and avoid real app/server startup.

Tasks2/3 source implementation remain gated on root review of Task1 fixture/red results. Their later source ownership will be the same listed files INSIDE this snapshot. This amendment does not authorize active checkout edits or represent completed installation. After isolated acceptance root must choose a separately recorded adoption transaction that updates matching package/runtime/declarations with source; until then no installed-app or production qualification claim. Root owns aggregate checks and handoff proposal.


## Root Task2 isolated source admission

Root inspected the actual-service fixture SHA2406b5a7d9d096735fb88992312126a34f020f321a2214c0b0b7feba097391ab and native-plugin config SHA2b4192e6e00c455526a6a26a4149df5142a07f7f61ee01c6904c4cdd35c7db8c. Seven genuine behavioral failures and two controls establish reconnect publication, stale close/send/history and activity ownership gaps. Initial collection/config failures are not behavioral evidence. Native installed Svelte plugin remains in use; the fixture-only explicit esbuild compiler options are accepted for this focused lane, not full application compilation. Exact Vitest2/nestedVite5 tooling is recorded separately from top-levelVite6.

Admit Task2 edits to the single listed service INSIDE `/tmp/minion-14-consumers-61p26r_z/minion_site`, plus previously owned fixture/config/SUMMARY. Preserve original copied service and manifest hashes before editing. Expand meaningful cases to all three reporters with canaries/throwing sinks, duplicate publication, stale challenge/initialization/polling/history/send outcomes and same-object reconnect. Do not replay effects or change optimistic chat selection. Source freeze returns to root for independent review. Task3 may run focused fixture/declaration checks only; full app check/adoption remains root-scheduled. No active checkout/source/installed package mutation follows from this admission.
