---
phase: 14-sdk-transport
plan: "13"
type: execute
wave: 2
depends_on: ["11-07"]
files_modified:
  - "packages/shared/src/gateway/shells.ts"
  - "packages/shared/src/gateway/shells-outcome.test.ts"
autonomous: false
requirements: ["SDK-01", "AGT-04"]
readiness: "ADMITTED: root accepted independent review b7bc433c; execute exactly two source files using the recorded private native runner."
closure_policy: "A pure version contract cannot advertise a live durable receiver or prove sender delivery."
must_haves:
  truths:
    - "Durable version 1 is selected only when both sides explicitly advertise version 1."
    - "Missing capabilities retain explicit legacy behavior; unsupported or malformed versions never become durable v1."
    - "The existing canonical admission/outcome and legacy register shapes remain compatible."
  artifacts:
    - path: "packages/shared/src/gateway/shells.ts"
      provides: "Additive receiver acknowledgment field and browser-safe bilateral version decision"
    - path: "packages/shared/src/gateway/shells-outcome.test.ts"
      provides: "Actual canonical negotiation cross-product and existing contract regression cases"
  key_links:
    - from: "packages/shared/src/gateway/shells.ts"
      to: "minion/src/shells/manager.ts"
      via: "Later exact candidate package and independently admitted receiver wiring; no live feature advertisement here"
---

<objective>
Finish the minimal canonical bilateral negotiation contract before building another complete private shared package. Separate this two-file additive change from gateway parity, input canonicalization, runtime persistence and package adoption.
</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/11-agent-lifecycle/11-07-VERIFICATION.md
@.planning/phases/14-sdk-transport/14-DURABLE-RECEIVER-DECISION.md
@.planning/phases/14-sdk-transport/14-12-PLAN-REVIEW.md
@packages/shared/src/gateway/shells.ts
@packages/shared/src/gateway/shells-outcome.test.ts
</context>
<boundaries>
Exactly two canonical source/test files. Read all applicable instructions before source mutation. Do not modify gateway shim, bridge, manifests, locks, dist, node_modules, receiver routing, UI or databases. Preserve concurrent WIP; no branch/stash/worktree/commit/publish/deploy. No provider, live gateway, browser, network or package installation. Root prepares a credential-free temporary copied-source test environment using installed tools through read-only links, excluding mutable .vite/.vite-temp caches. Record and verify actual private cache resolution before testing. Do not reuse the earlier 14-11 archive as if it contains this contract. This child requires a later complete emitted package and gateway parity/input child; neither is hidden in this scope.
</boundaries>
<tasks>
<task type="auto">
  <name>Task 1: Add and test explicit bilateral durable negotiation</name>
  <files>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/shells-outcome.test.ts</files>
  <read_first>ShellCapabilities, ShellsRegisterResponse, ShellOutcomeValidationError, original register shape tests, receiver decision and independent review</read_first>
  <action>Add optional durableOutcomeVersion?: 1 to ShellsRegisterResponse with a precise comment: the receiver sets it only after actual storage readiness and current registered peer checks. Reuse the existing fixed validation error. Add a small pure browser-safe helper that takes both sides' unknown advertised versions and returns 1 only for (1,1), undefined for supported legacy absence on either side, and throws for any explicit unsupported/malformed value. Validate both supplied values even if the other side is absent; no truthy coercion, default version, automatic fallback or inferred readiness. Use undefined only for missing advertisement; null, booleans, strings, nonfinite/fractional numbers, objects and arrays are invalid. This helper validates a contract, never caller authority, storage readiness or wire envelope size.

Write meaningful tests before implementation for the full supported cross-product, all explicit malformed classes on either side, and simultaneous legacy/malformed input. Preserve the existing admission/outcome/digest/limits tests and typed legacy registration response without the new field. Use the actual canonical helper, not a copied implementation. Keep required-durable policy at the later caller: undefined is observable legacy, never permission to silently downgrade a required durable operation. Add an exact-site TODO and root proposal pointer for the still-unwired receiver/sender use. No full register schema rewrite or generic protocol framework.</action>
  <verify><automated>node node_modules/vitest/vitest.mjs run src/gateway/shells-outcome.test.ts --maxWorkers=1</automated></verify>
  <done>Meaningful failing negotiation cases turn green and existing canonical contracts remain passing under an isolated native runner.</done>
</task>
<task type="auto">
  <name>Task 2: Freeze canonical source and verify browser-safe types</name>
  <files>packages/shared/src/gateway/shells.ts, packages/shared/src/gateway/shells-outcome.test.ts</files>
  <action>Run the same focused suite, isolated strict package type check and scoped lint without broad socket tests. Verify barrels already expose the helper and no Node builtin/dependency enters the browser-safe module. Record before/after hashes, exact commands, runtime/tool versions, selected tests and private-cache evidence. Root independently reviews the diff and repeats the focused signal. Freeze source for a separate complete private package emission; do not alter active dist or claim published version compatibility.</action>
  <verify><automated>git diff --check -- packages/shared/src/gateway/shells.ts packages/shared/src/gateway/shells-outcome.test.ts</automated></verify>
  <done>Root has an independently verified, exact source identity suitable for later full-package qualification. No runtime integration or requirement closure is asserted.</done>
</task>
</tasks>
<verification>
Standards: strict types, existing error vocabulary, native test setup and private caches, two-file ownership. Spec: only bilateral explicit v1 selects durability; absence and malformed explicit advertisement differ. Prior 11-07 journal runtime bytes remain a dated receipt; this additive contract must be included in a new matching artifact before gateway imports. Root owns summary/proposal/global inventory and cannot close AGT-04 or SDK-01 from this helper.
</verification>
<success_criteria>
Canonical sender and receiver can share one deterministic version decision, with every supported/invalid branch tested and no live receiver advertisement before its later implementation.
</success_criteria>
<output>
Root creates 14-13-SUMMARY.md and independent verification after actual execution, with exact source/artifact boundaries and remaining package/receiver/sender gates.
</output>


## Root execution admission

Root accepts independent review SHA b7bc433ccedefbfa99c3ed4421372d4f89eeed87dd618a0eb8c7c5e60726ef84. Test snapshot is /tmp/minion-14-13-cuet0lis/shared (31 current source files, six individually linked installed dependencies, private .vite/.vite-temp/private-cache). Use native run-contract.mjs with empty environment and actual cache-path assertion; do not run the sample CLI through an active default configuration. Execute on existing Node only. Root owns these two source files until freeze, then an independent reviewer verifies. No receiver or complete artifact generation is admitted here.
