---
phase: 14-sdk-transport
plan: "06"
type: execute
wave: 2
depends_on: ["14-04", "14-09", "14-15"]
files_modified:
  - "minion_hub/src/lib/plugins/PluginIframe.svelte"
  - "minion_hub/src/lib/plugins/PluginIframe.test.ts"
  - "minion_hub/vitest.plugin-compat.config.ts"
  - "minion_hub/tests/fixtures/plugin-compat/Fixture.svelte"
  - "minion_hub/tests/fixtures/plugin-compat/gateway-fixture.svelte.ts"
  - "minion_hub/tests/fixtures/plugin-compat/build.mjs"
  - "minion_hub/tests/fixtures/plugin-compat/README.md"
  - ".planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md"
autonomous: false
requirements: ["SDK-01", "SDK-02"]
readiness: "Tasks 1–3 independently verified in private candidate, including actual native iframe/source/order checks and final paired Hub check; active/deployed adoption remains separate."
closure_policy: "Component tests cannot certify gateway manifest normalization, installed extension bundles or deployed gateways."
must_haves:
  truths:
    - "A plugin with declared requirements cannot mount a privileged bridge while the corresponding gateway facts are unknown or incompatible."
    - "Late capability imports, hello changes and gateway switches cannot authorize an old iframe against a new connection."
    - "Unconstrained legacy plugins and omitted protocol version v1 remain supported under 14-04's peer validation."
  artifacts:
    - path: "minion_hub/src/lib/plugins/PluginIframe.test.ts"
      provides: "Actual mounted Svelte component and bridge behavior rather than a component-existence assertion"
    - path: ".planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md"
      provides: "Selected connection/hello identity, local tests and browser/runtime limits"
  key_links:
    - from: "minion_hub/src/lib/plugins/PluginIframe.svelte"
      to: "minion_hub/src/lib/plugins/compat.ts"
      via: "Reactive constrained compatibility gate before iframe and privileged host creation"
    - from: "minion_hub/src/lib/plugins/PluginIframe.svelte"
      to: "minion_hub/src/lib/services/gateway-rpc.ts"
      via: "Reviewed active-client identity checked at actual dispatch, including after asynchronous module resolution"
---

<objective>
Connect the reviewed compatibility utility to the actual plugin component lifecycle. Eliminate the permissive pre-capability mount and stale one-shot capability snapshot without expanding this child into gateway loader or transport reconstruction.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
@.planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md
@.planning/phases/14-sdk-transport/14-04-SUMMARY.md
@minion_hub/src/lib/plugins/PluginIframe.svelte
@minion_hub/src/lib/plugins/PluginIframe.test.ts
@minion_hub/src/lib/plugins/compat.ts
@minion_hub/src/lib/plugins/bridge-host.ts
@minion_hub/src/lib/state/gateway/gateway-data.svelte.ts
@minion_hub/src/lib/state/gateway/connection.svelte.ts
@minion_hub/src/lib/state/features/hosts.svelte.ts
@minion_hub/src/lib/services/gateway-rpc.ts
</context>

<boundaries>
This draft is not execution authority. Root owns admission, global plans/status, proposal integration and the 14-04 source freeze. Initial reviewed Hub HEAD was a25528b603dfe25f7ea9c0900d271060e5394f35 on feat/level-2026-07-30; recheck exact source hashes and concurrent transport edits before admission. Do not change branches/worktrees/stash, commit, install packages, edit node_modules/locks, use application env/secrets, start the full application, call a live gateway/API, or use paid services.

Only listed files may change. Read-only inspection of existing state, client and gateway manifest/projection is allowed. Gateway manifest.ts/server-methods/plugins.ts, gateway.svelte.ts, gateway-rpc.ts, connection/state modules, shared client packages, protocol/compat utility and token/RPC authorization policy remain outside this child. If the component cannot establish connection-to-hello association using existing public read-only seams, stop the dependent source step and request an exact child amendment; do not pretend gatewayUrl equality or a fabricated test epoch proves it. Source implementation choices already authorized by the user require root coordination, not repeated user permission.

UI governance applies to component logic and any waiting/error markup. Reuse semantic tokens and existing state primitives; no general restyling, new visual system, theme cleanup or weakened lint baseline. Browser fixtures use synthetic state and plugin content with loopback-only service, deny external requests, and obtain a uniquely owned browser-harness session under separate root admission. No visible desktop browser or default shared daemon during concurrent browser work.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze actual connection identity and mounting test seams</name>
  <files>.planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md</files>
  <read_first>PluginIframe.svelte:140-170 and 255-377, gateway-data.svelte.ts:14, connection.svelte.ts, gateway-rpc.ts:getClient/sendRequest, gateway.svelte.ts:wsConnect/resetGatewayData, existing PluginIframe.test.ts and vitest.config.ts</read_first>
  <action>Record exact hashes and active source ownership. Trace hello publication/clearing, connected state, active host, client replacement, same-URL reconnect and prop changes. The current component resolves gw once, snapshots hello, permits before capsLoaded and reuses global dispatch after awaiting a module import. Establish a real association between the component's selected gateway, the hello being evaluated and the exact dispatch client. Reuse existing getClient/state seams without altering them; a stable client snapshot checked before and after awaits may fence dispatch, but is not by itself proof that an older hello belongs to that client. Freeze behavior for absent compat, requiredRpc/minimum-version unknown, known empty methods, malformed constraints, late hello, module-import failure, disconnect, reconnect, gateway switch and failed later compatibility. Inspect installed Svelte testing library/happy-dom and compiler conditions without installing anything. Specify an isolated config selecting this component test only, retaining safe env stubs and excluding application bootstrap. Root must accept this identity/test receipt before source changes. Gateway normalization/projection is independently testable and excluded unless this inspection proves an inseparable missing identity seam.</action>
  <verify><automated>git diff --check -- .planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md</automated></verify>
  <done>A root-reviewed receipt names the actual identity and reactive observation mechanism, with any missing upstream seam blocking the corresponding claim.</done>
</task>

<task type="auto">
  <name>Task 2: Gate and retire the actual mounted plugin lifecycle</name>
  <files>minion_hub/src/lib/plugins/PluginIframe.svelte, minion_hub/src/lib/plugins/PluginIframe.test.ts, minion_hub/vitest.plugin-compat.config.ts</files>
  <action>Replace existence-only tests with real mounted Svelte component tests under the isolated config. Keep actual compat, mountHostBridge and HostBridge implementations; inject only external gateway state/dispatch and synthetic browser peers. Drive state through Svelte reactivity rather than mutating a nonreactive mock object. Write red cases proving zero iframe/privileged bridge/forwarded RPC while declared required facts are unknown, including the delayed-import window. Apply the selected gate; distinguish pending constraints from known incompatibility using existing UI primitives. Unconstrained legacy remains supported and peer version omission remains v1.

Observe late hello and changes after the initial import. Retire the old bridge and clear per-mount timers, ready/loaded/diagnostic/save state when identity changes, capabilities are revoked or the component is destroyed. Fence asynchronous module resolution, timeout probes and result handlers so stale work cannot authorize or update a later mount. Evaluate capability and connection identity at dispatch as well as render; a queued old-frame RPC or an await before dispatch must never reach a replacement client. Preserve legitimate generation/edit timeouts, ready-before-onload registration, both handshake orderings, locale/theme updates, external save-bar bindings and separated plugin asset origin versus gateway identity. Do not broaden the privileged method set or change auth token delivery here.

Tests must cover known empty methods versus missing advertisement; valid requirements; missing required method; unsupported/malformed bridge/minimum strings via the real utility; late hello; rejected import; disconnect/reconnect including same URL; old hello/new client mismatch; gateway switch during RPC module resolution; old iframe ready/RPC after replacement; mount/unmount/remount; and delayed diagnostics after disposal. Assert zero old-client/new-client privileged calls in denial cases, exact accepted client identity in success cases, listener/timer cleanup and no theme/locale-induced remount loop. If a test requires changes outside ownership, request an exact amendment rather than weakening it.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.plugin-compat.config.ts src/lib/plugins/PluginIframe.test.ts &amp;&amp; bun run lint:design &amp;&amp; bun run lint:tokens)</automated></verify>
  <done>The actual component's constrained gate and selected connection lifecycle pass; prior behavior assertions remain meaningful. Root serializes the full Hub check after source freeze.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify native iframe behavior in an isolated browser fixture</name>
  <files>minion_hub/tests/fixtures/plugin-compat/Fixture.svelte, minion_hub/tests/fixtures/plugin-compat/gateway-fixture.svelte.ts, minion_hub/tests/fixtures/plugin-compat/build.mjs, minion_hub/tests/fixtures/plugin-compat/README.md, .planning/phases/14-sdk-transport/14-PLUGIN-MOUNT-RECEIPT.md</files>
  <action>Build a standalone fixture using the actual PluginIframe component and actual host bridge, with reactive gateway state and explicit captured RPC counters from the fixture module. Reuse the installed Vite/Svelte approach of tests/fixtures/overlay-native/build.mjs without editing that fixture. Build only to a root-selected unique temporary directory; no full Hub build. Generate entry HTML and synthetic plugin frames from the builder; no extra unlisted source fixtures or package changes. The fixture must expose deterministic controls for capabilities, client identity, gateway switch and late hello without calling production APIs.

Before browser execution read browser-harness, obtain exact loopback port/output and exclusive session admission, and deny all non-fixture requests. Prove with real WindowProxy/message events that a constrained iframe is absent while unknown, appears only after compatible current hello, retires on revocation/switch and cannot dispatch from an old window. Exercise ordinary iframe source/origin checks and post-disposal behavior, both ready/hello orderings, and waiting/incompatible states at 390x844 and 1440x900. Record screenshots and exact served fixture/source digests; use rendered states and counters, not a component screenshot alone. Do not infer mobile or deployed platform compatibility from this narrow fixture. Browser unavailability remains a named open gate, not a skipped passing test.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node tests/fixtures/plugin-compat/build.mjs)</automated></verify>
  <done>Receipt separates mounted unit, native browser, installed-package and deployed evidence; root independently reviews it. Gateway manifest/projection and bundle adoption remain separate children.</done>
</task>
</tasks>

<verification>
Exact local source and artifact hashes, true red/green consumer tests, synthetic data only. Scoped diff checks and the UI lint gates are required. Root owns serialized Hub check and browser admission. Standards and Spec reviews are separate. A changed utility or fake bridge cannot count as actual component enforcement.
</verification>

<success_criteria>
The selected component connection/hello gate is enforced before privileged mounting and dispatch, and stale connection work cannot cross to a new client. No mandatory outside-owner gate is silently omitted. SDK-01/02 remain open pending upstream/installed/runtime receipts.
</success_criteria>

<output>
After actual execution, request root admission for 14-06-SUMMARY.md with plan hash, selected identity, tests, browser evidence and remaining gates. Root owns proposal/status integration. This draft creates no source or browser authority.
</output>


## Root evidence-task admission

Task 1 and its owned receipt only are admitted. Root reviewed the exact file list and excluded seams; source Tasks 2/3 remain gated on that receipt, connection or bridge-region contract decisions, and separate browser-session admission. No UI, gateway state, artifact, stored content or dependency change is authorized by this evidence task.


## Root paired component fixture admission

Root accepts the updated identity receipt d19ad4a87a7e6ada6998f16b4371fa25b0cf021bc321faf2badad2329cf3b3ce and independently verified14-09 Task4 service d047a0516d53d2a2c8077205b2b41cfb8a1cbcce9e40952a742595a9d8752f7e, fixture e20561f0439040f2192681f1e6570c65fbd848c41be6028826503378ead64f16. All53 service cases and full native Hub check pass. Use only the same private /tmp/minion-14-consumers-61p26r_z/minion_hub candidate, which retains the exact prior14-11 reconnect archive. Active Hub remains unchanged.

Initial Task2 admission covers only PluginIframe.test.ts and vitest.plugin-compat.config.ts in that private candidate. Create genuine mounted component red cases with actual HostBridge/compat/service/state/RPC/shared client, actual native host-selection reactivity and synthetic transport/HTTP surroundings. Reuse the existing native compiler/cache pattern; inspect effective client compilation for both .svelte/.svelte.ts and real reactive subscription. No application environment, server, browser, provider, DB, package install or unrelated default suite. Record fake transport/mocks and zero native network attempts. Root must review the baseline before PluginIframe.svelte behavior changes. No replacement of the session accessors with a fixture counter or fake predicate.

Component authorization policy selected for later implementation: all privileged mounts require a current accepted session, including unconstrained legacy plugins. Legacy compatibility remains supported once authenticated. Declared unknown facts block the mount. Capture the real token/client and a separate component mount identity; validate both before/after awaited dispatch, HTTP JSON, probes and save callbacks. A→B→A may restore the same service token if A stayed authenticated, but must never resurrect a retired component's callbacks. Preserve generation/edit timeouts, token delivery policy, same-mount theme/locale and existing save bindings. URL/CDN routing equality is not authority; capture selected host association and reject a mismatched component gateway prop. Browser Task3 and active adoption remain separate gates.


First-mount routing follow-up: root identified that the token also needs construction-time gateway URL to compare the component's routing prop without trusting a subsequently edited host list. A two-file14-09 correction is admitted before component source work. The fixture may prepare independently, but its final paired baseline must use that corrected token. Exact HTTP/WS URL normalization belongs to this component; CDN override remains an asset origin and never substitutes for connection identity.


## Root Task 2 source admission after independent red repeat

Root independently reran the frozen paired fixture: 13 tests, 5 passing controls and 8 assertion failures, 4.13 seconds, exit1; root-plugin-compat-red.log in the private Hub records the result. No setup or unhandled errors were reported. Receipt 4a5ff26819404fe58033e9005ab5088c8fb03146c0d39dc131904e1e95379c57 and component f3a33500d1f80697f6499164a219d2ec8259b588dfd0ac19389ec52ae11835a3 are the reviewed baseline. Paired accepted-session service is now50c4ac224ce35a48a9ed09c86bc8c054f60231df162bb22d246bf8fdd31f5110 (58 focused cases independently green; final aggregate check pending).

Task2 source/config/tests are admitted only in /tmp/minion-14-consumers-61p26r_z/minion_hub and only the listed three files. Keep the actual service/client/HostBridge/compat and selected native compiler/cache/zero-network fixture. Fix the first-ready effect dependency loop as part of lifecycle ownership; one ready message must retain the authenticated peer. Add the task's remaining asynchronous retirement, reconnect/cutover, theme/locale/save and timeout coverage. Root owns final aggregate check and adoption. No active Hub source edit, browser task, additional file or dependency mutation is admitted here. Update the owned receipt with exact frozen evidence and limitations.


## Root Task2 review corrections and typed classification dependency

Independent47-case repeat passed, but full native Hub check found two fixture typing errors. The component/test owner corrected them, and two additional real mounted regressions proved native URL serialization differences could prevent bridge mounting.49 focused cases now pass at component1e63b1be2ac3a76a5487369eec0542cd168e0e7b51e94ab78bff02df5b1b9ba2 and test63acb032460eb6b074df3f042141b9eb89887c29ffbdb142ab384f6a593a7184; finalaggregate pending.

Root review also confirmed that existing compat utility cannot distinguish missing facts from known incompatibility.14-15 owns exactly that utility/test amendment in the same private candidate. Once its typed result freezes, Task2 may consume false verdict kind pending/incompatible inside the existing component/test ownership, preserving unknown fact absence instead of coercing it to an empty advertisement. Manual component denial reasons must declare their kind. Add actual rendered waiting-versus-incompatible cases, including invalid-constraint precedence, without copying version grammar or parsing prose. Both false branches remain unprivileged. Utility and component tests/fullcheck must run only after coordinated source freeze. BrowserTask3 remains separately gated.


## Root native fixture build admission

Final component/test/config are frozen at the identities in14-06-VERIFICATION.md;56 independent cases and the full paired Hub check pass. Task3 may implement only its four listed private fixture files in the existing Hub snapshot. Reuse actual PluginIframe, compat, HostBridge, shared client, gateway service/RPC/state/hosts; no substituted session authority. Synthetic WebSocket/fetch must be installed before service imports, with explicit request counters and controls driving real challenge/connect/hello. Unrelated application modules may be virtual aliases only, recorded individually.

Build output is `/tmp/minion-14-06-native-browser-20260909`, at most50MiB/500files and120seconds. Installed native Vite/Svelte/Tailwind only; no environment loading, native network, dependency edits or remote fonts. Generated static server must bind literal127.0.0.1 only, ports18876/18877/18878, accept GET/HEAD for manifest-listed files only, reject traversal and other methods, provide no app route fallback, and deny external connections with CSP. Root reviews generated server before starting it. Root exclusively owns headless9223 session `minion-360-root-ui`; no browser is started by the fixture agent. Native event/WindowProxy claims require genuine browser events. Active source, packages and deployment remain unchanged.
