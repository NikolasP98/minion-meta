---
phase: 14-sdk-transport
plan: "07"
type: execute
wave: 2
depends_on: ["14-04"]
files_modified:
  - "minion_hub/src/lib/artifacts/artifact-bridge.ts"
  - "minion_hub/scripts/artifacts/build-inline-bridge.mjs"
  - "minion_hub/scripts/artifacts/inline-bridge-NOTICES.txt"
  - "minion_hub/src/lib/artifacts/builtin/bridge.provenance.json"
  - "minion_hub/src/lib/artifacts/builtin/overview/index.html"
  - "minion_hub/src/lib/artifacts/builtin/triage/index.html"
  - "minion_hub/src/lib/artifacts/builtin/artifact-builder/index.html"
  - "minion_hub/src/lib/artifacts/builtin/bridge.contract.test.ts"
  - "minion_hub/src/lib/server/artifacts/builder-prompt.ts"
  - "minion_hub/src/lib/server/artifacts/builder-prompt.test.ts"
  - "minion_hub/tests/fixtures/artifact-bridge/build.mjs"
  - "minion_hub/tests/fixtures/artifact-bridge/README.md"
  - ".planning/phases/14-sdk-transport/14-ARTIFACT-BRIDGE-RECEIPT.md"
autonomous: false
requirements: ["SDK-01", "SDK-02"]
readiness: "ADMITTED_TASK2: generated package reuse and exact adapter contract selected; native browser Task 3 remains gated."
closure_policy: "Local builtin and prompt tests do not certify arbitrary generated HTML or mutate stored artifacts."
must_haves:
  truths:
    - "Each of the three actual builtin HTML peers validates the expected parent window and exact host origin before accepting host frames."
    - "The raw peers interoperate with the reviewed opaque sandbox host, including omitted-version legacy v1."
    - "Prompt/template expectations and generation enforcement are distinguished; stored artifacts are not silently rewritten."
  artifacts:
    - path: "minion_hub/src/lib/artifacts/builtin/bridge.contract.test.ts"
      provides: "Actual builtin script execution paired with the actual Hub HostBridge"
    - path: ".planning/phases/14-sdk-transport/14-ARTIFACT-BRIDGE-RECEIPT.md"
      provides: "Per-builtin before/after bytes, prompt propagation evidence and explicit generation/storage/browser gaps"
  key_links:
    - from: "minion_hub/src/lib/artifacts/builtin/overview/index.html"
      to: "minion_hub/src/lib/server/artifacts/builder-prompt.ts"
      via: "Real reference HTML embedded by buildBuilderPrompt and buildRegeneratePrompt"
    - from: "minion_hub/src/lib/artifacts/builtin/bridge.contract.test.ts"
      to: "minion_hub/src/lib/plugins/bridge-protocol.ts"
      via: "Actual generated package scripts exchange structured-clone frames with actual opaque-mode host"
---

<objective>
Replace the three handwritten artifact protocol implementations with a generated inline bundle of the qualified PluginBridge plus a thin context/render adapter, and make prompt propagation explicit. Preserve self-contained artifact delivery and the restricted opaque sandbox context RPC without claiming that prose instructions enforce arbitrary generated code.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.claude/skills/ui-design-governance/SKILL.md
@.planning/phases/14-sdk-transport/14-PLUGIN-BRIDGE-MATRIX.md
@.planning/phases/14-sdk-transport/14-04-SUMMARY.md
@minion_hub/src/lib/components/artifacts/ArtifactHost.svelte
@minion_hub/src/lib/artifacts/builtin/overview/index.html
@minion_hub/src/lib/artifacts/builtin/triage/index.html
@minion_hub/src/lib/artifacts/builtin/artifact-builder/index.html
@minion_hub/src/lib/server/artifacts/builder-prompt.ts
@minion_hub/src/lib/server/artifacts/builder-prompt.test.ts
@minion_hub/src/lib/server/artifacts/builder.ts
</context>

<boundaries>
Draft only until root admission. Preserve all concurrent WIP and the 14-04 source freeze. Initial inspected Hub HEAD was a25528b603dfe25f7ea9c0900d271060e5394f35 on feat/level-2026-07-30; record live source hashes before execution. No branch/worktree/stash changes, commits, dependency/lock edits, package installs, model calls, external network, application env or database access. Do not start the full Hub or change stored HTML, artifact store, generation routes or customer data.

Own exactly the three local HTML peers, thin artifact adapter, isolated inline generator/provenance, actual-script tests, pure builder-prompt module/tests and isolated browser builder/receipt. ArtifactHost.svelte, bridge-protocol.ts, builder.ts, store.ts and all generated/persisted HTML are read-only. Changes to render content, data authorization, markup/styles/theme ownership or general HTML sanitization need a separate child. UI governance applies: retain semantic token usage, add no styling debt and run both lint gates even for script-only HTML changes.

A local VM/DOM fixture may execute only checked-in trusted builtin scripts, with injected synthetic DOM/windows and no fetch/process/service access. It is a test mechanism, not a sandbox for arbitrary generated or user HTML. Native browser tests require root-selected temporary output, loopback port and exclusive browser-harness session; block external requests. No visible browser or sharing another agent's session.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze three actual scripts and generation contract limits</name>
  <files>.planning/phases/14-sdk-transport/14-ARTIFACT-BRIDGE-RECEIPT.md</files>
  <action>Record exact complete HTML/script hashes and the actual ArtifactHost mount options. Confirm sandbox allow-scripts produces child origin null; the child's inbound host origin remains the exact nonopaque parent origin from its URL hint. Inventory the raw peer differences, pending-request storage, duplicate hello behavior, theme/token projection, render/error paths and teardown. All three currently check origin only and accept unvalidated host fields. They issue ready version1 and request hub.artifact.context.get after hello.

Trace builder.ts importing overview HTML as the actual reference and passing it into pure buildBuilderPrompt/buildRegeneratePrompt. Current prose says copy the whole script verbatim while also editing render(), which is inside that script; record a precise reusable bridge-region versus render-adapter contract that resolves this contradiction without a new dependency. Current validateBundle checks only superficial HTML/script/context-call presence. Select whether this child updates prompt wording and tests only (recommended minimum), or request a separate builder enforcement child. Do not mistake a matching script substring/hash inside arbitrary generated HTML for proof that no other unsafe script runs. Root must accept these semantics and exact test fixtures before source edits. No generated-output or stored-artifact migration in this task.</action>
  <verify><automated>git diff --check -- .planning/phases/14-sdk-transport/14-ARTIFACT-BRIDGE-RECEIPT.md</automated></verify>
  <done>The receipt freezes supported legacy, origin, lifecycle and prompt scope, and names the generation-enforcement/storage migration gates separately.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the qualified package into actual artifact peers</name>
  <files>minion_hub/src/lib/artifacts/artifact-bridge.ts, minion_hub/scripts/artifacts/build-inline-bridge.mjs, minion_hub/src/lib/artifacts/builtin/bridge.provenance.json, minion_hub/src/lib/artifacts/builtin/overview/index.html, minion_hub/src/lib/artifacts/builtin/triage/index.html, minion_hub/src/lib/artifacts/builtin/artifact-builder/index.html, minion_hub/src/lib/artifacts/builtin/bridge.contract.test.ts, minion_hub/src/lib/server/artifacts/builder-prompt.ts, minion_hub/src/lib/server/artifacts/builder-prompt.test.ts</files>
  <action>Apply the root-selected reuse contract in 14-ARTIFACT-REUSE-AMENDMENT.md (daa02905ed86fcf05348410e4468f233651682ef9e93c450bca1a7cfbc8de2da). Source admission requires the independently verified 14-04 request-identity amendment and root-selected new entry digest; old installed0.4.0 is not a fallback. Freeze exact Vite/lock/tool inputs. Add the three owned adapter/generator/provenance files to this task's execution manifest.

Build one thin adapter importing PluginBridge. It owns exact host hint parsing, validated theme projection, one context call per supported bridge lifetime, render/failure continuation fences and document lifecycle only. No frame parser, pending map, request-ID generator, duplicate RPC transport or new dependency. On pagehide dispose the active bridge; on persisted pageshow create exactly one fresh lifetime and handshake. Initial nonpersisted pageshow does not duplicate initialization. Final disposal removes document handlers. Late prior-lifetime context responses must not render.

Use the already declared Vite library IIFE build with configFile:false, envFile:false, write:false, fixed target/minifier and no sourcemap. Alias only the qualified package import to the exact selected entry. Assert one standalone JS chunk, expected graph modules only, no external/dynamic imports, assets, maps, eval/new Function or network bootstrap. Preserve applicable license notices. Generator --check rebuilds in memory and compares actual delimited regions and provenance; write changes only those regions in the three HTML files plus provenance. Preserve unrelated render/markup/style bytes. Record input and emitted region digests, no timestamps or absolute workstation paths. Two actual builds must be byte-identical. Negative tests reject digest/graph/module/output mismatches and old installed candidate selection.

Write meaningful baseline tests against the actual checked-in HTML. Then execute actual generated script regions from all three final HTML files against the actual Hub HostBridge with queued structured-clone windows and injected controlled DOM. Cover wrong source/origin, invalid host hints, malformed fields, legacy omitted/current/unsupported version, success/error/empty context, unknown correlation IDs, duplicate hello/response, theme updates, send failure, pagehide and persisted-pageshow, and same-tick late responses across two package lifetimes. Verify exact context-method counts and zero unauthorized forwarding. Native browser restoration remains separate from synthetic persisted events.

Update pure builder prompt/regenerate/repair expectations using the actual new overview reference: protected generated region must be retained; render adapter customization remains explicit outside it. Preserve shallow validateBundle acceptance behavior and record its limitation at the owned prompt boundary. builder.ts is read-only; exact-site generation enforcement changes require separate admission. No paid generation or stored-artifact rewrite.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/artifacts/builtin/bridge.contract.test.ts src/lib/server/artifacts/builder-prompt.test.ts --maxWorkers=1 &amp;&amp; bun run lint:design &amp;&amp; bun run lint:tokens)</automated></verify>
  <done>Each actual builtin peer and pure prompt path passes; no copy of the protocol substituted for the consumer. Root serializes full Hub check after source freeze.</done>
</task>

<task type="auto">
  <name>Task 3: Verify actual opaque iframes and record unchanged stored-artifact gates</name>
  <files>minion_hub/tests/fixtures/artifact-bridge/build.mjs, minion_hub/tests/fixtures/artifact-bridge/README.md, .planning/phases/14-sdk-transport/14-ARTIFACT-BRIDGE-RECEIPT.md</files>
  <action>Create a standalone local browser fixture builder using installed Vite tooling and actual mountHostBridge/HostBridge imports. Generate the small parent control page in the selected temporary output and copy the three exact current HTML artifacts there without modifying their bodies. Serve only that output over an admitted loopback port. Parent uses sandbox='allow-scripts', empty gateway credentials and a synthetic allowlisted context responder; source hashes and RPC counters are visible to the harness. Do not run the app, ArtifactHost API fetch, real agent registry or builder pipeline.

After separate root browser/session admission and reading browser-harness, execute all three artifacts using their real iframe navigation URL/hash and actual WindowProxy semantics. Verify child MessageEvent.origin is null at the host, exact parent origin at the child, wrong-sibling frames cannot trigger context RPC or rendering, malformed/unsupported hello is ignored, supported legacy/current hello renders synthetic context, and removing/navigating an iframe cannot update the replacement. Capture each loading/render/error outcome at 390x844 and 1440x900 using screenshots plus call counters/console evidence; distinguish script rendering from full responsive product qualification. Record exact served bytes and cleanup receipts. Verify native crypto.getRandomValues inside the actual opaque iframe. Qualify real persisted-pageshow restoration and record the actual persisted flag, exactly one fresh context request and no stale rendering; if the browser does not enter BFCache, preserve that native gate as unverified rather than substitute a synthetic pass. If native lifecycle exposes an ArtifactHost lifecycle issue outside ownership, request a separate exact component child rather than altering it here.

Document that existing database-stored/generated artifacts retain their original bytes and may still contain old peers. A later read-only inventory and an explicit migration/regeneration policy must precede any stored rewrite. Prompt tests prove template propagation only; runtime injection, generation enforcement, arbitrary script confinement and stored adoption remain separate gates.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node tests/fixtures/artifact-bridge/build.mjs)</automated></verify>
  <done>Receipt separates actual local HTML script tests, native opaque browser behavior, prompt propagation, arbitrary generated output and stored/deployed adoption. No missing gate is marked passed.</done>
</task>
</tasks>

<verification>
Capture exact before/after HTML and script-region bytes, original red failures, per-builtin real-host results, UI lint gates and root's serialized check. Browser work remains gated until its exact fixture/session is admitted. Standards and Spec reviews are independent. A regex or substring check on generated HTML is not a code-confinement security proof.
</verification>

<success_criteria>
All three local builtin peers enforce the selected host identity/message/lifecycle contract and remain compatible with the real opaque-mode host. Prompt propagation is accurate and tested. No claim extends to untested stored/generated/deployed HTML or broader artifact security.
</success_criteria>

<output>
After actual execution, request root admission for 14-07-SUMMARY.md with plan hash, source/served identities, per-builtin tests and outstanding generation/storage/runtime decisions. Root owns proposal/index/status integration. This draft admits no implementation.
</output>


## Root evidence-task admission

Task 1 and its owned receipt only are admitted. Root reviewed the exact file list and excluded seams; source Tasks 2/3 remain gated on that receipt, connection or bridge-region contract decisions, and separate browser-session admission. No UI, gateway state, artifact, stored content or dependency change is authorized by this evidence task.

## Root reuse/lifecycle selection

Root selects generated package reuse and fresh persisted-pageshow lifetimes per the reviewed amendment packet. The three added files above are the complete ownership expansion. Task 2 remains gated only on revised package acceptance and exact generator/fixture selection; Task 3 additionally requires isolated browser/session admission. No source or browser work follows from this planning update alone.

## Root Task 2 execution admission

Root reviewed 14-ARTIFACT-EXECUTION-REVIEW.md SHA-256 81b1195e4f72f18f89335850d625ff1de93c3f9f5a409774a98a3d6f76b6a0a8 and admits Task 2 within the existing source/test list, superseding earlier Task1-only statements. Task 3 is not admitted. Receipt updates are allowed; do not create SUMMARY until native scope is resolved.

Exact package entry: minion/packages/plugin-ui-bridge/dist/index.js SHA-256 4f7bd0418fdf85d8a3206cab1c9f3db767de7dc5745a93fbb73b6ab110a098d6. Associated source cd8380839105f35ee996a5d6e893f87a3dd676616b4c2be9c827f55b150e4862 independently passes84 package/Hub tests. Verify these and all actual module graph inputs before generation; package source and installed packages remain frozen.

Select public IIFE global generatedPluginBridge with mount({render,fail}) returning final disposal. Exact markers are /* MINION_GENERATED_BRIDGE_START */ and /* MINION_GENERATED_BRIDGE_END */. Fixed Vite library settings: formats:['iife'], name:'generatedPluginBridge', target:'es2020', minify:false, sourcemap:false, write:false, configFile:false, envFile:false, envDir:false. Use existing declared Vite8.1.3. Root accepts one-time script split/removal of old protocol shell and insertion of the generated script plus external mount invocation. Preserve existing renderer/helper bodies and all markup/styles; record hashes. Add only a comment TODO at overview stats.innerHTML for outstanding renderer/schema safety, recording that explicit comment-only difference. Subsequent generator writes only delimited regions/provenance after complete input/target validation.

Select exactly one nonempty hostOrigin hash value, no normalization/fallback; package owns exact-origin/source/frame validation. Project only own '--' custom properties and preserve existing binary dark-class behavior. Context stays unknown and passes unchanged; renderer and adapter errors yield fixed text 'artifact unavailable' without payload/cause/RPC details. One context request per lifetime, fresh bridge on persisted pageshow, clear owner before disposal, contain stale fulfillment/error/render callbacks. Follow all actual renderer-specific cases and review R1-style lifecycle constraints in the packet; no general schema repair or new protocol parser.

Reject any case-insensitive closing-script sequence in output; no escaping fallback. Runtime graph has only the named adapter and qualified package modules; reject new imports/assets/maps/dynamic execution/network bootstrap. The existing serving CSP does not confine network access, so make no such claim. Tests use synthetic windows/DOM and actual emitted scripts plus actual host; no app, browser, network, provider or DB. Temporary negative-write/check artifacts may use a new private /tmp/minion-14-07-XXXXXXXX directory; no source destination mutation in tests. Run required script/prompt tests and both UI lint gates, then freeze for root independent review and a serialized Hub check.


## Root closed compiler-helper graph amendment

The fixed es2020 build added four virtual compiler helpers and was correctly rejected before output writes. Root admits only the following exact virtual IDs and actual getModuleInfo(id).code SHA-256 values in addition to the adapter and qualified package entry. Here NUL denotes the leading zero codepoint, not a printable prefix:

- NUL@oxc-project+runtime@0.139.0/helpers/esm/defineProperty.js: 2bad7c96ee851cf2cfe1320a54fd88dd5f769a7faf8bd92d60c9f2e6d1e381dd
- NUL@oxc-project+runtime@0.139.0/helpers/esm/toPrimitive.js: 9660291e71923cb5ff9aaaa1de354515c26e3220cba56ac5d21b806f23875545
- NUL@oxc-project+runtime@0.139.0/helpers/esm/toPropertyKey.js: 7f0000acc9c9cf7594b412095be66cdc59066b25458a16fe1434fd00cff478e7
- NUL@oxc-project+runtime@0.139.0/helpers/esm/typeof.js: 928bcbbdf74fe24ed7486e7f982c4389c1859886b3ed904ebd2677f22193b57a

These are compiler-generated inline helpers, not new application dependencies or permitted external imports. Retain fixed target/config and all output safety checks. Reject absent/extra IDs or changed helper source bytes, recording exact virtual IDs/digests and original/transformed adapter identities in provenance. Add meaningful negative coverage for helper source tampering and an extra virtual module. Retain the original graph denial log and original admission hash. No basename-only allowlist, target change, package install or runtime network bootstrap is admitted.


## Root compiler-helper attribution amendment

Root verified the registry SHA-512 integrity for @oxc-project/runtime0.139.0 archive SHA-256 `8db2efabe4944e1df57b74421876dd8334c538e81b82729a508b383ec8543bd0`. All four archive src/helpers/esm members exactly match the admitted virtual helper digests. Shipped README explicitly credits copied @babel/runtime code. Retain actual shipped OXC MIT notice SHA `95ced5ecf1133fbf41d409b5555c86c344f83f3b019926057ddbc07cfdcc27b3` and Babel upstream MIT notice retrieved2026-09-09 SHA `117da2af0d4ce0fe1c8e19b5cff9dcd806adf973d328d27b11d4448c4ff24f76`. Files preserved under `/tmp/minion-14-07-root-licenses/package/LICENSE` and `/tmp/minion-14-07-root-licenses/babel-LICENSE`. Primary sources: https://registry.npmjs.org/@oxc-project%2fruntime/0.139.0 and https://raw.githubusercontent.com/babel/babel/main/LICENSE. The latter is a fetched content snapshot, not an immutable release tag.

Admit one additional owned source notice file `minion_hub/scripts/artifacts/inline-bridge-NOTICES.txt`, containing exact retained notices and factual package/credit/source identification. Generator must pin this notice digest, include it in provenance and emitted comment, and reject unsafe comment/script terminators as before. Existing parent-package notice stays. Do not relabel bundler's license as proof for runtime helpers; it may remain as separately labeled bundler notice. Refresh generator, three regions and provenance through the existing generator only, and add meaningful assertion that the helper notices survive output. Retain prior106-test freeze receipt then append exact new hashes/retest. This closes the identified four-helper attribution gap, not a platform-wide legal assessment or permission to distribute/release. Source Task2 only; native Task3 remains separately admitted.


## Root Task3 local native fixture admission

Task2 independently passes106 cases, regenerated notice checks, and full Hub check at2,422-file snapshot SHA f77649f4811e6696322404555e83e71401927e659fbb04b586c5e8b1373de9af with0errors/0warnings. Root owns Task3 builder/README and temporary output `/tmp/minion-14-07-browser-3rist9cx`. Use installed Vite without app config/env; actual HostBridge/mountHostBridge and exact selected package, copied unchanged builtinHTML. Root's exclusive browser session remains BU_NAME=minion-360-root-ui at127.0.0.1:9223. Admit loopback-only port8764 if free; server serves only this synthetic output, no filesystem escape/proxy/appenvironment/provider/data access. Browser external requests must be denied where supported and actual request evidence retained. Any inability to enter nativeBFCache remains explicit; synthetic event replay cannot close it. No app UI/ArtifactHost/store/prompt/model/deployment changes. Final receipt/SUMMARY may describe executed native scope and remaining component/stored/release gates only.
