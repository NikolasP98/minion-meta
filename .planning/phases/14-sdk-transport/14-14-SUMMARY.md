---
phase: 14-sdk-transport
plan: "14"
status: verified_private_prerequisite
requirements-completed: []
---

# Canonical gateway facade and text input

The private gateway candidate replaces repeated Shells definitions with selective canonical exports and adds a pure bounded text/key input normalizer using existing Zod and native crypto. Required organization is supplied separately by the future authenticated caller. No routing, storage, identity allocation or durability selection occurs here.

91 focused cases and strict types pass independently; author scoped type-aware lint/format pass. Initial facade red had copied-object identity failures and missing durable exports; missing new parser module was structural only. Five additional inherited-field/getter cases reproduced meaningful failures and pass after descriptor-safe own-field projection. Test-only prototype mutations are synchronous and restored in finally.

Full125-file shared package,4501 unchanged original inputs,101 dependency links and active-source separation were independently checked. See14-14-VERIFICATION.md for exact hashes and commands/receipts. Candidate: /tmp/minion-14-14-qwkly7fi/minion. Active installation remains old; same-version local artifact is unpublished and not production identity.

Receiver14-12, explicit required-durable wire/config choice, caller scopes/routing, combined record/query capacity and immutable adoption remain open. This scoped prerequisite closes no whole requirement or phase.

## Profile amendment (2026-09-09, root accepted)

The two-file facade amendment re-exports the adopted 14-16 durable contract from the fresh complete 14-11 artifact `f6a3c237c0264a2a8ef75906991ae8fa84504bf637d3b6655d07058582b603fa` (125 members): `SHELLS_METHODS`, `SHELL_DURABLE_V1_INPUT_POLICY`, `SHELL_DURABLE_V1_OUTCOME_LIMITS`, `SHELL_DURABLE_V1_PROFILE`, `normalizeShellsInvokeDurableResponse`, and the `ShellsInvokeDurableParams`/`ShellsInvokeDurableResponse` types. `MAX_TIMER_DELAY_MS` stays local. The qualified parser pair is byte-unchanged.

110 tests pass (37 facade/profile, 73 unchanged parser); strict types, type-aware lint 0/0 and format pass. Root independently reran tests and types. Candidate: /tmp/minion-14-14-profile-lz9tu2fp/minion. Exact hashes in 14-14-VERIFICATION.md. Still no active adoption, receiver, routing or release.
