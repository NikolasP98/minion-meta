---
phase: 14-sdk-transport
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/14-sdk-transport/14-WORKFORCE-HTTP-PACKET.md"
  - "packages/workforce-client/src/client.ts"
  - "packages/workforce-client/src/client.test.ts"
  - "packages/workforce-client/README.md"
  - "minion_hub/src/lib/server/workforce-fetch.ts"
  - "minion_hub/src/lib/server/workforce-fetch.test.ts"
  - "minion_hub/src/lib/server/workforce-http-boundary.contract.test.ts"
autonomous: false
requirements: ["SDK-01", "SDK-02"]
readiness: "ADMITTED_TASK3: scoped helper integration and exact candidate/installed verification configurations selected; published/deployed adoption remains gated."
closure_policy: "Typed client changes do not close streaming proxy, all route schemas, package publication or installed/deployed consumer adoption."
must_haves:
  truths:
    - "The existing reference typed-error fix is selected and reconciled before new Workforce code is written."
    - "Selected JSON calls obey a finite byte and cancellation/deadline policy through fetch and body consumption."
    - "Browser-facing errors contain no upstream page, credentials or raw cause; transport errors retain honest provenance."
  artifacts:
    - path: ".planning/phases/14-sdk-transport/14-WORKFORCE-HTTP-PACKET.md"
      provides: "Exact patch selection, per-path transport policy and consumer adoption gates"
    - path: "packages/workforce-client/src/client.test.ts"
      provides: "Actual Response/ReadableStream tests for byte bounds and cancellation"
    - path: "minion_hub/src/lib/server/workforce-http-boundary.contract.test.ts"
      provides: "Actual consumer and route error-boundary evidence with synthetic upstream data"
  key_links:
    - from: "packages/workforce-client/src/client.ts"
      to: "minion-meta/packages/workforce-client/src/client.ts"
      via: "Root-selected immutable reference patch and retained historical regression cases"
    - from: "minion_hub/src/lib/server/workforce-fetch.ts"
      to: "packages/workforce-client/src/client.ts"
      via: "Exact locally resolved package bytes and caller cancellation policy"
---

<objective>
Bound Workforce JSON response processing, carry caller cancellation and classify safe HTTP errors without disturbing legitimate streaming or binary proxy routes. Preserve existing typed-error work rather than implementing it twice.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.planning/operations/360/PROGRAM.md
@.planning/DECISIONS.md
@.planning/research/360-sdk-container-history.md
@minion-meta/specs/2026-08-17-pkg-workforce-client-json-error-spec.md
@minion-meta/proposals/2026-08-17-hub-workforce-error-body-leak.md
@packages/workforce-client/src/client.ts
@packages/workforce-client/src/client.test.ts
@minion-meta/packages/workforce-client/src/client.ts
@minion_hub/src/lib/server/workforce-fetch.ts
@minion_hub/src/routes/api/workforce/[...path]/+server.ts
</context>

<boundaries>
Root admits Task1 read-only discovery and its packet only; later source tasks remain gated. Task 1 is read-only discovery plus the packet; root must accept its exact patch/API/policy/consumer-resolution receipt before Task 2 source edits and again before Task 3 uses candidate bytes. Existing user authority permits routine local choices; no additional user confirmation is invented.

Preserve WIP, branches, worktrees and stash. No commits, release/version/manifest/lock changes, dependency downloads, node_modules edits, remote service/registry calls, customer data, paid calls or live database access. Use local injected fetch and native Response/ReadableStream fixtures, never actual HTTP/WS endpoints. Source, installed archive, registry publication and runtime are separate identities. Record missing installed candidate bytes rather than using an old package as false proof.

Owned scope is the JSON client and the listed Hub helper/tests. Do not edit the streaming proxy route, hooks, auth minting, 25 domain API modules, backend/gateway server, all loaders or UI. Discovery must enumerate those paths and produce exact child-plan requests when a failing boundary lives there. A child request is not a fix and cannot close SDK-01/02. Root owns those admissions and proposal/global status edits.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Select the existing error patch and freeze per-path HTTP policy</name>
  <files>.planning/phases/14-sdk-transport/14-WORKFORCE-HTTP-PACKET.md</files>
  <read_first>packages/workforce-client/src/client.ts, packages/workforce-client/src/client.test.ts, packages/workforce-client/src/api/health.ts, packages/workforce-client/src/api/dashboard.ts, minion-meta/packages/workforce-client/src/client.ts, minion-meta/packages/workforce-client/src/client.test.ts, minion_hub/src/lib/server/workforce-fetch.ts, minion_hub/src/routes/api/workforce/[...path]/+server.ts, minion_hub/src/server/integration/workforce-proxy.test.ts</read_first>
  <action>Verify current branches/SHA/diffs, compare the exact reference typed-error source/tests/README and record the selected patch hash. Existing reference head was 59c4b56c192a730dbd63bb8e51dd8305eb2ad424; reverify, never trust a moved branch label. Enumerate actual createWorkforceClient/workforceRawFetch callers and route catches that expose body/raw/message/cause. Distinguish JSON requests, binary/streaming relay and background clients. Record current auth headers/company ownership without changing authority. Select finite JSON response byte and total deadline policies from real caller requirements; do not invent an SLO or apply a JSON cap to unbounded legitimate streams. Decide additive client-level/request-level signal precedence, timeout/abort/oversize/HTTP/protocol error codes, cancellation cleanup and selected response validation scope. Successful arbitrary JSON remains unvalidated outside specifically selected endpoints. Require root review of exact policy and candidate package-resolution method. New route or endpoint-schema files require a separate child plan before edit.</action>
  <verify><automated>git diff --check -- .planning/phases/14-sdk-transport/14-WORKFORCE-HTTP-PACKET.md</automated></verify>
  <done>The packet names exact patch selection, each consumer/path category, finite JSON policies, unresolved choices and ownership. Root admits those choices before source mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile typed errors and bound the actual JSON transport</name>
  <files>packages/workforce-client/src/client.ts, packages/workforce-client/src/client.test.ts, packages/workforce-client/README.md</files>
  <behavior>Selected calls stop at the configured byte limit or cancellation/deadline, release stream resources, and preserve honest HTTP versus transport/protocol errors. No request is retried automatically.</behavior>
  <action>Apply only the reviewed reference error patch while retaining its original regression cases. Add reviewed additive signal/limit options to client/request contracts without changing domain method signatures or Error.message compatibility unnecessarily. Bound streamed bytes before concatenation/JSON.parse; do not trust Content-Length or mistake a truncated raw error field for bounded input. Read decoded UTF-8 correctly across chunk boundaries. Cancel the reader and abort transport on limit/deadline; clear timers/listeners on every outcome and absorb late completion without publishing success. Caller abort must work before fetch, before headers and during body read; a body stall cannot outlive the total deadline. Keep valid JSON under misleading content-type working, preserve empty 204 semantics, and distinguish malformed JSON 2xx from non-2xx HTTP errors. Raw Error/cause/upstream HTML must not become safe-to-log metadata merely by being bounded. Test native Response/ReadableStream chunks for limit-1/limit/limit+1 bytes, multibyte UTF-8, missing/misleading Content-Length, 502 HTML, 200 HTML, empty bodies, body-reader failure, immediate/late abort, hung fetch/body, cleanup, concurrent calls with independent budgets and no retry. Use fake clocks and deterministic release controls; no real network or arbitrary sleeps. Document residual domain shape validation and streaming proxy boundaries.</action>
  <verify><automated>pnpm --filter @minion-stack/workforce-client test &amp;&amp; pnpm --filter @minion-stack/workforce-client typecheck &amp;&amp; pnpm --filter @minion-stack/workforce-client build</automated></verify>
  <done>Actual transport fixtures pass with retained historical tests, finite work and cleanup assertions. Selected API/body/error policy is documented; unvalidated domain shapes and proxy ownership remain explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Carry request cancellation into Hub and qualify safe error boundaries</name>
  <files>minion_hub/src/lib/server/workforce-fetch.ts, minion_hub/src/lib/server/workforce-fetch.test.ts, minion_hub/src/lib/server/workforce-http-boundary.contract.test.ts, .planning/phases/14-sdk-transport/14-WORKFORCE-HTTP-PACKET.md</files>
  <behavior>Selected Hub JSON helpers use the reviewed transport policy and actual caller signal; their tested consumer boundary never returns an upstream page or credential-bearing error.</behavior>
  <action>Require root's candidate package-resolution receipt before changing Hub helper calls. Use an isolated local candidate resolution approved by root for verification; never silently alias away the installed package or edit active locks. Carry event.request.signal for request-scoped typed clients. Give background clients the reviewed finite policy without inventing a request owner. Consolidate workforceRawFetch only for paths proven JSON by Task 1, preserving caller headers, method/query/body, tenant authority and accepted statuses. Do not change streaming proxy semantics here. Exercise actual helper plus identified route/loader error handling using synthetic fetch Responses and canaries in upstream HTML, JSON error payload, headers and cause; inspect both response and log sinks. Compare old supported consumer behavior and candidate behavior for JSON success, empty success, structured backend error, non-JSON error, cancellation and oversize. If the actual unsafe serialization lives outside owned files, leave the failing evidence visible and request an exact root-admitted child plan; do not mock that serialization away or report the boundary repaired. Record package provenance/release-note/consumer lock adoption work under 12-03/14-02 before any eventual release.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/server/workforce-fetch.test.ts src/lib/server/workforce-http-boundary.contract.test.ts &amp;&amp; bun run check)</automated></verify>
  <done>Evidence names the exact candidate package bytes and actual consumer functions exercised. Passing local paths, failing external-owner paths and remaining installed/release gates are separated; no TODO/proposal handoff is omitted.</done>
</task>
</tasks>

<threat_model>
The upstream response is untrusted and may contain echoed credentials or maliciously large bodies. Enforce byte/deadline bounds before materializing it, keep cancellation linked to its caller, and emit allowlisted diagnostics. Browser error projection is a distinct trust boundary. Company/actor authority remains server-established; transport policy cannot authorize a request. No automatic retry of mutations, guessed upstream status on transport errors, or raw-body logging is allowed.
</threat_model>

<verification>
Use installed tools and no network. Red/green fixtures must exercise the real transport and real selected consumer, not a mocked client facade. Capture cleanup, independent concurrent budgets, byte accounting and original typed-error regressions. Run Hub aggregate check only in root's serialized validation window. Independent Standards/Spec review checks exact scope and all outside-owner failures. Add exact-site TODO(handoff) and root-owned proposal entries before handoff for unresolved source work.
</verification>

<success_criteria>
All admitted tasks have scoped evidence. Typed transport success alone does not prove route error projection, every schema, streaming proxy, package distribution or deployment. Requirements remain open until generated children and actual supported consumers are independently verified.
</success_criteria>

<output>
After execution create 14-05-SUMMARY.md containing selected plan/reference/source hashes, policy receipt, exact commands, source-versus-installed results and remaining child/release gates. No global status changes or commits by this executor.
</output>


## Root Task 2 policy selection

Root accepts packet `72af4ce8fb83ca9754cd2d7433d522e3e85d6bc3f192752d71eb27e495b04ddb` and selects the pinned three-file reference patch `c00995ca8127a6906acd6f9368160c61ed3b69171a3480d70c5f0b4bfc2182ce` as the baseline. Task 2 owns only client.ts, client.test.ts and README. Root executes this source slice; no Hub helper edit or candidate alias is admitted yet.

- Candidate defaults are 30,000 ms total and 4 MiB decoded response-body bytes. These are explicit finite failure ceilings, not measured production SLOs. Optional positive finite integer per-client/per-request overrides are allowed; timeout must fit the platform timer range. Per-request numeric overrides replace defaults; client/request signals combine with OR cancellation. No retry.
- Preserve the reference HTTP error and empty-success semantics and all 14 historical regressions. Add `code=http_error` for completed non-2xx and `code=invalid_json` for malformed successful responses, retaining actual status and bodyKind. A successful upstream status is provenance, not a valid SvelteKit error status. Task 3 must safely project it before adoption.
- Dedicated policy errors have fixed safe codes/messages for request_aborted, deadline_exceeded and response_too_large; no fake HTTP status, raw cause, URL, headers, signal.reason or body in those errors. Unrelated native fetch/read failures retain their original identity.
- Limit actual streamed bytes before string/JSON materialization; ignore Content-Length as authority. Abort transport and cancel/release the reader on policy termination; caller waits must settle even if injected fetch/cancel ignores the signal. Absorb late completion/rejection and close a late response body. Clear all timers/listeners on every outcome. Deadline covers fetch and body; synchronous bounded parsing does not become preemptible.
- Raw upstream JSON/text error bodies remain untrusted internal evidence, never safe logging metadata. README must remove the reference recommendation to log raw bodies. Caller error/logging projection, raw RequestInit normalization, workspace two-second cancellation, streaming proxy, upload stubs, endpoint schemas and deployed limits remain separate named gates with exact-site TODO/proposal references.

The package's `export * from './client.js'` already exposes additive types; no index or domain API file change is needed. Use native Response/ReadableStream and fake-clock/deferred fixtures only. Task 3 and package/lock publication/adoption remain gated. No full build or Hub check during the 12-07 profiling resource window; focused package tests/typecheck may run.


## Root Task 3 source and candidate-resolution admission

Root reviewed appended packet SHA-256 `873502f2bf87a9c8adbba4d8d7ad6480cd29e41557d102c691215f5adbe5e9b5`. Admit only the existing helper, its existing test, the named boundary contract test and packet/SUMMARY. The three package source files remain frozen. Additionally admit exactly `/tmp/minion-360-14-05/vitest.hub-candidate.config.ts` and `vitest.hub-installed.config.ts` as temporary verification controls, with hash validation before test import, exact main-entry alias, retained base aliases/stubs, empty envDir and explicit separate installed mode. Root owns any later full-check snapshot declaration mapping; normal product configs/locks/node_modules remain untouched.

Selected helper policy:

1. Normalize trusted server caller HeadersInit using native Headers with caller-over-default precedence, case-insensitively. Preserve JWT/board-key selection and canonical mutation authority; never copy browser inbound headers. Correcting duplicate-case ambiguity is deliberate and must be tested for records, tuples and Headers.
2. Use one private injected-fetch adapter per raw call to preserve captured RequestInit method/body/other fields byte-for-byte. The base JSON reader may use a documented GET placeholder with no body because its method is only forwarded to this adapter; the adapter MUST restore the captured actual method before real fetch. Tests assert actual GET/POST/DELETE/HEAD/OPTIONS, body/null/empty semantics, query, headers and signals. No arbitrary second destination, mutable cross-call state, duplicate response reader or parse/reserialize step. This is an explicit private transport adapter, not a new public package API.
3. Wrap request on the original returned client object once, retaining the original method. All namespace factories dynamically use that same base. Protect construction/minting and transport failures through a fresh helper-owned error with fixed message/code, no copied original body/cause/message/stack/URL/headers. Preserve actual completed HTTP 400–599 statuses, especially 404. Map malformed-success, oversize and native/setup failures to 502; deadline to 504. Request abort is rethrown with a fixed reason-free code and NO invented HTTP status. The helper rejects; downstream loaders that swallow cancellation remain separate work and are not qualified by this slice.
4. Preserve source compatibility with the currently installed public API when possible using its existing exports and structural option objects, without casts that fabricate bounded support. New optional policy error recognition must use the selected actual constructor identity, never accept arbitrary forged code/status objects. If installed types/exports make this impossible, stop for root review rather than silently changing normal resolution. The old installed client's lack of byte/deadline guarantees must remain a real explicit negative control. Helper error sanitization can improve independently of those missing package capabilities.
5. Candidate checks invoke actual helper → exact built candidate → synthetic fetch → actual selected route/loader log and response sinks. Test canaries in HTML, JSON, native causes, headers and abort reasons. Existing route-local DB/schema failures, cancellation-swallowing loaders and streaming proxy remain outside source ownership. Do not mock the helper/client or pretend those unrelated causes are sanitized. Keep company 404/single-ID behavior through synthetic dependencies only.
6. Record full transitive candidate dist manifest and separate installed manifest. Main-entry alias is an exact regex, never a package-prefix alias; explicit identity-jwt subpath keeps installed bytes. Default/installed checks must not be described as bounded-candidate qualification. No test-dependent behavior in product code.

Root serializes full Hub checks after freeze. No application startup, network, database or registry call is admitted. Missing fixture identity/empty selection is a failure; no weakened assertion or unexplained skip may close acceptance. All mandatory runtime/adoption/cancellation/schema/proxy gaps remain in the proposal ledger and owned-site TODOs.
