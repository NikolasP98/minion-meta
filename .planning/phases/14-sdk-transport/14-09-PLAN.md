---
phase: 14-sdk-transport
plan: "09"
type: execute
wave: 2
depends_on: ["14-08"]
files_modified:
  - "minion_hub/src/lib/services/gateway.svelte.ts"
  - "minion_hub/src/lib/state/gateway/gateway-data.svelte.ts"
  - "minion_hub/src/lib/services/gateway.contract.fixture.ts"
  - "minion_hub/vitest.gateway-contract.config.ts"
  - ".planning/phases/14-sdk-transport/14-09-SUMMARY.md"
autonomous: false
requirements: ["SDK-01", "SDK-02"]
readiness: "Private candidate Tasks 1–3 independently pass; Task 4 read-only session seam is admitted below in the same private snapshot. Active source/package adoption remains gated."
closure_policy: "Publishing current session metadata does not itself enforce PluginIframe capabilities or qualify all downstream hydration."
must_haves:
  truths:
    - "Hub publishes fresh hello and connected state after every successful active handshake, including internal reconnect on the same client."
    - "A reactive monotonic session revision identifies each accepted active session; disconnect invalidates its hello."
    - "A backup session remains private until a valid cutover, and stale callbacks cannot publish or invalidate a successor."
  artifacts:
    - path: "minion_hub/src/lib/services/gateway.svelte.ts"
      provides: "Single accepted-session publication path with staged cutover and lifecycle fences"
    - path: "minion_hub/src/lib/state/gateway/gateway-data.svelte.ts"
      provides: "Reactive session revision consumed by later plugin gating"
    - path: "minion_hub/src/lib/services/gateway.contract.fixture.ts"
      provides: "Actual gateway service and candidate shared-client reconnect/cutover evidence"
  key_links:
    - from: "minion_hub/src/lib/services/gateway.svelte.ts"
      to: "packages/shared/src/gateway/client.ts"
      via: "Optional onAuthenticated observer qualified on exact installed candidate"
    - from: "minion_hub/src/lib/state/gateway/gateway-data.svelte.ts"
      to: ".planning/phases/14-sdk-transport/14-06-PLAN.md"
      via: "Real reactive session revision and current hello become component-gating prerequisite"
---

<objective>
Repair the Hub's missing publication after shared-client internal reconnect. Preserve current make-before-break cutover, binary Yjs wiring, eager reconnect and selected chat-history behavior while giving later plugin gates a real reactive authenticated-session identity.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@minion_hub/CLAUDE.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/phases/14-sdk-transport/14-RECONNECT-SESSION-DECISION.md
@.planning/phases/14-sdk-transport/14-08-SUMMARY.md
@.planning/phases/14-sdk-transport/14-02-PLAN.md
@minion_hub/src/lib/services/gateway.svelte.ts
@minion_hub/src/lib/services/gateway-rpc.ts
@minion_hub/src/lib/state/gateway/connection.svelte.ts
@minion_hub/src/lib/state/gateway/gateway-data.svelte.ts
@minion_hub/src/lib/services/gateway/connection-lifecycle.ts
@minion_hub/src/lib/services/gateway/chat-rpc.ts
@minion_hub/vitest.config.ts
</context>

<boundaries>
Only four product/config/test files plus the summary may change after independent root admission. No component markup/style, PluginIframe, bridge protocol, compatibility utility, gateway-rpc leaf, connection state, chat-rpc/history helpers, lifecycle helper, shared package or gateway source edits. Preserve all concurrent WIP and existing history patches. No commit/branch/worktree/stash, package/lock/install/node_modules change, application server, browser, remote/loopback gateway, provider, actual fetch/token API, telemetry or production action.

The callback does not exist in the inspected Hub-installed 0.9.0 declaration/runtime. Product source edits therefore wait for root's separately owned exact package transaction and declaration proof, or an explicitly amended isolated candidate workflow. Do not widen TypeScript or add production source aliases to bypass this prerequisite. Test-only candidate alias evidence must be labeled and can qualify fixtures before installation; it cannot qualify the installed app. Root owns all dependencies/global status/proposal work and full-check scheduling.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Freeze callback adoption, fixture and reporting selection</name>
  <files>minion_hub/src/lib/services/gateway.contract.fixture.ts, minion_hub/vitest.gateway-contract.config.ts, .planning/phases/14-sdk-transport/14-09-SUMMARY.md</files>
  <action>Read verified 14-08 and record its exact source/runtime declaration identities. Recheck Hub service hash (draft 91e8c69ca2c717744f73bc8fb34cb48cc684c63cc369b6298b8b870de0adbaed), state hash (ee103bf834c8937d405f05b26b19ec1e5ffd15bbe6f9b04988ba3f2c76e7e0cb) and read-only history helper (a18c5241dfae80472a3bcb827a72d7f1b418f37b741cfda5f772c78f60eb971c). Preserve drift under root coordination.

Build an isolated Svelte-aware fixture config selecting only gateway.contract.fixture.ts. Import actual gateway service and actual candidate GatewayClient; inject the socket implementation at its existing global/injection seam. Mock token/JWT/storage/HTTP/update/toast/telemetry surroundings and dangerous module initialization, never the session publication or reconnect algorithm. No fixture-only session epoch. Keep mocks and synthetic fixtures in the owned test file. Use installed Svelte/Vitest dependencies only. A test-only shared-source alias must name the exact source digest and have a separately selected installed-entry run; it is not production resolution. Prove all HTTP attempts are intercepted and no native socket is opened.

Obtain root's explicit three-hook consumer reporting selection: onEventError/onReconnectError/onSocketError each uses an established sanitized sink or explicitly accepted default. Do not silently preserve a one-hook-only historical adoption policy or initiate reconnect from an error reporter. Record it before source changes. Confirm exact emitted declarations and archive/installation receipt from root's separate transaction. Then establish failing reconnect publication and stale-cutover assertions on the unchanged Hub service.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/gateway.contract.fixture.ts --maxWorkers=1)</automated></verify>
  <done>The real service produces the intended red assertions, fixture isolation is reviewed, the reporting policy and installed declaration/archive gate are recorded, and root admits source work.</done>
</task>
<task type="auto">
  <name>Task 2: Publish and invalidate the current session through one path</name>
  <files>minion_hub/src/lib/services/gateway.svelte.ts, minion_hub/src/lib/state/gateway/gateway-data.svelte.ts, minion_hub/src/lib/services/gateway.contract.fixture.ts</files>
  <action>Add gw.sessionRevision as a monotonically increasing reactive number. Increment only once per published active (client, socket generation) tuple. Publish current hello, presence and connection flags synchronously in the same success path; Date.now remains display information only. Current-client close/disconnect/replacement invalidates hello and connected state immediately. Do not reset chat/history and all gateway caches on a same-host transient drop; preserve the existing cross-host reset on committed cutover. Do not reset the revision counter with gateway data.

Use onAuthenticated for initial and internal success. Remove duplicate state publication from the initial connect promise, retaining its failure/lifecycle handling. Keep a per-client staged handshake record: backup callbacks store but do not publish; backup close marks it invalid even when it is not current. Commit only if existing source client/lifecycle remains current, source connected and staged backup generation still live. Swap, reset as required and publish once. After commit, future automatic reconnects of that same backup become ordinary active sessions; do not permanently fence them with a creation-time lifecycle token invalidated by the commit itself. Stale source close cannot invalidate the new hello.

Preserve onOpen binary listener behavior and explicit rewiring after cutover, eager reconnect scheduling, auth-fatal decisions and per-session traceparent. Apply root's three-hook reporting choice without changing retry behavior. Invoke onHelloOk once per accepted session, never once from both callback and promise. Fence direct service-owned asynchronous initialization, delayed polling setup and flow-fetch/registration work before state updates or later RPC dispatch. Bind delayed registration to the captured client/session so it cannot send to a successor through the global leaf.

Preserve existing lifecycle helper and RPC leaf architecture. Downstream helpers outside this file, especially chat history/config hydration, may require their own session fences; do not claim this fixes those just by checking before starting them. If required acceptance needs an outside-owner edit, request an exact amendment with preserved history behavior and actual tests.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/gateway.contract.fixture.ts --maxWorkers=1)</automated></verify>
  <done>Actual service tests prove fresh reactive publication and no stale active-session replacement while supported cutover, eager and binary behavior remains intact.</done>
</task>
<task type="auto">
  <name>Task 3: Qualify session boundaries and freeze the component prerequisite</name>
  <files>minion_hub/src/lib/services/gateway.contract.fixture.ts, .planning/phases/14-sdk-transport/14-09-SUMMARY.md</files>
  <action>Require initial and same-client reconnect with changed server version/methods; zero success on open/challenge; disconnect invalidation; duplicate success idempotence; callback and initial promise cannot duplicate initialization; late old success/close/error no-op; manual reconnect superseding token fetch/cutover; backup success withheld until commit; backup close after hello but before commit denied; source close while backup waits denied; committed backup later reconnect publishes normally. Read the actual reactive revision and state, not a fixture model.

Assert one binary listener/polling owner, preserved no-replay behavior, stale delayed flow fetch cannot register against new client, and no hidden network calls. Run the existing lifecycle/eager reconnect and chat-rpc context regressions without editing them. Root runs the aggregate Hub check only after source freeze and the installed candidate transaction; do not claim candidate alias tests make old installed declarations compatible. Provide separate Standards/Spec review and before/reference/source/archive/installed hashes. Hand 14-06 the exact revision/hello/connected contract and keep component/browser enforcement pending.</action>
  <verify><automated>(cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/services/gateway/connection-lifecycle.test.ts src/lib/services/gateway/eager-reconnect.test.ts src/lib/services/gateway/chat-rpc.context.test.ts --maxWorkers=1)</automated></verify>
  <done>Focused current-source and exact installed candidate evidence are distinguished; root independently verifies and serializes full Hub check. SDK-01/02 remain open for component/other transport/runtime gates.</done>
</task>

<task type="auto">
  <name>Task 4: Expose the accepted session association for first-mount consumers</name>
  <files>minion_hub/src/lib/services/gateway.svelte.ts, minion_hub/src/lib/services/gateway.contract.fixture.ts</files>
  <read_first>Actual private AuthenticatedSession/isCurrentSession/publishSession, hosts selectHost/applyOrgAssignedHost, 14-PLUGIN-MOUNT-RECEIPT.md and PluginIframe.svelte</read_first>
  <action>In the existing private candidate only, expose a minimal read-only accepted-session snapshot and current-snapshot check backed by the actual private activeSession/isCurrentSession predicate. Bind exact client, accepted hello, authenticated host ID and session revision; use a stable snapshot for one accepted session or an explicit opaque identity, without a second counter or mutable authority exposed to callers. Read actual reactive revision/hello/connected/selected-host state so a Svelte consumer observes authentication, invalidation and host selection. The public check must reject a snapshot after same-object reconnect, changed selected host, close, replacement or private backup staging. It must not authorize a first mount merely by pairing current selected host with retained hello/client, or by URL equality. A current valid session remains usable on first mount; no forced reconnect/wait workaround. This is an internal UI lifecycle seam, not token/tenant authorization.

Write meaningful failing cases before implementing: select host B while A remains retained and connected, then call the same public accessor a new component will use; it must return no valid session. Include valid first mount, A→B→A selection, internal same-client reconnect with old/new snapshots, staged backup/private hello and accepted cutover, and real rune subscriber notifications. Preserve all 44 existing cases. No PluginIframe/state/shared-client/API/package changes. Exposing a snapshot does not yet prove mounted component dispatch; that remains 14-06. Record exact before/after hashes and root review. Add exact-site handoff comments at unchanged onAgentEvent/onChatEvent timer/history/stream continuation seams to match the existing proposal; these comments record remaining scope, not a fix.
  </action>
  <verify><automated>node node_modules/vitest/vitest.mjs run --config vitest.gateway-contract.config.ts src/lib/services/gateway.contract.fixture.ts --maxWorkers=1</automated></verify>
  <done>Actual accepted-session association is available to first-mount and delayed-dispatch consumers, with genuine reactive regression evidence. Root independently verifies the frozen candidate before component admission.</done>
</task>

</tasks>

<verification>
Allowlisted PATH/HOME/LANG; all environment/API/network inputs synthetic. Source and installed fixture runs must identify their actual resolution independently. Failed or absent emitted declaration/archive/installed checks remain a prerequisite, not an ignored type failure. No full Hub check during another heavy window. Scoped diff check covers the four files and summary. Root owns source TODO/proposal acceptance for any remaining hydration/adoption gaps.
</verification>

<output>
14-09-SUMMARY.md records selected reporting policy, exact source and installed callback identity, red/green tests, reactive session contract, remaining downstream hydration/component gates and root check receipt. This draft does not authorize implementation.
</output>

## Root consumer review selection

Root accepts 14-CONSUMER-PLAN-REVIEW.md SHA-256 baa6ea65d3247b0f25986f7cb9fbe0df9c1dec4da9ad3ea5eef1d248092446a2. Its R1–R5 requirements are normative additions to the corresponding tasks. This plan remains a draft until the exact package and temporary candidate path admission is complete.

Select .contract.fixture.ts with an explicit include in the dedicated config; ordinary test discovery must omit it, dedicated discovery must find exactly it with passWithNoTests:false. Do not widen normal config ownership. Use maxWorkers:1 and minWorkers:1 together with envDir:false. Root will amend the exact temporary runtime/declaration snapshot paths after 14-11 candidate artifact review; no source sketch or ambient cast can substitute.

Select all three reporting hooks: event failures use console.error with only the fixed app-prefixed message from the review; reconnect/socket failures use console.warn with only their fixed respective messages. Ignore obsolete-client diagnostics; reporters never retry, change connection state or send raw errors/frames/event names/URLs. Test secret canaries and throwing sinks. Existing unrelated raw log paths remain an explicit follow-up, not a claim of platform-wide redaction.

Capture source accepted (client, socket generation) or sessionRevision before cutover awaits. Require that exact tuple at commit, not merely object/lifecycle/connected. Test source A close then same-object B authentication while backup waits: backup is rejected/closed, B survives. Guard resolveServerId and activity-bin fetch after every await using captured host/session; guard delayed polling startup and each poll's finally by its own session/operation so old completion cannot clear a new in-flight guard. Explicitly test held server/activity responses and old-poll-finally/new-poll overlap. Initialization covers agents/sessions/health/presence/channels/cron/dynamic imports and each flow registration. Imported history/config/group helper completion remains outside source ownership and requires exact TODO plus proposal.


## Root isolated consumer admission

The independently verified 14-11 archive `b896e1df2d7da596f6f6dd0f3d2a26f225ffbe11587d95fae1deb233818595e9` is selected. Exact snapshot: `/tmp/minion-14-consumers-61p26r_z/minion_hub`. Its private node_modules/@minion-stack/shared contains all121 extracted files, runtime and declarations together. Other dependencies are read-only links enumerated in dependency-links.json; before-manifest.json identifies copied current source. Do not mutate linked dependencies, active checkout, package caches or installed peers. Force optimizer/cache output inside the private snapshot. No source alias for shared, no mixed old declarations, and no silent nested-resolution substitution: record the actual resolved shared entry and types.

Task1 is admitted in this snapshot only, owning src/lib/services/gateway.contract.fixture.ts and vitest.gateway-contract.config.ts plus private check logs and the canonical SUMMARY. Read all app instructions from the active checkout. Run the unchanged actual service with the emitted candidate, synthetic environment and transport to establish genuine failing acceptance assertions. The candidate runtime deliberately includes the new callback while unchanged service still omits adoption; the active old installed package remains a preserved separate control. Inspect installed runner version: Vitest2 accepts minWorkers/maxWorkers, Vitest4 must use maxWorkers only. Override envDir:false and avoid real app/server startup.

Tasks2/3 source implementation remain gated on root review of Task1 fixture/red results. Their later source ownership will be the same listed files INSIDE this snapshot. This amendment does not authorize active checkout edits or represent completed installation. After isolated acceptance root must choose a separately recorded adoption transaction that updates matching package/runtime/declarations with source; until then no installed-app or production qualification claim. Root owns aggregate checks and handoff proposal.


Root fixture prerequisite: copied unmodified generated .svelte-kit/tsconfig.json from the freshly checked14-07 snapshot into the private Hub consumer snapshot. Its paths are relative and match the unchanged app compiler configuration. This supplies native Svelte/Vite resolution; it is not app typecheck evidence for the consumer candidate. Retain native installed Svelte plugin. The attempted direct compiler transform is not accepted runner evidence. Exact source copy and generated config hash are recorded in the Task1 receipt. A test-only passthrough spy may attach a close-delivery continuation to the actual GatewayClient.connect promise and return the original promise, to schedule backup close between authentication and cutover; it must not manufacture authentication/session identity or replace the algorithm.


## Root Task2 isolated source admission

Root reviewed native client-reactive fixture fd747025109a586a79d331040f59ecd6445d79d0c42bbfde35c1fa0815abc6bd and config0c94b9dd37ee979d3941098da4905eef868d7bedf4cc63e7b7f71711eea223f2. Eight actual behavioral failures and three controls, including public toStore/flushSync reactive publication, establish the intended defects. The native plugin/compiler and real shared generation remain operative; earlier alternative compiler setup is not acceptance evidence.

Admit Task2 edits to the two listed service/state files INSIDE `/tmp/minion-14-consumers-61p26r_z/minion_hub`, plus existing fixture/config/SUMMARY. Preserve baseline copies. Implement all normative R1–R5 fences and reporting contracts; extend real fixture cases for source close/re-authentication during cutover, backup later reconnect, old poll finally versus new poll, delayed registration, obsolete diagnostics and throwing sinks. The candidate cannot be adopted into the active checkout while the old package remains installed. Return frozen source and focused results for root review. No aggregate app check is authorized to this lane; root schedules it after complete candidate source/type prerequisites.


## Root deferred event continuation amendment

Root selects the author's exact minimal proposal within the same snapshot service/fixture/SUMMARY scope. For update.available and failed update.applied, capture the accepted session at event receipt and gate only deferred msgs().then toasts by current captured session; retain synchronous event behavior. For update.migrating's awaited cutover fallback capture accepted session, monotonic sessionRevision, lifecycle token and sourceHostId before cutover. Permit failure fallback only when lifecycle/revision/host are unchanged and activeSession is either that session or null. The null case preserves intended current-source-close fallback; explicit disconnect/replacement changes lifecycle and newly authenticated B changes revision. Do not use timestamp or host identity alone.

Qualify positive/current and stale delayed toasts, live-source failed cutover, source-close failed cutover before eager replacement, replacement/re-authentication before failure, explicit disconnect, and successful backup. Use actual event/cutover algorithms and controlled delivery gates. If same-object reconnect requires disarming the actual eager scheduler in a test, label that controlled scheduling explicitly; ordinary migration replacement should also be covered. No fabricated generation, substituted cutover result or unrelated event/chat architecture edit. Source ownership reopens only for this amendment; refreeze with focused regression evidence and report imported async chat/history/config/group seams separately.


## Root authenticated-event admission correction

Independent root inspection found onEvent still checks only current client identity. The actual shared client can deliver non-challenge events before hello; Hub can then mutate lastSeq or domain state without an accepted session, including during same-client reconnect. Site already gates this boundary. Admit the minimal same-file correction: capture activeSession, require it belongs to this client and isCurrentSession before sequence mutation/handleEvent. Shared connect.challenge handling remains untouched. Add genuine actual-client event fixtures before first hello and between current close/reconnect hello, verifying no domain/sequence mutation, plus authenticated positive event after hello. Preserve the existing42 cases. No gateway/protocol/history/helper edits. Root aggregate check waits for this small source refreeze.


## Root Task 4 amendment

The independently verified prior source is af6d5ae7c7083265cede7a78dd295363f3c20808705e4ba85b167fa862da8d5c and fixture e5a920702fe4e8f08e013271b105d95f10a252e8f154f003b64eb82d573cbbc0. Use only /tmp/minion-14-consumers-61p26r_z/minion_hub and its matching 14-11 package/private caches. The 44-case/full-check receipt remains valid for those prior bytes. Task 4 source and test changes require new focused verification and a new aggregate source receipt; no active checkout adoption or browser authority follows. Summary updates are within the already listed ownership.


### Root Task 4 routing identity correction

Before component implementation, extend the same private service/test scope to expose the accepted connection's captured gateway URL alongside its host ID. Capture immutable host identity/URL at actual client construction, before asynchronous authentication; do not read a subsequently edited host record to label an existing socket. A shallow frozen copy of the construction-time Host is acceptable inside the private session, with only the required readonly URL exposed in the public token. No auth token is exposed. Keep existing selected-host predicate and A→B→A policy unchanged; component routing compares its gateway prop against this captured URL, with an explicit ws/http URL normalization policy in14-06.

Tests must prove host URL edits or replacement during handshake and after authentication cannot relabel a token's actual connection destination; reconnect of the existing client still carries its original URL, while a genuinely new client captures the new URL. Preserve all53 cases. This is an accepted-session identity correction, not a new automatic reconnect or host-edit policy. Prior53/full-check receipt remains valid for the prior source identity; a new focused and aggregate check follows the final freeze.
