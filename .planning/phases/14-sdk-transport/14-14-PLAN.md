---
phase: 14-sdk-transport
plan: "14"
type: execute
wave: 3
depends_on: ["14-13", "14-11"]
files_modified:
  - "minion/src/shells/shared-types.ts"
  - "minion/src/shells/shared-types.test.ts"
  - "minion/src/shells/invoke-contract.ts"
  - "minion/src/shells/invoke-contract.test.ts"
autonomous: false
requirements: ["SDK-01", "SDK-02", "AGT-04"]
readiness: "Four-file private source candidate admitted after root package, syntax and isolated-runner review. Routing/config/durability selector remains excluded."
closure_policy: "Canonical facade and pure input contract only; receiver, routing/authority, durable-mode configuration, installed package and production remain separate."
must_haves:
  truths:
    - "Gateway Shells types and validators reuse the exact emitted canonical package rather than copied definitions."
    - "Every existing shim export remains compatible and the gateway-only timer constant stays local."
    - "Text digest preserves exact validated text and cannot grant organization/run authority."
    - "InvocationKey is scoped deduplication input, never an inferred durable-mode selector."
  artifacts:
    - path: "minion/src/shells/shared-types.ts"
      provides: "Selective emitted-package facade with unchanged local timer constant"
    - path: "minion/src/shells/invoke-contract.ts"
      provides: "Pure bounded text/session/key/context normalization and fixed native digest"
  key_links:
    - from: "minion/src/shells/shared-types.ts"
      to: "@minion-stack/shared"
      via: "Same complete private artifact for actual runtime and declarations"
    - from: "minion/src/shells/invoke-contract.ts"
      to: "minion/src/shells/shared-types.ts"
      via: "Canonical shared error/byte/limit helpers; no duplicate outcome validation"
---

<objective>
Replace the stale local Shells type mirror with a selective canonical facade and define the gateway-owned durable-text invocation grammar before receiver implementation. Keep all source work within four files. Do not route calls or fabricate a required-durable wire/config surface.
</objective>
<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Root owns admission, isolated package materialization, global status and final summary/verification.
</execution_context>
<context>
@AGENTS.md
@minion/AGENTS.md
@minion/.dmux-hooks/CLAUDE.md
@.agents/skills/minion-engineering/SKILL.md
@.planning/DECISIONS.md (D360-15)
@.planning/phases/14-sdk-transport/14-GATEWAY-INVOKE-CONTRACT-DECISION.md
@.planning/phases/14-sdk-transport/14-11-SUMMARY.md (Task4 durable artifact)
@.planning/phases/14-sdk-transport/14-13-VERIFICATION.md
@.planning/phases/14-sdk-transport/14-12-PLAN-REVIEW.md
@minion/src/shells/shared-types.ts
@minion/src/gateway/server-methods/shells.ts
@minion/src/shells/manager.ts
@minion/src/config/types.gateway.ts
@minion/src/config/zod-schema.ts
</context>

<readiness_gates>
1. Root independently accepts new archive SHA61ccc90584addf49dd8509cb71a1103ba5fa6dd632a46936d72f1716b794fef0 and complete extracted manifest5240824d8f59f61a9ca0c9d9abefa68d96356be8699e131ede7a842a4a35ef37 from /tmp/minion-14-11-FOyKEiLN. Prior14-11 reconnect candidate and active shared0.6.0 do not include the required contract. No inherited source alias or old declaration is permitted.
2. Root selects the packet's exact own-data envelope, text/identifier emptiness semantics, optional-field handling and explicit limits-policy shape. Numeric production bounds remain unresolved: callers must provide validated positive safe-integer policy for local tests; no invented defaults.
3. Root prepares a new credential-free private gateway snapshot, complete package copy and native runner/compiler selection. Freeze source/config/lock/tool/library hashes, runtime/declaration resolutions and private cache paths before execution. Do not mutate existing Hub/Site snapshots. Gateway branch fix/ci-cost-remaining-gaps is read-only evidence, not a switch instruction.
4. Required-durable invocation has no current field/config surface. Root retains the separate routing/config decision described below. This pure helper cannot make or advertise a wire guarantee. Its qualified source can precede that routing decision, but receiver/caller activation cannot silently pass the gate.
</readiness_gates>

<boundaries>
Exactly the four listed gateway source/test files after admission; this draft owns no source. No canonical shared source/barrel edit, manager, registry, provider, bridge endpoint, caller handler, method list/scope, config, schema migration, lifecycle/startup, package/lock/dist/node_modules, UI or global-plan edit. Current shim consumers are read-only compatibility context. No install/download/network/browser/live socket/HTTP/provider/model/database call or full build. No branch/stash/worktree/commit/publish/adoption. Preserve concurrent WIP and all before-images.

Reuse installed Zod4.4.3 for strict domain schema validation and native node:crypto for the selected fixed digest; reuse actual canonical UTF-8 byte/limit/error helpers. No new dependency, copied outcome validator, generic object-schema framework or arbitrary JSON hash. Root creates summary/proposal records; source executor adds exact-site TODOs for still-unwired facade/parser consumers.
</boundaries>

<tasks>
<task type="auto">
  <name>Task1: Freeze exact package resolution and legacy facade contract</name>
  <files>minion/src/shells/shared-types.test.ts</files>
  <action>After root's setup/admission, record exact emitted runtime and declaration resolution from the private gateway. Require both to land in the same full copied package matching the accepted extracted manifest. Preserve active shared0.6.0 as the missing-contract baseline; do not change it. Verify installed Zod and test/compiler bytes from the snapshot rather than infer from ranges.

Read complete current facade and existing consumers. Freeze its full named export inventory from the packet, including MAX_TIMER_DELAY_MS. Add actual-public-package identity/type tests before facade replacement. Assert constants and helper functions are the canonical runtime objects, so a byte-identical-looking copied implementation still fails identity. Keep representative existing legacy provision/summary/capability/text/multimodal invoke shapes assignable; new optional key/version fields remain additive. Record missing-export/setup failures separately from useful behavioral failures. No existing consumer is edited or granted broader authority.</action>
  <verify>Root reviews exact candidate path/hash/declaration resolution and meaningful focused test baseline; no source alias, mock shared library or copied validator.</verify>
  <done>Canonical runtime/types and the legacy import facade are explicitly identified before source replacement.</done>
</task>
<task type="auto">
  <name>Task2: Replace duplicate facade bodies with selective canonical exports</name>
  <files>minion/src/shells/shared-types.ts, minion/src/shells/shared-types.test.ts</files>
  <action>Re-export existing domain constants/types by explicit name from the real supported shared root/gateway public entry. Preserve every existing facade name from the packet. Keep only the gateway-local MAX_TIMER_DELAY_MS literal2_147_483_647 and any precise handoff comment locally. Add the packet's finite canonical durable helper/type re-export set needed by14-12; no export-star broadening or private /gateway/shells path.

Replace the stale remove-after0.7.0 comment with accurate facade/provenance wording. Do not claim package installation/release from a source re-export. Preserve existing consumer contracts through focused tests and strict native typechecking of the actual importing modules where the isolated check can safely include them. If canonical types expose a real consumer mismatch, stop for bounded root amendment; do not change a fifth source file, cast as any or weaken shared types. Source TODO points to proposal Shells lifecycle and separate receiver/routing/adoption gates.</action>
  <verify>Real facade identity checks, existing export inventory and local timer tests pass against the paired package; no copied definitions remain.</verify>
  <done>Gateway import path is stable while shared protocol truth is canonical.</done>
</task>
<task type="auto">
  <name>Task3: Implement the bounded gateway text-input contract</name>
  <files>minion/src/shells/invoke-contract.ts, minion/src/shells/invoke-contract.test.ts</files>
  <action>Implement one pure normalizer receiving unknown caller envelope, separate authoritative org value and explicit limits policy. Use strict Zod objects for exact allowed caller fields shellId/sessionId/input/invocationKey?/wakeIfArchived? and exact text input kind/text. Reject unknown org/tenant/run/admission/digest/durability fields. A small own-data descriptor check for these known objects rejects getters/symbols/non-enumerables/unsupported prototypes before schema evaluation; do not recursively parse arbitrary objects or promise malicious-Proxy immunity.

Use root-selected well-formed nonempty text and nonblank identifier/org/key semantics from the packet. Preserve all accepted whitespace/Unicode bytes; missing wake defaults true and explicit false stays false, while strings/numbers/null reject. Missing optional key remains absent; it cannot select mode or generate an ID. Use actual canonical normalizeShellOutcomeLimits and shellOutcomeByteLength, plus native string well-formedness and positive-safe-integer Zod checks for explicit additional policy bounds. No policy defaults or environment reads. Check cheap character bounds before UTF-8 measurement where appropriate, then measure only the fresh validated known envelope for the selected envelopeBytes cap.

Return fresh normalized text invoke data, context org, optional exact org/shell/key tuple and SHA-256 of UTF-8 JSON.stringify(['minion.shells.input',1,'text',text]). Never hash arbitrary input JSON, include routing wake in text identity, allocate a run/time, read a shell/store or forward input. Session/key/org equality is separately preserved for later receiver checks. Reuse the fixed canonical validation error without leaking Zod/raw input details. Document syntax validation versus authenticated org provenance; a string alone is no proof of membership.

Write independent golden-byte/digest tests and boundary cases: exact text and nonmutation; invalid/missing policy; UTF-8 multibyte limits and unpaired surrogates; empty/nonblank policy; unknown fields; accessors/toJSON/coercion canaries uncalled; optional wake/key cases; text-only refusal of multimodal; oversized normalized envelope; same text/different property order equal digest; whitespace/Unicode/newline changes differ; org/key/session/wake changes do not alter text digest but remain distinct in result; delimiter-bearing key tuples do not collide. No fake manager/session/persistence model. Add exact-site handoff for the real caller route that still drops keys and for mode selection owned outside this child.</action>
  <verify>Focused actual normalizer/facade tests and strict native types pass with explicit synthetic limits; source code uses actual installed schema/canonical helpers and no live resources.</verify>
  <done>Reusable pure input contract is qualified without asserting route enforcement or durable execution.</done>
</task>
<task type="auto">
  <name>Task4: Freeze paired evidence and route/config handoff</name>
  <files>minion/src/shells/shared-types.test.ts, minion/src/shells/invoke-contract.test.ts</files>
  <action>Run only the two admitted focused test files with root-selected native runner/config and explicit private cache/env before start. Run scoped native typecheck/lint/format; root separately schedules necessary existing-consumer checks. Verify full package copy and runtime/declaration resolution unchanged, active files untouched and only four source/test paths changed in the candidate. Record actual red/green versus setup errors and exact source/tool/command/package hashes for root's independent review/summary. No full gateway build or installed adoption.

Carry the unresolved selection into caller C/lifecycle L: current invoke has no durability field; existing config has no durable mode. Proposed server-only disabled|required policy is an explicit foundation choice, not caller wire intent. Required mode refuses unavailable/incompatible without fallback; disabled legacy branch must not silently ignore invocationKey. If client-requested durability across differently configured gateways is required, root separately chooses an additive required method or canonical field and its real dispatcher/capability tests. No hidden method/field/config is added here.14-12 keyed recovery/native receipts, organization authority, caller scopes, no-key native IDs and full package adoption remain their own checks.</action>
  <verify>Independent Standards/Spec review at exact four-file freeze; root receives paired source/artifact/declaration and remaining mode/routing decisions.</verify>
  <done>Qualified prerequisite with narrow ownership; no receiver, routing, package release or requirement completion claim.</done>
</task>
</tasks>

<verification>
Standards: strict types, installed-library reuse, fixed errors, no new dependencies, safe copied package/runner and four-file scope. Spec: selective canonical parity and exact text/key/context validation with deterministic input projection; required durability is never inferred from a key/helper/capability. Source facade success does not certify runtime adoption. Future numeric receiver reservation/combined-query bounds and all14-12 review gates remain necessary.
</verification>
<output>
Root creates14-14-SUMMARY.md and independent verification after execution. This draft and14-GATEWAY-INVOKE-CONTRACT-DECISION.md provide exact scope/choices only. No execution or new child source admission follows from their existence.
</output>


## Root four-file isolated source admission

Root accepts the full14-11 durable archive61ccc90584addf49dd8509cb71a1103ba5fa6dd632a46936d72f1716b794fef0 and repeated runtime/closure/type evidence in14-11-VERIFICATION. All125 files are copied into /tmp/minion-14-14-qwkly7fi/minion/node_modules/@minion-stack/shared; no source alias or active package edit. Snapshot contains4502 source/config files and101 individual read-only dependency links; manifest /tmp/minion-14-14-qwkly7fi/snapshot.json SHA111af51c0141e9311487e0bca8333d7b47d4dca683d711f89ac5f528f8a9d761. Native preflight confirms ESM public runtime under this copied package, Vitest4.1.10/TypeScript5.9.3/Zod4.4.3, envDir:false and private qc-cache/vitest/da39a3ee5e6b4b0d3255bfef95601890afd80709 before collection. Two initial root receipt attempts used unexported require.resolve paths and failed; corrected ESM/physical tool-path inspection passes. No test baseline is claimed from setup failures.

Select nonempty well-formed text; preserve whitespace-only text and all accepted Unicode/whitespace. IDs, org and optional key must be nonblank without rewriting accepted bytes. Missing wake defaults true, explicit booleans only; absent key stays absent. Select policy exactly {contract: ShellOutcomeLimits, organizationBytes, invocationKeyBytes, envelopeBytes}, all own-data, explicit and validated; no defaults or production numeric bounds. Digest uses the fixed text projection in Task3. Separate authenticated context remains the only future org authority. This helper grants none.

Only the four listed source/test files inside this private candidate may change. The native lane is env -i PATH=/usr/bin:/bin HOME=/tmp/minion-14-14-qwkly7fi LANG=C.UTF-8 node qc-run-contract.mjs; root config selects exactly the two owned test files with a zero-native-network setup. Run strict types via node node_modules/typescript/bin/tsc -p qc-types.json --noEmit in the same allowlisted environment. Root selects any expanded existing-consumer check. No default gateway test suite, source/package installation, runtime/config route, DB, network or fifth source file. Preserve before-images and freeze exact source/package/runtime/declaration evidence. Missing-module baselines are structural; distinguish them from meaningful behavioral negatives. Root owns independent verification, source adoption and summary.


## Required v1 profile facade continuation

14-16 canonical source114cases and the new complete14-11 archive `f6a3c237c0264a2a8ef75906991ae8fa84504bf637d3b6655d07058582b603fa` independently pass. Admit fresh private snapshot preparation under `/tmp/minion-14-14-profile-XXXXXX`, preserving the entire earlier91-case candidate. Copy its recorded full gateway source/config inputs and four qualified files, individual readonly dependency links, private empty caches, and the complete new125-file extracted package. Record runtime/declaration resolution before collection. No active gateway source, package/lock or installed dependency changes.

After root reviews the exact setup, continuation source ownership is only `shared-types.ts` and `shared-types.test.ts`: selectively expose the required response normalizer/types and immutable input/outcome/profile constants; preserve every legacy facade export and local timer. Add real emitted-package parity and actual unchanged text-parser consumption of the fixed input profile, including64KiB input/UTF8/escaped envelope boundaries and4KiB outcome distinction. Do not change invoke-contract.ts or its existing test. Scope strict types/lint and existing91cases with additions. A missing canonical export cannot be replaced with a copied guard, source alias or cast. Root reviews frozen result before receiver admission; caller/config/runtime remain separate.
