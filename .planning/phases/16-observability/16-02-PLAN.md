---
phase: 16-observability
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - "minion_hub/scripts/qc/telemetry-fixture.ts"
  - "minion_hub/scripts/qc/telemetry-fixture.test.ts"
  - ".planning/phases/16-observability/16-SENTRY-ACCESS.md"
  - "minion_hub/vite.config.ts"
  - "minion_hub/src/instrumentation.server.ts"
  - "minion_hub/.github/workflows/ci.yml"
  - ".planning/phases/16-observability/16-RECEIPT.md"
autonomous: true
requirements: ["OBS-02"]
readiness: "Local fixture executable; remote upload/receipt requires existing legitimate Sentry access and identified target environment."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Local source map resolves fixture line and candidate; access report separates absent tooling from unverified remote state."
    - "Actual environment receipt matches source map/release/fixture line; source-only integration cannot close OBS-02."
  artifacts:
    - path: "minion_hub/scripts/qc/telemetry-fixture.ts"
      provides: "Prepare release-map fixture and access evidence"
    - path: "minion_hub/vite.config.ts"
      provides: "Wire release upload and execute configured-environment receipt gate"
  key_links:
    - from: "minion_hub/scripts/qc/telemetry-fixture.ts"
      to: "minion_hub/vite.config.ts"
      via: "Read-first contract and behavior verification for prepare release-map fixture and access evidence"
    - from: "minion_hub/vite.config.ts"
      to: ".planning/phases/16-observability/16-SENTRY-ACCESS.md"
      via: "Read-first contract and behavior verification for wire release upload and execute configured-environment receipt gate"
---

<objective>
Prove source-map and telemetry receipt from execution environment.
Purpose: OBS-02 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/16-observability/16-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/vite.config.ts
@minion_hub/src/hooks.server.ts
@.planning/research/360-ui-dependency-verification.md
@.planning/phases/16-observability/16-SENTRY-ACCESS.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Prepare release-map fixture and access evidence</name>
  <files>minion_hub/scripts/qc/telemetry-fixture.ts, minion_hub/scripts/qc/telemetry-fixture.test.ts, .planning/phases/16-observability/16-SENTRY-ACCESS.md</files>
  <read_first>minion_hub/vite.config.ts, minion_hub/src/hooks.server.ts, .planning/research/360-ui-dependency-verification.md</read_first>
  <behavior>Local source map resolves fixture line and candidate; access report separates absent tooling from unverified remote state. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Discover legitimate Sentry project/release/source-map capability using installed tools and allowlisted config existence, never printing credentials. Generate a build fixture error with unique test marker and source location, then verify local minified-to-source mapping. Prepare exact instrumentation/upload changes following primary Sentry docs. If org/project auth is absent, record needed access and ready-to-run command; don't create a new account/project or send synthetic production errors to prove access.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run scripts/qc/telemetry-fixture.test.ts</automated></verify>
  <done>Local source map resolves fixture line and candidate; access report separates absent tooling from unverified remote state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release upload and execute configured-environment receipt gate</name>
  <files>minion_hub/vite.config.ts, minion_hub/src/instrumentation.server.ts, minion_hub/.github/workflows/ci.yml, .planning/phases/16-observability/16-RECEIPT.md</files>
  <read_first>.planning/phases/16-observability/16-SENTRY-ACCESS.md</read_first>
  <behavior>Actual environment receipt matches source map/release/fixture line; source-only integration cannot close OBS-02. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Proceed only after actual installed Sentry compatibility and target environment are known. Wire explicit release/environment and private source-map upload using existing CI secret authority, keeping maps out of public output as configured. Trigger the harmless fixture in the authorized disposable/preview runtime and query receipt/source line by marker. Record event/release/runtime identity without payload secrets. Missing remote access remains an explicit checkpoint, not local-only acceptance.</action>
  <verify><automated>cd minion_hub &amp;&amp; bun run build &amp;&amp; node node_modules/vitest/vitest.mjs run scripts/qc/telemetry-fixture.test.ts</automated></verify>
  <done>Actual environment receipt matches source map/release/fixture line; source-only integration cannot close OBS-02.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Untrusted error payload / telemetry transport / alert destination | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-16-02-01 | I/S | Secret leakage or false environment attribution | mitigate | Allowlisted sanitization, verified release context and explicit target/test authorization. |
| T-16-02-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/16-observability/16-02-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

