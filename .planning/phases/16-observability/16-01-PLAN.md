---
phase: 16-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "minion_hub/src/lib/server/observability-context.ts"
  - "minion_hub/src/lib/server/observability-context.test.ts"
  - "minion_hub/src/hooks.server.ts"
  - "minion_hub/src/lib/server/posthog.ts"
  - ".planning/phases/16-observability/16-PRODUCER-MATRIX.md"
autonomous: true
requirements: ["OBS-01"]
readiness: "Executable after stated dependency and exclusive file-ownership gates; independently check before dispatch."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Allowed identities survive; sensitive and unbounded properties never reach a capture client; unknown release stays explicit."
    - "Hub error events are attributable and redacted; every required agent producer is either verified or named in an executable gap plan."
  artifacts:
    - path: "minion_hub/src/lib/server/observability-context.ts"
      provides: "Define sanitizer and request identity contract"
    - path: "minion_hub/src/hooks.server.ts"
      provides: "Wire request errors and document agent producer gaps"
  key_links:
    - from: "minion_hub/src/lib/server/observability-context.ts"
      to: "minion_hub/src/hooks.server.ts"
      via: "Read-first contract and behavior verification for define sanitizer and request identity contract"
    - from: "minion_hub/src/hooks.server.ts"
      to: "minion_factory/runner/src/containment-effects.ts"
      via: "Read-first contract and behavior verification for wire request errors and document agent producer gaps"
---

<objective>
Attach sanitized execution identity to server and agent events.
Purpose: OBS-01 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/16-observability/16-CONTEXT.md
@minion_hub/CLAUDE.md
@minion_hub/src/hooks.server.ts
@minion_hub/src/lib/server/posthog.ts
@packages/shared/src/gateway/traceparent.ts
@minion_factory/runner/src/containment-effects.ts
@drone/src/types.ts

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Define sanitizer and request identity contract</name>
  <files>minion_hub/src/lib/server/observability-context.ts, minion_hub/src/lib/server/observability-context.test.ts</files>
  <read_first>minion_hub/src/hooks.server.ts, minion_hub/src/lib/server/posthog.ts, packages/shared/src/gateway/traceparent.ts</read_first>
  <behavior>Allowed identities survive; sensitive and unbounded properties never reach a capture client; unknown release stays explicit. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Create typed allowlisted environment/release/route-template/trace/request/run/effect properties. Derive release from validated deployment metadata and actor from server auth, never user-supplied tracing headers as authority. Redact tokens, cookie/auth headers, raw query text, payload bodies and customer identifiers from error properties. Include unknown attribution explicitly rather than inventing a release. Fixture hostile nested Error.cause and oversized messages.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/server/observability-context.test.ts</automated></verify>
  <done>Allowed identities survive; sensitive and unbounded properties never reach a capture client; unknown release stays explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire request errors and document agent producer gaps</name>
  <files>minion_hub/src/hooks.server.ts, minion_hub/src/lib/server/posthog.ts, minion_hub/src/lib/server/observability-context.test.ts, .planning/phases/16-observability/16-PRODUCER-MATRIX.md</files>
  <read_first>minion_factory/runner/src/containment-effects.ts, drone/src/types.ts</read_first>
  <behavior>Hub error events are attributable and redacted; every required agent producer is either verified or named in an executable gap plan. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Apply sanitizer to serverErrorHandler and capture paths, preserving serverless flush/shutdown behavior and bounded retry. Inventory actual Factory/Drone/Shells/gateway producers with source/runtime identity and secret handling; add exact child plans for missing producers rather than assuming Hub instrumentation covers agents. No remote event upload beyond authorized configured telemetry in this source task; test uses fake transports.</action>
  <verify><automated>cd minion_hub &amp;&amp; node node_modules/vitest/vitest.mjs run src/lib/server/observability-context.test.ts &amp;&amp; bun run check</automated></verify>
  <done>Hub error events are attributable and redacted; every required agent producer is either verified or named in an executable gap plan.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Untrusted error payload / telemetry transport / alert destination | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-16-01-01 | I/S | Secret leakage or false environment attribution | mitigate | Allowlisted sanitization, verified release context and explicit target/test authorization. |
| T-16-01-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/16-observability/16-01-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

