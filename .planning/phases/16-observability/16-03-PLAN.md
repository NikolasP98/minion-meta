---
phase: 16-observability
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - "scripts/qc/incident-query-contract.mjs"
  - "scripts/qc/incident-query-contract.test.mjs"
  - ".planning/phases/16-observability/16-INCIDENT-VIEWS.md"
  - ".planning/phases/16-observability/16-ALERT-ROUTING.md"
  - "scripts/qc/alert-routing-contract.test.mjs"
autonomous: true
requirements: ["OBS-03"]
readiness: "Read-only views/local contracts executable; external messages require explicit recipient/test authorization."
closure_policy: "Plan coverage does not fulfill a requirement. Discovery, missing policy, generated gap plans, actual-runtime and release gates remain open until independently evidenced."
must_haves:
  truths:
    - "Incident queries have valid denominators and explicit missing attribution; no customer or secret payload is exposed."
    - "Threshold and recipient authority known; routed synthetic receipt or an explicit pending gate is recorded, never assumed."
  artifacts:
    - path: "scripts/qc/incident-query-contract.mjs"
      provides: "Build queries with valid denominators and environment filters"
    - path: ".planning/phases/16-observability/16-ALERT-ROUTING.md"
      provides: "Prepare and verify the actual alert recipient path"
  key_links:
    - from: "scripts/qc/incident-query-contract.mjs"
      to: ".lavish/minion-qc-2026-09-08/telemetry.json"
      via: "Read-first contract and behavior verification for build queries with valid denominators and environment filters"
    - from: ".planning/phases/16-observability/16-ALERT-ROUTING.md"
      to: ".planning/phases/16-observability/16-INCIDENT-VIEWS.md"
      via: "Read-first contract and behavior verification for prepare and verify the actual alert recipient path"
---

<objective>
Qualify attributable incident views and alert routes.
Purpose: OBS-03 in the user-authorized360 program. Deliver the full stated behavior; where evidence must choose the exact repair, this plan is an admission gate and cannot close the requirement by itself.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
Installed runtime is ~/.claude/get-shit-done; the catalog's ~/.Codex path is absent. Do not create or modify installations to satisfy that stale path.
</execution_context>

<context>
@.planning/operations/360/PROGRAM.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/phases/16-observability/16-CONTEXT.md
@minion_hub/CLAUDE.md
@.lavish/minion-qc-2026-09-08/telemetry.json
@.planning/phases/16-observability/16-PRODUCER-MATRIX.md
@.planning/phases/16-observability/16-INCIDENT-VIEWS.md

</context>

<boundaries>
Per D360-01, preserve existing WIP; inspect branch/SHA/diff and record scoped before-images. No branch/worktree/stash/automatic commit, publishing or production mutation. Read the nearest repository instructions; root grants exclusive file ownership before edits. Current source, upstream, installed archive and deployed image are separate identities.
Per D360-02 retain raw-SQL containment; per D360-03 no Factory containment activation or live dispatch/merge flag change without its explicit gate; per D360-04 no paid provider calls by default. Per D360-05 validate actual assignment/schema identity rather than historical comments. Per D360-06 waves inside this phase do not override ROADMAP cross-phase integration dependencies or shared-file ownership.
Any prerequisite file produced by an earlier task/plan must exist with passing evidence before its consumer runs. If research or a fixture identifies a new source seam, create an exact bounded child PLAN, validate/review it, add it to the inventory and keep this requirement pending until that plan executes. Do not hide a failed behavior behind a report-only success. Only missing consequential policy, actual authentication or live-action authority creates a dynamic checkpoint; continue independent local evidence while it is pending.
No fixed SLO, retention duration, RPO/RTO, harness credentials or paid-test budget is inferred from these plans. Configuration proposals remain proposals until established authority/decision permits them.
</boundaries>

<tasks>
<task type="auto">
  <name>Task 1: Build queries with valid denominators and environment filters</name>
  <files>scripts/qc/incident-query-contract.mjs, scripts/qc/incident-query-contract.test.mjs, .planning/phases/16-observability/16-INCIDENT-VIEWS.md</files>
  <read_first>.lavish/minion-qc-2026-09-08/telemetry.json, .planning/phases/16-observability/16-PRODUCER-MATRIX.md</read_first>
  <behavior>Incident queries have valid denominators and explicit missing attribution; no customer or secret payload is exposed. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Use PostHog/Sentry read-only schema discovery to construct per-environment/release incident queries. Distinguish occurrences, distinct sessions/users, requests and eligible run/effect counts; don't sum overlapping distinct counts or divide mixed-environment errors by production pageviews. Define sparse/unknown attribution presentation and raw-data exclusion. Local fixtures validate query cardinality and filters before saving any dashboard.</action>
  <verify><automated>node --test scripts/qc/incident-query-contract.test.mjs</automated></verify>
  <done>Incident queries have valid denominators and explicit missing attribution; no customer or secret payload is exposed.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare and verify the actual alert recipient path</name>
  <files>.planning/phases/16-observability/16-ALERT-ROUTING.md, scripts/qc/alert-routing-contract.test.mjs</files>
  <read_first>.planning/phases/16-observability/16-INCIDENT-VIEWS.md</read_first>
  <behavior>Threshold and recipient authority known; routed synthetic receipt or an explicit pending gate is recorded, never assumed. Include negative/failure cases named in the action before implementation. For evidence scripts, test rejection of incomplete or unsafe manifests as well as accepted synthetic fixtures.</behavior>
  <action>Read existing alert rules/recipients without sending notifications. Document exact environment, rule, threshold source, suppression/dedup and destination. Derive thresholds from established SLO or recorded observations; missing target/threshold is a policy gate, not a guessed paging rule. Prepare an identified synthetic test packet; send only with explicit existing notification authorization. Remote delivery must be evidenced before claiming alert closure.</action>
  <verify><automated>node --test scripts/qc/alert-routing-contract.test.mjs</automated></verify>
  <done>Threshold and recipient authority known; routed synthetic receipt or an explicit pending gate is recorded, never assumed.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| Untrusted error payload / telemetry transport / alert destination | Authority and data cross this boundary; preserve the existing tenant and effect contract. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-16-03-01 | I/S | Secret leakage or false environment attribution | mitigate | Allowlisted sanitization, verified release context and explicit target/test authorization. |
| T-16-03-02 | I/T | Evidence and fixture artifacts | mitigate | Synthetic data, allowlisted metadata, no credentials/customer payloads; record exact source and runtime identity. |
</threat_model>

<verification>
Run each automated command from meta root unless it includes an explicit cd. Use installed tool versions and the owning repository's package manager; never install an unrelated latest runner through bunx/npx. Red-green or explicit before/after evidence is required for meaningful behavior changes. Long builds/browser/disposable integration checks may exceed60seconds: keep the tool session attached and report progress; do not replace them with weaker checks. Missing executables, fixtures, environment or skipped cases are pending/blocked evidence, not pass.
Independent review separates Standards from Spec/requirements. Verify same-wave files are exclusive, dependency outputs exist, and no unrelated WIP was changed. Run scoped git diff --check and relevant project gates. Any unresolved source implementation gets both TODO(handoff) at its site and matching proposal under root ownership.
</verification>

<success_criteria>
Both task done criteria have actual evidence at the recorded candidate. A source fix does not establish deployment, mocked harness does not establish real ACP, and a generated child plan does not establish its implementation. Requirement closure additionally requires every associated plan and mandatory runtime/policy gate. Keep phase open while any such gate is pending.
</success_criteria>

<output>
Create .planning/phases/16-observability/16-03-SUMMARY.md with commands/results, exact identities, owned files, decision IDs, evidence limits and next gated plan. Do not modify canonical ROADMAP/STATE/REQUIREMENTS or commit; root owns those updates. Independent verifier writes the phase verification only after all plans, including generated gaps, are accounted for.
</output>

