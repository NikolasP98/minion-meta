---
phase: 01-clean-slate
plan: 05
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - specs/clean-slate-inventory.md
  # Plus each affected subproject's `.git/config` (upstream tracking config — mutates subproject .git, not meta-repo)
autonomous: false
requirements:
  - CLEAN-02
must_haves:
  truths:
    - "Every subproject's default branch tracks a valid upstream remote (verify: `git -C <path> rev-parse --abbrev-ref --symbolic-full-name @{u}` returns a valid refspec — no [gone] marker, no error)"
    - "`minion_plugins/` main branch tracks a real remote branch (not origin/master [gone]) — specifically: if `origin/main` exists on the remote, track that; otherwise coordinate with user on remote repair"
    - "Every fix executed matches an APPROVED disposition in specs/clean-slate-inventory.md §Broken Upstreams"
    - "No branch rename, no remote add/remove, and no push was performed without explicit user approval in Task 3"
  artifacts:
    - path: "specs/clean-slate-inventory.md"
      provides: "§Resolution Log — 01-05 recording each upstream fix"
      contains: "## Resolution Log — 01-05"
  key_links:
    - from: "<subproject>/.git/config [branch \"<default>\"]"
      to: "<remote>/<branch>"
      via: "git branch --set-upstream-to=<remote>/<branch> <default-branch>"
      pattern: "set-upstream-to"
---

<objective>
Fix every broken upstream tracking relationship across the 7 subprojects, as documented in `specs/clean-slate-inventory.md §Broken Upstreams`. Known target: `minion_plugins` main → origin/master [GONE]. Additional broken upstreams discovered in Plan 01-01 must also be resolved here.

Purpose: CLEAN-02 — every default branch tracks a valid upstream.

Output: Fixed upstream tracking on every affected subproject + Resolution Log entries.

**Hard constraints:**
- NEVER push to default branches (main/master/dev/DEV/minion-integration) in this plan
- NEVER rename or delete default branches
- ONLY mutate local `.git/config` upstream tracking (`git branch --set-upstream-to`) after verifying the target remote branch exists
- If the remote itself is broken (e.g., the repo was renamed on GitHub), surface that to the user — do not add new remotes or rename local branches without explicit approval
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-clean-slate/01-CONTEXT.md
@specs/clean-slate-inventory.md
@CLAUDE.md

# Memory files (authoritative for known broken tracking):
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_minion_plugins_broken_tracking.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_ai_repo.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_paperclip_fork.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse broken upstream list + probe remotes to determine the correct fix per subproject</name>
  <files>specs/clean-slate-inventory.md (read-only in this task)</files>
  <read_first>
    - specs/clean-slate-inventory.md §Broken Upstreams (and any per-subproject `Upstream: [GONE]` or `Upstream: MISSING` notes in the Subprojects section)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_minion_plugins_broken_tracking.md
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md (local dir → GitHub repo mapping to build correct remote URLs)
  </read_first>
  <action>
1. Parse `specs/clean-slate-inventory.md §Broken Upstreams`. Build a list of broken entries, each with:
   - Subproject path
   - Local branch name (e.g., `main`)
   - Current upstream (e.g., `origin/master [GONE]` or `MISSING`)
   - Recommendation from the inventory (e.g., "set-upstream-to origin/main")
   - Disposition tag (APPROVED / HOLD / MODIFY)

2. Also scan the Subprojects section of the inventory for any subproject whose `Upstream:` field is `[GONE]` or `MISSING` — add it to the work list if not already there.

3. For each item on the work list, PROBE the remote to determine the correct target BEFORE proposing a fix:
   - `git -C <subproject> remote -v` → list remotes; verify `origin` points to the expected repo (per reference_github_repo_names.md)
   - `git -C <subproject> fetch origin --prune` → refresh remote refs (this is non-destructive — only updates remote-tracking branches)
   - `git -C <subproject> ls-remote --heads origin` → list all branches on the remote
   - Identify the intended target branch: typically `main` if it exists, else `master`, else the only remaining branch
   - Record the target in the work list

4. Print the resolved work list to chat for Task 3 checkpoint approval. Format:
   ```
   BROKEN UPSTREAMS WORK LIST
   - minion_plugins: local=main, current-upstream=origin/master [GONE], probe: origin has branches [main, dev], proposed=set-upstream-to origin/main, disposition=APPROVED
   - ... (additional entries)
   ```

5. If a subproject's `origin` remote URL no longer matches what `reference_github_repo_names.md` says it should be (e.g., the repo was renamed on GitHub), flag it in the work list with `REMOTE-URL-MISMATCH` and surface in Task 3 — do NOT auto-change the remote URL.

6. Do NOT execute `git branch --set-upstream-to` in this task. Only probe + build work list.
  </action>
  <verify>
    <automated>git -C /home/nikolas/Documents/CODE/AI/minion_plugins remote -v 2>&1 | head -2</automated>
  </verify>
  <acceptance_criteria>
    - Work list contains an entry for every subproject flagged `[GONE]` or `MISSING` in the inventory
    - For each entry, the probe ran successfully: `git fetch origin --prune` completed and `ls-remote --heads origin` output was captured
    - For each entry, a proposed target branch was identified (or `REMOTE-URL-MISMATCH` flagged)
    - Work list printed to agent transcript in the documented format
    - NO `git branch --set-upstream-to`, `git remote set-url`, `git remote add`, `git remote remove`, or `git push` was executed in this task (verify by reviewing Bash transcript)
  </acceptance_criteria>
  <done>Work list with proposed fixes + disposition tags is ready for Task 2 checkpoint.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Checkpoint — confirm each proposed upstream fix (especially REMOTE-URL-MISMATCH cases)</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - Work list from Task 1 (in conversation context — includes probe results per subproject)
    - specs/clean-slate-inventory.md §Broken Upstreams (for cross-reference)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_minion_plugins_broken_tracking.md (known minion_plugins case)
  </read_first>
  <action>
Present the Task-1 work list to the user with these details per entry: subproject, current local branch, current broken upstream (e.g., `origin/master [GONE]`), remote branches discovered by `ls-remote --heads origin`, proposed target branch, and any `REMOTE-URL-MISMATCH` flag. Highlight every `REMOTE-URL-MISMATCH` case prominently — these require extra scrutiny because changing a remote URL has bigger consequences than changing upstream tracking. Block on the user's choice (confirm-all-as-proposed / confirm-some / modify-targets / skip). Do NOT execute `git branch --set-upstream-to`, `git remote set-url`, `git remote add`, or `git push` during this task.
  </action>
  <verify>
    <automated>git -C /home/nikolas/Documents/CODE/AI/minion_plugins rev-parse --abbrev-ref --symbolic-full-name @{u} 2>&1 | head -1</automated>
  </verify>
  <acceptance_criteria>
    - User response captured (one of `confirm-all-as-proposed`, `confirm-some: ...`, `modify-targets: ...`, `skip`)
    - No upstream-tracking, remote, or push mutation was performed during this task (verify Bash transcript)
    - Any user-specified modification (e.g., `modify-targets: minion_plugins=origin/main-alt`) is recorded in conversation context for Task 3 consumption
  </acceptance_criteria>
  <done>User's decision is captured; Task 3 knows which fixes to execute and any overrides.</done>
  <decision>Approve the proposed set-upstream-to command for each work list entry, or redirect mismatched remotes.</decision>
  <context>
    Task 1 built a list of broken upstreams with probe results and proposed fixes. Before executing `git branch --set-upstream-to`, confirm:
    1. The proposed target branch is correct (e.g., `origin/main` vs `origin/dev`)
    2. Any `REMOTE-URL-MISMATCH` case is intentional — do you want to update the remote URL, move to a new remote, or leave the mismatch in place?

    Known cases from memory:
    - `minion_plugins`: expected fix is `set-upstream-to origin/main` IF origin/main exists. Per memory `project_minion_plugins_broken_tracking.md`, the upstream has `origin/master [gone]` — you may have renamed master to main on GitHub but local still tracks the old name.

    If the work list is empty (no broken upstreams), answer `skip`.
  </context>
  <options>
    <option id="confirm-all-as-proposed">
      <name>Execute all proposed set-upstream-to commands as listed</name>
      <pros>Fastest path. All work-list items were APPROVED in the inventory and probes confirmed target branches exist.</pros>
      <cons>If a REMOTE-URL-MISMATCH exists and you didn't notice, the fix will set tracking to the wrong remote. Re-read the work list carefully.</cons>
    </option>
    <option id="confirm-some">
      <name>Confirm SOME items — specify which subprojects to fix, hold the rest</name>
      <pros>Surgical. Any unsure case stays broken for the moment.</pros>
      <cons>Requires listing subprojects.</cons>
    </option>
    <option id="modify-targets">
      <name>Modify one or more proposed targets — provide new `<remote>/<branch>` per subproject</name>
      <pros>Overrides a wrong probe result.</pros>
      <cons>Requires specifying the override per item.</cons>
    </option>
    <option id="skip">
      <name>Work list is empty or all items are HOLD — no action this plan</name>
      <pros>No-op.</pros>
      <cons>None.</cons>
    </option>
  </options>
  <resume-signal>Answer: `confirm-all-as-proposed`, `confirm-some: <subproject> <subproject>...`, `modify-targets: <subproject>=<remote>/<branch>, ...`, or `skip`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute confirmed upstream fixes</name>
  <files>
    specs/clean-slate-inventory.md
    # Also mutates <subproject>/.git/config for each fixed subproject (not meta-repo files)
  </files>
  <read_first>
    - Work list from Task 1, filtered by Task 2 confirmation
    - specs/clean-slate-inventory.md (Resolution Log section)
  </read_first>
  <action>
For each subproject confirmed in Task 2:

1. Verify target remote branch exists (re-probe): `git -C <subproject> ls-remote --heads origin <target-branch>` returns a sha
2. If probe returns empty, halt for that item and log `SKIPPED — target branch <target> no longer exists on remote`
3. Execute: `git -C <subproject> branch --set-upstream-to=origin/<target-branch> <local-branch>`
4. Verify: `git -C <subproject> rev-parse --abbrev-ref --symbolic-full-name @{u}` returns `origin/<target-branch>` (not `[gone]`, not an error)
5. Also verify: `git -C <subproject> rev-list --left-right --count HEAD...@{u}` runs without error (confirms tracking is functional)
6. Append to `specs/clean-slate-inventory.md` under `## Resolution Log — 01-05`:
   `- UPSTREAM-FIX <subproject>: <local-branch> → origin/<target-branch> (was: <old-upstream>)`

For REMOTE-URL-MISMATCH cases (only if Task 2 explicitly approved fixing the remote URL):
1. `git -C <subproject> remote set-url origin <new-url>` (URL from Task 2 confirmation)
2. `git -C <subproject> fetch origin --prune`
3. Re-run the upstream fix steps above
4. Append: `- REMOTE-URL-UPDATE <subproject>: origin → <new-url>`

**Hard rules:**
- NEVER `git push` in this plan (we are only fixing local tracking, not pushing anything)
- NEVER `git branch --unset-upstream && git branch --set-upstream-to` as a chain — just use `--set-upstream-to` which overwrites
- NEVER modify the default branch name (`git branch -m`) — only change its upstream pointer
- NEVER add/remove a remote without Task 2 approval

After all fixes, run a final verification sweep:
```
for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do
  echo -n "$d: "
  git -C /home/nikolas/Documents/CODE/AI/$d rev-parse --abbrev-ref --symbolic-full-name @{u} 2>&1
done
```
Expect: every subproject prints a valid `origin/<branch>` line with no errors and no `[gone]` marker (except for any HOLD items).

Record the final sweep output in the Resolution Log for audit.
  </action>
  <verify>
    <automated>for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do echo -n "$d: "; git -C /home/nikolas/Documents/CODE/AI/$d rev-parse --abbrev-ref --symbolic-full-name @{u} 2>&1; done</automated>
  </verify>
  <acceptance_criteria>
    - Every subproject confirmed in Task 2: `git -C <path> rev-parse --abbrev-ref --symbolic-full-name @{u}` returns a valid refspec (no error, no `[gone]`)
    - Every fix has one line in `specs/clean-slate-inventory.md §Resolution Log — 01-05` in format `- UPSTREAM-FIX <subproject>: ...` or `- REMOTE-URL-UPDATE <subproject>: ...`
    - NO `git push`, `git branch -m`, `git branch -D`, `git remote remove`, or `git remote add` was executed without explicit per-subproject approval in Task 2 (verify by reviewing Bash transcript)
    - The final verification sweep output is appended to the Resolution Log as a code block for audit purposes
    - For `minion_plugins` specifically (per memory `project_minion_plugins_broken_tracking.md`): `git -C minion_plugins rev-parse --abbrev-ref --symbolic-full-name @{u}` no longer returns `origin/master` — it returns either `origin/main` (if fix was approved) or still shows the broken state (if HOLD was chosen, which should be surfaced in Task 2)
  </acceptance_criteria>
  <done>All confirmed upstream fixes applied; Resolution Log records every change; final sweep confirms every touched subproject tracks a valid upstream.</done>
</task>

</tasks>

<verification>
  Plan 01-05 is complete when:
  - For every subproject NOT on the HOLD list: `git -C <path> rev-parse --abbrev-ref --symbolic-full-name @{u}` returns a valid upstream with no `[gone]` marker
  - `specs/clean-slate-inventory.md §Resolution Log — 01-05` records every fix
  - No push, branch rename, or remote add/remove occurred without explicit Task 2 approval

  Commands:
  - `for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do git -C /home/nikolas/Documents/CODE/AI/$d rev-parse --abbrev-ref --symbolic-full-name @{u} 2>&1 | grep -c '\[gone\]' || true; done` — expect 0 `[gone]` markers across all lines
  - `grep -c '^## Resolution Log — 01-05' specs/clean-slate-inventory.md` returns 1
</verification>

<success_criteria>
  - CLEAN-02 satisfied: every subproject's default branch tracks a valid upstream (or has an explicit HOLD disposition)
  - The `minion_plugins main → origin/master [gone]` defect is resolved (or explicitly held)
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate/01-05-SUMMARY.md` documenting:
- Count of upstream fixes applied
- Per-subproject before/after upstream values
- Any HOLD items with reasons
- Final sweep showing `@{u}` for every subproject
- Link to updated inventory doc
</output>
