---
phase: 01-clean-slate
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - specs/clean-slate-inventory.md
  # Plus PR mutations on GitHub (merge / close / rebase) — these touch subproject remotes, not files in the meta-repo
autonomous: false
requirements:
  - CLEAN-04
must_haves:
  truths:
    - "Every open PR across the 7 subproject GitHub repos appears in `specs/clean-slate-inventory.md §Open PRs` with a classification (merge / close / rebase / hold) and a rationale"
    - "Every PR classified as `merge` that was confirmed for execution is merged on GitHub (verify: `gh pr view <pr> --json state` returns `MERGED`)"
    - "Every PR classified as `close` that was confirmed for execution is closed (verify: `gh pr view <pr> --json state` returns `CLOSED`)"
    - "Every PR classified as `rebase` has a documented follow-up (rebase run OR left open with a comment explaining the next step)"
    - "Every PR classified as `hold` has a written reason in the inventory"
    - "No force-push to default branches (main/master/dev/DEV/minion-integration) occurred"
  artifacts:
    - path: "specs/clean-slate-inventory.md"
      provides: "§Open PRs section populated with classifications + §Resolution Log — 01-03 recording actions taken"
      contains: "## Open PRs, ## Resolution Log — 01-03"
  key_links:
    - from: "specs/clean-slate-inventory.md §Open PRs"
      to: "GitHub PR state (MERGED / CLOSED / OPEN)"
      via: "gh pr merge / gh pr close / gh pr comment"
      pattern: "Classification:"
---

<objective>
Triage every open PR across all 7 subproject GitHub repos. For each PR: classify as `merge` / `close` / `rebase` / `hold`, then after user approval, execute the merges and closes. Rebases and holds remain open with recorded rationale.

Purpose: CLEAN-04 — every PR is classified in `specs/clean-slate-inventory.md` with a documented outcome.

Output: Populated `## Open PRs` section in `specs/clean-slate-inventory.md` + Resolution Log of merged/closed PRs.

**Critical constraint:** Per CLAUDE.md and CONTEXT.md: never force-push to default branches. Never use `--no-verify`. Never delete default branches. PR merges use `gh pr merge` (squash/rebase/merge strategy per repo convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-clean-slate/01-CONTEXT.md
@specs/clean-slate-inventory.md
@CLAUDE.md

# Memory files (authoritative inputs — prior PR sweeps are the FOUNDATION, don't re-triage from zero):
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr11.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr17.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_ai_repo.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_paperclip_fork.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_agent_bot_github.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/feedback_bot_pr_patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch 7 parallel general-purpose subagents — one per subproject repo — to classify open PRs</name>
  <files>specs/clean-slate-inventory.md (appended in Task 2 with aggregated results)</files>
  <read_first>
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr11.md (prior classifications — use as INPUT, not re-triage from zero)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr17.md (second sweep, April 17)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md (local dir → GitHub repo mapping)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_ai_repo.md (minion → minion-ai; old minion repo is archived)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_paperclip_fork.md (paperclip-minion → NikolasP98/paperclip fork)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/feedback_bot_pr_patterns.md (bot-authored PR failure modes)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_agent_bot_github.md (bot account details)
    - specs/clean-slate-inventory.md (any notes recorded per subproject)
  </read_first>
  <action>
Dispatch EXACTLY 7 `general-purpose` subagents in a SINGLE message (one tool block with 7 Task tool calls) — per CONTEXT.md §Subagent architecture.

Each subagent's target repo (resolve local-dir → GitHub repo via reference_github_repo_names.md):

| # | Local dir | GitHub repo for `gh` calls |
|---|-----------|----------------------------|
| 1 | minion | `NikolasP98/minion-ai` (canonical; old `minion` repo is archived) |
| 2 | minion_hub | `NikolasP98/minion-hub` |
| 3 | minion_site | `NikolasP98/minion-site` |
| 4 | minion-shared | `NikolasP98/minion-shared` |
| 5 | minion_plugins | `NikolasP98/minion-plugins` |
| 6 | paperclip-minion | `NikolasP98/paperclip` (fork; NOT upstream) |
| 7 | pixel-agents | `NikolasP98/pixel-agents` (verify name with `gh repo view` if uncertain) |

Each subagent prompt MUST instruct it to:
1. Read the two prior PR sweep memory files (`project_pr_review_sweep_apr11.md`, `project_pr_review_sweep_apr17.md`) to understand what was previously classified for its repo — do NOT re-triage PRs that were already categorized there UNLESS the PR is still open and the hold reason may have expired.
2. Run `gh pr list --repo <repo> --state open --json number,title,author,baseRefName,headRefName,mergeable,isDraft,createdAt,updatedAt,labels,reviewDecision,statusCheckRollup --limit 100` to enumerate open PRs.
3. For each PR, classify into ONE of:
   - `merge` — ready to merge: CI green (`statusCheckRollup` all SUCCESS), approved or no review required, mergeable, targets default branch, PR author is trusted (human or approved bot), content matches the repo's purpose
   - `close` — dead: kitchen-sink bundling, wrong target branch, superseded by another PR, dead feature, author abandoned, OR matches a pattern from `feedback_bot_pr_patterns.md`
   - `rebase` — divergent but valuable: behind base branch by many commits or has merge conflicts, but the content is worth preserving
   - `hold` — uncertain: needs human decision, has unresolved review feedback, blocked by external dependency
4. Return a structured report in this EXACT format:
   ```
   ### <repo>
   Total open PRs: <count>
   PRs (one per line):
   - #<number> "<title>" by <author> — Classification: <merge|close|rebase|hold> — Rationale: <1 sentence> — CI: <all-green|failing|pending> — Prior-sweep: <apr11|apr17|none|stale-hold>
   ```
5. Do NOT execute any mutations (no `gh pr merge`, `gh pr close`, `gh pr review`). Classification only.
6. If the repo name resolves differently or returns 404, report that in the output so the main thread can fix the mapping before Task 2.

After all 7 subagents return, aggregate their reports.
  </action>
  <verify>
    <automated>gh auth status 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 7 subagent Task calls dispatched in a single message (verify by reviewing the agent's tool-call transcript — one block containing 7 Task invocations)
    - Each subagent returns a report starting with `### <repo>` where `<repo>` is one of the 7 expected repo names
    - Each report contains a `Total open PRs:` line
    - Every PR in each report has a `Classification:` field set to one of `merge | close | rebase | hold`
    - Every PR with classification `close` OR `hold` has a `Rationale:` with a specific reason (not blank, not "TBD", not "unsure")
    - No mutating `gh` commands (`gh pr merge`, `gh pr close`, `gh pr review --approve`, `gh pr comment`) appear in the Bash transcript
  </acceptance_criteria>
  <done>7 subagent PR classification reports received and saved for aggregation in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Append §Open PRs classification table to specs/clean-slate-inventory.md</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - The 7 subagent reports from Task 1 (in conversation context)
    - specs/clean-slate-inventory.md (to verify the `## Open PRs` section stub from Plan 01-01 still exists — if not, create it)
  </read_first>
  <action>
Use the Edit tool (or Write if replacing the whole file) to replace the `## Open PRs` STUB section in `specs/clean-slate-inventory.md` with the aggregated classification table.

Structure:

```markdown
## Open PRs

**Triage completed:** 2026-04-19 by Plan 01-03
**Method:** 7 parallel general-purpose subagents, one per repo. Built on prior sweeps `project_pr_review_sweep_apr11.md` + `project_pr_review_sweep_apr17.md`.

### Summary

| Repo | Open PRs | Merge | Close | Rebase | Hold |
|------|----------|-------|-------|--------|------|
| minion-ai | N | n | n | n | n |
| minion-hub | N | n | n | n | n |
| minion-site | N | n | n | n | n |
| minion-shared | N | n | n | n | n |
| minion-plugins | N | n | n | n | n |
| paperclip (fork) | N | n | n | n | n |
| pixel-agents | N | n | n | n | n |
| **TOTAL** | **N** | **n** | **n** | **n** | **n** |

### Per-repo breakdown

<paste aggregated subagent reports here — one `### <repo>` section each>

### Dispositions (to be filled by user before Task 3)

For each PR, append an inline tag: `APPROVED` to execute the classification, or `HOLD: <reason>` to skip.
Example:
  - #42 "Fix auth token refresh" by alice — Classification: merge — Rationale: CI green, approved, targets dev — APPROVED
  - #43 "Kitchen-sink refactor" by minion-agent-bot — Classification: close — Rationale: matches feedback_bot_pr_patterns kitchen-sink pattern — APPROVED
  - #44 "Experimental feature flag" by bob — Classification: hold — Rationale: blocked by upstream dep — (no tag needed, hold = no action)
```

Preserve ALL other sections of the inventory doc unchanged. Only replace the Open PRs stub.
  </action>
  <verify>
    <automated>grep -c '^### ' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md && grep -c 'Classification:' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## Open PRs` section in `specs/clean-slate-inventory.md` no longer contains the text `STUB — populated by Plan 01-03` (verify: `grep -q 'STUB — populated by Plan 01-03' specs/clean-slate-inventory.md` returns non-zero)
    - The Open PRs section contains a Summary table with one row per repo (verify: `grep -c '| minion-\|| paperclip\|| pixel-agents' specs/clean-slate-inventory.md` returns ≥ 7)
    - Every open PR from the 7 subagent reports is present in the doc with a `Classification:` tag (verify: the count of `Classification:` lines matches the sum of the Total rows)
    - Other sections (`## Subprojects`, `## Stray Worktrees`, `## Root Artifacts`, `## Broken Upstreams`) remain intact (verify: `grep -c '^## ' specs/clean-slate-inventory.md` still returns 5)
  </acceptance_criteria>
  <done>Open PRs section fully populated; user can now tag dispositions inline before Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — user tags dispositions on each PR classification</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - specs/clean-slate-inventory.md §Open PRs (the classifications populated in Task 2)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/feedback_bot_pr_patterns.md (bot PR patterns — double-check bot-authored PRs)
  </read_first>
  <action>
Present the `## Open PRs` section of `specs/clean-slate-inventory.md` to the user and pause. The executor must: (1) print the Summary table with per-repo counts, (2) highlight any PR authored by `minion-agent-bot` so the user can cross-reference with `feedback_bot_pr_patterns.md`, (3) list any PR whose base branch is a default branch (main/master/dev/DEV/minion-integration) for extra scrutiny, (4) block on the user's response. Do NOT call `gh pr merge`, `gh pr close`, `gh pr comment`, `gh pr review`, or any other mutating gh command during this task. The user's job: append `APPROVED` or `HOLD: <reason>` to each merge/close classification line in the inventory.
  </action>
  <verify>
    <automated>grep -q '## Open PRs' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md && echo "Open PRs section present"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms approval (resume-signal answered) before Task 4 executes
    - Every PR classified `merge` or `close` has an inline disposition tag (`APPROVED` or `HOLD: <reason>`) in `specs/clean-slate-inventory.md` at the moment Task 4 starts
    - No mutating `gh` command was run during this checkpoint (verify Bash transcript)
  </acceptance_criteria>
  <done>User's dispositions captured on every merge/close classification; Task 4 knows what to execute.</done>
  <what-built>
    `specs/clean-slate-inventory.md §Open PRs` is now populated with:
    - A Summary table of counts per repo
    - Per-repo breakdown with every open PR and its classification (merge/close/rebase/hold)
    - Rationales from the subagents

    Nothing has been mutated on GitHub yet. No PRs merged. No PRs closed.
  </what-built>
  <how-to-verify>
    1. Open `specs/clean-slate-inventory.md` in your editor and scroll to `## Open PRs`.
    2. Review the Summary table — does the PR count per repo match your mental model?
    3. For each PR classified `merge` or `close`, review the Rationale. If you agree, append `APPROVED` to that line. If not, either append `HOLD: <reason>` or change the classification.
    4. PRs classified `rebase` do not need tags — they stay open and Task 4 posts a comment noting the rebase recommendation.
    5. PRs classified `hold` do not need tags — they stay open and Task 4 posts a comment with the hold reason.
    6. Pay special attention to any PR authored by `minion-agent-bot` — cross-reference with `feedback_bot_pr_patterns.md` to make sure the classification is correct.
    7. If any PR is a force-push candidate or touches a default branch (main/master/dev/DEV), flag it manually — the executor will NOT force-push default branches under any circumstance.
  </how-to-verify>
  <resume-signal>Type "approved" after tagging every merge/close PR with APPROVED or HOLD. Or describe revisions needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute APPROVED merges + closes; post comments on rebase/hold PRs</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - specs/clean-slate-inventory.md §Open PRs (parse inline APPROVED / HOLD tags)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr11.md (repo-specific merge strategies if documented)
  </read_first>
  <action>
Parse the Open PRs section from `specs/clean-slate-inventory.md`. Build four execution lists:

1. **MERGE** — PRs tagged APPROVED with classification=merge
2. **CLOSE** — PRs tagged APPROVED with classification=close
3. **REBASE-COMMENT** — PRs with classification=rebase (comment only; rebase happens later if user wants)
4. **HOLD-COMMENT** — PRs with classification=hold (comment the hold reason so the author knows why the PR is parked)

If any PR classification is `merge` or `close` but has NO tag (neither APPROVED nor HOLD), halt and request user tag them.

**For each MERGE item:**
1. Verify PR is still mergeable: `gh pr view <num> --repo <repo> --json mergeable,state`
2. If state != OPEN or mergeable != MERGEABLE, skip and log "skipped — state changed"
3. Determine merge strategy from repo convention (default: squash unless repo's prior merges show otherwise — check with `gh pr list --repo <repo> --state merged --limit 5 --json mergeCommit,number`)
4. `gh pr merge <num> --repo <repo> --squash --delete-branch` (or --merge / --rebase per strategy)
5. Verify: `gh pr view <num> --repo <repo> --json state` returns `MERGED`
6. NEVER use `--admin` flag to bypass checks
7. NEVER merge a PR targeting the default branch if it has failing CI (gh will refuse anyway, but do not try to bypass)
8. Append to Resolution Log: `- MERGE <repo>#<num>: <title>`

**For each CLOSE item:**
1. `gh pr close <num> --repo <repo> --comment "Closed during Phase 1 clean-slate triage. Rationale: <rationale from inventory>. Phase plan: .planning/phases/01-clean-slate/01-03-PLAN.md."`
2. Verify: `gh pr view <num> --repo <repo> --json state` returns `CLOSED`
3. Append to Resolution Log: `- CLOSE <repo>#<num>: <title> (reason: <short>)`

**For each REBASE-COMMENT item:**
1. `gh pr comment <num> --repo <repo> --body "Flagged for rebase during Phase 1 clean-slate triage. Rationale: <rationale>. Leaving PR open; rebase onto <base-branch> at your convenience, then re-run CI. Tracked in .planning/phases/01-clean-slate/01-03-PLAN.md."`
2. Append to Resolution Log: `- REBASE-COMMENT <repo>#<num>: <title>`

**For each HOLD-COMMENT item:**
1. `gh pr comment <num> --repo <repo> --body "Held during Phase 1 clean-slate triage. Reason: <rationale>. Will revisit when <condition>. Tracked in .planning/phases/01-clean-slate/01-03-PLAN.md."`
2. Append to Resolution Log: `- HOLD-COMMENT <repo>#<num>: <title>`

Append a `## Resolution Log — 01-03` section to `specs/clean-slate-inventory.md` with all entries.

**Hard rules (never violate):**
- NEVER use `gh pr merge --admin`
- NEVER push directly to main/master/dev/DEV/minion-integration on any repo
- NEVER force-push to default branches
- If ANY gh command fails with auth error, halt — do not retry with different credentials
  </action>
  <verify>
    <automated>grep -c '^- \(MERGE\|CLOSE\|REBASE-COMMENT\|HOLD-COMMENT\) ' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - Every PR in the MERGE list is now in state MERGED on GitHub (verify: `gh pr view <num> --repo <repo> --json state -q '.state'` returns `MERGED` for each)
    - Every PR in the CLOSE list is now in state CLOSED (verify: same `gh pr view` check returns `CLOSED`)
    - Every PR in the REBASE-COMMENT and HOLD-COMMENT lists has a new comment posted (verify: `gh pr view <num> --repo <repo> --json comments -q '.comments[-1].body'` contains the phrase "Phase 1 clean-slate triage")
    - A `## Resolution Log — 01-03` section exists in `specs/clean-slate-inventory.md` with one line per executed action (verify: `grep -c '^## Resolution Log — 01-03' specs/clean-slate-inventory.md` returns 1)
    - No `gh pr merge --admin`, no direct pushes to default branches, no `--force` flags (verify by reviewing Bash tool transcript)
    - No open PR with tag APPROVED remains without a corresponding Resolution Log entry (verify by counting: `grep -c 'APPROVED$' specs/clean-slate-inventory.md` ≤ `grep -c '^- \(MERGE\|CLOSE\) ' specs/clean-slate-inventory.md`)
  </acceptance_criteria>
  <done>All APPROVED merges/closes executed; all rebase/hold PRs have tracking comments; Resolution Log records every action.</done>
</task>

</tasks>

<verification>
  Plan 01-03 is complete when:
  - `specs/clean-slate-inventory.md §Open PRs` contains a classification for every open PR across all 7 repos
  - `specs/clean-slate-inventory.md §Resolution Log — 01-03` records every merge/close/comment action
  - No default branches were force-pushed
  - No `--admin` bypasses were used

  Commands:
  - `grep -c '^### ' specs/clean-slate-inventory.md` returns at least 14 (7 subproject sections + 7 per-repo PR sections)
  - `grep -c 'Classification:' specs/clean-slate-inventory.md` ≥ the total PR count
  - `grep -c '^- \(MERGE\|CLOSE\|REBASE-COMMENT\|HOLD-COMMENT\)' specs/clean-slate-inventory.md` matches the confirmed-action count
</verification>

<success_criteria>
  - CLEAN-04 satisfied: every open PR across all 7 subproject repos is classified in `specs/clean-slate-inventory.md`
  - Merges and closes for APPROVED items executed cleanly
  - Rebase and hold PRs have tracking comments
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate/01-03-SUMMARY.md` documenting:
- Total PRs triaged (per repo + total)
- Counts of merged / closed / rebase-comment / hold-comment actions
- Any PRs where auth or CI state blocked execution (with follow-up notes)
- Link to updated inventory doc
</output>
