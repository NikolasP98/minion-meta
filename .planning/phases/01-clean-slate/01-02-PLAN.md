---
phase: 01-clean-slate
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - specs/clean-slate-inventory.md
  # Plus touching each subproject's working tree via git commands (commits go to subproject repos, not the meta-repo)
autonomous: false
requirements:
  - CLEAN-01
  - CLEAN-03
must_haves:
  truths:
    - "Every subproject's `git status --porcelain` returns zero lines (working tree clean) OR has an explicit HOLD disposition recorded in specs/clean-slate-inventory.md"
    - "No stray worktree paths exist at /home/nikolas/Documents/CODE/AI/paperclip-pi-fallback/ or /home/nikolas/Documents/CODE/AI/paperclip-meta-repair/ (unless a HOLD disposition was recorded in the inventory)"
    - "Every executed mutation has a corresponding APPROVED disposition in specs/clean-slate-inventory.md"
    - "Every skipped mutation has a HOLD disposition with a written reason in specs/clean-slate-inventory.md"
    - "No `discard` (destructive) operation runs without a checkpoint:decision captured in this plan's execution log"
  artifacts:
    - path: "specs/clean-slate-inventory.md"
      provides: "Updated inventory with Resolution Log section recording what was committed/stashed/discarded/left"
      contains: "## Resolution Log — 01-02"
  key_links:
    - from: "specs/clean-slate-inventory.md §Recommendations"
      to: "Each subproject's .git working tree"
      via: "git commit / git stash / git worktree remove, gated on APPROVED disposition"
      pattern: "APPROVED"
---

<objective>
Resolve the dirty working trees and stray worktrees recorded in `specs/clean-slate-inventory.md`. Every mutation is gated on an `APPROVED` disposition tag in the inventory — items tagged `HOLD` or missing a tag are skipped with a logged reason. Destructive operations (`discard`, `worktree remove --force`) pause for an in-plan `checkpoint:decision` before running.

Purpose: Bring every subproject to `git status` clean (CLEAN-01) and remove abandoned worktrees (CLEAN-03).

Output: Committed/stashed dirty trees; removed (or merged-back) stray worktrees; Resolution Log appended to `specs/clean-slate-inventory.md` documenting what happened per item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-clean-slate/01-CONTEXT.md
@specs/clean-slate-inventory.md
@CLAUDE.md

# Memory files (authoritative inputs for how to handle known drift):
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_docs_restructure_pending.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/feedback_symlink_flip_loop_bug.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_paperclip_pi_openrouter_fallback.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse dispositions from inventory and build executable work lists</name>
  <files>specs/clean-slate-inventory.md (read-only in this task — parses dispositions into scratch work lists)</files>
  <read_first>
    - specs/clean-slate-inventory.md (every section, every `Recommendation:` line — look for the inline disposition tag `APPROVED`, `HOLD: <reason>`, or `MODIFY: <plan>`)
    - .planning/phases/01-clean-slate/01-CONTEXT.md §Implementation Decisions (resolution categories, no-destructive-defaults rule)
  </read_first>
  <action>
Parse `specs/clean-slate-inventory.md` and categorize every `Recommendation:` line into four work lists:

1. **COMMIT list** — items tagged APPROVED whose recommendation is `commit`. Record: subproject path, file paths, proposed commit message.
2. **STASH list** — items tagged APPROVED whose recommendation is `stash`. Record: subproject path, file paths, stash name.
3. **WORKTREE list** — items tagged APPROVED in the Stray Worktrees section. Record: worktree path, action (remove / merge-back), branch name.
4. **DISCARD list** — items tagged APPROVED whose recommendation is `discard`. These are DESTRUCTIVE — they go to Task 3 (checkpoint:decision).
5. **HOLD list** — items tagged HOLD or missing a tag. Record: subproject, recommendation, reason. These are skipped and logged.

If ANY item in the inventory lacks an explicit disposition tag (i.e., has a `Recommendation:` but no `APPROVED`/`HOLD`/`MODIFY` inline next to it or immediately after), halt and tell the user: "Plan 01-01 checkpoint was not completed — <N> recommendations lack dispositions. Please tag them and re-run." Do not proceed.

If parse succeeds, print the four work lists to chat for verification BEFORE Task 2 runs.
  </action>
  <verify>
    <automated>grep -c 'Recommendation:' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - Every `Recommendation:` line in the inventory doc is categorized into one of the 5 lists (COMMIT / STASH / WORKTREE / DISCARD / HOLD)
    - Zero `Recommendation:` lines are uncategorized
    - If any untagged recommendations exist, the task halts with a clear message (verify by checking that the four work lists are only printed when ALL recommendations are tagged)
    - The COMMIT, STASH, WORKTREE, DISCARD, HOLD lists are printed to the agent transcript before Task 2 executes
  </acceptance_criteria>
  <done>Four work lists exist in conversation context and are ready for sequential execution in Tasks 2–4.</done>
</task>

<task type="auto">
  <name>Task 2: Execute APPROVED commit + stash mutations (non-destructive)</name>
  <files>
    # Each COMMIT item mutates its own subproject's .git — not the meta-repo.
    # Each STASH item writes to the subproject's stash log.
    specs/clean-slate-inventory.md
  </files>
  <read_first>
    - The COMMIT, STASH work lists from Task 1 (in conversation context)
    - specs/clean-slate-inventory.md (to append Resolution Log entries after each mutation)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_docs_restructure_pending.md (IF docs/ is on either list — it has ~130 deletions + untracked, handle carefully)
  </read_first>
  <action>
For each item in the COMMIT list:
1. `cd` into the subproject directory (use `git -C <path>` — NEVER change the shell cwd).
2. Stage specific files listed in the recommendation: `git -C <path> add <file1> <file2> ...` (NEVER `git add .` or `git add -A` — per CLAUDE.md safety protocol).
3. Commit with the message from the recommendation: `git -C <path> commit -m "<message>"`.
4. Verify: `git -C <path> log -1 --format='%h %s'` — capture the sha and subject.
5. Append a line to `specs/clean-slate-inventory.md` under a new `## Resolution Log — 01-02` section:
   `- COMMIT <subproject>: <sha> <subject> (files: <count>)`

For each item in the STASH list:
1. `git -C <path> stash push -m "<stash-name>" -- <file1> <file2> ...` (partial stash with message — path-limited).
2. Verify: `git -C <path> stash list | head -1` — should show the new stash at top.
3. Append to the Resolution Log: `- STASH <subproject>: <stash-name> (files: <count>)`

If any commit or stash fails (e.g., pre-commit hook rejects, merge conflict), STOP execution and report the failure. Do NOT force or bypass hooks. The user will decide whether to fix the underlying issue or move the item to HOLD.

After all items processed, run `git -C <path> status --porcelain` for each touched subproject and verify it returns zero lines.

SPECIAL HANDLING for `docs/` if it appears in the lists: Per memory `project_docs_restructure_pending.md`, docs/ has ~130 deleted files + many untracked dirs from an agent registry restructure that was never committed. If the recommendation is `commit`, the commit will be large. Split into logical chunks ONLY if the inventory's recommendation explicitly says so; otherwise do one commit with the message from the recommendation.
  </action>
  <verify>
    <automated>for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do echo "=== $d ==="; git -C /home/nikolas/Documents/CODE/AI/$d status --porcelain 2>&1 | head -5; done</automated>
  </verify>
  <acceptance_criteria>
    - Every subproject in the COMMIT list produces a new commit (verify: `git -C <path> log -1 --format='%h'` differs from the HEAD sha recorded in the inventory's pre-mutation state)
    - Every subproject in the STASH list has a new stash entry (verify: `git -C <path> stash list` contains the stash name from the recommendation)
    - No subproject on these lists has any remaining uncommitted/unstashed tracked changes (verify: `git -C <path> status --porcelain` returns empty for files that were on the lists — untracked files that were NOT on the lists are allowed)
    - A `## Resolution Log — 01-02` section exists in `specs/clean-slate-inventory.md` with one line per processed item (verify: `grep -c '^- COMMIT\|^- STASH' specs/clean-slate-inventory.md` equals COMMIT-count + STASH-count)
    - NO `git add .`, `git add -A`, `git commit -a`, `git commit --amend`, or `git commit --no-verify` was used (verify by reviewing the Bash tool transcript — only path-limited `git add` and plain `git commit -m` are allowed)
  </acceptance_criteria>
  <done>All APPROVED commits and stashes executed; Resolution Log updated; no destructive operations performed yet.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Checkpoint — approve discard operations before Task 4 executes</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - DISCARD and WORKTREE work lists from Task 1 (in conversation context)
    - specs/clean-slate-inventory.md (for cross-reference of each DISCARD item's file paths)
    - .planning/phases/01-clean-slate/01-CONTEXT.md §Implementation Decisions (no-destructive-defaults rule)
  </read_first>
  <action>
Present the DISCARD work list (from Task 1) to the user for per-item confirmation. For each DISCARD item, display:
  - Subproject
  - Files to be discarded (specific paths, not globs)
  - Recommendation rationale from the inventory
  - Reminder that discard is irreversible (no stash/backup unless one was approved in Task 2's work list)

Block until the user answers with one of the four options (`confirm-all` / `confirm-some: <items>` / `hold-all` / `skip`). Do NOT execute `git checkout --`, `git clean`, `git reset`, or any destructive command during this task.

If the DISCARD list is empty (Task 1 produced zero discard items), announce that and offer `skip` as the only option.
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - User response captured before Task 4 starts
    - No `git checkout --`, `git clean -f`, `git reset --hard`, `rm` was executed in this task (verify Bash transcript)
    - The user's decision (`confirm-all` / `confirm-some` / `hold-all` / `skip`) is recorded in conversation context for Task 4 to consume
  </acceptance_criteria>
  <done>User decision received and recorded; Task 4 knows which DISCARD items to execute vs skip.</done>
  <decision>Confirm each DISCARD list item one by one before any destructive git operation runs.</decision>
  <context>
    The inventory flagged one or more items with `Recommendation: discard` + `APPROVED` tag. Per CONTEXT.md §No destructive defaults, this plan MUST pause for per-item human confirmation before running `git checkout -- <file>`, `git clean -fd`, or similar destructive commands. Even though the inventory has an `APPROVED` tag, the design spec explicitly requires a second human confirmation at mutation time.

    If the DISCARD list is empty, answer `skip` and Task 4 will no-op.
  </context>
  <options>
    <option id="confirm-all">
      <name>Confirm all DISCARD items — execute as listed</name>
      <pros>Moves the phase forward. Inventory dispositions were already reviewed.</pros>
      <cons>Destructive. Irreversible without the stash/backup the recommendation should have offered. Re-check each item's file list one more time before answering.</cons>
    </option>
    <option id="confirm-some">
      <name>Confirm SOME items — specify which ones to execute, hold the rest</name>
      <pros>Surgical. Any item you are unsure about stays in HOLD.</pros>
      <cons>Requires you to list subproject+file for each approval.</cons>
    </option>
    <option id="hold-all">
      <name>Hold all DISCARD items — re-tag them as HOLD in the inventory, skip Task 4</name>
      <pros>Nothing destructive happens. You can revisit later.</pros>
      <cons>Dirty trees remain dirty; CLEAN-01 not satisfied for those items until a later pass.</cons>
    </option>
    <option id="skip">
      <name>DISCARD list is empty — skip Task 4</name>
      <pros>No-op. Task 4 does nothing.</pros>
      <cons>None.</cons>
    </option>
  </options>
  <resume-signal>Answer with one of: `confirm-all`, `confirm-some: <subproject>/<file>, <subproject>/<file>...`, `hold-all`, or `skip`.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute confirmed DISCARD operations + process WORKTREE list</name>
  <files>
    specs/clean-slate-inventory.md
  </files>
  <read_first>
    - DISCARD list from Task 1 (filtered by the confirmation from Task 3 — only items the user confirmed execute)
    - WORKTREE list from Task 1
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_paperclip_pi_openrouter_fallback.md (paperclip-pi-fallback context — determine if any unique work exists before removing)
    - specs/clean-slate-inventory.md (Stray Worktrees section)
  </read_first>
  <action>
**DISCARD operations (only items confirmed in Task 3):**
For each confirmed discard:
1. `git -C <path> checkout -- <specific-file>` (per-file checkout — NEVER `git checkout -- .`)
2. For untracked files: `git -C <path> clean -f <specific-file>` (per-file clean — NEVER `git clean -fd`)
3. Verify: `git -C <path> status --porcelain <file>` returns empty
4. Append to Resolution Log: `- DISCARD <subproject>/<file> (confirmed Task 3)`

**WORKTREE processing:**
For each item in the WORKTREE list:

If action is `merge-back`:
1. Inspect the worktree's unique commits: `git -C <worktree-path> log <mainline>..HEAD --oneline`
2. Record the commits in the Resolution Log.
3. Halt and request user guidance: "Worktree <path> has N unique commits. Cherry-pick / merge / rebase into which branch? <branches available>." Do NOT merge without an explicit target branch from the user.

If action is `remove`:
1. Verify no unique commits exist: `git -C <worktree-path> log <mainline>..HEAD --oneline` should return empty (if non-empty, switch to merge-back flow even if the inventory said remove — unique work must not be silently destroyed).
2. `git -C <parent-repo> worktree remove <worktree-path>` (plain remove, NO --force unless the user explicitly approves in chat).
3. If the worktree directory still exists after `worktree remove` (e.g., untracked files blocked removal), halt and ask the user whether to force-remove.
4. Verify: `test -d <worktree-path>` returns non-zero (directory gone).
5. Append to Resolution Log: `- WORKTREE removed <worktree-path>`

**Known worktree targets (from memory + CONTEXT.md):**
- `/home/nikolas/Documents/CODE/AI/paperclip-pi-fallback/` → check for unique commits; merge-back to paperclip-minion minion-integration branch OR remove if redundant
- `/home/nikolas/Documents/CODE/AI/paperclip-meta-repair/` → check for unique commits; likely remove

After all processing, verify the overall state:
- Run `git -C <path> status --porcelain` for every subproject that was touched — expect clean for all APPROVED items
- Run `test -d /home/nikolas/Documents/CODE/AI/paperclip-pi-fallback` and `test -d /home/nikolas/Documents/CODE/AI/paperclip-meta-repair` — expect non-zero (gone) unless user chose hold/merge-back
  </action>
  <verify>
    <automated>test ! -d /home/nikolas/Documents/CODE/AI/paperclip-pi-fallback && test ! -d /home/nikolas/Documents/CODE/AI/paperclip-meta-repair && echo "both stray worktrees removed"</automated>
  </verify>
  <acceptance_criteria>
    - Every DISCARD item confirmed in Task 3 is no longer in its subproject's `git status --porcelain` (verify: `git -C <path> status --porcelain <file>` empty for each)
    - Every WORKTREE list item with action=remove is gone from disk (verify: `test -d <worktree-path>` returns non-zero) UNLESS the user explicitly held it
    - No `git worktree remove --force`, `git clean -fd`, `git checkout -- .`, or `git reset --hard` was run without explicit per-item user approval in chat (verify by reviewing Bash transcript)
    - Every executed action has a line in the Resolution Log (verify: `grep -cE '^- (DISCARD|WORKTREE)' specs/clean-slate-inventory.md` matches execution count)
    - At phase-level: every subproject whose recommendations were fully APPROVED returns `git status --porcelain` empty (verify with `for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do git -C /home/nikolas/Documents/CODE/AI/$d status --porcelain | head -1; done`)
  </acceptance_criteria>
  <done>All destructive operations executed with per-item confirmation; stray worktrees cleaned up or merged back per user direction; Resolution Log is complete.</done>
</task>

</tasks>

<verification>
  Plan 01-02 is complete when:
  - `specs/clean-slate-inventory.md` has a `## Resolution Log — 01-02` section with one line per processed recommendation
  - Every subproject whose items were fully APPROVED returns `git status --porcelain` empty
  - `paperclip-pi-fallback/` and `paperclip-meta-repair/` directories are gone from disk (or explicitly held)
  - No force / no-verify / no-gpg-sign / reset-hard operations were used without explicit per-item user approval

  Commands:
  - `grep -c '^## Resolution Log — 01-02' specs/clean-slate-inventory.md` returns 1
  - `grep -cE '^- (COMMIT|STASH|DISCARD|WORKTREE|HOLD)' specs/clean-slate-inventory.md` matches the inventory's recommendation count
  - `for d in minion minion_hub minion_site minion-shared minion_plugins paperclip-minion pixel-agents; do test -z "$(git -C /home/nikolas/Documents/CODE/AI/$d status --porcelain)" && echo "$d clean" || echo "$d DIRTY"; done` — every subproject prints "clean" unless it had a HOLD disposition
</verification>

<success_criteria>
  - CLEAN-01 satisfied (or partial with HOLD items documented): every subproject with fully-APPROVED recommendations is clean
  - CLEAN-03 satisfied (or partial with HOLD items documented): stray worktrees are either removed or merged back per user direction
  - Resolution Log in inventory doc provides an audit trail of every mutation
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate/01-02-SUMMARY.md` documenting:
- Count of COMMIT / STASH / DISCARD / WORKTREE / HOLD items processed
- List of subprojects now in clean state vs those with HOLD items
- Any worktrees removed (with their former paths)
- Any worktrees merged back (with target branch)
- Link to the updated inventory doc
</output>
