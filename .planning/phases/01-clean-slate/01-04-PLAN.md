---
phase: 01-clean-slate
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - specs/clean-slate-inventory.md
  # Plus relocations of root-level files out of /home/nikolas/Documents/CODE/AI/ to /home/nikolas/Documents/CODE/AI/ai-studio/retention/ (or deletion where approved)
autonomous: false
requirements:
  - CLEAN-05
must_haves:
  truths:
    - "Root directory /home/nikolas/Documents/CODE/AI/ contains only meta-repo files (PROJECT-level configs, CLAUDE.md, README.md, .env, mascot.png, .git, .planning, specs, packages/, .dmux-hooks, agents/), symlinks (docs/, ai-studio/, DOCS/), and the 7 subproject directories"
    - "No A3_*.md, RETENTION_*.md, KPI_*.md, 00_START_HERE.md, VISUAL_SUMMARY_A3.txt, README_A3_INVESTIGATION.md, RESEARCH_OUTPUT_INDEX.md, ASSUMPTION_A3_VERDICT.md remain at the root (verify: `ls /home/nikolas/Documents/CODE/AI/A3_* 2>/dev/null | wc -l` returns 0)"
    - "Every file moved has a tracked destination recorded in specs/clean-slate-inventory.md Resolution Log"
    - "Every file deleted has explicit per-file approval recorded in the Resolution Log (discards are NEVER silent)"
  artifacts:
    - path: "specs/clean-slate-inventory.md"
      provides: "§Resolution Log — 01-04 recording each file relocation or deletion"
      contains: "## Resolution Log — 01-04"
    - path: "ai-studio/retention/ (or the relocation target chosen per file)"
      provides: "Target home for relocated research artifacts (if relocate was approved for that file)"
  key_links:
    - from: "/home/nikolas/Documents/CODE/AI/<file>"
      to: "/home/nikolas/Documents/CODE/AI/ai-studio/retention/<file> (for relocations)"
      via: "mv <src> <dst>"
      pattern: "RELOCATE "
---

<objective>
Remove one-time research artifacts from the meta-repo root, leaving only meta-repo files + symlinks + 7 subproject directories. Each file is either relocated to `ai-studio/retention/` (or another approved destination) or deleted, per the disposition in `specs/clean-slate-inventory.md §Root Artifacts`.

Purpose: CLEAN-05 — root directory contains only meta-repo-relevant files.

Output: Clean root directory + Resolution Log in the inventory doc recording every move/delete.

**Critical constraint:** `ai-studio/` is a symlink to `~/Documents/VAULT/MINION/strategy/` (or similar, per memory `project_vault_minion_doc_structure.md`). Moving files INTO `ai-studio/` actually moves them into the VAULT. This is deliberate and fine — but the executor must verify the symlink target is writable before relocating.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-clean-slate/01-CONTEXT.md
@specs/clean-slate-inventory.md
@CLAUDE.md

# Memory files (context for where these artifacts came from):
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_vault_minion_doc_structure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse root-artifact dispositions from inventory + verify symlink targets</name>
  <files>specs/clean-slate-inventory.md (read-only in this task)</files>
  <read_first>
    - specs/clean-slate-inventory.md §Root Artifacts (every `Recommendation: relocate to <target> | delete` line + its inline disposition tag)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_vault_minion_doc_structure.md (ai-studio symlink target + VAULT structure)
  </read_first>
  <action>
1. Parse `specs/clean-slate-inventory.md §Root Artifacts`. For each line matching `- <filename>: Recommendation: ...`, extract:
   - Filename
   - Action (relocate / delete)
   - Target path (if relocate)
   - Disposition tag (APPROVED / HOLD / MODIFY)

2. Build two work lists:
   - **RELOCATE list**: filename + destination path, for every APPROVED relocate item
   - **DELETE list**: filename, for every APPROVED delete item

3. If any root-artifact line lacks a disposition tag, halt and ask the user to tag it. Do not proceed.

4. For each RELOCATE destination:
   - Resolve the destination if it goes through a symlink (`readlink -f <dest>`)
   - Verify the real directory exists and is writable (`test -w <real-path>`)
   - If the destination directory doesn't exist, create it with `mkdir -p <dest>` (the relocate target might be `ai-studio/retention/` which may need creation)
   - Record the real path next to the work list entry

5. Print both work lists to chat for sanity-check before Task 2 executes.

6. Also run `ls -1 /home/nikolas/Documents/CODE/AI/` and cross-check that EVERY file matching the known patterns (A3_*.md, RETENTION_*.md, KPI_*.md, plus 00_START_HERE.md, VISUAL_SUMMARY_A3.txt, README_A3_INVESTIGATION.md, RESEARCH_OUTPUT_INDEX.md, ASSUMPTION_A3_VERDICT.md) appears in one of the two lists. If any file is unaccounted for, halt and report it — the inventory is incomplete.
  </action>
  <verify>
    <automated>ls -1 /home/nikolas/Documents/CODE/AI/ | grep -E '^(A3_|RETENTION_|KPI_|00_START_HERE|VISUAL_SUMMARY_A3|README_A3|RESEARCH_OUTPUT_INDEX|ASSUMPTION_A3)' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Every file at `/home/nikolas/Documents/CODE/AI/` matching `A3_*`, `RETENTION_*`, `KPI_*`, `00_START_HERE*`, `VISUAL_SUMMARY_A3*`, `README_A3_*`, `RESEARCH_OUTPUT_INDEX*`, `ASSUMPTION_A3_*` is either on the RELOCATE or DELETE list, OR has a HOLD disposition tagged in the inventory
    - RELOCATE destinations resolved via `readlink -f` all point to writable directories
    - The RELOCATE and DELETE lists are printed to the agent transcript
  </acceptance_criteria>
  <done>Work lists built and destinations verified; ready for mutation in Task 2 (relocate) and Task 3 (delete with checkpoint).</done>
</task>

<task type="auto">
  <name>Task 2: Execute APPROVED relocations (non-destructive — move to target paths)</name>
  <files>
    specs/clean-slate-inventory.md
    # Source: /home/nikolas/Documents/CODE/AI/<filename>
    # Destination: <target path from inventory, typically ai-studio/retention/<filename>>
  </files>
  <read_first>
    - RELOCATE work list from Task 1 (in conversation context)
    - specs/clean-slate-inventory.md (for Resolution Log appending)
  </read_first>
  <action>
For each item in the RELOCATE list:

1. Source path: `/home/nikolas/Documents/CODE/AI/<filename>` (absolute)
2. Destination path: as recorded in the inventory (typically `/home/nikolas/Documents/CODE/AI/ai-studio/retention/<filename>`)
3. Pre-check: destination file must NOT already exist (if it does, compare sha256 — if identical, remove source and log as "already-at-destination"; if different, halt and ask user which to keep)
4. Execute: `mv /home/nikolas/Documents/CODE/AI/<filename> <destination>` (use absolute paths; do not change shell cwd)
5. Verify:
   - `test -f <destination>` returns 0 (file now exists at target)
   - `test ! -e /home/nikolas/Documents/CODE/AI/<filename>` returns 0 (source is gone)
6. Append to `specs/clean-slate-inventory.md` under `## Resolution Log — 01-04`:
   `- RELOCATE <filename>: /home/nikolas/Documents/CODE/AI/ → <destination>`

Do NOT batch-move with `mv *.md <dest>` — always name individual files so an unexpected match (e.g., an accidentally-similar-named important file) isn't caught.

Do NOT use `rsync -a --remove-source-files` or any tool that could lose data on crash — use `mv` which is atomic within a filesystem.

If source and destination are on different filesystems (e.g., `ai-studio` symlinks to `~/Documents/VAULT/MINION/...` which may be a different mount), `mv` degrades to copy+delete. That's fine, but verify the destination sha256 matches the source before deleting the source.
  </action>
  <verify>
    <automated>grep -c '^- RELOCATE ' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - For every RELOCATE list item: `test ! -e /home/nikolas/Documents/CODE/AI/<filename>` returns 0 (source is gone from root)
    - For every RELOCATE list item: `test -f <destination>` returns 0 (file exists at target)
    - One `- RELOCATE` log line per executed move exists in `specs/clean-slate-inventory.md` under `## Resolution Log — 01-04`
    - No file was overwritten without an explicit decision (verify: no mv used `-f` flag unless explicitly logged as "overwrite approved")
    - NO files on the DELETE list were touched in this task (Task 3 handles deletes, gated by checkpoint)
  </acceptance_criteria>
  <done>All APPROVED relocations executed; files are now at their target paths; Resolution Log records each move.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Checkpoint — confirm each DELETE item before destruction</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - DELETE list from Task 1 (in conversation context)
    - specs/clean-slate-inventory.md §Root Artifacts (cross-reference each DELETE filename)
  </read_first>
  <action>
Present the DELETE list to the user with per-file detail: filename, file size, last-modified date (use `ls -lh /home/nikolas/Documents/CODE/AI/<filename>`), and the recommendation rationale from the inventory. Offer the four confirmation options (confirm-all / confirm-some / relocate-instead / skip). Block on the user's response. Do NOT execute any `rm`, `mv`, `unlink`, `rmdir`, or `git clean` during this task.
  </action>
  <verify>
    <automated>for f in /home/nikolas/Documents/CODE/AI/A3_*.md /home/nikolas/Documents/CODE/AI/RETENTION_*.md /home/nikolas/Documents/CODE/AI/KPI_*.md 2>/dev/null; do test -f "$f" && ls -lh "$f"; done 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - User response captured (one of `confirm-all`, `confirm-some: <filenames>`, `relocate-instead`, `skip`)
    - No `rm` or destructive command executed during this task (verify Bash transcript — only read-only `ls`/`stat` calls allowed)
    - The user's decision is stored in conversation context for Task 4 consumption
  </acceptance_criteria>
  <done>User's DELETE decision is recorded and Task 4 knows which files to remove vs move-to-tmp vs skip.</done>
  <decision>Confirm each file on the DELETE list before running `rm`.</decision>
  <context>
    Task 1 built a DELETE list from `specs/clean-slate-inventory.md §Root Artifacts` items tagged APPROVED with recommendation `delete`. Per CONTEXT.md §No destructive defaults, a second confirmation is required at mutation time before any file is permanently removed.

    If the DELETE list is empty (all items were relocate), answer `skip`.
  </context>
  <options>
    <option id="confirm-all">
      <name>Confirm all DELETE items — rm each one</name>
      <pros>Clean root directory. Every item was APPROVED in the inventory.</pros>
      <cons>Irreversible. Files are not in a git history (they were untracked research artifacts at meta-repo root). Consider relocating instead if you are unsure.</cons>
    </option>
    <option id="confirm-some">
      <name>Confirm SOME items — list them explicitly</name>
      <pros>Surgical. Unsure items are held.</pros>
      <cons>Requires listing filenames to delete.</cons>
    </option>
    <option id="relocate-instead">
      <name>Relocate all DELETE items to /tmp/AI-deleted-<date>/ as a backup, then continue</name>
      <pros>Safety net in case you change your mind. /tmp is volatile but survives this session.</pros>
      <cons>Adds a relocate step. Not as clean as rm.</cons>
    </option>
    <option id="skip">
      <name>DELETE list is empty — skip Task 4</name>
      <pros>No-op.</pros>
      <cons>None.</cons>
    </option>
  </options>
  <resume-signal>Answer: `confirm-all`, `confirm-some: <filename>, <filename>...`, `relocate-instead`, or `skip`.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute confirmed deletions</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - DELETE list from Task 1, filtered by the confirmation from Task 3
    - specs/clean-slate-inventory.md (Resolution Log section)
  </read_first>
  <action>
For each file confirmed for deletion in Task 3:

Option A — if user answered `confirm-all` or `confirm-some`:
1. `rm /home/nikolas/Documents/CODE/AI/<filename>` (absolute path, one file at a time — NEVER `rm *.md` or `rm -rf`)
2. Verify: `test ! -e /home/nikolas/Documents/CODE/AI/<filename>` returns 0
3. Append to Resolution Log: `- DELETE <filename> (confirmed Task 3)`

Option B — if user answered `relocate-instead`:
1. `mkdir -p /tmp/AI-deleted-2026-04-19/`
2. `mv /home/nikolas/Documents/CODE/AI/<filename> /tmp/AI-deleted-2026-04-19/<filename>`
3. Verify source gone, destination present
4. Append to Resolution Log: `- RELOCATE-TO-TMP <filename>: → /tmp/AI-deleted-2026-04-19/ (user chose safety-net backup)`

Option C — if user answered `skip`:
1. No-op. Log: `- DELETE list: skipped by user choice`

After execution, run final verification:
- `ls -1 /home/nikolas/Documents/CODE/AI/ | grep -cE '^(A3_|RETENTION_|KPI_|00_START_HERE|VISUAL_SUMMARY_A3|README_A3|RESEARCH_OUTPUT_INDEX|ASSUMPTION_A3)'`
  - Expected: 0 if all approved + executed
  - If > 0, list the remaining files and verify they have HOLD dispositions in the inventory

Record the final count in the Resolution Log.
  </action>
  <verify>
    <automated>ls -1 /home/nikolas/Documents/CODE/AI/ | grep -cE '^(A3_|RETENTION_|KPI_|00_START_HERE|VISUAL_SUMMARY_A3|README_A3|RESEARCH_OUTPUT_INDEX|ASSUMPTION_A3)'</automated>
  </verify>
  <acceptance_criteria>
    - Every file confirmed in Task 3 is gone from `/home/nikolas/Documents/CODE/AI/` (verify: `test ! -e /home/nikolas/Documents/CODE/AI/<filename>` for each confirmed filename)
    - If option `relocate-instead` was chosen, `/tmp/AI-deleted-2026-04-19/` contains the moved files (verify: `ls /tmp/AI-deleted-2026-04-19/` lists them)
    - No file was removed that was NOT on the Task 3 confirmation list (verify by reviewing Bash transcript — every `rm` call targets a filename from the confirmation)
    - No `rm -rf`, `rm -r`, `rm *`, or glob-based rm was used (verify by reviewing Bash transcript — every `rm` names a single file)
    - The Resolution Log has one line per DELETE or RELOCATE-TO-TMP action
    - Final `ls /home/nikolas/Documents/CODE/AI/` shows only: meta-repo files (package.json, CLAUDE.md, README.md, mascot.png, .env, .env.local, .gitignore, .git, .planning, specs, packages if created, .dmux-hooks, agents, minion.json if created), symlinks (docs, ai-studio, DOCS), and the 7 subproject dirs — plus any HOLD items explicitly noted
  </acceptance_criteria>
  <done>Root directory contains only meta-repo-relevant files + symlinks + subproject dirs; Resolution Log provides an audit trail.</done>
</task>

</tasks>

<verification>
  Plan 01-04 is complete when:
  - No root-level research artifact (A3/RETENTION/KPI/etc.) remains at `/home/nikolas/Documents/CODE/AI/` (except explicit HOLD items)
  - `specs/clean-slate-inventory.md §Resolution Log — 01-04` records every relocation, deletion, or held item

  Commands:
  - `ls -1 /home/nikolas/Documents/CODE/AI/ | grep -cE '^(A3_|RETENTION_|KPI_|00_START_HERE|VISUAL_SUMMARY_A3|README_A3|RESEARCH_OUTPUT_INDEX|ASSUMPTION_A3)'` returns 0 (or the count of HOLD items)
  - `grep -c '^## Resolution Log — 01-04' specs/clean-slate-inventory.md` returns 1
</verification>

<success_criteria>
  - CLEAN-05 satisfied: root directory contains only meta-repo-relevant files + symlinks + 7 subproject dirs
  - Every root-artifact file has a documented outcome (relocated / deleted / held)
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate/01-04-SUMMARY.md` documenting:
- Count of relocated / deleted / held files
- Destinations for relocated files
- Final `ls` of the root directory
- Link to updated inventory doc
</output>
