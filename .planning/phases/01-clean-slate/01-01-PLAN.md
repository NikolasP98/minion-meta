---
phase: 01-clean-slate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/clean-slate-inventory.md
autonomous: false
requirements:
  - CLEAN-06
must_haves:
  truths:
    - "specs/clean-slate-inventory.md exists at the meta-repo root"
    - "The inventory doc contains a section for each of the 7 subprojects (minion, minion_hub, minion_site, minion-shared, minion_plugins, paperclip-minion, pixel-agents)"
    - "Each subproject section records: current branch, upstream tracking (including [gone] markers), ahead/behind counts, dirty file counts (modified/staged/untracked), worktree list, open-PR classifications"
    - "The inventory doc surfaces the known drift targets: docs/ ~130 deletions + untracked, paperclip-pi-fallback/, paperclip-meta-repair/, minion_plugins main→origin/master [gone]"
    - "The inventory doc's Root Artifacts section enumerates every A3_*.md / RETENTION_*.md / KPI_*.md / 00_START_HERE.md / VISUAL_SUMMARY_A3.txt / README_A3_INVESTIGATION.md / RESEARCH_OUTPUT_INDEX.md / ASSUMPTION_A3_VERDICT.md present at root with a recommendation (relocate to ai-studio/retention/ vs delete)"
    - "No mutations to any subproject occur in this plan (read-only audit)"
  artifacts:
    - path: "specs/clean-slate-inventory.md"
      provides: "Pre-mutation state snapshot + recommendations for all 7 subprojects"
      contains: "### minion, ### minion_hub, ### minion_site, ### minion-shared, ### minion_plugins, ### paperclip-minion, ### pixel-agents, ### Root Artifacts, ### Stray Worktrees"
  key_links:
    - from: "specs/clean-slate-inventory.md"
      to: "Plan 01-02, 01-03, 01-04, 01-05"
      via: "Each Wave-2 plan consumes a specific section of the inventory as its work list"
      pattern: "Recommendation:"
---

<objective>
Produce `specs/clean-slate-inventory.md` — a single, durable audit doc capturing the pre-mutation state of every subproject in `AI/`. This is a READ-ONLY phase. No mutations occur. The inventory is the input to all downstream Phase 1 plans (01-02, 01-03, 01-04, 01-05) and must be reviewed by the user before any mutation runs.

Purpose: Phase 1's foundational artifact. Satisfies CLEAN-06. Subsequent plans classify each inventory item as "approved to execute" vs "hold for human decision" — this plan provides the raw material.

Output: `specs/clean-slate-inventory.md` with sections per subproject (branch, upstream, dirty files, worktrees, PRs) plus Root Artifacts and Stray Worktrees sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-clean-slate/01-CONTEXT.md
@specs/2026-04-19-minion-meta-repo-design.md
@CLAUDE.md

# Memory files (canonical inputs for known-drift targets — executor MUST read these before dispatching subagents):
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_docs_restructure_pending.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_minion_plugins_broken_tracking.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_ai_repo.md
@/home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_paperclip_fork.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch 7 parallel Explore subagents — one per subproject</name>
  <files>specs/clean-slate-inventory.md (created by aggregating subagent reports)</files>
  <read_first>
    - .planning/phases/01-clean-slate/01-CONTEXT.md (subproject list, branch conventions, canonical references)
    - CLAUDE.md (Project Map table — branches and GitHub remote names)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_github_repo_names.md (local dir → GitHub repo name mapping)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_minion_ai_repo.md (minion → minion-ai canonical)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/reference_paperclip_fork.md (paperclip-minion → NikolasP98/paperclip fork)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_docs_restructure_pending.md (known docs/ drift)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_minion_plugins_broken_tracking.md (minion_plugins broken upstream)
  </read_first>
  <action>
Use the superpowers `dispatching-parallel-agents` pattern to dispatch EXACTLY 7 `Explore` subagents in a SINGLE message (one tool block with 7 Task tool calls). Do NOT dispatch sequentially.

Subagent targets (one per subproject):
1. `/home/nikolas/Documents/CODE/AI/minion/` (expected branch: DEV)
2. `/home/nikolas/Documents/CODE/AI/minion_hub/` (expected branch: dev)
3. `/home/nikolas/Documents/CODE/AI/minion_site/` (expected branch: master)
4. `/home/nikolas/Documents/CODE/AI/minion-shared/` (branch: unknown, capture it)
5. `/home/nikolas/Documents/CODE/AI/minion_plugins/` (expected branch: main — KNOWN to track origin/master [gone])
6. `/home/nikolas/Documents/CODE/AI/paperclip-minion/` (expected branch: minion-integration)
7. `/home/nikolas/Documents/CODE/AI/pixel-agents/` (expected branch: main)

Each subagent prompt MUST instruct the subagent to:
- Run `git -C <path> status --porcelain=v1 -b` to get branch + modified/staged/untracked file counts
- Run `git -C <path> rev-parse --abbrev-ref --symbolic-full-name @{u}` to get upstream (capture stderr for "[gone]" indicators)
- Run `git -C <path> rev-list --left-right --count HEAD...@{u}` for ahead/behind counts (skip gracefully if upstream missing)
- Run `git -C <path> worktree list` to list worktrees
- Run `git -C <path> log -1 --format='%h %s'` for HEAD commit
- Return a ≤200-word structured report in this EXACT format:
  ```
  ### <subproject_name>
  - Path: <absolute path>
  - Branch: <branch name>
  - Upstream: <upstream or "[GONE]" or "MISSING">
  - Ahead/Behind: <N ahead, M behind> (or "N/A — no upstream")
  - Dirty: <modified=X, staged=Y, untracked=Z>
  - Worktrees: <list or "none extra">
  - HEAD: <sha> <subject>
  - Notable: <any specifically surprising state — e.g., detached HEAD, merge in progress, rebase in progress>
  ```
- Do NOT run any mutating git commands (no add/commit/stash/checkout/reset/discard)
- Do NOT run `gh pr list` — that is Plan 01-03's job
- Return the report as the final message

After all 7 return, aggregate into `specs/clean-slate-inventory.md` with the template given in Task 2. This task is COMPLETE when all 7 subagent responses have been received.
  </action>
  <verify>
    <automated>test -n "$(ls /home/nikolas/Documents/CODE/AI/minion 2>/dev/null)" && test -n "$(ls /home/nikolas/Documents/CODE/AI/paperclip-minion 2>/dev/null)" && echo "subproject dirs exist — subagents have targets"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 subagent Task calls dispatched in a SINGLE message (verify by reviewing agent-run transcript: 7 `Task` invocations in one block)
    - Each subagent report contains the literal header `### <subproject_name>` matching one of: minion, minion_hub, minion_site, minion-shared, minion_plugins, paperclip-minion, pixel-agents
    - Each report contains the keys `Branch:`, `Upstream:`, `Ahead/Behind:`, `Dirty:`, `Worktrees:`, `HEAD:`, `Notable:`
    - At least one report surfaces an `[GONE]` upstream indicator (expected: minion_plugins)
    - At least one report surfaces a non-trivial `Dirty:` count (expected: the docs/ symlink target or similar, depending on where the drift currently lives)
  </acceptance_criteria>
  <done>All 7 Explore subagent reports received and saved into a scratch buffer ready for aggregation in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Aggregate subagent reports + root artifacts scan into specs/clean-slate-inventory.md</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - The 7 subagent reports from Task 1 (in conversation context)
    - specs/2026-04-19-minion-meta-repo-design.md §M0 (for recommendations format)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr11.md (prior PR sweep context)
    - /home/nikolas/.claude/projects/-home-nikolas-Documents-CODE-AI/memory/project_pr_review_sweep_apr17.md (prior PR sweep context)
  </read_first>
  <action>
Create `specs/clean-slate-inventory.md` using the Write tool (NOT heredoc). Use this EXACT structure:

```markdown
# Clean Slate Inventory

**Generated:** 2026-04-19
**Phase:** 01-clean-slate
**Status:** Pre-mutation audit — REVIEW BEFORE MUTATIONS
**Plan:** 01-01
**Authority:** `specs/2026-04-19-minion-meta-repo-design.md` §M0

## How to review this doc

Plans 01-02 through 01-05 consume specific sections:
- 01-02 reads: Dirty Trees + Stray Worktrees
- 01-03 reads: Open PRs (populated later by Plan 01-03 itself; this plan leaves a stub)
- 01-04 reads: Root Artifacts
- 01-05 reads: Broken Upstreams

For every recommendation labeled `Recommendation:` below, flag it with one of these dispositions before any Wave-2 plan executes:
- `APPROVED` — executor may proceed
- `HOLD: <reason>` — executor must skip and log
- `MODIFY: <new plan>` — rewrite the recommendation

## Subprojects

<paste aggregated reports from Task 1 here, in this order: minion, minion_hub, minion_site, minion-shared, minion_plugins, paperclip-minion, pixel-agents>

For each subproject, append a `Recommendations` subsection that classifies each dirty item / worktree / broken-upstream as one of:
  - commit (with proposed commit message)
  - stash (with proposed stash name)
  - discard (MUST be gated on explicit user approval per CONTEXT.md §Implementation Decisions)
  - leave-in-place (user will resume later)
  - fix-upstream (for broken upstreams — target `<remote>/<new-branch>`)
  - remove-worktree (for abandoned worktrees)
  - merge-worktree (for worktrees with unique commits)

## Stray Worktrees (cross-subproject view)

List every worktree that is NOT the canonical subproject root. Known at time of writing:
- /home/nikolas/Documents/CODE/AI/paperclip-pi-fallback/ (associated with paperclip-minion — see memory: project_paperclip_pi_openrouter_fallback.md — check if unique commits remain)
- /home/nikolas/Documents/CODE/AI/paperclip-meta-repair/ (associated with paperclip-minion — check if unique commits remain)

For each, record: HEAD sha, branch name, unique commits vs mainline (`git -C <worktree> log mainline..HEAD`), and a recommendation (remove / merge-back / keep).

## Root Artifacts

Run `ls -1 /home/nikolas/Documents/CODE/AI/` and enumerate all top-level files. For each of the following, verify presence and record a recommendation:

Expected candidates (from CONTEXT.md and design spec §M0.4):
- 00_START_HERE.md
- A3_*.md (glob all)
- RETENTION_*.md (glob all)
- KPI_*.md (glob all)
- VISUAL_SUMMARY_A3.txt
- README_A3_INVESTIGATION.md
- RESEARCH_OUTPUT_INDEX.md
- ASSUMPTION_A3_VERDICT.md

For EACH file present, write a line in this exact format:
  `- <filename>: Recommendation: relocate to <target path> | delete (rationale: <1-sentence>)`

Do NOT touch these files. This is an audit only.

## Broken Upstreams

Pull from subproject sections above. Known target:
- minion_plugins: main tracks origin/master [GONE] — Recommendation: `git branch --set-upstream-to=origin/main main` (verify origin/main exists first via `git -C minion_plugins ls-remote --heads origin main`)

## Open PRs

STUB — populated by Plan 01-03 (`gh pr list` across 7 repos, with triage classifications).
Plan 01-03 will append a `### Open PRs` section below this line.
```

Write this file. Do NOT use heredoc. Use the Write tool so the file state is tracked.
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md && grep -c '^### ' /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `specs/clean-slate-inventory.md` exists (test -f returns 0)
    - File contains the literal headings `## Subprojects`, `## Stray Worktrees`, `## Root Artifacts`, `## Broken Upstreams`, `## Open PRs` (verify: `grep -c '^## ' specs/clean-slate-inventory.md` returns 5)
    - File contains `### ` subheadings for EACH of the 7 subprojects (verify: `grep -cE '^### (minion|minion_hub|minion_site|minion-shared|minion_plugins|paperclip-minion|pixel-agents)$' specs/clean-slate-inventory.md` returns 7)
    - File contains at least one `Recommendation:` line (verify: `grep -c 'Recommendation:' specs/clean-slate-inventory.md` returns ≥ 1)
    - File contains the `STUB` marker in the Open PRs section (verify: `grep -q 'STUB — populated by Plan 01-03' specs/clean-slate-inventory.md`)
    - NO mutating git commands were run against any subproject during this plan (verify by reviewing Bash tool transcript — no `git add`, `git commit`, `git stash`, `git checkout`, `git reset` outside read-only flags)
  </acceptance_criteria>
  <done>
    `specs/clean-slate-inventory.md` written with all 5 required sections, 7 subproject entries, root artifacts enumerated with recommendations, and Open PRs stub in place. Ready for user review before Wave-2 plans execute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — user reviews inventory before Wave 2 mutations</name>
  <files>specs/clean-slate-inventory.md</files>
  <read_first>
    - specs/clean-slate-inventory.md (the file you just produced in Task 2)
  </read_first>
  <action>
Present `specs/clean-slate-inventory.md` to the user and pause for review. Do NOT proceed to any Wave-2 plan until the user responds. The user's job during this checkpoint is to add inline disposition tags (`APPROVED` / `HOLD: <reason>` / `MODIFY: <plan>`) to every `Recommendation:` line in the inventory. The executor's job is to: (1) display the current `specs/clean-slate-inventory.md` contents, (2) summarize counts per section (subproject count, total Recommendations, known-drift targets surfaced, stray worktrees found, root artifacts flagged, broken upstreams found), (3) block on the user's response. Do NOT run any git mutations, `gh pr merge`, `rm`, `mv`, or `git branch --set-upstream-to` during this task.
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/specs/clean-slate-inventory.md && echo "inventory present and ready for review"</automated>
  </verify>
  <acceptance_criteria>
    - User has responded with `approved` (or an equivalent confirmation) before the agent proceeds to Wave 2
    - Every `Recommendation:` line in the inventory has an inline disposition tag after the user's review (verify at Wave-2 entry: `grep -c 'Recommendation:' specs/clean-slate-inventory.md` equals `grep -cE 'APPROVED|HOLD:|MODIFY:' specs/clean-slate-inventory.md`)
    - No mutations occurred during this checkpoint (verify by reviewing Bash transcript — no `git commit`, `git stash`, `gh pr merge`, `rm`, `mv` calls)
  </acceptance_criteria>
  <done>User has confirmed approval (resume-signal answered) and the inventory has disposition tags on every Recommendation line.</done>
  <what-built>
    `specs/clean-slate-inventory.md` — the pre-mutation audit snapshot for all 7 subprojects. Every subproject section has a branch/upstream/dirty/worktree status and a list of `Recommendation:` lines. Root artifacts are enumerated with relocate/delete recommendations. Broken upstreams are listed with fix proposals. The Open PRs section is a stub that Plan 01-03 fills.
  </what-built>
  <how-to-verify>
    1. Open `specs/clean-slate-inventory.md` in your editor.
    2. For each subproject section, confirm the `Dirty:`, `Worktrees:`, and `Upstream:` values match your mental model. If any value looks wrong, note it — the executor will re-run the subagent for that subproject.
    3. For every `Recommendation:` line in the doc, add a disposition tag inline:
       - `APPROVED` — the Wave-2 plan may execute this recommendation
       - `HOLD: <reason>` — skip, log the reason
       - `MODIFY: <new plan>` — rewrite the recommendation
    4. Pay special attention to any `discard` recommendation — these destroy work. Only approve if you are certain.
    5. Confirm the Stray Worktrees section lists `paperclip-pi-fallback/` and `paperclip-meta-repair/` if they are still present.
    6. Confirm the Root Artifacts section enumerates the A3/RETENTION/KPI research files with plausible relocation targets (likely `ai-studio/retention/` per prior memory).
    7. Confirm the Broken Upstreams section lists `minion_plugins main → origin/master [GONE]` with a fix-upstream recommendation.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2 (Plans 01-02, 01-03, 01-04, 01-05). Or describe changes needed and the planner will revise Plan 01-01 before Wave 2 starts.</resume-signal>
</task>

</tasks>

<verification>
  Plan 01-01 is complete when `specs/clean-slate-inventory.md` exists with all 5 required sections, 7 subproject reports aggregated with Recommendation lines, root artifacts enumerated, broken upstreams documented, and the Open PRs stub present. No subproject is mutated in this plan.

  Verify with:
  - `test -f specs/clean-slate-inventory.md`
  - `grep -c '^## ' specs/clean-slate-inventory.md` returns 5
  - `grep -cE '^### (minion|minion_hub|minion_site|minion-shared|minion_plugins|paperclip-minion|pixel-agents)$' specs/clean-slate-inventory.md` returns 7
  - No new git commits or stashes appear in any subproject (verify with `git -C <path> stash list` and `git -C <path> log -1` — unchanged from pre-plan state).
</verification>

<success_criteria>
  - specs/clean-slate-inventory.md exists and is reviewable
  - User has reviewed the inventory and tagged dispositions on each Recommendation line before Wave 2 starts
  - CLEAN-06 (pre-mutation state captured for auditability) is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate/01-01-SUMMARY.md` documenting:
- Count of subprojects audited (expect: 7)
- Number of dirty trees found (extract from inventory doc)
- Number of broken upstreams (expect: ≥ 1 for minion_plugins)
- Number of stray worktrees (expect: ≥ 2 for paperclip-pi-fallback, paperclip-meta-repair)
- Number of root artifacts flagged for relocate/delete
- Link to specs/clean-slate-inventory.md
</output>
