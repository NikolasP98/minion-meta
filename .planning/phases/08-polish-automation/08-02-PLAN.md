---
phase: 08-polish-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .github/workflows/release.yml
  - .planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md
autonomous: true
requirements:
  - POLISH-02

must_haves:
  truths:
    - "A push to main with staged changesets opens a Version Packages PR automatically"
    - "Merging the Version Packages PR publishes bumped @minion-stack/* packages to npm"
    - "The release workflow uses the automation-type NPM_TOKEN GitHub secret"
    - "The release workflow uses changesets/action@v1.7.0"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Changesets-driven release automation"
      contains: "changesets/action@v1.7.0"
    - path: ".planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md"
      provides: "Manual setup steps for NPM_TOKEN GitHub secret"
      contains: "automation"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "secrets.NPM_TOKEN"
      via: "env.NPM_TOKEN"
      pattern: "NPM_TOKEN: \\$\\{\\{ secrets.NPM_TOKEN \\}\\}"
    - from: ".github/workflows/release.yml"
      to: "package.json release:version + release:publish"
      via: "changesets/action inputs"
      pattern: "version: pnpm run release:version"
---

<objective>
Stand up changesets release automation so that merges to `main` automatically publish bumped `@minion-stack/*` packages to npm.

Purpose: POLISH-02 — Eliminate the "forgot to publish / forgot to bump" class of error. Previous phases required manual `cd packages/<pkg> && npm publish --access public` with 2FA prompts (captured in STATE.md blockers). This automates that.

Output:
1. `.github/workflows/release.yml` that uses `changesets/action@v1.7.0`.
2. A short setup doc listing the manual one-time steps (generate npm automation token, set `NPM_TOKEN` GitHub secret, enable Actions write permissions). These steps cannot be automated by Claude.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-automation/08-RESEARCH.md

# From plan 08-01 — root scripts including `release:version` and `release:publish` must exist
@package.json

# Changesets config (correct — access: public, baseBranch: main)
@.changeset/config.json
</context>

<interfaces>
<!-- Existing root scripts (from plan 08-01) needed by release.yml -->

```json
"release:version": "changeset version",
"release:publish": "changeset publish",
"build-all": "pnpm -r run build"
```

<!-- Changeset config already correct (no edits needed) -->

```json
{
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch"
}
```

<!-- Staged changeset present -->
`.changeset/ws-consolidation-0.3.0.md` — will be consumed on first release run.

<!-- GitHub repo: NikolasP98/minion-meta (verified via git remote -v in research) -->
</interfaces>

<user_setup>
  - service: npm (for @minion-stack scope)
    why: "Release workflow publishes packages to npm on merge to main"
    env_vars:
      - name: NPM_TOKEN
        source: "https://www.npmjs.com/settings/NikolasP98/tokens → Generate New Token → Classic Token → type: Automation (bypasses 2FA for publish)"
    dashboard_config:
      - task: "Create NPM_TOKEN GitHub repo secret"
        location: "https://github.com/NikolasP98/minion-meta/settings/secrets/actions → New repository secret → Name: NPM_TOKEN → Value: <paste token>"
      - task: "Enable Actions write permissions for Version Packages PRs"
        location: "https://github.com/NikolasP98/minion-meta/settings/actions → Workflow permissions → Select: Read and write permissions → Check: Allow GitHub Actions to create and approve pull requests"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Write NPM_TOKEN-SETUP.md manual-steps doc</name>
  <files>.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md</files>
  <read_first>
    - /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/08-RESEARCH.md (Common Pitfalls #1, #3)
  </read_first>
  <action>
Create `.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md` with this EXACT content:

```markdown
# Release Workflow — One-Time Manual Setup

The `.github/workflows/release.yml` workflow requires two manual steps that Claude cannot automate. Complete these BEFORE merging the first PR to `main` after plan 08-02 lands, otherwise the release job will fail.

## 1. Generate npm automation token

1. Visit https://www.npmjs.com/settings/NikolasP98/tokens
2. Click **Generate New Token** → **Classic Token**
3. Select type: **Automation** (this is critical — automation tokens bypass 2FA for publish, which is required because the `@minion-stack` scope has 2FA enforced)
4. Scope: leave default (publish to all packages the user owns)
5. Copy the token immediately — you cannot view it again

## 2. Add NPM_TOKEN as a GitHub repo secret

1. Visit https://github.com/NikolasP98/minion-meta/settings/secrets/actions
2. Click **New repository secret**
3. Name: `NPM_TOKEN`
4. Value: paste the token from step 1
5. Click **Add secret**

## 3. Enable Actions write permissions

The `changesets/action` needs to open the "Version Packages" PR, which requires write permissions.

1. Visit https://github.com/NikolasP98/minion-meta/settings/actions
2. Scroll to **Workflow permissions**
3. Select **Read and write permissions**
4. Check **Allow GitHub Actions to create and approve pull requests**
5. Click **Save**

## 4. Verify

After merging plan 08-02 to main with at least one staged changeset in `.changeset/*.md`:

- Within ~1 min, a PR titled `chore: version packages` should appear at https://github.com/NikolasP98/minion-meta/pulls
- Merging that PR should trigger the publish step; verify with `npm view @minion-stack/shared version` showing the bumped version within ~2 min of merge.

## Rollback

If the release workflow fails:
- Check the Actions log for the specific error
- Common failures: `EOTP` (2FA — regenerate as automation token), `401` (token expired or scope wrong), `refusing to allow GitHub App` (Actions permissions not set)
- The Version Packages PR can be closed manually; no cleanup is required beyond that.

## Future: trusted publishing / OIDC

Classic `NPM_TOKEN` is the Phase 8 choice per D-01. npm now supports trusted publishing via OIDC for public packages (all `@minion-stack/*` are public). A future `REL-03` v2 requirement may migrate to `id-token: write` OIDC, eliminating token rotation. Not in Phase 8 scope.
```

This doc is the single source of truth for the human setup that 08-02 cannot automate. Link from the phase summary on completion.
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md && grep -c "automation" /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md</automated>
  </verify>
  <done>
    - `NPM_TOKEN-SETUP.md` exists in phase dir
    - `grep -q "Automation" NPM_TOKEN-SETUP.md` succeeds (token type)
    - `grep -q "github.com/NikolasP98/minion-meta/settings/secrets/actions" NPM_TOKEN-SETUP.md` succeeds
    - `grep -q "Read and write permissions" NPM_TOKEN-SETUP.md` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .github/workflows/release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/08-RESEARCH.md (Pattern 2 section)
    - /home/nikolas/Documents/CODE/AI/package.json (confirm release:version + release:publish scripts exist from plan 08-01)
    - /home/nikolas/Documents/CODE/AI/.changeset/config.json (confirm access: public, baseBranch: main)
  </read_first>
  <action>
Create `.github/workflows/release.yml` with this EXACT content:

```yaml
# .github/workflows/release.yml
# Release automation via changesets/action.
# On push to main:
#   - If .changeset/*.md files exist → opens/updates a "Version Packages" PR
#   - If no changesets remain (i.e. Version PR was merged) → runs changeset publish → npm
# Manual setup required: see .planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md
name: Release

on:
  push:
    branches: [main]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4.4.0
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm run build-all

      - name: Create Release PR or Publish
        uses: changesets/action@v1.7.0
        with:
          version: pnpm run release:version
          publish: pnpm run release:publish
          commit: "chore: release @minion-stack packages"
          title: "chore: version packages"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
```

Key decisions embedded:
- Per D-01: classic automation `NPM_TOKEN` (not OIDC). OIDC requires additional web-UI config and isn't needed for Phase 8.
- `cancel-in-progress: false` — do NOT cancel an in-flight publish (data integrity: two concurrent publishes can corrupt npm state).
- `fetch-depth: 0` — changesets needs full history to detect unreleased changes.
- `permissions` block is explicit — `contents: write` + `pull-requests: write` are required for changesets/action to open the Version Packages PR. No `id-token: write` because we use classic token per D-01.
- `registry-url: 'https://registry.npmjs.org'` — writes `.npmrc` with the correct registry and wires `NPM_TOKEN` as `_authToken`.
- `build-all` runs BEFORE the changesets step — the published artifacts are the freshly built `dist/` directories.
- Commit message pinned to `chore: release @minion-stack packages` for auditability.
- PR title pinned to `chore: version packages` (matches changesets default, kept explicit).

Do NOT add:
- A `matrix` (single Node 22 job is sufficient)
- Manual version bump logic (`changeset version` handles it)
- Deployment to anywhere other than npm (Netcup deploys are out of scope)
- OIDC `id-token: write` (deferred per D-01)
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/.github/workflows/release.yml && grep -c "changesets/action@v1.7.0" /home/nikolas/Documents/CODE/AI/.github/workflows/release.yml</automated>
  </verify>
  <done>
    - `.github/workflows/release.yml` exists
    - File contains literal strings: `changesets/action@v1.7.0`, `pnpm/action-setup@v4.4.0`, `NPM_TOKEN: ${{ secrets.NPM_TOKEN }}`, `fetch-depth: 0`, `pnpm run release:version`, `pnpm run release:publish`, `contents: write`, `pull-requests: write`, `cancel-in-progress: false`
    - No reference to `id-token: write` (OIDC not used per D-01)
    - No matrix strategy
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm registry | npm publish surface — token compromise = supply chain compromise |
| GitHub Secrets → Actions env | Well-established GitHub trust boundary; automatic log masking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | `NPM_TOKEN` in logs | mitigate | GitHub masks secrets in Actions logs automatically; `changesets/action` does not echo the token |
| T-08-02-02 | Elevation of Privilege | Leaked token → rogue publish | mitigate | Use automation-type token (narrower scope than user token); document token rotation in NPM_TOKEN-SETUP.md |
| T-08-02-03 | Tampering | Compromised `changesets/action` version | mitigate | Pin to `@v1.7.0` (minor version); do not float to `@v1` or `@main` |
| T-08-02-04 | Tampering | Malicious PR adds changeset to elevate version | accept | Single-maintainer repo; PRs are reviewed before merge; low threat |
| T-08-02-05 | Denial of Service | In-flight publish cancelled by re-push | mitigate | `cancel-in-progress: false` on release workflow |
</threat_model>

<verification>
Manual end-to-end verification (cannot automate — requires real npm publish):

1. Complete steps in `NPM_TOKEN-SETUP.md` (human action).
2. Merge this plan's PR to main.
3. Observe Actions tab — `Release` workflow should run within ~1 min.
4. Expected outcome: a new PR titled `chore: version packages` appears, bumping `@minion-stack/shared` to the version in `.changeset/ws-consolidation-0.3.0.md`.
5. Merge the Version Packages PR.
6. Release workflow runs again; `npm view @minion-stack/shared version` returns the new version within ~2 min.

If any step fails, consult `NPM_TOKEN-SETUP.md § Rollback`.
</verification>

<success_criteria>
- `.github/workflows/release.yml` exists using `changesets/action@v1.7.0`
- `NPM_TOKEN-SETUP.md` documents the three manual steps
- On first merge to main post-landing (with staged changeset), a Version Packages PR opens automatically
- On merge of the Version Packages PR, npm registry reflects the bumped version
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-automation/08-02-SUMMARY.md`.
</output>
