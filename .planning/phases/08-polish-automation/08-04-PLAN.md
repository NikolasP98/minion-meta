---
phase: 08-polish-automation
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - POLISH-04

must_haves:
  truths:
    - "Root CLAUDE.md no longer references minion-shared/ as a standalone directory"
    - "Root CLAUDE.md no longer calls Phases 4–6 'future' work"
    - "Root CLAUDE.md lists all seven @minion-stack packages in the Shared Packages table"
    - "Root CLAUDE.md keeps the full subproject tables (per D-04, audience unchanged)"
    - "README.md reflects current CI + release automation workflow"
    - "grep for 'minion-shared' in CLAUDE.md returns zero hits in Project Map and Build Quick Reference tables"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Steady-state meta-repo orchestrator guide"
      contains: "@minion-stack/shared"
    - path: "README.md"
      provides: "Onboarding quickstart + CI workflow reference"
      contains: "minion doctor"
  key_links:
    - from: "CLAUDE.md Shared packages table"
      to: "packages/shared, packages/db, packages/auth"
      via: "explicit table rows with package names"
      pattern: "@minion-stack/(shared|db|auth)"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "CI workflow reference"
      pattern: ".github/workflows/ci.yml"
---

<objective>
Rewrite stale content in root `CLAUDE.md` so it reflects the steady-state meta-repo (not the Phase 1–7 migration narrative). Update `README.md` to document the new CI + release automation story.

Per D-04 (locked): **Keep full subproject tables. Update stale content only, do not trim.** The audience is still the orchestrator agent that dispatches work across subprojects. Cross-Project Data Flow, Cross-Project Impact Zones, Orchestration Guide, and Key Conventions stay as-is.

**Stale strings to eliminate** (verified via grep):
- Line 12 (Project Map row): `minion-shared/` listed as a standalone directory — it was folded into `packages/shared` in Phase 4
- Line 62: "Future phases (M3+) add `@minion-stack/shared`, `@minion-stack/db`, `@minion-stack/auth`" — all three are published now
- Line 75 (Cross-Project Data Flow diagram): `minion-shared/` labelled as its own box — should reference `@minion-stack/shared`
- Line 91: "Types live in `minion-shared/`" — should be `@minion-stack/shared`
- Line 162: "### minion-shared/ — Protocol Types" section — content should merge into Architecture Overview > Shared Packages
- Line 239 (Commands Quick Reference): `minion-shared/` row — remove (no longer a subproject)
- Line 249, 255, 269: misc `minion-shared` mentions in orchestration text

Purpose: POLISH-04 — A future orchestrator reading CLAUDE.md sees the actual repo shape, not the migration snapshot.

Output: Updated `CLAUDE.md` (no `minion-shared/` as a standalone dir; all seven packages listed; no "future phases" language) and `README.md` (references the new CI/release workflows and `minion doctor` polish).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-automation/08-RESEARCH.md

# Files to rewrite
@CLAUDE.md
@README.md

# Truth sources for what's actually published
@packages/shared/package.json
@packages/db/package.json
@packages/auth/package.json
@minion.json
</context>

<interfaces>
<!-- Stale strings currently in CLAUDE.md (from grep on 2026-04-21) -->

```
12:| `minion-shared/` | Shared gateway protocol types + utils | npm, TypeScript | — | — |
62:Future phases (M3+) add `@minion-stack/shared`, `@minion-stack/db`, `@minion-stack/auth`.
75:minion-shared/          ← Protocol types (frames, agents, sessions, chat events)
91:All frontends connect to the gateway via WebSocket using a custom JSON frame protocol with three frame types: `req`, `res`, and `event`. Types live in `minion-shared/`.
162:### minion-shared/ — Protocol Types
239:| minion-shared/ | — | `npm run build` | — | — |
249: Relevant cross-project context (e.g., "this touches the WS protocol — changes must be reflected in minion-shared, hub, and site")
255:| Gateway protocol (frame types, events) | `minion-shared/` → `minion_hub/` → `minion_site/` |
269:- **Package managers**: pnpm for minion/ and paperclip-minion/. Bun for SvelteKit projects. npm for pixel-agents and minion-shared. Don't mix.
```

<!-- Current Shared packages table (line 56-60 area, needs extension) -->

```
| @minion-stack/cli       | The `minion` bin (this workflow's entrypoint) |
| @minion-stack/env       | 6-layer env resolver (wraps Infisical CLI)    |
| @minion-stack/tsconfig  | Base / node / svelte / library TS configs     |
| @minion-stack/lint-config | oxlint + flat-ESLint + Prettier presets     |
```

<!-- Must extend to include shared/db/auth -->

<!-- README.md line count: 138 (verified). Preserve overall structure; add CI automation callouts. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root CLAUDE.md — kill stale minion-shared references, add shared/db/auth</name>
  <files>CLAUDE.md</files>
  <read_first>
    - /home/nikolas/Documents/CODE/AI/CLAUDE.md (all 283 lines)
  </read_first>
  <action>
Apply these concrete edits to `CLAUDE.md`. Use the Edit tool or equivalent string replacement. Run the exact grep checks in the `<done>` block after to confirm every stale string is gone.

**Edit 1 — Project Map table (remove `minion-shared/` row).**

Delete the line:
```
| `minion-shared/` | Shared gateway protocol types + utils | npm, TypeScript | — | — |
```

Do NOT delete any other project map row. Keep all 8 remaining: `minion/`, `minion_hub/`, `minion_site/`, `minion_plugins/`, `docs/`, `paperclip-minion/`, `pixel-agents/`, `ai-studio/`.

**Edit 2 — Shared packages table (lines ~56-62).**

Extend the existing table. Replace the current block:

```markdown
| Package | Purpose |
|---|---|
| `@minion-stack/cli` | The `minion` bin (this workflow's entrypoint) |
| `@minion-stack/env` | 6-layer env resolver (wraps Infisical CLI) |
| `@minion-stack/tsconfig` | Base / node / svelte / library TS configs |
| `@minion-stack/lint-config` | oxlint + flat-ESLint + Prettier presets |

Future phases (M3+) add `@minion-stack/shared`, `@minion-stack/db`, `@minion-stack/auth`.
```

With:

```markdown
| Package | Purpose |
|---|---|
| `@minion-stack/cli` | The `minion` bin (this workflow's entrypoint) |
| `@minion-stack/env` | 6-layer env resolver (wraps Infisical CLI) |
| `@minion-stack/tsconfig` | Base / node / svelte / library TS configs |
| `@minion-stack/lint-config` | oxlint + flat-ESLint + Prettier presets |
| `@minion-stack/shared` | Gateway protocol types (frames, agents, sessions, chat events) + WS client + utils — consumed by hub, site, paperclip |
| `@minion-stack/db` | Canonical Drizzle schema (38 tables) + migration runner — consumed by hub + site |
| `@minion-stack/auth` | Better Auth `createAuth()` factory — consumed by hub + site with shared session continuity |

Releases are automated: merges to `main` with `.changeset/*.md` trigger a "Version Packages" PR via `changesets/action`; merging that PR publishes to npm.
```

**Edit 3 — Cross-Project Data Flow diagram.**

Replace the ASCII block around line 73-88 (starting with `minion-shared/          ← Protocol types`). The new block:

```
@minion-stack/shared     ← Protocol types (frames, agents, sessions, chat events) + WS client
  ├──→ minion_hub/       ← Imports types + WS utils for dashboard
  ├──→ minion_site/      ← Imports types + WS utils for members area
  └──→ paperclip-minion/ ← openclaw_gateway adapter consumes the shared WS client

minion/ (gateway)
  ├── WebSocket server  ←──→  minion_hub/ (dashboard connects via WS)
  ├── WebSocket server  ←──→  minion_site/ (members area connects via WS)
  ├── Channel extensions (telegram, discord, slack, etc.)
  └── REST API + CLI

minion_hub/ ←──shared DB──→ minion_site/
  (@minion-stack/db schema + @minion-stack/auth factory — identical config both sides)
```

**Edit 4 — Gateway Protocol section.**

Replace the sentence:
> All frontends connect to the gateway via WebSocket using a custom JSON frame protocol with three frame types: `req`, `res`, and `event`. Types live in `minion-shared/`.

With:
> All frontends connect to the gateway via WebSocket using a custom JSON frame protocol with three frame types: `req`, `res`, and `event`. Types and the WS client live in `@minion-stack/shared` (consumed by hub, site, and paperclip's `openclaw_gateway` adapter).

**Edit 5 — Delete the `### minion-shared/ — Protocol Types` section.**

Delete the entire section starting with the heading `### minion-shared/ — Protocol Types` (line ~162) through its body. The content is now captured in Edits 2 + 3 above.

**Edit 6 — Commands Quick Reference table.**

Delete the row:
```
| minion-shared/ | — | `npm run build` | — | — |
```

(`minion-shared/` no longer exists as a directory — commands flow through `pnpm -r` at the meta-repo root.)

**Edit 7 — Orchestration Guide + Cross-Project Impact Zones updates.**

In the "Relevant cross-project context" example, replace:
```
"this touches the WS protocol — changes must be reflected in minion-shared, hub, and site"
```
With:
```
"this touches the WS protocol — changes must be reflected in @minion-stack/shared, hub, site, and paperclip's openclaw_gateway adapter"
```

In the Cross-Project Impact Zones table, replace the row:
```
| Gateway protocol (frame types, events) | `minion-shared/` → `minion_hub/` → `minion_site/` |
```
With:
```
| Gateway protocol (frame types, events) | `packages/shared/` → `minion_hub/` + `minion_site/` + `paperclip-minion/` (openclaw_gateway adapter) |
```

**Edit 8 — Package managers list.**

Replace the sentence:
```
- **Package managers**: pnpm for minion/ and paperclip-minion/. Bun for SvelteKit projects. npm for pixel-agents and minion-shared. Don't mix.
```
With:
```
- **Package managers**: pnpm for the meta-repo root, `minion/`, and `paperclip-minion/`. Bun for SvelteKit projects (`minion_hub/`, `minion_site/`). npm for `pixel-agents/`. Don't mix within a subproject.
```

**Edit 9 — Add a new "CI & Release Automation" subsection under Meta-repo Workflow.**

After the existing "Env hierarchy" section and before the "Subprojects stay independent" section, add:

```markdown
### CI & Release Automation

The meta-repo ships two GitHub Actions workflows:

| Workflow | Trigger | Purpose |
|---|---|---|
| `.github/workflows/ci.yml` | PR to main, push to main | Runs `pnpm run build-all`, `typecheck-all`, `lint-all`, `test-all`, and `changeset:status` on every PR |
| `.github/workflows/release.yml` | Push to main | Uses `changesets/action@v1.7.0` to open a "Version Packages" PR when changesets are present; publishes `@minion-stack/*` to npm when that PR is merged |

Root scripts that fan out across workspace packages:

| Script | What |
|---|---|
| `pnpm run build-all` | `pnpm -r run build` (sequential — dependency-order) |
| `pnpm run typecheck-all` | `pnpm -r --parallel --if-present run typecheck` |
| `pnpm run lint-all` | `pnpm -r --parallel --if-present run lint` |
| `pnpm run test-all` | `pnpm -r --parallel --if-present run test` |
| `pnpm run ci` | build-all → typecheck-all → lint-all → test-all → changeset:status |
| `pnpm run changeset` | Interactive changeset authoring |

Release tokens and secrets: `NPM_TOKEN` (automation type) must be set as a GitHub repo secret — see `.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md` for one-time setup.
```

**After all edits, run the following grep to confirm staleness is gone (all must return 0):**

```bash
grep -n '^| `minion-shared/`' CLAUDE.md        # Project Map row — must be 0
grep -n 'Future phases' CLAUDE.md              # "future phases (M3+)" — must be 0
grep -n '^| minion-shared/' CLAUDE.md          # Commands table row — must be 0
grep -n '### minion-shared/ — Protocol Types' CLAUDE.md  # section heading — must be 0
grep -n 'Types live in `minion-shared/`' CLAUDE.md       # must be 0
```

Note: mentions of `minion-shared` in historical context (e.g., documenting the fold migration) are acceptable only if explicitly framed as past-tense history. If a reference is NOT past-tense, rewrite it. Contextual past-tense mentions are optional to preserve; the priority is eliminating any language that implies `minion-shared/` still exists.

**Keep intact (do NOT touch):**
- Entire "Subprojects stay independent" section
- Entire "Key Conventions" section except Edit 8
- Entire "Environment" section
- All subproject detail sections for `minion/`, `minion_hub/`, `minion_site/`, etc.
- "Deployment" table
- "Orchestration Guide" headings and preamble
  </action>
  <verify>
    <automated>cd /home/nikolas/Documents/CODE/AI && test $(grep -c '^| `minion-shared/`' CLAUDE.md) -eq 0 && test $(grep -c 'Future phases (M3' CLAUDE.md) -eq 0 && test $(grep -c '### minion-shared/ — Protocol Types' CLAUDE.md) -eq 0 && grep -q '@minion-stack/shared' CLAUDE.md && grep -q '@minion-stack/db' CLAUDE.md && grep -q '@minion-stack/auth' CLAUDE.md</automated>
  </verify>
  <done>
    - `grep -c '^| \`minion-shared/\`' CLAUDE.md` returns 0
    - `grep -c 'Future phases (M3' CLAUDE.md` returns 0
    - `grep -c '^| minion-shared/ | — | \`npm run build\`' CLAUDE.md` returns 0
    - `grep -c '### minion-shared/ — Protocol Types' CLAUDE.md` returns 0
    - `grep -c '@minion-stack/shared' CLAUDE.md` returns at least 2
    - `grep -c '@minion-stack/db' CLAUDE.md` returns at least 2
    - `grep -c '@minion-stack/auth' CLAUDE.md` returns at least 2
    - `grep -c 'CI & Release Automation' CLAUDE.md` returns 1 (new section present)
    - `grep -c 'changesets/action' CLAUDE.md` returns at least 1
    - Subproject sections for `minion/`, `minion_hub/`, `minion_site/`, `paperclip-minion/`, `pixel-agents/` all still present (count: 5 subsections minimum)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md to reflect Phase 8 automation</name>
  <files>README.md</files>
  <read_first>
    - /home/nikolas/Documents/CODE/AI/README.md (current 138 lines)
    - /home/nikolas/Documents/CODE/AI/.github/workflows/ci.yml (from plan 08-01, for reference)
    - /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md (from plan 08-02)
  </read_first>
  <action>
Update `README.md` so a new developer onboarding (POLISH-05 dry-run subject) sees accurate prerequisites and steady-state workflow. Concrete edits:

**Edit 1 — Ensure the file has a "CI & Releases" section near the top.**

Add (or update if present) a section after the Quickstart, before any deep dive:

```markdown
## CI & Releases

Meta-repo PRs are gated by `.github/workflows/ci.yml`:
- `pnpm install --frozen-lockfile`
- `pnpm run build-all` (sequential — respects workspace dependency order)
- `pnpm run typecheck-all`
- `pnpm run lint-all`
- `pnpm run test-all`
- `pnpm run changeset:status` (PR only — blocks PRs missing a changeset)

Releases are automated via `.github/workflows/release.yml` (changesets/action):
1. Land a feature PR with a staged `.changeset/*.md` file
2. On merge to main, a "Version Packages" PR opens automatically
3. On merge of that PR, `@minion-stack/*` packages publish to npm

One-time setup: see [`.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md`](.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md) for `NPM_TOKEN` + Actions permissions.
```

**Edit 2 — Update the `minion doctor` description in the Quickstart step.**

Find the `minion doctor` reference in the quickstart and extend the description to reflect Phase 8 polish. Example patch (match the actual surrounding text):

Replace any instance of:
```
minion doctor    # sanity check env + infisical + bin availability
```
With:
```
minion doctor    # env validation + Infisical auth + link drift + git status + clone-presence — one table
```

**Edit 3 — Update any stale reference to `minion-shared/` as a directory.**

Grep `README.md` for `minion-shared` and replace directory-style references with `@minion-stack/shared`. Past-tense historical mentions may stay only if explicitly labelled as historical.

**Edit 4 — Confirm Prerequisites section lists:**

The onboarding dry-run in plan 08-05 assumes these are pre-installed on the host. Ensure the README Prerequisites list includes (add any missing):
- `Node.js >= 22`
- `pnpm >= 10`
- `infisical` CLI (for env resolution)
- `gh` CLI (for opening subproject PRs)
- SSH keys set up for GitHub (for cloning private repos)

If a "Prerequisites" section does not exist at the top of the quickstart, ADD one with the above list before the first `git clone` step.

**Edit 5 — Add a note about `@minion-stack/shared`, `db`, `auth` being published.**

Somewhere in the package listing or architecture blurb, ensure all seven `@minion-stack/*` packages are at least mentioned by name. A bullet list is fine.

**Do NOT:**
- Rewrite the entire README — preserve existing quickstart flow
- Add timing estimates (Plan 08-05 captures actual timings)
- Add sections about future phases / v2 requirements
- Claim timing is under 10 minutes yet (that is verified by 08-05)
  </action>
  <verify>
    <automated>cd /home/nikolas/Documents/CODE/AI && grep -q 'CI & Releases\|CI & Release' README.md && grep -q 'changesets/action\|Version Packages' README.md && grep -q 'minion doctor' README.md && grep -q 'NPM_TOKEN-SETUP' README.md</automated>
  </verify>
  <done>
    - `grep -c 'CI & Release' README.md` returns at least 1
    - `grep -c 'changesets/action\|Version Packages' README.md` returns at least 1
    - `grep -c 'NPM_TOKEN-SETUP.md' README.md` returns at least 1
    - `grep -c 'minion doctor' README.md` returns at least 1
    - `grep -c '@minion-stack/shared' README.md` returns at least 1
    - Prerequisites section lists Node 22, pnpm 10, infisical, gh (confirmed via grep: `grep -c 'Node\|pnpm\|infisical\|gh ' README.md` returns at least 4)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — docs-only plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | Docs referencing secret locations | mitigate | NPM_TOKEN-SETUP.md linked by path, not by value; no token content in docs |
| T-08-04-02 | Repudiation | Docs drift from code | mitigate | Grep-verifiable stale-string checks in `<done>` enforce concrete cleanup |
</threat_model>

<verification>
After both tasks, a fresh read of `CLAUDE.md` top-to-bottom should describe the CURRENT meta-repo: 8 project directories (no `minion-shared/`), 7 shared packages, CI + release workflows, Infisical 6-layer env, and per-subproject orchestration. Zero references to "future phases." Zero implications that `minion-shared/` is a live directory.

`README.md` should describe how to go from clone to `minion dev` with accurate Prerequisites, reference the CI + release workflows, and link to the NPM_TOKEN setup doc.
</verification>

<success_criteria>
- All nine `CLAUDE.md` grep checks in Task 1 `<done>` pass
- All six `README.md` grep checks in Task 2 `<done>` pass
- `CLAUDE.md` keeps every subproject detail section (minion, hub, site, paperclip, pixel-agents, docs, ai-studio) per D-04
- No regression of Key Conventions, Environment, or Orchestration Guide content
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-automation/08-04-SUMMARY.md`.
</output>
