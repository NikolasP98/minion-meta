---
phase: 08-polish-automation
plan: 05
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - .planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md
autonomous: false
requirements:
  - POLISH-05

must_haves:
  truths:
    - "A maintainer performs a timed dry-run of README.md onboarding from a scratch shell"
    - "Each step's wall-clock time is recorded (clone, install CLI, configure Infisical, minion dev)"
    - "Total time from scratch-shell to successful minion dev <id> is under 10 minutes"
    - "If total exceeds 10 min, README.md is patched and the dry-run re-run"
    - "The timing evidence lives in ONBOARDING-DRY-RUN.md as phase UAT artifact"
  artifacts:
    - path: ".planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md"
      provides: "UAT evidence for POLISH-05 <10-min criterion"
      contains: "Total"
  key_links:
    - from: "ONBOARDING-DRY-RUN.md"
      to: "README.md"
      via: "verified step-by-step walkthrough"
      pattern: "README.md"
---

<objective>
POLISH-05 is a UAT requirement: prove the README.md onboarding takes under 10 minutes for a "fresh developer." Per D-05 (locked), the dry-run happens on the maintainer's machine with a scratch shell (`bash --noprofile --norc`, empty `~/.config/minion/`). This is not a fully-fresh VM; the Prerequisites section of README.md assumes Node 22, pnpm 10, infisical CLI, gh CLI, and SSH keys are already installed.

The plan has one task: a human-verify checkpoint where the user (maintainer) performs the timed dry-run, records results in `ONBOARDING-DRY-RUN.md`, and iterates on README.md if needed. Claude drafts the artifact template and orchestrates the checkpoint but cannot automate the timing itself.

Purpose: Confirm the onboarding promise in README.md is real.

Output: `ONBOARDING-DRY-RUN.md` with timed steps and a pass/fail verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-automation/08-RESEARCH.md

# The target artifact being validated
@README.md

# Updated CLAUDE.md (for reference during dry-run)
@CLAUDE.md

# Setup doc from plan 08-02
@.planning/phases/08-polish-automation/NPM_TOKEN-SETUP.md
</context>

<interfaces>
<!-- Scratch shell recipe -->

```bash
# Recommended scratch-shell launcher for the dry-run:
env -i HOME=/tmp/minion-dry-run-home PATH=/usr/bin:/bin bash --noprofile --norc

# Pre-conditions (per Prerequisites in README):
#   - Node 22, pnpm 10, infisical CLI, gh CLI on PATH
#   - SSH keys to NikolasP98 on the agent's keyring
#   - npm login NOT required for read-only `npm install`
```

<!-- Target success: under 10 minutes end-to-end -->

<!-- Failure loop: if > 10 min, patch README.md and re-run. Up to 2 iterations expected. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Draft ONBOARDING-DRY-RUN.md template</name>
  <files>.planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md</files>
  <read_first>
    - /home/nikolas/Documents/CODE/AI/README.md
    - /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/08-RESEARCH.md (Pitfall #8, POLISH-05 test map row)
  </read_first>
  <action>
Create `.planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md` with this content (the human will fill in the timing columns during the checkpoint task that follows):

```markdown
# Onboarding Dry-Run — POLISH-05 UAT Evidence

**Target:** From a scratch shell, clone and launch `minion dev <id>` for any subproject in under **10 minutes**.

**Date:** {TBD — filled during dry-run}
**Host:** {maintainer machine hostname — e.g. `arch-laptop`}
**Shell:** `env -i HOME=/tmp/minion-dry-run-home PATH=/usr/bin:/bin bash --noprofile --norc`

## Assumed pre-installed

Per README.md Prerequisites:
- [ ] Node.js >= 22 (`node -v`)
- [ ] pnpm >= 10 (`pnpm -v`)
- [ ] infisical CLI (`infisical --version`)
- [ ] gh CLI (`gh --version`)
- [ ] SSH keys registered with GitHub (`ssh -T git@github.com` returns success)

If any prerequisite is missing, STOP and document the gap in README.md Prerequisites before timing.

## Timed steps

Start the stopwatch at step 1. Record wall-clock time for each step. Stop at the moment `minion dev <id>` is observed serving traffic (or equivalent success signal per subproject).

| # | Step | Expected output | Time (mm:ss) |
|---|------|-----------------|--------------|
| 1 | `cd /tmp && rm -rf minion-meta && git clone git@github.com:NikolasP98/minion-meta.git && cd minion-meta` | Cloned repo | |
| 2 | `npm install -g @minion-stack/cli` (or `pnpm add -g`) | `minion` bin on PATH | |
| 3 | `minion --version` | Prints CLI version | |
| 4 | Configure Infisical auth: either export `INFISICAL_UNIVERSAL_AUTH_CLIENT_ID` + `_CLIENT_SECRET` or write `~/.config/minion/infisical-auth.json` | `minion doctor` reports `infisical-cli-ok` + no INFISICAL auth warnings | |
| 5 | `minion doctor` | Table prints, all subprojects show `(not cloned — skip)` in warnings | |
| 6 | Choose one subproject to clone (e.g., `minion_site`): `git clone git@github.com:NikolasP98/minion-site.git minion_site` | Clone succeeds | |
| 7 | Install subproject deps per its package manager (`cd minion_site && bun install && cd ..`) | Install completes | |
| 8 | `minion dev site` | Subproject dev server running | |
| **Total** | — | — | **{fill in}** |

## Pass/fail criteria

- [ ] Total time < 10:00 → **PASS**
- [ ] Each step's `Expected output` observed
- [ ] No undocumented manual steps required
- [ ] No Claude intervention needed (the maintainer performs all steps themselves)

## Observed issues

{TBD — maintainer fills this in during dry-run. Examples of what to capture:}
- Was any README instruction ambiguous?
- Did any tool print unexpected prompts (npm login, gh auth)?
- Did the Infisical auth flow require dashboard clicks that weren't documented?
- Were any SSH permissions wrong (did git clone fail the first time)?

## README.md patches applied

{TBD — if issues were found, list the README.md diff here and re-run the dry-run. If re-run was needed, note the iteration count. Up to 2 iterations expected.}

## Final verdict

{TBD — PASS / FAIL with total time}
```

This file is the UAT artifact. It gets filled in during Task 2 (human checkpoint).
  </action>
  <verify>
    <automated>test -f /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md && grep -c 'Total' /home/nikolas/Documents/CODE/AI/.planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md</automated>
  </verify>
  <done>
    - `ONBOARDING-DRY-RUN.md` exists at the expected path
    - Template contains: `Timed steps` heading, `Pass/fail criteria` heading, 8 numbered step rows with empty `Time (mm:ss)` cells, `Total` row, `Final verdict` heading
    - Scratch-shell command line (`env -i HOME=... bash --noprofile --norc`) is present verbatim
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Timed onboarding dry-run (human-performed)</name>
  <what-built>
The preceding four plans produced:
- `.github/workflows/ci.yml` + `.github/workflows/release.yml` (plans 08-01, 08-02)
- Extended `minion doctor` with link-drift for shared/db/auth + git-status column + clone-presence handling (plan 08-03)
- Updated `CLAUDE.md` + `README.md` (plan 08-04)
- `ONBOARDING-DRY-RUN.md` template (plan 08-05 task 1)

The onboarding story is now documented in README.md. POLISH-05 requires a literal timed dry-run to verify the <10-minute criterion.
  </what-built>
  <how-to-verify>
**Before you start:**
1. Open `.planning/phases/08-polish-automation/ONBOARDING-DRY-RUN.md` in one pane
2. Open `README.md` in another pane (treat this as the instruction set — do NOT look anywhere else)
3. Verify prerequisites: `node -v` (>=22), `pnpm -v` (>=10), `infisical --version`, `gh --version`, `ssh -T git@github.com`
4. Prepare a stopwatch (phone timer or `time` command at each step)

**Run the dry-run:**
1. Launch the scratch shell:
   ```
   env -i HOME=/tmp/minion-dry-run-home PATH=/usr/bin:/bin bash --noprofile --norc
   ```
2. **Start the stopwatch.**
3. Follow each step in README.md exactly as written (do not use knowledge not present in the README). If README is missing a step, write it down — that is a documentation bug.
4. Record wall-clock time for each step in `ONBOARDING-DRY-RUN.md`.
5. Stop the stopwatch when you see `minion dev <id>` serving traffic (or equivalent dev-server-ready signal).

**After:**
1. Total time: sum up all steps, write into `Total` row.
2. Record any README.md ambiguities or undocumented steps in the `Observed issues` section.
3. If total > 10:00 or any step required undocumented intervention → patch README.md, document the patch in `README.md patches applied`, and RE-RUN. Expect up to 2 iterations.
4. When total < 10:00 and all steps are documented, write `PASS` + final time in `Final verdict` and sign off.

**Exit criteria:**
- `ONBOARDING-DRY-RUN.md` has all 8 step times filled, a `Total` under 10:00, and `Final verdict: PASS`.
- If FAIL after 2 iterations, document the shortfall with recommendations for a future POLISH-05 follow-up and report back — do not gate phase completion on perfection, but do be honest about the gap.
  </how-to-verify>
  <resume-signal>Type "approved" once `ONBOARDING-DRY-RUN.md` has total time filled and a PASS verdict (or a FAIL verdict with documented next-steps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — UAT plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | Dry-run logs may capture SSH key paths | accept | Single-maintainer test; dry-run log stays in `.planning/phases/08-polish-automation/` and is committed — no secrets written, only step timings |
| T-08-05-02 | Repudiation | Pre-warmed maintainer env produces unrealistic timings | mitigate | D-05 locked: "Maintainer machine with scratch shell is sufficient." Scratch shell + explicit Prerequisites list make assumptions transparent |
</threat_model>

<verification>
Phase is complete when:
- `ONBOARDING-DRY-RUN.md` has all step timings, a total, and a verdict
- If PASS: POLISH-05 satisfied
- If FAIL (after documented iterations): POLISH-05 marked partial with follow-up item; phase still closes with explicit known-gap acknowledgement
</verification>

<success_criteria>
- `ONBOARDING-DRY-RUN.md` exists in phase dir
- All 8 step timings filled by the maintainer
- Total time recorded
- Final verdict set (PASS or FAIL with rationale)
- README.md updated if patches were required during the dry-run
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-automation/08-05-SUMMARY.md`.
</output>
