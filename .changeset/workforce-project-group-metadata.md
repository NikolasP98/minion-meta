---
"@minion-stack/workforce-client": minor
---

Expose Paperclip project `portfolioId` and stable `repositoryKey`/`groupKey` metadata so Workforce clients can render repository and concern groups without inferring them from display names or workspace paths.
