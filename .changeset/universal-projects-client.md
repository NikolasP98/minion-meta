---
"@minion-stack/workforce-client": minor
---

Add typed Portfolio and Pipeline APIs (`portfolios`, `pipelines`) plus Issue type extensions (`pipelineId`, execution stage `meta`, `IssueExecutionDecision`) for the Universal Projects Module.
