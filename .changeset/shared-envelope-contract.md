---
"@minion-stack/shared": minor
---

Gateway client envelope contract (14-01): frozen protocol 3 constants, frame parsing and defect classification, typed `GatewayError` (source/code/details/retryable), connect-challenge and hello-ok validation, `hello.policy.maxPayload` enforcement before send, pending-slot release on send failure, valid-parent-only traceparent.
