---
'@minion-stack/env': minor
---

Export `cacheStatus()` so operators can inspect the active cache mode, legacy-cache removal,
directory security, and quarantined cache objects without fetching or exposing secret values.
